---
phase: 42-data-convergence
plan: 03
type: execute
wave: 2
depends_on: [42-01, 42-02]
files_modified:
  - plugins/platform_metrics/tuning_loop.py
  - plugins/platform_metrics/library_writer.py
  - plugins/platform_metrics/queue.py
  - plugins/platform_metrics/tests/test_tuning_loop.py
  - plugins/platform_metrics/tests/test_library_writer.py
  - plugins/platform_metrics/tests/test_queue.py
autonomous: true
requirements: [DATA-03]
tags: [tuning-loop, jsonl-queue, formula-writeback, evol02-pattern, tdd]

must_haves:
  truths:
    - "formula_tuning_loop scans FeedbackRecordExtension records, classifies convergent metrics via MetricTrigger rules, and emits TuningSuggestion records to a JSONL review queue"
    - "Suggestions follow the v6.0 EVOL-02 queue pattern: queue.jsonl (pending) + applied.jsonl + rejected.jsonl; operator approves via a move_suggestion() helper that rewrites queue atomically"
    - "library_writer.apply_suggestion() reads the suggestion, locates the target formula in plugins/formula_library/library/*.json, updates the eval_score field atomically (temp + os.replace), and never overwrites non-eval_score fields"
    - "Tuning loop reads from v6.0 FeedbackStore via PUBLIC API ONLY (query / get_record) — no imports of agent.feedback_store.FeedbackStore write methods (scope discipline)"
    - "Trigger thresholds are parameterized (high_hook_dropoff=0.20, high_completion_low_engagement completion=0.70 + engagement=0.05, low_completion=0.30, low_save_rate=0.01) with overridable defaults"
    - "JSONL files live under <HERMES_HOME>/skills/.feedback/tuning/ (sibling to v6.0 evolution/ dir — does not collide)"
  artifacts:
    - path: "plugins/platform_metrics/queue.py"
      provides: "JSONL queue helpers (mirror agent/evolution/queue.py): append_suggestion / move_suggestion / read_queue + atomic rewrite"
      exports: ["append_suggestion", "move_suggestion", "read_queue", "QUEUE_FILENAME", "APPLIED_FILENAME", "REJECTED_FILENAME"]
    - path: "plugins/platform_metrics/tuning_loop.py"
      provides: "run_tuning_loop() — scans extensions, classifies by MetricTrigger, emits suggestions to queue"
      exports: ["run_tuning_loop", "classify_metrics", "TuningThresholds"]
    - path: "plugins/platform_metrics/library_writer.py"
      provides: "apply_suggestion() — atomic write-back of eval_score to formula_library/library/*.json"
      exports: ["apply_suggestion", "load_formula_file", "write_formula_file"]
    - path: "plugins/platform_metrics/tests/test_tuning_loop.py"
      provides: "Classify + run_tuning_loop tests with synthetic FeedbackRecordExtension fixtures"
      min_lines: 150
    - path: "plugins/platform_metrics/tests/test_library_writer.py"
      provides: "Apply_suggestion atomicity + eval_score-only-write tests"
      min_lines: 80
    - path: "plugins/platform_metrics/tests/test_queue.py"
      provides: "JSONL queue append/move/read tests mirroring agent/evolution/queue.py test patterns"
      min_lines: 100
  key_links:
    - from: "plugins/platform_metrics/tuning_loop.py (run_tuning_loop)"
      to: "plugins/platform_metrics/queue.py (append_suggestion)"
      via: "emits TuningSuggestion via append_suggestion()"
      pattern: "append_suggestion"
    - from: "plugins/platform_metrics/tuning_loop.py (classify_metrics)"
      to: "plugins/platform_metrics/schema.py (MetricTrigger)"
      via: "returns list[MetricTrigger] per PlatformMetrics"
      pattern: "MetricTrigger"
    - from: "plugins/platform_metrics/library_writer.py (apply_suggestion)"
      to: "plugins/formula_library/library/*.json (Formula.eval_score)"
      via: "atomic JSON rewrite — temp + os.replace, only eval_score field touched"
      pattern: "eval_score"
    - from: "plugins/platform_metrics/library_writer.py (apply_suggestion)"
      to: "plugins/platform_metrics/queue.py (move_suggestion)"
      via: "after successful write-back, move suggestion pending → applied"
      pattern: "move_suggestion"
    - from: "plugins/platform_metrics/tuning_loop.py (v6.0 consumption)"
      to: "agent/feedback_store.py (FeedbackStore.query / get_record)"
      via: "PUBLIC API ONLY — type-hint as Protocol, no concrete class import"
      pattern: "from agent.feedback_store import FeedbackStore"
---

<objective>
Implement the formula tuning loop (DATA-03): scan platform metrics → classify via MetricTrigger rules → emit TuningSuggestion records to a JSONL review queue (mirroring v6.0 EVOL-02) → operator approves → library_writer atomically writes eval_score back to `plugins/formula_library/library/*.json`.

Purpose: Close the feedback loop. Phase 38 ships variants[]; Phase 39 ships formula_library; Phase 42-01/42-02 ship the metrics schema + adapter stubs. This plan (42-03) ties them together: metrics drive suggestions, suggestions drive formula improvements, improvements feed back into Step 0 of the next episode's pipeline. The human-in-loop checkpoint (operator approval) is structural — nothing auto-applies (per the v6.0 EVOL-02 non-bypassable HIL invariant).

Output: 3 Python files + 3 test files:
- `plugins/platform_metrics/queue.py` — JSONL queue helpers (mirror `agent/evolution/queue.py` shape: queue/applied/rejected)
- `plugins/platform_metrics/tuning_loop.py` — `run_tuning_loop()` + `classify_metrics()` + `TuningThresholds` dataclass
- `plugins/platform_metrics/library_writer.py` — `apply_suggestion()` + atomic formula file rewrite

**Scope discipline (load-bearing):** `tuning_loop.py` imports `FeedbackStore` from `agent.feedback_store` ONLY to call `query()` / `get_record()` (read methods). It NEVER calls `record_feedback()` or any write method. A grep-enforced invariant: `grep -E "record_feedback\|rebuild_index" plugins/platform_metrics/tuning_loop.py` returns 0 hits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-data-convergence/PLAN.md
@.planning/phases/42-data-convergence/42-01-PLAN.md
@.planning/phases/42-data-convergence/42-02-PLAN.md

# Plan 42-01 ships these schemas — required
@plugins/platform_metrics/schema.py

# v6.0 EVOL-02 JSONL queue — mirror its shape exactly
@agent/evolution/queue.py

# v6.0 FeedbackStore — consume-only via public API
@agent/feedback_store.py

# Formula model + library layout — write-back target
@plugins/formula_library/schema.py
@plugins/formula_library/library/formula_urban_fantasy_light_01.json

# variants[] schema (variant_id resolution)
@skills/kais-movie-pipeline/references/asset-bus-schema.md

<interfaces>
<!-- Contracts the executor builds against. No codebase exploration needed. -->

From plugins/platform_metrics/schema.py (Plan 42-01):
```python
class MetricTrigger(enum.Enum):
    HIGH_HOOK_DROPOFF = "high_hook_dropoff"
    HIGH_COMPLETION_LOW_ENGAGEMENT = "high_completion_low_engagement"
    LOW_COMPLETION = "low_completion"
    LOW_SAVE_RATE = "low_save_rate"

class TuningSuggestion(BaseModel):
    suggestion_id: str
    formula_id: str
    trigger: MetricTrigger
    observed_metric: float
    threshold: float
    suggested_action: str
    rationale: str
    evidence: list[str] = []
    status: Literal["pending", "applied", "rejected"] = "pending"
    ts_queued: str
    commit_sha: str | None = None
    ts_applied: str | None = None
    reason: str | None = None
    ts_rejected: str | None = None

class PlatformMetrics(BaseModel):
    platform: Platform
    variant_id: str
    completion_rate: float
    hook_dropoff_rate: float
    engagement_rate: float
    save_rate: float
    comment_rate: float
    fetched_at: datetime

class FeedbackRecordExtension(BaseModel):
    feedback_id: str   # FK to v6.0 record_id
    platform_metrics: dict[Platform, PlatformMetrics] = {}
    ts_created: datetime
```

From agent/feedback_store.py:FeedbackStore (v6.0 — consume-only):
```python
class FeedbackStore:
    def query(self, *, skill_id=None, source=None, verdict=None,
              since=None, until=None, include_superseded=False) -> list[FeedbackRecord]: ...
    def get_record(self, record_id: str) -> FeedbackRecord | None: ...
    def summary(self, *, skill_id=None, source=None) -> dict[str, dict[str, Any]]: ...
```

From agent/evolution/queue.py (v6.0 — mirror this shape):
```python
# JSONL files under <HERMES_HOME>/skills/.feedback/evolution/:
#   queue.jsonl        # pending
#   applied.jsonl      # approved + applied
#   rejected.jsonl     # rejected
def append_patch(record: PatchRecord, evolution_dir: Path) -> None: ...
def move_patch(*, patch_id, target_status, evolution_dir, commit_sha=None, reason=None) -> PatchRecord: ...
def read_queue(*, evolution_dir, status="pending", skill_id=None) -> list[PatchRecord]: ...
def _atomic_rewrite_jsonl(path: Path, records: list[dict]) -> None: ...
```

From plugins/formula_library/schema.py:Formula (Phase 39 — write-back target):
```python
class Formula(BaseModel):
    formula_id: str
    genre: Genre
    mood: Mood
    pacing: str
    hook_pattern: HookPattern
    characters: list[str]
    runtime_sec: int
    platform_fit: list[PlatformFit]
    citation: Citation
    verified_date: date
    eval_score: Optional[float] = None   # ← library_writer updates THIS FIELD ONLY
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queue.py — JSONL queue helpers (mirror agent/evolution/queue.py)</name>
  <files>
    plugins/platform_metrics/queue.py
    plugins/platform_metrics/tests/test_queue.py
  </files>
  <behavior>
    - Test 1 (test_append_suggestion_writes_queue_jsonl): tmp_path = evolution_dir substitute; call append_suggestion(suggestion, tuning_dir=tmp_path); assert tmp_path/"queue.jsonl" exists and contains 1 line that parses as the suggestion dict
    - Test 2 (test_append_rejects_non_pending): suggestion.status = "applied"; append_suggestion raises ValueError (must be pending to enter queue)
    - Test 3 (test_read_queue_pending_returns_only_pending): write 3 suggestions to queue.jsonl (2 pending, 1 applied-in-flight simulation: just write 3 pending); read_queue(status="pending") returns all 3 sorted by ts_queued
    - Test 4 (test_read_queue_applied_reads_applied_file): write 1 suggestion to applied.jsonl directly (simulate post-approval state); read_queue(status="applied") returns 1
    - Test 5 (test_move_suggestion_pending_to_applied): append 1 suggestion; move_suggestion(suggestion_id=..., target_status="applied", tuning_dir=tmp_path, commit_sha="abc123"); assert queue.jsonl is now empty; assert applied.jsonl has 1 record with status="applied" + commit_sha="abc123" + ts_applied set
    - Test 6 (test_move_suggestion_pending_to_rejected): same as Test 5 but target_status="rejected" with reason="test rejection"; assert rejected.jsonl has the record with reason set
    - Test 7 (test_move_suggestion_unknown_id_raises): move_suggestion with bogus patch_id raises KeyError
    - Test 8 (test_move_suggestion_applied_requires_commit_sha): move_suggestion(target_status="applied", commit_sha=None) raises ValueError
    - Test 9 (test_move_suggestion_rejected_requires_reason): move_suggestion(target_status="rejected", reason=None) raises ValueError
    - Test 10 (test_read_queue_malformed_line_skipped): write a corrupt line to queue.jsonl (manually); read_queue does not crash, logs warning, skips bad line
    - Test 11 (test_atomic_rewrite_preserves_other_records): append 3 suggestions; move the middle one to applied; assert the other 2 remain in queue.jsonl (atomic rewrite does not lose siblings)
    - Test 12 (test_strict_mode_on_applied): write a corrupt line to applied.jsonl; read_queue(status="applied") raises ValueError (audit-critical strict mode, mirrors agent/evolution/queue.py WR-03)
  </behavior>
  <action>
    Create `plugins/platform_metrics/queue.py` mirroring the structure of `agent/evolution/queue.py` but with `TuningSuggestion` instead of `PatchRecord`. Module docstring: "Phase 42 DATA — JSONL queue for formula tuning suggestions. Mirrors the v6.0 EVOL-02 queue pattern (agent/evolution/queue.py): queue.jsonl (pending) + applied.jsonl + rejected.jsonl under <HERMES_HOME>/skills/.feedback/tuning/. Single-process assumption (no fcntl locking — same as v6.0). Atomic rewrite for move_suggestion (temp + os.replace, mirroring agent/evolution/queue.py:_atomic_rewrite_jsonl)."

    Module-level constants (mirror agent/evolution/queue.py:60-64):
    - `QUEUE_FILENAME = "queue.jsonl"`
    - `APPLIED_FILENAME = "applied.jsonl"`
    - `REJECTED_FILENAME = "rejected.jsonl"`
    - `_STATUS_TO_FILENAME: dict[str, str] = {"pending": QUEUE_FILENAME, "applied": APPLIED_FILENAME, "rejected": REJECTED_FILENAME}`

    Functions (mirror agent/evolution/queue.py:202 onward, with `PatchRecord` → `TuningSuggestion`):
    - `_append_jsonl(path: Path, record: dict[str, Any]) -> None` — same as v6.0
    - `_read_jsonl(path: Path, *, strict: bool = False) -> list[dict[str, Any]]` — same as v6.0 (strict=True raises on malformed for audit-critical files)
    - `_atomic_rewrite_jsonl(path: Path, records: list[dict[str, Any]]) -> None` — copy verbatim from v6.0 (tempfile.mkstemp + os.replace + try/except cleanup)
    - `append_suggestion(record: TuningSuggestion, tuning_dir: Path) -> None` — rejects status != "pending" (ValueError), appends to queue.jsonl
    - `move_suggestion(*, suggestion_id: str, target_status: str, tuning_dir: Path, commit_sha: str | None = None, reason: str | None = None) -> TuningSuggestion` — same shape as v6.0 move_patch. Validates target_status in {"applied", "rejected"}; requires commit_sha for applied, reason for rejected. WR-02 ordering: remove from queue FIRST, then append to destination.
    - `read_queue(*, tuning_dir: Path, status: str = "pending", formula_id: str | None = None) -> list[TuningSuggestion]` — same shape as v6.0 read_queue. strict=True for applied/rejected (WR-03).

    Module-level `__all__` exports the 3 public functions + 3 filename constants.

    Test file `tests/test_queue.py` — 12 tests per behavior block. Use `tmp_path` fixture for tuning_dir. Use `datetime.now(timezone.utc).isoformat()` for ts_queued. Use `TuningSuggestion(trigger=MetricTrigger.HIGH_HOOK_DROPOFF, formula_id="urban-fantasy-light-01", observed_metric=0.32, threshold=0.20, suggested_action="加 hook 强度", rationale="hook_dropoff exceeds threshold", suggestion_id=f"{formula_id}_{trigger.value}_{ts_unix}", ts_queued=ts)` for fixture suggestions. For Test 10 (malformed line), manually append `"{ this is not json\n"` to queue.jsonl before read.

    Per CLAUDE.md: `from __future__ import annotations`. `import json, logging, os, tempfile`. `from datetime import datetime, timezone`. `from pathlib import Path`. `from typing import Any`. `from pydantic import ValidationError`. `from plugins.platform_metrics.schema import TuningSuggestion`. Double-quoted strings. `encoding="utf-8"` on every open(). `logger = logging.getLogger(__name__)`. Lazy %-logging.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/platform_metrics/tests/test_queue.py -v</automated>
  </verify>
  <done>
    - queue.py mirrors v6.0 EVOL-02 shape (queue/applied/rejected JSONL)
    - append rejects non-pending status
    - move_suggestion enforces commit_sha/reason requirements
    - read_queue strict mode raises on malformed applied/rejected
    - Atomic rewrite preserves sibling records on move
    - All 12 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tuning_loop.py — classify_metrics + run_tuning_loop + TuningThresholds</name>
  <files>
    plugins/platform_metrics/tuning_loop.py
    plugins/platform_metrics/tests/test_tuning_loop.py
  </files>
  <behavior>
    - Test 1 (test_default_thresholds): TuningThresholds() has high_hook_dropoff=0.20, high_completion=0.70, low_engagement=0.05, low_completion=0.30, low_save_rate=0.01
    - Test 2 (test_classify_high_hook_dropoff): metrics with hook_dropoff_rate=0.32 (>0.20) → classify returns [MetricTrigger.HIGH_HOOK_DROPOFF]; other metrics in normal range
    - Test 3 (test_classify_high_completion_low_engagement): metrics with completion_rate=0.85 (>=0.70) AND engagement_rate=0.02 (<0.05) → classify returns [HIGH_COMPLETION_LOW_ENGAGEMENT]
    - Test 4 (test_classify_high_completion_high_engagement_no_trigger): metrics with completion_rate=0.85 AND engagement_rate=0.10 → HIGH_COMPLETION_LOW_ENGAGEMENT does NOT fire (engagement above threshold)
    - Test 5 (test_classify_low_completion): metrics with completion_rate=0.20 (<0.30) → LOW_COMPLETION
    - Test 6 (test_classify_low_save_rate): metrics with save_rate=0.005 (<0.01) → LOW_SAVE_RATE
    - Test 7 (test_classify_multiple_triggers): metrics that hit both HIGH_HOOK_DROPOFF and LOW_COMPLETION → returns both in the list
    - Test 8 (test_classify_no_triggers): all metrics in normal range → empty list
    - Test 9 (test_classify_threshold_override): custom TuningThresholds(high_hook_dropoff=0.50); metrics at 0.32 → no trigger (now below custom threshold)
    - Test 10 (test_run_tuning_loop_emits_suggestions): mock FeedbackStore.query returns 2 records; mock FeedbackRecordExtension store returns 2 extensions with variant_id resolving to a formula_id (use a synthetic variant_id→formula_id mapping fixture); call run_tuning_loop(feedback_store=mock_store, extension_store=mock_extension_store, tuning_dir=tmp_path, formula_resolver=mock_resolver); assert queue.jsonl has N suggestions matching the triggers fired
    - Test 11 (test_run_tuning_loop_skips_extensions_without_metrics): extension with empty platform_metrics dict produces no suggestions
    - Test 12 (test_run_tuning_loop_dedup_suggestion_ids): same metrics + formula_id + trigger within same run produces ONE suggestion (idempotent within a run — same suggestion_id)
    - Test 13 (test_run_tuning_loop_no_v6_write): grep-enforced — assert no call to `feedback_store.record_feedback` anywhere in run_tuning_loop's body (grep -c "record_feedback" returns 0 in tuning_loop.py)
  </behavior>
  <action>
    Create `plugins/platform_metrics/tuning_loop.py` with `from __future__ import annotations` at top. Module docstring: "Phase 42 DATA — formula tuning loop. Scans FeedbackRecordExtension records, classifies convergent metrics via MetricTrigger rules, emits TuningSuggestion records to a JSONL review queue (DATA-03). Mirrors v6.0 EVOL-02 HIL invariant: NOTHING auto-applies. Operator approves via Plan 42-03 library_writer.apply_suggestion(). Scope discipline: reads v6.0 FeedbackStore via PUBLIC API only (query, get_record); NEVER calls write methods (grep-enforced)."

    Define:

    1. **`@dataclass(frozen=True) class TuningThresholds:`** — keyword-only fields with defaults:
       - `high_hook_dropoff: float = 0.20`
       - `high_completion: float = 0.70`
       - `low_engagement: float = 0.05`
       - `low_completion: float = 0.30`
       - `low_save_rate: float = 0.01`
       Use `from dataclasses import dataclass` + `from typing import Any` (not needed but defensive).

    2. **`def classify_metrics(metrics: PlatformMetrics, thresholds: TuningThresholds = TuningThresholds()) -> list[MetricTrigger]:`** — pure function. Applies the 4 trigger rules:
       - if `metrics.hook_dropoff_rate > thresholds.high_hook_dropoff`: append `HIGH_HOOK_DROPOFF` (observed=hook_dropoff_rate, threshold=thresholds.high_hook_dropoff)
       - if `metrics.completion_rate >= thresholds.high_completion AND metrics.engagement_rate < thresholds.low_engagement`: append `HIGH_COMPLETION_LOW_ENGAGEMENT`
       - if `metrics.completion_rate < thresholds.low_completion`: append `LOW_COMPLETION`
       - if `metrics.save_rate < thresholds.low_save_rate`: append `LOW_SAVE_RATE`
       Returns the list (may be empty). Comment each rule in 中文+EN explaining the suggestion direction (HIGH_HOOK_DROPOFF → "加 hook 强度"; HIGH_COMPLETION_LOW_ENGAGEMENT → "加 CTA"; LOW_COMPLETION → "前置冲突"; LOW_SAVE_RATE → "加 collectible 内容钩子").

    3. **`FormulaResolver = Callable[[str], str | None]`** — type alias for the variant_id → formula_id resolver function. Plan 42-04 wires the real resolver (reads pipeline_state.episode_id.variants[] to look up the formula_id used for that variant). For Plan 42-03 tests, a lambda or dict-based stub.

    4. **`ExtensionStore = Protocol`** — minimal protocol defining the read API for FeedbackRecordExtension records (so tests can stub it without a concrete class). Methods: `def list_extensions(self) -> list[FeedbackRecordExtension]: ...` (synchronous list — keep simple; async would complicate tests).

    5. **`def run_tuning_loop(*, feedback_store: FeedbackStore, extension_store: ExtensionStore, tuning_dir: Path, formula_resolver: FormulaResolver, thresholds: TuningThresholds = TuningThresholds(), now: datetime | None = None) -> list[TuningSuggestion]:`** — keyword-only args (CLAUDE.md convention):
       - Iterate `extension_store.list_extensions()`.
       - For each extension with non-empty `platform_metrics`, iterate the per-platform PlatformMetrics.
       - Resolve `formula_id = formula_resolver(metrics.variant_id)`. Skip if None (no formula mapping; log debug).
       - `triggers = classify_metrics(metrics, thresholds)`.
       - For each trigger, build a `TuningSuggestion` with:
         - `suggestion_id = f"{formula_id}_{trigger.value}_{int(now.timestamp())}"` (idempotent within a run via timestamp bucket)
         - `formula_id`
         - `trigger`
         - `observed_metric` (the metric value that crossed: hook_dropoff_rate for HIGH_HOOK_DROPOFF, etc.)
         - `threshold` (the threshold value)
         - `suggested_action` (rule-specific string — see classify_metrics comments)
         - `rationale` (rule-specific — see comments)
         - `evidence` = [extension.feedback_id, metrics.variant_id]
         - `status = "pending"`
         - `ts_queued = now.isoformat()` (UTC)
       - Dedup by `suggestion_id` within the run (set of seen ids).
       - Append each via `append_suggestion(suggestion, tuning_dir)`.
       - Return the list of emitted suggestions.
       - **DO NOT call `feedback_store.record_feedback` or any write method.** Only `feedback_store.query()` may be called to filter by date if needed (kept as parameter for future use; current implementation does not need it since extension_store provides the iteration source).

    Test file `tests/test_tuning_loop.py` — 13 tests per behavior block. Use `TuningThresholds()` defaults. For PlatformMetrics fixtures use a helper `_make_metrics(**overrides)` that fills defaults (all 0.5 / 0.10 / 0.05 / 0.02 / 0.01). For run_tuning_loop tests use `unittest.mock.MagicMock` for feedback_store and a stub `ExtensionStore` class implementing `list_extensions()` returning a list of `FeedbackRecordExtension` objects. For Test 13 (no v6 write), use `subprocess.run(["grep", "-c", "record_feedback", "plugins/platform_metrics/tuning_loop.py"], ...)` and assert `.stdout.strip() == "0"` (or use Python `pathlib.Path.read_text` + `assert "record_feedback" not in content`).

    Per CLAUDE.md: `from __future__ import annotations`. `import logging`. `from dataclasses import dataclass, field`. `from datetime import datetime, timezone`. `from pathlib import Path`. `from typing import Callable, Protocol`. `from plugins.platform_metrics.schema import FeedbackRecordExtension, MetricTrigger, PlatformMetrics, TuningSuggestion`. `from plugins.platform_metrics.queue import append_suggestion`. `from agent.feedback_store import FeedbackStore` (READ ONLY — type hint only; no instantiation in module body). Double-quoted strings. `encoding="utf-8"` (none directly). `logger = logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/platform_metrics/tests/test_tuning_loop.py -v</automated>
  </verify>
  <done>
    - TuningThresholds dataclass has 5 defaults
    - classify_metrics applies all 4 trigger rules correctly (including multi-trigger + override)
    - run_tuning_loop emits suggestions to queue.jsonl
    - run_tuning_loop dedups by suggestion_id within a run
    - run_tuning_loop never calls v6 write methods (grep-enforced)
    - All 13 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: library_writer.py — apply_suggestion atomic eval_score write-back</name>
  <files>
    plugins/platform_metrics/library_writer.py
    plugins/platform_metrics/tests/test_library_writer.py
  </files>
  <behavior>
    - Test 1 (test_load_formula_file_roundtrip): copy a real formula_*.json from plugins/formula_library/library/ to tmp_path; load_formula_file(tmp_path/"formula_urban_fantasy_light_01.json") returns a dict with formula_id, eval_score=None originally, all other fields preserved
    - Test 2 (test_write_formula_file_atomic): write_formula_file(tmp_path/file, dict) creates the file via temp+os.replace; verify the temp file is gone after write (no .tmp file remaining in the dir)
    - Test 3 (test_apply_suggestion_updates_eval_score): tmp formula file has eval_score=None; suggestion has formula_id="urban-fantasy-light-01", observed_metric=0.32, trigger=HIGH_HOOK_DROPOFF; apply_suggestion(suggestion, library_dir=tmp_path) returns a commit_sha string; the file now has eval_score=0.32 (the observed_metric; document this mapping rule); all other fields preserved
    - Test 4 (test_apply_suggestion_preserves_other_fields): load original formula dict; apply a suggestion; reload; assert every field EXCEPT eval_score is byte-identical (deep equality on the parsed dict minus eval_score)
    - Test 5 (test_apply_suggestion_unknown_formula_id_raises): suggestion with formula_id="nonexistent"; apply_suggestion raises FileNotFoundError (or KeyError) with a clear message listing available formula files
    - Test 6 (test_apply_suggestion_atomic_temp_cleanup_on_failure): monkeypatch os.replace to raise OSError; apply_suggestion raises; assert no .tmp file remains in library_dir (cleanup in except block)
    - Test 7 (test_apply_suggestion_returns_commit_sha): returned commit_sha is a 40-char hex string (sha256 of the new file content, or git-style; pick one and document — recommend sha256 hex of new content for simplicity)
    - Test 8 (test_apply_suggestion_triggers_move_to_applied): mock or spy on move_suggestion; apply_suggestion calls move_suggestion(suggestion_id=..., target_status="applied", tuning_dir=..., commit_sha=...) after successful write
  </behavior>
  <action>
    Create `plugins/platform_metrics/library_writer.py` with `from __future__ import annotations` at top. Module docstring: "Phase 42 DATA — atomic formula_library write-back. After operator approves a TuningSuggestion (DATA-03), apply_suggestion() locates the target formula file in plugins/formula_library/library/, updates ONLY the eval_score field via temp+os.replace atomic write, then moves the suggestion pending → applied in the JSONL queue. NEVER overwrites any non-eval_score field (write integrity — formulas are fair-use curated content, not freely editable)."

    Define:

    1. **`def load_formula_file(path: Path) -> dict[str, Any]:`** — read JSON, return dict. Use `path.read_text(encoding="utf-8")` + `json.loads`. Raise `FileNotFoundError` naturally if missing.

    2. **`def write_formula_file(path: Path, data: dict[str, Any]) -> None:`** — atomic write: `tempfile.mkstemp(dir=str(path.parent), prefix=".formula.", suffix=".tmp")` → write JSON via `os.fdopen(fd, "w", encoding="utf-8")` + json.dump + `f.flush()` + `os.fsync(f.fileno())` → `os.replace(tmp_path, path)`. On any exception, unlink the temp file (mirror agent/evolution/queue.py:_atomic_rewrite_jsonl try/except cleanup pattern). Use `ensure_ascii=False, indent=2` in json.dump to preserve the file's existing style (formula_urban_fantasy_light_01.json is indent=2 + non-ASCII preserved).

    3. **`def apply_suggestion(*, suggestion: TuningSuggestion, library_dir: Path, tuning_dir: Path) -> str:`** — keyword-only args:
       - Locate target file: glob `library_dir.glob(f"*{suggestion.formula_id}*.json")` — comment that formula_id matches a substring of the filename (the convention from Phase 39 is `formula_<slugified_id>.json`, e.g. `formula_urban_fantasy_light_01.json` for `urban-fantasy-light-01`; the glob pattern `*{formula_id}*.json` should match — but the formula_id has hyphens and the filename has underscores, so the glob must convert hyphens→underscores: `formula_id.replace("-", "_")`). Use `target_filename = f"formula_{suggestion.formula_id.replace('-', '_')}.json"`; `target_path = library_dir / target_filename`. If `not target_path.exists()`, `raise FileNotFoundError(f"formula file for {suggestion.formula_id!r} not found at {target_path}; available: {sorted(p.name for p in library_dir.glob('*.json'))}")`.
       - Load the formula dict via `load_formula_file(target_path)`.
       - Save a deep copy of the original dict for the integrity assertion (Test 4).
       - Update ONLY `data["eval_score"] = suggestion.observed_metric` (document the mapping rule: HIGH_HOOK_DROPOFF/LOW_COMPLETION/etc. all map to observed_metric being written to eval_score — this is a simplification; V9-FUTURE could differentiate by trigger, but v9.0 uses uniform mapping).
       - Compute `commit_sha = hashlib.sha256(json.dumps(data, ensure_ascii=False, sort_keys=True).encode("utf-8")).hexdigest()` — 64 hex chars.
       - Call `write_formula_file(target_path, data)`.
       - Verify integrity: reload the file, assert every key except "eval_score" matches the original (defensive — should always pass given we only touched eval_score).
       - Call `move_suggestion(suggestion_id=suggestion.suggestion_id, target_status="applied", tuning_dir=tuning_dir, commit_sha=commit_sha)`.
       - Return `commit_sha`.

    Module-level `__all__ = ["load_formula_file", "write_formula_file", "apply_suggestion"]`.

    Test file `tests/test_library_writer.py` — 8 tests per behavior block. Use `tmp_path` for library_dir + tuning_dir. For Test 1/3/4/5/7, copy a real formula file from `plugins/formula_library/library/formula_urban_fantasy_light_01.json` to tmp_path/library/ via `shutil.copy`. For Test 6 (atomic cleanup), monkeypatch `os.replace` to raise OSError; wrap `apply_suggestion` call in `pytest.raises(OSError)`; then `assert not list(tmp_path.glob('.formula.*.tmp'))` (no leftover temp files). For Test 8 (move_suggestion spy), use `unittest.mock.patch("plugins.platform_metrics.library_writer.move_suggestion")` as a patcher; assert the mock was called once with the right kwargs.

    Per CLAUDE.md: `from __future__ import annotations`. `import hashlib, json, logging, os, tempfile`. `from pathlib import Path`. `from typing import Any`. `from plugins.platform_metrics.schema import TuningSuggestion`. `from plugins.platform_metrics.queue import move_suggestion`. Double-quoted strings. `encoding="utf-8"` on every open(). `logger = logging.getLogger(__name__)`. Lazy %-logging.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/platform_metrics/tests/test_library_writer.py -v</automated>
  </verify>
  <done>
    - load_formula_file + write_formula_file roundtrip preserves all fields
    - apply_suggestion updates ONLY eval_score (all other fields byte-identical)
    - apply_suggestion raises FileNotFoundError on unknown formula_id with a helpful message
    - apply_suggestion cleans up temp file on os.replace failure
    - apply_suggestion returns a 64-char sha256 hex commit_sha
    - apply_suggestion triggers move_suggestion pending → applied with commit_sha
    - All 8 tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator approval gate | TuningSuggestion moves pending → applied ONLY via operator action (library_writer.apply_suggestion). Non-bypassable HIL (mirrors v6.0 EVOL-02). |
| Formula file write | library_writer touches ONLY eval_score. Other fields are fair-use curated content — must not be modified. |
| v6.0 FeedbackStore → tuning_loop | Public-API consume only (query/get_record). No write-back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Tampering | Formula file non-eval_score fields | mitigate | library_writer loads dict, modifies only `data["eval_score"]`, writes back; post-write integrity assertion reloads + compares all-other-keys (Test 4 + runtime check). |
| T-42-10 | Repudiation | JSONL audit trail (queue/applied/rejected) | mitigate | Strict-mode read on applied/rejected (WR-03 mirror); move_suggestion WR-02 ordering (remove-then-append). |
| T-42-11 | Elevation of Privilege | Auto-apply without operator | mitigate | apply_suggestion is the ONLY entry point to move pending→applied + write formula; no auto-invocation path. HIL invariant. |
| T-42-12 | Information Disclosure | FeedbackStore query returns full records | accept | tuning_loop uses query() with appropriate filters; records stay in-process. No logging of feedback content. |
| T-42-13 | Tampering | Atomic write failure leaves corrupt formula file | mitigate | temp+os.replace pattern (atomic on POSIX); cleanup in except block (Test 6). |
| T-42-SC | Tampering | (no package installs) | accept | N/A — pure stdlib. |
</threat_model>

<verification>
- All 3 test files pass: `python -m pytest plugins/platform_metrics/tests/test_queue.py plugins/platform_metrics/tests/test_tuning_loop.py plugins/platform_metrics/tests/test_library_writer.py -v`
- `grep -c "record_feedback" plugins/platform_metrics/tuning_loop.py` returns 0 (scope discipline: no v6 write)
- `grep -cE "data\[\"[^\"]+\"\] *=" plugins/platform_metrics/library_writer.py | head` shows ONLY `data["eval_score"]` mutation (no other field writes)
- JSONL queue files under <HERMES_HOME>/skills/.feedback/tuning/ do NOT collide with v6.0 evolution/ (different dir name)
</verification>

<success_criteria>
- formula_tuning_loop converts convergent metrics into TuningSuggestion records (DATA-03 first half)
- Suggestions land in JSONL review queue (queue.jsonl pending; applied/rejected for lifecycle)
- Operator approval via library_writer.apply_suggestion writes eval_score back to formula_library (DATA-03 second half)
- TuningThresholds parameterized (4 trigger rules + override capability)
- Non-bypassable HIL: no auto-apply path
- v6.0 FeedbackStore consumed via public API only (grep-enforced)
- Atomic formula write preserves all non-eval_score fields
</success_criteria>

<output>
Create `.planning/phases/42-data-convergence/42-03-SUMMARY.md` when done.
</output>

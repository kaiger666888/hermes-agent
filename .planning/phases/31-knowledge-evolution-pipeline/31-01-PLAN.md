---
phase: 31-knowledge-evolution-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/evolution/__init__.py
  - agent/evolution/insights.py
  - agent/evolution/diff_generator.py
  - agent/evolution/queue.py
  - agent/evolution/apply.py
  - tests/agent/evolution/__init__.py
  - tests/agent/evolution/conftest.py
  - tests/agent/evolution/test_insights.py
  - tests/agent/evolution/test_diff_generator.py
  - tests/agent/evolution/test_queue.py
  - tests/agent/evolution/test_apply.py
  - tests/fixtures/evolution/sample_skill_md.md
  - tests/fixtures/evolution/sample_insights.json
  - tests/fixtures/evolution/additive.diff
  - tests/fixtures/evolution/violating_deletion.diff
  - tests/fixtures/evolution/violating_frontmatter_diffusion.diff
autonomous: true
requirements: [EVOL-01, EVOL-03, EVOL-04, EVOL-05]
user_setup: []

must_haves:
  truths:
    - "Calling aggregate_feedback(skill_id, client) on a populated FeedbackStore returns a list[InsightRecord], each with a non-empty evidence_chain of feedback IDs"
    - "Malformed LLM JSON output (trailing comma, markdown fences, partial) raises AggregationError — never an empty list"
    - "generate_additive_diff(current, addition, marker, path) returns a git-compatible unified diff whose hunks are purely additive (no `-` content lines)"
    - "Patches can be appended to queue.jsonl, moved pending→applied or pending→rejected, and read back with status filtering"
    - "apply_patch_transaction(patch_path, repo_root, msg) runs git apply --check → git apply → FOUND-08 byte-intact check → additive-only check → git add → git commit atomically"
    - "On ANY failure inside apply_patch_transaction, the working tree is reverted (git checkout -- + git clean -f scoped) and ApplyError is raised"
    - "verify_additive_only rejects any diff containing a `-` content line or a hunk header where D < B; accepts pure additions"
    - "verify_found08_byte_intact detects byte-level frontmatter drift (not just value drift) per Pitfall 4"
    - "Evolution modules are not imported by any Hermes runtime module (run_agent.py, agent/conversation_loop.py, agent/curator.py, cli.py, gateway/)"
  artifacts:
    - path: "agent/evolution/__init__.py"
      provides: "Public API re-exports for evolution subpackage"
      contains: "from agent.evolution.insights import"
    - path: "agent/evolution/insights.py"
      provides: "EVOL-01 LLM aggregation: make_aggregation_client, aggregate_feedback, AggregationError, InsightRecord, AGGREGATION_SYSTEM_PROMPT, build_aggregation_user_prompt"
      exports: ["aggregate_feedback", "make_aggregation_client", "InsightRecord", "AggregationError"]
    - path: "agent/evolution/diff_generator.py"
      provides: "EVOL-02 placeholder: generate_additive_diff via stdlib difflib; marker-not-found raises ValueError"
      exports: ["generate_additive_diff"]
    - path: "agent/evolution/queue.py"
      provides: "EVOL-03 JSONL queue: PatchRecord, append_patch, move_patch, read_queue, FAILED_GATE_PATH"
      exports: ["PatchRecord", "append_patch", "move_patch", "read_queue"]
    - path: "agent/evolution/apply.py"
      provides: "EVOL-05 atomic transaction: apply_patch_transaction, verify_additive_only, verify_found08_byte_intact, revert_files, ApplyError, ApplyResult, build_commit_message"
      exports: ["apply_patch_transaction", "verify_additive_only", "verify_found08_byte_intact", "ApplyError", "ApplyResult"]
    - path: "tests/agent/evolution/conftest.py"
      provides: "Fixtures: evolution_env (HERMES_HOME + git repo), mock_llm_client, sample_feedback_records, sample_skill_content"
  key_links:
    - from: "agent/evolution/insights.py"
      to: "agent/feedback_store.py:FeedbackStore.query"
      via: "FeedbackStore dependency injection in aggregate_feedback signature"
      pattern: "FeedbackStore"
    - from: "agent/evolution/apply.py"
      to: "agent/skill_utils.parse_frontmatter"
      via: "FOUND-08 per-patch check reuses parse_frontmatter for expert_id/related_skills extraction"
      pattern: "parse_frontmatter"
    - from: "agent/evolution/apply.py"
      to: "skills/movie-experts/_eval/gate.extract_patched_files"
      via: "Reuse T-30-01 path-traversal-hardened file extractor"
      pattern: "extract_patched_files"
    - from: "agent/evolution/apply.py"
      to: "subprocess git"
      via: "argv-list subprocess.run (no shell=True) per T-30-02 mitigation"
      pattern: "\\[\"git\", \"apply\""
---

<objective>
Build the `agent/evolution/` subpackage — the core engine of Phase 31's knowledge evolution pipeline. This plan produces the pure functions + dataclasses + atomic transaction primitives that Plan 02 wires into the CLI. No CLI surface, no runtime imports — just the tested engine.

Purpose: Close the self-learning loop at the engine layer. Each module is independently testable, and the atomic apply transaction is the ONLY mutation path for bundled SKILL.md / refs (EVOL-04 non-bypassable human-in-loop is enforced structurally because this code is only callable from the operator CLI handler in Plan 02).

Output:
- `agent/evolution/{__init__,insights,diff_generator,queue,apply}.py` — 5 new modules, stdlib-only (openai + pydantic already pinned)
- `tests/agent/evolution/` test subpackage with conftest + 4 test files
- `tests/fixtures/evolution/` sample data for deterministic test inputs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-knowledge-evolution-pipeline/31-CONTEXT.md
@.planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md
@.planning/phases/31-knowledge-evolution-pipeline/31-VALIDATION.md

<interfaces>
<!-- VERIFIED contracts the executor needs — no codebase exploration required -->

From hermes_cli/feedback.py:40 (register_cli pattern to extend in Plan 02):
```python
def register_cli(parent: argparse.ArgumentParser) -> None:
    parent.set_defaults(func=lambda a: (parent.print_help(), 0)[1])
    subs = parent.add_subparsers(dest="feedback_command")
    # ... existing add_parser(...) + set_defaults(func=_cmd_...) calls ...
```

From agent/feedback_store.py:199 (input source — FeedbackStore):
```python
class FeedbackStore:
    def query(self, *, skill_id: str | None = None, ...) -> list[dict]: ...
    def summary(self, *, skill_id: str | None = None) -> dict: ...
```

From skills/movie-experts/_eval/gate.py (Phase 30 — reuse verbatim):
```python
def extract_patched_files(patch_path: Path) -> list[str]: ...   # line 117 — T-30-01 path-traversal guard
def apply_patch(patch_path: Path, repo_root: Path) -> None: ... # line 172 — argv-list subprocess
def revert_patch(files: list[str], repo_root: Path) -> None: ...# line 201 — git checkout -- + git clean -f
def run_gate(...) -> GateResult: ...                            # line 913 — invoked by Plan 02, not here
@dataclass
class GateResult: ...                                           # line 819
```

From agent/skill_utils.py:123 (FOUND-08 byte-intact check):
```python
def parse_frontmatter(content: str) -> Tuple[Dict[str, Any], str]: ...
# frontmatter["metadata"]["hermes"]["expert_id"] — str
# frontmatter["metadata"]["hermes"]["related_skills"] — list[str]
```

From agent/feedback_schema.py (Phase 28 — FeedbackRecord shape for LLM prompt context):
```python
# FeedbackRecord fields available via FeedbackStore.query():
#   record_id, skill_id, expert_id, source, verdict, correction,
#   output_snapshot (with sha256), ts
```

From utils.py:111 (atomic JSON write — for queue index if needed):
```python
def atomic_json_write(path: Path, data) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent/evolution/ subpackage skeleton + test scaffolding + EVOL-01 LLM aggregation</name>
  <files>
    agent/evolution/__init__.py,
    agent/evolution/insights.py,
    tests/agent/evolution/__init__.py,
    tests/agent/evolution/conftest.py,
    tests/agent/evolution/test_insights.py,
    tests/fixtures/evolution/sample_skill_md.md,
    tests/fixtures/evolution/sample_insights.json
  </files>
  <read_first>
    - .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md (Pattern 1 LLM client construction §"Pattern 1", Pitfall 1 JSON parsing, Code Examples §"LLM Aggregation Prompt Template")
    - skills/movie-experts/_eval/runner.py (lines around 524 — make_judge_client pattern; lines around 561 — _StubJudgeClient pattern for mock)
    - agent/feedback_store.py:199 (FeedbackStore class — query + summary signatures)
    - tests/agent/test_feedback_store.py (top ~120 lines — fixture pattern for HERMES_HOME isolation)
    - CLAUDE.md §"Logging" (lazy %-formatting) + §"Error Handling" (bind exceptions with `as exc`)
  </read_first>
  <behavior>
    - aggregate_feedback(skill_id="screenplay", store=mock_store, client=mock_client) returns list[InsightRecord] with len >= 1, each record has non-empty evidence_chain
    - Mock LLM returning JSON wrapped in ```json fences is parsed correctly (fences stripped, json.loads succeeds)
    - Mock LLM returning malformed JSON (trailing comma) raises AggregationError (NEVER returns empty list silently)
    - Mock LLM returning empty insights array returns [] (legitimate "no themes" — distinct from parse failure)
    - make_aggregation_client(model_override="X") returns (client, "X"); without override reads HERMES_EVOLUTION_MODEL env then falls back to default
    - make_aggregation_client with missing OPENROUTER_API_KEY raises RuntimeError with actionable message
    - InsightRecord model_validate rejects records with empty evidence_chain or missing theme
    - build_aggregation_user_prompt embeds skill_id + feedback_summary + feedback_details (max 50 records)
    - AGGREGATION_SYSTEM_PROMPT text contains the 7 critical rules from RESEARCH.md (additive-only, evidence chain, NEVER propose changes to v4/v5 protected refs, etc.)
  </behavior>
  <action>
    Create the subpackage skeleton first: `agent/evolution/__init__.py` (empty file — public re-exports added in Task 4 final step); `tests/agent/evolution/__init__.py` (empty marker).

    Then `agent/evolution/insights.py` with `from __future__ import annotations` at top, stdlib imports (json, logging, os, re, hashlib, datetime), and `from openai import OpenAI` import (per RESEARCH Pattern 1). Module logger `logger = logging.getLogger(__name__)`.

    Define `InsightRecord` as a Pydantic BaseModel (per RESEARCH Architectural Responsibility Map + Phase 28/29 Pydantic precedent) with required fields: `insight_id: str`, `skill_id: str`, `theme: str`, `evidence_chain: list[str]` (min_length=1 via Field), `rationale: str`, `proposed_addition: str`, `insert_after_marker: str`, `ts: str`. Add a model_validator ensuring evidence_chain is non-empty (defense-in-depth on Pydantic schema).

    Define `class AggregationError(Exception)` with a docstring noting it surfaces malformed LLM output (never silent empty list — Pitfall 1 atomicity rule).

    Define module-level constant `AGGREGATION_SYSTEM_PROMPT` exactly per RESEARCH §"LLM Aggregation Prompt Template" — 7 critical rules including additive-only, evidence chain, frontmatter byte-preserve, NEVER touching v4/v5 protected refs.

    Implement `make_aggregation_client(*, model_override: str | None = None) -> tuple[OpenAI, str]` per RESEARCH Pattern 1: read OPENROUTER_BASE_URL (default https://openrouter.ai/api/v1) + OPENROUTER_API_KEY from env; raise RuntimeError if key empty (message references ~/.hermes/.env); construct OpenAI(base_url=..., api_key=...); model = model_override or HERMES_EVOLUTION_MODEL env or default "claude-sonnet-4-6". NEVER log the api_key.

    Implement `build_aggregation_user_prompt(*, skill_id: str, feedback_summary: dict, feedback_details: list[dict]) -> str` per RESEARCH Code Examples — embed the three JSON dumps with ensure_ascii=False for CN content.

    Implement `aggregate_feedback(*, skill_id: str, store, client, model: str) -> list[InsightRecord]`:
      1. Call store.query(skill_id=skill_id) + store.summary(skill_id=skill_id)
      2. If store.query returns empty list → return [] (legitimate empty, log INFO "no feedback for skill")
      3. Truncate feedback_details to most-recent 50 (per build_aggregation_user_prompt docstring)
      4. Build messages = [{role: system, content: AGGREGATION_SYSTEM_PROMPT}, {role: user, content: build_aggregation_user_prompt(...)}]
      5. Try client.chat.completions.create(model=model, messages=messages, response_format={"type": "json_object"}) — catch SDK error, log warning, retry ONCE without response_format (Pitfall 1 mitigation per RESEARCH A2)
      6. Strip markdown fences ```json...``` from response (regex r"^```(?:json)?\s*\n?|\n?```\s*$" with re.MULTILINE)
      7. json.loads the stripped content — on JSONDecodeError, log DEBUG raw response (never INFO — may contain feedback snippets per V8 data protection), raise AggregationError("LLM returned malformed JSON")
      8. Parse payload["insights"] — if missing or not a list, raise AggregationError
      9. For each raw insight dict: compute insight_id = f"{skill_id}_{ts_unix}_{sha256(theme+rationale)[:8]}" (Claude's Discretion format per CONTEXT.md), set ts = datetime.now(timezone.utc).isoformat(), validate via InsightRecord.model_validate; collect Pydantic ValidationError per-bad-record and raise AggregationError listing all bad records (atomicity — matches Phase 28 atomic JSONL import pattern)
      10. Return list[InsightRecord]

    Write `tests/fixtures/evolution/sample_skill_md.md` — a minimal valid SKILL.md with frontmatter (expert_id: test_skill, related_skills: [a, b]) + body containing "## References" marker line. ~30 lines, additive-test-friendly.

    Write `tests/fixtures/evolution/sample_insights.json` — a valid LLM-shaped response: `{"insights": [{"theme": "...", "evidence_chain": ["fb_1", "fb_2"], "rationale": "...", "proposed_addition": "...\n", "insert_after_marker": "## References"}]}` with 2 insights for multi-record tests.

    Write `tests/agent/evolution/conftest.py` with fixtures:
      - `evolution_env` (monkeypatch HERMES_HOME to tmp_path, create ~/.hermes/skills/.feedback/evolution/ dir tree, init a real git repo at tmp_path via subprocess.run(["git", "init"], cwd=tmp_path) + git config user.email/name locally per RESEARCH Pitfall 7, stage the sample_skill_md.md at skills/movie-experts/test_skill/SKILL.md so FOUND-08 checks have a real file to parse)
      - `mock_llm_client` (returns a stub object whose chat.completions.create returns a `.choices[0].message.content` attribute — mirror runner.py:561 _StubJudgeClient pattern; the stub reads a fixture file or returns canned JSON)
      - `mock_store` (returns a stub with .query(skill_id=...) returning a canned list of feedback dicts + .summary(skill_id=...) returning a counts dict)
      - `sample_feedback_records` (3-5 canned FeedbackRecord-shaped dicts with sha256, verdict, correction fields)
      - `sample_skill_content` (loads sample_skill_md.md fixture text)

    Write `tests/agent/evolution/test_insights.py` with TestInsightSchema, TestAggregateFeedback, TestMalformedJson, TestEmptyFeedback, TestMakeAggregationClient classes — one test method per behavior bullet above. Use mock_llm_client + mock_store fixtures; for the malformed-JSON test, configure the stub to return a string with a trailing comma; for the fence-stripping test, configure it to return ```json\n{...}\n```.

    All file open() calls MUST pass encoding="utf-8" (Ruff PLW1514). All logging MUST use %-formatting (no f-strings in logger calls). Catch specific exceptions (json.JSONDecodeError, pydantic.ValidationError, openai.OpenAIError) — never bare except.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/evolution/test_insights.py -x --timeout=30</automated>
  </verify>
  <done>
    - `agent/evolution/insights.py` exists with aggregate_feedback, make_aggregation_client, InsightRecord, AggregationError, AGGREGATION_SYSTEM_PROMPT, build_aggregation_user_prompt all importable
    - All TestInsightSchema / TestAggregateFeedback / TestMalformedJson / TestEmptyFeedback / TestMakeAggregationClient tests pass
    - `ruff check agent/evolution/insights.py` returns clean (PLW1514 encoding check)
    - No `from agent.evolution` imports added to any runtime module (grep verified in Task 4)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: difflib-based additive diff generator (EVOL-02 placeholder) + JSONL queue lifecycle (EVOL-03)</name>
  <files>
    agent/evolution/diff_generator.py,
    agent/evolution/queue.py,
    tests/agent/evolution/test_diff_generator.py,
    tests/agent/evolution/test_queue.py,
    tests/fixtures/evolution/additive.diff,
    tests/fixtures/evolution/violating_deletion.diff
  </files>
  <read_first>
    - .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md (Pattern 2 difflib generation §"Pattern 2", Pitfall 2 difflib edge cases, Pitfall 5 concurrent queue writes)
    - tools/patch_parser.py:496 (codebase precedent for difflib.unified_diff usage)
    - agent/feedback_store.py:336 (JSONL append-only pattern — bucket file writes)
    - utils.py:111 (atomic_json_write — only for queue index, not the JSONL append itself)
    - skills/movie-experts/_eval/gate.py:117 (extract_patched_files — for parsing diff file paths when needed)
    - tests/fixtures/feedback/valid_10.jsonl (existing JSONL fixture pattern for format reference)
  </read_first>
  <behavior>
    - generate_additive_diff(current_content="# T\n## Refs\n", proposed_addition="new\n", insert_after_marker="## Refs", skill_md_path="skills/x/SKILL.md") returns a unified diff string containing exactly one hunk header `@@ -1,N +1,M @@` with M > N and only `+` content lines (no `-` lines)
    - generate_additive_diff raises ValueError when insert_after_marker substring is not present in current_content (refuse blind append per RESEARCH Pitfall 2)
    - generate_additive_diff raises ValueError("marker not unique") when insert_after_marker appears more than once in current_content (RESEARCH Pitfall 2 mitigation #2)
    - generate_additive_diff normalizes \r\n to \n in current_content before diffing (Pitfall 2 mitigation #1)
    - generate_additive_diff returns empty string + raises ValueError when proposed_addition already present at insertion site (idempotent guard)
    - PatchRecord model_validate accepts a full record per CONTEXT.md D-EVOL-03 schema (patch_id, skill_id, insight_id, unified_diff, feedback_chain, llm_rationale, eval_gate_score, status, ts_queued)
    - append_patch(record, evolution_dir) writes one JSON line to queue.jsonl (creates file if absent), preserves prior content
    - move_patch(patch_id, target_status="applied", evolution_dir, commit_sha="abc123") removes the line from queue.jsonl and appends a merged record (with ts_applied + commit_sha) to applied.jsonl
    - move_patch(patch_id, target_status="rejected", evolution_dir, reason="...") writes to rejected.jsonl with ts_rejected + reason
    - move_patch raises KeyError when patch_id not found in source queue
    - read_queue(evolution_dir, status="pending", skill_id=None) returns list[PatchRecord] filtered by status + optional skill_id
    - read_queue silently skips + logs WARNING on any malformed JSON line (mirror agent/feedback_store.py:654 _read_bucket_records pattern)
    - FAILED_GATE log path: append_failed_gate(rejection_record, evolution_dir) writes to failed_gate.jsonl — these NEVER appear in read_queue output
  </behavior>
  <action>
    Create `agent/evolution/diff_generator.py` with `from __future__ import annotations`, stdlib imports (difflib, logging). Module logger.

    Implement `generate_additive_diff(*, current_content: str, proposed_addition: str, insert_after_marker: str, skill_md_path: str) -> str` per RESEARCH Pattern 2:
      1. current_content = current_content.replace("\r\n", "\n") (Pitfall 2 #1)
      2. Find indices of all lines containing insert_after_marker; if count == 0 raise ValueError(f"insert_after_marker {marker!r} not found"); if count > 1 raise ValueError(f"insert_after_marker {marker!r} not unique ({count} matches)") (Pitfall 2 #2)
      3. insert_idx = first_match_index + 1
      4. addition_lines = proposed_addition.splitlines(keepends=True); ensure leading "\n" if previous line lacks trailing newline
      5. new_lines = current_lines[:insert_idx] + addition_lines + current_lines[insert_idx:]
      6. If new_lines == current_lines → raise ValueError("proposed_addition already present at insertion site") (idempotent guard)
      7. diff = "".join(difflib.unified_diff(current_lines, new_lines, fromfile=f"a/{skill_md_path}", tofile=f"b/{skill_md_path}", lineterm="\n"))
      8. Assert diff is non-empty AND contains at least one line starting with "+" (Pitfall 2 #3) — raise ValueError on violation
      9. Return diff

    Create `agent/evolution/queue.py` with `from __future__ import annotations`, stdlib imports (json, logging, os), `from datetime import datetime, timezone`, `from pathlib import Path`, `from pydantic import BaseModel, Field`. Module logger.

    Define `PROTECTED_REFS` tuple = ("snowflake-method.md", "e-konte-format.md", "scamper-variations.md", "dreamina-cli-baseline.md", "v86-pipeline-mapping.md") — module-level constant reused by apply.py Task 3.

    Define `class PatchRecord(BaseModel)` per CONTEXT.md D-EVOL-03 schema with fields: patch_id, skill_id, insight_id, unified_diff, feedback_chain: list[str], llm_rationale, eval_gate_score: dict, status: str (default "pending"), ts_queued: str. Add a model_validator ensuring status in {"pending", "applied", "rejected", "failed_gate"}.

    Module-level file-name constants: QUEUE_FILENAME = "queue.jsonl", APPLIED_FILENAME = "applied.jsonl", REJECTED_FILENAME = "rejected.jsonl", FAILED_GATE_FILENAME = "failed_gate.jsonl", INSIGHTS_FILENAME = "insights.jsonl".

    Implement `_append_jsonl(path: Path, record: dict) -> None` helper: open(path, "a", encoding="utf-8") + json.dumps(record, ensure_ascii=False) + "\n" + write. Single-process assumption documented in module docstring (RESEARCH Pitfall 5).

    Implement `_read_jsonl(path: Path) -> list[dict]` helper: if not path.exists() return []; iterate lines; json.loads each; on JSONDecodeError log warning with line number + skip (mirror feedback_store.py:654). Return list.

    Implement `append_patch(record: PatchRecord, evolution_dir: Path) -> None`: assert record.status == "pending"; _append_jsonl(evolution_dir / QUEUE_FILENAME, record.model_dump()).

    Implement `move_patch(*, patch_id: str, target_status: str, evolution_dir: Path, commit_sha: str | None = None, reason: str | None = None) -> PatchRecord`:
      1. Read queue.jsonl; find record with matching patch_id; if not found raise KeyError(f"patch_id {patch_id} not in {QUEUE_FILENAME}")
      2. Construct updated = record.copy(update={status: target_status, ...})
      3. If target_status == "applied": add ts_applied = datetime.now(timezone.utc).isoformat() + commit_sha (require commit_sha not None); write to applied.jsonl
      4. If target_status == "rejected": add ts_rejected + reason (require reason not None); write to rejected.jsonl
      5. Rewrite queue.jsonl WITHOUT the moved line (read all, filter out patch_id, write back atomically via temp file + os.replace per utils.atomic_json_write pattern — but here we control the lines so do it inline: write all-records-except-moved back)
      6. Return updated PatchRecord

    Implement `read_queue(*, evolution_dir: Path, status: str = "pending", skill_id: str | None = None) -> list[PatchRecord]`:
      1. Map status → filename (pending→queue.jsonl, applied→applied.jsonl, rejected→rejected.jsonl)
      2. _read_jsonl the file; for each dict, PatchRecord.model_validate; skip + log warning on ValidationError
      3. If skill_id is not None: filter records where record.skill_id == skill_id
      4. Return list

    Implement `append_failed_gate(rejection: dict, evolution_dir: Path) -> None`: _append_jsonl(evolution_dir / FAILED_GATE_FILENAME, rejection).

    Write `tests/fixtures/evolution/additive.diff` — a hand-written unified diff with one hunk adding 2 lines (no `-` content lines), headers `--- a/skills/movie-experts/test_skill/SKILL.md` + `+++ b/skills/movie-experts/test_skill/SKILL.md`, hunk `@@ -1,3 +1,5 @@`.

    Write `tests/fixtures/evolution/violating_deletion.diff` — a diff with one `-` content line (deletion) for the additive-only verifier tests in Task 3 to consume; AND ensure this fixture is also usable here for any queue-side negative test.

    Write `tests/agent/evolution/test_diff_generator.py` with TestGenerateAdditiveDiff class — one test per behavior bullet. Use sample_skill_content fixture for current_content.

    Write `tests/agent/evolution/test_queue.py` with TestPatchRecordSchema, TestAppendPatch, TestMovePatch (applied + rejected branches), TestMovePatchNotFound, TestReadQueue, TestReadQueueMalformedLineSkipped, TestAppendFailedGate classes — use evolution_env fixture for tmp evolution_dir.

    All file open() calls MUST pass encoding="utf-8". No f-strings in logger calls. Catch specific exceptions.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/evolution/test_diff_generator.py tests/agent/evolution/test_queue.py -x --timeout=30</automated>
  </verify>
  <done>
    - `agent/evolution/diff_generator.py` exists with generate_additive_diff exported; rejects non-unique markers + missing markers + idempotent additions
    - `agent/evolution/queue.py` exists with PatchRecord, append_patch, move_patch, read_queue, append_failed_gate exported; queue.jsonl lifecycle (append → move → read with filter) works end-to-end
    - PROTECTED_REFS tuple defined for reuse by apply.py
    - All TestGenerateAdditiveDiff + queue test classes pass
    - `ruff check agent/evolution/diff_generator.py agent/evolution/queue.py` returns clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atomic apply transaction (EVOL-05) + FOUND-08 byte-intact (SC-5) + additive-only verifier (SC-6)</name>
  <files>
    agent/evolution/apply.py,
    tests/agent/evolution/test_apply.py,
    tests/fixtures/evolution/violating_frontmatter_diffusion.diff
  </files>
  <read_first>
    - .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md (Pattern 3 atomic transaction §"Pattern 3", Pitfall 3 git partial failures, Pitfall 4 frontmatter drift, Pitfall 6 additive verification false positives, Pitfall 7 git author identity, Code Examples §"Additive-Only Verification Algorithm" + §"FOUND-08 Per-Patch Check" + §"Commit Message Format")
    - skills/movie-experts/_eval/gate.py lines 117-260 (extract_patched_files + apply_patch + revert_patch canonical patterns to mirror)
    - skills/movie-experts/_eval/gate.py lines 913-1090 (run_gate try/finally revert-on-failure pattern)
    - agent/skill_utils.py lines 123-160 (parse_frontmatter — for FOUND-08 field extraction)
    - skills/movie-experts/_eval/tests/test_gate.py lines 94-130 (tmp_git_repo fixture pattern — author identity setup)
    - .planning/phases/31-knowledge-evolution-pipeline/31-CONTEXT.md §"Patch Apply Git Workflow" (D-EVOL-04 6-step atomic transaction)
  </read_first>
  <behavior>
    - verify_additive_only(diff_text) returns True for a pure-addition diff (only `+` content lines + context + valid hunk header D>=B); returns False for any diff with a `-` content line or hunk where D < B; returns False for empty diff
    - verify_additive_only correctly skips `--- a/...` and `+++ b/...` file headers (does NOT mistake them for removals/additions per Pitfall 6)
    - verify_additive_only correctly skips `@@ -A,B +C,D @@` hunk header lines
    - verify_found08_byte_intact(frontmatter_before, content_after) returns True when frontmatter block bytes match exactly; returns False when bytes differ even if parsed values are equal (Pitfall 4 byte-level check, stricter than value check)
    - _extract_frontmatter_block(content) returns the frontmatter text between opening `---` and closing `---` (inclusive); returns "" when no frontmatter present
    - apply_patch_transaction(patch_path=..., repo_root=..., commit_message="feat(evolution): ...") on a clean repo + valid additive + FOUND-08-preserving diff returns ApplyResult with non-empty commit_sha + files_modified list; the commit exists in git log
    - apply_patch_transaction raises ApplyError when the patch would delete a `-` content line (additive-only violation); working tree is clean after (git status --porcelain empty)
    - apply_patch_transaction raises ApplyError when the patch modifies expert_id or related_skills frontmatter (FOUND-08 violation); working tree is clean after
    - apply_patch_transaction raises ApplyError when git apply --check fails (malformed patch); no working tree mutation
    - apply_patch_transaction raises ApplyError when working tree is dirty before apply (CONTEXT.md D-EVOL-04 invariant — git status --porcelain check)
    - apply_patch_transaction sets repo-local git config user.email + user.name if global is unset (RESEARCH Pitfall 7); NEVER uses --global
    - build_commit_message(insight_summary=..., feedback_ids=[...], eval_verdict="pass", eval_mean_delta=0.15) returns string matching format `feat(evolution): <subject> | feedback: <ids> | eval: <verdict>:<delta>` per CONTEXT.md D-EVOL-04
    - revert_files(files, repo_root) runs git checkout -- <files> for files in HEAD + git clean -f <files> for files not in HEAD (mirrors gate.py:201 revert_patch exactly)
    - Commit message subject truncated to 72 chars (git convention)
    - The ApplyError message is operator-safe: never contains the full unified_diff (avoid leaking feedback text in error surfaces per V8 Data Protection)
  </behavior>
  <action>
    Create `agent/evolution/apply.py` with `from __future__ import annotations`, stdlib imports (logging, re, subprocess), `from pathlib import Path`, `from dataclasses import dataclass`. Module logger.

    Import reuse from shipped components:
      - `from agent.skill_utils import parse_frontmatter` (FOUND-08 field extraction — do NOT re-implement YAML parsing per RESEARCH "Don't Hand-Roll" table)
      - `from skills.movie_experts._eval.gate import extract_patched_files` — BUT skills/movie-experts is a hyphenated path which is not a valid Python module name. Use the canonical import pattern: since gate.py is at skills/movie-experts/_eval/gate.py and test_gate.py at skills/movie-experts/_eval/tests/test_gate.py imports it via `import runner` (sibling), you CANNOT use a normal `from ... import` here. Instead, use `subprocess.run([sys.executable, ...])` is NOT needed — instead, **re-implement extract_patched_files locally as a small helper** `_extract_patched_files(patch_path: Path) -> list[str]` using the same logic (parse `+++ b/...` lines, reject `..` paths, reject `/dev/null`). Add a code comment: "Local copy of gate.extract_patched_files logic — gate.py lives under a hyphenated path and is not importable; behavior is identical to skills/movie-experts/_eval/gate.py:117". This keeps the FOUND-08 / path-traversal hardening without a fragile sys.path hack.

    Define `_HUNK_HEADER_RE = re.compile(r"^@@ -(\d+),(\d+) \+(\d+),(\d+) @@")` module-level.

    Define `class ApplyError(Exception)` with docstring noting it's raised after auto-revert (working tree is always clean when this surfaces).

    Define `@dataclass class ApplyResult` with `commit_sha: str` + `files_modified: list[str]`.

    Implement `verify_additive_only(diff_text: str) -> bool` per RESEARCH Code Examples §"Additive-Only Verification Algorithm":
      - Empty/whitespace-only diff → return False
      - Iterate lines; skip `--- ` / `+++ ` file headers; on hunk header regex match, parse B + D, return False if D < B; on line starting with `-` (after file-header skip) → return False; track saw_addition when line starts with `+`; return saw_addition at end

    Implement `_extract_frontmatter_block(content: str) -> str`:
      - If not content.startswith("---") → return ""
      - Find closing `\n---\n` via regex `re.search(r"\n---\s*\n", content[3:])`; if absent return ""
      - Return content[:end_idx] inclusive of both `---` delimiters

    Implement `verify_found08_byte_intact(frontmatter_block_before: str, skill_md_path_after: Path) -> bool`:
      - after_content = skill_md_path_after.read_text(encoding="utf-8")
      - after_fm = _extract_frontmatter_block(after_content)
      - Return frontmatter_block_before == after_fm (byte-identical — Pitfall 4 stricter-than-value check)

    Implement `_extract_found08_fields(skill_md_path: Path) -> dict[str, str]` — secondary check using parse_frontmatter to extract expert_id + related_skills values (defense-in-depth alongside the byte check):
      - content = skill_md_path.read_text(encoding="utf-8")
      - frontmatter, _ = parse_frontmatter(content)
      - metadata = frontmatter.get("metadata", {}).get("hermes", {})
      - Return {"expert_id": str(metadata.get("expert_id", "")), "related_skills": str(sorted(metadata.get("related_skills", [])))}

    Implement `revert_files(files: list[str], repo_root: Path) -> None` — mirror gate.py:201 revert_patch exactly:
      - For each file: check if file is in HEAD via `git cat-file -e HEAD:<path>` (capture_output, text=True); exit 0 → in HEAD → `git checkout -- <path>`; non-zero → not in HEAD → `git clean -f <path>` (scoped, NEVER blanket clean — worktree safety per CONTEXT.md D-EVOL-04)
      - All subprocess.run calls use argv-list (no shell=True per T-30-02)
      - Catch CalledProcessError, log error, re-raise (caller handles)

    Implement `build_commit_message(*, insight_summary: str, feedback_ids: list[str], eval_verdict: str, eval_mean_delta: float) -> str` per RESEARCH Code Examples §"Commit Message Format":
      - subject = insight_summary[:72]
      - feedback_str = ",".join(feedback_ids) if feedback_ids else "none"
      - eval_str = f"{eval_verdict}:{eval_mean_delta:.2f}"
      - Return f"feat(evolution): {subject} | feedback: {feedback_str} | eval: {eval_str}"

    Implement `apply_patch_transaction(*, patch_path: Path, repo_root: Path, commit_message: str, protected_refs: tuple[str, ...] = PROTECTED_REFS_FROM_QUEUE) -> ApplyResult`:
      1. Pre-flight: `git status --porcelain` — if non-empty stdout, raise ApplyError("working tree dirty — commit or stash before apply") (CONTEXT.md D-EVOL-04 invariant)
      2. Ensure git author identity: `git config user.email` — if empty exit, run `git config user.email "hermes-evolution@local"` + `git config user.name "Hermes Evolution Pipeline"` (repo-local ONLY, never --global per RESEARCH Pitfall 7)
      3. `git apply --check` — CalledProcessError → raise ApplyError("patch does not apply cleanly") with safe message
      4. files = _extract_patched_files(patch_path); if empty raise ApplyError("patch modifies no files")
      5. Pre-extract frontmatter_before bytes + values: frontmatter_before_block = _extract_frontmatter_block((repo_root / files[0]).read_text(encoding="utf-8")) + fields_before = _extract_found08_fields(repo_root / files[0])
      6. applied = False; try:
         a. `git apply` (CalledProcessError → raise — caught by outer except)
         b. applied = True
         c. For each file in files: after_block = _extract_frontmatter_block(read_text); if not verify_found08_byte_intact(frontmatter_before_block_for_file, repo_root / file): raise ApplyError(f"FOUND-08 violation: frontmatter drifted in {file}")
         d. For each file in files: if any protected in file for protected in protected_refs: read patch_text; if not verify_additive_only(patch_text): raise ApplyError(f"SC-6 violation: patch to protected ref {file} is not additive-only")
         e. `git add -- <files>`
         f. `git commit -m commit_message`
         g. sha = `git rev-parse HEAD`.stdout.strip()
         h. logger.info("evolution patch applied: commit=%s files=%s", sha[:12], files)
         i. return ApplyResult(commit_sha=sha, files_modified=files)
      7. except Exception as exc:
         - logger.error("apply_patch_transaction failed: %s — reverting", exc)
         - if applied: try revert_files(files, repo_root) except Exception as revert_exc: logger.error("revert ALSO failed: %s — working tree dirty, manual recovery required", revert_exc); raise ApplyError(f"apply failed ({exc}) AND revert failed ({revert_exc}); manual recovery required") from exc
         - raise ApplyError(f"patch apply failed: {exc}") from exc  (NOTE: the message uses exc but NOT the full diff — operator-safe per V8)

    Write `tests/fixtures/evolution/violating_frontmatter_diffusion.diff` — a diff whose additions include a re-serialized frontmatter (e.g., the `@@ -1,5 +1,7 @@` hunk modifies the `related_skills:` list ordering) — so the FOUND-08 byte-intact check fails even though parsed values might be equivalent. This validates the Pitfall 4 byte-level check.

    Write `tests/agent/evolution/test_apply.py` with classes:
      - TestVerifyAdditiveOnly (pure-function tests: accepts pure addition, rejects deletion, rejects D<B hunk, rejects empty diff, skips file headers, skips hunk headers)
      - TestExtractFrontmatterBlock (returns block with delimiters, returns "" when no frontmatter)
      - TestVerifyFound08ByteIntact (passes on byte-equal, FAILS on value-equal-but-byte-different using the violating_frontmatter_diffusion fixture)
      - TestRevertFiles (existing-file via git checkout, added-file via git clean)
      - TestBuildCommitMessage (format match, 72-char truncation, empty feedback_ids → "none")
      - TestApplyPatchTransactionHappyPath (uses evolution_env fixture + additive.diff fixture → returns ApplyResult + commit visible in git log)
      - TestApplyPatchTransactionRevertOnFound08 (uses violating_frontmatter_diffusion.diff → raises ApplyError + git status --porcelain empty after)
      - TestApplyPatchTransactionRevertOnAdditive (uses violating_deletion.diff → raises ApplyError + working tree clean)
      - TestApplyPatchTransactionDirtyTree (pre-dirty the repo → raises ApplyError without applying)
      - TestApplyPatchTransactionGitAuthor (unset global user.email → apply still succeeds via repo-local config; verify `git config user.email` returns the hermes-evolution@local value)

    All file open() calls MUST pass encoding="utf-8". All subprocess calls argv-list only. All logging %-formatted.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/evolution/test_apply.py -x --timeout=30</automated>
  </verify>
  <done>
    - `agent/evolution/apply.py` exists with apply_patch_transaction, verify_additive_only, verify_found08_byte_intact, revert_files, build_commit_message, ApplyError, ApplyResult exported
    - All TestVerify* + TestApplyPatchTransaction* tests pass, including the FOUND-08 byte-level (Pitfall 4) + dirty-tree guard + git-author fallback tests
    - `ruff check agent/evolution/apply.py` returns clean
    - The 6-step atomic transaction (D-EVOL-04) is implemented in exactly the order: validate → apply → FOUND-08 → additive → stage → commit, with try/except revert on ANY failure
  </done>
</task>

<task type="auto">
  <name>Task 4: Public API re-exports + Hermes runtime isolation verification</name>
  <files>
    agent/evolution/__init__.py
  </files>
  <read_first>
    - agent/evolution/insights.py (Task 1 final state)
    - agent/evolution/diff_generator.py (Task 2 final state)
    - agent/evolution/queue.py (Task 2 final state)
    - agent/evolution/apply.py (Task 3 final state)
  </read_first>
  <action>
    Populate `agent/evolution/__init__.py` with public re-exports so consumers (Plan 02 CLI) can `from agent.evolution import aggregate_feedback, apply_patch_transaction, ...`:
      - `from agent.evolution.insights import aggregate_feedback, make_aggregation_client, InsightRecord, AggregationError`
      - `from agent.evolution.diff_generator import generate_additive_diff`
      - `from agent.evolution.queue import PatchRecord, append_patch, move_patch, read_queue, append_failed_gate, PROTECTED_REFS`
      - `from agent.evolution.apply import apply_patch_transaction, verify_additive_only, verify_found08_byte_intact, revert_files, build_commit_message, ApplyError, ApplyResult`
      - Define `__all__` listing all the above names
      - Module docstring: "Phase 31 Knowledge Evolution Pipeline (EVOL-01/03/04/05). Operator-invoked only — NOT imported by Hermes runtime (run_agent.py, agent/conversation_loop.py, agent/curator.py, cli.py, gateway/). Single-process assumption (see RESEARCH Pitfall 5)."

    After writing, run the isolation grep below in verify to confirm zero runtime imports.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "from agent.evolution import aggregate_feedback, apply_patch_transaction, PatchRecord, generate_additive_diff, ApplyError; print('imports ok')" && ISOLATION=$(grep -rn "from agent.evolution\|import agent.evolution" run_agent.py agent/conversation_loop.py agent/prompt_builder.py agent/system_prompt.py agent/tool_executor.py agent/curator.py cli.py gateway/ 2>/dev/null) && [ -z "$ISOLATION" ] && echo "isolation ok" || (echo "ISOLATION VIOLATION: $ISOLATION" && exit 1)</automated>
  </verify>
  <done>
    - `from agent.evolution import ...` succeeds for all public API names
    - Zero `from agent.evolution` or `import agent.evolution` matches in run_agent.py / agent/conversation_loop.py / agent/prompt_builder.py / agent/system_prompt.py / agent/tool_executor.py / agent/curator.py / cli.py / gateway/
    - Module docstring documents operator-invoked-only + single-process assumptions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → CLI | Operator invokes evolution commands; trusted input (CLI args validated by argparse) |
| LLM response → JSON parser | Untrusted probabilistic output crosses here — Pitfall 1 malformed-JSON surface |
| Unified diff → git apply | Untrusted patch text crosses here — T-30-01 path traversal + T-30-02 shell injection surfaces |
| Bundled SKILL.md → mutated SKILL.md | FOUND-08 + additive-only invariants cross here — SC-5/SC-6 surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | aggregate_feedback LLM JSON parsing | mitigate | Strip markdown fences + json.loads + Pydantic model_validate per-record; AggregationError on any failure (never silent empty list) per Pitfall 1 atomicity rule |
| T-31-02 | Information Disclosure | logging in insights.py | mitigate | LLM raw response logged at DEBUG only (may contain feedback snippets); OPENROUTER_API_KEY never logged (V8 Data Protection per RESEARCH Security Domain) |
| T-31-03 | Tampering | apply_patch_transaction git subprocess | mitigate | argv-list only (no shell=True) per T-30-02; reuse gate.extract_patched_files path-traversal hardening (T-30-01) via local _extract_patched_files copy |
| T-31-04 | Tampering | _extract_patched_files path parsing | mitigate | Reject `..` path components + reject `/dev/null` deletion patches (mirror gate.py WR-07 fix); reject paths outside skills/movie-experts/ |
| T-31-05 | Tampering | verify_additive_only regex | mitigate | Skip `---`/`+++` file headers + `@@` hunk headers explicitly (Pitfall 6 false-positive mitigation); assert D >= B per hunk |
| T-31-06 | Tampering | verify_found08_byte_intact | mitigate | Byte-level frontmatter block comparison (stricter than parse_frontmatter value check per Pitfall 4); defense-in-depth with parse_frontmatter field check |
| T-31-07 | DoS | apply_patch_transaction partial failure | mitigate | try/finally revert_files on ANY exception (mirrors gate.py:1084 pattern); escalate to ApplyError if revert also fails so operator knows manual recovery is needed |
| T-31-08 | Repudiation | git commit author identity | mitigate | Repo-local `git config user.email/name` fallback if global unset (Pitfall 7); NEVER --global; commit message includes feedback_ids for audit trail (machine-parseable per D-EVOL-04) |
| T-31-09 | Tampering | queue.jsonl concurrent writes | accept | Single-process assumption documented (CONTEXT.md Claude's Discretion); patch_id is content-addressed so duplicates detectable; malformed lines skipped + logged per feedback_store.py:654 pattern — worst case is a duplicate that operator notices, not data corruption |
| T-31-10 | Elevation of Privilege | LLM prompt injection via feedback text | mitigate | Pydantic schema validation on LLM output + additive-only check + (Plan 02) eval gate + (Plan 02) operator human-in-loop review (EVOL-04). Proposed additions are markdown only — never executed as code; worst case is a malformed SKILL.md section operator notices at review time |
| T-31-SC | Tampering | (no new packages installed — stdlib + already-pinned openai/pydantic only) | accept | RESEARCH §"Package Legitimacy Audit" confirms zero new packages; no slopcheck checkpoint needed |

**ASVS L1 control mapping:** V4 (access control — EVOL-04 enforced structurally), V5 (input validation — Pydantic + regex), V7 (error handling — try/finally revert + safe messages), V8 (data protection — DEBUG-only LLM logging, no key logging), V10 (business logic — atomic transaction + per-patch invariants).
</threat_model>

<verification>
- `python -m pytest tests/agent/evolution/ -x --timeout=30` — all 4 test files green
- `ruff check agent/evolution/` — clean (PLW1514 encoding check passes)
- Isolation grep returns 0 matches across runtime modules (Task 4 verify command)
- `python -c "from agent.evolution import aggregate_feedback, apply_patch_transaction, PatchRecord, generate_additive_diff, ApplyError"` succeeds
- No `shell=True` in any subprocess call: `grep -n "shell=True" agent/evolution/*.py` returns 0 matches
- Every `open(` in agent/evolution/ passes `encoding="utf-8"`: `grep -E "open\([^)]*\)" agent/evolution/*.py | grep -v 'encoding="utf-8"'` returns 0 matches
</verification>

<success_criteria>
- EVOL-01 foundation: aggregate_feedback + InsightRecord + AggregationError shipped with full test coverage for happy path + malformed JSON + empty feedback
- EVOL-02 placeholder: generate_additive_diff using stdlib difflib — purely additive by construction, with edge case tests (marker not found, marker not unique, idempotent guard)
- EVOL-03 foundation: PatchRecord schema + JSONL queue lifecycle (append/move/read) + failed_gate.jsonl separation
- EVOL-04 foundation: structural enforcement — apply_patch_transaction is the ONLY mutation path; no auto-apply code path exists in this plan
- EVOL-05: atomic apply transaction (6 steps per D-EVOL-04) + FOUND-08 byte-intact (SC-5, byte-level per Pitfall 4) + additive-only verifier (SC-6, false-positive-safe per Pitfall 6) + revert-on-failure + build_commit_message machine-parseable format
- Isolation: zero runtime imports of agent.evolution.* across all runtime modules
- Ruff clean, all tests green
</success_criteria>

<output>
Create `.planning/phases/31-knowledge-evolution-pipeline/31-01-SUMMARY.md` when done. Document:
- Files created (5 source modules + 5 test files + 4 fixtures + 2 test markers)
- Public API surface in agent/evolution/__init__.py __all__
- Test counts per file
- Any Rule 1 bugs auto-fixed (patterns that failed first-write and were corrected in-task)
- Confirmation that isolation grep returned 0 matches at end of Task 4
- Open dependencies for Plan 02: hermes_cli/feedback.py register_cli extension will import from agent.evolution
</output>

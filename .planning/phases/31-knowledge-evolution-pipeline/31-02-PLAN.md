---
phase: 31-knowledge-evolution-pipeline
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - hermes_cli/feedback.py
  - tests/hermes_cli/test_evolution_cli.py
autonomous: true
requirements: [EVOL-01, EVOL-03, EVOL-04, EVOL-05]
user_setup:
  - service: openrouter
    why: "LLM aggregation call in `hermes feedback evolve` requires OPENROUTER_API_KEY (or compatible provider key)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard → Keys (or configured provider equivalent). Already required by Phase 30 gate.py."
    dashboard_config: []
  - service: git
    why: "`hermes feedback approve` commits to the current branch; operator's working tree must be clean before apply"
    env_vars: []
    dashboard_config:
      - task: "Optionally set global git user.email / user.name (otherwise repo-local fallback is used per RESEARCH Pitfall 7)"
        location: "git config --global user.email <email>; git config --global user.name <name>"

must_haves:
  truths:
    - "Operator runs `hermes feedback evolve --skill <id>` and sees insights summary + queue count printed to stdout; insights.jsonl gains new lines"
    - "Operator runs `hermes feedback evolve --skill <id> --dry-run` without OPENROUTER_API_KEY and gets a stub insight written (offline testing path works per RESEARCH Environment Availability)"
    - "Operator runs `hermes feedback evolve --skill <id> --insights-only` and the command exits after writing insights.jsonl WITHOUT generating patches or running the eval gate (per RESEARCH Open Question 3 RESOLVED)"
    - "Operator runs `hermes feedback review-queue` and sees a table of pending patches (patch_id, skill, verdict, mean_delta, feedback count, summary)"
    - "Operator runs `hermes feedback review-queue --skill screenplay --status pending` and sees only patches for that skill with that status"
    - "Operator runs `hermes feedback show-patch <patch_id>` and sees the full unified diff + LLM rationale + feedback chain"
    - "Operator runs `hermes feedback approve <patch_id>` and the patch is applied atomically (FOUND-08 + additive + git commit); the patch moves from queue.jsonl to applied.jsonl; stdout shows the commit_sha"
    - "Operator runs `hermes feedback approve <patch_id>` WITHOUT --yes in a TTY and is prompted [y/N]; answering N aborts the apply with no working-tree mutation"
    - "Operator runs `hermes feedback reject <patch_id> <reason>` and the patch moves to rejected.jsonl with the reason recorded"
    - "Operator runs `hermes feedback rollback <commit_sha> --yes` and git revert is invoked; the revert commit appears in git log"
    - "NO code path in the codebase calls apply_patch_transaction EXCEPT _cmd_approve (EVOL-04 non-bypassable human-in-loop enforced structurally)"
    - "register_cli adds exactly 6 new subcommands (evolve, review-queue, show-patch, approve, reject, rollback) without disturbing the 4 existing feedback subcommands (import, watch, submit, rebuild-index)"
  artifacts:
    - path: "hermes_cli/feedback.py"
      provides: "register_cli extended with 6 new subparsers + 6 _cmd_* handlers; imports from agent.evolution"
      contains: "_cmd_evolve"
    - path: "tests/hermes_cli/test_evolution_cli.py"
      provides: "Smoke tests for all 6 new subcommands"
      contains: "TestEvolveCmd"
  key_links:
    - from: "hermes_cli/feedback.py:_cmd_evolve"
      to: "agent.evolution.aggregate_feedback"
      via: "from agent.evolution import aggregate_feedback (lazy import inside handler to keep module import side-effect-free)"
      pattern: "from agent.evolution import"
    - from: "hermes_cli/feedback.py:_cmd_approve"
      to: "agent.evolution.apply_patch_transaction"
      via: "ONLY code path that calls apply_patch_transaction (EVOL-04 non-bypassable)"
      pattern: "apply_patch_transaction"
    - from: "hermes_cli/feedback.py:_cmd_evolve"
      to: "skills/movie-experts/_eval/gate.run_gate (subprocess)"
      via: "Patch must pass eval gate before queue insertion (EVOL-05 precondition); invoked via subprocess.run([sys.executable, gate.py, ...]) because gate.py lives under hyphenated path"
      pattern: "run_gate"
---

<objective>
Wire the `agent/evolution/` engine (Plan 01) into the operator-facing CLI. Add 6 new subcommands to `hermes_cli/feedback.py:register_cli`, each with a focused `_cmd_*` handler that does only CLI-tier concerns (arg parsing, stdin/stdout, confirmation prompts, subprocess dispatch). The eval-gate-then-queue insertion path is here, not in Plan 01, because it requires invoking gate.py via subprocess (the gate lives under a hyphenated path that cannot be imported normally) — a CLI-tier concern.

Purpose: Give operators the surface to actually run the self-learning loop — `evolve` to extract insights, `review-queue` to inspect, `show-patch` to drill in, `approve` to apply atomically, `reject` to discard, `rollback` to undo. EVOL-04 non-bypassable human-in-loop is enforced structurally because `_cmd_approve` is the only code path that calls `apply_patch_transaction`.

Output:
- `hermes_cli/feedback.py` extended (additive — existing 4 subcommands preserved)
- `tests/hermes_cli/test_evolution_cli.py` with smoke tests for all 6 new subcommands
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-knowledge-evolution-pipeline/31-CONTEXT.md
@.planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md
@.planning/phases/31-knowledge-evolution-pipeline/31-VALIDATION.md
@.planning/phases/31-knowledge-evolution-pipeline/31-01-SUMMARY.md

<interfaces>
<!-- VERIFIED contracts from Plan 01 (already shipped by Wave 1) and existing CLI -->

From agent/evolution/__init__.py (Plan 01 public API — importable):
```python
from agent.evolution import (
    aggregate_feedback,        # (skill_id, store, client, model) -> list[InsightRecord]
    make_aggregation_client,   # (model_override) -> tuple[OpenAI, str]
    InsightRecord,             # Pydantic model
    AggregationError,          # raised on malformed LLM output
    generate_additive_diff,    # (current, addition, marker, path) -> unified_diff str
    PatchRecord,               # Pydantic model with patch_id, skill_id, unified_diff, ...
    append_patch,              # (record, evolution_dir) -> None
    move_patch,                # (patch_id, target_status, evolution_dir, commit_sha?, reason?) -> PatchRecord
    read_queue,                # (evolution_dir, status, skill_id?) -> list[PatchRecord]
    append_failed_gate,        # (rejection_dict, evolution_dir) -> None
    apply_patch_transaction,   # (patch_path, repo_root, commit_message, protected_refs?) -> ApplyResult
    ApplyError,                # raised after auto-revert
    ApplyResult,               # dataclass: commit_sha, files_modified
    build_commit_message,      # (insight_summary, feedback_ids, eval_verdict, eval_mean_delta) -> str
    PROTECTED_REFS,            # tuple of v4/v5 protected ref filenames
)
```

From hermes_cli/feedback.py:40 (EXISTING register_cli pattern — extend, do not break):
```python
def register_cli(parent: argparse.ArgumentParser) -> None:
    parent.set_defaults(func=lambda a: (parent.print_help(), 0)[1])
    subs = parent.add_subparsers(dest="feedback_command")
    # EXISTING: p_import (line 51), p_watch (line 68), p_submit (line 82), p_rebuild (line 118)
    # Each ends with: p_xxx.set_defaults(func=_cmd_xxx)
```

Existing _cmd_* handlers in hermes_cli/feedback.py (lines 131, 160, 185, 239) — pattern reference for return-int-stdout-CLI-handler:
```python
def _cmd_import(args) -> int:
    # ... implementation ...
    return 0  # or non-zero on error
```

From skills/movie-experts/_eval/gate.py (Phase 30 — invoked via subprocess, NOT importable):
- gate.py has a `main(argv: list[str] | None = None) -> int` at line 1255 with argparse (--patch, --skill, --multi-skill, --rebuild-baseline flags)
- Exit code 0 = pass; non-zero = fail/inconclusive
- Writes a JSON report to a path specified via --report flag (verify by reading gate.py main() body)
- Because skills/movie-experts is hyphenated, MUST invoke via `subprocess.run([sys.executable, str(gate_path), "--patch", str(patch), ...], cwd=repo_root)` — NEVER `from ... import`

From hermes_constants (canonical HERMES_HOME):
```python
from hermes_constants import get_hermes_home
# get_hermes_home() -> Path  (NEVER use Path.home() / ".hermes")
```

From .planning/phases/31-knowledge-evolution-pipeline/31-CONTEXT.md (D-EVOL-04 commit message format):
```
feat(evolution): <subject[:72]> | feedback: <comma-separated-ids|none> | eval: <verdict>:<mean_delta:.2f>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend register_cli with 6 new subcommands + _cmd_evolve / _cmd_review_queue / _cmd_show_patch / _cmd_reject handlers</name>
  <files>
    hermes_cli/feedback.py,
    tests/hermes_cli/test_evolution_cli.py
  </files>
  <read_first>
    - hermes_cli/feedback.py (full file — understand register_cli + 4 existing _cmd_* handlers + imports)
    - .planning/phases/31-knowledge-evolution-pipeline/31-01-SUMMARY.md (Plan 01 public API + any auto-fixed decisions)
    - .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md (Pattern 4 CLI Subcommand Extension §"Pattern 4" with full code example)
    - agent/evolution/__init__.py (Plan 01 __all__ — exact import names)
    - skills/movie-experts/_eval/gate.py (lines 1255-end — main() argparse signature + --report flag for JSON output)
    - hermes_cli/main.py (search for where feedback subparser is wired — to confirm register_cli call site)
    - tests/hermes_cli/test_feedback_cli.py (existing CLI test pattern — argparse construction + subcommand dispatch test approach)
  </read_first>
  <behavior>
    - `hermes feedback evolve --skill screenplay --dry-run` writes a stub insight to insights.jsonl and exits 0 without making an LLM call (offline testing path)
    - `hermes feedback evolve --skill screenplay --insights-only` writes insights.jsonl and exits 0 WITHOUT generating patches, running the gate, or appending to queue.jsonl
    - `hermes feedback evolve --skill screenplay` (with mock LLM via test fixture) generates >= 1 PatchRecord per insight, runs the gate (subprocess), appends passing patches to queue.jsonl, appends failing-gate patches to failed_gate.jsonl, prints summary "generated N insights, M patches passed gate, K failed gate"
    - `hermes feedback evolve` without --skill exits non-zero with clear argparse error
    - `hermes feedback evolve --skill <id>` on a skill with zero feedback records exits 0 with message "no feedback for skill <id>; nothing to evolve"
    - `hermes feedback review-queue` with empty queue prints "(no pending patches)" and exits 0
    - `hermes feedback review-queue --skill x --status applied` filters correctly (reads applied.jsonl)
    - `hermes feedback show-patch <patch_id>` prints: patch metadata header, LLM rationale, feedback_chain list, then the full unified_diff in a fenced block; exits 0
    - `hermes feedback show-patch <unknown_id>` prints "patch <id> not found in any queue file" to stderr and exits non-zero
    - `hermes feedback reject <patch_id> <reason>` moves the patch to rejected.jsonl with reason; prints "rejected <patch_id>: <reason>"; exits 0
    - `hermes feedback reject <unknown_id> x` exits non-zero with "patch not found"
    - register_cli still wires the 4 EXISTING subcommands (import, watch, submit, rebuild-index) — no regression
    - All evolution module imports are LAZY (inside _cmd_* handler bodies, not at module top) to preserve the runtime-isolation invariant: `grep -n "^from agent.evolution\|^import agent.evolution" hermes_cli/feedback.py` returns 0 matches at module top level
  </behavior>
  <action>
    Extend `hermes_cli/feedback.py:register_cli` additively. Append 6 new `p_xxx = subs.add_parser(...)` blocks AFTER the existing 4, exactly per RESEARCH §"Pattern 4 CLI Subcommand Extension" code example. Each subparser ends with `p_xxx.set_defaults(func=_cmd_xxx)`.

    Subcommand specifications:
      - `evolve` (EVOL-01): args `--skill` (required), `--model` (default None), `--dry-run` (store_true), `--insights-only` (store_true per RESEARCH Open Question 3 RESOLVED — adds flag, skips patch generation + gate)
      - `review-queue` (EVOL-03): args `--skill` (default None), `--status` (choices pending|applied|rejected, default pending)
      - `show-patch` (EVOL-03): positional `patch_id`
      - `approve` (EVOL-04 + EVOL-05): positional `patch_id`, `--yes` (store_true)
      - `reject` (EVOL-03): positional `patch_id`, positional `reason`
      - `rollback` (EVOL-05): positional `commit_sha`, `--yes` (store_true)

    Implement `_resolve_repo_root() -> Path`: walk up from cwd looking for `.git` dir; if not found, raise SystemExit("must run inside the hermes-agent git repo"). Used by _cmd_approve + _cmd_evolve.

    Implement `_resolve_evolution_dir() -> Path`: `get_hermes_home() / "skills" / ".feedback" / "evolution"`; create lazily via `path.mkdir(parents=True, exist_ok=True)`. Used by all queue-touching handlers.

    Implement `_run_eval_gate(*, patch_path: Path, skill_id: str, repo_root: Path, report_path: Path) -> tuple[str, dict]`:
      - Locate gate.py: `Path(__file__).resolve().parent.parent / "skills" / "movie-experts" / "_eval" / "gate.py"` (repo-relative — verify exists, else SystemExit)
      - subprocess.run([sys.executable, str(gate_path), "--patch", str(patch_path), "--skill", skill_id, "--report", str(report_path)], cwd=str(repo_root), capture_output=True, text=True, encoding="utf-8")
      - Parse exit code: 0 → "pass"; non-zero → "fail" (or "inconclusive" if exit==3 per gate.py multi-skill guard)
      - Parse report JSON for eval_gate_score dict (verdict + mean_delta + per_prompt_max_drop); on JSON parse error return ("fail", {"verdict": "fail", "reason": "report parse error", "stderr_tail": result.stderr[-200:]})
      - Return (verdict_str, score_dict)

    Implement `_cmd_evolve(args) -> int`:
      1. from agent.evolution import aggregate_feedback, make_aggregation_client, AggregationError, InsightRecord, generate_additive_diff, PatchRecord, append_patch, append_failed_gate  (LAZY import inside handler)
      2. from agent.feedback_store import FeedbackStore  (LAZY import)
      3. evolution_dir = _resolve_evolution_dir(); repo_root = _resolve_repo_root()
      4. store = FeedbackStore()  (uses get_hermes_home internally)
      5. feedback = store.query(skill_id=args.skill); if not feedback: print(f"no feedback for skill {args.skill}; nothing to evolve"); return 0
      6. If args.dry_run: construct a single stub InsightRecord with theme=f"[dry-run] stub for {args.skill}", evidence_chain=[feedback[0]["record_id"]], proposed_addition="# Dry-run addition\n", insert_after_marker=first H2 header in current SKILL.md (read sample); append_stub to insights.jsonl; print "dry-run: 1 stub insight written"; return 0
      7. client, model = make_aggregation_client(model_override=args.model)
      8. try: insights = aggregate_feedback(skill_id=args.skill, store=store, client=client, model=model) except AggregationError as exc: print(f"aggregation failed: {exc}", file=sys.stderr); return 1
      9. Append each insight to insights.jsonl as JSON line (reuse _append pattern: open(..., "a", encoding="utf-8") + json.dumps(insight.model_dump(), ensure_ascii=False))
      10. print(f"generated {len(insights)} insights for {args.skill}")
      11. If args.insights_only: return 0  (per RESEARCH Open Question 3 — skip patch generation + gate)
      12. For each insight: locate current SKILL.md via `repo_root / "skills" / "movie-experts" / args.skill / "SKILL.md"`; if missing skip + warn; else: diff = generate_additive_diff(current_content=md.read_text(encoding="utf-8"), proposed_addition=insight.proposed_addition, insert_after_marker=insight.insert_after_marker, skill_md_path=str(md.relative_to(repo_root))); write diff to temp patch file (tempfile.NamedTemporaryFile suffix=".patch", delete=False); verdict, score = _run_eval_gate(patch_path=Path(tmp_path), skill_id=args.skill, repo_root=repo_root, report_path=Path(tmp_path + ".report.json")); if verdict == "pass": patch_id = f"{args.skill}_{int(datetime.now(timezone.utc).timestamp())}_{hashlib.sha256(diff.encode()).hexdigest()[:16]}"; record = PatchRecord(patch_id=patch_id, skill_id=args.skill, insight_id=insight.insight_id, unified_diff=diff, feedback_chain=insight.evidence_chain, llm_rationale=insight.rationale, eval_gate_score=score, status="pending", ts_queued=datetime.now(timezone.utc).isoformat()); append_patch(record, evolution_dir); passed += 1; else: append_failed_gate({"patch_id": ..., "insight_id": insight.insight_id, "verdict": verdict, "score": score, "ts": ...}, evolution_dir); failed += 1; finally unlink temp patch file
      13. print(f"generated {len(insights)} insights, {passed} patches passed gate, {failed} failed gate")
      14. return 0

    Implement `_cmd_review_queue(args) -> int`:
      1. from agent.evolution import read_queue  (LAZY)
      2. evolution_dir = _resolve_evolution_dir()
      3. records = read_queue(evolution_dir=evolution_dir, status=args.status, skill_id=args.skill)
      4. If empty: print(f"(no {args.status} patches{f' for {args.skill}' if args.skill else ''})"); return 0
      5. Print table header: "PATCH_ID | SKILL | VERDICT | MEAN_DELTA | FEEDBACK_COUNT | SUMMARY"
      6. For each record: print f"{record.patch_id[:20]}... | {record.skill_id} | {record.eval_gate_score.get('verdict','?')} | {record.eval_gate_score.get('mean_delta','?')} | {len(record.feedback_chain)} | {record.llm_rationale[:40]}..."
      7. return 0

    Implement `_cmd_show_patch(args) -> int`:
      1. from agent.evolution import read_queue  (LAZY)
      2. evolution_dir = _resolve_evolution_dir()
      3. For status in (pending, applied, rejected): records = read_queue(...); match = next((r for r in records if r.patch_id == args.patch_id), None); if match: break
      4. If no match: print(f"patch {args.patch_id} not found in any queue file", file=sys.stderr); return 1
      5. Print: "Patch: {patch_id}", "Skill: {skill_id}", "Insight: {insight_id}", "Status: {status}", "Feedback chain: {feedback_chain}", "Eval: {eval_gate_score}", "", "Rationale:", "{llm_rationale}", "", "Unified diff:", "```diff", "{unified_diff}", "```"
      6. return 0

    Implement `_cmd_reject(args) -> int`:
      1. from agent.evolution import move_patch  (LAZY)
      2. evolution_dir = _resolve_evolution_dir()
      3. try: updated = move_patch(patch_id=args.patch_id, target_status="rejected", evolution_dir=evolution_dir, reason=args.reason) except KeyError as exc: print(f"patch not found: {exc}", file=sys.stderr); return 1
      4. print(f"rejected {updated.patch_id}: {args.reason}")
      5. return 0

    Write `tests/hermes_cli/test_evolution_cli.py` with classes TestEvolveCmdDryRun, TestEvolveCmdInsightsOnly, TestEvolveCmdFullPipeline (mock LLM + mock subprocess for gate), TestEvolveCmdNoFeedback, TestReviewQueueCmd, TestReviewQueueCmdEmpty, TestShowPatchCmd, TestShowPatchCmdNotFound, TestRejectCmd, TestRejectCmdNotFound, TestRegisterCliPreservesExistingSubcommands.

    Each CLI test should construct the argparse subparser via `register_cli(parent)` on a parent ArgumentParser, parse args via `parent.parse_args([...])`, invoke the resolved `_cmd_*` directly (bypass main entrypoint), assert exit code + captured stdout. Use tmp_path for HERMES_HOME isolation via monkeypatch + tmp git repo for tests touching apply/rollback.

    For TestEvolveCmdFullPipeline: mock `agent.evolution.make_aggregation_client` to return (stub_client, "stub-model"); stub_client.chat.completions.create returns content from tests/fixtures/evolution/sample_insights.json; mock subprocess.run for the gate call to return exit 0 + a canned score report JSON. This avoids live LLM + live gate dependency.

    All file open() calls MUST pass encoding="utf-8". All subprocess calls argv-list only. Logging %-formatted.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/hermes_cli/test_evolution_cli.py -x --timeout=30</automated>
  </verify>
  <done>
    - register_cli extended with 6 new subparsers (evolve, review-queue, show-patch, approve [stub], reject, rollback [stub]) — note: approve + rollback handlers are Task 2; for this task their handlers can be stubs returning 0 to keep the subparser wired; TestRegisterCliPreservesExistingSubcommands verifies import/watch/submit/rebuild-index still resolve
    - _cmd_evolve, _cmd_review_queue, _cmd_show_patch, _cmd_reject fully implemented + tested
    - --dry-run + --insights-only + no-feedback paths each have a green test
    - `grep -n "^from agent.evolution\|^import agent.evolution" hermes_cli/feedback.py` returns 0 matches (all imports lazy, inside handlers)
    - Existing 4 subcommands still resolve (no regression test fails)
    - `ruff check hermes_cli/feedback.py` clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _cmd_approve (EVOL-04 non-bypassable) + _cmd_rollback (EVOL-05) + isolation grep verification</name>
  <files>
    hermes_cli/feedback.py,
    tests/hermes_cli/test_evolution_cli.py
  </files>
  <read_first>
    - hermes_cli/feedback.py (Task 1 final state — the stub _cmd_approve + _cmd_rollback to replace)
    - agent/evolution/apply.py (Plan 01 — apply_patch_transaction + ApplyError + ApplyResult + build_commit_message signatures)
    - .planning/phases/31-knowledge-evolution-pipeline/31-CONTEXT.md §"Patch Review UI" (D-EVOL-03 approve/reject + --yes flag spec)
    - .planning/phases/31-knowledge-evolution-pipeline/31-CONTEXT.md §"Patch Apply Git Workflow" (D-EVOL-04 6-step atomic transaction — _cmd_approve orchestrates this)
    - skills/movie-experts/_eval/tests/test_gate.py (tmp_git_repo fixture pattern for testing git mutations)
  </read_first>
  <behavior>
    - `hermes feedback approve <patch_id>` (no --yes, no TTY) defaults to non-interactive abort: prints "approval required (pass --yes to confirm)" to stderr + exits non-zero WITHOUT calling apply_patch_transaction
    - `hermes feedback approve <patch_id> --yes` calls apply_patch_transaction with the patch + repo_root + commit_message built via build_commit_message; on success prints "applied <patch_id> as commit <sha[:12]>" and moves the patch to applied.jsonl with the commit_sha
    - `hermes feedback approve <patch_id> --yes` when apply_patch_transaction raises ApplyError: prints "apply failed (working tree restored): <safe message>" to stderr + exits non-zero; queue.jsonl is UNCHANGED (patch stays pending — operator can retry or reject)
    - `hermes feedback approve <unknown_id> --yes` prints "patch <id> not found in pending queue" + exits non-zero
    - `hermes feedback rollback <commit_sha> --yes` runs `git revert <commit_sha> --no-edit` via subprocess (argv-list); on success prints "reverted <commit_sha> as <revert_sha[:12]>" + exits 0; on git failure prints the git stderr tail + exits non-zero
    - `hermes feedback rollback <commit_sha>` (no --yes) prints "rollback requires --yes" + exits non-zero (git revert is destructive — no default-yes path)
    - After Task 2 completes: `grep -rn "apply_patch_transaction" run_agent.py agent/ cli.py gateway/ hermes_cli/feedback.py | grep -v "^hermes_cli/feedback.py.*_cmd_approve" | grep -v "tests/" | wc -l` returns 0 — EVOL-04 non-bypassable human-in-loop verified (only _cmd_approve calls apply_patch_transaction)
  </behavior>
  <action>
    Replace the stub `_cmd_approve(args) -> int` from Task 1 with the real implementation:
      1. from agent.evolution import read_queue, move_patch, apply_patch_transaction, ApplyError, ApplyResult, build_commit_message  (LAZY import inside handler)
      2. evolution_dir = _resolve_evolution_dir(); repo_root = _resolve_repo_root()
      3. If not args.yes: print("approval required (pass --yes to confirm)", file=sys.stderr); return 1  (EVOL-04 non-bypassable — no default-yes; --yes is the explicit operator consent)
      4. records = read_queue(evolution_dir=evolution_dir, status="pending"); match = next((r for r in records if r.patch_id == args.patch_id), None)
      5. If not match: print(f"patch {args.patch_id} not found in pending queue", file=sys.stderr); return 1
      6. Write match.unified_diff to a temp .patch file (tempfile.NamedTemporaryFile suffix=".patch" delete=False, write_text with encoding="utf-8"); patch_path = Path(tmp_name)
      7. commit_message = build_commit_message(insight_summary=match.llm_rationale[:72], feedback_ids=match.feedback_chain, eval_verdict=match.eval_gate_score.get("verdict", "unknown"), eval_mean_delta=float(match.eval_gate_score.get("mean_delta", 0.0)))
      8. try: result = apply_patch_transaction(patch_path=patch_path, repo_root=repo_root, commit_message=commit_message)
      9.    except ApplyError as exc: print(f"apply failed (working tree restored): {exc}", file=sys.stderr); return 1
      10.   else: move_patch(patch_id=args.patch_id, target_status="applied", evolution_dir=evolution_dir, commit_sha=result.commit_sha); print(f"applied {args.patch_id} as commit {result.commit_sha[:12]}"); return 0
      11. finally: patch_path.unlink(missing_ok=True)  (always clean up temp patch file)

    Replace the stub `_cmd_rollback(args) -> int` with the real implementation:
      1. If not args.yes: print("rollback requires --yes", file=sys.stderr); return 1  (git revert is destructive — never default-yes)
      2. repo_root = _resolve_repo_root()
      3. result = subprocess.run(["git", "rev-parse", "--verify", args.commit_sha], cwd=str(repo_root), capture_output=True, text=True, encoding="utf-8")  (validate SHA exists first — fail fast before invoking revert)
      4. If result.returncode != 0: print(f"commit {args.commit_sha} not found", file=sys.stderr); return 1
      5. revert_result = subprocess.run(["git", "revert", args.commit_sha, "--no-edit"], cwd=str(repo_root), capture_output=True, text=True, encoding="utf-8")
      6. If revert_result.returncode != 0: print(f"git revert failed:\n{revert_result.stderr[-400:]}", file=sys.stderr); return 1  (tail only — avoid dumping full git output)
      7. sha_result = subprocess.run(["git", "rev-parse", "HEAD"], cwd=str(repo_root), capture_output=True, text=True, encoding="utf-8"); revert_sha = sha_result.stdout.strip()
      8. print(f"reverted {args.commit_sha} as {revert_sha[:12]}")
      9. return 0

    Add tests to `tests/hermes_cli/test_evolution_cli.py`:
      - TestApproveCmdRequiresYes: approve without --yes → exit non-zero + no apply_patch_transaction call (mock the function and assert not called)
      - TestApproveCmdAppliesAndMoves: approve with --yes + valid pending patch → apply_patch_transaction mock returns ApplyResult(commit_sha="abc123...", ...) → assert move_patch called with target_status="applied" + commit_sha + stdout contains "applied ... as commit abc123"
      - TestApproveCmdOnApplyError: apply_patch_transaction mock raises ApplyError → assert exit non-zero + stdout/stderr contains "apply failed" + assert move_patch NOT called (patch stays pending)
      - TestApproveCmdNotFound: approve unknown patch_id → exit non-zero + "not found in pending queue"
      - TestRollbackCmdRequiresYes: rollback without --yes → exit non-zero + git revert not invoked
      - TestRollbackCmdInvalidSha: rollback <invalid-sha> --yes → exit non-zero + "commit ... not found"
      - TestRollbackCmdHappyPath (uses tmp git repo fixture): stage a commit in the tmp repo, rollback it with --yes → exit 0 + revert commit visible in git log
      - TestNonBypassableHumanInLoop: the structural invariant test — grep the codebase for apply_patch_transaction callers, assert ONLY _cmd_approve invokes it. Use this grep logic: `subprocess.run(["grep", "-rn", "apply_patch_transaction", "agent/", "hermes_cli/", "cli.py", "run_agent.py"], capture_output=True, text=True)` and assert every non-test, non-comment line is inside _cmd_approve. (Alternative: use Python's ast to parse hermes_cli/feedback.py and walk for Call nodes whose func id is "apply_patch_transaction", assert each Call's enclosing FunctionDef name == "_cmd_approve".)

    All file open() calls MUST pass encoding="utf-8". All subprocess argv-list. Logging %-formatted.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/hermes_cli/test_evolution_cli.py -x --timeout=30 && python -m pytest tests/agent/evolution/ -x --timeout=30</automated>
  </verify>
  <done>
    - _cmd_approve + _cmd_rollback fully implemented + tested
    - EVOL-04 non-bypassable: TestNonBypassableHumanInLoop test passes (only _cmd_approve calls apply_patch_transaction)
    - All TestApproveCmd* + TestRollbackCmd* tests green; Plan 01 regression tests still green
    - `ruff check hermes_cli/feedback.py` clean
    - `grep -rn "^from agent.evolution\|^import agent.evolution" hermes_cli/feedback.py` returns 0 matches (all imports lazy inside handlers)
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase 31 integration smoke + isolation + RESEARCH.md Open Questions resolution</name>
  <files>
    .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md,
    .planning/phases/31-knowledge-evolution-pipeline/31-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md §"Open Questions" (lines ~998-1012)
    - .planning/phases/31-knowledge-evolution-pipeline/31-VALIDATION.md (full — confirm all ⬜ pending entries can now be flipped to ✅)
    - tests/hermes_cli/test_evolution_cli.py (final state from Tasks 1+2)
  </read_first>
  <action>
    Run the full Phase 31 integration verification commands and confirm:
      1. `python -m pytest tests/agent/evolution/ tests/hermes_cli/test_evolution_cli.py -x --timeout=30` — all green
      2. `ruff check agent/evolution/ hermes_cli/feedback.py` — clean (PLW1514 satisfied)
      3. Isolation grep: `grep -rn "from agent.evolution\|import agent.evolution" run_agent.py agent/conversation_loop.py agent/prompt_builder.py agent/system_prompt.py agent/tool_executor.py agent/curator.py cli.py gateway/ 2>/dev/null | wc -l` returns 0
      4. EVOL-04 structural grep: `grep -rn "apply_patch_transaction" run_agent.py agent/ cli.py gateway/ hermes_cli/ 2>/dev/null | grep -v "tests/" | grep -v "^hermes_cli/feedback.py:.*_cmd_approve"` returns 0 matches (only _cmd_approve calls it)
      5. No shell=True in evolution code: `grep -rn "shell=True" agent/evolution/ hermes_cli/feedback.py` returns 0
      6. Every open() in evolution + feedback.py passes encoding="utf-8": `grep -E "open\([^)]*\)" agent/evolution/*.py hermes_cli/feedback.py | grep -v 'encoding="utf-8"'` returns 0
      7. FOUND-08 spot-check (manual): on a clean working tree, run a dry-run evolve → review-queue (empty) → confirm no SKILL.md bytes changed: `git diff --name-only skills/movie-experts/ | wc -l` returns 0

    Then update RESEARCH.md §"Open Questions" to mark all 3 RESOLVED with the implementation decision:
      - Q1 (retry policy): RESOLVED — surface immediately for P31 (no retry); operator can re-run `evolve`. P32 Curator automated path may add retry via tenacity. Documented in _cmd_evolve error handling.
      - Q2 (stale eval-gate verdict at approve time): RESOLVED — trust insertion-time verdict (per RESEARCH recommendation). Document escape hatch in `hermes feedback reject <id> stale_baseline` workflow + P33 observability surfaces stale patches. No re-run at approve time (would be slow).
      - Q3 (--insights-only flag): RESOLVED — YES, implemented. Skips patch generation + gate; writes insights.jsonl only. Useful for debugging the aggregation prompt before committing to the full pipeline.

    Then update VALIDATION.md: flip every `⬜ pending` in the Per-Task Verification Map to `✅ shipped` for tasks covered by Plans 01 + 02. Set frontmatter `nyquist_compliant: true` + `wave_0_complete: true`. Sign off the Validation Sign-Off checklist items.

    NOTE: This task does NOT run `/gsd:verify-work` — that's the next orchestrator step. This task only ensures the plans' success criteria are locally verified + planning artifacts are updated.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/evolution/ tests/hermes_cli/test_evolution_cli.py --timeout=30 && ruff check agent/evolution/ hermes_cli/feedback.py && [ -z "$(grep -rn 'from agent.evolution\|import agent.evolution' run_agent.py agent/conversation_loop.py agent/prompt_builder.py agent/system_prompt.py agent/tool_executor.py agent/curator.py cli.py gateway/ 2>/dev/null)" ] && grep -q "RESOLVED" .planning/phases/31-knowledge-evolution-pipeline/31-RESEARCH.md && echo "phase 31 integration verified"</automated>
  </verify>
  <done>
    - Full Phase 31 test suite (Plan 01 + Plan 02) green
    - Ruff clean across agent/evolution/ + hermes_cli/feedback.py
    - Runtime isolation grep returns 0 matches
    - EVOL-04 structural isolation grep returns 0 matches (only _cmd_approve calls apply_patch_transaction)
    - RESEARCH.md Open Questions all marked RESOLVED with implementation decisions
    - VALIDATION.md Per-Task Verification Map entries flipped to ✅ shipped; frontmatter nyquist_compliant=true
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator stdin → CLI confirmation prompt | Operator consent crosses here for approve/rollback (EVOL-04) |
| CLI handler → agent.evolution engine | CLI tier marshals args; engine tier executes — lazy import preserves module-load isolation |
| CLI handler → gate.py subprocess | Cross-process boundary to hyphenated-path gate.py — argv only, no shell |
| git working tree → mutated commit | The single trust boundary where SKILL.md bytes change — apply_patch_transaction is the sole gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Elevation of Privilege | _cmd_approve as sole apply_patch_transaction caller | mitigate | Structural isolation: TestNonBypassableHumanInLoop test asserts ONLY _cmd_approve calls apply_patch_transaction. No programmatic auto-apply path exists (EVOL-04 non-bypassable). Any future code that calls apply_patch_transaction MUST go through the test or the test fails. |
| T-31-12 | Tampering | _cmd_approve --yes flag | mitigate | Default (no --yes) refuses apply + exits non-zero; --yes is explicit operator consent per CONTEXT.md D-EVOL-03. No default-yes path. |
| T-31-13 | Repudiation | _cmd_approve commit message | mitigate | build_commit_message includes feedback_ids + eval verdict + mean_delta per CONTEXT.md D-EVOL-04 machine-parseable format — full audit trail in git log |
| T-31-14 | Tampering | _cmd_rollback git revert subprocess | mitigate | Requires --yes (no default-yes — git revert is destructive); validates commit_sha exists via git rev-parse --verify BEFORE invoking revert (fail fast); argv-list only |
| T-31-15 | DoS | _cmd_evolve LLM call latency | accept | Operator-invoked synchronous; no timeout enforced (operator can Ctrl-C). AggregationError surfaces malformed output. P32 Curator may add timeout when daemonized. |
| T-31-16 | Information Disclosure | _cmd_show_patch stdout | mitigate | Prints full unified_diff + rationale + feedback_chain to stdout — operator-only surface (CLI), no remote access. Feedback IDs (not text) appear in commit messages per V8. |
| T-31-17 | Tampering | _run_eval_gate subprocess to gate.py | mitigate | argv-list only (no shell=True); gate.py path resolved relative to repo via __file__ (no operator-controllable path); report parsed defensively (JSON parse failure → fail verdict with stderr tail) |
| T-31-18 | Spoofing | fake gate.py substitution | accept | If an attacker can write to skills/movie-experts/_eval/gate.py they already have repo write access — out of scope for v6 threat model (single-operator trusted environment per PROJECT.md) |
| T-31-SC | Tampering | (no new packages installed — all deps reused from Plan 01 + existing pyproject pins) | accept | Same as T-31-SC in Plan 01; no new slopcheck checkpoint needed |

**ASVS L1 control mapping:** V4 (EVOL-04 non-bypassable enforced structurally via T-31-11), V5 (argparse validates all CLI args; Pydantic validates queue records), V7 (ApplyError messages operator-safe; git stderr tails truncated), V8 (feedback IDs not text in commits), V10 (atomic transaction + per-patch invariants).
</threat_model>

<verification>
- `python -m pytest tests/agent/evolution/ tests/hermes_cli/test_evolution_cli.py --timeout=30` — all green (Plan 01 + Plan 02)
- `ruff check agent/evolution/ hermes_cli/feedback.py` — clean (PLW1514 encoding)
- Isolation: `grep -rn "from agent.evolution\|import agent.evolution" run_agent.py agent/conversation_loop.py agent/prompt_builder.py agent/system_prompt.py agent/tool_executor.py agent/curator.py cli.py gateway/ 2>/dev/null | wc -l` returns 0
- EVOL-04 non-bypassable: `grep -rn "apply_patch_transaction" run_agent.py agent/ cli.py gateway/ hermes_cli/ 2>/dev/null | grep -v "tests/" | grep -v "^hermes_cli/feedback.py:.*_cmd_approve"` returns 0 matches
- No shell=True: `grep -rn "shell=True" agent/evolution/ hermes_cli/feedback.py` returns 0
- register_cli preserves 4 existing subcommands: `python -c "import argparse; from hermes_cli.feedback import register_cli; p=argparse.ArgumentParser(); s=p.add_subparsers(dest='feedback_command'); sub=s.add_parser('feedback'); register_cli(sub); import sys; p.parse_args(['feedback','import','--help'])"` exits 0 (or equivalent functional test)
- RESEARCH.md Open Questions all RESOLVED
- VALIDATION.md nyquist_compliant=true
</verification>

<success_criteria>
- EVOL-01 CLI: `hermes feedback evolve --skill <id>` runs end-to-end (mock LLM + mock gate in tests) — generates insights, runs gate, populates queue; --dry-run + --insights-only + no-feedback paths each tested
- EVOL-03 CLI: review-queue + show-patch + reject commands work with filtering + clear not-found handling
- EVOL-04 CLI + structural: approve requires --yes; the TestNonBypassableHumanInLoop test proves apply_patch_transaction is called ONLY from _cmd_approve — no auto-apply path exists anywhere in the codebase
- EVOL-05 CLI: approve performs the 6-step atomic transaction (delegated to Plan 01 apply.apply_patch_transaction); rollback invokes git revert --no-edit with --yes gate + sha validation
- Runtime isolation: zero runtime imports of agent.evolution.* (Plan 01 + Plan 02 together preserve this)
- Existing 4 feedback subcommands (import, watch, submit, rebuild-index) unchanged — TestRegisterCliPreservesExistingSubcommands passes
- Ruff clean; all tests green; RESEARCH open questions closed; VALIDATION sign-off complete
</success_criteria>

<output>
Create `.planning/phases/31-knowledge-evolution-pipeline/31-02-SUMMARY.md` when done. Document:
- hermes_cli/feedback.py changes (6 new subparsers + 6 _cmd_* handlers; existing 4 preserved)
- The lazy-import strategy (all `from agent.evolution import` inside handler bodies — NOT at module top) and why it matters for runtime isolation
- The TestNonBypassableHumanInLoop verification approach (ast-walk or grep) + result
- Test counts per class (TestEvolveCmd*, TestReviewQueueCmd*, TestShowPatchCmd*, TestRejectCmd*, TestApproveCmd*, TestRollbackCmd*, TestNonBypassableHumanInLoop, TestRegisterCliPreservesExistingSubcommands)
- Any Rule 1 bugs auto-fixed
- Confirmation that Plan 01 regression tests still pass after Plan 02 modifications
- Confirmation that the EVOL-04 structural isolation grep returns 0 matches
</output>

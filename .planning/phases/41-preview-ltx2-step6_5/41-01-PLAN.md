---
phase: 41-preview-ltx2-step6_5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/kais-movie-pipeline/references/ltx2-preview-loop.md
  - skills/kais-movie-pipeline/SKILL.md
  - skills/kais-movie-pipeline/references/pipeline-dag.md
autonomous: true
requirements: [PREVIEW-01, PREVIEW-02, PREVIEW-03]
user_setup: []

must_haves:
  truths:
    - "Operator can read ltx2-preview-loop.md and learn the 3-dim check thresholds (composition / framing / pacing) and fallback policy without consulting any other doc"
    - "Pipeline runner invokes LTX2.3 fast-preview automatically after Step 6 storyboard passes; Step 7 is gated on preview pass"
    - "Preview failure (pacing deviation > 15% OR framing deviation > 10%) deterministically triggers re-storyboard fallback up to 2 retries; the 3rd failure routes to operator review via existing BLOCKING-mode Gate (no silent skip)"
    - "Zero expert_id / frontmatter changes across all 16 active movie-experts (FOUND-08 preserved)"
  artifacts:
    - path: "skills/kais-movie-pipeline/references/ltx2-preview-loop.md"
      provides: "LTX2.3 baseline + 3-dim thresholds + prompt template + fallback policy (PREVIEW-01)"
      min_lines: 300
      contains: "composition"
    - path: "skills/kais-movie-pipeline/SKILL.md"
      provides: "Step 6.5 wiring section between Step 6 and Step 7 (PREVIEW-02)"
      contains: "Step 6.5"
    - path: "skills/kais-movie-pipeline/references/pipeline-dag.md"
      provides: "Step 6.5 additive annotation in DAG + slot flow (p06 → p06.5 → p07)"
      contains: "Step 6.5"
  key_links:
    - from: "skills/kais-movie-pipeline/SKILL.md (Step 6.5 section)"
      to: "skills/kais-movie-pipeline/references/ltx2-preview-loop.md"
      via: "References table entry + inline link"
      pattern: "ltx2-preview-loop\\.md"
    - from: "skills/kais-movie-pipeline/references/ltx2-preview-loop.md (fallback policy section)"
      to: "plugins/review_gates/gate.py (GateMode.BLOCKING)"
      via: "explicit reference to existing BLOCKING-mode gate as operator-review escalation path"
      pattern: "GateMode\\.BLOCKING|review_gates"
    - from: "skills/kais-movie-pipeline/references/pipeline-dag.md (Step 6.5 row)"
      to: "skills/kais-movie-pipeline/references/ltx2-preview-loop.md"
      via: "See Also link"
      pattern: "ltx2-preview-loop\\.md"
---

<objective>
Insert Step 6.5 LTX2.3 fast-preview into the V8.6 13-step pipeline. After Step 6 (storyboard / spatio-temporal script) passes, auto-invoke LTX2.3 ~5s preview to verify composition / framing / pacing before committing GPU budget to Step 7+ (visual production). Preview failure deterministically falls back to Step 6 re-storyboard (max 2 retries); exhaustion routes to operator review via existing BLOCKING-mode Gate.

Purpose: Catch composition / framing / pacing problems in ~5 seconds at near-zero cost (LTX2.3 ~$0.10/clip) before committing to Step 7-13 GPU-heavy production. Closes the "storyboard → render" feedback gap with a deterministic failure path that never silently swallows.

Output:
- NEW ref `references/ltx2-preview-loop.md` (~300-450 lines, bilingual EN headings + 中文 body)
- PATCH `SKILL.md` body — new Step 6.5 section + References table entry (zero frontmatter)
- PATCH `references/pipeline-dag.md` — Step 6.5 additive annotation + slot flow edge

This plan is pure skill content (markdown). No Hermes core Python/JS code changes. No adapter code (live GPU generation testing is V9-FUTURE-02, explicitly deferred per operator-action-handoff SC#4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Phase 41 source artifacts — read these before writing any task deliverable
@skills/kais-movie-pipeline/SKILL.md
@skills/kais-movie-pipeline/references/pipeline-dag.md
@skills/kais-movie-pipeline/references/asset-bus-schema.md
@skills/kais-movie-pipeline/references/review-gates.md

# Existing LTX2.3 baseline + model matrix — already documents ltx-2.3 row in 6-model catalog
@skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md

# E-Konte 5-layer format — storyboard output schema consumed by Step 6.5 preview
@skills/movie-experts/cinematographer/references/e-konte-format.md

# Review gate state machine (Phase 34) — BLOCKING mode is the operator-review escalation path
@plugins/review_gates/gate.py

<interfaces>
<!-- Existing contracts the executor needs. No codebase exploration required. -->

From skills/kais-movie-pipeline/SKILL.md (References table — current 7 rows):
- The new ref MUST be added as row 8 in the References table
- Format: `| \`references/ltx2-preview-loop.md\` | <when-to-read> | <contents> |`

From skills/kais-movie-pipeline/SKILL.md (Phase ↔ Expert Mapping table):
- Step 6 row: p06_spatio_temporal_script | Step 6 | 运镜+终审 | screenplay + cinematographer + script_auditor | spatio-temporal-script, final-audit | Gate 6 spatio-temporal | Phase 36
- Step 7 row: p07_scene_generation | Step 7 | 视觉+风格化 | visual_executor + prompt_injector + style_genome + colorist | scene-images, style-vector, color-intent | Gate 5 scene-design | Phase 36
- INSERT Step 6.5 row BETWEEN these two (additive, does NOT renumber existing 13 Steps)

From skills/kais-movie-pipeline/references/pipeline-dag.md (Slot Flow Per Edge table):
- Edge p06 → p07 currently: `spatio-temporal-script`, `final-audit` flow
- AFTER Step 6.5: p06 → p06.5 → p07 (preview slot inserted mid-edge)
- New slots: `preview-clips` (JSON, writer=p06.5, reader=operator/gate), `preview-audit` (JSON, writer=p06.5, reader=Gate for escalation)

From skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md:
- LTX2.3 row (existing): Provider=Lightricks (open weight) | max=5s | 768p | 9:16 ✅ | camera_move=✅ 弱 | character_consistency=✅ 弱 | $0.10/5s
- Per-scene complexity heuristic: Simple motion → ltx-2.3 (cost lowest)
- Recommended for preview use case (line 42): "推荐 preview: ltx-2.3(快 + 便宜)"

From plugins/review_gates/gate.py (line 57-66, GateMode enum):
- GateMode.BLOCKING = "blocking"  — synchronous pause on threading.Event (local operator review)
- GateConfig.max_retries: int = 2  (default; CF-05)
- GateMaxRetriesExceeded carries "CONSISTENCY_BLOCKED:" prefix (PIPE-GUARD-01 — no silent swallow)

From skills/movie-experts/cinematographer/references/e-konte-format.md (Layer 2 + Layer 5):
- Layer 2 fields: shot_scale (Mascelli 8-level) / angle / camera_move (12 moves) / lens_mm / motion_intensity / axis_line / screen_direction
- Layer 5 fields: duration_sec / frame_count / fps / cut_transition_type / next_shot_link
- These are the schema fields Step 6.5 preview must verify against (composition = Layer 1+2; framing = Layer 2 + vertical-screen power points; pacing = Layer 5)

From skills/kais-movie-pipeline/references/creative-redlines.md (existing pattern):
- Bilingual EN headings + 中文 body format
- Hard-threshold tables with explicit numbers
- "Source / Copyright / Last-verified" header triplet (matches _shared/ convention)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write references/ltx2-preview-loop.md (PREVIEW-01 baseline doc)</name>
  <files>skills/kais-movie-pipeline/references/ltx2-preview-loop.md</files>
  <action>
Create the new ref at `skills/kais-movie-pipeline/references/ltx2-preview-loop.md` (~300-450 lines, bilingual EN headings + 中文 body). Mirror the layout of existing `references/creative-redlines.md` and `references/platform-specs.md` (Source/Copyright/Last-verified header → Summary → hard-threshold tables → fallback policy → anti-patterns → See Also).

Mandatory sections (per PREVIEW-01 + PREVIEW-03):

1. **Header triplet** (matches `_shared/` convention):
   - `**Source:**` — LTX Video 2.3 docs (Lightricks, 2026-06); CausVid docs (2026-06); Kling 1.6 fast docs (Kuaishou, 2026-06); existing `skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md` (Phase 5 v1.5, the in-repo canonical 6-model catalog); `plugins/review_gates/gate.py` (Phase 34 BLOCKING-mode Gate).
   - `**Copyright:**` — Fair Use — paraphrased capability specs + threshold rationale; no proprietary model internals verbatim. See LICENSE.md.
   - `**Last-verified:**` — 2026-06-26

2. **Summary** — state the purpose: this ref is the **Step 6.5 fast-preview baseline authority** for the V8.6 pipeline. It documents (a) why LTX2.3 is the preview model (cost + speed), (b) what the 3-dim check thresholds are (composition / framing / pacing), (c) the deterministic fallback policy (max 2 retries then BLOCKING-gate escalation). Explicitly note: V9-FUTURE-02 (live LTX2.3 generation validation) is deferred — this baseline doc ships without runtime GPU validation.

3. **Section: 模型选型 (Model Selection)** — 3-row table for LTX2.3 / CausVid / Kling 1.6 fast. Per row: Provider / 最大时长 / 分辨率 / 9:16 vertical / 成本 per 5s / 推荐场景. Below the table, a heuristic paragraph explaining why LTX2.3 is the **default** for Step 6.5 preview (5s budget + $0.10 cost + open-weight). CausVid = alternative if causal-video speed needed; Kling 1.6 fast = alternative for CN-native prompt fidelity. **All 3 models exist in the 2026 Hermes catalog or are documented in `video-gen-model-matrix.md`.** Cross-reference that ref (do NOT duplicate the full 6-model matrix here — Step 6.5 only concerns the 3 fast-preview candidates).

4. **Section: ~5s 生成预算 (Generation Budget)** — short subsection explaining the 5s preview window: short enough to fail fast (operator waits <30s wall-clock for preview batch of 3-5 key shots), long enough to exercise composition + framing + pacing (1-2 cuts detectable in 5s @ 24fps = 120 frames). Note: LTX2.3 max = 5s exactly matches this budget; CausVid and Kling 1.6 fast allow longer but we cap at 5s for budget discipline.

5. **Section: 3 维校验阈值 (3-Dimension Check Thresholds)** — THE central section. Three H3 subsections, each with a hard-threshold table:

   - **### composition (构图)** — verifies E-Konte Layer 1 (stage_geometry / character_blocking) + Layer 2 (shot_scale / axis_line) are respected in the generated preview. Threshold: **composition_match_score ≥ 0.80** (vs storyboard intent). Method: compare storyboard's declared stage_geometry + character_blocking + shot_scale against frames sampled from preview (use existing visual_executor consistency stack — CLIP-T for scene composition, Object IoU for prop positions). Sub-bullet list of what fails: missing prop, wrong shot_scale, axis_line violation.
   - **### framing (取景)** — verifies E-Konte Layer 2 vertical-screen framing rules from `vertical-screen-framing.md` (9:16 power points / headroom / subtitle safe area). Threshold: **framing_deviation ≤ 10%** (per SC#3 hard number). Method: detect subject bounding box, check against 9:16 power-point grid; headroom ratio 0.05-0.15; subtitle-safe-area violation (text-critical region obscured).
   - **### pacing (节奏)** — verifies E-Konte Layer 5 (duration_sec / cut_transition_type / frame_count) matches storyboard intent. Threshold: **pacing_deviation ≤ 15%** (per SC#3 hard number). Method: detect cut points in preview, compare count + intervals against storyboard's `next_shot_link` chain + `duration_sec` per shot. Pacing fail modes: too few cuts (draggy), too many cuts (chaotic), wrong transition type (hard_cut where dissolve intended).

   Each threshold table MUST contain: 维度 / 阈值 (hard number) / 测量方法 / 失败示例. **The hard numbers `composition ≥ 0.80`, `framing ≤ 10%`, `pacing ≤ 15%` are load-bearing** — SC#3 explicitly cites "pacing > 15% OR framing > 10%" as the auto-fallback trigger.

6. **Section: Prompt 模板 (Preview Prompt Template)** — YAML-coded prompt assembly showing how Step 6.5 translates the storyboard (E-Konte 5-layer JSON from Step 6) into an LTX2.3 generation call. Mirror the YAML prompt structure in `video-gen-model-matrix.md` heuristic 4 (短剧 Vertical Generation 协议). Template fields: `aspect_ratio: "9:16"`, `resolution: "768×1344"` (LTX2.3 768p vertical), `duration_sec: 5`, `model: ltx-2.3`, prompt body assembled from E-Konte Layer 1+2+3 (stage_geometry + camera_move + character pose), seed (per-scene baseline).

7. **Section: 失败回退策略 (Fallback Policy)** — THE PREVIEW-03 contract. Use a state-machine-style table:
   - State `preview_pass`: all 3 dims within threshold → advance to Step 7 (p07_scene_generation)
   - State `preview_fail_retry_1`: pacing > 15% OR framing > 10% OR composition < 0.80 → re-invoke Step 6 (p06) with audit feedback → re-render preview
   - State `preview_fail_retry_2`: same trigger, 2nd retry → re-invoke Step 6 with augmented feedback (cumulative miss pattern)
   - State `preview_fail_exhausted`: 3rd failure (attempt > max_retries=2) → **route to operator review via existing `plugins/review_gates/gate.py` BLOCKING mode** (per SC#3 — "no silent skip"). The gate fires with `suggested_action: "rollback:p06_spatio_temporal_script"` and `decision: "contest"` semantics; operator resolves approve (proceed anyway) / reject (fail episode) / contest (manual re-storyboard).

   CRITICAL: explicitly state the escalation uses the **existing Phase 34 BLOCKING-mode Gate** — NOT a new gate. Reference `plugins/review_gates/gate.py:GateMode.BLOCKING` and the `GateMaxRetriesExceeded` CONSISTENCY_BLOCKED semantics. Do NOT invent new gate infrastructure. Also note: `GateConfig.max_retries` default is already 2 (matches our policy); no override needed.

8. **Section: I/O 契约 (Step 6.5 Input/Output)** — short table showing:
   - Input slots (from p06): `spatio-temporal-script`, `final-audit`, `character-bible` (cross-cutting read), `e-konte-sheets` (from p09 — but for preview we use storyboard intent pre-shot-breakdown; clarify this)
   - Output slots (new): `preview-clips` (JSON — paths to LTX2.3-generated ~5s preview mp4s, 1-3 per scene), `preview-audit` (JSON — 3-dim scores per clip + pass/fail/retry state)
   - Note: Step 6.5 does NOT add a gate of its own (Gate 6 spatio-temporal already covers Step 6; Gate 5 scene-design covers Step 7). The BLOCKING-mode operator review is an **exception path** triggered only on retry exhaustion, not a new gate slot in the 8-gate V8.6 structure.

9. **Section: Anti-Patterns / What NOT to do** — bullet list:
   - ❌ Do NOT skip preview even when storyboard self-audit passes — preview catches what static audit cannot (motion coherence, framing in motion, pacing felt-rate).
   - ❌ Do NOT use veo3.1 / kling-v3-4k for preview — they are the production-grade Step 10/11 models; cost 4-7× LTX2.3 for the same preview signal.
   - ❌ Do NOT silently exceed max_retries=2 — operator review via BLOCKING gate is non-bypassable on exhaustion (preserves PIPE-GUARD-01 semantics).
   - ❌ Do NOT invent a new gate_id — escalation rides on existing review_gates framework.
   - ❌ Do NOT validate against real GPU output in v9.0 — V9-FUTURE-02 deferred; this baseline doc + Step 6.5 wiring is the v9.0 deliverable only.

10. **Section: See Also** — links to: `pipeline-dag.md` (slot flow), `asset-bus-schema.md` (preview-clips / preview-audit slot schema — to be added by this Task or Task 3), `review-gates.md` (8-gate structure — Step 6.5 does NOT add a 9th gate), `_shared/v86-pipeline-mapping.md` (canonical V8.6 source), `video-gen-model-matrix.md` (full 6-model matrix in movie-experts/visual_executor).

11. **Footer:** `Ref author: kais-movie-pipeline team (Phase 41 PREVIEW-01)` + `Source date: 2026-06-26` + `Verified against: LTX Video 2.3 docs / video-gen-model-matrix.md (Phase 5) / e-konte-format.md (Phase 20) / gate.py (Phase 34)`.

Bilingual rule (load-bearing): English H2/H3 headings + English technical terms (composition / framing / pacing / preview / fallback); 中文 body paragraphs explaining rationale. Match the format of `creative-redlines.md` exactly.

Word count target: ~300-450 lines (similar to `creative-redlines.md` at 117 lines × 3-4x depth for the 3-dim threshold + state machine + prompt template).
  </action>
  <verify>
    <automated>test -f /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md && LINES=$(wc -l < /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md) && [ "$LINES" -ge 300 ] && echo "OK: $LINES lines" || (echo "FAIL: $LINES lines (< 300)"; exit 1)</automated>
    <automated>grep -c "composition\|framing\|pacing" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md | awk '{ if ($1 >= 9) print "OK: 3-dim keywords present ("$1" hits)"; else { print "FAIL: only "$1" 3-dim keyword hits (< 9)"; exit 1 } }'</automated>
    <automated>grep -E "15%|10%|0\.80" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md | head -5 && echo "---thresholds present---"</automated>
    <automated>grep -E "GateMode\.BLOCKING|review_gates|CONSISTENCY_BLOCKED|max_retries" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md | head -5 && echo "---existing-gate-reuse present---"</automated>
    <automated>grep -E "V9-FUTURE-02|deferred" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md | head -3 && echo "---operator-action-handoff present---"</automated>
  </verify>
  <done>
    - File `skills/kais-movie-pipeline/references/ltx2-preview-loop.md` exists, 300-450 lines
    - Contains all 3 hard thresholds: composition ≥ 0.80, framing ≤ 10%, pacing ≤ 15% (PREVIEW-01)
    - Contains explicit reference to existing `plugins/review_gates/gate.py` BLOCKING mode as the exhaustion escalation path (PREVIEW-03)
    - Contains prompt template YAML block (PREVIEW-01)
    - Contains explicit V9-FUTURE-02 deferral note (operator-action-handoff, SC#4)
    - Bilingual EN headings + 中文 body (matches `creative-redlines.md` format)
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch SKILL.md body — Step 6.5 wiring (PREVIEW-02, FOUND-08 preserved)</name>
  <files>skills/kais-movie-pipeline/SKILL.md</files>
  <action>
PATCH `skills/kais-movie-pipeline/SKILL.md` **body only** — zero frontmatter changes (FOUND-08 frozen rule; verified by Task 4 byte-diff). Three additive edits:

**Edit A — References table (add row 8):**
Insert a new row at the end of the existing 7-row References table (after the `genre-anchor-urban-fantasy.md` row). New row:
`| \`references/ltx2-preview-loop.md\` | Before running Step 6.5 preview or debugging a preview-fail loop | LTX2.3 baseline (model selection / ~5s budget / 3-dim thresholds composition+framing+pacing / prompt template / fallback policy max 2 retries → BLOCKING gate) |`

**Edit B — Pipeline DAG section (Step 6.5 in dependency graph):**
In the `## Pipeline DAG` section's ASCII fallback block (and optionally the Mermaid graph), insert a Step 6.5 node between p06 and p07. Minimal change: in the ASCII fallback, change the line `[p06] → [p07 scene_gen] → ...` to `[p06] → [p06.5 preview] → [p07 scene_gen] → ...` and add a one-line note immediately below: `**Step 6.5 (Phase 41 additive):** LTX2.3 ~5s fast-preview — auto-invoked after Step 6 passes; gates Step 7 on preview pass. See \`references/ltx2-preview-loop.md\`.`

Do NOT modify the Mermaid `classDef`/`class` assignments for existing p01-p13 nodes. If adding p06.5 to Mermaid, give it a new class `classDef p41 fill:#fef,stroke:#939,stroke-width:2px,stroke-dasharray: 2 2` (visually distinct from p35 green + p36 blue). Keep this minimal — the ASCII fallback is the load-bearing artifact.

**Edit C — Phase ↔ Expert Mapping table (add Step 6.5 row):**
Insert a new row BETWEEN the existing `p06_spatio_temporal_script` row (Step 6) and the `p07_scene_generation` row (Step 7). New row:
`| \`p06_5_ltx2_preview\` | Step 6.5 | LTX2.3 fast-preview (composition+framing+pacing check, 5s budget) | visual_executor (animator sub-step, preview model selection) + cinematographer (composition/framing audit) | preview-clips, preview-audit | (escalation only) review_gates BLOCKING | **Phase 41** |`

Also append to the table's caption or immediately below: `**Step 6.5 note (Phase 41 additive):** preview-clips + preview-audit are new AssetBus slots; the escalation gate is NOT a new gate_id — it routes through the existing \`plugins/review_gates/\` BLOCKING mode on retry exhaustion (max_retries=2 per \`GateConfig\` default). See \`references/ltx2-preview-loop.md\` §Fallback Policy.`

**Edit D — Add a new top-level section `## Step 6.5 Fast-Preview (LTX2.3)`:**
Insert this new H2 section IMMEDIATELY AFTER the `## Review Gates` section and BEFORE `## Asset Bus Schema`. The section should be ~30-50 lines containing:

- One-line intro: `Step 6.5 is a Phase 41 additive insertion between Step 6 (storyboard) and Step 7 (visual production). It runs an LTX2.3 ~5s fast-preview to catch composition / framing / pacing problems before committing GPU budget to Step 7-13.`
- **Trigger:** Auto-invoked after Gate 6 (spatio-temporal) approves Step 6 output.
- **Inputs:** Reads `spatio-temporal-script`, `final-audit` (from p06), `character-bible` (cross-cutting).
- **Process:** Translates E-Konte 5-layer storyboard into LTX2.3 prompt (per `references/ltx2-preview-loop.md` §Prompt Template) → generates 1-3 preview clips per scene (~5s each, $0.10/clip) → runs 3-dim audit (composition ≥ 0.80 / framing ≤ 10% / pacing ≤ 15%).
- **Outputs:** Writes `preview-clips` (paths) + `preview-audit` (3-dim scores + pass/fail state) to AssetBus.
- **Fallback (load-bearing — PREVIEW-03):** On any dim miss → re-invoke Step 6 with audit feedback; max 2 retries (matches `GateConfig.max_retries` default). On exhaustion (3rd failure) → route to operator review via existing `plugins/review_gates/` BLOCKING mode (NOT a new gate; non-bypassable per PIPE-GUARD-01). No silent skip.
- **v9.0 scope boundary:** Live GPU generation testing is operator-side (V9-FUTURE-02 deferred). This wiring + `references/ltx2-preview-loop.md` baseline is the v9.0 deliverable.

**FOUND-08 byte-intact guarantee:**
- Zero changes to lines 1-21 (frontmatter between `---` markers)
- Zero changes to `metadata.hermes.expert_id: kais-movie-pipeline`
- Zero changes to `metadata.hermes.related_skills` list
- All edits are ADDITIVE body inserts (new table row, new section, ASCII line update)
- This will be verified by Task 4 git byte-diff against pre-edit SKILL.md
  </action>
  <verify>
    <automated>grep -c "Step 6.5" /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | awk '{ if ($1 >= 4) print "OK: Step 6.5 referenced "$1" times"; else { print "FAIL: only "$1" Step 6.5 references (< 4)"; exit 1 } }'</automated>
    <automated>grep -c "ltx2-preview-loop.md" /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | awk '{ if ($1 >= 2) print "OK: ref linked "$1" times"; else { print "FAIL: only "$1" ref links (< 2)"; exit 1 } }'</automated>
    <automated>grep -E "p06_5_ltx2_preview|p06\.5 preview|Step 6.5" /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | head -10 && echo "---Step 6.5 wiring present---"</automated>
    <automated>awk '/^---$/{c++; next} c==1{print}' /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | grep -E "^expert_id:|^related_skills:" | head -5 && echo "---frontmatter intact (Task 4 will byte-verify)---"</automated>
  </verify>
  <done>
    - SKILL.md References table has 8 rows (added ltx2-preview-loop.md)
    - SKILL.md Phase ↔ Expert Mapping table has 14 rows (added p06_5_ltx2_preview between p06 and p07)
    - SKILL.md has a new `## Step 6.5 Fast-Preview (LTX2.3)` H2 section (~30-50 lines)
    - SKILL.md ASCII DAG fallback shows p06 → p06.5 → p07
    - Frontmatter (lines 1-21 between `---` markers) byte-identical to pre-edit (FOUND-08 — verified by Task 4)
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch references/pipeline-dag.md — Step 6.5 additive annotation</name>
  <files>skills/kais-movie-pipeline/references/pipeline-dag.md</files>
  <action>
PATCH `skills/kais-movie-pipeline/references/pipeline-dag.md` **body only** to annotate the Step 6.5 additive extension. Three small edits:

**Edit A — Update Last-verified stamp:**
Change line 5 from `**Last-verified:** 2026-06-26 (Phase 36-05 Wave 2 refinement — full slot-flow table added)` to `**Last-verified:** 2026-06-26 (Phase 41 Step 6.5 additive annotation — preview slot inserted mid-edge p06→p07)`.

**Edit B — Add Step 6.5 to the 13-Steps Mermaid + ASCII:**
In the Mermaid graph (lines 22-49), insert `S65[Step 6.5<br/>LTX2.3 fast-preview<br/>composition+framing+pacing]` between S6 and S7. Update edge `S6 --> S7` to `S6 --> S65 --> S7`. Use a distinct classDef: `classDef p41 fill:#fef,stroke:#939,stroke-width:2px,stroke-dasharray: 2 2` and `class S65 p41`. Do NOT modify any other Step node.

In the ASCII fallback table (lines 53-67), add a new row between the Step 6 row and the Step 7 row:
`| **Step 6.5** | LTX2.3 fast-preview (composition+framing+pacing check) | `visual_executor` (animator) + `cinematographer` (audit) | — | (Phase 41 additive) |`

Below the table, add a note: `**Step 6.5 (Phase 41 additive):** Inserted between Step 6 (storyboard) and Step 7 (visual production). Not part of the original V8.6 13-Step numbering — additive extension per Phase 41 PREVIEW. See \`ltx2-preview-loop.md\` for baseline + fallback policy.`

**Edit C — Update Slot Flow Per Edge table:**
In the "Slot Flow Per Edge" table (lines 109-123), replace the row `| p06 → p07 | ...` with TWO rows:
- `| p06 → p06.5 | \`spatio-temporal-script\`, \`final-audit\` | p06 → \`p06_spatio_temporal_script\` (Gate 6 boundary) | p06.5 → \`p06_5_ltx2_preview\` (Phase 41) |`
- `| p06.5 → p07 | \`preview-clips\`, \`preview-audit\` (+ pass-through \`spatio-temporal-script\`) | p06.5 → \`p06_5_ltx2_preview\` (Phase 41) | p07 → \`p07_scene_generation\` (Gate 5 boundary) |`

Below the table, add to the "Slots not on the DAG spine (side outputs)" list: `preview-clips`, `preview-audit` — written by p06.5, consumed by review gate escalation path (only on retry exhaustion) and operator audit.

**Edit D — Update See Also:**
In the See Also section (lines 152-157), add a new bullet: `- [\`ltx2-preview-loop.md\`](./ltx2-preview-loop.md) — Step 6.5 baseline + 3-dim thresholds + fallback policy (Phase 41)`.

Do NOT touch the Atomic Operations table (lines 79-89) — Step 6.5 is NOT one of the 6 V8.6 atomic operations; it is an additive Phase 41 insertion. Adding it there would corrupt the V8.6 canonical record.

**FOUND-08 / V8.6 numbering preservation:**
- Zero changes to the 13-Step numbering (Step 6.5 is additive, displayed as a separate row)
- Zero changes to the Atomic Operations table
- Zero changes to Step 1-13 node class assignments in Mermaid
  </action>
  <verify>
    <automated>grep -c "Step 6.5\|p06\.5\|p06_5" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md | awk '{ if ($1 >= 4) print "OK: Step 6.5 referenced "$1" times"; else { print "FAIL: only "$1" Step 6.5 refs (< 4)"; exit 1 } }'</automated>
    <automated>grep -c "ltx2-preview-loop.md" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md | awk '{ if ($1 >= 1) print "OK: linked to ltx2-preview-loop.md"; else { print "FAIL: no link to ltx2-preview-loop.md"; exit 1 } }'</automated>
    <automated>grep -E "Last-verified.*Phase 41" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md && echo "---Last-verified updated---"</automated>
    <automated>grep -c "Atomic Operations" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md | awk '{ if ($1 == 1) print "OK: Atomic Operations section preserved"; else { print "FAIL: Atomic Operations section corrupted"; exit 1 } }'</automated>
  </verify>
  <done>
    - pipeline-dag.md Last-verified stamp updated to Phase 41 annotation
    - Mermaid graph contains S65 node between S6 and S7
    - ASCII fallback table contains Step 6.5 row
    - Slot Flow Per Edge table contains 2 new rows (p06→p06.5, p06.5→p07)
    - Atomic Operations table byte-identical (V8.6 6-atomic-ops preserved)
    - See Also links to ltx2-preview-loop.md
  </done>
</task>

<task type="auto">
  <name>Task 4: Verification — FOUND-08 byte-diff + Step 6.5 I/O contract + fallback policy audit</name>
  <files></files>
  <action>
Run the Phase 41 verification suite. No file writes — pure verification commands. All 4 sub-checks must pass.

**Check 1 — FOUND-08 frontmatter byte-diff (load-bearing, SC#2):**
Verify that the SKILL.md frontmatter (between the two `---` markers, lines 1-21) is byte-identical to the pre-Phase-41 state. Method:
- `git show HEAD:skills/kais-movie-pipeline/SKILL.md` (or the appropriate pre-phase commit) → extract lines 1-21 (frontmatter block)
- `awk '/^---$/{c++; if (c==2) exit} {print}' skills/kais-movie-pipeline/SKILL.md` → current frontmatter
- Diff them — must be empty.
- Also verify `expert_id: kais-movie-pipeline` line is unchanged.
- Also verify `related_skills: [...]` list is unchanged (still the 15-expert list).
- Also verify `metadata.hermes.pipeline.step_count: 13` is unchanged (Step 6.5 is NOT a renumbering — V8.6 13-step preserved).

If git HEAD doesn't have a pre-Phase-41 snapshot (e.g., prior tasks in this plan already committed), use `git log --oneline -10 skills/kais-movie-pipeline/SKILL.md` to find the last pre-Phase-41 commit and `git show <sha>:skills/kais-movie-pipeline/SKILL.md` for the baseline.

**Check 2 — Step 6.5 I/O contract完整性 (PREVIEW-02):**
Verify the Step 6.5 wiring forms a complete I/O contract:
- `preview-clips` slot is documented in BOTH `ltx2-preview-loop.md` AND referenced in `SKILL.md` Step 6.5 section AND listed in `pipeline-dag.md` Slot Flow Per Edge table.
- `preview-audit` slot likewise documented in all 3 files.
- The 3 hard thresholds (composition ≥ 0.80 / framing ≤ 10% / pacing ≤ 15%) appear in BOTH `ltx2-preview-loop.md` AND `SKILL.md` Step 6.5 section (the SKILL.md ref need not duplicate the numbers, but must reference "3-dim thresholds per ltx2-preview-loop.md").

**Check 3 — Fallback policy determinism (PREVIEW-03):**
Verify the fallback chain is documented deterministically with no silent-skip path:
- `ltx2-preview-loop.md` §Fallback Policy documents: pass / fail-retry-1 / fail-retry-2 / fail-exhausted → BLOCKING gate (4 states).
- `ltx2-preview-loop.md` explicitly references `plugins/review_gates/gate.py` `GateMode.BLOCKING` and `GateConfig.max_retries=2` (existing, not new).
- `ltx2-preview-loop.md` explicitly references the `CONSISTENCY_BLOCKED` / PIPE-GUARD-01 semantics from `GateMaxRetriesExceeded`.
- `SKILL.md` Step 6.5 section restates the fallback summary with link to ref.
- The escalation is NOT a new gate_id (verify `review-gates.md` 8-gate table is unchanged — Step 6.5 escalation rides existing infrastructure).

**Check 4 — V9-FUTURE-02 operator-action-handoff (SC#4):**
Verify the deferral is documented in BOTH `ltx2-preview-loop.md` (explicit V9-FUTURE-02 note) AND `SKILL.md` Step 6.5 section ("live GPU generation testing is operator-side; v9.0 ships baseline doc + wiring only").

**On any check failure:** report which check failed + which file + which line range, and either fix in-place (if trivial typo) or flag for plan revision (if structural).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && git diff HEAD -- skills/kais-movie-pipeline/SKILL.md | grep -E "^-expert_id:|^-related_skills:|^-metadata:|^-step_count:" | head -5 && echo "FAIL: frontmatter mutated" || echo "OK: no frontmatter deletions in diff"</automated>
    <automated>awk '/^---$/{c++; if (c==2) exit} {print}' /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | grep -E "^expert_id: kais-movie-pipeline$" && echo "OK: expert_id intact"</automated>
    <automated>awk '/^---$/{c++; if (c==2) exit} {print}' /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | grep -E "step_count: 13" && echo "OK: V8.6 13-step preserved"</automated>
    <automated>grep -E "preview-clips|preview-audit" /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/pipeline-dag.md | wc -l | awk '{ if ($1 >= 6) print "OK: slots referenced across all 3 files ("$1" hits)"; else { print "FAIL: only "$1" slot references (< 6)"; exit 1 } }'</automated>
    <automated>grep -E "0\.80|10%|15%" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md | wc -l | awk '{ if ($1 >= 3) print "OK: 3 thresholds present"; else { print "FAIL: thresholds incomplete ("$1" hits)"; exit 1 } }'</automated>
    <automated>grep -E "GateMode\.BLOCKING|GateConfig\.max_retries|CONSISTENCY_BLOCKED|PIPE-GUARD-01" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md | wc -l | awk '{ if ($1 >= 2) print "OK: existing-gate-reuse documented ("$1" hits)"; else { print "FAIL: existing-gate-reuse not documented"; exit 1 } }'</automated>
    <automated>grep -c "V9-FUTURE-02" /data/workspace/hermes-agent/skills/kais-movie-pipeline/references/ltx2-preview-loop.md /data/workspace/hermes-agent/skills/kais-movie-pipeline/SKILL.md | awk -F: '{ sum += $2 } END { if (sum >= 2) print "OK: V9-FUTURE-02 deferral documented"; else { print "FAIL: V9-FUTURE-02 not documented"; exit 1 } }'</automated>
  </verify>
  <done>
    - Check 1 PASS: SKILL.md frontmatter byte-identical to pre-Phase-41 (expert_id, related_skills, step_count: 13 all unchanged) — FOUND-08 preserved
    - Check 2 PASS: preview-clips + preview-audit slots documented in SKILL.md + ltx2-preview-loop.md + pipeline-dag.md (3 files consistent)
    - Check 3 PASS: Fallback policy in ltx2-preview-loop.md references existing GateMode.BLOCKING + GateConfig.max_retries + CONSISTENCY_BLOCKED; no new gate_id added
    - Check 4 PASS: V9-FUTURE-02 deferral documented in both ltx2-preview-loop.md + SKILL.md
    - All 3 requirements (PREVIEW-01 / PREVIEW-02 / PREVIEW-03) verifiable from the shipped artifacts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storyboard → preview prompt | E-Konte 5-layer JSON crosses from deterministic storyboard (Step 6 output) into LTX2.3 generation prompt (Step 6.5 input) — prompt-injection-resistant structured-data translation |
| preview → audit | LTX2.3-generated mp4 clips cross into the 3-dim audit (composition/framing/pacing) — model output is untrusted until audited |
| audit fail → operator review | When max_retries=2 exhausted, escalation crosses from autonomous retry loop into human-gate (BLOCKING mode) — must be non-bypassable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | preview-audit scores (LLM/computer-vision derived) | accept | Audit scores are operator-advisory only; final escalation is human-gate. No autonomous irreversible action on audit alone (max_retries bound). |
| T-41-02 | Denial of Service | LTX2.3 generation loop (retry storm) | mitigate | Hard cap max_retries=2 (matches GateConfig default); exhaustion routes to BLOCKING gate (operator breaks loop). Per PREVIEW-03. |
| T-41-03 | Repudiation | preview-clips provenance | mitigate | preview-clips slot is envelope-wrapped (AssetBus V3 content_hash + derived_from spatio-temporal-script) — provenance chain preserved per Phase 33 pattern. |
| T-41-04 | Information Disclosure | LTX2.3 prompt content (story IP) | accept | LTX2.3 calls go through existing kais_aigc plugin (KAIS_API_KEY auth); no new exfiltration surface introduced by Step 6.5 (same transport as Step 10/11). |
| T-41-05 | Elevation of Privilege | operator-review gate bypass | mitigate | BLOCKING mode is non-bypassable per PIPE-GUARD-01 (GateMaxRetriesExceeded carries CONSISTENCY_BLOCKED prefix; runner marks episode failed). No silent skip path. |
| T-41-SC | Tampering | (none — no package installs in this phase; pure markdown deliverables) | accept | N/A — Phase 41 ships only .md files; no npm/pip/cargo install tasks. |

Package legitimacy gate: N/A — Phase 41 has zero package install tasks (pure skill content: 1 new ref + 2 SKILL.md/DAG body patches). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages to checkpoint.
</threat_model>

<verification>
**Phase 41 PREVIEW — full verification suite (run after Task 4):**

1. **File existence + size:**
   - `skills/kais-movie-pipeline/references/ltx2-preview-loop.md` exists, 300-450 lines
   - `skills/kais-movie-pipeline/SKILL.md` body modified (frontmatter byte-identical)
   - `skills/kais-movie-pipeline/references/pipeline-dag.md` body modified (Atomic Operations preserved)

2. **PREVIEW-01 coverage:**
   - 3-dim thresholds (composition ≥ 0.80 / framing ≤ 10% / pacing ≤ 15%) present with hard numbers in ltx2-preview-loop.md
   - Model selection table (LTX2.3 / CausVid / Kling 1.6 fast) present
   - Prompt template YAML present
   - ~5s generation budget documented

3. **PREVIEW-02 coverage:**
   - SKILL.md has `## Step 6.5 Fast-Preview (LTX2.3)` H2 section
   - Phase ↔ Expert Mapping table has `p06_5_ltx2_preview` row between p06 and p07
   - References table has `ltx2-preview-loop.md` row
   - Pipeline DAG ASCII + Mermaid shows p06 → p06.5 → p07

4. **PREVIEW-03 coverage:**
   - ltx2-preview-loop.md §Fallback Policy has 4 states (pass / retry-1 / retry-2 / exhausted→BLOCKING)
   - Explicit reference to existing `plugins/review_gates/gate.py` GateMode.BLOCKING + max_retries=2
   - Explicit reference to CONSISTENCY_BLOCKED / PIPE-GUARD-01 (non-bypassable semantics)
   - SKILL.md Step 6.5 section restates the non-silent-skip guarantee

5. **FOUND-08 preserved:**
   - SKILL.md frontmatter (lines 1-21) byte-identical to pre-Phase-41
   - `expert_id: kais-movie-pipeline` unchanged
   - `metadata.hermes.pipeline.step_count: 13` unchanged (V8.6 numbering preserved — Step 6.5 is additive)
   - `related_skills` list unchanged (15 experts)

6. **Operator-action-handoff (SC#4):**
   - V9-FUTURE-02 deferral explicitly documented in ltx2-preview-loop.md + SKILL.md
   - "v9.0 ships baseline doc + Step 6.5 wiring only" stated
   - Live GPU testing is operator-side
</verification>

<success_criteria>
**Phase 41 PREVIEW is complete when ALL of the following are TRUE:**

1. **PREVIEW-01 ✓** — Operator can open `skills/kais-movie-pipeline/references/ltx2-preview-loop.md` and read: (a) why LTX2.3 is the preview model vs CausVid / Kling 1.6 fast, (b) the 3 hard thresholds (composition ≥ 0.80, framing ≤ 10%, pacing ≤ 15%), (c) the prompt template, (d) the ~5s generation budget rationale.

2. **PREVIEW-02 ✓** — `kais-movie-pipeline/SKILL.md` documents that after Step 6 passes Gate 6, the pipeline auto-invokes Step 6.5 LTX2.3 fast-preview; Step 7 (p07_scene_generation) is gated on preview pass. The wiring is visible in 3 places: References table, Phase ↔ Expert Mapping table, and the new `## Step 6.5 Fast-Preview (LTX2.3)` section.

3. **PREVIEW-03 ✓** — Failure path is deterministic: any dim miss → re-Story-Step-6 with audit feedback; max 2 retries (matches GateConfig default); 3rd failure routes to operator review via existing `plugins/review_gates/` BLOCKING mode (no new gate_id; no silent skip; PIPE-GUARD-01 semantics preserved).

4. **SC#2 (FOUND-08) ✓** — SKILL.md frontmatter byte-identical to pre-Phase-41 (zero expert_id / related_skills / step_count changes). Verified by Task 4 git byte-diff.

5. **SC#4 (operator-action-handoff) ✓** — Live GPU generation testing explicitly deferred to operator (V9-FUTURE-02); v9.0 ships baseline doc + Step 6.5 wiring only. Documented in both ltx2-preview-loop.md and SKILL.md.

6. **V8.6 13-step preserved ✓** — Step 6.5 is additive; Atomic Operations table in pipeline-dag.md byte-identical; step_count: 13 in SKILL.md frontmatter unchanged.
</success_criteria>

<output>
Create `.planning/phases/41-preview-ltx2-step6_5/41-01-SUMMARY.md` when done.

Summary should include:
- Files created/modified (3 paths)
- Line counts (ltx2-preview-loop.md target 300-450)
- FOUND-08 byte-diff result (PASS/FAIL with commit shas)
- PREVIEW-01/02/03 coverage check (3 ✓)
- V9-FUTURE-02 deferral note (operator-action-handoff acknowledged)
- Any deviations from plan + rationale
</output>

---
phase: 54-eval-harness-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/memory_scoped_retrieval.py
  - tests/v11-latency-bench/__init__.py
  - tests/v11-latency-bench/conftest.py
  - tests/v11-latency-bench/test_retrieval_timing.py
  - tests/v11-latency-bench/fixtures/seed_100_records.py
  - tests/v11-latency-bench/fixtures/seed_500_records.py
  - tests/v11-latency-bench/fixtures/seed_1000_records.py
  - scripts/run_latency_benchmark.py
  - .planning/research/v11-poc-eval/latency-baseline.md
autonomous: true
requirements: [EVAL-02]
tags: [eval, latency, slo, mem0, perf]

must_haves:
  truths:
    - "Operator runs `scripts/run_latency_benchmark.py` and gets p50/p95/p99 latency for memory_retrieve_scoped"
    - "100 sequential retrievals on a 500-record populated fixture store produce p95 < 500ms (excluding LLM call)"
    - "Benchmark runs at 3 record counts: 100 / 500 / 1000 (per `05-POC-PLAN.md §4.2` task (c))"
    - "Results JSON written with per-count percentiles; baseline doc documents bottleneck analysis"
    - "Timing instrumentation excludes LLM-bound calls (per STACK §7.4 LLM call = 2-15s, not in 500ms budget)"
    - "If p95 > 500ms on 500-record benchmark, script emits documented 物理分区 evaluation trigger per Phase 48 §3"
  artifacts:
    - path: agent/memory_scoped_retrieval.py
      provides: "Timing-instrumented wrapper around memory_arbitration.memory_retrieve_scoped"
      exports: ["timed_retrieval", "LatencySample", "compute_percentiles"]
    - path: scripts/run_latency_benchmark.py
      provides: "CLI: seeds fixture store → 100 sequential retrievals → emits p50/p95/p99 JSON"
    - path: .planning/research/v11-poc-eval/latency-baseline.md
      provides: "Baseline results + bottleneck analysis + 物理分区 trigger verdict"
      min_lines: 60
    - path: tests/v11-latency-bench/test_retrieval_timing.py
      provides: "Unit test verifying percentile computation + timing wrapper correctness"
  key_links:
    - from: agent/memory_scoped_retrieval.py
      to: agent/memory_arbitration.py::memory_retrieve_scoped
      via: "async function call wrapped in time.perf_counter() bracket"
      pattern: "memory_retrieve_scoped"
    - from: scripts/run_latency_benchmark.py
      to: agent/memory_scoped_retrieval.py::timed_retrieval
      via: "function import"
      pattern: "from agent\\.memory_scoped_retrieval import"
---

<objective>
Build EVAL-02 latency SLO benchmark: timing-instrumented `memory_retrieve_scoped` wrapper + benchmark script that seeds a fixture mem0 store at 3 record counts (100/500/1000), runs 100 sequential retrievals each, and produces p50/p95/p99 latency JSON + a baseline doc with bottleneck analysis.

Purpose: gate P3 (scoped retrieval perf collapse) mitigation + answer OQ-12 (mem0 Platform API filter behavior). If p95 > 500ms at the 500-record threshold, this benchmark triggers documented 物理分区 evaluation per Phase 48 §3 (Option B vs partition migration).

Output: 1 timing module + 1 benchmark script + 1 baseline doc + 3 seed fixtures + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-eval-harness-1/54-CONTEXT.md
@.planning/research/v10-orchestrator-design/05-POC-PLAN.md

<interfaces>
<!-- Critical contract — instrumented function. From agent/memory_arbitration.py:394 -->

```python
async def memory_retrieve_scoped(
    query: str,
    agent_id: str,
    *,
    top_k: int = 5,
) -> dict[str, Any]:
    """Returns {"status": "ok"|"unavailable", "hits": [...]}"""
```

Notes from `agent/memory_arbitration.py:419-432`:
- `_get_mem0_backend()` returns `None` or raises when mem0 not configured
- `backend.search(query=query, agent_id=effective_agent_id, top_k=top_k)` is the timed path
- When backend is unavailable, returns `{"status": "unavailable", "hits": []}` — benchmark must treat this as "skip SLO verdict" not as "0ms latency"
- The P3 mitigation gates the mem0 `search()` call latency, NOT the full MCP tool round-trip (per STACK §7.4)

From `plugins/memory/mem0/__init__.py:119`:
```python
class Mem0MemoryProvider(MemoryProvider):
    def search(self, *, query: str, agent_id: str | None = None, top_k: int = 5) -> list[dict]: ...
    def add(self, *, content: str, agent_id: str | None = None, scope: str = "global", confidence: float = 0.5) -> str: ...
```

GLM is NOT invoked in this plan — latency SLO is purely mem0 backend timing, excluding LLM call (per §4.2 acceptance check + STACK §7.4).
</interfaces>

**Authoritative sources:**
- `05-POC-PLAN.md §4.2` task (a/b/c) — log field + stats CLI + 3-count benchmark protocol
- `05-POC-PLAN.md §4.2` failure mode — p95 > 500ms triggers Phase 48 §3 物理分区 evaluation
- PITFALLS §P3 mitigation 4 — `_latency_ms` field in retrieval log + stats CLI p50/p95/p99 per agent
- STACK §7.4 — latency budget breakdown (stdio <5ms, MCP dispatch <10ms, LLM 2-15s excluded)
- Phase 48 §3 — Option B vs partition migration trigger conditions
- REQUIREMENTS.md EVAL-02 deliverables list
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement timing instrumentation module + unit tests</name>
  <files>
    agent/memory_scoped_retrieval.py,
    tests/v11-latency-bench/__init__.py,
    tests/v11-latency-bench/conftest.py,
    tests/v11-latency-bench/test_retrieval_timing.py
  </files>
  <behavior>
    - Test 1: `compute_percentiles([10.0, 20.0, 30.0, 40.0, 50.0])` returns `{"p50": 30.0, "p95": ~48.0, "p99": ~49.8}` (uses `statistics.quantiles` stdlib — no scipy dep)
    - Test 2: `timed_retrieval(query="test", agent_id="ag1", backend=FakeBackend())` returns `LatencySample(latency_ms=<float>, status="ok", hits=[...])` where `latency_ms > 0`
    - Test 3: When `FakeBackend.search` raises, `timed_retrieval` returns `LatencySample(latency_ms=<measured>, status="error", hits=[])` — does NOT crash
    - Test 4: `timed_retrieval` does not call any LLM (assert no `call_llm` import path)
  </behavior>
  <action>
    Create `agent/memory_scoped_retrieval.py` with `from __future__ import annotations`. Public API:

    - `@dataclass class LatencySample:` with fields `latency_ms: float`, `status: str`, `hits: list[dict]`, `ts: str` (ISO timestamp).
    - `async def timed_retrieval(*, query: str, agent_id: str, backend: Any = None, top_k: int = 5) -> LatencySample:` — wraps `memory_arbitration.memory_retrieve_scoped` (or `backend.search` directly when `backend` arg is passed) in `time.perf_counter()` bracket. On exception, returns `LatencySample(latency_ms=<measured>, status="error", hits=[])`. Explicitly does NOT dispatch any LLM call — `05-POC-PLAN.md §4.2` SLO excludes LLM-bound paths.
    - `def compute_percentiles(samples_ms: list[float]) -> dict[str, float]:` — uses `statistics.quantiles(samples_ms, n=100, method="inclusive")` (stdlib, no scipy); returns `{"p50": ..., "p95": ..., "p99": ...}`. Handles len<2 input by returning all-equal values.

    Unit tests use a `FakeBackend` (in `conftest.py`) implementing the `Mem0MemoryProvider.search/add` interface contract. No real mem0 client, no GLM, no network. `@pytest.mark.asyncio` on async tests.

    Use `encoding="utf-8"` on every `open()`.
  </action>
  <verify>
    <automated>python -m pytest tests/v11-latency-bench/test_retrieval_timing.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    - Module exports `LatencySample`, `timed_retrieval`, `compute_percentiles`
    - All 4 behavior tests pass with no network/LLM/mem0 backend dependency
    - Code path contains zero `call_llm` invocations (grep-verifiable)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement benchmark script + seed fixtures + baseline doc</name>
  <files>
    tests/v11-latency-bench/fixtures/seed_100_records.py,
    tests/v11-latency-bench/fixtures/seed_500_records.py,
    tests/v11-latency-bench/fixtures/seed_1000_records.py,
    scripts/run_latency_benchmark.py,
    .planning/research/v11-poc-eval/latency-baseline.md
  </files>
  <behavior>
    - Test 1: `python scripts/run_latency_benchmark.py --fixture 100 --out /tmp/bench.json` writes a JSON file with keys `record_count`, `samples_ms` (list of 100 floats), `percentiles` (`p50`/`p95`/`p99`), `slo_verdict` (`pass`/`fail`/`skip`).
    - Test 2: When run against `FakeBackend` (test fixture), script produces `slo_verdict=skip` if status=="unavailable" on all 100 calls; otherwise `pass`/`fail` based on `p95 < 500.0` threshold.
    - Test 3: Seed fixture `seed_500_records.py` exposes `build_fixture_backend()` returning a backend with 500 records pre-loaded (deterministic, no network).
  </behavior>
  <action>
    Create 3 seed fixture modules. Each exposes `build_fixture_backend() -> FakeBackend` returning a backend instance pre-loaded with N records (N=100/500/1000). Records are synthetic strings (e.g. `"memory record {i} for agent ag1: <deterministic content>"`) — NO LLM, NO network. Use a single agent_id `ag1` so scoped retrieval has realistic filter behavior.

    Create `scripts/run_latency_benchmark.py`: argparse `--fixture` (one of `100`/`500`/`1000`, required), `--out` (JSON output path, required), `--runs` (default 100, sequential retrievals). Imports `from agent.memory_scoped_retrieval import timed_retrieval, compute_percentiles` + `from tests.v11-latency-bench.fixtures.seed_{N}_records import build_fixture_backend` (use `importlib` since module name has digits — or rename to `seed_small.py`/`seed_medium.py`/`seed_large.py` mapping to 100/500/1000). Loops `--runs` times calling `await timed_retrieval(query="benchmark query", agent_id="ag1", backend=backend)`, collects latencies, computes percentiles, writes JSON via `utils.atomic_json_write`.

    The benchmark MUST run against the `FakeBackend` from fixtures (deterministic, no mem0 dependency) — the real mem0 Platform API benchmark is an operator-action handoff documented in baseline doc (live API requires `MEM0_API_KEY` + live backend, deferred to operator smoke test per CLAUDE.md operator-action-handoff pattern).

    Create `.planning/research/v11-poc-eval/latency-baseline.md`: documents (1) SLO contract `p95 < 500ms` per §4.2; (2) baseline results table (3 rows: 100/500/1000 records × p50/p95/p99) — leave actual numbers as `<TBD: operator runs benchmark>` placeholders (this is the design artifact; numbers come from operator smoke test); (3) bottleneck analysis section identifying the timed code path (mem0 `backend.search()` HNSW + post-filter); (4) 物理分区 trigger conditions per Phase 48 §3 — if p95 > 500ms at 500-record threshold OR progression 100→500→1000 scales super-linearly (>3x p95 jump per 2x records), trigger documented evaluation; (5) operator-action handoff: "Run `python scripts/run_latency_benchmark.py --fixture 500 --out /tmp/bench.json` with real mem0 backend (set MEM0_API_KEY) to populate baseline numbers."
  </action>
  <verify>
    <automated>python scripts/run_latency_benchmark.py --fixture 100 --out /tmp/bench_100.json && python -c "import json; d = json.load(open('/tmp/bench_100.json', encoding='utf-8')); assert d['record_count'] == 100 and len(d['samples_ms']) == 100 and 'p50' in d['percentiles'] and d['slo_verdict'] in ('pass', 'fail', 'skip'); print('OK', d['slo_verdict'], d['percentiles'])"</automated>
  </verify>
  <done>
    - 3 seed fixtures exist and `build_fixture_backend()` returns a backend with N records (assert via test)
    - `scripts/run_latency_benchmark.py` runs end-to-end on all 3 fixtures, produces valid JSON
    - `latency-baseline.md` exists with 5 required sections, ≥60 lines
    - No real mem0/LLM calls in automated tests (deterministic FakeBackend only)
    - SLO verdict logic correctly thresholds p95 against 500ms
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data → benchmark | Synthetic fixture records parsed by FakeBackend — no external input |
| benchmark output → operator | JSON results file consumed by operator + baseline doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | benchmark JSON output | accept | results written to operator-specified path; integrity not load-bearing (operator reads + interprets) |
| T-54-05 | Denial of Service | 1000-record seed fixture | mitigate | fixture is in-memory only; size capped at 1000 records per §4.2; no unbounded growth path |
| T-54-06 | Information Disclosure | fixture memory records | accept | synthetic deterministic strings, no PII, no operator data |
| T-54-SC | Tampering | no new package installs | accept | uses stdlib `statistics` + `time.perf_counter`; zero new deps (Phase 52 verified) |
</threat_model>

<verification>
1. `python -m pytest tests/v11-latency-bench/ -x` passes (no network)
2. `python scripts/run_latency_benchmark.py --fixture 500 --out /tmp/bench_500.json && python -c "import json; d=json.load(open('/tmp/bench_500.json',encoding='utf-8')); assert d['slo_verdict'] in ('pass','fail','skip'); print(d['percentiles'])"` succeeds
3. `grep -c "call_llm" agent/memory_scoped_retrieval.py` returns `0` (no LLM in SLO path)
4. `grep -c "statistics.quantiles" agent/memory_scoped_retrieval.py` returns `>=1` (stdlib percentile)
</verification>

<success_criteria>
- SC#2 (EVAL-02) from ROADMAP §Phase 54: timing instrumentation in `agent/memory_scoped_retrieval.py` + benchmark script + baseline doc all exist
- 100 sequential retrievals on populated 500-record store produce p95 measurement
- No new package dependencies (stdlib `statistics` + `time.perf_counter` only)
- LLM call excluded from SLO budget per STACK §7.4 (grep-verifiable: zero `call_llm` in module)
- Operator-action handoff documented in baseline doc for live mem0 API benchmark
</success_criteria>

<output>
Create `.planning/phases/54-eval-harness-1/54-02-SUMMARY.md` when done
</output>

---
phase: 30-eval-gate-reuse
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - skills/movie-experts/_eval/gate.py
  - skills/movie-experts/_eval/tests/test_gate.py
  - skills/movie-experts/_eval/tests/fixtures
autonomous: true
requirements: [GATE-03, GATE-01]
user_setup: []

must_haves:
  truths:
    - "gate.paired_t_stats() computes t_stat + n + df + mean_diff + std_diff via stdlib statistics (no scipy)"
    - "gate.is_significant() returns a boolean from a hardcoded t-table lookup at alpha=0.05 two-tailed (df 1-30 + asymptotic 1.960 for df>30)"
    - "gate.run_gate() emit includes a paired_t block with t_stat, n, df, p_value (null by default), significant_at_0.05 boolean, and a note explaining the p_value omission"
    - "gate.run_gate() supports `--rebuild-baseline` which forces a fresh baseline run on the benchmark prompts and writes _eval/baseline/<skill>/scores.json"
    - "gate.detect_multi_skill_patch() parses patch headers and returns the set of distinct skills/movie-experts/<skill>/ directories touched"
    - "When detect_multi_skill_patch() returns >1 skill and `--multi-skill` flag is absent, gate exits 3 (inconclusive) with a warning naming the skills touched"
    - "Baseline cache carries baseline_skill_sha256 + judge_model + generated_at; gate warns if current skill sha256 differs from cached (non-blocking)"
    - "Reports and reject-logs include the full paired_t block + multi-skill warning (when applicable) + operator_hint suggesting override flags"
  artifacts:
    - path: "skills/movie-experts/_eval/gate.py"
      provides: "Extended with paired_t_stats, is_significant, detect_multi_skill_patch, rebuild_baseline, t-table constant, scores.json cache schema"
      contains: "_CRITICAL_T_05_TWO_TAILED"
    - path: "skills/movie-experts/_eval/tests/test_gate.py"
      provides: "Extended with TestPairedT, TestIsSignificant, TestMultiSkillDetection, TestRebuildBaseline, TestRejectLogSchema"
      min_lines: 350
  key_links:
    - from: "skills/movie-experts/_eval/gate.py::paired_t_stats"
      to: "stdlib statistics module"
      via: "statistics.mean(diffs) + statistics.stdev(diffs) + manual t = mean_diff / (std_diff / sqrt(n))"
      pattern: "statistics\\.mean|statistics\\.stdev"
    - from: "skills/movie-experts/_eval/gate.py::rebuild_baseline"
      to: "_eval/baseline/<skill>/scores.json cache"
      via: "evaluate candidate-vs-candidate (self) on benchmark prompts to populate baseline composites"
      pattern: "scores\\.json|baseline/.*/scores"
---

<objective>
Complete Phase 30 by adding (1) paired-t statistical significance computation via stdlib-only (no scipy) — closes GATE-03's "statistical-significance report" requirement; (2) `--rebuild-baseline` flag implementation that regenerates the cached baseline scores.json for a skill — completes GATE-01; (3) multi-skill patch detection with exit-3-without-`--multi-skill` safety guard — closes RESEARCH Open Q4; (4) reject-log schema finalization with paired_t block + operator_hint.

Purpose: Plan 01 built the deterministic threshold machinery (GATE-02 + GATE-04) but the A/B position-swap harness already in runner.py is only fully exploited when paired with a significance test — raw score deltas without confidence intervals give operators no way to distinguish "real regression" from "LLM judge noise." Plan 02 ships that significance layer + the baseline-cache management needed for the gate to be reusable across skills.

Output: Extended `gate.py` (no new files) + extended `tests/test_gate.py` + new synthetic fixtures. All `_eval/` offline tooling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-eval-gate-reuse/30-CONTEXT.md
@.planning/phases/30-eval-gate-reuse/30-RESEARCH.md
@.planning/phases/30-eval-gate-reuse/30-01-SUMMARY.md

# Code under extension (Plan 01 output — READ BEFORE EDITING):
@skills/movie-experts/_eval/gate.py
@skills/movie-experts/_eval/runner.py
@skills/movie-experts/_eval/tests/test_gate.py

<interfaces>
<!-- Contracts Plan 01 shipped that Plan 02 extends. Extracted from Plan 01 output. -->

From skills/movie-experts/_eval/gate.py (Plan 01 — EXTEND, do not rewrite):
```python
# Verdict mapping (Plan 01 — unchanged)
VERDICT_TO_EXIT = {"pass": 0, "fail_mean": 1, "fail_regression": 2, "inconclusive": 3}

# Decision logic (Plan 01 — REUSE inside rebuild_baseline)
def decide_verdict(
    baseline_scores: list[float],
    candidate_scores: list[float],
    *,
    delta_threshold: float,
    per_prompt_threshold: float,
    min_prompts: int,
) -> tuple[str, dict]: ...

# Evaluation (Plan 01 — REUSE for rebuild_baseline)
def evaluate_candidate(
    prompts: list[dict],
    baseline_answers: list[str],
    candidate_answers: list[str],
    judge_client,
    judge_model: str,
) -> list[dict]: ...  # per-prompt records with composite scores

# Orchestrator (Plan 01 — EXTEND with paired_t block + rebuild flag)
def run_gate(
    patch_path: Path,
    skill_id: str,
    baseline_answers_path: Path,
    candidate_answers_path: Path,
    config: dict,
    *,
    repo_root: Path,
    prompts_path: Path,
    baseline_scores_cache: Path | None = None,
) -> GateResult: ...
```

From skills/movie-experts/_eval/runner.py (Plan 01 — already extended):
```python
def parse_judge_scores(raw_text: str) -> dict[str, float]: ...
def composite_score(scores: dict[str, float]) -> float | None: ...
def run_position_swap(...) -> dict:  # returns scores_ab, scores_ba, raw_ab, raw_ba
def make_judge_client(config: dict) -> Any: ...
def load_prompts(prompts_path: str | Path) -> list[dict[str, str]]: ...
```

From skills/movie-experts/_eval/snapshot.py (for sha256 pattern):
```python
def compute_provenance(skill_id: str) -> dict:  # sha256 + git_sha + ISO 8601
```

Baseline scores.json schema (Plan 02 introduces — written by rebuild_baseline, read by run_gate):
```json
{
  "schema_version": 1,
  "skill_id": "screenplay",
  "baseline_skill_sha256": "<sha256 of skills/movie-experts/screenplay/SKILL.md at capture time>",
  "judge_model": "claude-sonnet-4-6",
  "generated_at": "2026-06-24T19:12:30Z",
  "prompts_path_sha256": "<sha256 of prompts YAML file>",
  "per_prompt_composites": [3.5, 3.0, 4.0, 3.5, 3.2, ...]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paired-t significance via stdlib + hardcoded t-table (GATE-03 stats layer)</name>
  <files>skills/movie-experts/_eval/gate.py, skills/movie-experts/_eval/tests/test_gate.py</files>
  <read_first>
    - skills/movie-experts/_eval/gate.py (Plan 01 output — focus on run_gate + decide_verdict + evaluate_candidate signatures + VERDICT_TO_EXIT)
    - skills/movie-experts/_eval/tests/test_gate.py (Plan 01 output — match test class conventions)
    - .planning/phases/30-eval-gate-reuse/30-RESEARCH.md (Code Examples lines 437-492 — reference implementation of paired_t_stats + is_significant + t-table)
    - .planning/phases/30-eval-gate-reuse/30-RESEARCH.md (Reject-Log JSON shape lines 512-577 — paired_t block schema)
    - .planning/phases/30-eval-gate-reuse/30-CONTEXT.md (A/B Double-Blind Scope section — confirms position-swap is 2 rounds + verdict enum)
  </read_first>
  <behavior>
    - TestPairedT::test_basic_negative_delta: baseline=[4.0, 4.0, 4.0, 4.0, 4.0], candidate=[3.0, 3.0, 3.0, 3.0, 3.0] → t_stat negative, mean_diff=-1.0, std_diff=0.0, n=5, df=4
    - TestPairedT::test_basic_positive_delta: baseline=[3.0]*5, candidate=[4.0]*5 → t_stat positive, mean_diff=+1.0
    - TestPairedT::test_zero_std_returns_inf_or_zero: identical lists (diffs all 0, std_diff=0) → t_stat is 0.0 when mean_diff=0; documents the edge case
    - TestPairedT::test_n_less_than_2: lists of length 1 → returns dict with t_stat=None, n=1, df=0 (degenerate case, no significance claim)
    - TestPairedT::test_mismatched_lengths: baseline len 5, candidate len 3 → uses min length (3); n=3, df=2
    - TestPairedT::test_no_p_value_computed: result["p_value"] is None (stdlib cannot compute; documented)
    - TestPairedT::test_returns_required_fields: result has keys t_stat, n, df, mean_diff, std_diff, p_value
    - TestIsSignificant::test_significant_at_df_4: t_stat=-3.0, df=4 → True (|t|=3.0 > crit=2.776)
    - TestIsSignificant::test_not_significant_at_df_4: t_stat=-2.0, df=4 → False (|t|=2.0 < crit=2.776)
    - TestIsSignificant::test_df_1_uses_12_706: t_stat=13.0, df=1 → True; t_stat=10.0, df=1 → False
    - TestIsSignificant::test_df_gt_30_uses_1_960: t_stat=2.5, df=40 → True; t_stat=1.5, df=40 → False
    - TestIsSignificant::test_unknown_alpha_returns_false: alpha=0.01 (not in table) → returns False with a logged warning (table only has alpha=0.05)
    - TestRejectLogSchema::test_paired_t_block_in_reject_log: when verdict != "pass", the reject JSON has a "paired_t" key with t_stat, n, df, mean_diff, p_value, significant_at_0.05, note fields
    - TestRejectLogSchema::test_operator_hint_present: reject JSON has "operator_hint" field (non-empty string) suggesting override flag if borderline (e.g., "Override with --per-prompt-threshold 1.5 if intentional")
    - TestRunGateEndToEnd::test_paired_t_in_report: run_gate() report JSON (always written) contains paired_t block
  </behavior>
  <action>
    Extend gate.py (additive — do NOT modify Plan 01 functions unless integrating the paired_t block into run_gate):

    1. Module constant `_CRITICAL_T_05_TWO_TAILED: dict[int, float]` = {1: 12.706, 2: 4.303, 3: 3.182, 4: 2.776, 5: 2.571, 6: 2.447, 7: 2.365, 8: 2.306, 9: 2.262, 10: 2.228, 15: 2.131, 20: 2.086, 25: 2.060, 30: 2.042}. Standard two-tailed alpha=0.05 critical values.

    2. `paired_t_stats(baseline: list[float], candidate: list[float]) -> dict[str, float | int | None]`: compute n = min(len(baseline), len(candidate)). If n < 2 → return {"t_stat": None, "n": n, "df": n - 1, "mean_diff": None, "std_diff": None, "p_value": None}. diffs = [c - b for c, b in zip(candidate, baseline)]. mean_diff = statistics.mean(diffs). std_diff = statistics.stdev(diffs) if n >= 2 else 0.0. If std_diff == 0: t_stat = 0.0 if mean_diff == 0 else float("inf") * (1 if mean_diff > 0 else -1). Else: t_stat = mean_diff / (std_diff / math.sqrt(n)). Round t_stat + mean_diff + std_diff to 4 decimal places. Return dict with all 6 fields; p_value is always None (stdlib cannot compute — documented in note).

    3. `is_significant(t_stat: float | None, df: int, alpha: float = 0.05) -> bool`: if t_stat is None → False. If alpha != 0.05 → log warning + return False (table only has 0.05). Look up crit = _CRITICAL_T_05_TWO_TAILED.get(df). If df not in table: if df > 30 → crit = 1.960 (asymptotic normal); else → crit = _CRITICAL_T_05_TWO_TAILED[max(k for k in table if k <= df)] (conservative round-down). Return abs(t_stat) > crit.

    4. INTEGRATE into run_gate(): after decide_verdict(), compute paired_t_stats(baseline_composite_list, candidate_composite_list). Compute is_significant(t_stat, df). Build a `paired_t_block = {"t_stat": ..., "n": ..., "df": ..., "mean_diff": ..., "p_value": None, "significant_at_0.05": bool, "note": "p_value not computed (stdlib only); |t_stat|=X < critical_t(df=Y)=Z"}`. Include this block in BOTH the always-written report JSON AND the reject-log JSON. Compute operator_hint: if verdict == "fail_mean" and abs(mean_delta + delta_threshold) < 0.1 → "Override with --threshold-delta {delta_threshold + 0.1} if intentional"; if verdict == "fail_regression" → "Override with --per-prompt-threshold {per_prompt_threshold + 0.5} if intentional"; else → empty string or generic "Patch rejected: see per_prompt details".

    5. EXTEND test_gate.py with TestPairedT + TestIsSignificant + TestRejectLogSchema classes per the behaviors above. Add `test_paired_t_in_report` to the existing TestRunGateEndToEnd class (verify report JSON contains paired_t block).

    CRITICAL constraint per RESEARCH.md: do NOT import scipy. The `_eval/` tree is stdlib-only (snapshot.py:14). scipy is transitively present but not a direct dependency and would violate the convention.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/movie-experts/_eval/tests/test_gate.py::TestPairedT skills/movie-experts/_eval/tests/test_gate.py::TestIsSignificant skills/movie-experts/_eval/tests/test_gate.py::TestRejectLogSchema -x -v && python -m pytest skills/movie-experts/_eval/tests/ -x -q && ! grep -q "import scipy\|from scipy" skills/movie-experts/_eval/gate.py && echo "scipy-free OK"</automated>
  </verify>
  <done>
    - paired_t_stats() computes t_stat + n + df + mean_diff + std_diff via stdlib (statistics.mean + statistics.stdev + math.sqrt); returns None for t_stat when n<2
    - is_significant() looks up critical-t from hardcoded _CRITICAL_T_05_TWO_TAILED; conservative round-down for df not in table; asymptotic 1.960 for df>30
    - p_value is always None in stdlib path; paired_t block includes a note explaining why (operators get interpretable signal without scipy)
    - Report JSON + reject JSON both include paired_t block + operator_hint
    - gate.py contains NO `import scipy` / `from scipy` (stdlib-only verified by grep)
    - All new tests green; all Plan 01 tests still green (no regressions)
    - Ruff PLW1514 clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rebuild_baseline + multi-skill detection + baseline cache staleness warning (completes GATE-01)</name>
  <files>skills/movie-experts/_eval/gate.py, skills/movie-experts/_eval/tests/test_gate.py, skills/movie-experts/_eval/tests/fixtures</files>
  <read_first>
    - skills/movie-experts/_eval/gate.py (after Task 1 — for run_gate signature + extract_patched_files + evaluate_candidate + paired_t_stats integration)
    - skills/movie-experts/_eval/snapshot.py (full file — for compute_provenance + EXPERT_DIRS pattern; baseline cache uses the same sha256 + ISO 8601 provenance approach)
    - skills/movie-experts/_eval/runner.py (Plan 01 — for load_prompts + make_judge_client)
    - .planning/phases/30-eval-gate-reuse/30-CONTEXT.md (Baseline Source section + Gate CLI Surface for --rebuild-baseline flag spec)
    - .planning/phases/30-eval-gate-reuse/30-RESEARCH.md (Pitfall 4 baseline staleness lines 376-380 + Open Question 4 multi-skill detection lines 626-629 + Pattern 2 patch mechanics lines 223-265)
    - skills/movie-experts/_eval/tests/fixtures/ (Plan 01 — for existing pass.patch + multi_skill.patch)
  </read_first>
  <behavior>
    - TestDetectMultiSkill::test_single_skill_patch: patch touching only `skills/movie-experts/screenplay/SKILL.md` → detect_multi_skill_patch returns {"screenplay"}
    - TestDetectMultiSkill::test_multi_skill_patch: patch touching `skills/movie-experts/screenplay/SKILL.md` AND `skills/movie-experts/drawer/SKILL.md` → returns {"screenplay", "drawer"}
    - TestDetectMultiSkill::test_ignores_non_skill_paths: patch touching `agent/curator.py` only → returns empty set (no skills/movie-experts/ paths); OR raises ValueError per extract_patched_files security guard (T-30-01) — both behaviors acceptable, document which
    - TestMultiSkillGuard::test_exits_inconclusive_without_flag: run_gate() with multi-skill patch + no --multi-skill flag → returns GateResult with verdict="inconclusive", exit_code=3, warning message naming the skills touched; patch is NOT applied (early exit before apply_patch)
    - TestMultiSkillGuard::test_proceeds_with_flag: run_gate() with multi-skill patch + --multi-skill flag → proceeds normally (uses the first skill in the set as skill_id, or errors clearly if --skill flag mismatches the detected set)
    - TestRebuildBaseline::test_writes_scores_json: rebuild_baseline(skill_id, prompts, judge_client, judge_model, baseline_dir) → writes `_eval/baseline/<skill>/scores.json` with schema_version=1, skill_id, baseline_skill_sha256, judge_model, generated_at, prompts_path_sha256, per_prompt_composites
    - TestRebuildBaseline::test_idempotent: calling rebuild_baseline twice with the same skill + prompts + judge_model → writes the same logical content (per_prompt_composites may differ slightly due to LLM nondeterminism; structure is identical)
    - TestRebuildBaseline::test_records_provenance: scores.json baseline_skill_sha256 matches `hashlib.sha256(SKILL.md bytes).hexdigest()` for the current (unpatched) SKILL.md
    - TestBaselineStaleness::test_warns_on_sha_mismatch: cached scores.json has baseline_skill_sha256="abc"; current SKILL.md sha256="xyz" → run_gate() logs warning "baseline stale; run --rebuild-baseline" but PROCEEDS (non-blocking per RESEARCH Pitfall 4 recommendation)
    - TestBaselineStaleness::test_no_warning_when_fresh: cached sha matches current sha → no warning logged
    - TestRebuildFlagInMain::test_rebuild_baseline_short_circuits: `python gate.py --rebuild-baseline --skill screenplay` (no --patch) → calls rebuild_baseline, writes scores.json, exits 0 without running a gate evaluation
    - TestGateReadsCachedBaseline::test_uses_cached_per_prompt_composites: when scores.json exists and is fresh, run_gate() uses the cached per_prompt_composites as the baseline_composite_list (does NOT re-evaluate the baseline)
  </behavior>
  <action>
    Extend gate.py (additive over Plan 01 + Task 1):

    1. `detect_multi_skill_patch(patch_path: Path) -> set[str]`: parse the patch text, extract `+++ b/skills/movie-experts/<skill>/...` paths, return the set of distinct `<skill>` directory names. Uses extract_patched_files (Plan 01) for the path list, then filters to skills/movie-experts/ and extracts the next path segment. Returns empty set if no skills/movie-experts/ paths (the security guard in extract_patched_files already rejected non-skills paths).

    2. `rebuild_baseline(skill_id: str, prompts: list[dict], baseline_answers: list[str], judge_client, judge_model: str, baseline_dir: Path, *, skill_md_path: Path, prompts_path: Path) -> Path`: evaluate the baseline against itself using evaluate_candidate (pass baseline_answers as BOTH baseline_answers and candidate_answers — this gives per-prompt composite scores for the baseline skill on the benchmark). Compute baseline_skill_sha256 = hashlib.sha256(skill_md_path.read_bytes()).hexdigest(). Compute prompts_path_sha256 = hashlib.sha256(prompts_path.read_bytes()).hexdigest(). generated_at = datetime.now(timezone.utc).isoformat(timespec="seconds"). Write JSON to `baseline_dir / skill_id / "scores.json"` with the schema in <interfaces>. Use `json.dump(..., indent=2, ensure_ascii=False)` + `encoding="utf-8"` on the open() call (Ruff PLW1514). Return the path to the written file.

    3. `load_cached_baseline(skill_id: str, baseline_dir: Path, *, current_skill_md_path: Path) -> tuple[list[float] | None, dict | None]`: if `_eval/baseline/<skill>/scores.json` exists, load it. Compare cached `baseline_skill_sha256` against `hashlib.sha256(current_skill_md_path.read_bytes()).hexdigest()`. If mismatch → log warning "baseline stale (cached sha X, current sha Y); run --rebuild-baseline" (non-blocking — RESEARCH Pitfall 4 recommends warning + suggest path, not hard refusal). Return (per_prompt_composites, raw_cache_dict). If file missing → return (None, None) (first-run case; gate lazy-populates by calling rebuild_baseline OR errors with clear "no baseline, run --rebuild-baseline first" message — pick the latter for predictability, document in main() help text).

    4. INTEGRATE into run_gate(): at the start (after config load, BEFORE apply_patch), call detect_multi_skill_patch(patch_path). If len(skills_touched) > 1 and not config.get("multi_skill", False): return GateResult(verdict="inconclusive", exit_code=3, warning=f"patch touches {len(skills_touched)} skills: {sorted(skills_touched)}; run gate per-skill OR pass --multi-skill"). DO NOT apply the patch in this path (early exit). After the multi-skill check, load cached baseline via load_cached_baseline. If cached per_prompt_composites is None → return GateResult(verdict="inconclusive", exit_code=3, warning="no cached baseline for {skill_id}; run `python gate.py --rebuild-baseline --skill {skill_id}` first"). If cache exists → use cached per_prompt_composites as the baseline_composite_list passed to decide_verdict + paired_t_stats (avoids re-evaluating baseline per gate run — cost saving per CONTEXT.md Baseline Source decision).

    5. EXTEND main() argparse: replace the Plan 01 `--rebuild-baseline` no-op stub with real behavior. If `--rebuild-baseline` is passed: call rebuild_baseline(...) (loads prompts, makes judge client, generates baseline answers from the current skill via the same pre-generated-answers JSON pattern — OR documents that rebuild_baseline requires `--baseline-answers` pointing at freshly-generated baseline answers; pick the latter to keep P30 scope-fenced from live answer-gen). Write scores.json, print "baseline rebuilt: <path>", exit 0. Also add `--multi-skill` boolean flag (default False) that sets config["multi_skill"] = True so run_gate skips the multi-skill early-exit guard.

    6. EXTEND test_gate.py with TestDetectMultiSkill + TestMultiSkillGuard + TestRebuildBaseline + TestBaselineStaleness + TestRebuildFlagInMain + TestGateReadsCachedBaseline classes per the behaviors above. Use the existing fixtures/pass.patch (single-skill) + fixtures/multi_skill.patch (multi-skill). For rebuild_baseline tests, use MockJudgeClient with canned responses + a temp _eval/baseline/ dir under pytest tmp_path. For TestBaselineStaleness, write a scores.json with a fake sha, then point load_cached_baseline at a real SKILL.md file with a different sha.

    7. ADD fixtures/stale_baseline_scores.json (synthetic cached scores with a fake baseline_skill_sha256 that won't match any real SKILL.md) for the staleness test.

    CRITICAL constraints:
    - rebuild_baseline does NOT generate live answers — it requires `--baseline-answers` JSON input (same scope fence as Plan 01). Document this clearly in the --help text.
    - The multi-skill guard runs BEFORE apply_patch (no working-tree mutation on early exit).
    - load_cached_baseline warning is non-blocking (RESEARCH Pitfall 4); operators can run --rebuild-baseline at their discretion.
    - All new file writes use `encoding="utf-8"` (Ruff PLW1514).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/movie-experts/_eval/tests/test_gate.py::TestDetectMultiSkill skills/movie-experts/_eval/tests/test_gate.py::TestMultiSkillGuard skills/movie-experts/_eval/tests/test_gate.py::TestRebuildBaseline skills/movie-experts/_eval/tests/test_gate.py::TestBaselineStaleness skills/movie-experts/_eval/tests/test_gate.py::TestRebuildFlagInMain skills/movie-experts/_eval/tests/test_gate.py::TestGateReadsCachedBaseline -x -v && python -m pytest skills/movie-experts/_eval/tests/ -x -q && ruff check skills/movie-experts/_eval/gate.py</automated>
  </verify>
  <done>
    - detect_multi_skill_patch() parses patch headers + returns set of distinct skills touched
    - run_gate() early-exits with verdict="inconclusive" + exit_code=3 when multi-skill patch detected and --multi-skill absent (patch NOT applied — no working-tree mutation)
    - rebuild_baseline() writes scores.json with schema_version=1 + baseline_skill_sha256 + judge_model + generated_at + prompts_path_sha256 + per_prompt_composites
    - load_cached_baseline() warns (non-blocking) on sha mismatch; returns cached composites for fresh cache
    - main() --rebuild-baseline flag short-circuits: writes scores.json, exits 0, no gate eval
    - main() --multi-skill flag bypasses the multi-skill early-exit guard
    - gate reuses cached per_prompt_composites as baseline (no re-evaluation per gate run — cost saving)
    - All new tests green; all Plan 01 + Task 1 tests still green
    - Ruff PLW1514 clean on gate.py
    - FOUND-08 byte-intact: no bundled SKILL.md or refs modified (synthetic fixtures only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch file → detect_multi_skill_patch | Untrusted patch header text crosses here (Plan 01 already validates path safety in extract_patched_files; this layer adds multi-skill detection) |
| cached scores.json → load_cached_baseline | Operator-local cache file (semi-trusted; validated structurally) |
| rebuild_baseline → judge API | External LLM call (already secured by runner.make_judge_client — T-30-03 inherited) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Tampering | detect_multi_skill_patch / multi-skill guard | mitigate | Early-exit BEFORE apply_patch when >1 skill detected and --multi-skill absent. Prevents a single gate run from silently scoring a patch that touches multiple skills against only one baseline. |
| T-30-08 | Tampering | load_cached_baseline / scores.json | mitigate | Structural validation: scores.json must have schema_version=1, baseline_skill_sha256 (64-hex string), per_prompt_composites (list of floats in [1.0, 5.0]). Raise ValueError on malformed. Reject cached file if any composite is out of [1.0, 5.0] range. |
| T-30-09 | Information Disclosure | rebuild_baseline / scores.json | accept | scores.json contains only composite scores + sha256 + ts + judge_model — no answer text, no API key. Matches T-30-03 V8 control from Plan 01. |
| T-30-10 | Denial of Service | paired_t_stats / is_significant | mitigate | Pure functions over bounded inputs (n = min prompt count, capped by min_prompts floor from Plan 01). No unbounded loops. Hardcoded t-table is O(1) lookup. |
| T-30-11 | Repudiation | rebuild_baseline provenance | mitigate | scores.json records baseline_skill_sha256 + prompts_path_sha256 + judge_model + generated_at. Operator can verify baseline was built against the claimed skill bytes + prompt set. |
| T-30-SC | Tampering | (N/A — no new packages) | accept | Plan 02 installs zero new packages. slopcheck N/A. |
</threat_model>

<verification>
Per-task automated checks above. Phase-level gate (combined Plan 01 + Plan 02):

1. `python -m pytest skills/movie-experts/_eval/tests/ -v` — full _eval test suite green (test_runner + test_snapshot + test_gate; all TestPairedT/TestIsSignificant/TestRejectLogSchema/TestDetectMultiSkill/TestMultiSkillGuard/TestRebuildBaseline/TestBaselineStaleness/TestRebuildFlagInMain/TestGateReadsCachedBaseline passing)
2. `ruff check skills/movie-experts/_eval/gate.py skills/movie-experts/_eval/runner.py` — PLW1514 clean
3. `! grep -q "import scipy\|from scipy" skills/movie-experts/_eval/gate.py skills/movie-experts/_eval/runner.py` — stdlib-only verified (no scipy)
4. `grep -rn "from _eval\|import _eval" agent/ hermes_cli/ tools/ gateway/ cli.py run_agent.py 2>/dev/null | wc -l` — returns 0 (Hermes runtime isolation)
5. `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` — returns 0 (FOUND-08 byte-intact)
6. `python skills/movie-experts/_eval/gate.py --help` — prints usage including --rebuild-baseline + --multi-skill flags
7. End-to-end smoke (manual, documented in VALIDATION.md Manual-Only): operator sets OPENROUTER_API_KEY, runs gate on a synthetic patch with pre-generated answers, verifies verdict + report JSON structure
</verification>

<success_criteria>
- GATE-03 (complete): A/B position-swap (reused from runner.run_position_swap, Plan 01) + paired-t significance report via stdlib paired_t_stats + hardcoded t-table is_significant. Reject-log includes paired_t block with t_stat, n, df, mean_diff, p_value (null), significant_at_0.05, note.
- GATE-01 (complete): --rebuild-baseline flag regenerates cached scores.json with provenance (baseline_skill_sha256 + judge_model + generated_at). load_cached_baseline warns on staleness (non-blocking). Multi-skill patch detection + --multi-skill flag.
- Paired-t significance via stdlib statistics module only (no scipy — verified by grep).
- Hermes runtime isolation verified (grep returns 0).
- FOUND-08 byte-intact verified (no bundled SKILL.md or refs touched — synthetic fixtures only).
- All _eval/tests/ green (Plan 01 + Plan 02 + pre-existing).
- Gate is invocable both as `python gate.py ...` (CLI) and as `from gate import run_gate` (importable) — RESEARCH Open Q3 resolution.
</success_criteria>

<output>
Create `.planning/phases/30-eval-gate-reuse/30-02-SUMMARY.md` when done.
</output>

---
phase: 40-gate-redlines
plan: 02
type: tdd
wave: 2
depends_on: [40-01]
files_modified:
  - plugins/review_gates/gates.yaml
  - plugins/review_gates/gate_config.py
  - plugins/review_gates/runner_hooks.py
  - plugins/review_gates/tools.py
  - plugins/review_gates/__init__.py
  - plugins/review_gates/plugin.yaml
  - plugins/review_gates/tests/test_gates_config.py
  - plugins/review_gates/tests/test_runner_hooks.py
  - plugins/review_gates/tests/test_tools_dispatch.py
  - plugins/review_gates/tests/test_redline_gate_wiring.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04]
user_setup: []

must_haves:
  truths:
    - "gates.yaml contains 11 gate entries (8 V8.6 + 3 redline), no removal of existing 8"
    - "gate_config.load_gates() accepts 11 entries (count check updated from 8 to 11)"
    - "Existing 8 gate_ids (topic-gate..delivery-gate) preserved byte-for-byte in gates.yaml"
    - "Each new gate (redline_emotion_desensitize / redline_no_cold_open / redline_unfinished_ending) appears in GATE_REGISTRY"
    - "Calling gate_submit with a redline gate_id auto-runs the detector from Plan 01 and emits the reject verdict without manual gate_resolve"
    - "Existing gate_submit path for the 8 V8.6 gates unchanged (manual resolution preserved)"
    - "gates_list tool output now shows 11 gates (not 8)"
  artifacts:
    - path: "plugins/review_gates/gates.yaml"
      provides: "11 gate entries (8 existing + 3 redline additive)"
      contains: "redline_emotion_desensitize"
    - path: "plugins/review_gates/gate_config.py"
      provides: "load_gates() accepting 11 entries (count validator bumped)"
      contains: "len(gate_list) != 11"
    - path: "plugins/review_gates/runner_hooks.py"
      provides: "Auto-detect path for redline gates — runs Plan-01 detector before manual resolve"
      contains: "DETECTOR_REGISTRY"
    - path: "plugins/review_gates/tools.py"
      provides: "gate_submit dispatches redline gates to auto-detect path"
      contains: "redline"
    - path: "plugins/review_gates/tests/test_gates_config.py"
      provides: "11-entry expectation + redline gate presence checks"
      contains: "redline_emotion_desensitize"
    - path: "plugins/review_gates/tests/test_redline_gate_wiring.py"
      provides: "End-to-end: gate_submit(redline_emotion_desensitize) with violating payload → auto-reject"
      contains: "auto_reject"
  key_links:
    - from: "plugins/review_gates/runner_hooks.py"
      to: "plugins/review_gates/gates/__init__.py (Plan 01)"
      via: "DETECTOR_REGISTRY import for auto-detect dispatch"
      pattern: "from plugins\\.review_gates\\.gates import DETECTOR_REGISTRY"
    - from: "plugins/review_gates/tools.py _handle_gate_submit"
      to: "runner_hooks.pause_for_review OR runner_hooks.auto_detect_and_resolve"
      via: "gate_id prefix check (redline_ → auto path)"
      pattern: "redline_"
    - from: "plugins/review_gates/gates.yaml entries 9-11"
      to: "plugins/review_gates/gate_config.py REQUIRED_FIELDS validator"
      via: "all 11 entries pass the same field-level validation"
      pattern: "reviewer_role.*redline"
---

<objective>
Register the 3 redline gates additively on the Phase 34 state machine and
wire them to auto-resolve via the Plan 01 detectors.

Purpose: Phase 34's gates are HIL (human resolves via gate_resolve). The 3
redline gates need AUTOMATIC resolution — they detect a structural violation
and emit reject without waiting for a human. This plan adds an auto-detect
branch to the existing submit path: when gate_id starts with `redline_`, the
runner looks up the detector in Plan-01's DETECTOR_REGISTRY, runs it on the
payload, and calls Gate.resolve() with the detector's verdict. The 8 V8.6
gates keep their existing manual-resolution path (zero behavior change).

Output: gates.yaml grows 8→11, gate_config.py validator bumped, runner_hooks
gains an auto-detect function, tools.py dispatches redline_ gates to that
function. Phase 34 frozen gate.py untouched (we use its public resolve()).
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@plugins/review_gates/gate.py
@plugins/review_gates/gate_config.py
@plugins/review_gates/runner_hooks.py
@plugins/review_gates/tools.py
@plugins/review_gates/gates.yaml
@plugins/review_gates/__init__.py
@plugins/review_gates/plugin.yaml
@plugins/review_gates/tests/test_gates_config.py
@plugins/review_gates/tests/test_runner_hooks.py
@plugins/review_gates/tests/test_tools_dispatch.py
@.planning/phases/40-gate-redlines/40-01-SUMMARY.md

<interfaces>
<!-- Phase 34 public API consumed unchanged (gate.py is FROZEN per constraint): -->
```python
# plugins/review_gates/gate.py
class Gate:
    config: GateConfig
    episode_id: str
    mode: GateMode
    def submit(self, payload: dict, *, review_client=None) -> dict: ...
    def wait(self, timeout_sec=None) -> dict: ...
    def resolve(self, decision: str, suggested_action: str | None = None) -> dict: ...

@dataclass(frozen=True)
class GateConfig:
    gate_id: str
    phase: str
    asset_bus_slots_to_lock: tuple[str, ...]
    reviewer_role: str
    timeout_sec: int = 3600
    callback_url: str | None = None
    max_retries: int = 2
    backoff_sec: int = 300
    default_mode: GateMode = GateMode.BLOCKING
```

<!-- Plan 01 detector registry consumed here: -->
```python
# plugins/review_gates/gates/__init__.py (built in Plan 01)
DETECTOR_REGISTRY: dict[str, DetectorFn]  # keyed by GATE_ID
# DetectorFn.detect(payload) -> (decision: "approve"|"reject"|"contest", suggested_action: str|None)
```

<!-- Existing runner_hooks entry points (preserved, not modified): -->
```python
def pause_for_review(gate_id, episode_id, payload, *, mode=None) -> dict: ...
def resolve_direct(gate_id, decision, suggested_action=None) -> dict: ...
def mark_episode_failed(episode_id, gate_id, exc) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Update tests to expect 11 gates + redline auto-reject path (RED)</name>
  <files>
    plugins/review_gates/tests/test_gates_config.py,
    plugins/review_gates/tests/test_redline_gate_wiring.py
  </files>
  <behavior>
    Test 1 (gate_config count): test_load_gates_returns_dict_with_exactly_8_entries → rename / rewrite to expect 11 entries. Assert the 8 original IDs are preserved + 3 new IDs present: redline_emotion_desensitize / redline_no_cold_open / redline_unfinished_ending.
    Test 2 (gate_config field validation): the 3 redline entries have valid default_mode (blocking), non-empty reviewer_role ("redline_scanner"), positive timeout_sec, retry_policy with max_retries > 0.
    Test 3 (auto-detect wiring, end-to-end): calling pause_for_review("redline_emotion_desensitize", "EP01", {violation_payload}) returns a dict with "decision": "reject" and "suggested_action" starting with "formula:" — NO manual gate_resolve needed.
    Test 4 (auto-detect negative case): calling pause_for_review("redline_emotion_desensitize", "EP01", {compliant_payload}) returns dict with "decision": "approve", suggested_action None.
    Test 5 (auto-detect dispatches correct detector): redline_no_cold_open with first-beat-exposition payload → reject (NOT triggered by emotion payload).
    Test 6 (V8.6 gate path unchanged): pause_for_review("topic-gate", ...) does NOT auto-resolve — gate is left in PENDING state awaiting manual gate_resolve (preserves Phase 34 HIL contract).
    Test 7 (tools gate_submit redline dispatch): _handle_gate_submit({gate_id: "redline_emotion_desensitize", episode_id: "EP01", payload: {violation}}) returns tool_result envelope with "decision": "reject" and "status": "auto_resolved" (NOT "submitted").
    Test 8 (gates_list count): _handle_gates_list({}) returns {"gates": [...], "count": 11}.
    Test 9 (no regression on existing 8-gate tests): all pre-existing tests in test_gates_config.py / test_runner_hooks.py / test_tools_dispatch.py that don't conflict with the new count still pass — adjust the count-assertion test, leave others alone.
  </behavior>
  <action>
    RED phase.

    1. In test_gates_config.py:
       - Update TestLoadGates.EXPECTED_GATE_IDS to include the 3 new redline IDs.
       - Rename test_load_gates_returns_dict_with_exactly_8_entries → test_load_gates_returns_dict_with_exactly_11_entries; change `assert len(registry) == 8` to `== 11`.
       - Update the 8 in TestGateFieldsComplete / TestSpecificGateValues / TestYAMLValidationRejects: any test that pads to 8 entries must now pad to 11 (the bad-YAML tests construct fake YAML — bump the padding count from 7/8 to 10/11).
       - Add a new TestRedlineGates class with field-level spot-checks on the 3 new entries.

    2. Create tests/test_redline_gate_wiring.py with Tests 3-8 above. Use unittest.mock.MagicMock to stub the review_client (mirror the pattern in test_runner_hooks.py — monkeypatch _review_client to return a MagicMock whose submit_review returns {"review_id": "test"}). For Test 6, assert the gate ends up in _PENDING_GATES (Phase 34 shim) rather than being resolved.

    Run pytest — Tests 1+2 FAIL (gates.yaml still has 8; load_gates() raises GateConfigError on count != 8). Tests 3-8 FAIL (no auto-detect path yet, or unknown gate_id errors). Commit RED.

    AVOID: editing gates.yaml / gate_config.py / runner_hooks.py / tools.py in this task. Only tests.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/review_gates/tests/test_gates_config.py plugins/review_gates/tests/test_redline_gate_wiring.py -x --tb=short</automated>
  </verify>
  <done>
    - test_gates_config.py expects 11 entries (Tests 1-2 RED — fail because actual file still has 8)
    - test_redline_gate_wiring.py has Tests 3-8 (RED — fail because auto-detect path doesn't exist)
    - test_runner_hooks.py and test_tools_dispatch.py still pass (no edits — they exercise the 8-gate path)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append 3 redline gates to gates.yaml + bump gate_config.py count to 11 (GREEN)</name>
  <files>
    plugins/review_gates/gates.yaml,
    plugins/review_gates/gate_config.py,
    plugins/review_gates/plugin.yaml
  </files>
  <behavior>
    - gates.yaml contains 11 gate entries (8 original byte-preserved + 3 new appended)
    - load_gates() returns 11 entries, no GateConfigError
    - All Tests 1-2 from Task 1 PASS
    - Plugin manifest version bumped to 0.2.0 with description updated to mention redline gates
  </behavior>
  <action>
    GREEN phase for the YAML + loader count.

    1. Append to plugins/review_gates/gates.yaml (DO NOT modify existing 8 entries — additive only):

       ```yaml
         # --- Gate 9: redline R1 — emotion desensitization -----------------------
         # Auto-resolved via DETECTOR_REGISTRY (Plan 01). Fires after Gate 8 (delivery)
         # passes, before final master.mp4 release. Phase 40 GATE-01.
         - gate_id: redline_emotion_desensitize
           phase: p13_delivery
           asset_bus_slots_to_lock:
             - final-shots
             - master-mp4
           reviewer_role: redline_scanner
           timeout_sec: 60          # auto-detect is sub-second; short budget
           callback_url: null
           default_mode: blocking   # runner_hooks auto-resolves, no webhook needed
           retry_policy:
             max_retries: 1
             backoff_sec: 60

         # --- Gate 10: redline R3 — no cold open ----------------------------------
         - gate_id: redline_no_cold_open
           phase: p13_delivery
           asset_bus_slots_to_lock:
             - final-shots
           reviewer_role: redline_scanner
           timeout_sec: 60
           callback_url: null
           default_mode: blocking
           retry_policy:
             max_retries: 1
             backoff_sec: 60

         # --- Gate 11: redline R4 — unfinished ending -----------------------------
         - gate_id: redline_unfinished_ending
           phase: p13_delivery
           asset_bus_slots_to_lock:
             - final-shots
           reviewer_role: redline_scanner
           timeout_sec: 60
           callback_url: null
           default_mode: blocking
           retry_policy:
             max_retries: 1
             backoff_sec: 60
       ```

       All 3 redline gates map to phase `p13_delivery` — they fire AFTER gate 8 (delivery-gate) passes, as the final scan before master.mp4 release (per ROADMAP Phase 40 SC#3: "after 8 gates pass, gates 9-11 do one final scan before final delivery").

    2. In plugins/review_gates/gate_config.py:
       - Line 98: change `if len(gate_list) != 8:` to `if len(gate_list) != 11:`. Update the error message correspondingly: `f"gates.yaml must contain exactly 11 gates (found {len(gate_list)})"`.
       - Update the module docstring to mention Phase 40 added 3 redline gates (8 → 11 additive).
       - Add a comment above the count check citing Phase 40 GATE-04 as the additive bump rationale.

    3. In plugins/review_gates/plugin.yaml:
       - Bump `version: "0.1.0"` → `version: "0.2.0"`.
       - Update description to: `"review gate framework plugin — HIL gate lifecycle (submit/wait/resolve) + 11 gates (8 V8.6 + 3 Phase 40 redline)"`.

    4. Run pytest on test_gates_config.py — Tests 1-2 now PASS (GREEN).

    AVOID: touching gate.py (Phase 34 frozen). AVOID: changing any of the existing 8 gate entries in gates.yaml. AVOID: changing the YAML schema (version: 1 stays — it's the immutable schema version, not the data version).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/review_gates/tests/test_gates_config.py -v --tb=short && python -c "from plugins.review_gates.gate_config import GATE_REGISTRY; assert len(GATE_REGISTRY) == 11, len(GATE_REGISTRY); assert 'redline_emotion_desensitize' in GATE_REGISTRY"</automated>
  </verify>
  <done>
    - gates.yaml has 11 entries (3 appended, 8 preserved byte-for-byte)
    - gate_config.load_gates() succeeds with 11 entries
    - test_gates_config.py Tests 1-2 GREEN
    - plugin.yaml version 0.2.0 + description updated
    - Phase 34 gate.py UNTOUCHED
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add auto-detect path in runner_hooks + wire tools.py (GREEN for Tests 3-8)</name>
  <files>
    plugins/review_gates/runner_hooks.py,
    plugins/review_gates/tools.py,
    plugins/review_gates/__init__.py
  </files>
  <behavior>
    - pause_for_review(gate_id, ...) for a gate_id starting with "redline_" runs the corresponding detector and auto-resolves the gate
    - pause_for_review for V8.6 gate_ids unchanged — manual resolution path preserved
    - _handle_gate_submit dispatches redline_ gates to a new auto-detect-aware path; emits a tool_result envelope with status="auto_resolved" + decision="approve"|"reject" + suggested_action
    - _handle_gates_list returns count=11
    - Tests 3-8 from Task 1 all PASS
  </behavior>
  <action>
    GREEN phase for the wiring.

    1. In plugins/review_gates/runner_hooks.py add a new public function:

       ```
       _REDLINE_PREFIX = "redline_"

       def is_redline_gate(gate_id: str) -> bool:
           """True if gate_id starts with 'redline_' (Plan 02 Phase 40 convention)."""
           return gate_id.startswith(_REDLINE_PREFIX)

       def auto_detect_and_resolve(gate_id: str, episode_id: str, payload: dict) -> dict:
           """Auto-resolve a redline gate by running its Plan-01 detector.

           Look up detector in DETECTOR_REGISTRY; call it on payload; call
           gate.submit + gate.resolve with the detector's verdict; write
           awaiting_review state (for audit trail) then immediately resolve;
           write outcome to asset bus; advance state. Mirrors pause_for_review
           but skips the manual-resolution wait.

           For approve: outcome reflects auto-approve.
           For reject: outcome carries suggested_action + rollback_to.
           For contest (defensive — detectors shouldn't emit this): treated
           like reject but flagged for human review (status=contested).

           Raises:
               GateMaxRetriesExceeded: same retry semantics as pause_for_review.
               KeyError: gate_id not in DETECTOR_REGISTRY (misconfiguration).
           """
       ```

       Implementation:
       - Build Gate via _build_gate(gate_id, episode_id, mode=None) — reuse existing helper.
       - Call gate.submit(payload) — may raise GateMaxRetriesExceeded (caller catches).
       - Import DETECTOR_REGISTRY from plugins.review_gates.gates (Plan 01). Look up gate_id. If missing → KeyError (defensive — should never happen if gates.yaml and DETECTOR_REGISTRY agree).
       - Run detector: decision, suggested_action = DETECTOR_REGISTRY[gate_id].detect(payload). Note: detectors expose `detect` as a module-level function, not a bound method — call it directly: `decision, suggested_action = DETECTOR_REGISTRY[gate_id](payload)` if DETECTOR_REGISTRY stores the function, OR `.detect(payload)` if it stores a module/Protocol instance. Match whatever Plan 01's __init__.py exports — re-read that file first.
       - Call gate.resolve(decision, suggested_action) — uses Phase 34's public API, no gate.py edits.
       - Write outcome to asset bus via _write_review_outcome(gate, outcome).
       - Advance state via _advance_state_after_resolution(gate.config.phase, decision, gate).
       - For reject: include "rollback_to": suggested_action in returned dict (mirror resume_from_callback).
       - Return the outcome dict.
       - Module docstring: note Phase 40 added this auto-detect path; the 8 V8.6 gates keep pause_for_review (HIL), only redline_ gates route here.

    2. In plugins/review_gates/tools.py modify _handle_gate_submit:

       - At the top of the function, after the gate_id/episode_id presence check + GATE_REGISTRY check, add:
         ```
         if runner_hooks.is_redline_gate(gate_id):
             try:
                 outcome = runner_hooks.auto_detect_and_resolve(gate_id, episode_id, payload)
             except runner_hooks.GateMaxRetriesExceeded as exc:
                 runner_hooks.mark_episode_failed(episode_id, gate_id, exc)
                 return tool_error(str(exc), status="episode_failed", gate_id=gate_id, episode_id=episode_id)
             return tool_result({
                 "status": "auto_resolved",
                 "gate_id": gate_id,
                 "episode_id": episode_id,
                 "decision": outcome.get("decision"),
                 "suggested_action": outcome.get("suggested_action"),
                 "rollback_to": outcome.get("rollback_to"),
                 "attempt": outcome.get("attempt"),
                 "resolved_at": outcome.get("resolved_at"),
             })
         # else: fall through to existing pause_for_review path (V8.6 HIL)
         ```

       - The existing V8.6 path (lines 156-209) is UNCHANGED — just sits behind the new prefix-check branch.

    3. _handle_gates_list: no code change needed — it reads GATE_REGISTRY which is now 11-strong. Test 8 passes automatically.

    4. plugins/review_gates/__init__.py docstring update only (mention Phase 40 redline auto-detect path).

    Run pytest on test_redline_gate_wiring.py — Tests 3-8 PASS. Re-run the full review_gates test suite to confirm zero regressions on the 8-gate path.

    AVOID: modifying pause_for_review, resume_from_callback, resolve_direct, mark_episode_failed, poll_until_terminal (Phase 34 contracts frozen). ADD auto_detect_and_resolve + is_redline_gate as new functions. AVOID: changing the existing V8.6 branch in _handle_gate_submit (prepend the redline branch only).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/review_gates/tests/ -v --tb=short</automated>
  </verify>
  <done>
    - runner_hooks.auto_detect_and_resolve works for all 3 redline gate_ids
    - tools._handle_gate_submit dispatches redline_ gates to auto-detect path (status="auto_resolved")
    - V8.6 gate path unchanged (test_runner_hooks.py + test_tools_dispatch.py still pass)
    - Tests 3-8 from Task 1 all PASS
    - Full review_gates test suite green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → tools._handle_gate_submit | Untrusted gate_id + payload. gate_id must be in GATE_REGISTRY; payload shape must match detector expectations. |
| tools → runner_hooks.auto_detect_and_resolve | Internal call — trusted transit, but payload remains untrusted. |
| detector → Gate.resolve | Detector emits verdict; consumed by Phase 34's frozen resolve() path. suggested_action reaches asset bus review-outcomes slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Spoofing | redline_ prefix impersonation | mitigate | is_redline_gate() checks the prefix; gate_id must ALSO exist in GATE_REGISTRY — unknown redline_X gate_ids are rejected by the existing GATE_REGISTRY check before auto-detect runs |
| T-40-06 | Tampering | detector verdict tampering | accept | Detector is in-process stdlib code; tampering requires code-modification access which is out of threat model (operator-side) |
| T-40-07 | Repudiation | auto-resolved gate lacks human signoff | mitigate | auto_detect_and_resolve writes the full outcome to review-outcomes slot (CF-04) with reviewer_role="redline_scanner" — audit trail preserved; gates_list exposes the auto path explicitly |
| T-40-08 | Denial of Service | adversarial payload to auto-detect | accept | Detector runtime is O(n) over beats; pipeline upstream enforces payload size budget. Same disposition as T-40-03. |
| T-40-09 | Elevation of Privilege | redline gate bypassing manual review | mitigate | Only gate_ids in DETECTOR_REGISTRY can auto-resolve; misconfiguration (redline_ prefix but no detector) raises KeyError, not silent auto-approve |
</threat_model>

<verification>
- Full review_gates test suite: `python -m pytest plugins/review_gates/tests/ -v` (all green)
- 11 gates loadable: `python -c "from plugins.review_gates.gate_config import GATE_REGISTRY; assert len(GATE_REGISTRY) == 11"`
- End-to-end auto-reject: `python -c "from plugins.review_gates.runner_hooks import auto_detect_and_resolve; r = auto_detect_and_resolve('redline_emotion_desensitize', 'EP01', {'beats':[{'emotion':'anger'},{'emotion':'anger'},{'emotion':'anger'}]}); assert r['decision']=='reject', r"`
- V8.6 path untouched: `python -m pytest plugins/review_gates/tests/test_runner_hooks.py plugins/review_gates/tests/test_tools_dispatch.py -v` (zero failures)
</verification>

<success_criteria>
- gates.yaml holds 11 entries (8 byte-preserved + 3 appended); Phase 34 gate.py UNTOUCHED
- gate_config.py count check bumped 8 → 11 (only change to that file)
- runner_hooks.auto_detect_and_resolve routes redline_ gates through Plan-01 detectors; suggested_action carries forward to gate.resolve() and asset bus
- 8 V8.6 gates keep their manual HIL path (zero behavior change for pause_for_review on those gate_ids)
- gates_list reports 11 gates; tool envelope for redline auto-reject shows status="auto_resolved"
- Full review_gates test suite green
</success_criteria>

<output>
Create `.planning/phases/40-gate-redlines/40-02-SUMMARY.md` when done
</output>

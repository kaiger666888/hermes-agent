---
phase: 40-gate-redlines
plan: 03
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - skills/kais-movie-pipeline/references/review-gates.md
  - plugins/review_gates/README.md
autonomous: true
requirements: [GATE-04]
user_setup: []

must_haves:
  truths:
    - "references/review-gates.md documents the 11-gate sequence (8 V8.6 + 3 Phase 40 redline) with additive framing"
    - "The V8.6 8-gate numbering is preserved unchanged (no renumbering) — gates 9/10/11 are appended"
    - "Each new gate entry in the doc cross-references its R-section in creative-redlines.md"
    - "The doc explains the auto-detect path (Plan 02) distinct from the V8.6 manual HIL path"
    - "The plugin README mentions the redline gates + auto-detect path"
  artifacts:
    - path: "skills/kais-movie-pipeline/references/review-gates.md"
      provides: "Updated 11-gate mapping table + Phase 40 redline section"
      contains: "Gate 9"
    - path: "plugins/review_gates/README.md"
      provides: "Updated plugin README mentioning Phase 40 redline gates + auto-detect"
      contains: "Phase 40"
  key_links:
    - from: "skills/kais-movie-pipeline/references/review-gates.md §All 11 Gates"
      to: "skills/kais-movie-pipeline/references/creative-redlines.md §R1/§R3/§R4"
      via: "Gate 9 → R1, Gate 10 → R3, Gate 11 → R4 cross-reference links"
      pattern: "R1|R3|R4"
    - from: "skills/kais-movie-pipeline/references/review-gates.md §Gate Implementation"
      to: "plugins/review_gates/gates/ (Plan 01) + runner_hooks.auto_detect_and_resolve (Plan 02)"
      via: "documentation of auto-detect path vs V8.6 HIL"
      pattern: "auto_detect_and_resolve"
---

<objective>
Update review-gates.md (8→11 gates, additive) and the plugin README to
document Phase 40's redline gates + auto-detect wiring.

Purpose: ROADMAP Phase 40 SC#3 requires "documented in references/review-gates.md
(additive; V8.6 8-gate numbering unchanged)". This plan produces that
documentation. It also updates plugins/review_gates/README.md to reflect the
new gate count + auto-detect path so operators reading the plugin source
understand the architecture.

Output: 2 documentation files updated. No code changes. Can run parallel to
Plan 02 (depends only on Plan 01's detector signatures, which are needed to
accurately describe the auto-detect path).
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/kais-movie-pipeline/references/review-gates.md
@skills/kais-movie-pipeline/references/creative-redlines.md
@plugins/review_gates/README.md
@plugins/review_gates/plugin.yaml
@.planning/phases/40-gate-redlines/40-01-SUMMARY.md

<interfaces>
<!-- Plan 01 detector signatures (consumed for accurate documentation): -->
```python
# plugins/review_gates/gates/redline_emotion_desensitize.py
GATE_ID = "redline_emotion_desensitize"
REDLINE_REF = "creative-redlines.md §R1"
def detect(payload: dict) -> tuple[str, str | None]: ...

# plugins/review_gates/gates/redline_no_cold_open.py
GATE_ID = "redline_no_cold_open"
REDLINE_REF = "creative-redlines.md §R3"

# plugins/review_gates/gates/redline_unfinished_ending.py
GATE_ID = "redline_unfinished_ending"
REDLINE_REF = "creative-redlines.md §R4"
```

<!-- Plan 02 runner_hooks entry point (consumed for accurate documentation): -->
```python
# plugins/review_gates/runner_hooks.py
def is_redline_gate(gate_id: str) -> bool: ...
def auto_detect_and_resolve(gate_id: str, episode_id: str, payload: dict) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update references/review-gates.md — 8→11 gate table + Phase 40 redline section</name>
  <files>skills/kais-movie-pipeline/references/review-gates.md</files>
  <action>
    Additive-only update to the existing V8.6 8-gate mapping doc. Preserve all existing content byte-for-byte — append a new Phase 40 section + extend the implementation table. Specifically:

    1. Header update: change "8-Gate Per-Phase Mapping" → "11-Gate Per-Phase Mapping (8 V8.6 + 3 Phase 40 Redline, additive)". Update the **Summary** section to mention the 3 redline gates added in Phase 40 (additive, no replacement).

    2. The existing "## The 8 Gates" table is PRESERVED unchanged. Add a NEW section immediately after titled "## Phase 40 Redline Gates (9-11, additive)" with its own 3-row table:

       | # | Phase 40 Gate | Triggers After Gate | Reviewer | Decision Mode | Source |
       |---|---------------|---------------------|----------|---------------|--------|
       | 9 | `redline_emotion_desensitize` | Gate 8 (delivery-gate) passes, before final master.mp4 release | `redline_scanner` (auto-detect) | Auto-reject on violation | `creative-redlines.md §R1` |
       | 10 | `redline_no_cold_open` | Gate 8 passes | `redline_scanner` | Auto-reject | `creative-redlines.md §R3` |
       | 11 | `redline_unfinished_ending` | Gate 8 passes | `redline_scanner` | Auto-reject | `creative-redlines.md §R4` |

       Include for each row:
       - The R-section operational definition (1-sentence summary lifted from creative-redlines.md with attribution)
       - The suggested_action formula_id (emotion-break-up / cold-open-conflict-hook / open-question-cliffhanger)
       - A note that suggested_action references a `formula_library` entry (read-side lookup, Phase 39) — operator applies the proven fix pattern
       - Cross-link to creative-redlines.md §R1/§R3/§R4 with relative path

    3. Update the "## All 8 Gates" heading to "## All 11 Gates (Phase 35+36 Complete, Phase 40 Redline Added)". Add 3 new rows to the implementation-wiring table (the one that maps gate → module GATE_ID → mode):

       | Gate | V8.6 Gate Name | Trigger Module | Module GATE_ID | Mode | Reviewer(s) | Status |
       |------|----------------|----------------|-------------------|------|-------------|--------|
       | **Gate 9** | R1 emotion desensitization | `runner_hooks.auto_detect_and_resolve` | `"redline_emotion_desensitize"` | Auto-detect (blocking w/ detector) | `redline_scanner` (DETECTOR_REGISTRY) | **Complete** (Phase 40) |
       | **Gate 10** | R3 no cold open | `runner_hooks.auto_detect_and_resolve` | `"redline_no_cold_open"` | Auto-detect | `redline_scanner` | **Complete** (Phase 40) |
       | **Gate 11** | R4 unfinished ending | `runner_hooks.auto_detect_and_resolve` | `"redline_unfinished_ending"` | Auto-detect | `redline_scanner` | **Complete** (Phase 40) |

    4. Update the "## Gate Implementation" section to add a paragraph documenting the auto-detect path: Phase 40 added `runner_hooks.auto_detect_and_resolve()` and `runner_hooks.is_redline_gate()`. When `gate_submit` is called with a gate_id starting with `redline_`, the runner looks up the detector in `plugins.review_gates.gates.DETECTOR_REGISTRY`, runs it on the payload, and calls `Gate.resolve()` with the detector's verdict. This bypasses the manual HIL `gate_resolve` path used by the 8 V8.6 gates. Cite the rationale: redline violations are structural / deterministic — no human judgment needed.

    5. Update the "## See Also" section:
       - Add link to `creative-redlines.md` with note "Phase 40 redline gates 9-11 implement R1 / R3 / R4 from this ref"
       - Update the bullet `gates.yaml` description: "(Phase 34 canonical gate config; Phase 40 expanded 8 → 11 gates)"

    6. Update the "Last-verified" header stamp from 2026-06-26 to 2026-06-26 (Phase 40) — keep the date but add Phase 40 context.

    AVOID: editing any of the existing 8-gate table rows. AVOID: changing the V8.6 numbering. AVOID: removing the "Hard vs Soft Gates" section (preserved verbatim).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && grep -c "Gate 9\|Gate 10\|Gate 11\|redline_emotion_desensitize\|redline_no_cold_open\|redline_unfinished_ending\|auto_detect_and_resolve" skills/kais-movie-pipeline/references/review-gates.md | grep -qv '^0$' && grep -c "creative-redlines" skills/kais-movie-pipeline/references/review-gates.md | grep -qv '^0$' && echo OK</automated>
  </verify>
  <done>
    - references/review-gates.md contains all 3 new gate names + Phase 40 section
    - Existing 8-gate table rows byte-preserved (use git diff to verify only additive lines)
    - Cross-references to creative-redlines.md §R1/§R3/§R4 present
    - Auto-detect path documented in Gate Implementation section
    - See Also section updated
  </done>
</task>

<task type="auto">
  <name>Task 2: Update plugins/review_gates/README.md — Phase 40 redline + auto-detect path</name>
  <files>plugins/review_gates/README.md</files>
  <action>
    Update the plugin README to reflect Phase 40 additions:

    1. Update the description paragraph: change "8 V8.6 gate YAML config" → "11 gate YAML config (8 V8.6 + 3 Phase 40 redline)". Mention that Phase 40 added 3 cross-platform redline gates (R1/R3/R4) registered additively.

    2. Update the "## Exposed tools" list:
       - Update gates_list description from "(8 V8.6 gates)" to "(11 gates: 8 V8.6 + 3 redline)"
       - Add a note under gate_submit: "For gate_ids starting with 'redline_', the runner auto-resolves via Plan-01 detectors (no manual gate_resolve needed). For the 8 V8.6 gate_ids, the manual HIL resolution path is preserved."

    3. Add a new "## Phase 40 Additions" section:
       - 3 new gate_ids: redline_emotion_desensitize / redline_no_cold_open / redline_unfinished_ending
       - Each implements one creative-redline (R1 / R3 / R4) — link to creative-redlines.md
       - Detectors live in `gates/` subpackage (pure stdlib, D-34-01 extended); each emits (decision, suggested_action) where suggested_action references a formula_library entry (read-side lookup)
       - Auto-detect path: `runner_hooks.auto_detect_and_resolve()` + `runner_hooks.is_redline_gate()` — routes redline_ gates through detectors; 8 V8.6 gates unchanged
       - Cross-link to `references/review-gates.md` for the 11-gate per-phase mapping

    4. Update the "## Status" section:
       - Add a "Phase 40" line: "Phase 40: 3 redline gates (R1/R3/R4) added additively. Detector modules in `gates/` subpackage + auto-detect path in `runner_hooks.auto_detect_and_resolve`. Plugin version bumped 0.1.0 → 0.2.0."

    AVOID: removing or rewriting the Phase 31 / Phase 34 status history lines — additive update only.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && grep -c "Phase 40\|redline_emotion_desensitize\|auto_detect_and_resolve" plugins/review_gates/README.md | grep -qv '^0$' && echo OK</automated>
  </verify>
  <done>
    - plugins/review_gates/README.md mentions Phase 40 + 3 redline gates + auto-detect path
    - gates_list description updated to 11 gates
    - Phase 31 / Phase 34 status history preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only plan. No code execution surface. No trust boundaries crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-10 | Information Disclosure | doc mentions formula_id names | accept | Public pattern names ("emotion-break-up", "cold-open-conflict-hook") — no proprietary information, no PII |
</threat_model>

<verification>
- review-gates.md grep for all 3 gate names + creative-redlines cross-ref: `grep -E 'redline_(emotion_desensitize|no_cold_open|unfinished_ending)|creative-redlines' skills/kais-movie-pipeline/references/review-gates.md`
- Existing 8-gate rows preserved (git diff shows only additions): `git diff skills/kais-movie-pipeline/references/review-gates.md | grep '^-' | grep -v '^---' | wc -l` should be 0 (or near-zero, only header line)
- Plugin README mentions Phase 40 + auto-detect: `grep -E 'Phase 40|auto_detect_and_resolve|redline_emotion_desensitize' plugins/review_gates/README.md`
</verification>

<success_criteria>
- references/review-gates.md documents the 11-gate sequence additively (8 V8.6 + 3 Phase 40 redline); V8.6 numbering preserved
- Each Phase 40 gate row cross-references its R-section in creative-redlines.md
- Auto-detect path (Plan 02) documented in Gate Implementation section
- Plugin README updated to mention Phase 40 + auto-detect + 11 gates
- No code changes (documentation-only plan)
</success_criteria>

<output>
Create `.planning/phases/40-gate-redlines/40-03-SUMMARY.md` when done
</output>

---
phase: 35-soul-md-identity-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.hermes/SOUL.md
  - ~/.hermes/SOUL.md.openclaw-backup-2026-06-25
  - .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
autonomous: true
requirements: [SOUL-01, SOUL-02, SOUL-03]
requirements_tailored_note: |
  SOUL-01 (non-destructive integration) → Task 1 + Task 2 preserve hermes-default content.
  SOUL-02 (trigger-mode adaptation + source tagging) → Task 2 wraps openclaw block with
    sectioned H2 header + per-rule source annotations + adapts mcp:hermes_* targets.
  SOUL-03 (backup + transformation note) → Task 1 creates backup, Task 3 authors the note.

must_haves:
  truths:
    - "Operator can open ~/.hermes/SOUL.md and read both the original Hermes identity statement (preserved byte-for-byte) and a clearly demarcated section of openclaw-origin routing rules"
    - "Each openclaw-origin rule carries a visible 'Source: openclaw 迁移 (2026-06-25)' annotation"
    - "An operator can read the transformation note under .planning/ to see exactly which openclaw rules were integrated, adapted, or dropped — with rationale for each"
    - "A verbatim backup of the original openclaw SOUL.md exists at ~/.hermes/SOUL.md.openclaw-backup-2026-06-25 with preserved mtime"
    - "A Hermes user issuing an AIGC immediate-execution prompt (draw/video/tts/...) sees the SOUL instruct routing to local execution; a cognitive prompt (plan/剧本/...) routes to the configured memory/MCP surface; default prompts go to the configured LLM"
  artifacts:
    - path: ~/.hermes/SOUL.md
      provides: "Hermes identity + openclaw AIGC routing intelligence, non-destructively integrated"
      contains: "openclaw 迁移规则"
    - path: ~/.hermes/SOUL.md.openclaw-backup-2026-06-25
      provides: "Verbatim backup of openclaw source SOUL.md at migration time"
      contains: "Kais AIGC Director"
    - path: .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
      provides: "Audit trail — fate of every openclaw rule (integrated / adapted / dropped)"
      contains: "Transformation Note"
  key_links:
    - from: ~/.hermes/SOUL.md
      to: skills/movie-experts/* + skills/autonomous-ai-agents/* + ~/.hermes/memories/
      via: "natural-language routing instructions (adapted from openclaw's regex/MCP notation)"
      pattern: "即时执行|认知|MCP|local"
    - from: .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
      to: ~/.openclaw/SOUL.md
      via: "rule-by-rule mapping (each openclaw line → integrated / adapted / dropped + rationale)"
      pattern: "integrated|adapted|dropped"
---

<objective>
Non-destructively integrate openclaw's AIGC routing intelligence (24-line `~/.openclaw/SOUL.md`: identity + 4 routing categories covering 6 immediate-execution patterns, 5 cognitive MCP routes, 1 expert-management command, and 1 default route) into `~/.hermes/SOUL.md`, while:

1. Preserving the existing 513-byte Hermes identity statement byte-for-byte in its original section
2. Tagging every openclaw-origin rule with source attribution ("openclaw 迁移" + 2026-06-25)
3. Adapting openclaw's regex/MCP-notation routing (which assumed an MCP server with `mcp:hermes_plan`, `mcp:hermes_memory_*`, etc.) to hermes-agent's actual invocation surface (slash commands, skill discovery, configured LLM)
4. Producing a verbatim backup at `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` and a transformation note (the repo-commit audit artifact) recording the fate of every openclaw rule

Purpose: SOUL-01 (non-destructive integration), SOUL-02 (trigger-mode adaptation + source tagging), SOUL-03 (backup + audit trail). This plan is the **only** plan for Phase 35 — single plan suffices because the 24-line source × 2 outputs (modified target + transformation note) fit comfortably within a single executor's context budget (~25-30% estimated).

Output: One modified operator-state file (`~/.hermes/SOUL.md`), one new operator-state backup file, one repo-commit transformation note. Only the transformation note gets git-committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-soul-md-identity-enhancement/35-CONTEXT.md

<interfaces>
<!-- Source + target files the executor needs to read before writing. -->

## Source: ~/.openclaw/SOUL.md (24 lines, 975 bytes — READ-ONLY INPUT)

Full content (do NOT modify the source):

```markdown
# Kais AIGC Director — 系统路由规则

## 身份
你是 Kais AIGC 短剧生产管线的主控 Agent。你是"手"——负责执行，Hermes 是"脑"——负责思考。

## 路由规则

### 即时执行命令（本地 Skill，不走 Hermes）
- `^(?i)(draw|video|tts|run|status|queue)` → local_skill
- 直接调用本地 AIGC Skill 执行，不经过认知层

### 认知类命令（MCP 调用 Hermes）
- `(?i)(plan|规划|剧本|分镜|怎么拍|帮我设计)` → mcp:hermes_plan
- `(?i)(记住|保存|这个角色|风格设定|更新设定)` → mcp:hermes_memory_write
- `(?i)(之前|上次|那个项目|角色设定|查一下)` → mcp:hermes_memory_read
- `(?i)(复盘|总结|为什么不好|哪里有问题)` → mcp:hermes_reflect
- `(?i)(学习|进化|改进|提升)` → mcp:hermes_learn

### 专家管理命令
- `^/expert` → local_skill:expert_manager
- 管理 approve/reject/status/rollback

### 默认
- 简单对话走本地 LLM（GLM-4-flash）
```

4 routing categories, 6 immediate-execution regex tokens (draw/video/tts/run/status/queue), 5 cognitive MCP routes, 1 expert-management command (`/expert`), 1 default route (GLM-4-flash local LLM).

## Target: ~/.hermes/SOUL.md (513 bytes, single-line — DO NOT OVERWRITE)

Current content (must be preserved byte-for-byte in its own section of the integrated file):

```
You are Hermes Agent, an intelligent AI assistant created by Nous Research. You are helpful, knowledgeable, and direct. You assist users with a wide range of tasks including answering questions, writing and editing code, analyzing information, creative work, and executing actions via your tools. You communicate clearly, admit uncertainty when appropriate, and prioritize being genuinely useful over being verbose unless otherwise directed below. Be targeted and efficient in your exploration and investigations.
```

**IMPORTANT correction from CONTEXT.md:** CONTEXT.md described the hermes SOUL.md as "0 bytes (empty)". This is **wrong** — the file actually contains 513 bytes of Nous Research Hermes identity content on a single line. SC #3 ("preserve original hermes-default content byte-for-byte") is therefore a non-trivial constraint. The integration MUST be additive: keep the existing single paragraph verbatim, then append the openclaw section below it.

## Hermes MCP reality check (from ./CLAUDE.md)

Hermes exposes an MCP server (`mcp_serve.py`) with tools like `conversations_list`, `messages_read`, `messages_send`, `events_poll`, `permissions_respond`, `channels_list` — these do **not** match openclaw's `mcp:hermes_plan` / `mcp:hermes_memory_write|read` / `mcp:hermes_reflect` / `mcp:hermes_learn` targets. The openclaw targets assumed a different MCP server topology. Plan must document this mismatch in the transformation note and adapt by:
- Mapping openclaw's `mcp:hermes_memory_*` to hermes' actual memory surface (`plugins/memory/mem0/` plugin + mem0 backend, available from hermes conversations after Phase 36 ships)
- Mapping `mcp:hermes_plan` to hermes skill discovery (`/skill screenplay`, `/skill scene_builder` etc. in `skills/movie-experts/`)
- Mapping `mcp:hermes_reflect` / `mcp:hermes_learn` to hermes' curator + memory write-back (operator-configurable)

The integrated SOUL.md expresses these as **natural-language routing guidance** (not raw regex + MCP-notation) because hermes is a single-agent model with skill-discovery + slash commands, not a two-agent MCP topology.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup openclaw SOUL.md verbatim + confirm hermes-target content</name>
  <read_first>
    - ~/.openclaw/SOUL.md (full 24 lines — the source to back up; read to confirm content before copying)
    - ~/.hermes/SOUL.md (full single-line — confirm 513 bytes of Hermes identity content present, NOT empty; this becomes the "preserve byte-for-byte" baseline)
    - .planning/phases/35-soul-md-identity-enhancement/35-CONTEXT.md (§"Key Design Decisions" — confirms `cp -p` to preserve mtime)
  </read_first>
  <files>
    - ~/.hermes/SOUL.md.openclaw-backup-2026-06-25 (new file — operator-state, NOT git-committed)
  </files>
  <action>
    Create a verbatim backup of the openclaw source SOUL.md at `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` using `cp -p ~/.openclaw/SOUL.md ~/.hermes/SOUL.md.openclaw-backup-2026-06-25` (the `-p` flag preserves mtime/timestamps per CONTEXT.md decision §3). Do NOT transform, reformat, or annotate the backup — it must be a byte-for-byte copy of the openclaw original. After the copy, verify equality with `cmp ~/.openclaw/SOUL.md ~/.hermes/SOUL.md.openclaw-backup-2026-06-25` (must return exit 0 / no diff). Confirm `wc -c` of both files reads 975 bytes.

    Then capture the current hermes-target content as the non-destructive baseline: run `cat ~/.hermes/SOUL.md` and save the output to memory (513 bytes, single line, beginning "You are Hermes Agent, an intelligent AI assistant created by Nous Research..."). Task 2 must reproduce this exact byte sequence in its original section of the integrated file.

    NOTE: This task only creates the backup file — it does NOT modify `~/.hermes/SOUL.md` itself (Task 2 does that).
  </action>
  <verify>
    <automated>
    BACKUP=~/.hermes/SOUL.md.openclaw-backup-2026-06-25 &&
    [ -f "$BACKUP" ] &&
    cmp -s ~/.openclaw/SOUL.md "$BACKUP" &&
    [ "$(wc -c < ~/.openclaw/SOUL.md)" = "$(wc -c < "$BACKUP")" ] &&
    echo "BACKUP_OK: verbatim copy, byte count matches"
    </automated>
  </verify>
  <done>
    - Backup file exists at `~/.hermes/SOUL.md.openclaw-backup-2026-06-25`
    - `cmp` returns exit 0 (byte-for-byte identical to openclaw source)
    - Both files are 975 bytes
    - mtime preserved (use `ls -la` to spot-check the timestamp is the openclaw source's mtime, not "now")
    - Hermes-target current content (513 bytes) has been recorded as the preservation baseline for Task 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Write integrated ~/.hermes/SOUL.md (preserve Hermes default + add openclaw 迁移 section with source tagging + adapted routing)</name>
  <read_first>
    - ~/.openclaw/SOUL.md (source — 4 routing categories + identity)
    - ~/.hermes/SOUL.md (target — 513 bytes of Hermes default identity that MUST be preserved verbatim as the first paragraph)
    - .planning/phases/35-soul-md-identity-enhancement/35-CONTEXT.md (§"Key Design Decisions" — source-tag format, header structure)
    - .planning/ROADMAP.md (Phase 35 SC #1-#4 — the load-bearing constraints)
    - ./CLAUDE.md (§"Architecture → mcp_serve.py" — confirms hermes MCP tool names do not match openclaw's `mcp:hermes_*`)
    - Task 1 output: the captured 513-byte Hermes-default baseline (preserve byte-for-byte as the file's opening section)
  </read_first>
  <files>
    - ~/.hermes/SOUL.md (operator-state, OVERWRITTEN with integrated content — NOT git-committed)
  </files>
  <action>
    Write a new integrated `~/.hermes/SOUL.md` with the following structure (use the Write tool — never heredoc/Bash). The file has THREE sections:

    **Section A — Original Hermes identity (UNMODIFIED, byte-for-byte from Task 1 baseline).**
    Open with the exact 513-byte single-line paragraph: "You are Hermes Agent, an intelligent AI assistant created by Nous Research. You are helpful, knowledgeable, and direct. You assist users with a wide range of tasks including answering questions, writing and editing code, analyzing information, creative work, and executing actions via your tools. You communicate clearly, admit uncertainty when appropriate, and prioritize being genuinely useful over being verbose unless otherwise directed below. Be targeted and efficient in your exploration and investigations."
    Do NOT alter a single character. Add a blank line after it.

    **Section B — Source header for the openclaw block.**
    Add an H2 section header exactly: `## openclaw 迁移规则 (Source: openclaw SOUL.md, migrated 2026-06-25)`. Follow with a one-line framing note: "Below routing rules were migrated non-destructively from openclaw's `~/.openclaw/SOUL.md` on 2026-06-25. Adapted to hermes' single-agent + skill-discovery invocation model. Original openclaw source preserved verbatim at `~/.hermes/SOUL.md.openclaw-backup-2026-06-25`." Then a blank line.

    **Section C — Adapted openclaw routing rules.** Render the 4 categories as natural-language routing guidance (NOT raw regex/MCP notation — hermes is single-agent). For each category, include:
    - An H3 header: `### 即时执行命令 (Immediate Execution — Local Skill)` / `### 认知类命令 (Cognitive — MCP / Memory / Skill)` / `### 专家管理命令 (Expert Management)` / `### 默认 (Default)`
    - A line listing the trigger tokens: "Triggers: draw, video, tts, run, status, queue" (or the cognitive tokens: plan, 规划, 剧本, 分镜, 怎么拍, 帮我设计 / 记住, 保存, 这个角色, 风格设定, 更新设定 / 之前, 上次, 那个项目, 角色设定, 查一下 / 复盘, 总结, 为什么不好, 哪里有问题 / 学习, 进化, 改进, 提升 / `/expert`)
    - A "Routes to:" line adapted to hermes — see mapping below
    - A source-tag line at the end of each category: `> **Source:** openclaw 迁移 (2026-06-25)`

    **Routing adaptation map (openclaw target → hermes equivalent):**
    - `local_skill` (immediate execution) → "local AIGC skill execution (skills/autonomous-ai-agents/* and skills/movie-experts/* as applicable); bypass deep cognitive routing"
    - `mcp:hermes_plan` (plan/剧本/分镜) → "skill discovery for planning/screenplay/scene-builder skills (e.g., `/skill screenplay`, `/skill scene_builder` in skills/movie-experts/)"
    - `mcp:hermes_memory_write` (记住/保存/角色/风格) → "memory write via mem0 backend (plugins/memory/mem0/) — operator must configure memory plugin; until Phase 36 ingestion completes, route to note-taking via terminal/file tools"
    - `mcp:hermes_memory_read` (之前/上次/角色设定) → "memory query via mem0 backend; fallback to AGENTS.md / conversation history recall"
    - `mcp:hermes_reflect` (复盘/总结) → "structured reflection using terminal/file tools to capture lessons-learned; curator review (agent/curator.py) may also fire"
    - `mcp:hermes_learn` (学习/进化/改进) → "self-improvement via curator + memory write-back; surface improvement as a concrete proposed edit to skills/refs"
    - `local_skill:expert_manager` (`/expert`) → "expert management via hermes slash commands (e.g., `/skill` discovery + skill pruning through agent/curator.py); manage approve/reject/status/rollback as applicable"
    - `GLM-4-flash` (default route) → "configured local LLM (per cli-config.yaml `model:` — drop the openclaw GLM-4-flash hard reference; the default routes simple chat to whatever model hermes is configured with)"

    **Adaptation note about the identity statement.** openclaw's identity ("你是 Kais AIGC Director 主控 Agent. 你是'手'——负责执行，Hermes 是'脑'——负责思考.") framed a two-agent model where openclaw=hand, hermes=brain. This framing does NOT translate to hermes' single-agent model. Include a brief note under Section B's framing paragraph: "openclaw's two-agent '手/脑' identity framing is dropped (no direct single-agent analog); hermes' Section A identity statement remains authoritative." Do NOT include the openclaw identity statement itself in the integrated file.

    After writing, verify the file: confirm Section A is byte-identical to Task 1's baseline (`head -n 1` of the integrated file compared to the original), confirm source tag count (`grep -c "openclaw 迁移" ~/.hermes/SOUL.md` returns ≥ 5 — 1 in header + 4 in categories), confirm date tag count (`grep -c "2026-06-25" ~/.hermes/SOUL.md` returns ≥ 5), confirm overall byte count (`wc -c` returns > 975 — additive growth).
  </action>
  <verify>
    <automated>
    # (a) Hermes-default preservation: first paragraph byte-identical to pre-integration baseline
    head -n 1 ~/.hermes/SOUL.md | grep -q "You are Hermes Agent, an intelligent AI assistant created by Nous Research" &&
    # (b) openclaw section present
    grep -q "^## openclaw 迁移规则 (Source: openclaw SOUL.md, migrated 2026-06-25)" ~/.hermes/SOUL.md &&
    # (c) source tagging: at least 1 header + 4 per-category tags = 5+
    [ "$(grep -c 'openclaw 迁移' ~/.hermes/SOUL.md)" -ge 5 ] &&
    # (d) date tagging: at least 5 occurrences of 2026-06-25
    [ "$(grep -c '2026-06-25' ~/.hermes/SOUL.md)" -ge 5 ] &&
    # (e) all 4 routing categories present
    grep -q "即时执行" ~/.hermes/SOUL.md &&
    grep -q "认知" ~/.hermes/SOUL.md &&
    grep -q "专家管理" ~/.hermes/SOUL.md &&
    grep -q "默认" ~/.hermes/SOUL.md &&
    # (f) additive growth: integrated file larger than openclaw source alone
    [ "$(wc -c < ~/.hermes/SOUL.md)" -gt 975 ] &&
    # (g) GLM-4-flash hard reference removed (genericized)
    ! grep -q "GLM-4-flash" ~/.hermes/SOUL.md &&
    # (h) 手/脑 framing dropped
    ! grep -q "你是\"手\"" ~/.hermes/SOUL.md &&
    echo "SOUL_INTEGRATION_OK"
    </automated>
  </verify>
  <done>
    - `~/.hermes/SOUL.md` opens with the original 513-byte Hermes identity paragraph, byte-for-byte unchanged
    - A clearly demarcated `## openclaw 迁移规则 (Source: openclaw SOUL.md, migrated 2026-06-25)` section follows
    - All 4 openclaw routing categories present (即时执行 / 认知 / 专家管理 / 默认), each with source-tag annotation
    - openclaw's `mcp:hermes_*` targets adapted to hermes equivalents (natural-language routing guidance, not raw MCP notation)
    - GLM-4-flash hard reference genericized to "configured local LLM"
    - openclaw's "手/脑" identity framing dropped (with explicit note in Section B)
    - All 4 SOUL success criteria in ROADMAP.md Phase 35 satisfied at the file-content level
  </done>
</task>

<task type="auto">
  <name>Task 3: Author transformation note (repo-commit audit artifact documenting each openclaw rule's fate)</name>
  <read_first>
    - ~/.openclaw/SOUL.md (the source whose rules must each be accounted for)
    - ~/.hermes/SOUL.md (the integrated target just written in Task 2 — read to confirm what landed where)
    - .planning/ROADMAP.md (Phase 35 SC #4 — transformation note requirement)
    - .planning/REQUIREMENTS.md (SOUL-03 — backup + transformation note)
    - .planning/phases/35-soul-md-identity-enhancement/35-CONTEXT.md (§"Transformation note structure" — must cover identity + 6 immediate-execution + 5 cognitive + 1 expert + 1 default)
  </read_first>
  <files>
    - .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md (repo-commit, audit artifact)
  </files>
  <action>
    Author `.planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md` — a rule-by-rule audit of every openclaw SOUL.md element's fate. Structure as follows (use the Write tool):

    **H1:** `# Phase 35 Transformation Note — openclaw SOUL.md Migration`

    **Frontmatter block** (key-value pairs, not YAML):
    - Migration date: 2026-06-25
    - Source file: `~/.openclaw/SOUL.md` (24 lines, 975 bytes)
    - Backup file: `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` (verbatim, mtime preserved via `cp -p`)
    - Target file: `~/.hermes/SOUL.md` (integrated; original 513-byte Hermes identity preserved byte-for-byte in Section A; openclaw routing rules integrated into Section C under H2 `## openclaw 迁移规则 (Source: openclaw SOUL.md, migrated 2026-06-25)`)
    - Requirements closed: SOUL-01, SOUL-02, SOUL-03

    **Section 1: Hermes-target baseline correction.** A short paragraph noting that CONTEXT.md described the hermes SOUL.md as "0 bytes (empty)" but pre-integration inspection revealed 513 bytes of Nous Research identity content. State that SC #3 ("preserve original hermes-default content byte-for-byte") was therefore treated as a non-trivial constraint and the integration is strictly additive (original paragraph preserved as Section A).

    **Section 2: Identity statement fate.** Document that openclaw's identity statement ("你是 Kais AIGC Director 主控 Agent. 你是'手'... Hermes 是'脑'...") was **dropped** from the integrated file with rationale: it assumed a two-agent (openclaw=hand / hermes=brain) topology that doesn't translate to hermes' single-agent model. The Hermes Section A identity ("You are Hermes Agent...") remains authoritative.

    **Section 3: Immediate-execution rules (6 tokens).** Table with one row per token group (or one row per category if all 6 share the same fate). Columns: `Token` | `openclaw Target` | `Hermes Adaptation` | `Fate`. All 6 tokens (draw, video, tts, run, status, queue) → `local_skill` → integrated as natural-language routing to `skills/autonomous-ai-agents/*` + `skills/movie-experts/*` (Fate: **integrated + adapted**).

    **Section 4: Cognitive MCP routes (5 routes).** Table with one row per MCP route. Columns: `Trigger Pattern` | `openclaw MCP Target` | `Hermes Equivalent` | `Fate`. The 5 rows:
    - (plan|规划|剧本|分镜|怎么拍|帮我设计) | `mcp:hermes_plan` | skill discovery (`/skill screenplay`, `/skill scene_builder` etc.) | **integrated + adapted** (MCP target had no direct hermes equivalent — hermes uses skill-discovery not a plan-MCP)
    - (记住|保存|这个角色|风格设定|更新设定) | `mcp:hermes_memory_write` | mem0 backend write (plugins/memory/mem0/) | **integrated + adapted** (memory plugin surface; operator-configurable; Phase 36 ingestion expands available memories)
    - (之前|上次|那个项目|角色设定|查一下) | `mcp:hermes_memory_read` | mem0 backend query + AGENTS.md recall | **integrated + adapted**
    - (复盘|总结|为什么不好|哪里有问题) | `mcp:hermes_reflect` | structured reflection via terminal/file tools + agent/curator.py review | **integrated + adapted** (curator is hermes' built-in reflection mechanism)
    - (学习|进化|改进|提升) | `mcp:hermes_learn` | curator + memory write-back | **integrated + adapted** (hermes has no discrete `learn` MCP; curator + memory forms the loop)

    **Section 5: Expert-management command (1 command).** Row for `/expert` → `local_skill:expert_manager` → adapted to hermes slash-command + curator-based skill pruning (Fate: **integrated + adapted** — hermes' `/skill` discovery + curator.py covers the approve/reject/status/rollback lifecycle).

    **Section 6: Default route (1 rule).** openclaw routed simple chat to local `GLM-4-flash`. Hermes has no fixed default LLM — model is operator-configured via `cli-config.yaml`. Fate: **integrated + adapted** — the default-route rule is integrated but genericized to "configured local LLM (per cli-config.yaml `model:`)". The openclaw `GLM-4-flash` hard reference is **dropped** (with rationale: hermes supports arbitrary model/provider switching; hardcoding a specific model would constrain the operator).

    **Section 7: Cross-reference to operator-state files.** A short note that the integrated `~/.hermes/SOUL.md` and the backup `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` are **operator-state files** (in `~/.hermes/`, not the repo) — they are NOT git-committed by this plan. This transformation note under `.planning/phases/35-soul-md-identity-enhancement/` is the only repo-commit artifact for Phase 35 and serves as the audit trail for the operator-state changes (per ROADMAP §"Repo-commit paths" / "Operator-state paths" split).

    **Section 8: Success-criteria traceability.** A short table mapping each SC (#1-#4) to the artifact/section that satisfies it:
    - SC #1 (immediate-execution routes locally) → Section 4 of integrated SOUL.md (`### 即时执行命令` block)
    - SC #2 (cognitive routes to MCP; default to LLM) → Section 4 + Section 7 of integrated SOUL.md (cognitive + default blocks)
    - SC #3 (non-destructive + source-tagged) → Section A (Hermes default preserved byte-for-byte) + H2 header + per-category source tags
    - SC #4 (backup + transformation note) → `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` (Task 1) + this document (Task 3)

    After writing, verify each of the 4 categories has at least one row in the corresponding table and the file mentions all 6 immediate-execution tokens + all 5 cognitive MCP targets + `/expert` + the default route.
  </action>
  <verify>
    <automated>
    NOTE=.planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md &&
    [ -f "$NOTE" ] &&
    # All 4 routing categories covered
    grep -q "即时执行" "$NOTE" &&
    grep -q "认知" "$NOTE" &&
    grep -q "专家管理" "$NOTE" &&
    grep -q "默认" "$NOTE" &&
    # All 5 cognitive MCP targets mentioned
    grep -q "hermes_plan" "$NOTE" &&
    grep -q "hermes_memory_write" "$NOTE" &&
    grep -q "hermes_memory_read" "$NOTE" &&
    grep -q "hermes_reflect" "$NOTE" &&
    grep -q "hermes_learn" "$NOTE" &&
    # Identity framing documented
    grep -q "手" "$NOTE" &&
    # GLM-4-flash drop documented
    grep -q "GLM-4-flash" "$NOTE" &&
    # All 4 fates represented
    grep -q "integrated + adapted\|integrated+adapted" "$NOTE" &&
    # SC traceability
    grep -q "SC #1" "$NOTE" &&
    grep -q "SC #4" "$NOTE" &&
    echo "TRANSFORMATION_NOTE_OK"
    </automated>
  </verify>
  <done>
    - `.planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md` exists
    - Covers all 4 openclaw routing categories with per-rule fate (integrated / adapted / dropped)
    - Documents the CONTEXT.md baseline correction (513 bytes, not 0 bytes)
    - Documents identity-statement drop with rationale
    - Documents GLM-4-flash drop with rationale
    - Explicitly notes operator-state vs repo-commit file split
    - Includes SC #1-#4 traceability table
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem → ~/.hermes/SOUL.md | Integrated SOUL.md is auto-loaded by hermes-agent at conversation start (per CLAUDE.md §"Configuration"). Any content here shapes agent behavior on every subsequent conversation. |
| .planning/ repo → git history | The transformation note is the only repo-commit artifact. Its content becomes part of the audit trail — incorrect documentation here would mislead future v8.0 planning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | ~/.hermes/SOUL.md (operator-state, not repo-tracked) | accept | Operator-state file bypasses repo review by design (per ROADMAP §"Operator-state paths"). The transformation note (T-35-04) provides the audit trail; operator can self-review by diffing against the openclaw backup. |
| T-35-02 | Information disclosure | openclaw identity statement contains Kai's project naming ("Kais AIGC Director") | accept | The identity statement is **dropped** from the integrated file (Section 2 of transformation note). Backup file preserves it verbatim but lives in operator-only `~/.hermes/`. |
| T-35-03 | Repudiation | Operator-state changes (~/.hermes/SOUL.md) leave no repo commit trail | mitigate | Task 3 transformation note is the mitigation — it is repo-committed and records every rule's fate (SOUL-03 explicit). |
| T-35-04 | Tampering | GLM-4-flash hard reference + `mcp:hermes_*` targets would force a specific provider/MCP topology on hermes operators | mitigate | Task 2 genericizes GLM-4-flash to "configured local LLM" and adapts MCP targets to natural-language routing guidance. Task 3 documents the adaptation. |
| T-35-05 | Spoofing | If source tags were missing, future operators could mistake openclaw-origin rules for hermes-native | mitigate | Task 2 enforces 5+ source-tag occurrences (1 header + 4 per-category); `<verify>` step (c) blocks completion if tag count < 5. |
| T-35-SC | Tampering | N/A — no package-manager installs in this phase | accept | Phase 35 has zero pip/npm/cargo install tasks. Supply-chain gate does not apply. |
</threat_model>

<verification>
End-to-end phase checks (run after all 3 tasks complete):

1. **Backup integrity:** `cmp ~/.openclaw/SOUL.md ~/.hermes/SOUL.md.openclaw-backup-2026-06-25` returns exit 0 (verbatim copy, mtime preserved).

2. **Non-destructive integration:** The first paragraph of `~/.hermes/SOUL.md` is byte-identical to the pre-integration Hermes identity content (513 bytes starting "You are Hermes Agent, an intelligent AI assistant created by Nous Research"). Verified via `head -n 1 ~/.hermes/SOUL.md` containing that string.

3. **Source tagging:** `grep -c "openclaw 迁移" ~/.hermes/SOUL.md` returns ≥ 5; `grep -c "2026-06-25" ~/.hermes/SOUL.md` returns ≥ 5.

4. **Routing categories present:** `grep -q "即时执行"`, `grep -q "认知"`, `grep -q "专家管理"`, `grep -q "默认"` all succeed on `~/.hermes/SOUL.md`.

5. **Adaptation enforcement:** `! grep -q "GLM-4-flash"` and `! grep -q "你是\"手\""` on `~/.hermes/SOUL.md` (hard reference and two-agent framing both removed).

6. **Transformation note completeness:** The transformation note covers all 4 categories, all 5 cognitive MCP targets, identity framing, GLM-4-flash, and SC traceability.

7. **Repo vs operator-state split:** Only `.planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md` is staged for git commit. The two `~/.hermes/SOUL.md*` files are operator-state and must NOT appear in `git status` (they're outside the repo worktree).
</verification>

<success_criteria>
Phase 35 is complete when ALL of the following are TRUE:

1. `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` exists and is byte-identical to `~/.openclaw/SOUL.md` (mtime preserved)
2. `~/.hermes/SOUL.md` opens with the original 513-byte Hermes identity paragraph, byte-for-byte preserved
3. `~/.hermes/SOUL.md` contains a clearly demarcated `## openclaw 迁移规则` H2 section with all 4 routing categories present
4. Every openclaw-origin category carries a `> **Source:** openclaw 迁移 (2026-06-25)` annotation
5. openclaw's `mcp:hermes_*` targets adapted to hermes equivalents; GLM-4-flash genericized; "手/脑" identity framing dropped
6. `.planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md` exists documenting each openclaw rule's fate with rationale, including the CONTEXT.md baseline correction (513 bytes, not 0)
7. Only the transformation note is git-committed; operator-state files remain in `~/.hermes/` (no accidental repo staging)

These satisfy ROADMAP Phase 35 SC #1-#4 and requirements SOUL-01, SOUL-02, SOUL-03.
</success_criteria>

<output>
Create `.planning/phases/35-soul-md-identity-enhancement/35-01-SUMMARY.md` when done.

**Commit scope (CRITICAL — git add ONLY this file):**
```
git add .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
git commit -m "docs(35): SOUL.md identity enhancement — transformation note (SOUL-01..03)"
```

Do NOT git-add `~/.hermes/SOUL.md` or `~/.hermes/SOUL.md.openclaw-backup-2026-06-25` — those are operator-state files outside the repo worktree. The executor should still create them (Tasks 1 and 2), but they do not appear in `git status` of the hermes-agent repo.
</output>

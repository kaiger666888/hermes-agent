---
phase: 60-live-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed_mem0_backend.py
  - scripts/run_latency_benchmark.py
  - plugins/memory/mem0/__init__.py
  - .planning/research/v11-poc-eval/latency-baseline.md
  - .planning/research/v12-poc-eval/物理分区-triggers.md
autonomous: false
requirements: [EVAL-01]
tags: [mem0, latency, benchmark, slo, production-backend, p95]
user_setup:
  - service: mem0
    why: "Production mem0 Platform API access for live-backend latency benchmark (EVAL-01)"
    env_vars:
      - name: MEM0_API_KEY
        source: "https://app.mem0.ai → Login → API Keys → Create new key"
    dashboard_config:
      - task: "Confirm mem0 account is provisioned (free tier sufficient for 500-record benchmark)"
        location: "https://app.mem0.ai Dashboard"

must_haves:
  truths:
    - "Operator with MEM0_API_KEY can seed 500 screenplay-scoped records to live mem0 backend via a single idempotent script"
    - "Operator can run a 100-call p95 latency benchmark against the seeded live backend and receive a JSON result file"
    - "Live p50/p95/p99 numbers populate latency-baseline.md §2.2 (replacing TBD placeholders)"
    - "If p95 > 500ms, 物理分区-triggers.md documents the Phase 48 §3 migration decision"
    - "If MEM0_API_KEY is not set, the scripts exit cleanly with a human_needed marker in latency-baseline.md (no exceptions raised)"
  artifacts:
    - path: "scripts/seed_mem0_backend.py"
      provides: "Idempotent seeder that writes 500 screenplay-scoped records to live mem0 backend via Mem0MemoryProvider"
      contains: "def main()"
    - path: "scripts/run_latency_benchmark.py"
      provides: "Extended benchmark runner with --backend mem0 flag (live backend) alongside existing --fixture flag"
      contains: "--backend"
    - path: ".planning/research/v11-poc-eval/latency-baseline.md"
      provides: "§2.2 live-backend row populated with empirical p50/p95/p99 + run date + operator"
      contains: "Live mem0 Platform API baseline"
    - path: ".planning/research/v12-poc-eval/物理分区-triggers.md"
      provides: "NEW (conditional). Trigger evaluation document when p95 > 500ms per Phase 48 §3"
      contains: "物理分区"
  key_links:
    - from: "scripts/seed_mem0_backend.py"
      to: "plugins.memory.mem0.Mem0MemoryProvider"
      via: "instantiates provider, calls provider.initialize() + provider.handle_tool_call('mem0_conclude', ...)"
      pattern: "Mem0MemoryProvider"
    - from: "scripts/run_latency_benchmark.py"
      to: "agent.memory_scoped_retrieval.timed_retrieval"
      via: "100 sequential timed_retrieval calls with live backend"
      pattern: "timed_retrieval"
    - from: "agent.memory_arbitration.memory_retrieve_scoped"
      to: "plugins.memory.mem0.backend"
      via: "lazy import + backend.search(query, agent_id, top_k)"
      pattern: "backend\\.search"
---

<objective>
Wire the production mem0 Platform API as a live backend for the Phase 54 latency benchmark, seed a 500-record store scoped to the `screenplay` agent, run the deferred 100-call p95 benchmark from §3.3 of the v11.0 smoke-test-report, and populate `latency-baseline.md §2.2` with empirical live numbers. If p95 > 500ms, author `物理分区-triggers.md` per Phase 48 §3 to document the v13+ migration decision.

Purpose: Close the EVAL-01 operator-action handoff flagged SHADOW-VERIFIED in v11.0 §3.3. Until this plan runs, the latency SLO gate is technically unverified (fixture-only p95 is structurally sub-ms; only the live backend can demonstrate HNSW + network cost).

Output:
- `scripts/seed_mem0_backend.py` (NEW, idempotent)
- `scripts/run_latency_benchmark.py` (MODIFIED, adds `--backend mem0` flag)
- `plugins/memory/mem0/__init__.py` (MODIFIED — add `backend` module attribute expected by `agent.memory_arbitration._get_mem0_backend()`)
- `.planning/research/v11-poc-eval/latency-baseline.md` (UPDATED §2.2 live row)
- `.planning/research/v12-poc-eval/物理分区-triggers.md` (NEW, conditional on p95 > 500ms)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-live-eval/60-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/v11-poc-eval/latency-baseline.md
@.planning/research/v11-poc-eval/smoke-test-report.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From agent/memory_arbitration.py:569-584 (the live-backend resolver):
```python
def _get_mem0_backend() -> Any:
    """Lazy-import mem0 backend; return None if unavailable."""
    import os as _os
    if not _os.environ.get("MEM0_API_KEY"):
        return None
    try:
        from plugins.memory.mem0 import backend as _backend  # type: ignore[import-not-found]
    except ImportError:
        return None
    if not getattr(_backend, "is_available", lambda: False)():
        return None
    return _backend
```

CRITICAL GAP: `plugins.memory.mem0` currently exports `Mem0MemoryProvider` class + `register()` but NOT a `backend` module attribute. The Phase 60 Task 1 must add an instantiated `backend` object that exposes:
- `is_available() -> bool`
- `search(query: str, agent_id: str, top_k: int) -> list[dict]`
- `add(content: str, agent_id: str, scope: str, confidence: float) -> str`

From agent/memory_scoped_retrieval.py (the timed wrapper, contract):
```python
@dataclass
class LatencySample:
    latency_ms: float
    status: str   # "ok" | "unavailable" | "error"

async def timed_retrieval(
    query: str,
    agent_id: str,
    *,
    backend: Any,  # Optional — if None, resolves via _get_mem0_backend
    top_k: int = 5,
) -> LatencySample

def compute_percentiles(samples_ms: list[float]) -> dict[str, float]
    # Returns {"p50": float, "p95": float, "p99": float}
```

From scripts/run_latency_benchmark.py:74-90 (existing fixture loader pattern):
```python
def _load_fixture_backend(fixture_count: int):
    """Import the seed fixture module by name; return its built backend."""
    ...
    backend = module.build_fixture_backend()
```

From plugins/memory/mem0/__init__.py:119-219 (Mem0MemoryProvider — already wired):
```python
class Mem0MemoryProvider(MemoryProvider):
    def is_available(self) -> bool: ...
    def initialize(self, session_id: str, **kwargs) -> None: ...
    def handle_tool_call(self, tool_name: str, args: dict, **kwargs) -> str: ...
        # tool_name="mem0_conclude" → provider.add()
        # tool_name="mem0_search"   → provider.search()
```

From Phase 54 seed_500_records.py (the deterministic seeding pattern):
- 500 records, agent_id="ag1", scope="global", confidence=0.5
- Each record: `content=f"memory record {i} for agent {AGENT_ID}: deterministic content #{i}"`
- Assertion: `len(backend) == RECORD_COUNT`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/seed_mem0_backend.py + wire backend attribute in mem0 plugin</name>
  <files>scripts/seed_mem0_backend.py (NEW), plugins/memory/mem0/__init__.py (MODIFY — add module-level `backend` instance after register())</files>
  <read_first>
    - plugins/memory/mem0/__init__.py (full file — understand Mem0MemoryProvider class structure)
    - plugins/memory/mem0/scripts/batch_ingest.py lines 1-100 (existing ingestion pattern that already calls _load_config + MemoryClient directly)
    - tests/v11-latency-bench/fixtures/seed_500_records.py (the fixture pattern being mirrored for live backend)
    - agent/memory_arbitration.py lines 569-584 (_get_mem0_backend — the consumer of the new `backend` attribute)
    - agent/memory_arbitration.py lines 394-444 (memory_retrieve_scoped — confirms expected search() signature: query, agent_id, top_k)
    - agent/memory_arbitration.py lines 452-509 (memory_submit_record — confirms expected add() signature: content, agent_id, scope, confidence)
  </read_first>
  <action>
Two changes in one task (single concern: "make live mem0 backend instantiable + seedable"). Both files <200 lines total, tightly coupled — splitting creates a broken intermediate state.

**Change 1 — `plugins/memory/mem0/__init__.py`:**
After the existing `register(ctx)` function (final lines of the file), add a module-level singleton that satisfies the `from plugins.memory.mem0 import backend` import in `agent/memory_arbitration._get_mem0_backend()`. The singleton must be a thin adapter wrapping `Mem0MemoryProvider` with the three methods the arbitration runtime expects:

- `is_available() -> bool` — delegates to `Mem0MemoryProvider().is_available()` (which checks `MEM0_API_KEY` is set).
- `search(query: str, agent_id: str, top_k: int = 5) -> list[dict]` — instantiates the provider, calls `provider.initialize(session_id="benchmark")`, then calls `provider.handle_tool_call("mem0_search", {"query": query, "top_k": top_k, "rerank": False})`. Parse the JSON result string returned by `handle_tool_call` into a list of dicts shaped like `{"memory": str, "score": float, "agent_id": str}`. Inject `agent_id` into each hit dict (mem0 Platform API does not return it; the arbitration runtime's T-53-06 layered defense expects it for scope verification). On any exception or non-dict result, return `[]` and log at warning level — never raise.
- `add(content: str, agent_id: str, scope: str = "global", confidence: float = 0.5) -> str | None` — call `provider.handle_tool_call("mem0_conclude", {"conclusion": content})`. Parse the result; return the result string or a synthesized `f"mem0-{int(time.time()*1000)}"`-style ID if mem0 does not echo one (mem0 Platform's `add(infer=False)` returns success without an ID). On any exception, return `None` and log warning.

CRITICAL: Per CLAUDE.md "Eager plugin/provider imports" anti-pattern, this singleton must NOT initialize the underlying `MemoryClient` at module import time. Lazy-init inside `search()` / `add()` is already the pattern in `Mem0MemoryProvider._get_client()`. The module-level singleton is only the adapter shell, not a live HTTP client. The existing `_get_mem0_backend()` guard in `agent/memory_arbitration.py` already checks `is_available()` before calling `search()`, so the `MEM0_API_KEY`-missing path is covered.

Add a 6-line `_BackendAdapter` class (or named `_LiveBackendAdapter`) and a single module-level instance `backend = _BackendAdapter()`. Doc-comment the class with a back-reference to Phase 60 Task 1 + the consumer at `agent/memory_arbitration.py:579`.

**Change 2 — NEW `scripts/seed_mem0_backend.py`:**
Idempotent seeder, mirrors the deterministic 500-record pattern in `tests/v11-latency-bench/fixtures/seed_500_records.py` but writes to the live mem0 backend via the new `backend.add()` adapter. Structure:

1. `sys.path` bootstrap (mirror `scripts/run_latency_benchmark.py:48-57`).
2. `_REPO_ROOT` + import `plugins.memory.mem0.backend`.
3. `--agent-id` (default `screenplay` — per CONTEXT.md decision #2; NOT `ag1` from the fixture because the fixture was a generic placeholder, but EVAL-01 benchmarks the real screenplay agent namespace).
4. `--count` (default `500`, choices `[100, 500, 1000]` per `latency-baseline.md §2.2` three-row table).
5. `--dry-run` flag — enumerate + hash the records WITHOUT calling `backend.add()`. Mirrors `plugins/memory/mem0/scripts/batch_ingest.py:218`. Required so the script works without `MEM0_API_KEY`.
6. `--idempotency-key` (default `phase60-eval01-seed`) — every record's content is prefixed with this key so re-runs can identify + skip already-seeded records (avoids duplicate inserts across operator re-runs). Content pattern: `f"[{idempotency_key}] record {i}/{count} for agent {agent_id}: deterministic content #{i}"`.
7. Before seeding: check `backend.is_available()`. If False, print clear error to stderr: `MEM0_API_KEY not set. Run with --dry-run, or set MEM0_API_KEY in ~/.hermes/.env` (mirror `batch_ingest.py:68`). Exit code 2.
8. Loop count times, call `backend.add(content=..., agent_id=args.agent_id, scope="per_agent", confidence=0.5)`. Per CONTEXT.md decision #2 the scope is `per_agent` (agent-private namespace, the production deployment shape). Note that `agent/memory_arbitration._normalize_scope_for_arbitration` will coerce `per_agent` → `session` at arbitration time; that is expected + correct per the CR-01 fix comment at `agent/memory_arbitration.py:530-538`.
9. Print progress every 50 records (avoid stdout spam — mem0 API calls are ~50-300ms each per `latency-baseline.md §3.1`, so 500 records ≈ 1-4 minutes).
10. Final stdout line: `seeded count=500 agent_id=screenplay elapsed_s=XX.X idempotency_key=phase60-eval01-seed` — greppable for the task-3 verify step.

Do NOT modify the existing `tests/v11-latency-bench/fixtures/seed_*.py` files — those are the deterministic fixture path used by `--fixture 500` and must remain stable for CI runs without `MEM0_API_KEY`. The new script is the live-backend counterpart.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; python -c "import sys; sys.path.insert(0,'.'); from plugins.memory.mem0 import backend; assert backend.is_available() == False or True; assert hasattr(backend, 'search') and hasattr(backend, 'add'); print('backend adapter OK')" &amp;&amp; python scripts/seed_mem0_backend.py --dry-run --count 5 --agent-id smoke-test | grep -c "would seed" &amp;&amp; python scripts/seed_mem0_backend.py --dry-run --count 500 --agent-id screenplay 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>
    - `plugins.memory.mem0.backend` singleton importable; has `is_available()`, `search()`, `add()` methods.
    - `scripts/seed_mem0_backend.py` runs in `--dry-run` mode without `MEM0_API_KEY` and exits 0.
    - Dry-run emits "would seed" lines for every record (500 for default count).
    - When `MEM0_API_KEY` is set, `--dry-run=false` calls `backend.add()` per record (verified by executor on operator env, or by grepping the source for `backend.add(`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend scripts/run_latency_benchmark.py with --backend mem0 flag</name>
  <files>scripts/run_latency_benchmark.py (MODIFY)</files>
  <read_first>
    - scripts/run_latency_benchmark.py (full file — understand current --fixture-only flow)
    - agent/memory_scoped_retrieval.py (the timed_retrieval wrapper — confirm it accepts a `backend` parameter)
    - .planning/research/v11-poc-eval/latency-baseline.md §3.1 (timed code path: backend.search() is the SLO-bounded stage, NOT including LLM)
    - agent/memory_arbitration.py lines 394-444 (the actual memory_retrieve_scoped the benchmark wraps)
  </read_first>
  <action>
Extend the existing benchmark runner so the operator can choose between the existing fixture path (CI, no credentials) and the new live mem0 backend path (production validation). The current `--fixture` flag is mutually exclusive with the new `--backend` flag — make this explicit in argparse via a mutually-exclusive group.

Implementation steps:

1. Add a new argparse group with `--backend` (choices: `[mem0]`, default: None). Use `parser.add_argument("--backend", choices=["mem0"], default=None, help="Live backend to benchmark (requires MEM0_API_KEY). Mutually exclusive with --fixture.")`.
2. Add a `--agent-id` flag (default `screenplay` per CONTEXT.md decision #2 — the live benchmark targets the same namespace as Task 1's seeder). Only used when `--backend mem0`.
3. Create a mutually-exclusive group via `parser.add_mutually_exclusive_group(required=True)` containing `--fixture` and `--backend`. This makes the CLI intent explicit (CI = `--fixture`, production = `--backend mem0`).
4. Add a new `_load_live_backend()` helper that returns `plugins.memory.mem0.backend` after asserting `backend.is_available()` is True. If `is_available()` returns False, raise `SystemExit(2)` with a clear stderr message: `MEM0_API_KEY not set. Set it in ~/.hermes/.env or use --fixture for CI runs.` (mirror the convention from `batch_ingest.py:68`).
5. Refactor `_run_benchmark` to accept a `backend` parameter directly (already supported by `timed_retrieval`). When `args.backend == "mem0"`, call `_load_live_backend()` and pass it as the `backend=` kwarg to `timed_retrieval`. When `args.fixture`, keep the existing `_load_fixture_backend()` path.
6. The `runs` parameter, percentile computation, JSON output schema, and SLO verdict logic all stay UNCHANGED. The existing `_compute_slo_verdict` function already handles all-`unavailable` statuses as `skip` — that is the correct behavior if the live backend goes down mid-benchmark.
7. Add a `record_count` field to the JSON output. For fixture runs it equals `args.fixture` (existing behavior). For live runs it equals the count seeded via Task 1's script — the operator passes `--record-count 500` to disambiguate which row of `latency-baseline.md §2.2` table the run corresponds to (100/500/1000). Add `--record-count` arg, default 500, used only when `--backend mem0`.
8. Update the docstring at the top of the file to document both modes. Mirror the existing style (the file already has a comprehensive Usage block).
9. Print stdout summary line unchanged, but add `backend=mem0` or `backend=fixture` token so the operator can grep the run log.

DO NOT change the `slo_verdict` thresholds (`SLO_P95_THRESHOLD_MS = 500.0`) — that value is the formal §4.2 acceptance gate and is referenced from `latency-baseline.md §1`. DO NOT remove the existing `--fixture` flag or change its semantics — the fixture path remains the CI default per Phase 54 plan 02 design.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; python scripts/run_latency_benchmark.py --fixture 500 --runs 5 --out /tmp/bench-fixture-smoke.json &amp;&amp; python -c "import json,sys; d=json.load(open('/tmp/bench-fixture-smoke.json')); assert d['slo_verdict']=='pass', f&quot;fixture regression: {d['slo_verdict']}&quot;; assert 'percentiles' in d; print('fixture path regression OK')" &amp;&amp; python scripts/run_latency_benchmark.py --backend mem0 2&gt;&amp;1 | grep -c "MEM0_API_KEY not set" &amp;&amp; python scripts/run_latency_benchmark.py --fixture 100 --backend mem0 2&gt;&amp;1 | grep -c "not allowed with option"</automated>
  </verify>
  <done>
    - `--fixture` path produces identical JSON shape to Phase 54 (regression: fixture 500 p95 still sub-ms, `slo_verdict=pass`).
    - `--backend mem0` without `MEM0_API_KEY` exits 2 with the documented error message.
    - `--fixture` and `--backend` are mutually exclusive (argparse rejects both).
    - `--backend mem0 --record-count 500` (with key set) produces a JSON file with `record_count=500`, percentile fields, `slo_verdict` ∈ {pass, fail, skip}.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run live benchmark + populate latency-baseline.md §2.2 (+ conditional 物理分区-triggers.md)</name>
  <files>.planning/research/v11-poc-eval/latency-baseline.md (UPDATE §2.2 table), .planning/research/v12-poc-eval/物理分区-triggers.md (NEW, only if p95 > 500ms)</files>
  <read_first>
    - .planning/research/v11-poc-eval/latency-baseline.md (full — §2.2 table is the target, §3 explains bottleneck analysis, §4 documents trigger conditions verbatim)
    - .planning/research/v10-orchestrator-design/06-CROSS-POC-IMPACT.md §3 (Option B vs 物理分区 migration triggers — the source for the conditional doc)
    - scripts/seed_mem0_backend.py (Task 1 output — needed to seed before benchmark)
    - scripts/run_latency_benchmark.py (Task 2 output — needed to run benchmark)
  </read_first>
  <action>
This is a blocking human-verify checkpoint — the operator runs the live benchmark with real `MEM0_API_KEY` credentials, populates `latency-baseline.md §2.2` with empirical numbers, and conditionally authors `物理分区-triggers.md` if p95 ≥ 500ms.

**Pre-condition (operator):** `MEM0_API_KEY` is set in `~/.hermes/.env` (verify with `grep MEM0_API_KEY ~/.hermes/.env`). If missing, SKIP the benchmark portion, mark SC#1 as `human_needed` in `latency-baseline.md §2.2`, and proceed to the doc-update step only.

**Step 1 — Seed the live backend (only once per idempotency key):**
```bash
cd /data/workspace/hermes-agent
python scripts/seed_mem0_backend.py --agent-id screenplay --count 500
```
Expected: exit 0, stdout ends with `seeded count=500 agent_id=screenplay elapsed_s=XX.X idempotency_key=phase60-eval01-seed`. Takes 1-4 minutes (500 × ~50-300ms API calls per `latency-baseline.md §3.1`).

**Step 2 — Run the live benchmark:**
```bash
python scripts/run_latency_benchmark.py --backend mem0 --record-count 500 --runs 100 --out /tmp/bench-live-500.json
```
Expected: exit 0, stdout line `benchmark backend=mem0 record_count=500 runs=100 p50=X.XXXms p95=X.XXXms p99=X.XXXms slo_verdict=pass|fail (threshold=500.0ms)`.

**Step 3 — Inspect the JSON + populate `latency-baseline.md §2.2`:**
Open `/tmp/bench-live-500.json`, copy p50/p95/p99 values into the 500-record row of `.planning/research/v11-poc-eval/latency-baseline.md §2.2` table. Update the `Date` column with today's date + `Operator` column with `Kai (Phase 60)`.

**Step 4 — Decision gate (conditional `物理分区-triggers.md`):**
- If `slo_verdict == "pass"` (p95 < 500ms): NO further action needed. Optionally also run `--record-count 100` and `--record-count 1000` to populate the other two rows of the §2.2 table, then skip to Step 5.
- If `slo_verdict == "fail"` (p95 ≥ 500ms): Author `.planning/research/v12-poc-eval/物理分区-triggers.md` (NEW). Content per `latency-baseline.md §4`:
  - Section 1: "Trigger tripped" — cite the exact p95 value + run date + record count.
  - Section 2: "Failure mode analysis" — classify as network-bound (Stage 3 in §3.1, 50-300ms typical) vs HNSW post-filter collapse (Stage 4, scales with record count). If `--record-count 100` also exceeds SLO, suspect network; if 100 passes but 500 fails, suspect HNSW.
  - Section 3: "Option B mitigations considered" — query cache (PITFALLS §P3 mitigation 5), local SQLite fallback index (mitigation 2), backend tuning. Cite which mitigations are in-scope for v13.0 vs deferred.
  - Section 4: "v13.0 物理分区 evaluation plan" — per Phase 48 §3, the evaluation itself is mandatory; the migration may be deferred but the evaluation cannot be. Specify the evaluation owner + decision deadline.
  - Section 5: "Impact on v12.0 milestone" — verdict on whether the SLO fail blocks v12.0 ship or proceeds as `tech_debt` (per smoke-test-report §4 triage matrix).

**Step 5 — Commit the populated doc (+ conditional new doc):**
```bash
git add .planning/research/v11-poc-eval/latency-baseline.md
git add .planning/research/v12-poc-eval/物理分区-triggers.md 2>/dev/null || true   # only if p95 > 500ms
git commit -m "docs(60-01): populate EVAL-01 live mem0 latency baseline (p95=X.Xms, verdict=pass|fail)"
```

**If MEM0_API_KEY missing:** Skip Steps 1-2. In `latency-baseline.md §2.2`, replace the `TBD` cells with `DEFERRED — operator MEM0_API_KEY not set (Phase 60 SC#1 human_needed)` and commit that single doc update. EVAL-02 (Plan 02) can still proceed because it uses GLM only, not mem0.

Resume by typing one of:
- `approved` — Steps 1-5 completed, `latency-baseline.md §2.2` populated with live numbers, verdict recorded
- `deferred: <reason>` — MEM0_API_KEY not set or other operator block; doc updated with DEFERRED marker
- `p95-fail: <value>ms` — p95 ≥ 500ms; `物理分区-triggers.md` authored per Step 4; ready for review
  </action>
  <verify>
Operator confirms one of:
- `latency-baseline.md §2.2` 500-record row populated with empirical p50/p95/p99 + Date + Operator (`grep -c "500.*ms.*pass\|fail" .planning/research/v11-poc-eval/latency-baseline.md` returns at least 1).
- If p95 ≥ 500ms: `.planning/research/v12-poc-eval/物理分区-triggers.md` exists with all 5 sections (`grep -c "^## " .planning/research/v12-poc-eval/物理分区-triggers.md` returns 5).
- If MEM0_API_KEY missing: `latency-baseline.md §2.2` shows `DEFERRED — operator MEM0_API_KEY not set`.
  </verify>
  <done>
- Live p50/p95/p99 numbers visible in `latency-baseline.md §2.2` 500-record row (or DEFERRED marker if operator lacks key).
- If p95 > 500ms: `物理分区-triggers.md` authored with all 5 sections per Step 4.
- Single doc-update commit landed on `main` (or `v12.0-dev`).
- Operator signaled `approved` / `deferred: <reason>` / `p95-fail: <value>ms`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env → mem0 Platform API | `MEM0_API_KEY` crosses from `~/.hermes/.env` into outbound HTTPS to `app.mem0.ai` |
| Live benchmark → mem0 backend | 100-call burst has potential to exceed mem0 Platform rate limits (operator account tier-dependent) |
| mem0 response → JSON parse | Untrusted external API response parsed into Python dicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-SC | Tampering | `plugins.memory.mem0.backend` adapter | mitigate | Adapter wraps `Mem0MemoryProvider.handle_tool_call` (existing JSON validation); on any exception return `[]` / `None` and log warning — never raise into the benchmark loop. |
| T-60-01-DO | DoS | mem0 Platform API (100-call burst) | accept | 100 sequential retrievals × ~50-300ms = ~5-30s total. mem0 Platform handles this load routinely; circuit breaker at `_BREAKER_THRESHOLD=5` consecutive failures (already implemented in `Mem0MemoryProvider`) protects against sustained hammering. |
| T-60-01-IZ | Information disclosure | `MEM0_API_KEY` in process env | accept | Standard env-var secret handling; not logged by adapter (mirror existing `Mem0MemoryProvider` which never logs `api_key`). |
| T-60-01-SP | Spoofing | mem0 response impersonation | accept | HTTPS TLS on `app.mem0.ai` provides transport authentication; no separate response-signing check (would require upstream mem0 Platform change). |
| T-60-01-RE | Repudiation | Benchmark run attribution | mitigate | `latency-baseline.md §2.2` table has `Date` + `Operator` columns; Task 3 commits populate both. Git commit SHA provides permanent attribution. |
</threat_model>

<verification>
- `python scripts/run_latency_benchmark.py --fixture 500 --runs 5 --out /tmp/bench-fixture-regression.json` still passes (Phase 54 regression check).
- `python scripts/seed_mem0_backend.py --dry-run --count 500 --agent-id screenplay` emits 500 "would seed" lines without `MEM0_API_KEY`.
- After operator run (Task 3): `latency-baseline.md §2.2` 500-record row populated with empirical p50/p95/p99 + Date + Operator.
- If p95 ≥ 500ms: `物理分区-triggers.md` exists in `.planning/research/v12-poc-eval/` with all 5 sections per Step 4.
</verification>

<success_criteria>
1. `scripts/seed_mem0_backend.py` exists, idempotent, dry-run works without `MEM0_API_KEY`.
2. `scripts/run_latency_benchmark.py --backend mem0` flag works (verified by Task 2 automated tests).
3. `plugins.memory.mem0.backend` adapter importable + satisfies `_get_mem0_backend()` contract.
4. `latency-baseline.md §2.2` populated with live numbers OR marked `DEFERRED — human_needed` if operator lacks `MEM0_API_KEY`.
5. If p95 > 500ms: `物理分区-triggers.md` authored per Phase 48 §3.
6. Phase 54 fixture path regression: existing `--fixture 500` benchmark still passes (no behavior change).
</success_criteria>

<output>
Create `.planning/phases/60-live-eval/60-01-SUMMARY.md` when done. Summary should record: live p50/p95/p99 values (or DEFERRED marker), slo_verdict, whether 物理分区-triggers.md was authored, and the operator-run timestamp.
</output>

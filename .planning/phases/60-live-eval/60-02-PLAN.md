---
phase: 60-live-eval
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - scripts/compute_fitness_baseline.py
  - agent/fitness_battery.py
  - .planning/research/v12-poc-eval/fitness-battery-baseline.md
autonomous: false
requirements: [EVAL-02]
tags: [fitness-battery, real-mode, discrimination, glm, aux-pool, baseline]
user_setup:
  - service: glm-aux-pool
    why: "Fitness battery real-mode uses ~400K tokens across 8 scenarios × ~24 LLM calls. Phase 59 auxiliary pool isolates this from the main gateway pool to avoid key exhaustion."
    env_vars:
      - name: GLM_AUX_API_KEY_1
        source: "z.ai Dashboard → API Keys → Create key (designated 'auxiliary' pool)"
      - name: GLM_AUX_API_KEY_2
        source: "z.ai Dashboard → API Keys → Create key (auxiliary)"
      - name: GLM_AUX_API_KEY_3
        source: "z.ai Dashboard → API Keys → Create key (auxiliary)"
      - name: GLM_AUX_API_KEY_4
        source: "z.ai Dashboard → API Keys → Create key (auxiliary)"
    dashboard_config:
      - task: "Confirm 4 GLM keys designated as auxiliary pool (falls back to single GLM_API_KEY if not set, but that risks gateway contention)"
        location: "~/.hermes/.env"

must_haves:
  truths:
    - "Operator with GLM keys can run the full 8-scenario fitness battery in real-mode (no --shadow) end-to-end"
    - "Mean score + per-scenario scores are appended to fitness_trend.jsonl with a real-mode entry (no stub markers)"
    - "Discrimination baseline is computed: persona-aligned screenplay agent mean vs generic-LLM (no system prompt) mean"
    - "Discrimination delta ≥ 0.3 documented in fitness-battery-baseline.md (persona-aligned expected 0.7+, generic expected 0.4-0.5)"
    - "Phase 59 auxiliary pool is used for all battery LLM calls (zero consumption from main gateway pool)"
  artifacts:
    - path: "scripts/compute_fitness_baseline.py"
      provides: "NEW baseline-comparison runner — runs persona-aligned battery + generic-LLM battery, computes delta"
      contains: "def main()"
    - path: "agent/fitness_battery.py"
      provides: "MODIFIED — _dispatch_agent gains a generic-LLM dispatch path (no system prompt, raw call_llm) triggered by baseline_mode flag"
      contains: "baseline_mode"
    - path: ".planning/research/v12-poc-eval/fitness-battery-baseline.md"
      provides: "NEW — Phase 60 EVAL-02 discrimination baseline document with both battery run summaries + delta analysis"
      contains: "discrimination"
    - path: "~/.hermes/eval/fitness_trend.jsonl"
      provides: "Appended real-mode baseline entry (no shadow stub)"
      contains: "battery_version"
  key_links:
    - from: "scripts/compute_fitness_baseline.py"
      to: "agent.fitness_battery.run_battery"
      via: "two runs (persona-aligned + generic-LLM) with baseline_mode flag"
      pattern: "run_battery"
    - from: "agent.fitness_battery._dispatch_agent"
      to: "agent.auxiliary_client.call_llm"
      via: "generic-LLM path: empty system prompt + raw user input"
      pattern: "call_llm"
    - from: "agent.auxiliary_client.call_llm"
      to: "agent.credential_pool.load_named_pool"
      via: "pool_name='auxiliary' targets GLM_AUX_API_KEY_1..4 (Phase 59 POOL-01)"
      pattern: "auxiliary"
---

<objective>
Run the deferred v11.0 EVAL-01 fitness battery in real-mode (no `--shadow`) and author the discrimination baseline comparing the persona-aligned screenplay agent against a generic-LLM (no system prompt) baseline. Closes the §3.2 SHADOW-VERIFIED handoff from the v11.0 smoke-test-report.

Purpose: Until this plan runs, the discrimination criterion (persona-aligned ≥ 0.7 vs generic 0.4-0.5, delta ≥ 0.3) is unmeasured — the v11.0 shadow run produced `mean_score=0.0187` because agent dispatch was stubbed. Real-mode numbers are required for the VALIDATE-01 (Phase 61) production-readiness verdict.

Output:
- `scripts/compute_fitness_baseline.py` (NEW — orchestrates both battery runs + delta computation)
- `agent/fitness_battery.py` (MODIFIED — add generic-LLM dispatch path triggered by `baseline_mode` flag)
- `.planning/research/v12-poc-eval/fitness-battery-baseline.md` (NEW — baseline document)
- `~/.hermes/eval/fitness_trend.jsonl` (APPENDED — real-mode entry)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-live-eval/60-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/v11-poc-eval/smoke-test-report.md
@.planning/phases/59-aux-pool-isolation/59-02-SUMMARY.md
@.planning/phases/60-live-eval/60-01-PLAN.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From agent/fitness_battery.py (the runner being extended):
```python
def _dispatch_agent(scenario: dict, *, shadow: bool = False) -> str:
    """Dispatch the agent on scenario['input'].
    v1 PoC: shadow=True returns stub; conflict-resolution invokes arbitrate_two_memories;
    screenplay + persona-drift return stub (live dispatch is Phase 56 VALIDATE scope —
    THIS IS THE FUNCTION THAT MUST GAIN A REAL-MODE PATH IN TASK 1).
    """

def run_battery(
    battery_dir: Path,
    *,
    judge_llm: Callable[..., Any] | None = None,
    persona_sha256: str,
    model_id: str = "glm-5.2",
    provider: str = "zai",
    shadow: bool = False,
) -> dict:
    """Returns: {battery_version, mean_score, per_prompt_scores, scenarios_run, ...}"""
```

From agent/auxiliary_client.py:5445-5510 (call_llm — already routes through aux pool per Phase 59):
```python
def call_llm(
    task: str = None,
    *,
    provider: str = None,
    model: str = None,
    messages: list,
    ...
) -> Any:
    # Phase 58 THROTTLE-01: acquire_slot(task, pool_name="auxiliary")
    # Phase 59 POOL-01: pool_name="auxiliary" documents aux pool usage
    # Phase 57 ENDPOINT-01: long-prompt routing for synthesis/compaction
```

From scripts/run_fitness_battery.py (the existing CLI — extended by Task 2):
```python
parser.add_argument("--shadow", action="store_true", help="Shadow mode: agent dispatch is stubbed.")
parser.add_argument("--persona-sha256", required=True, help="Agent persona hash being probed.")
```

From tests/v11-fitness-battery/scenarios/ (8 scenarios, fixed set):
```
conflict-resolution-2party.yaml
conflict-resolution-confidence-voting.yaml
conflict-resolution-scope-precedence.yaml
hook09-emotion-curve-marker.yaml
persona-drift-probe.yaml
screenplay-step3-hook09.yaml
screenplay-step3-mckee-value-shift.yaml
screenplay-step3-snyder-beat.yaml
```

From MEMORY.md feedback-glm-5-2-only.md (hard constraint):
- All LLM calls MUST go through glm-5.2. No model switching.

From MEMORY.md feedback-glm-overload-reduce-concurrency.md (hard constraint):
- global_concurrency == 1. Fitness battery must NOT parallelize agent dispatch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generic-LLM dispatch path + baseline_mode to agent/fitness_battery.py</name>
  <files>agent/fitness_battery.py (MODIFY), tests/v11-fitness-battery/test_baseline_mode_dispatch.py (NEW)</files>
  <read_first>
    - agent/fitness_battery.py lines 60-130 (_dispatch_agent — the function gaining a new branch)
    - agent/fitness_battery.py lines 360-420 (run_battery — the function gaining a new kwarg)
    - agent/auxiliary_client.py lines 5445-5520 (call_llm signature — the dispatcher used for generic-LLM baseline)
    - tests/v11-fitness-battery/test_fitness_battery.py or any existing test (mirror test style — look for the monkeypatch.setattr(fitness_battery, "_dispatch_agent", stub) pattern)
    - tests/v11-fitness-battery/scenarios/screenplay-step3-hook09.yaml (representative scenario shape — the input block the dispatcher consumes)
  </read_first>
  <action>
Extend `_dispatch_agent` + `run_battery` with a third dispatch mode alongside `shadow` and the existing stub paths. The new mode is triggered by `baseline_mode="generic_llm"` and invokes `agent.auxiliary_client.call_llm` with NO system prompt — exactly the "generic-LLM baseline" required by CONTEXT.md decision #4.

Implementation steps:

1. Add a new keyword arg `baseline_mode: str | None = None` to `_dispatch_agent`. Valid values: `None` (existing behavior), `"persona_aligned"` (real dispatch via auxiliary_client with the screenplay agent's persona), `"generic_llm"` (no system prompt, raw user input).

2. Add the same `baseline_mode: str | None = None` kwarg to `run_battery` and thread it through to every `_dispatch_agent(scenario, shadow=shadow, baseline_mode=baseline_mode)` call.

3. In `_dispatch_agent`, when `baseline_mode == "persona_aligned"`: extract the scenario input as a user message, prepend the persona system prompt loaded from the screenplay agent YAML at `~/.hermes/agents/screenplay.agent.yaml` (use `agent.registry_loader` if available, else fall back to a simple `yaml.safe_load`). Call `agent.auxiliary_client.call_llm(task="fitness_battery_agent", provider="glm", messages=[{system}, {user}], max_tokens=2048, timeout=60.0)`. Return the `choices[0].message.content` string. On exception, log warning + return the existing stub JSON (graceful degradation).

4. In `_dispatch_agent`, when `baseline_mode == "generic_llm"`: build messages with ONLY a user message (no system prompt). Use the SAME user message text as `persona_aligned` so the comparison is apples-to-apples. Call `call_llm` with the same `task`, `provider`, `max_tokens`, `timeout`. Return `choices[0].message.content`. The point of this mode is to measure how a GLM-5.2 without any persona scores on the SAME rubric — the discrimination delta is `persona_aligned_mean - generic_llm_mean`.

5. For both new modes: extract the user-message text from `scenario["input"]`. For screenplay + persona-drift scenarios, the input block typically has a `prompt` or `user` field — read the actual scenario YAMLs to confirm the exact key name (likely `input.prompt` or `input.user_message`; if varies across scenarios, write a helper `_extract_user_message(scenario)` that tries multiple keys in order).

6. CRITICAL — do NOT parallelize dispatch. Per MEMORY.md `feedback-glm-overload-reduce-concurrency.md`, global_concurrency == 1 by design. The `run_battery` loop must remain sequential (it already is — keep it that way). The Phase 59 RPM throttle (30 RPM/task via `acquire_slot`) will naturally pace the calls; do NOT bypass it.

7. Add a new test file `tests/v11-fitness-battery/test_baseline_mode_dispatch.py` with at least 4 tests:
   - `test_dispatch_persona_aligned_calls_auxiliary_client` — monkeypatch `fitness_battery._call_llm` to a stub that records the messages list; verify the system prompt is present.
   - `test_dispatch_generic_llm_has_no_system_prompt` — same monkeypatch; verify messages list has exactly ONE entry with `role="user"` (no `role="system"` entry).
   - `test_dispatch_generic_llm_returns_content_string` — verify the return value is the `choices[0].message.content` string, not a JSON stub.
   - `test_run_battery_threads_baseline_mode` — call `run_battery` with `baseline_mode="generic_llm"` on a tiny 1-scenario dir; monkeypatch `_dispatch_agent` + `score_scenario`; verify `baseline_mode` is forwarded.
   All tests must monkeypatch `_call_llm` so no real GLM call is made in CI. Use the existing test style (look for `monkeypatch.setattr(fitness_battery, "_call_llm", stub)` pattern).

8. Update the `BATTERY_VERSION` constant in `fitness_battery.py` to `"v1-screenplay-baseline-real"` if the existing version is `"v1-screenplay-baseline"` — the trend entry should distinguish real-mode from shadow runs. If the version is already unique per-run, leave it. Read the file to confirm.

DO NOT change the `--shadow` flag semantics — it must remain for CI. DO NOT change the conflict-resolution dispatch path — it already invokes `arbitrate_two_memories` correctly.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; python -m pytest tests/v11-fitness-battery/test_baseline_mode_dispatch.py -x -q &amp;&amp; python -m pytest tests/v11-fitness-battery/ -x -q --timeout=30 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
    - New tests pass (4+ tests green).
    - Existing fitness_battery tests still pass (no regression).
    - `_dispatch_agent` accepts `baseline_mode` kwarg; both `persona_aligned` and `generic_llm` modes invoke `call_llm` with the documented message shape.
    - `run_battery` threads `baseline_mode` to every dispatch call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/compute_fitness_baseline.py (orchestrates both runs + delta)</name>
  <files>scripts/compute_fitness_baseline.py (NEW)</files>
  <read_first>
    - scripts/run_fitness_battery.py (full — the existing single-run CLI to mirror)
    - agent/fitness_battery.py lines 360-420 (run_battery signature post-Task-1)
    - scripts/run_latency_benchmark.py (sys.path bootstrap pattern)
    - .planning/phases/60-live-eval/60-CONTEXT.md (decision #4 — discrimination delta ≥ 0.3; decision #5 — ~400K tokens)
    - tests/v11-fitness-battery/scenarios/ ls (confirm 8 scenario YAMLs)
  </read_first>
  <action>
Create a single CLI that runs the battery TWICE (persona-aligned + generic-LLM), computes the discrimination delta, and writes a structured JSON summary. The operator runs this ONCE to produce the baseline numbers for Task 3.

CLI design (mirror `run_fitness_battery.py` style):
```
python scripts/compute_fitness_baseline.py \
    --battery tests/v11-fitness-battery/scenarios \
    --persona-sha256 <screenplay_persona_sha256> \
    --out /tmp/fitness-baseline-phase60.json
```

Flags:
1. `--battery` (default: `tests/v11-fitness-battery/scenarios`) — same as `run_fitness_battery.py`.
2. `--persona-sha256` (required) — the screenplay agent persona hash. Compute this from `~/.hermes/agents/screenplay.agent.yaml` at script start: `hashlib.sha256(yaml.dump(persona_block).encode()).hexdigest()`. Print it on stdout so the operator can verify.
3. `--out` (required) — output JSON path.
4. `--model-id` (default `glm-5.2`) — per MEMORY.md feedback-glm-5-2-only.md, locked.
5. `--provider` (default `zai`).
6. `--skip-generic` (optional flag) — for debugging, run only persona-aligned. Default False (run both).

Behavior:
1. Compute persona_sha256 from the agent YAML (if `--persona-sha256` not explicitly passed — make the flag optional with this auto-compute default).
2. Run battery in `baseline_mode="persona_aligned"`: `summary_persona = run_battery(battery_dir, baseline_mode="persona_aligned", persona_sha256=persona_sha256, model_id=args.model_id, provider=args.provider)`. Sequential per MEMORY.md global_concurrency==1.
3. Append the persona-aligned entry to `fitness_trend.jsonl` via `append_trend_entry` (existing helper in `fitness_battery.py`). The entry should include a new `"mode": "persona_aligned"` field so downstream analysis can distinguish modes.
4. If not `--skip-generic`: Run battery in `baseline_mode="generic_llm"`: `summary_generic = run_battery(..., baseline_mode="generic_llm", persona_sha256="generic-llm-baseline", ...)`. Append this entry too with `"mode": "generic_llm"`.
5. Compute delta: `delta = summary_persona["mean_score"] - summary_generic["mean_score"]`.
6. Print stdout summary:
```
persona_aligned: mean=X.XXXX per-scenario={...}
generic_llm:     mean=Y.YYYY per-scenario={...}
delta:           D.DDDD
verdict:         meaningful|not_meaningful   (threshold=0.3 per CONTEXT.md decision #4)
```
7. Write JSON output with all four summaries (both runs' full dicts + delta + verdict + timestamp + token cost estimate per CONTEXT.md decision #5).

Token cost estimate: each scenario dispatches ~3 LLM calls (agent + judge). 8 scenarios × 2 modes × 2 calls (agent + judge) × ~5K tokens avg ≈ 160K tokens. Per CONTEXT.md decision #5 the estimate is ~400K — use the higher number for budget reporting. At glm-5.2 pricing ≈ 0.20 CNY total.

CRITICAL — do NOT run the two modes in parallel. Both modes share the auxiliary pool; sequential execution lets the Phase 59 TPM tracker reset between modes. Per MEMORY.md global_concurrency==1, all battery dispatch is sequential by design.

Do NOT remove or modify `scripts/run_fitness_battery.py` — that script remains for shadow-mode CI runs + single-mode operator checks. The new script is the dual-mode orchestrator.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; python -c "import ast; ast.parse(open('scripts/compute_fitness_baseline.py').read()); print('syntax OK')" &amp;&amp; python scripts/compute_fitness_baseline.py --help 2&gt;&amp;1 | grep -c "persona-sha256\|--battery\|--out\|--skip-generic" &amp;&amp; python -m pytest tests/v11-fitness-battery/test_baseline_mode_dispatch.py -x -q 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <done>
    - `scripts/compute_fitness_baseline.py` exists; `--help` lists all 6 documented flags.
    - Syntax valid (ast.parse succeeds).
    - When invoked with valid args, runs both batteries sequentially, computes delta, writes JSON.
    - Operator-ready: a single command produces the baseline.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run real-mode baseline battery + author fitness-battery-baseline.md</name>
  <files>.planning/research/v12-poc-eval/fitness-battery-baseline.md (NEW), ~/.hermes/eval/fitness_trend.jsonl (APPENDED)</files>
  <read_first>
    - .planning/research/v11-poc-eval/smoke-test-report.md §3.2 (the deferred handoff being closed — shadow run produced mean_score=0.0187)
    - .planning/phases/60-live-eval/60-CONTEXT.md decisions #3-#5 (aux pool, discrimination, token cost)
    - scripts/compute_fitness_baseline.py (Task 2 output — the CLI to run)
    - agent/fitness_battery.py (Task 1 output — confirm baseline_mode threading)
    - .planning/research/v12-poc-eval/ (ls — confirm directory exists; if not, mkdir -p)
  </read_first>
  <action>
Blocking human-verify checkpoint — the operator runs the dual-mode (persona-aligned + generic-LLM) fitness battery against real GLM via the Phase 59 auxiliary pool, then authors `fitness-battery-baseline.md` with the discrimination analysis.

**Pre-condition (operator):**
1. GLM keys available. Either `GLM_AUX_API_KEY_1..4` set in `~/.hermes/.env` (preferred — uses Phase 59 auxiliary pool, isolated from gateway) OR `GLM_API_KEY` set (fallback per Phase 59-01 `_seed_aux_env`).
2. `~/.hermes/agents/screenplay.agent.yaml` exists (created by Phase 53 MIGR-01 — 9 sample agents include screenplay).
3. Recommend pausing hermes-gateway during the run to free GLM RPM quota: `systemctl --user stop hermes-gateway.service` (per MEMORY.md `hermes-gateway-systemd.md`). Resume after.

**Step 1 — Create the research dir if missing:**
```bash
mkdir -p /data/workspace/hermes-agent/.planning/research/v12-poc-eval
```

**Step 2 — Run the dual-mode battery:**
```bash
cd /data/workspace/hermes-agent
python scripts/compute_fitness_baseline.py \
    --battery tests/v11-fitness-battery/scenarios \
    --out /tmp/fitness-baseline-phase60.json 2>&1 | tee /tmp/fitness-baseline-phase60.log
```
Expected runtime: ~10-25 minutes (8 scenarios × 2 modes × ~3 calls × ~5s each = ~24min per CONTEXT.md decision #3 + §3.2 of smoke-test-report). Phase 58 RPM throttle (30/task) will naturally pace.

Expected stdout tail:
```
persona_aligned: mean=X.XXXX per-scenario={...8 scores...}
generic_llm:     mean=Y.YYYY per-scenario={...8 scores...}
delta:           D.DDDD
verdict:         meaningful|not_meaningful
```

**Step 3 — Verify fitness_trend.jsonl has BOTH new entries:**
```bash
tail -2 ~/.hermes/eval/fitness_trend.jsonl | python -m json.tool | grep -c "mode.*persona_aligned\|mode.*generic_llm"
```
Expected: `2` (both entries appended).

**Step 4 — Author `.planning/research/v12-poc-eval/fitness-battery-baseline.md`:**
Mirror `.planning/research/v11-poc-eval/latency-baseline.md` style. Required sections:
1. **Header:** Title + Phase 60 EVAL-02 source + author + date.
2. **§1 — Discrimination criterion:** Restate CONTEXT.md decision #4 (persona-aligned ≥ 0.7, generic 0.4-0.5, delta ≥ 0.3 = meaningful).
3. **§2 — Persona-aligned run results:** Copy the per-scenario table + mean_score from `/tmp/fitness-baseline-phase60.json` `"persona_aligned"` block.
4. **§3 — Generic-LLM run results:** Same table shape from `"generic_llm"` block.
5. **§4 — Discrimination analysis:** Overall delta + verdict; per-scenario delta breakdown (hypothesis: screenplay scenarios show largest delta; conflict-resolution smaller); comparison to expected bands.
6. **§5 — Token cost report:** Total tokens consumed, CNY cost at glm-5.2 pricing, % of auxiliary pool TPM budget used.
7. **§6 — Implications for v12.0 ship decision:**
   - delta ≥ 0.3 AND persona mean ≥ 0.7: PASS.
   - delta < 0.3: persona provides no measurable advantage — v13.0 design concern.
   - persona mean < 0.7 but delta ≥ 0.3: tech_debt for v13.0 persona refinement.
8. **§7 — References:** smoke-test-report §3.2, REQUIREMENTS.md EVAL-02, CONTEXT.md decisions #3-#5.

**Step 5 — Commit the new doc:**
```bash
cd /data/workspace/hermes-agent
git add .planning/research/v12-poc-eval/fitness-battery-baseline.md
git commit -m "docs(60-02): ship EVAL-02 real-mode fitness baseline (persona=X.XX, generic=Y.YY, delta=D.DD, verdict=meaningful|not_meaningful)"
```

**Step 6 — Resume gateway if paused:**
```bash
systemctl --user start hermes-gateway.service
```

Resume by typing one of:
- `approved` — battery ran end-to-end, `fitness-battery-baseline.md` authored, verdict recorded
- `partial: <reason>` — one mode ran but the other failed; attach the log excerpt
- `deferred: <reason>` — GLM keys unavailable or other operator block
  </action>
  <verify>
Operator confirms:
- `~/.hermes/eval/fitness_trend.jsonl` has 2 new entries (one per mode): `tail -2 ~/.hermes/eval/fitness_trend.jsonl | grep -c "persona_aligned\|generic_llm"` returns 2.
- `.planning/research/v12-poc-eval/fitness-battery-baseline.md` exists with all 7 sections (`grep -c "^## " .planning/research/v12-poc-eval/fitness-battery-baseline.md` returns at least 7).
- Verdict recorded in §6 (PASS / design concern / tech_debt).
  </verify>
  <done>
- Real-mode persona-aligned + generic-LLM battery runs completed (~10-25 min wall-clock).
- 2 entries appended to `fitness_trend.jsonl` (no shadow stub markers).
- `fitness-battery-baseline.md` authored with 7 sections + verdict.
- Single doc commit landed.
- Operator signaled `approved` / `partial: <reason>` / `deferred: <reason>`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env → GLM API (aux pool) | `GLM_AUX_API_KEY_1..4` cross from `~/.hermes/.env` into outbound HTTPS to `api.z.ai` |
| Scenario YAML → LLM prompt | Untrusted scenario input (`*.yaml` from `tests/v11-fitness-battery/scenarios/`) is injected into LLM prompts — prompt injection surface |
| LLM response → JSON parse | GLM judge response (markdown-fenced JSON per v11.0 §3.2 known issue) parsed into score dict |
| fitness_trend.jsonl | Persistent eval log; append-only, but a corrupted entry could skew downstream trend analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-PI | Tampering / Prompt injection | Scenario YAML → LLM prompt | accept | Scenarios are developer-authored test fixtures committed to the repo (not user-supplied); the LLM judge has no privileged tools. Defensive prompt structure (system before user) is best-effort. |
| T-60-02-DO | DoS | GLM aux pool (8 scenarios × 2 modes × 3 calls) | mitigate | Phase 58 THROTTLE-01 RPM bucket (30/task default) + Phase 59 POOL-02 per-key TPM tracking prevent pool exhaustion. Sequential dispatch per MEMORY.md global_concurrency==1. |
| T-60-02-CO | Corruption | `fitness_trend.jsonl` append | mitigate | `append_trend_entry` opens in append mode + uses `encoding="utf-8"` (PLW1514 compliant). Each entry is single-line JSON; a partial write would corrupt one line, not the file. |
| T-60-02-IZ | Information disclosure | GLM prompt contains screenplay persona content | accept | Persona YAMLs are developer-authored + committed; no PII or operator secrets. Standard GLM API TLS protects transit. |
| T-60-02-RE | Repudiation | Battery run attribution | mitigate | `fitness_trend.jsonl` entry includes `ts` + `persona_sha256` + `model_id`. Task 3 commits the baseline doc with attribution in the header. |
</threat_model>

<verification>
- `python -m pytest tests/v11-fitness-battery/test_baseline_mode_dispatch.py -x` passes (4+ tests).
- `python -m pytest tests/v11-fitness-battery/ -x` no regression (existing tests still green).
- `python scripts/compute_fitness_baseline.py --help` lists all documented flags.
- After operator run (Task 3): `~/.hermes/eval/fitness_trend.jsonl` has 2 new entries (one per mode).
- `fitness-battery-baseline.md` exists with all 7 sections; verdict recorded in §6.
</verification>

<success_criteria>
1. `_dispatch_agent` supports `baseline_mode` ∈ {None, "persona_aligned", "generic_llm"} — all 3 paths tested.
2. `run_battery` threads `baseline_mode` to every dispatch invocation.
3. `scripts/compute_fitness_baseline.py` runs both modes sequentially + writes JSON summary with delta + verdict.
4. `fitness_trend.jsonl` receives 2 real-mode entries (no shadow stub markers).
5. `fitness-battery-baseline.md` documents discrimination delta + verdict against the ≥0.3 threshold.
6. Phase 59 auxiliary pool used throughout (no main pool consumption).
7. Zero parallel dispatch (global_concurrency==1 honored).
</success_criteria>

<output>
Create `.planning/phases/60-live-eval/60-02-SUMMARY.md` when done. Summary should record: persona-aligned mean + per-scenario, generic-LLM mean + per-scenario, delta, verdict (meaningful / not_meaningful), token cost, operator-run timestamp.
</output>

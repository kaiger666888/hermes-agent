---
phase: 14-visual-executor-merge
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - skills/movie-experts/character_designer/SKILL.md
  - skills/movie-experts/scene_builder/SKILL.md
  - skills/movie-experts/editor/SKILL.md
  - skills/movie-experts/colorist/SKILL.md
  - skills/movie-experts/cinematographer/SKILL.md
  - skills/movie-experts/composer/SKILL.md
  - skills/movie-experts/production/SKILL.md
  - skills/movie-experts/performer/SKILL.md
  - skills/movie-experts/storyboard_designer/SKILL.md
  - skills/movie-experts/continuity_auditor/SKILL.md
  - skills/movie-experts/compliance_gate/SKILL.md
  - skills/movie-experts/foley/SKILL.md
  - skills/movie-experts/lip_sync/SKILL.md
  - skills/movie-experts/animation_studio/SKILL.md
  - skills/movie-experts/style_genome/SKILL.md
autonomous: true
requirements: [MERGE-01]
user_setup: []

must_haves:
  truths:
    - "Every consumer SKILL.md that previously listed 'drawer' or 'animator' in metadata.hermes.related_skills now lists 'visual_executor' instead"
    - "Every consumer SKILL.md that previously listed BOTH drawer and animator has exactly ONE visual_executor entry (deduplicated — not two)"
    - "Every consumer SKILL.md body prose that referenced the drawer or animator expert (via markdown link, Collaboration bullet, or JSON literal) is updated to reference visual_executor"
    - "English-noun uses of 'drawer' (rare — 'the drawer' as in 'the person drawing') and 'animator' (e.g., 'Senior animators draw key frames' in animation_studio) are NOT changed"
    - "No stranded expert_id references to 'drawer' or 'animator' remain in any consumer SKILL.md outside the redirect stubs themselves"
  artifacts:
    - path: "skills/movie-experts/character_designer/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/scene_builder/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/editor/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/colorist/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/cinematographer/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/composer/SKILL.md"
      provides: "Consumer with visual_executor edges (no body change — only related_skills)"
      contains: "visual_executor"
    - path: "skills/movie-experts/production/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/performer/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/storyboard_designer/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/continuity_auditor/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/compliance_gate/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/foley/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/lip_sync/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/animation_studio/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
    - path: "skills/movie-experts/style_genome/SKILL.md"
      provides: "Consumer with visual_executor edges + body references"
      contains: "visual_executor"
  key_links:
    - from: "skills/movie-experts/character_designer/SKILL.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "related_skills array entry"
      pattern: "related_skills:.*visual_executor"
    - from: "skills/movie-experts/lip_sync/SKILL.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "markdown link"
      pattern: "\\(\\.\\./visual_executor/SKILL\\.md\\)"
    - from: "skills/movie-experts/cinematographer/SKILL.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "related_skills array entry"
      pattern: "related_skills:.*visual_executor"
---

<objective>
Propagate the drawer + animator → visual_executor merge across all 15 consumer SKILL.md files. Each consumer's `metadata.hermes.related_skills` array must reference `visual_executor` (deduplicated — not both drawer and animator as two entries) and every body prose reference to the `drawer` or `animator` expert (markdown links, Collaboration bullets, JSON literals, narrative references) must be updated to `visual_executor`.

Purpose: Fulfill ROADMAP §14 success criterion #4 (all consumers updated). Bidirectional edge sync — visual_executor lists these 15 consumers in its related_skills (set in plan 14-01), and they list visual_executor back (set in this plan). Without this, the collaboration graph is one-directional and downstream agent dispatch can fail.

Output:
- 15 consumer SKILL.md files updated with `visual_executor` in related_skills (replacing drawer and/or animator entries)
- Body prose references in those 15 files updated (markdown links, Collaboration bullets, JSON literals)
- Zero stranded expert_id references to `drawer` or `animator` outside the redirect stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-executor-merge/14-CONTEXT.md
@.planning/phases/14-visual-executor-merge/14-01-PLAN.md
@skills/movie-experts/visual_executor/SKILL.md

# Phase 13's 13-01-PLAN Task 2 is the precedent for consumer edge sync — same shape, different old/new IDs.
@.planning/phases/13-expert-rename-alias-scaffolding/13-01-PLAN.md
@.planning/phases/13-expert-rename-alias-scaffolding/13-01-SUMMARY.md

# CLAUDE.md §"Skill File Conventions (SKILL.md)": related_skills is bidirectional — A listing B implies B should list A.

<interfaces>
<!-- The 15 consumer SKILL.md files and their current drawer/animator related_skills edges -->

| Consumer | Current related_skills entries touching drawer/animator |
|----------|--------------------------------------------------------|
| character_designer | drawer, animator (BOTH → collapse to ONE visual_executor) |
| scene_builder | drawer, animator (BOTH → collapse to ONE visual_executor) |
| editor | animator (only) |
| colorist | drawer, animator (BOTH → collapse to ONE visual_executor) |
| cinematographer | animator, drawer (BOTH → collapse to ONE visual_executor) |
| composer | none in related_skills (only English-noun usage in body) |
| production | drawer, animator (BOTH → collapse to ONE visual_executor) |
| performer | drawer, animator (BOTH → collapse to ONE visual_executor) |
| storyboard_designer | drawer, animator (BOTH → collapse to ONE visual_executor) |
| continuity_auditor | drawer, animator (BOTH → collapse to ONE visual_executor — Phase 13 set these from continuity rename) |
| compliance_gate | drawer (only) |
| foley | animator (only) |
| lip_sync | animator (only — but heavy body prose references) |
| animation_studio | none in related_skills (body prose references only) |
| style_genome | drawer (only) |

<!-- Edge replacement rules -->

1. **Dedup rule (CRITICAL):** When a consumer lists BOTH `drawer` and `animator` in related_skills, replace with exactly ONE `visual_executor` entry. Do NOT leave two entries.
2. **Single-edge rule:** When a consumer lists only `drawer` or only `animator`, replace that single entry with `visual_executor`.
3. **No-touch rule for body nouns:** The word "drawer" used as an English noun (e.g., "the drawer of a check") and "animator" used as an English noun (e.g., "Senior animators draw key frames" in animation_studio/project-corpus) is NOT an expert_id reference and MUST NOT be changed.

<!-- Consumer body-prose reference patterns to update -->

Patterns that MUST change:
- `(../drawer/SKILL.md)` → `(../visual_executor/SKILL.md)`
- `(../animator/SKILL.md)` → `(../visual_executor/SKILL.md)`
- `` [`drawer`](../drawer/SKILL.md) `` → `` [`visual_executor`](../visual_executor/SKILL.md) ``
- `` [`animator`](../animator/SKILL.md) `` → `` [`visual_executor`](../visual_executor/SKILL.md) ``
- `- **<- drawer**:` → `- **<- visual_executor**:` (in Collaboration sections)
- `- **-> drawer**:` → `- **-> visual_executor**:`
- `- **<- animator**:` → `- **<- visual_executor**:`
- `- **-> animator**:` → `- **-> visual_executor**:`
- `` - **-> [`drawer`](../drawer/SKILL.md)**: `` → `` - **-> [`visual_executor`](../visual_executor/SKILL.md)**: ``
- `` - **-> [`animator`](../animator/SKILL.md)**: `` → `` - **-> [`visual_executor`](../visual_executor/SKILL.md)**: ``
- `"drawer",` inside JSON arrays (e.g., character_designer line 179) → `"visual_executor",`
- `"animator",` inside JSON arrays → `"visual_executor",`
- `drawer / animator / lip_sync / continuity_auditor` (narrative lists) → `visual_executor / lip_sync / continuity_auditor`
- `(drawer, animator)` parenthetical pairs in narrative → `(visual_executor)`

Patterns that MUST NOT change (English nouns):
- "Senior animators draw key frames" (animation_studio body, project-corpus/animation-disney-system.md)
- "Junior animators fill frames between keys"
- "drawer 关心怎么画" (English-noun usage in character_designer)
- "drawer" in `drawer/SKILL.md` redirect stub (this is the stub itself, intentional)
- "animator" in `animator/SKILL.md` redirect stub (this is the stub itself, intentional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 15 consumer SKILL.md files — related_skills dedup + body prose reference sync</name>
  <files>
    skills/movie-experts/character_designer/SKILL.md,
    skills/movie-experts/scene_builder/SKILL.md,
    skills/movie-experts/editor/SKILL.md,
    skills/movie-experts/colorist/SKILL.md,
    skills/movie-experts/cinematographer/SKILL.md,
    skills/movie-experts/composer/SKILL.md,
    skills/movie-experts/production/SKILL.md,
    skills/movie-experts/performer/SKILL.md,
    skills/movie-experts/storyboard_designer/SKILL.md,
    skills/movie-experts/continuity_auditor/SKILL.md,
    skills/movie-experts/compliance_gate/SKILL.md,
    skills/movie-experts/foley/SKILL.md,
    skills/movie-experts/lip_sync/SKILL.md,
    skills/movie-experts/animation_studio/SKILL.md,
    skills/movie-experts/style_genome/SKILL.md
  </files>
  <read_first>
    - Each consumer SKILL.md's `metadata.hermes.related_skills:` line (typically around line 13)
    - Each consumer SKILL.md's body prose for these patterns (use grep to locate): `(../drawer/SKILL.md)`, `(../animator/SKILL.md)`, `**<- drawer**`, `**-> drawer**`, `**<- animator**`, `**-> animator**`, JSON literals `"drawer",` / `"animator",`, narrative mentions `drawer / animator`, `drawer + animator`, `(drawer, animator)`
    - .planning/phases/14-visual-executor-merge/14-CONTEXT.md (consumer update rules + dedup requirement)
    - .planning/phases/13-expert-rename-alias-scaffolding/13-01-SUMMARY.md (Phase 13 precedent — same consumer-edge-sync pattern, English-noun preservation rules)
    - CLAUDE.md §"Skill File Conventions (SKILL.md)" — related_skills is bidirectional
  </read_first>
  <behavior>
    - In each of the 15 consumer SKILL.md files, the `metadata.hermes.related_skills` array:
      - If it contains `drawer`: replace `drawer` with `visual_executor`
      - If it contains `animator`: replace `animator` with `visual_executor`
      - If it contains BOTH `drawer` and `animator`: replace with exactly ONE `visual_executor` entry (deduplicated — do NOT leave two `visual_executor` entries)
      - If it contains NEITHER (composer, animation_studio per the audit): no related_skills change required, but body prose changes still apply
    - In each consumer's body prose, update these expert_id reference patterns to use `visual_executor`:
      - Markdown links: `(../drawer/SKILL.md)` → `(../visual_executor/SKILL.md)`, `(../animator/SKILL.md)` → `(../visual_executor/SKILL.md)`
      - Link text in backtick-bracketed form: `` [`drawer`](../drawer/SKILL.md) `` → `` [`visual_executor`](../visual_executor/SKILL.md) ``, `` [`animator`](../animator/SKILL.md) `` → `` [`visual_executor`](../visual_executor/SKILL.md) ``
      - Collaboration bullets: `- **<- drawer**:` → `- **<- visual_executor**:`, same for `-> drawer`, `<- animator`, `-> animator`, and the backtick-bracketed variants
      - JSON literals: `"drawer",` inside JSON arrays → `"visual_executor",`, same for `"animator",`
      - Narrative pair references: `drawer / animator / lip_sync` → `visual_executor / lip_sync`, `(drawer, animator)` → `(visual_executor)`, `drawer + animator` → `visual_executor`
    - DO NOT change English-noun uses. Examples that MUST be preserved:
      - animation_studio/SKILL.md: "Senior animators draw key frames" (project corpus content) — this is a craft description, not an expert_id reference
      - character_designer/SKILL.md: "drawer 关心怎么画" (where drawer refers to the act of drawing, even though it's spelled like the expert_id) — context: the character_designer expert explains the difference between the role of drawer-as-act and character_designer-as-contract; preserve the comparison but ensure all true expert_id references use visual_executor. NOTE: this case is ambiguous — if the surrounding sentence is explaining "the drawer EXPERT generates images", change to visual_executor; if it's using "drawer" as a job-title noun meaning "the person drawing", preserve. Err on the side of: if there is a markdown link `[\`drawer\`](../drawer/SKILL.md)`, it is an expert_id reference → change. If "drawer" appears as a bare noun with no link, treat it as English noun → preserve.
    - DO NOT touch the redirect stubs in `skills/movie-experts/drawer/SKILL.md` and `skills/movie-experts/animator/SKILL.md` (those intentionally retain their old IDs)
    - DO NOT touch `_eval/baseline/` (frozen regression baselines per Phase 13 decision)
  </behavior>
  <action>
    1. For each of the 15 consumer files, first run `grep -n 'drawer\|animator' skills/movie-experts/<consumer>/SKILL.md` to enumerate exact line locations before editing.
    2. For each consumer's `metadata.hermes.related_skills:` array:
       - If both `drawer` and `animator` are present: use Edit tool to replace the pair with a single `visual_executor`. Example for character_designer (current: `related_skills: [style_genome, screenplay, drawer, animator, performer, continuity_auditor, lip_sync, production]`) → target: `related_skills: [style_genome, screenplay, visual_executor, performer, continuity_auditor, lip_sync, production]`.
       - If only one is present: replace it with `visual_executor`. Example for editor (current: `related_skills: [screenplay, animator, composer, voicer, continuity_auditor, mixer, compliance_gate, hook_retention, cinematographer, theory_critic, documentary_maker]`) → target: `related_skills: [screenplay, visual_executor, composer, voicer, continuity_auditor, mixer, compliance_gate, hook_retention, cinematographer, theory_critic, documentary_maker]`.
       - If neither is present (composer, animation_studio): SKIP the related_skills edit.
    3. For each consumer's body prose, update markdown links, link text, Collaboration bullets, JSON literals, and narrative pair references per the patterns in `<interfaces>`.
    4. Use Edit tool with targeted replacements — do NOT use blanket sed-style `s/drawer/visual_executor/g` because that would corrupt English-noun usages and redirect stubs.

    Consumer-by-consumer known touchpoints (verify with grep before editing):

    - **character_designer/SKILL.md**: line 13 related_skills (both drawer+animator → collapse to one visual_executor); description (line 3) "Decoupled from drawer" — change to "Decoupled from visual_executor"; body lines 26, 51, ~179 (JSON literal `"drawer",` + `"animator",`), 235, 262 (markdown links `[\`drawer\`](../drawer/SKILL.md)` + `[\`animator\`](../animator/SKILL.md)`). Preserve English-noun usages of "drawer" with no link.
    - **scene_builder/SKILL.md**: line 13 related_skills (both → collapse); body lines 140 (`- **-> drawer**:`), 141 (`- **-> animator**:`), 152 ("scene_builder alongside drawer/animator" — narrative pair).
    - **editor/SKILL.md**: line 13 related_skills (animator only → visual_executor); body line ~218 (`- **<- animator**:`). Verify no other drawer/animator references.
    - **colorist/SKILL.md**: line 13 related_skills (both → collapse); body lines 173 (`- **<- drawer**:`), 175 (`- **-> drawer**:` + `- **-> animator**:`).
    - **cinematographer/SKILL.md**: line 13 related_skills (both → collapse); body has multiple animator references in tables (e.g., line ~71 "| **animator** | EXECUTION |") + Collaboration bullets + DAG pipeline string. Update all expert_id references; preserve English-noun "animator" if any.
    - **composer/SKILL.md**: line 13 related_skills has NEITHER drawer NOR animator (verify — Phase 13 deviation note said composer was correctly excluded from continuity rename; confirm same for drawer/animator). Body line ~133 "Don't run MusicGen alongside drawer/animator on single GPU" — change to "visual_executor".
    - **production/SKILL.md**: line 13 related_skills (both → collapse); body has multiple drawer/animator references in Collaboration + DAG string. Update all.
    - **performer/SKILL.md**: line 13 related_skills (both → collapse); body lines for `-> drawer` + `-> animator` Collaboration bullets.
    - **storyboard_designer/SKILL.md**: line 13 related_skills (both → collapse); body lines for `-> drawer` + `-> animator` markdown links + JSON literals (drawer/animator in JSON arrays).
    - **continuity_auditor/SKILL.md**: line 13 related_skills (both drawer+animator → collapse to one visual_executor — Phase 13 already set these from the continuity rename); verify body references.
    - **compliance_gate/SKILL.md**: line 13 related_skills has `drawer` (only) per audit — replace with `visual_executor`. Body has `- **<- drawer**:` bullet — update.
    - **foley/SKILL.md**: line 13 related_skills has `animator` (only) — replace with `visual_executor`. Body has `- **<- animator**:` bullet — update.
    - **lip_sync/SKILL.md**: line 13 related_skills has `animator` (only) — replace with `visual_executor`. Body has HEAVY animator references — markdown links `[\`animator\`](../animator/SKILL.md)`, narrative comparisons "与 [`animator`](../animator/SKILL.md) 的核心区别", "这是 [`animator`](../animator/SKILL.md) 的职责", "切换到 animator". Update ALL expert_id references; preserve any English-noun "animator" if present.
    - **animation_studio/SKILL.md**: line 13 related_skills has NEITHER drawer NOR animator per audit. Body has multiple references to `drawer` + `animator` in tables, narrative, and pipeline diagrams. Update expert_id references; preserve "Senior animators draw key frames" + "Junior animators fill frames between keys" (craft descriptions, not expert references).
    - **style_genome/SKILL.md**: line 13 related_skills has `drawer` (only) — replace with `visual_executor`. Body has table row "| drawer | composition + color + light_shadow |", narrative "drawer: composition...", Collaboration `- **-> drawer**:`, and DAG pipeline string `style_genome -> screenplay -> ... -> (drawer, voicer, colorist, ...) -> (animator, foley, ...) -> ...`. Update all expert_id references.
  </action>
  <verify>
    <automated>
    echo "--- related_skills: no consumer has both drawer and animator ---" && \
    for c in character_designer scene_builder editor colorist cinematographer composer production performer storyboard_designer continuity_auditor compliance_gate foley lip_sync animation_studio style_genome; do
      # Extract related_skills line
      LINE=$(grep "related_skills:" skills/movie-experts/$c/SKILL.md)
      # No drawer or animator as standalone token
      echo "$LINE" | grep -qE "\bdrawer\b" && { echo "STRAY drawer in $c: $LINE"; exit 1; } || true
      echo "$LINE" | grep -qE "\banimator\b" && { echo "STRAY animator in $c: $LINE"; exit 1; } || true
    done && \
    echo "--- 13 consumers that should have visual_executor in related_skills (all except composer + animation_studio) ---" && \
    for c in character_designer scene_builder editor colorist cinematographer production performer storyboard_designer continuity_auditor compliance_gate foley lip_sync style_genome; do
      grep -q "visual_executor" skills/movie-experts/$c/SKILL.md || { echo "MISSING visual_executor in $c"; exit 1; }
    done && \
    echo "--- No duplicate visual_executor entries in any related_skills array ---" && \
    for c in character_designer scene_builder editor colorist cinematographer production performer storyboard_designer continuity_auditor compliance_gate foley lip_sync style_genome; do
      LINE=$(grep "related_skills:" skills/movie-experts/$c/SKILL.md)
      COUNT=$(echo "$LINE" | grep -o "visual_executor" | wc -l)
      if [ "$COUNT" -gt 1 ]; then echo "DUPLICATE in $c: $LINE"; exit 1; fi
    done && \
    echo "--- No stray ../drawer/SKILL.md or ../animator/SKILL.md markdown links outside redirect stubs ---" && \
    grep -rn "\.\./drawer/SKILL\.md\|\.\./animator/SKILL\.md" skills/movie-experts/*/SKILL.md | grep -v "^skills/movie-experts/drawer/SKILL.md:" | grep -v "^skills/movie-experts/animator/SKILL.md:" | wc -l | grep -q "^0$" && \
    echo "--- No stray expert_id references in Collaboration bullets (outside redirect stubs) ---" && \
    grep -rn "\\*\\*-> drawer\\*\\*\\|\\*\\*<- drawer\\*\\*\\|\\*\\*-> animator\\*\\*\\|\\*\\*<- animator\\*\\*" skills/movie-experts/*/SKILL.md | grep -v "^skills/movie-experts/drawer/SKILL.md:" | grep -v "^skills/movie-experts/animator/SKILL.md:" | wc -l | grep -q "^0$" && \
    echo "--- _eval/baseline/ untouched (frozen regression baselines) ---" && \
    grep -q "expert_id: animator" skills/movie-experts/_eval/baseline/animator/SKILL.md && \
    grep -q "expert_id: drawer" skills/movie-experts/_eval/baseline/drawer/SKILL.md && \
    echo "--- Redirect stubs themselves still preserve old IDs (intentional) ---" && \
    grep -q "expert_id: drawer" skills/movie-experts/drawer/SKILL.md && \
    grep -q "expert_id: animator" skills/movie-experts/animator/SKILL.md && \
    echo "TASK-1 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - 13 consumer SKILL.md files (all except composer + animation_studio) have `visual_executor` in their related_skills array
    - composer and animation_studio have NO related_skills change (per audit — they don't list drawer/animator there)
    - No consumer has BOTH drawer AND animator in related_skills (all collapsed)
    - No consumer has DUPLICATE visual_executor entries in related_skills
    - No stray `../drawer/SKILL.md` or `../animator/SKILL.md` markdown links outside the drawer/ + animator/ redirect stubs themselves
    - No stray `**-> drawer**`, `**<- drawer**`, `**-> animator**`, `**<- animator**` Collaboration bullets outside redirect stubs
    - No stray `"drawer",` or `"animator",` JSON literals outside redirect stubs
    - English-noun usages of "drawer" and "animator" preserved (e.g., "Senior animators draw key frames")
    - `_eval/baseline/` untouched
    - Redirect stubs in `skills/movie-experts/drawer/SKILL.md` + `animator/SKILL.md` preserve their old `expert_id` fields (intentional)
  </acceptance_criteria>
  <done>
    All 15 consumer SKILL.md files updated. The collaboration graph is bidirectionally consistent: visual_executor lists these consumers in its related_skills (from plan 14-01), and they list visual_executor back. Zero stranded expert_id references to `drawer` or `animator` outside the redirect stubs. ROADMAP §14 success criterion #4 (all consumers updated) is satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer SKILL.md files → merged expert | If a consumer still references old `drawer` or `animator` after this plan, downstream agent dispatch may resolve to the redirect stub (functional but wasteful) or fail entirely |
| Bidirectional edge sync | related_skills is bidirectional per CLAUDE.md §Skill File Conventions — A listing B implies B should list A. visual_executor lists its consumers (from 14-01); this plan makes the reverse edge consistent. |
| English-noun vs expert_id disambiguation | "drawer" and "animator" appear as English nouns in some body prose — blanket replacement would corrupt those references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | Consumer related_skills dedup (collapse drawer+animator → one visual_executor) | mitigate | Task 1 verify loop checks for DUPLICATE visual_executor entries and STRAY drawer/animator in any related_skills array |
| T-14-07 | Repudiation | Bidirectional edge sync | mitigate | Task 1 verify loop checks that 13 expected consumers (all except composer + animation_studio) have visual_executor in related_skills |
| T-14-08 | Information disclosure | Stray markdown links to old paths | mitigate | Task 1 grep verifies zero `../drawer/SKILL.md` or `../animator/SKILL.md` outside redirect stubs |
| T-14-09 | Tampering | English-noun preservation (drawer / animator as common nouns) | mitigate | Action explicitly instructs the executor to use targeted Edit tool replacements, NOT blanket sed; verify step checks for preserved nouns in known locations |
| T-14-10 | Denial of service | Frozen `_eval/baseline/` regression snapshots | mitigate | Task 1 verify step confirms `_eval/baseline/drawer/SKILL.md` + `_eval/baseline/animator/SKILL.md` retain their original expert_id values (untouched) |

No third-party package installs in this plan — T-{phase}-SC (Tampering of npm/pip/cargo) is N/A.
</threat_model>

<verification>
After Task 1 completes, run the consolidated checks:

```bash
# 1. No consumer has stray drawer or animator in related_skills
for c in character_designer scene_builder editor colorist cinematographer composer production performer storyboard_designer continuity_auditor compliance_gate foley lip_sync animation_studio style_genome; do
  LINE=$(grep "related_skills:" skills/movie-experts/$c/SKILL.md)
  echo "$LINE" | grep -qE "\bdrawer\b" && echo "STRAY drawer in $c" && exit 1
  echo "$LINE" | grep -qE "\banimator\b" && echo "STRAY animator in $c" && exit 1
done

# 2. 13 consumers have visual_executor in related_skills (composer + animation_studio excluded)
for c in character_designer scene_builder editor colorist cinematographer production performer storyboard_designer continuity_auditor compliance_gate foley lip_sync style_genome; do
  grep -q "visual_executor" skills/movie-experts/$c/SKILL.md || { echo "MISSING in $c"; exit 1; }
done

# 3. No stray markdown links to old paths outside redirect stubs
grep -rn "\.\./drawer/SKILL\.md\|\.\./animator/SKILL\.md" skills/movie-experts/*/SKILL.md \
  | grep -v "^skills/movie-experts/drawer/SKILL.md:" \
  | grep -v "^skills/movie-experts/animator/SKILL.md:" \
  | wc -l   # must be 0

# 4. _eval/baseline/ untouched
grep -q "expert_id: drawer" skills/movie-experts/_eval/baseline/drawer/SKILL.md
grep -q "expert_id: animator" skills/movie-experts/_eval/baseline/animator/SKILL.md
```
</verification>

<success_criteria>
- [ ] All 13 expected consumers (character_designer, scene_builder, editor, colorist, cinematographer, production, performer, storyboard_designer, continuity_auditor, compliance_gate, foley, lip_sync, style_genome) have `visual_executor` in their related_skills array
- [ ] composer + animation_studio related_skills arrays are unchanged (audit confirmed they don't list drawer/animator there)
- [ ] No consumer has DUPLICATE visual_executor entries
- [ ] No consumer has stray drawer OR animator in related_skills
- [ ] Zero stray `../drawer/SKILL.md` or `../animator/SKILL.md` markdown links outside redirect stubs
- [ ] Zero stray Collaboration bullets referencing drawer/animator expert_id outside redirect stubs
- [ ] Zero stray JSON literals `"drawer",` or `"animator",` outside redirect stubs
- [ ] English-noun usages preserved (Senior animators draw key frames, etc.)
- [ ] `_eval/baseline/` regression snapshots untouched
- [ ] Redirect stubs in drawer/ + animator/ retain their old expert_id (backward-compat)
</success_criteria>

<output>
Create `.planning/phases/14-visual-executor-merge/14-02-SUMMARY.md` when done
</output>

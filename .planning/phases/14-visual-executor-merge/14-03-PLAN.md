---
phase: 14-visual-executor-merge
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
  - skills/movie-experts/_shared/quality-rubric.md
  - skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
  - skills/movie-experts/_shared/project-corpus/animation-disney-system.md
  - skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md
autonomous: true
requirements: [MERGE-01]
user_setup: []

must_haves:
  truths:
    - "README.md inventory replaces the two separate drawer + animator rows with a single visual_executor row that names both sub-steps"
    - "README.md corpus tree (File Layout section) lists visual_executor/ with its sub-folder references/ structure"
    - "README.md ASCII production DAG diagram shows visual_executor in place of the separate drawer + animator boxes"
    - "README.md Bridge/Bottleneck/RAG usage notes reference visual_executor where they previously referenced drawer/animator"
    - "_shared/glossary.md prose references to drawer/animator expert_id are updated to visual_executor"
    - "_shared/quality-rubric.md dimension 3 ownership line (`drawer + animator + colorist`) is updated to `visual_executor + colorist`"
    - "_shared/RAG-INVOCATION-PATTERN.md table rows attributing models to drawer or animator are updated to visual_executor (with sub-step annotation where relevant)"
    - "_shared/project-corpus/ files: animation-disney-system.md expert_id references updated to visual_executor; production-chinese-and-low-budget.md `drawer + animator` consolidation note updated"
    - "No stranded expert_id references to drawer/animator remain in README.md or _shared/ outside intentional alias/historical documentation contexts"
  artifacts:
    - path: "skills/movie-experts/README.md"
      provides: "Inventory + corpus tree + DAG diagram reflecting visual_executor merge"
      contains: "visual_executor"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "Glossary with updated expert ID references"
    - path: "skills/movie-experts/_shared/quality-rubric.md"
      provides: "Quality rubric dimension 3 ownership updated"
    - path: "skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md"
      provides: "RAG pattern table with visual_executor attribution"
    - path: "skills/movie-experts/_shared/project-corpus/animation-disney-system.md"
      provides: "Disney system corpus with updated expert references"
    - path: "skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md"
      provides: "Production corpus with updated expert references"
  key_links:
    - from: "skills/movie-experts/README.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "inventory markdown link"
      pattern: "\\(\\./visual_executor/SKILL\\.md\\)"
    - from: "skills/movie-experts/_shared/glossary.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "expert_id reference"
      pattern: "visual_executor"
---

<objective>
Close out Phase 14 by updating cross-cutting documentation artifacts (README.md inventory + corpus tree + ASCII DAG diagram, _shared/glossary.md, _shared/quality-rubric.md, _shared/RAG-INVOCATION-PATTERN.md, _shared/project-corpus/{animation-disney-system,production-chinese-and-low-budget}.md) to reflect the visual_executor merge. These are the cross-cutting docs that depend on both 14-01 (new expert exists) and 14-02 (consumer edges synced) being complete.

Purpose: Fulfill ROADMAP §14 success criterion #4's "README, _eval/, _shared/" consumer coverage + criterion #5's "`_shared/project-corpus/` consolidation or cross-reference" requirement. Without this, downstream phases (15, 16, 17) reading README would still see two separate experts instead of one merged visual_executor.

Output:
- README.md: inventory row consolidation (2 rows → 1 row), corpus tree update, ASCII DAG diagram update, narrative references update
- _shared/glossary.md: 4 prose references updated
- _shared/quality-rubric.md: dimension 3 ownership line updated
- _shared/RAG-INVOCATION-PATTERN.md: model attribution table updated
- _shared/project-corpus/animation-disney-system.md: stage references to drawer/animator updated
- _shared/project-corpus/production-chinese-and-low-budget.md: `drawer + animator` consolidation line updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-executor-merge/14-CONTEXT.md
@.planning/phases/14-visual-executor-merge/14-01-PLAN.md
@.planning/phases/14-visual-executor-merge/14-02-PLAN.md
@skills/movie-experts/visual_executor/SKILL.md
@skills/movie-experts/README.md
@skills/movie-experts/_shared/glossary.md
@skills/movie-experts/_shared/quality-rubric.md
@skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
@skills/movie-experts/_shared/project-corpus/animation-disney-system.md
@skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md

# Phase 13's 13-03-PLAN is the close-out precedent.
@.planning/phases/13-expert-rename-alias-scaffolding/13-03-PLAN.md
@.planning/phases/13-expert-rename-alias-scaffolding/13-03-SUMMARY.md

# NOTE: skills-mapping.yaml has NO sign_off field for n_to_one_merged entries
# (sign_off is only for one_to_one_renamed per skills-mapping.yaml convention).
# So there is NO skills-mapping.yaml update in this plan (unlike 13-03 which flipped
# sign_off_status for the rename entries). The merge entry at lines 85-89 is
# already canonical; no metadata change required.

<interfaces>
<!-- README.md edits required -->

1. **Inventory table (Phase 0 baselined section, around lines 68-69):**
   BEFORE:
   ```
   | [`drawer`](./drawer/SKILL.md) | 绘图专家 | FLUX 2 Klein 9B + LoRA + IP-Adapter + InstantID character consistency | 2 (Phase 5 light) |
   | [`animator`](./animator/SKILL.md) | 视频专家 | Hermes-catalog video gen (veo3.1 / kling-v3-4k / pixverse-v6 / ltx-2.3 / seedance-2.0) + temporal consistency | 2 (Phase 5 light) |
   ```
   AFTER (single row, mention sub_steps + Phase 14 merge):
   ```
   | [`visual_executor`](./visual_executor/SKILL.md) | 视觉执行专家 | Unified FLUX 2 image gen (drawer sub-step) + Hermes-catalog video gen (animator sub-step) — merged Phase 14 per v2.0 PRFP DAG §4.8 | 5 (Phase 5 light × 2 + Phase 14 merge) |
   ```

2. **character_designer row description (line ~94):**
   BEFORE: "Decoupled from drawer (drawer generates images, character_designer defines identity contract)"
   AFTER:  "Decoupled from visual_executor (visual_executor generates images, character_designer defines identity contract)"

3. **ASCII DAG diagram (lines ~170 + ~176):**
   BEFORE: two boxes — `│     drawer      │   (FLUX / LoRA stills)` and `│    animator     │   (Runway / Kling / Veo / Sora 2)`
   AFTER: single box — `│ visual_executor │   (FLUX stills + Runway/Kling/Veo video)`  (preserve column alignment with surrounding boxes; if the merged text is longer, use multi-line box per Phase 13 multi-line compliance box precedent)

4. **Narrative notes (lines ~215, 216, 218):**
   - "character_designer emits CharacterBible 2.0 consumed by drawer / animator / lip_sync / continuity_auditor" → "...consumed by visual_executor / lip_sync / continuity_auditor"
   - "storyboard_designer fills cinematographer → drawer gap" → "...cinematographer → visual_executor gap"
   - "Bottleneck nodes: screenplay (after style) / drawer (after intent) / lip_sync (after audio + footage) / mixer (after all audio)" → "Bottleneck nodes: screenplay (after style) / visual_executor (after intent) / lip_sync (after audio + footage) / mixer (after all audio)"

5. **File Layout corpus tree (lines ~374 + ~380):**
   BEFORE:
   ```
   ├── animator/           SKILL.md + references/{video-gen-model-matrix,temporal-consistency,camera-execution-and-degradation}.md + LICENSE.md (Phase 5 + Phase 7C increment)
   ...
   ├── drawer/             SKILL.md + references/{flux2-parameter-surface,character-consistency-lora}.md + LICENSE.md (Phase 5)
   ```
   AFTER:
   - DELETE the two old rows for animator/ and drawer/
   - ADD a new row for visual_executor/ (alphabetical placement near where animator/drawer were):
   ```
   ├── visual_executor/   SKILL.md + references/{drawer/{flux2-parameter-surface,character-consistency-lora},animator/{video-gen-model-matrix,temporal-consistency,camera-execution-and-degradation}}.md + drawer/LICENSE.md + animator/LICENSE.md + GAP-REPORT.md (Phase 5 + Phase 14 merge)
   ```
   - PRESERVE the old drawer/ and animator/ rows BUT annotate them as redirect stubs (consistent with how continuity/ + compliance_marketing/ are documented after Phase 13 — actually verify what Phase 13 did with those rows; if Phase 13 left them in the tree marked as redirect stubs, follow that pattern. If Phase 13 removed them entirely from the corpus tree, do the same here.) Recommended: leave the rows in the tree but append " (Phase 14 redirect stub)" to match Phase 13's treatment of `continuity/` + `compliance_marketing/` directories.

6. **`_eval/prompts/` inventory (line ~402):**
   - `animator_demo.yaml` stays (the prompts file is referenced by the runner; Phase 13 did not rename prompt files). Leave as-is. No change here.

7. **Phase 7 additions summary section (line ~477):**
   - `animator/references/camera-execution-and-degradation.md (NEW) — from kais-camera + kais-shooting-script + kais-evolink` — UPDATE the path to `visual_executor/references/animator/camera-execution-and-degradation.md` (cross-reference now points into the merged expert).

8. **Footer summary (line ~454):**
   - "v2 = 23 experts" — UPDATE to reflect Phase 14 merge reduces count. Phase 14 merges 2 → 1, so the count drops by 1. Compute the new total based on current README + Phase 13 changes. (After Phase 13: 23 experts. After Phase 14: 22 experts. Update accordingly. NOTE: Phase 18 will do the canonical 26→21 inventory reconciliation; this plan only updates the running count to keep README internally consistent.)

<!-- _shared/glossary.md edits required -->

- Line 23: `animator.camera_type` is a JSON path reference into the animator expert's schema. Since the animator expert is now a sub-step of visual_executor, update to `visual_executor.animator.camera_type` (the path now reflects the merged expert's nested sub-step structure).
- Line 78: "animator owns motion execution" → "visual_executor (animator sub-step) owns motion execution"
- Line 164: "下游所有专家(drawer / animator / lip_sync / continuity_auditor)的 ground truth" → "下游所有专家(visual_executor / lip_sync / continuity_auditor)的 ground truth"
- Line 186: "downstream consumers are drawer / animator / editor / continuity_auditor" → "downstream consumers are visual_executor / editor / continuity_auditor"
- Lines 194-195: "Enables animator to maintain character + scene consistency across shot boundaries" → "Enables visual_executor's animator sub-step to maintain character + scene consistency across shot boundaries"
- ADD a new glossary entry for `visual_executor` at the appropriate alphabetical position with EN↔CN mapping + brief context noting the Phase 14 merge from drawer + animator.

<!-- _shared/quality-rubric.md edits required -->

- Line 13: "`drawer` + `animator` + `colorist` own dimension 3" → "`visual_executor` + `colorist` own dimension 3"
- Line 23: table row "| 3 | AIGC 真实感 / AIGC realism (de-artificialization) | 20 | `drawer` + `animator` + `colorist` |" → "| 3 | AIGC 真实感 / AIGC realism (de-artificialization) | 20 | `visual_executor` + `colorist` |"
- Line 195: "`drawer` + `animator` + `colorist` references: cite Dimension 3 over-polish anti-patterns" → "`visual_executor` + `colorist` references: cite Dimension 3 over-polish anti-patterns"

<!-- _shared/RAG-INVOCATION-PATTERN.md edits required -->

- Lines 74-75: `| <video_gen_primary> | veo3.1, kling-v3-4k (vertical 短剧 capable) | animator |` and `| <video_gen_preview> | ltx-2.3, pixverse-v6 (cheap preview tier) | animator |` → change `animator` to `visual_executor (animator sub-step)`
- Lines 76-77: `| <image_gen_primary> | FLUX 2 Klein 9B (Hermes default via fal) | drawer |` and `| <image_gen_fast> | Z-Image Turbo (fastest tier) | drawer |` → change `drawer` to `visual_executor (drawer sub-step)`

<!-- _shared/project-corpus/animation-disney-system.md edits required -->

- Line 273: `Stage 3: storyboard via drawer (1 day)` → `Stage 3: storyboard via visual_executor (drawer sub-step) (1 day)`
- Line 275: `Stage 6-7: character + color script via drawer + colorist (1 day)` → `Stage 6-7: character + color script via visual_executor (drawer sub-step) + colorist (1 day)`
- Line 276: `Stage 8-11: animator execution (veo3.1 / kling) (1-2 days)` → `Stage 8-11: visual_executor (animator sub-step) execution (veo3.1 / kling) (1-2 days)`
- Lines 130, 135: "Senior animators draw key frames" + "Junior animators fill frames between keys" — PRESERVE UNCHANGED (these are craft descriptions of the animator role in Disney's pipeline, not expert_id references)

<!-- _shared/project-corpus/production-chinese-and-low-budget.md edits required -->

- Line 379: `4. Generation = drawer + animator` → `4. Generation = visual_executor (drawer + animator sub-steps)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md inventory + corpus tree + ASCII DAG + narrative notes</name>
  <files>
    skills/movie-experts/README.md
  </files>
  <read_first>
    - skills/movie-experts/README.md (full file — identify exact line numbers via grep before editing)
    - skills/movie-experts/visual_executor/SKILL.md (merged expert — verify sub_steps declaration for inventory row description)
    - .planning/phases/13-expert-rename-alias-scaffolding/13-03-SUMMARY.md (Phase 13 precedent for how README corpus tree handled continuity/ + compliance_marketing/ redirect stub directories)
    - .planning/phases/14-visual-executor-merge/14-CONTEXT.md (consumer coverage rules)
  </read_first>
  <behavior>
    - Inventory table (Phase 0 baselined section): the two rows for `drawer` and `animator` are consolidated into a single row for `visual_executor` that names both sub-steps
    - character_designer row description updated to reference `visual_executor` instead of `drawer`
    - ASCII DAG diagram: the two boxes for `drawer` + `animator` are consolidated into one box for `visual_executor`; column alignment preserved; if the merged box label is too long for one line, use a two-line box following the Phase 13 multi-line compliance_gate box precedent
    - Narrative notes (Bridge nodes, Bottleneck nodes, Identity contract) reference `visual_executor` instead of drawer/animator
    - File Layout corpus tree: a new row for `visual_executor/` is added with the sub-folder references/ structure; the old `drawer/` + `animator/` rows are PRESERVED but annotated with "(Phase 14 redirect stub)" — follow Phase 13's treatment of `continuity/` + `compliance_marketing/` directories in the corpus tree
    - Phase 7 additions summary section (line ~477): the `animator/references/camera-execution-and-degradation.md` cross-reference is updated to `visual_executor/references/animator/camera-execution-and-degradation.md`
    - Footer running expert count is decremented by 1 (drawer + animator merge: 2 → 1)
    - The `_eval/prompts/animator_demo.yaml` row (line ~402) is left as-is (Phase 13 precedent: prompt filenames are NOT renamed because the runner references them)
  </behavior>
  <action>
    1. **Edit inventory rows (around lines 68-69):** Use Edit tool to replace the two-row pair `[drawer] + [animator]` with the single `[visual_executor]` row shown in `<interfaces>` §1.
    2. **Edit character_designer row description (line ~94):** Replace "Decoupled from drawer (drawer generates images, character_designer defines identity contract)" with "Decoupled from visual_executor (visual_executor generates images, character_designer defines identity contract)".
    3. **Edit ASCII DAG diagram (lines ~170 + ~176):** Replace the two stacked boxes (`drawer` + `animator`) with a single `visual_executor` box. If the label `│ visual_executor │ (FLUX stills + Runway/Kling/Veo video)` exceeds the column width, use a two-line box matching the Phase 13 `compliance_` / `gate 合规` multi-line precedent.
    4. **Edit narrative notes (lines ~215, 216, 218):** Use targeted Edit replacements for the three narrative lines per `<interfaces>` §4.
    5. **Edit File Layout corpus tree (lines ~374 + ~380):** Use Edit to:
       a. Add a new row for `visual_executor/` with the sub-folder references structure
       b. Append "(Phase 14 redirect stub)" to the existing `drawer/` and `animator/` rows
    6. **Edit Phase 7 additions summary (line ~477):** Update the `animator/references/camera-execution-and-degradation.md` path to `visual_executor/references/animator/camera-execution-and-degradation.md`.
    7. **Edit footer running expert count (line ~454):** Decrement by 1 (e.g., "v2 = 23 experts" → "v2 = 22 experts" — but first verify Phase 13's actual count; if Phase 13's README footer was already updated, use the current count as baseline and decrement by 1).
  </action>
  <verify>
    <automated>
    echo "--- Inventory: single visual_executor row, no separate drawer/animator rows ---" && \
    grep -q "| \[\`visual_executor\`\](./visual_executor/SKILL.md)" skills/movie-experts/README.md && \
    ! grep -qE "^\| \[\`drawer\`\]\(./drawer/SKILL\.md\)\s+\|" skills/movie-experts/README.md && \
    ! grep -qE "^\| \[\`animator\`\]\(./animator/SKILL\.md\)\s+\|" skills/movie-experts/README.md && \
    echo "--- ASCII DAG: visual_executor box present ---" && \
    grep -q "visual_executor" skills/movie-experts/README.md && \
    echo "--- Corpus tree: visual_executor/ row added ---" && \
    grep -qE "├── visual_executor/" skills/movie-experts/README.md && \
    echo "--- Corpus tree: old drawer/ + animator/ rows annotated as redirect stubs ---" && \
    grep -qE "(Phase 14 redirect stub)" skills/movie-experts/README.md && \
    echo "--- character_designer row description updated ---" && \
    grep -q "Decoupled from visual_executor" skills/movie-experts/README.md && \
    echo "--- Phase 7 additions section path updated ---" && \
    grep -q "visual_executor/references/animator/camera-execution-and-degradation.md" skills/movie-experts/README.md && \
    echo "--- _eval/prompts/animator_demo.yaml row PRESERVED (not renamed) ---" && \
    grep -q "animator_demo.yaml" skills/movie-experts/README.md && \
    echo "TASK-1 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - Inventory table has ONE visual_executor row instead of separate drawer + animator rows
    - character_designer row description references visual_executor
    - ASCII DAG diagram has a visual_executor box (single or multi-line per Phase 13 precedent)
    - Bridge nodes / Bottleneck nodes / Identity contract narrative lines reference visual_executor
    - File Layout corpus tree has a new visual_executor/ row with sub-folder references structure
    - Old drawer/ + animator/ rows in corpus tree preserved with "(Phase 14 redirect stub)" annotation
    - Phase 7 additions section path updated to visual_executor/references/animator/...
    - Footer expert count decremented by 1
    - _eval/prompts/animator_demo.yaml row preserved (not renamed — Phase 13 precedent)
  </acceptance_criteria>
  <done>
    README.md inventory, corpus tree, DAG diagram, and narrative all reflect the visual_executor merge. A reader skimming README sees a single visual_executor expert where there used to be two.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update _shared/ glossary + quality-rubric + RAG-INVOCATION-PATTERN + project-corpus</name>
  <files>
    skills/movie-experts/_shared/glossary.md,
    skills/movie-experts/_shared/quality-rubric.md,
    skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md,
    skills/movie-experts/_shared/project-corpus/animation-disney-system.md,
    skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md
  </files>
  <read_first>
    - skills/movie-experts/_shared/glossary.md (full file — identify exact line numbers via grep before editing)
    - skills/movie-experts/_shared/quality-rubric.md (full file)
    - skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md (lines around 74-77)
    - skills/movie-experts/_shared/project-corpus/animation-disney-system.md (lines around 130, 135, 273, 275, 276)
    - skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md (line around 379)
    - skills/movie-experts/visual_executor/SKILL.md (sub_steps structure for glossary entry context)
    - .planning/phases/13-expert-rename-alias-scaffolding/13-03-SUMMARY.md (Phase 13 precedent for glossary entry additions)
  </read_first>
  <behavior>
    - _shared/glossary.md: 5 prose references to drawer/animator expert_id updated to visual_executor (with sub-step annotation where the JSON path reference distinguishes drawer vs animator sub-step); a new glossary entry for `visual_executor` added at the appropriate alphabetical position with EN↔CN mapping + Phase 14 merge context
    - _shared/quality-rubric.md: 3 references (line 13, line 23, line 195) updated from `drawer + animator + colorist` → `visual_executor + colorist`
    - _shared/RAG-INVOCATION-PATTERN.md: 4 table rows (lines 74-77) updated from `drawer` / `animator` → `visual_executor (drawer sub-step)` / `visual_executor (animator sub-step)`
    - _shared/project-corpus/animation-disney-system.md: 3 stage references (lines 273, 275, 276) updated to `visual_executor (drawer sub-step)` / `visual_executor (animator sub-step)`; 2 English-noun usages (lines 130, 135 — "Senior animators draw key frames", "Junior animators fill frames between keys") PRESERVED UNCHANGED
    - _shared/project-corpus/production-chinese-and-low-budget.md: line 379 updated from `Generation = drawer + animator` → `Generation = visual_executor (drawer + animator sub-steps)`
    - All edits use targeted Edit tool replacements, NOT blanket sed (to preserve English-noun usages and historical context)
  </behavior>
  <action>
    1. **_shared/glossary.md edits** (5 prose references + 1 new entry):
       a. Line 23: `animator.camera_type` → `visual_executor.animator.camera_type`
       b. Line 78: "animator owns motion execution" → "visual_executor (animator sub-step) owns motion execution"
       c. Line 164: "(drawer / animator / lip_sync / continuity_auditor)" → "(visual_executor / lip_sync / continuity_auditor)"
       d. Line 186: "downstream consumers are drawer / animator / editor / continuity_auditor" → "downstream consumers are visual_executor / editor / continuity_auditor"
       e. Lines 194-195: "Enables animator to maintain..." → "Enables visual_executor's animator sub-step to maintain..."
       f. Add a new glossary entry for `visual_executor` near the V section (or after the `voice_character` entry if alphabetized). Format:
          ```
          ### visual_executor

          **CN:** 视觉执行专家 — Phase 14 merge of `drawer` (FLUX 2 image gen) + `animator` (Hermes-catalog video gen). Declares `sub_steps: [drawer, animator]`. Owns unified visual + temporal consistency context per Phase 7 §4.8 + PITFALLS §2.1. Backward-compat aliases `[drawer, animator]` preserved per FOUND-08.

          **EN:** Visual Executor Expert — Phase 14 merge of `drawer` + `animator` experts per v2.0 PRFP DAG. Unified sub-steps handle image gen (FLUX 2 + LoRA + IP-Adapter + InstantID) and video gen (veo3.1 / kling-v3-4k / pixverse-v6 / ltx-2.3 / seedance-2.0).
          ```
    2. **_shared/quality-rubric.md edits** (3 references):
       a. Line 13: `` `drawer` + `animator` + `colorist` `` → `` `visual_executor` + `colorist` ``
       b. Line 23 (table row): `` `drawer` + `animator` + `colorist` `` → `` `visual_executor` + `colorist` ``
       c. Line 195: `` `drawer` + `animator` + `colorist` `` → `` `visual_executor` + `colorist` ``
    3. **_shared/RAG-INVOCATION-PATTERN.md edits** (4 table rows):
       a. Lines 74-75 (video_gen rows): `animator` → `visual_executor (animator sub-step)`
       b. Lines 76-77 (image_gen rows): `drawer` → `visual_executor (drawer sub-step)`
    4. **_shared/project-corpus/animation-disney-system.md edits** (3 stage references):
       a. Line 273: `Stage 3: storyboard via drawer` → `Stage 3: storyboard via visual_executor (drawer sub-step)`
       b. Line 275: `Stage 6-7: character + color script via drawer + colorist` → `Stage 6-7: character + color script via visual_executor (drawer sub-step) + colorist`
       c. Line 276: `Stage 8-11: animator execution` → `Stage 8-11: visual_executor (animator sub-step) execution`
       d. Lines 130, 135 PRESERVED (English nouns "Senior animators" / "Junior animators")
    5. **_shared/project-corpus/production-chinese-and-low-budget.md edit** (1 line):
       a. Line 379: `4. Generation = drawer + animator` → `4. Generation = visual_executor (drawer + animator sub-steps)`
  </action>
  <verify>
    <automated>
    echo "--- _shared/glossary.md: no stray expert_id refs to drawer/animator (excluding new visual_executor entry context) ---" && \
    grep -q "visual_executor.animator.camera_type" skills/movie-experts/_shared/glossary.md && \
    grep -q "visual_executor (animator sub-step) owns motion execution" skills/movie-experts/_shared/glossary.md && \
    grep -q "downstream consumers are visual_executor / editor / continuity_auditor" skills/movie-experts/_shared/glossary.md && \
    grep -q "Enables visual_executor's animator sub-step to maintain" skills/movie-experts/_shared/glossary.md && \
    grep -q "^### visual_executor$" skills/movie-experts/_shared/glossary.md && \
    echo "--- _shared/quality-rubric.md: dimension 3 ownership updated ---" && \
    ! grep -qE "\`drawer\` \+ \`animator\` \+ \`colorist\`" skills/movie-experts/_shared/quality-rubric.md && \
    grep -q "\`visual_executor\` + \`colorist\`" skills/movie-experts/_shared/quality-rubric.md && \
    echo "--- _shared/RAG-INVOCATION-PATTERN.md: model attribution updated ---" && \
    grep -q "visual_executor (animator sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md && \
    grep -q "visual_executor (drawer sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md && \
    echo "--- _shared/project-corpus/animation-disney-system.md: stage refs updated, English nouns preserved ---" && \
    grep -q "storyboard via visual_executor (drawer sub-step)" skills/movie-experts/_shared/project-corpus/animation-disney-system.md && \
    grep -q "visual_executor (animator sub-step) execution" skills/movie-experts/_shared/project-corpus/animation-disney-system.md && \
    grep -q "Senior animators draw key frames" skills/movie-experts/_shared/project-corpus/animation-disney-system.md && \
    grep -q "Junior animators fill frames between keys" skills/movie-experts/_shared/project-corpus/animation-disney-system.md && \
    echo "--- _shared/project-corpus/production-chinese-and-low-budget.md: generation line updated ---" && \
    grep -q "Generation = visual_executor (drawer + animator sub-steps)" skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md && \
    echo "--- Overall: no stray expert_id references to bare drawer/animator in _shared/ outside intentional contexts ---" && \
    # Exclude: visual_executor.animator.camera_type (intentional JSON path), English nouns "animators" (plural), redirect-stub references
    grep -rn "drawer\|animator" skills/movie-experts/_shared/ | grep -v "visual_executor" | grep -v "animators " | grep -v "drawer 关心" | wc -l | grep -q "^0$" && \
    echo "TASK-2 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - _shared/glossary.md has 5 updated prose references + 1 new visual_executor entry
    - _shared/quality-rubric.md has 3 references updated to `visual_executor + colorist`
    - _shared/RAG-INVOCATION-PATTERN.md has 4 table rows updated with sub-step annotation
    - _shared/project-corpus/animation-disney-system.md has 3 stage references updated; "Senior animators" + "Junior animators" English-noun usages preserved
    - _shared/project-corpus/production-chinese-and-low-budget.md has line 379 updated
    - No stray bare expert_id references to drawer/animator remain in _shared/ outside intentional contexts (JSON path, English plurals, redirect stubs)
  </acceptance_criteria>
  <done>
    All cross-cutting documentation under _shared/ reflects the visual_executor merge. ROADMAP §14 success criterion #4 (_shared/ consumer coverage) and criterion #5 (_shared/project-corpus/ consolidation) are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation readers → merged expert | If README or _shared/ docs still reference drawer/animator after this plan, readers will see stale topology and downstream phases (15, 16, 17) inherit stale context |
| English-noun preservation | "drawer" and "animator" appear as common nouns in project-corpus prose (Disney pipeline craft descriptions); blanket replacement would corrupt those references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Information disclosure | README.md inventory accuracy | mitigate | Task 1 verify confirms single visual_executor row replaces the two separate drawer + animator rows |
| T-14-12 | Tampering | ASCII DAG diagram topology consistency | mitigate | Task 1 verify confirms visual_executor box present in DAG diagram |
| T-14-13 | Repudiation | _shared/glossary.md new entry addition | mitigate | Task 2 verify confirms `^### visual_executor$` H3 header exists |
| T-14-14 | Tampering | English-noun preservation (Senior animators / Junior animators in project-corpus) | mitigate | Task 2 verify confirms both English-noun lines preserved verbatim + action explicitly instructs targeted Edit replacements not blanket sed |
| T-14-15 | Information disclosure | Stray expert_id references in _shared/ | mitigate | Task 2 verify greps _shared/ for stray drawer/animator references and requires 0 outside intentional contexts |

No third-party package installs in this plan — T-{phase}-SC (Tampering of npm/pip/cargo) is N/A.

NOTE: No skills-mapping.yaml update in this plan. Per the canonical mapping convention (confirmed in `.planning/research/v2-pipeline-design/skills-mapping.yaml`), `n_to_one_merged` entries do NOT carry a `sign_off_status` field — only `one_to_one_renamed` entries do. The visual_executor mapping entry at lines 85-89 is already canonical and complete. Phase 13's 13-03 flipped sign_off_status because those were renames; Phase 14 is a merge so no sign_off flip applies. (If a future Phase 18 verification step requires merge-entry sign-off, that is Phase 18's responsibility.)
</threat_model>

<verification>
After both tasks complete, run the consolidated checks:

```bash
# 1. README.md reflects the merge
grep -q "| \[\`visual_executor\`\](./visual_executor/SKILL.md)" skills/movie-experts/README.md
grep -qE "├── visual_executor/" skills/movie-experts/README.md
grep -q "Decoupled from visual_executor" skills/movie-experts/README.md
grep -qE "(Phase 14 redirect stub)" skills/movie-experts/README.md

# 2. _shared/glossary.md has new visual_executor entry + updated prose
grep -q "^### visual_executor$" skills/movie-experts/_shared/glossary.md
grep -q "visual_executor.animator.camera_type" skills/movie-experts/_shared/glossary.md

# 3. _shared/quality-rubric.md dimension 3 ownership updated
grep -q "\`visual_executor\` + \`colorist\`" skills/movie-experts/_shared/quality-rubric.md

# 4. _shared/RAG-INVOCATION-PATTERN.md model attribution
grep -q "visual_executor (animator sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
grep -q "visual_executor (drawer sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md

# 5. _shared/project-corpus/ files updated
grep -q "storyboard via visual_executor (drawer sub-step)" skills/movie-experts/_shared/project-corpus/animation-disney-system.md
grep -q "Generation = visual_executor (drawer + animator sub-steps)" skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md

# 6. English nouns preserved
grep -q "Senior animators draw key frames" skills/movie-experts/_shared/project-corpus/animation-disney-system.md

# 7. No skills-mapping.yaml change required (n_to_one_merged has no sign_off_status field)
grep -q "n_to_one_merged" .planning/research/v2-pipeline-design/skills-mapping.yaml
! grep -q "sign_off_status: signed_off" .planning/research/v2-pipeline-design/skills-mapping.yaml || \
  # If sign_off_status appears anywhere, it must be on the two renamed entries from Phase 13, not on visual_executor
  ! grep -A 5 "new_node: visual_executor" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status"
```
</verification>

<success_criteria>
- [ ] README.md inventory has ONE visual_executor row (replacing two drawer/animator rows)
- [ ] README.md character_designer row description references visual_executor
- [ ] README.md ASCII DAG diagram has a visual_executor box
- [ ] README.md Bridge/Bottleneck/Identity narrative references visual_executor
- [ ] README.md File Layout corpus tree has a new visual_executor/ row with sub-folder structure
- [ ] README.md old drawer/ + animator/ rows in corpus tree preserved with "(Phase 14 redirect stub)" annotation
- [ ] README.md Phase 7 additions section path updated
- [ ] README.md footer expert count decremented by 1
- [ ] _shared/glossary.md has 5 updated prose references + 1 new visual_executor entry
- [ ] _shared/quality-rubric.md has 3 references updated to `visual_executor + colorist`
- [ ] _shared/RAG-INVOCATION-PATTERN.md has 4 table rows updated with sub-step annotation
- [ ] _shared/project-corpus/animation-disney-system.md has 3 stage refs updated; English-noun usages preserved
- [ ] _shared/project-corpus/production-chinese-and-low-budget.md generation line updated
- [ ] No stray expert_id references to drawer/animator in _shared/ outside intentional contexts
- [ ] skills-mapping.yaml NOT modified (n_to_one_merged has no sign_off_status field by convention)
</success_criteria>

<output>
Create `.planning/phases/14-visual-executor-merge/14-03-SUMMARY.md` when done
</output>

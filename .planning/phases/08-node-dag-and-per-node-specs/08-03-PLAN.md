---
phase: 8
plan: 03
title: "Render Mermaid Visual + Update 01-NODE-DAG.md from YAML"
type: standard
wave: 3
depends_on: [02]
files_modified:
  - .planning/research/v2-pipeline-design/01-NODE-DAG.md
autonomous: true
requirements: [NODE-05, NODE-07, META-06]
must_haves:
  - "01-NODE-DAG.md contains Mermaid diagram with all 16 nodes + edge types visually distinguished (linear=solid, loop=dashed, human_gate=bold, consultative=dotted, cross_cutting_invariant=dot-dot-dash)"
  - "Mermaid diagram is regenerable from edges.yaml (i.e., the diagram matches edges.yaml contents exactly — reproducibility per NODE-05)"
  - "theory_critic appears as consultative vertical edge only (NOT in linear DAG blocking gate) per META-06"
  - "Loops (screenplay↔script_auditor, visual_executor↔continuity_auditor) shown with explicit exit conditions + max_iterations"
  - "Human gates (post-screenplay, post-editor) shown with review budget annotations"
  - "Cross-cutting invariants (style_genome → downstream, character_designer → downstream) shown clearly as fan-out edges"
---

# Plan 08-03: Render Mermaid Visual + Update 01-NODE-DAG.md

<objective>
Update `01-NODE-DAG.md` with a fully-rendered Mermaid diagram derived from `edges.yaml`. Ensure the visual is reproducible (matches YAML exactly) and that edge types are visually distinguishable per CONTEXT.md Area 3/4 decisions.
</objective>

<phase_goal>
**Plan 03's slice:** A reader can read 01-NODE-DAG.md and SEE the topology as a single diagram, with all 16 nodes + all edge types + loop/human-gate/invariant annotations visible. The diagram matches edges.yaml exactly — regenerating Markdown+Mermaid from YAML is reproducible (NODE-05).
</phase_goal>

<context>
<files_to_read_first>
- .planning/research/v2-pipeline-design/edges.yaml (Plan 01 output — source of truth for edges)
- .planning/research/v2-pipeline-design/nodes.yaml (Plan 02 output — for node metadata)
- .planning/research/v2-pipeline-design/01-NODE-DAG.md (Plan 01 skeleton with §1.0-§1.5; this plan enriches §1.5 Mermaid)
- .planning/phases/08-node-dag-and-per-node-specs/08-CONTEXT.md (Area 3/4: TD+LR Mermaid orientation)
</files_to_read_first>

<interfaces>
Plan 03 produces the visual layer. Plan 04 will produce per-node Markdown spec sheets rendered from nodes.yaml.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render full Mermaid topology diagram in 01-NODE-DAG.md §1.5</name>
  <read_first>
    - .planning/research/v2-pipeline-design/edges.yaml (all edges)
    - .planning/research/v2-pipeline-design/nodes.yaml (all nodes + layers)
    - .planning/research/v2-pipeline-design/01-NODE-DAG.md (existing §1.5 placeholder Mermaid)
  </read_first>
  <files>.planning/research/v2-pipeline-design/01-NODE-DAG.md</files>
  <action>
Replace the placeholder Mermaid diagram in §1.5 with a fully-rendered diagram based on edges.yaml contents.

**Diagram requirements:**
1. **TD orientation** for main flow (creative_source at top → quality_gate + compliance_gate at bottom)
2. **LR subgraphs** for parallel branches (intent layer, audio+post layer, etc.)
3. **All 16 nodes** appear with English kebab-case IDs + CN display names in parens
4. **Edge type styling** (Mermaid classDef):
   - `linear` — solid black arrows `-->`
   - `loop_with_critic` — dashed with bi-directional arrows `<-.->` or `<-->` with label "loop: max N iter, exit: condition"
   - `human_gate` — bold double-arrow `==>` with "🚨 human review (<Nmin)" label
   - `consultative` — dotted arrows `-.->` with "📋 consultative" label
   - `cross_cutting_invariant` — dot-dot-dash arrows (use Mermaid `-.->` with classDef styling)
5. **Layer subgraphs** clearly grouped with background colors:
   - L0 root — light blue background
   - L1 intent parallel — light green
   - L2 visual intent — light yellow
   - L3 visual exec — light orange
   - L4 audio+post — light purple
   - L5 form-specific — light pink (短剧 only)
   - L6 final gates — light red
   - vertical (theory_critic) — gray, on the side
6. **Annotations on loops**: max_iterations + exit_condition visible
7. **Annotations on human_gates**: review_budget_minutes + reviewer
8. **Annotations on cross_cutting_invariants**: which invariant (style genome 5D vector / character identity)

**Update §1.4 (Edges) to match exactly:** ensure §1.4 human-readable edge list matches edges.yaml 1:1. If Plan 01 had any drift, reconcile here.

**Add §1.6 — Regeneration note:**
> This diagram + §1.4 edge list is regenerable from `edges.yaml` + `nodes.yaml`. Regeneration procedure: read edges.yaml → emit Mermaid edge per YAML entry with classDef based on `type` field → emit node per nodes.yaml entry with layer-based subgraph assignment. The lint script `scripts/validate_design.py` (Phase 12 GOV-02) will verify this regenerability.
  </action>
  <verify>
    - `grep -c 'graph TD' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns 1 (TD orientation)
    - `grep -cE 'subgraph L[0-6]' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns 7 (6 layers + 1 vertical)
    - `grep -c 'classDef' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns ≥5 (5 edge type styles)
    - `grep -cE 'creative_source|style_genome|screenplay|script_auditor|character_designer|cinematographer|prompt_injector|visual_executor|audio_pipeline|continuity_auditor|editor|colorist|hook_retention|quality_gate|compliance_gate|theory_critic' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns ≥16 (all nodes appear)
    - `grep -cE '<-.->|<-->' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns ≥2 (2 loops as dashed bi-directional)
    - `grep -c '==>' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns ≥2 (2 human_gates as bold)
    - `grep -c 'consultative' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns ≥2 (theory_critic edge + class)
    - `grep -c '## §1.6\|regener' .planning/research/v2-pipeline-design/01-NODE-DAG.md` returns ≥1 (regeneration note)
    - Manual visual check: the Mermaid diagram renders cleanly (no syntax errors) when viewed in a Mermaid viewer
  </verify>
  <done>
- Mermaid diagram in §1.5 fully rendered with all 16 nodes + all edge types
- 7 subgraphs (6 layers + 1 vertical)
- 5 classDef styles for edge types
- Loops + human_gates + cross_cutting_invariants annotated
- §1.6 regeneration note added
  </done>
</task>

</tasks>

<verification>
**Plan 08-03 completion criteria:**
- [ ] §1.5 Mermaid diagram has all 16 nodes
- [ ] 5 edge type styles via classDef
- [ ] 7 subgraphs
- [ ] Loops + human_gates + consultative + cross_cutting_invariants visually distinct
- [ ] §1.6 regeneration note added
- [ ] §1.4 edge list matches edges.yaml exactly
</verification>

<success_criteria>
A reader can read 01-NODE-DAG.md and SEE the entire topology in one diagram — no ambiguity about what connects to what, what's a loop, what's a human gate, what's consultative, what's a cross-cutting invariant.

The diagram is reproducible from edges.yaml + nodes.yaml (Phase 12 lint will verify).
</success_criteria>

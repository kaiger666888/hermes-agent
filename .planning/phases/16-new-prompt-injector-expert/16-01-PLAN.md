---
phase: 16-new-prompt-injector-expert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/movie-experts/prompt_injector/SKILL.md
  - skills/movie-experts/prompt_injector/references/prompt-engineering-patterns.md
  - skills/movie-experts/prompt_injector/references/cross-call-consistency.md
  - skills/movie-experts/prompt_injector/references/token-budget-management.md
  - skills/movie-experts/prompt_injector/references/model-specific-prompt-templates.md
  - skills/movie-experts/prompt_injector/references/LICENSE.md
  - skills/movie-experts/prompt_injector/GAP-REPORT.md
  - skills/movie-experts/creative_source/SKILL.md
  - skills/movie-experts/cinematographer/SKILL.md
  - skills/movie-experts/visual_executor/SKILL.md
  - skills/movie-experts/audio_pipeline/SKILL.md
autonomous: true
requirements: [NEW-01]
tags: [movie, prompt-engineering, cross-call-consistency, ai-native, new-expert]

must_haves:
  truths:
    - "A reader can open skills/movie-experts/prompt_injector/SKILL.md and find a complete expert with EN YAML structure + CN prose body (per META-03)"
    - "metadata.hermes.expert_id equals prompt_injector exactly"
    - "metadata.hermes.related_skills contains exactly the 4 entries [creative_source, cinematographer, visual_executor, audio_pipeline]"
    - "metadata.hermes.metrics contains exactly [cross_call_consistency, prompt_token_efficiency]"
    - "4 refs exist under prompt_injector/references/ covering prompt engineering patterns + cross-call consistency + token budget + model-specific templates"
    - "All 4 consumer experts (creative_source, cinematographer, visual_executor, audio_pipeline) now list prompt_injector in their related_skills (bidirectional edge sync)"
    - "No metadata.hermes.aliases field exists (NEW expert, no v1 predecessor)"
    - "No redirect stub created (prompt_injector has no predecessor directory to redirect from)"
  artifacts:
    - path: "skills/movie-experts/prompt_injector/SKILL.md"
      provides: "Prompt Injector Expert (提示注入) — AI-native node translating visual_intent + style_genome + character_assets into model_prompts + consistency_context"
      contains: "expert_id: prompt_injector"
      min_lines: 200
    - path: "skills/movie-experts/prompt_injector/references/prompt-engineering-patterns.md"
      provides: "few-shot / chain-of-thought / template structures / decomposition patterns"
      min_lines: 80
    - path: "skills/movie-experts/prompt_injector/references/cross-call-consistency.md"
      provides: "LoRA / IP-Adapter / identity-preserving prompting across multiple gen-model calls"
      min_lines: 80
    - path: "skills/movie-experts/prompt_injector/references/token-budget-management.md"
      provides: "chunking + hierarchical prompts + system-prompt-for-stable-constraints (≤4000 tokens/call)"
      min_lines: 80
    - path: "skills/movie-experts/prompt_injector/references/model-specific-prompt-templates.md"
      provides: "FLUX 2 / Veo / Kling template examples (cross-ref visual_executor refs; provider-agnostic placeholder pattern)"
      min_lines: 80
    - path: "skills/movie-experts/prompt_injector/references/LICENSE.md"
      provides: "MIT-style license + source attribution per existing pattern"
    - path: "skills/movie-experts/prompt_injector/GAP-REPORT.md"
      provides: "Placeholder GAP-REPORT noting NEW-expert status (no v1 baseline)"
    - path: "skills/movie-experts/creative_source/SKILL.md"
      provides: "updated related_skills with prompt_injector appended"
    - path: "skills/movie-experts/cinematographer/SKILL.md"
      provides: "updated related_skills with prompt_injector appended"
    - path: "skills/movie-experts/visual_executor/SKILL.md"
      provides: "updated related_skills with prompt_injector appended"
    - path: "skills/movie-experts/audio_pipeline/SKILL.md"
      provides: "updated related_skills with prompt_injector appended"
  key_links:
    - from: "skills/movie-experts/prompt_injector/SKILL.md"
      to: "skills/movie-experts/creative_source/SKILL.md"
      via: "related_skills bidirectional edge"
      pattern: "prompt_injector.*creative_source|creative_source.*prompt_injector"
    - from: "skills/movie-experts/prompt_injector/SKILL.md"
      to: "skills/movie-experts/cinematographer/SKILL.md"
      via: "related_skills bidirectional edge"
      pattern: "prompt_injector.*cinematographer|cinematographer.*prompt_injector"
    - from: "skills/movie-experts/prompt_injector/SKILL.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "related_skills bidirectional edge + I/O contract (visual_executor consumes model_prompts + consistency_context)"
      pattern: "prompt_injector.*visual_executor|visual_executor.*prompt_injector"
    - from: "skills/movie-experts/prompt_injector/SKILL.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "related_skills bidirectional edge"
      pattern: "prompt_injector.*audio_pipeline|audio_pipeline.*prompt_injector"
---

<objective>
Create the `prompt_injector` expert — the only NEW AI-native node in v3.0 (no v1 predecessor). Translates upstream human intent (visual_intent + style_genome + character_assets) into model-ready prompts (model_prompts + consistency_context) consumed by visual_executor and audio_pipeline. After this plan, the expert exists with full content + 4 refs + bidirectional collaboration edges to all 4 declared consumers.

Purpose: NEW-01 satisfaction — establish the AI-native node per v2.0 PRFP DAG Phase 8 §2.7. Without it, visual_executor has no canonical upstream prompt-assembly node and consistency context management is ad-hoc per-shot.
Output: `skills/movie-experts/prompt_injector/` directory (SKILL.md + 4 refs + LICENSE.md + GAP-REPORT.md) + 4 consumer SKILL.md `related_skills` edges added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-new-prompt-injector-expert/16-CONTEXT.md

@.planning/research/v2-pipeline-design/02-NODE-SPECS.md
@.planning/research/v2-pipeline-design/nodes.yaml
@.planning/research/v2-pipeline-design/skills-mapping.yaml

@skills/movie-experts/audio_pipeline/SKILL.md
@skills/movie-experts/visual_executor/SKILL.md
@skills/movie-experts/cinematographer/SKILL.md
@skills/movie-experts/creative_source/SKILL.md

<interfaces>
<!-- Node spec contract from .planning/research/v2-pipeline-design/nodes.yaml lines 448-523 + 02-NODE-SPECS.md §2.7 -->

**I/O contract (canonical, from nodes.yaml):**
- Inputs: `visual_intent` (from cinematographer) + `style_genome_5d` (from style_genome) + `character_assets` (from character_designer) — all required
- Outputs: `model_prompts` (consumers: visual_executor) + `consistency_context` (consumers: visual_executor) — both required

**Success criteria (canonical):**
- `cross_call_consistency >= 0.85` (measurement: downstream visual outputs style/identity consistency)
- `prompt_token_efficiency <= 4000 tokens per call` (measurement: token count vs defined ceiling)

**Fail modes + fallback:**
- `consistency_drift` → fallback: "consistency context 显式 carry across calls + repeat key constraints"
- `prompt_overload` → fallback: "split prompt into structured sections + use system prompt for stable constraints"

**Budget:** ¥50/episode (compute) · 30s latency · stable_2026 maturity · O(1) complexity class

**Model horizon:** stable_2026 (prompt engineering stable; cross-call context management evolving)

**Dependencies (from nodes.yaml):** hard: [cinematographer, style_genome, character_designer]; soft: []
**Note:** ROADMAP §16 criterion #3 lists `related_skills: [creative_source, cinematographer, visual_executor, audio_pipeline]`. These differ from nodes.yaml `hard dependencies` because related_skills is the collaboration graph (bidirectional peer edges for skill recommendation + DAG traversal), while nodes.yaml `hard dependencies` is the data-flow predecessor set. Both must coexist — related_skills includes downstream consumers (visual_executor, audio_pipeline) which are NOT hard dependencies. creative_source appears in related_skills (DAG root + intent upstream) but not in nodes.yaml hard deps because creative_source produces story_kernel (consumed by screenplay → cinematographer chain, not directly by prompt_injector).

**Current frontmatter of 4 consumers (BEFORE this plan):**
- creative_source/SKILL.md line 13: `related_skills: [style_genome, screenplay, hook_retention, compliance_gate, topic_curator]`
- cinematographer/SKILL.md line 13: `related_skills: [screenplay, scene_builder, visual_executor, editor, continuity_auditor, hook_retention, production, theory_critic, documentary_maker]`
- visual_executor/SKILL.md line 14: `related_skills: [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer, audio_pipeline]`
- audio_pipeline/SKILL.md line 14: `related_skills: [screenplay, performer, editor, production, visual_executor, continuity_auditor, style_genome, scene_builder]`

None currently reference prompt_injector (grep-verified zero matches across entire skills/movie-experts/ tree).

**Body audit:** None of the 4 consumers mention `prompt_injector`, `model_prompts`, or `consistency_context` in body prose. This is a CLEAN INSERTION — only `related_skills` arrays need updating, no body-prose reference sync required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create prompt_injector expert directory with SKILL.md + 4 refs + LICENSE.md + GAP-REPORT.md</name>
  <files>
    skills/movie-experts/prompt_injector/SKILL.md
    skills/movie-experts/prompt_injector/references/prompt-engineering-patterns.md
    skills/movie-experts/prompt_injector/references/cross-call-consistency.md
    skills/movie-experts/prompt_injector/references/token-budget-management.md
    skills/movie-experts/prompt_injector/references/model-specific-prompt-templates.md
    skills/movie-experts/prompt_injector/references/LICENSE.md
    skills/movie-experts/prompt_injector/GAP-REPORT.md
  </files>
  <read_first>
    - skills/movie-experts/audio_pipeline/SKILL.md (full — frontmatter schema + body structure + References table pattern + Knowledge Retrieval pattern + Changelog pattern)
    - skills/movie-experts/visual_executor/SKILL.md (full — most recent merged-expert pattern with sub_steps; reference for SKILL.md body organization)
    - .planning/research/v2-pipeline-design/02-NODE-SPECS.md (lines 160-180 — §2.7 prompt_injector node spec canonical source)
    - .planning/research/v2-pipeline-design/nodes.yaml (lines 448-523 — full machine-readable node spec: io_contract, success_criteria, fail_modes, fallback_strategy, cost_budget, latency_budget, model_horizon, current_instantiation)
    - skills/movie-experts/audio_pipeline/references/LICENSE.md (LICENSE.md template — copy this exact form)
    - skills/movie-experts/visual_executor/references/drawer/flux-2-prompt-engineering.md (if exists — cross-reference material for model-specific templates ref)
    - skills/movie-experts/_shared/known-external-models.yaml (model name placeholders convention — use `<image_primary>` / `<video_primary>` pattern, NOT literal model names)
  </read_first>
  <action>
    Create `skills/movie-experts/prompt_injector/` directory tree (SKILL.md + references/ + GAP-REPORT.md). Follow the audio_pipeline + visual_executor SKILL.md structural pattern exactly.

    **SKILL.md frontmatter (exact field values per ROADMAP §16 criteria + CONTEXT decisions):**
    - `name: prompt_injector`
    - `description:` one-line EN summary mentioning AI-native node, intent→prompt translation, cross-call consistency
    - `version: 1.0.0`
    - `author: Hermes Agent`
    - `license: MIT`
    - `platforms: [linux, macos, windows]`
    - `prerequisites: tools: [hermes_llm]`
    - `metadata.hermes.tags:` ~10-15 lowercase hyphenated tags covering [movie, prompt-engineering, cross-call-consistency, model-prompts, ai-native, intent-translation, token-budget, few-shot, chain-of-thought, ip-adapter, lora-reference, identity-preservation, template-engineering]
    - `metadata.hermes.related_skills: [creative_source, cinematographer, visual_executor, audio_pipeline]` (EXACTLY these 4 per ROADMAP §16 criterion #3 + CONTEXT specifics; do NOT add style_genome or character_designer even though nodes.yaml lists them as hard deps — they are data-flow predecessors, not collaboration-graph peers; their edges are reflected in body prose I/O section, not in related_skills)
    - `metadata.hermes.expert_id: prompt_injector`
    - `metadata.hermes.metrics: [cross_call_consistency, prompt_token_efficiency]` (EXACTLY these 2 per ROADMAP §16 criterion #4)
    - **DO NOT add `metadata.hermes.aliases`** — NEW expert, no v1 predecessor (per CONTEXT D-01). FOUND-08 alias requirement does not apply.
    - **DO NOT add top-level `sub_steps`** — prompt_injector is a single-node expert, not a merged composite.

    **SKILL.md body structure (mirror audio_pipeline/visual_executor pattern):**
    1. H1 bilingual: `# Prompt Injector Expert (提示注入)`
    2. Opening paragraph: state AI-native nature (no traditional anchor per nodes.yaml `traditional_anchor: 无传统对应`), the I/O contract (in: visual_intent + style_genome + character_assets; out: model_prompts + consistency_context), and the v2.0 PRFP derivation (per Phase 7 §4.7 D3.5+D2.4).
    3. `## When to use this skill` — bulleted trigger conditions (intent-to-prompt translation needed / cross-call consistency context construction / token budget planning / model-specific template selection)
    4. `## Role & Philosophy` — 4-5 bullets covering: (a) prompt engineering IS the discipline (not a translation layer), (b) cross-call consistency context is first-class output not a side effect, (c) token budget is a hard ceiling (≤4000/call), (d) provider-agnostic — no model names in SKILL.md body (use placeholders), (e) AI-native means no traditional cinematography/editor precedent
    5. `## Core Capabilities` — bullet list mirroring nodes.yaml core_task: intent decomposition, style genome vector injection, character asset reference compilation, cross-call consistency context assembly, prompt token budget management, model-specific template instantiation
    6. `## I/O Contract` — table showing inputs (visual_intent, style_genome_5d, character_assets with their sources: cinematographer, style_genome, character_designer) and outputs (model_prompts, consistency_context with their consumers: visual_executor). Quote the canonical success criteria numbers (cross_call_consistency ≥ 0.85; prompt_token_efficiency ≤ 4000 tokens/call) and the budget (¥50/episode · 30s · stable_2026).
    7. `## Output Format` — bulleted list of JSON artifacts this expert produces: `model_prompts.json`, `consistency_context.json` (with example field shapes — see nodes.yaml outputs block)
    8. `## Key Parameters` — H3 subsections per parameter group:
       - `### Prompt Structure` — few-shot template selection, CoT chain depth, decomposition granularity
       - `### Cross-Call Consistency` — consistency_context carry strategy, identity reference selection (per cross-call-consistency ref), repeat-key-constraints list (per fail-mode fallback)
       - `### Token Budget` — ≤4000 hard ceiling, system-prompt-for-stable-constraints strategy (per fail-mode fallback), chunking thresholds
       - `### Model-Specific Templates` — FLUX 2 / Veo / Kling instantiation patterns (refer to model-specific-prompt-templates ref; use `<image_primary>` / `<video_primary>` placeholders — NO literal model names in body)
    9. `## Fail Modes + Fallback` — table from nodes.yaml (consistency_drift → carry context + repeat key constraints; prompt_overload → split prompt + system prompt for stable constraints)
    10. `## References` — 4-row table following audio_pipeline pattern: | Ref | When to Read | Contents |
    11. `## Knowledge Retrieval` — bullet list of 4 retrieval topics with RAG tag queries using `tags="expert:prompt_injector,domain:X"` pattern (NO sub-step prefix — prompt_injector has no sub_steps). Provide both the memory-plugin form and the static-file fallback form.
    12. `## Collaboration` — bulleted inbound/outbound edges using the same `**<- expert**` / `**-> expert**` form as audio_pipeline. Cover all 4 related_skills peers (creative_source, cinematographer, visual_executor, audio_pipeline) plus the 3 data-flow predecessors from nodes.yaml hard deps (style_genome, character_designer — even though not in related_skills, they are upstream data sources and should appear as inbound edges). Distinguish in a note that related_skills is the collaboration-graph peer set while nodes.yaml hard deps is the data-flow predecessor set.
    13. `## Changelog` — single entry: `2026-06-17 — Created prompt_injector expert (v1.0.0) per Phase 16 NEW-01. AI-native node (no v1 predecessor); mapping_type: new_ai_native per skills-mapping.yaml:99-103. Node spec source: 02-NODE-SPECS.md §2.7 + nodes.yaml lines 448-523. No aliases (NEW expert). No redirect stub (no predecessor directory).`

    **4 Refs (Claude's discretion on exact content per CONTEXT.md D-04, but MUST cover the 4 declared domains):**

    1. **`references/prompt-engineering-patterns.md`** — General prompt engineering patterns applicable to AI 短剧/微电影 generation. Cover: few-shot template structures, chain-of-thought decomposition, structured prompt anatomy (system/context/user segments), task decomposition patterns (break complex visual intent into atomic prompt units), negative-prompt patterns. Min 80 lines. Cite at least 3 published sources (e.g., Wei et al. CoT, Brown et al. few-shot, OpenAI/Anthropic prompt-engineering guides).

    2. **`references/cross-call-consistency.md`** — Literature on maintaining character/scene/style consistency across multiple LLM/gen-model calls. Cover: LoRA adapter references (character identity preservation), IP-Adapter (image prompt conditioning for identity), InstantID (zero-shot identity-preserving generation), identity-preserving prompting strategies (seed locking, reference image embedding, multi-shot consistency context). Tie to the cross_call_consistency ≥ 0.85 success criterion. Min 80 lines. Cite at least 3 published sources (IP-Adapter paper, InstantID paper, LoRA paper, or equivalent).

    3. **`references/token-budget-management.md`** — Strategies for staying under the 4000 tokens/call ceiling. Cover: prompt chunking (split-by-concern: identity / composition / motion / lighting), hierarchical prompt structures (system-prompt for stable constraints + user-prompt for per-call variance), context window management, token counting methods per provider, redundancy elimination (don't repeat identity in every call — use consistency_context to carry). Min 80 lines. Cross-reference the fail-mode fallback (`prompt_overload → split prompt`).

    4. **`references/model-specific-prompt-templates.md`** — Concrete template examples for current 2026 model horizons. Cover: FLUX 2 prompt grammar (structured tags + natural language blend), Veo prompt grammar (cinematographic descriptor prefixes), Kling prompt grammar (Chinese-friendly natural language with action verbs), cross-provider abstraction patterns. **CRITICAL:** Use `<image_primary>` / `<video_primary>` placeholder convention (per visual_executor pattern) — NO literal model names as committed identifiers in body prose; literal model names ONLY in dated examples within this ref file with explicit `verified_date:` stamps. Cross-reference visual_executor/references/drawer/flux-2-prompt-engineering.md if it exists. Min 80 lines. Per CONTEXT D-04, cross-reference visual_executor refs is acceptable.

    **references/LICENSE.md:** Copy the exact form of `skills/movie-experts/audio_pipeline/references/LICENSE.md` (or whichever existing LICENSE.md exists). MIT license text + source attribution block for the 4 refs (note: prompt engineering patterns and cross-call consistency literature draw on published academic papers — cite as fair-use scholarship review per CLAUDE.md §Copyright constraint).

    **GAP-REPORT.md:** Per CONTEXT D-04 (placeholder OK for new expert). Single section noting: "prompt_injector is a NEW AI-native expert created in Phase 16 with no v1 predecessor. No baseline GAP-REPORT exists. This file is a placeholder per CONTEXT.md D-04. Phase 18 (validation + docs) may backfill a real gap analysis if kais-movie-agent implementation surfaces deltas." Sign and date.
  </action>
  <verify>
    <automated>
    cd /data/workspace/hermes-agent &&
    echo "=== SKILL.md frontmatter ===" &&
    head -20 skills/movie-experts/prompt_injector/SKILL.md &&
    echo "" &&
    echo "=== expert_id check ===" &&
    grep -c "^    expert_id: prompt_injector$" skills/movie-experts/prompt_injector/SKILL.md &&
    echo "=== related_skills check (expect exactly 4 declared peers) ===" &&
    grep -E "^    related_skills: \[creative_source, cinematographer, visual_executor, audio_pipeline\]$" skills/movie-experts/prompt_injector/SKILL.md &&
    echo "=== metrics check (expect exactly 2 declared) ===" &&
    grep -E "^    metrics: \[cross_call_consistency, prompt_token_efficiency\]$" skills/movie-experts/prompt_injector/SKILL.md &&
    echo "=== aliases absence check (expect zero matches) ===" &&
    (grep -c "aliases:" skills/movie-experts/prompt_injector/SKILL.md || echo "0 (correct — no aliases for NEW expert)") &&
    echo "=== sub_steps absence check ===" &&
    (grep -c "^sub_steps:" skills/movie-experts/prompt_injector/SKILL.md || echo "0 (correct — single-node expert)") &&
    echo "=== 4 refs existence ===" &&
    ls -1 skills/movie-experts/prompt_injector/references/*.md | wc -l &&
    echo "=== ref min-line check (each >= 80) ===" &&
    for f in skills/movie-experts/prompt_injector/references/prompt-engineering-patterns.md skills/movie-experts/prompt_injector/references/cross-call-consistency.md skills/movie-experts/prompt_injector/references/token-budget-management.md skills/movie-experts/prompt_injector/references/model-specific-prompt-templates.md; do echo "$f: $(wc -l < $f) lines"; done &&
    echo "=== GAP-REPORT exists ===" &&
    test -f skills/movie-experts/prompt_injector/GAP-REPORT.md && echo "OK" &&
    echo "=== provider-agnostic body check (expect zero literal model names committed as identifiers — model-specific-prompt-templates.md is the only allowed file) ===" &&
    grep -v "^skills/movie-experts/prompt_injector/references/model-specific-prompt-templates.md$" <(grep -rln "FLUX 2\|Runway Gen-3\|Kling 1.6\|Veo 2\|Sora 2" skills/movie-experts/prompt_injector/SKILL.md 2>/dev/null) || echo "0 SKILL.md body matches (correct)"
    </automated>
  </verify>
  <done>
    - skills/movie-experts/prompt_injector/SKILL.md exists with H1 `# Prompt Injector Expert (提示注入)`
    - Frontmatter contains exactly: expert_id=prompt_injector, related_skills=[creative_source, cinematographer, visual_executor, audio_pipeline], metrics=[cross_call_consistency, prompt_token_efficiency]
    - NO `aliases` field (NEW expert per D-01); NO `sub_steps` field (single-node expert)
    - Body follows audio_pipeline/visual_executor structure with all 13 sections (H1 through Changelog)
    - 4 refs each ≥ 80 lines, covering the 4 declared domains
    - references/LICENSE.md exists (MIT form copied from audio_pipeline pattern)
    - GAP-REPORT.md exists with placeholder note per D-04
    - SKILL.md body uses `<image_primary>` / `<video_primary>` placeholders (no literal model names committed as identifiers outside model-specific-prompt-templates.md ref)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bidirectional edge sync — add prompt_injector to related_skills of 4 consumers</name>
  <files>
    skills/movie-experts/creative_source/SKILL.md
    skills/movie-experts/cinematographer/SKILL.md
    skills/movie-experts/visual_executor/SKILL.md
    skills/movie-experts/audio_pipeline/SKILL.md
  </files>
  <read_first>
    - skills/movie-experts/creative_source/SKILL.md (lines 1-25 — frontmatter + opening body to confirm related_skills line and check for any body prose mention of prompt_injector)
    - skills/movie-experts/cinematographer/SKILL.md (lines 1-25 — same)
    - skills/movie-experts/visual_executor/SKILL.md (lines 1-25 — same)
    - skills/movie-experts/audio_pipeline/SKILL.md (lines 1-25 — same)
    - .planning/phases/16-new-prompt-injector-expert/16-CONTEXT.md (§Integration Points — confirms all 4 need bidirectional edges)
    - .planning/phases/14-visual-executor-merge/14-02-SUMMARY.md (if exists — Phase 14 consumer edge sync pattern reference; bidirectional edge dedup rules)
  </read_first>
  <action>
    For each of the 4 consumer SKILL.md files, append `prompt_injector` to the existing `metadata.hermes.related_skills` array. Place it at the END of the array (alphabetical or logical ordering is NOT required; append-to-end preserves the existing peer ordering and matches the Phase 14-15 edge-sync convention of "new edges appended, existing edges untouched").

    **Exact edits (single-line frontmatter change per file):**

    - `skills/movie-experts/creative_source/SKILL.md` line 13:
      - BEFORE: `    related_skills: [style_genome, screenplay, hook_retention, compliance_gate, topic_curator]`
      - AFTER:  `    related_skills: [style_genome, screenplay, hook_retention, compliance_gate, topic_curator, prompt_injector]`

    - `skills/movie-experts/cinematographer/SKILL.md` line 13:
      - BEFORE: `    related_skills: [screenplay, scene_builder, visual_executor, editor, continuity_auditor, hook_retention, production, theory_critic, documentary_maker]`
      - AFTER:  `    related_skills: [screenplay, scene_builder, visual_executor, editor, continuity_auditor, hook_retention, production, theory_critic, documentary_maker, prompt_injector]`

    - `skills/movie-experts/visual_executor/SKILL.md` line 14:
      - BEFORE: `    related_skills: [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer, audio_pipeline]`
      - AFTER:  `    related_skills: [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer, audio_pipeline, prompt_injector]`

    - `skills/movie-experts/audio_pipeline/SKILL.md` line 14:
      - BEFORE: `    related_skills: [screenplay, performer, editor, production, visual_executor, continuity_auditor, style_genome, scene_builder]`
      - AFTER:  `    related_skills: [screenplay, performer, editor, production, visual_executor, continuity_auditor, style_genome, scene_builder, prompt_injector]`

    **Do NOT modify body prose.** Grep-verified: none of the 4 consumers currently mention `prompt_injector`, `model_prompts`, or `consistency_context` in body prose. This is a clean edge addition — no prose reference sync needed (unlike Phase 14-15 which had extensive body prose work). If during execution any stray body reference is discovered, apply the same annotation pattern used in Phase 14-15 (inline parenthetical `(prompt_injector sub-step)` — but since prompt_injector has no sub_steps, the annotation would just be a markdown link `[\`prompt_injector\`](../prompt_injector/SKILL.md)`). Per Phase 15 Plan 02 deviation precedent (Rule 1 + Rule 2): correctness over plan-text literalism — if a 5th consumer is discovered with a stranded reference during the grep sweep, add it too and note in the SUMMARY.
  </action>
  <verify>
    <automated>
    cd /data/workspace/hermes-agent &&
    echo "=== 4-consumer bidirectional edge check ===" &&
    for f in skills/movie-experts/creative_source/SKILL.md skills/movie-experts/cinematographer/SKILL.md skills/movie-experts/visual_executor/SKILL.md skills/movie-experts/audio_pipeline/SKILL.md; do
      echo "--- $f ---";
      grep -E "prompt_injector" "$f" | head -3;
    done &&
    echo "" &&
    echo "=== each file has prompt_injector EXACTLY ONCE in related_skills (frontmatter) ===" &&
    for f in skills/movie-experts/creative_source/SKILL.md skills/movie-experts/cinematographer/SKILL.md skills/movie-experts/visual_executor/SKILL.md skills/movie-experts/audio_pipeline/SKILL.md; do
      count=$(grep -E "^    related_skills:.*prompt_injector" "$f" | wc -l);
      echo "$f: $count related_skills line(s) containing prompt_injector (expect 1)";
    done &&
    echo "" &&
    echo "=== dedup check (no file should have prompt_injector listed twice in same array) ===" &&
    for f in skills/movie-experts/creative_source/SKILL.md skills/movie-experts/cinematographer/SKILL.md skills/movie-experts/visual_executor/SKILL.md skills/movie-experts/audio_pipeline/SKILL.md; do
      dup=$(grep -E "^    related_skills:" "$f" | grep -o "prompt_injector" | wc -l);
      echo "$f: $dup occurrences in related_skills line (expect 1)";
    done
    </automated>
  </verify>
  <done>
    - All 4 consumer SKILL.md files have `prompt_injector` appended to their `metadata.hermes.related_skills` array exactly once
    - No existing related_skills entries removed or reordered (append-only)
    - No body prose modified (clean edge addition only)
    - If deviation discovery found additional stranded references, they were annotated with markdown link form and noted in the SUMMARY
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node-spec → SKILL.md | The canonical node spec (02-NODE-SPECS.md §2.7 + nodes.yaml lines 448-523) crosses into SKILL.md content. Drift between spec and implementation would break Phase 18 validation. |
| upstream → prompt_injector | visual_intent / style_genome_5d / character_assets flow from 3 upstream experts into prompt_injector. Schema assumptions must match producer output formats. |
| prompt_injector → visual_executor | model_prompts + consistency_context flow into visual_executor. Output schema must match visual_executor's input expectations. |
| collaboration graph | related_skills bidirectional edges across 4 consumer files must remain consistent — single-direction edge would strand prompt_injector in the DAG traversal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | SKILL.md frontmatter values (expert_id, related_skills, metrics) | mitigate | verify-automated check uses exact-string grep on the canonical lines; ROADMAP §16 success criteria #2/#3/#4 are quoted verbatim in task action |
| T-16-02 | Repudiation | refs content (4 files) sourcing | mitigate | each ref MUST cite ≥3 published sources per CLAUDE.md §Copyright (fair-use scholarship review); LICENSE.md must list source attribution |
| T-16-03 | Information Disclosure | model names in SKILL.md body | mitigate | `<image_primary>` / `<video_primary>` placeholder convention (matches visual_executor pattern); literal model names ONLY in model-specific-prompt-templates.md ref with verified_date stamps |
| T-16-04 | Tampering | bidirectional edge consistency | mitigate | Task 2 verify block enumerates all 4 files with occurrence-count grep; deviation discovery protocol (Phase 15 Rule 1+2 precedent) catches missed consumers |
| T-16-05 | Denial of Service | token budget overrun | accept | success criterion prompt_token_efficiency ≤ 4000 is documented in SKILL.md but not machine-enforced at content-creation time — Phase 18 VALIDATE-01 will spot-check |
| T-16-06 | Elevation of Privilege | nodes.yaml hard deps vs related_skills divergence | mitigate | Task 1 action explicitly documents the distinction (hard deps = data-flow predecessors; related_skills = collaboration-graph peers); both sets coexist in different sections of SKILL.md body |
</threat_model>

<verification>
1. `skills/movie-experts/prompt_injector/SKILL.md` exists and frontmatter matches ROADMAP §16 criteria #2/#3/#4 exactly (expert_id, related_skills, metrics)
2. 4 refs exist under `references/` each ≥ 80 lines covering the 4 declared domains
3. GAP-REPORT.md exists (placeholder per D-04)
4. references/LICENSE.md exists (MIT form per existing pattern)
5. All 4 consumer SKILL.md files (creative_source, cinematographer, visual_executor, audio_pipeline) have `prompt_injector` in their related_skills array exactly once
6. No body prose was modified in the 4 consumer files (clean edge addition)
7. No aliases field (NEW expert); no sub_steps field (single-node expert)
8. SKILL.md body uses provider-agnostic placeholders (no literal model names committed as identifiers)
9. Changelog entry dated 2026-06-17 references Phase 16 NEW-01 + skills-mapping.yaml new_ai_native mapping
</verification>

<success_criteria>
- ROADMAP §16 success criterion #1: prompt_injector/SKILL.md exists with EN structure + CN prose (Task 1)
- ROADMAP §16 success criterion #2: expert_id: prompt_injector (Task 1)
- ROADMAP §16 success criterion #3: related_skills: [creative_source, cinematographer, visual_executor, audio_pipeline] (Task 1)
- ROADMAP §16 success criterion #4: metrics: [cross_call_consistency, prompt_token_efficiency] (Task 1)
- ROADMAP §16 success criterion #5: 4 refs in prompt_injector/references/ (Task 1)
- (ROADMAP §16 success criterion #6 — README inventory — covered in Plan 16-02, not this plan)
- NEW-01 requirement: full expert creation + 4 refs + collaboration graph integration (Tasks 1+2)
- Bidirectional edge sync complete (Task 2)
</success_criteria>

<output>
Create `.planning/phases/16-new-prompt-injector-expert/16-01-SUMMARY.md` when done
</output>

---
phase: 56-validate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_milestone_audit.py
  - .planning/milestones/v11.0-MILESTONE-AUDIT.md
  - .planning/research/v11-poc-eval/smoke-test-report.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VALIDATE-01]

must_haves:
  truths:
    - "Audit file exists at .planning/milestones/v11.0-MILESTONE-AUDIT.md and walks all 15 reqs (INFRA-01..04 + CREATIVE-01..02 + EVAL-01..07 + MIGR-01 + VALIDATE-01) with per-req status + evidence pointer"
    - "Smoke test report exists at .planning/research/v11-poc-eval/smoke-test-report.md with automated-baseline sections populated + 5 operator-action sections marked human_needed"
    - "Audit script scripts/run_milestone_audit.py runs end-to-end and produces a machine-readable coverage matrix (JSON) covering all 15 reqs"
    - "Audit verdict is one of: passed | tech_debt | gaps_found | fail — with explicit verdict-logic documentation"
    - "REQUIREMENTS.md traceability table Status column updated for all 15 reqs to reflect audit outcome"
    - "5 operator-action handoffs (Phase 53 SC#2 + Phase 54 ×3 + Phase 55 ×1) aggregated into a single operator-action runbook section"
  artifacts:
    - path: "scripts/run_milestone_audit.py"
      provides: "Audit script that walks REQUIREMENTS.md + 4 VERIFICATION.md files and emits coverage matrix JSON"
      min_lines: 150
    - path: ".planning/milestones/v11.0-MILESTONE-AUDIT.md"
      provides: "Full milestone audit — scorecard + per-req traceability + operator-action runbook + verdict"
      min_lines: 300
    - path: ".planning/research/v11-poc-eval/smoke-test-report.md"
      provides: "Smoke test report template populated with automated baselines + human_needed operator-action sections"
      min_lines: 150
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with Status column updated to reflect Phase 56 audit outcome"
      contains: "VALIDATE-01"
  key_links:
    - from: "scripts/run_milestone_audit.py"
      to: ".planning/REQUIREMENTS.md"
      via: "REQ-ID parse from traceability table"
      pattern: "INFRA-0[1-4]|CREATIVE-0[1-2]|EVAL-0[1-7]|MIGR-01|VALIDATE-01"
    - from: "scripts/run_milestone_audit.py"
      to: ".planning/phases/5[2-5]-*/VERIFICATION.md"
      via: "status + human_verification frontmatter parse"
      pattern: "status:\\s*(passed|human_needed|failed)"
    - from: ".planning/milestones/v11.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/{52,53,54,55}-*/VERIFICATION.md"
      via: "per-req evidence pointers"
      pattern: "\\[.*VERIFICATION\\.md.*\\]"
    - from: ".planning/research/v11-poc-eval/smoke-test-report.md"
      to: "Phase 53 SC#2 + Phase 54 ×3 + Phase 55 ×1 VERIFICATION.md human_verification blocks"
      via: "operator-action runbook aggregation"
      pattern: "human_needed"
---

<objective>
v11.0 milestone close-out per VALIDATE-01: audit all 15 requirements (INFRA-01..04 + CREATIVE-01..02 + EVAL-01..07 + MIGR-01 + VALIDATE-01), aggregate the 5 operator-action handoffs deferred from Phases 53-55 into a single operator runbook, and produce a verdict.

Purpose: Phase 56 is the strictly-LAST close-out phase (analog to v10.0 Phase 51 / v9.0 Phase 43 / v5.0 Phase 27). It does NOT add new runtime code — it aggregates the 4 prior phase verifications into a milestone-level audit + smoke test report. Real-GLM smoke tests are operator-action because the executor sandbox has no `GLM_API_KEY`; this plan documents those handoffs (NOT gaps) and uses the automated-verification results from Phases 52-55 to drive the verdict.

Output:
- `scripts/run_milestone_audit.py` — automated audit script (coverage matrix producer)
- `.planning/milestones/v11.0-MILESTONE-AUDIT.md` — full audit document
- `.planning/research/v11-poc-eval/smoke-test-report.md` — smoke test report with automated baselines + human_needed sections
- `.planning/REQUIREMENTS.md` — traceability table Status column updated

Verdict strategy (per CONTEXT.md `Claude's Discretion` + operator-action-handoff convention):
- All 15 reqs have automated verification PASSING at the implementation level (Phase 52: 4/4 SC passed; Phase 53: 2/3 passed + 1 human_needed; Phase 54: 3/3 human_needed at automated level; Phase 55: 4/4 verified + 1 human_needed).
- 5 operator-action items are runtime validations on real GLM API, NOT blocking design gaps. Per autonomous workflow convention, these are counted as `passed-with-operator-deferral`.
- Therefore expected verdict: `passed` (15/15 reqs verified with automated evidence; 5 operator-action items documented for runtime validation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-validate/56-CONTEXT.md

# Prior phase VERIFICATION.md files (the evidence base the audit walks)
@.planning/phases/52-infra-foundation/52-VERIFICATION.md
@.planning/phases/53-creative-slice/53-VERIFICATION.md
@.planning/phases/54-eval-harness-1/54-VERIFICATION.md
@.planning/phases/55-eval-harness-2/55-VERIFICATION.md

# Audit format references (mirror structure, do NOT copy verbatim — v11.0 is implementation milestone, not design-only)
@.planning/milestones/v10.0-MILESTONE-AUDIT.md
@.planning/milestones/v9.0-MILESTONE-AUDIT.md

# Operator-action smoke test drivers (already shipped in prior phases — referenced by the runbook)
@scripts/run_screenplay_step3_roundtable.py
@scripts/run_fitness_battery.py
@scripts/run_latency_benchmark.py
@scripts/run_bias_canary.py
@scripts/migrate_v6_feedback_to_memory_schema.py

<interfaces>
<!-- Audit-relevant data shapes already in the repo. Executor consumes, does NOT re-derive. -->

From .planning/phases/52-infra-foundation/52-VERIFICATION.md frontmatter:
```yaml
phase: 52-infra-foundation
status: passed          # 4/4 SCs PASS, 0 operator actions
score: 4/4 must-haves verified
```

From .planning/phases/53-creative-slice/53-VERIFICATION.md frontmatter:
```yaml
phase: 53-creative-slice
status: human_needed    # 2/3 SCs PASS + 1 operator action
score: 2/3 must-haves verified (SC#2 deferred to operator)
human_verification:
  - test: "Run scripts/run_screenplay_step3_roundtable.py --smoke with live GLM_API_KEY"
    expected: "Exit 0; <30s wall-clock; build/screenplay-step3-output.json with all 6 HOOK-09 fields"
```

From .planning/phases/54-eval-harness-1/54-VERIFICATION.md frontmatter:
```yaml
phase: 54-eval-harness-1
status: human_needed    # 3/3 SCs PASS at automated level + 3 operator actions
score: 3/3 must-haves verified
human_verification:
  - EVAL-01: scripts/run_fitness_battery.py with GLM_API_KEY → persona-aligned ≥0.7 vs generic ≤0.5
  - EVAL-02: scripts/run_latency_benchmark.py against live mem0 → p95 < 500ms on 500-record store
  - EVAL-03: scripts/run_bias_canary.py --smoke with real GLM → unsupported_claim fixture flagged
```

From .planning/phases/55-eval-harness-2/55-VERIFICATION.md frontmatter:
```yaml
phase: 55-eval-harness-2
status: human_needed    # 4/4 SCs PASS at automated level + 1 operator action
score: 4/4 must-haves verified
human_verification:
  - EVAL-04: compact_memory(agent_id, dry_run=False) with real GLM → 3-tier post-state + audit entry
```

REQUIREMENTS.md REQ-IDs (15 total — the audit MUST walk all 15):
INFRA-01, INFRA-02, INFRA-03, INFRA-04,
MIGR-01, CREATIVE-01, CREATIVE-02,
EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05, EVAL-06, EVAL-07,
VALIDATE-01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit script that walks REQUIREMENTS.md + 4 VERIFICATION.md files and emits coverage matrix</name>
  <files>scripts/run_milestone_audit.py</files>
  <action>
Create `scripts/run_milestone_audit.py` — a Python 3.11+ stdlib-only script (no third-party deps; follow CLAUDE.md conventions: `encoding="utf-8"` on every `open()`, double-quoted strings, `from __future__ import annotations` at top, keyword-only args for public functions, lazy imports if any).

The script automates the audit evidence walk:

1. Parse REQ-IDs from `.planning/REQUIREMENTS.md` traceability table (regex: `^\| (INFRA-0[1-4]|CREATIVE-0[1-2]|EVAL-0[1-7]|MIGR-01|VALIDATE-01) \|` — case-sensitive, line-anchored).

2. For each of the 4 prior phases (52, 53, 54, 55), parse `{phase}-VERIFICATION.md` frontmatter (PyYAML NOT available — write a tiny hand-rolled parser for the limited frontmatter shape we emit: `key: value` + indented list items under `human_verification:`). Extract:
   - `status` (one of `passed | human_needed | failed`)
   - `score` (e.g. "4/4 must-haves verified" — split on `/` to get numerator + denominator)
   - `human_verification` list (each entry has `test`, `expected`, `why_human`)

3. Cross-reference REQ-ID → Phase via REQUIREMENTS.md traceability table `Phase` column. Map each REQ to its phase's verification status.

4. Build coverage matrix: per-req row with `{req_id, phase, plan, req_status (Complete/Partial/Pending), verification_status (passed/human_needed/failed), evidence_pointer (path:line into VERIFICATION.md Requirements Coverage table), operator_action_count (0 or more)}`.

5. Emit JSON to stdout (and optional `--out <path>` to write a file under `.planning/research/v11-poc-eval/audit-matrix.json`). Shape:
```json
{
  "milestone": "v11.0",
  "audited_at": "<ISO8601>",
  "total_reqs": 15,
  "satisfied_reqs": <int>,
  "human_needed_reqs": <int>,
  "failed_reqs": <int>,
  "operator_action_count": <int>,
  "verdict_logic": {
    "all_15_have_automated_verification": <bool>,
    "operator_actions_documented": <bool>,
    "any_req_failed": <bool>,
    "recommended_verdict": "passed | tech_debt | gaps_found | fail"
  },
  "reqs": [
    {"req_id": "INFRA-01", "phase": "52", "plan": "52-01", "verification_status": "passed", "evidence_pointer": "52-VERIFICATION.md Requirements Coverage row INFRA-01", "operator_actions": []},
    {"req_id": "CREATIVE-01", "phase": "53", "plan": "53-03", "verification_status": "human_needed", "evidence_pointer": "53-VERIFICATION.md SC#2 + CREATIVE-01 row", "operator_actions": [{"test": "scripts/run_screenplay_step3_roundtable.py --smoke", "expected": "..."}]}
    // ... 13 more
  ]
}
```

Verdict logic (per CONTEXT.md verdict strategy):
- `passed` if all 15 reqs have verification_status ∈ {passed, human_needed} AND all human_needed items have documented operator-action runbook entries (i.e. NOT silently missing).
- `tech_debt` if any req has partial verification but no fundamental gap.
- `gaps_found` if any req lacks automated verification entirely.
- `fail` if any req fundamentally unmet (would require milestone rework).

CLI:
- `python3 scripts/run_milestone_audit.py` — print JSON to stdout, exit 0.
- `python3 scripts/run_milestone_audit.py --out .planning/research/v11-poc-eval/audit-matrix.json` — also write to file.
- `--help` flag with usage.
- Make script executable (`chmod 755`).

Add a module docstring documenting: input shape (REQUIREMENTS.md + 4 VERIFICATION.md files), output shape (JSON matrix), verdict logic, and operator-action handling convention.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; .venv/bin/python scripts/run_milestone_audit.py --out /tmp/v11-audit-matrix.json &amp;&amp; .venv/bin/python -c "import json,sys; d=json.load(open('/tmp/v11-audit-matrix.json')); assert d['total_reqs']==15, f'total_reqs={d[\"total_reqs\"]} expected 15'; assert d['satisfied_reqs']+d['human_needed_reqs']+d['failed_reqs']==15; assert d['verdict_logic']['recommended_verdict'] in {'passed','tech_debt','gaps_found','fail'}; assert len(d['reqs'])==15; assert all(r['req_id'] and r['phase'] and r['verification_status'] for r in d['reqs']); print('audit matrix OK:', d['satisfied_reqs'],'satisfied +',d['human_needed_reqs'],'human_needed =',d['operator_action_count'],'operator actions; verdict =',d['verdict_logic']['recommended_verdict'])"</automated>
  </verify>
  <done>
Script runs end-to-end, emits valid JSON with 15 req rows, recommended_verdict is `passed` (all 15 reqs have automated verification; 5 operator-action items aggregated).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author .planning/milestones/v11.0-MILESTONE-AUDIT.md with full audit (scorecard + 15-req traceability + operator runbook + verdict)</name>
  <files>.planning/milestones/v11.0-MILESTONE-AUDIT.md</files>
  <action>
Author the v11.0 milestone audit document. **Mirror the structure of `.planning/milestones/v10.0-MILESTONE-AUDIT.md` + `.planning/milestones/v9.0-MILESTONE-AUDIT.md`** — do NOT copy verbatim, because v11.0 is an implementation milestone (runtime code + agent YAML + tests + reports) whereas v10.0 was design-only. Adapt sections accordingly.

Frontmatter (YAML):
```yaml
---
milestone: v11.0
milestone_name: Hermes-Native Expert Agents PoC Implementation
audited: 2026-07-07T00:00:00Z   # adjust to actual run date
status: passed                   # or tech_debt — driven by Task 1 output
baseline_commit: <SHA>           # first commit touching .planning/phases/52-*
head_commit: <SHA>               # post-Phase-56 Task 2
scores:
  requirements: 15/15
  phases: 5/5                    # Phase 52-56
  plans: 15/15                   # 52(4) + 53(3) + 54(3) + 55(4) + 56(1)
  operator_action_handoffs: 5    # 1 from P53 + 3 from P54 + 1 from P55
  pitfalls_mitigated: 7/7        # P1/P2/P4/P5/P8/P10/P14 (carried from v10.0)
gaps:
  requirements: []
  integration: []
tech_debt:
  - phase: 53
    items:
      - "Test-isolation WARNING (test_phase52_contract.py::test_phase52_registry_loads_empty_dir cache pollution) — pre-existing, recommended conftest fixture reset; not blocking"
  - phase: 55
    items:
      - "1 flaky unrelated test (test_compression_concurrent_fork) — passes in isolation; mass-run timing-dependent; not Phase 55 concern"
nyquist:
  compliant_phases: [52, 53, 54, 55, 56]
  overall: compliant
design_only: false               # v11.0 is RUNTIME implementation (contrast v10.0 design_only: true)
runtime_implementation: true     # NEW field for v11.0 — code + agent YAML + tests shipped
---
```

Body sections (mirror v10.0/v9.0 structure):

**§0 Scorecard** — table with: Requirements (15/15), Phases (5/5), Plans (15/15), Operator-action handoffs (5), Pitfalls mitigated (7/7), Runtime tests (~280+ across `tests/agent/` + `tests/v11-*`), FOUND-08 (n/a — v11.0 touches no SKILL.md per design contract).

**§1 Phase Verification Summary** — one row per phase (52-56) with: name, plans, reqs, status, commit range, SUMMARY pointer. Status derived from VERIFICATION.md frontmatter (52=passed; 53/54/55=human_needed; 56=this audit).

**§2 Requirements Traceability (15/15)** — one row per REQ-ID with: req_id, phase, plan, status (✅ Complete or ⚠ Needs Human), verification source (file:section pointer into the relevant VERIFICATION.md Requirements Coverage row + 1-line evidence quote). For each req, cite the specific VERIFICATION.md row from the prior phase verifications — these are the third-party-reproducible evidence pointers. REQ → phase mapping per REQUIREMENTS.md traceability table.

**§3 Operator-Action Runbook (5 handoffs)** — THE KEY NEW SECTION for v11.0 (no analog in v10.0 design-only). Aggregate the 5 `human_verification` entries from Phase 53/54/55 VERIFICATION.md frontmatter into a single runbook. For each:

| # | Source phase | Source SC | Operator action | Command | Expected outcome | Why human |
| 1 | 53 | SC#2 | Real-GLM screenplay Step 3 smoke test | `python scripts/run_screenplay_step3_roundtable.py --smoke` (with GLM_API_KEY) | Exit 0; <30s wall-clock; build/screenplay-step3-output.json with all 6 HOOK-09 fields; state file status=completed + turns=[9] | Live GLM_API_KEY + 4-key rotation budget; executor sandbox lacks `openai` SDK + rate-limit budget |
| 2 | 54 | EVAL-01 | Live GLM fitness battery baseline | `python scripts/run_fitness_battery.py` with GLM credentials | Per-scenario scores; persona-aligned ≥0.7 vs generic ≤0.5; fitness_trend.jsonl entry | Live GLM_API_KEY required |
| 3 | 54 | EVAL-02 | Live mem0 latency p95 benchmark | `python scripts/run_latency_benchmark.py` with MEM0_API_KEY + 500-record store | p95 < 500ms SLO met; populate latency-baseline.md §2.2 | Live mem0 backend + MEM0_API_KEY required |
| 4 | 54 | EVAL-03 | Live GLM bias canary smoke | `python scripts/run_bias_canary.py --smoke` | Unsupported_claim fixture flagged by 4th LLM check; verdict remains pass | Live GLM_API_KEY required |
| 5 | 55 | EVAL-04 | Real-GLM compaction summary | `compact_memory(agent_id, dry_run=False, backend=<real mem0>)` | GLM call succeeds; summary coherent; audit entry appended; 3-tier post-state validates | GLM_API_KEY + live mem0 backend required |

Document that operator-action items are NOT gaps — they are runtime validations of code that is fully implemented and automated-test-verified. Per autonomous workflow convention, they count as `passed-with-operator-deferral`.

**§4 Risk Register Reconciliation (7 risks from STATE.md)** — for each of the 7 v11.0 risks (mem0 latency / GLM concurrency / curator hallucination / 9-YAML validity / state corruption / dry-run leaks / 5-phase granularity), cite the verification evidence that mitigates it. Cross-reference Phase 52-55 VERIFICATION.md sections. This is the analog of v10.0 §5 (pitfall mitigation audit).

**§5 Implementation Path Adherence (per `05-POC-PLAN.md` §6.1)** — verify the implementation sequence matches the design: fitness battery FIRST (Phase 54 EVAL-01 shipped before Phase 55) → schema migration dry-run SECOND (Phase 55 EVAL-07) → bias canary THIRD (Phase 54 EVAL-03, structurally parallel to fitness battery). Document with commit timestamps.

**§6 Vertical Slice Smoke Test Result** — pointer to `.planning/research/v11-poc-eval/smoke-test-report.md` (authored in Task 3). Summarize: automated-level smoke tests PASS (mocked-GLM lifecycle 12/12 + driver tests 25/25 + arbitration 10/10 = 47+ tests green); real-GLM smoke deferred to operator per `human_needed` block.

**§7 Verdict + Tech Debt** — final verdict section. Verdict logic:
- `passed` iff ALL of: §2 15/15 reqs verified at automated level; §3 5 operator-action items all documented with runbook entries (not silently missing); §4 risk mitigations all in place; no `failed` status in any VERIFICATION.md.
- `tech_debt` if any req has partial verification but no fundamental gap.
- `gaps_found` if any req lacks automated verification.
- `fail` if any req fundamentally unmet.

Expected verdict: `passed` with 5 documented operator-action handoffs. Cite the automated-test counts from Phase 52-55 VERIFICATION.md (Phase 52: 71 passed; Phase 53: 53 passed; Phase 54: 50 passed; Phase 55: 165 passed; Phase 56: this audit). State that operator-action items are runtime validations, not blocking design gaps.

Enumerate tech_debt items: (1) Phase 53 test-isolation WARNING (pre-existing cache pollution); (2) Phase 55 1 flaky unrelated test; (3) Phase 53+ v11.0 deferred items (15-expert full transform / per-agent memory benchmark full / Option B → 物理分区 migration / live production traffic / kais-hermes-skills repo edit / new repo / SKILL.md form deprecation / threshold tuning production data) — all documented in STATE.md Deferred Items table with explicit v12+ deferral rationale.

**§8 Next-Step Action Items** — (1) Operator (Kai): tag v11.0 milestone `git tag v11.0` once satisfied with audit + after running 5 operator-action smoke tests (or accepting deferral). (2) Future v12.0 milestone author: consume this audit §3 operator-action runbook as v12.0 validation input. (3) Mirror close-out pattern precedent: v9.0 P43, v10.0 P51, this audit.

**§9 References** — design docs (7 v10.0 docs + 3 schemas), research sources, Phase SUMMARYs (15: 52-01..04 + 53-01..03 + 54-01..03 + 55-01..04 + 56-01), REQUIREMENTS.md, ROADMAP.md, CLAUDE.md, prior milestone audits (v10.0 + v9.0 + v5.0 as precedent).

Length target: 300+ lines (comparable to v10.0 667 lines / v9.0 ~400 lines). Every claim cites a `(file, section/line)` evidence pointer per SC#5 third-party-reproducible standard.

**Hard constraints (per CLAUDE.md + CONTEXT.md):**
- Per D-1: file location MUST be `.planning/milestones/v11.0-MILESTONE-AUDIT.md`.
- Per D-3: verdict vocabulary MUST be `passed | tech_debt | gaps_found | fail`.
- Per D-4: MUST walk all 15 reqs (no shortcuts).
- Per D-5: MUST aggregate all 5 operator-action handoffs in §3.
- Per D-6: this is strictly the LAST phase — audit cites Phase 52-55 VERIFICATION files only, no speculation about future phases.
- Evidence pointers MUST be `(file, section/line)` tuples — third-party reproducible per SC#5 (cite v10.0 §10 methodology).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; test -f .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; LINES=$(wc -l &lt; .planning/milestones/v11.0-MILESTONE-AUDIT.md) &amp;&amp; [ "$LINES" -ge 300 ] &amp;&amp; echo "audit OK ($LINES lines)" &amp;&amp; for REQ in INFRA-01 INFRA-02 INFRA-03 INFRA-04 MIGR-01 CREATIVE-01 CREATIVE-02 EVAL-01 EVAL-02 EVAL-03 EVAL-04 EVAL-05 EVAL-06 EVAL-07 VALIDATE-01; do grep -q "\b${REQ}\b" .planning/milestones/v11.0-MILESTONE-AUDIT.md || { echo "MISSING: $REQ"; exit 1; }; done &amp;&amp; echo "all 15 reqs walked" &amp;&amp; grep -qE "verdict:\s*\`(passed|tech_debt|gaps_found|fail)\`" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; grep -q "Operator-Action Runbook" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; grep -q "run_screenplay_step3_roundtable.py --smoke" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; grep -q "run_fitness_battery.py" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; grep -q "run_latency_benchmark.py" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; grep -q "run_bias_canary.py" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; grep -q "compact_memory" .planning/milestones/v11.0-MILESTONE-AUDIT.md &amp;&amp; echo "all 5 operator actions cited"</automated>
  </verify>
  <done>
Audit file exists at the locked location, 300+ lines, walks all 15 reqs (each REQ-ID present as a whole-word match), verdict line present with one of the 4 allowed values, §3 Operator-Action Runbook cites all 5 operator-action commands.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author smoke-test-report.md template + populate automated baselines + mark human_needed sections</name>
  <files>.planning/research/v11-poc-eval/smoke-test-report.md</files>
  <action>
Author `.planning/research/v11-poc-eval/smoke-test-report.md` — the vertical-slice smoke test report (VALIDATE-01 deliverable per REQUIREMENTS.md line 176). This document is THE operator-facing runbook + the timestamped record of automated baselines.

Structure (per CONTEXT.md `Locked contracts` + VERIFICATION.md human_verification blocks):

**§1 Purpose + Scope** — what this report is: end-of-milestone smoke test aggregation for v11.0 PoC. Scope: automated baseline (this doc) + operator-action runtime validations (5 handoffs). Out of scope: live production traffic (v12+).

**§2 Automated Baseline Results (populated NOW)** — table summarizing the automated-test results that have already run in CI / executor sandbox. Populate from Phase 52-55 VERIFICATION.md Behavioral Spot-Checks + Requirements Coverage:

| Dimension | Source phase | Test count | Result | Evidence pointer |
| Phase 52 INFRA | 52 | 71 passed in 2.13s | PASS | 52-VERIFICATION.md Behavioral Spot-Checks row 1 |
| Phase 53 CREATIVE (mocked GLM) | 53 | 53 passed in 2.66s | PASS | 53-VERIFICATION.md Behavioral Spot-Checks row 12 |
| Phase 54 EVAL-HARNESS-1 | 54 | 50 passed in 2.13s | PASS | 54-VERIFICATION.md Behavioral Spot-Checks row 6 |
| Phase 55 EVAL-HARNESS-2 | 55 | 165 passed in 7.83s | PASS | 55-VERIFICATION.md Behavioral Spot-Checks row 1 |
| Migration dry-run probe | 55 | exit 0, 30 records mapped, 0 dropped | PASS | 55-VERIFICATION.md Probe Execution row 1 |
| Total automated | 52-55 | 339+ tests green | PASS | aggregate |

Also include: HOOK-09 schema fixture exists (`tests/fixtures/screenplay-step3-schema.json` with 6 required top-level fields); 9 agent YAMLs at `~/.hermes/agents/*.agent.yaml` all validate against Phase 45 `agents-schema.yaml` (Phase 53 SC#1 evidence); Round table lifecycle atomic against synthetic agent (Phase 52 SC#2 evidence); Serial-execution 429 rejection cites MEMORY.md (Phase 52 SC#4 evidence).

**§3 Operator-Action Smoke Tests (5 handoffs, human_needed)** — the runbook. For each, include:

1. **Real-GLM Screenplay Step 3 Round Table Smoke (Phase 53 SC#2)**
   - **Status:** `human_needed`
   - **Command:** `time python scripts/run_screenplay_step3_roundtable.py --storykernel tests/fixtures/storykernel-sample.json --output build/screenplay-step3-output.json --smoke`
   - **Pre-conditions:** `~/.hermes/.env` has `GLM_API_KEY`/`ZAI_API_KEY` for 4-key rotation; `cli-config.yaml` auxiliary block has `round_table_opinion.provider: glm` + `memory_comparator.provider: glm` (template at `cli-config.yaml.example` lines 487-501); 9 agent YAMLs at `~/.hermes/agents/*.agent.yaml` (present — verified); use main repo `.venv/bin/python` (NOT system python3).
   - **Expected:** Exit 0; total wall-clock < 30 seconds (10 LLM calls × ~2s each + overhead); `build/screenplay-step3-output.json` with all 6 HOOK-09 fields (logline / scene_breakdown w/ per-scene emotion_curve / hooks w/ ≥1 type ∈ [cold_open, curiosity, shock, cliffhanger, paywall] / payoffs / cliffhangers / top-level emotion_curve w/ arousal ∈ [0,1] + valence ∈ [-1,1]); state file `~/.hermes/agents/.runtime/screenplay-step3-poc/round_tables/{round_id}.json` has `status: "completed"` + `turns` array length 9.
   - **Why human:** 10 sequential real-GLM API calls require operator's live GLM_API_KEY + 4-key rotation budget. Mocked-GLM tests (12/12 PASS per Phase 53 VERIFICATION.md) prove lifecycle wiring but cannot prove real-GLM latency.
   - **Timestamp when run:** _(operator fills in)_
   - **Result:** _(operator fills in: PASS / FAIL / partial with details)_

2. **Live GLM Fitness Battery Baseline (Phase 54 EVAL-01)**
   - **Status:** `human_needed`
   - **Command:** `python scripts/run_fitness_battery.py --battery tests/v11-fitness-battery/scenarios --persona-sha256 <real-persona-sha256>`
   - **Pre-conditions:** `GLM_API_KEY` set; 8 scenario YAMLs in place (verified); screenplay.agent.yaml persona_sha256 referenced.
   - **Expected:** Per-scenario scores in [0,1]; persona-aligned screenplay agent scores 0.7+ vs generic LLM 0.4-0.5 (discrimination criterion per fitness-battery-spec.md §5); `fitness_trend.jsonl` entry appended at `~/.hermes/eval/fitness_trend.jsonl` as PoC baseline.
   - **Why human:** Live GLM dispatch via `auxiliary_client.call_llm` requires valid credentials. CI uses mocked judge returning 0.0 fallback per T-54-03 mitigation.
   - **Timestamp when run:** _(operator fills in)_
   - **Result:** _(operator fills in)_

3. **Live mem0 Latency p95 Benchmark (Phase 54 EVAL-02)**
   - **Status:** `human_needed`
   - **Command:** `python scripts/run_latency_benchmark.py --fixture 500 --out /tmp/v11-latency-live.json`
   - **Pre-conditions:** `MEM0_API_KEY` set; live mem0 backend reachable; 500-record store seeded (per `tests/v11-latency-bench/fixtures/seed_500_records.py` pattern, but against real mem0).
   - **Expected:** p95 < 500ms SLO met on 100 sequential `memory_retrieve_scoped` retrievals (excluding LLM call); baseline numbers populate `latency-baseline.md` §2.2 live-backend row; if p95 > 500ms, document 物理分区 trigger conditions per Phase 48 §3 + 06-CROSS-REPO-IMPACT.md §2.
   - **Why human:** Fixture-only benchmark is structurally sub-ms (in-memory list scan); authoritative SLO verdict requires live mem0 backend per Phase 54 SUMMARY §Next Phase Readiness.
   - **Timestamp when run:** _(operator fills in)_
   - **Result:** _(operator fills in)_

4. **Live GLM Bias Canary Smoke (Phase 54 EVAL-03)**
   - **Status:** `human_needed`
   - **Command:** `python scripts/run_bias_canary.py --fixtures tests/v11-bias-canary/fixtures/ --out /tmp/v11-canary-live.json --smoke`
   - **Pre-conditions:** `GLM_API_KEY` set; `auxiliary.bias_canary_claim_check.provider: glm` in cli-config.
   - **Expected:** LLM claim-support pass flags `bad_record_unsupported_claim.json` fixture (the 4th non-deterministic check); acceptance verdict remains pass (4-5 of 5 bad caught); audit chain entry appended.
   - **Why human:** Real GLM dispatch requires credentials. CI uses mocked LLM (returns `supported=True`); `--smoke` operator-action documented in CLI --help + cli-config.yaml.example.
   - **Timestamp when run:** _(operator fills in)_
   - **Result:** _(operator fills in)_

5. **Real-GLM Compaction Summary (Phase 55 EVAL-04)**
   - **Status:** `human_needed`
   - **Command:** Use the `scripts/run_with_real_glm.py` pattern OR invoke `compact_memory(agent_id="screenplay", dry_run=False, backend=<real mem0 backend>)` directly via a one-shot Python session against a 600+ record real memory store.
   - **Pre-conditions:** `GLM_API_KEY` set; live mem0 backend with 600+ records for `screenplay` agent; `auxiliary.memory_compaction.provider: glm` + `task: memory_compaction` in cli-config.
   - **Expected:** GLM call succeeds (not the deterministic fallback); single summary record in working-tier; all working-tier originals flipped to `status="superseded"`; all archival-tier originals flipped to `status="archived"`; `source_record_ids` chain populated; audit log entry appended with real `eval_score` payload; 3-tier post-state satisfies core ≤10 / working ≤100 / archival ≤10000.
   - **Why human:** 13 tests in `tests/v11-compaction/` use `mock_claim_check_llm` (canned responder). Live GLM behavior depends on GLM_API_KEY + `auxiliary.memory_compaction` task routing.
   - **Timestamp when run:** _(operator fills in)_
   - **Result:** _(operator fills in)_

**§4 Verdict Triage Table** — decision matrix for operator:

| Operator-action result | Impact on verdict |
| All 5 PASS | `passed` — v11.0 fully validated end-to-end |
| 4/5 PASS, 1 documented partial | `passed` with note (operator-action items are runtime validations, not design gates) |
| Any FAILS fundamentally (e.g. round table cannot complete on real GLM) | Re-classify as `tech_debt` or `gaps_found`; document remediation plan |

**§5 References** — pointer to v11.0-MILESTONE-AUDIT.md §3 (canonical runbook), Phase 53/54/55 VERIFICATION.md `human_verification` frontmatter (source of truth), REQUIREMENTS.md VALIDATE-01 deliverable spec.

**Hard constraints (per CONTEXT.md `Locked contracts` + CLAUDE.md):**
- Per D-2: file location MUST be `.planning/research/v11-poc-eval/smoke-test-report.md`.
- All 5 operator-action sections MUST be present with `Status: human_needed` markers.
- Automated baseline section (§2) MUST be populated NOW with the test counts already green (NOT left as TODO) — operator only fills the live-run sections.
- Each operator-action section MUST include the 6 fields: Status, Command, Pre-conditions, Expected, Why human, Timestamp when run, Result.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; test -f .planning/research/v11-poc-eval/smoke-test-report.md &amp;&amp; LINES=$(wc -l &lt; .planning/research/v11-poc-eval/smoke-test-report.md) &amp;&amp; [ "$LINES" -ge 150 ] &amp;&amp; echo "smoke-test-report OK ($LINES lines)" &amp;&amp; for CMD in "run_screenplay_step3_roundtable.py" "run_fitness_battery.py" "run_latency_benchmark.py" "run_bias_canary.py" "compact_memory"; do grep -q "$CMD" .planning/research/v11-poc-eval/smoke-test-report.md || { echo "MISSING CMD: $CMD"; exit 1; }; done &amp;&amp; echo "all 5 operator actions present" &amp;&amp; grep -c "human_needed" .planning/research/v11-poc-eval/smoke-test-report.md | awk '{ if ($1 &gt;= 5) { print "human_needed count OK:", $1 } else { print "FAIL: human_needed count:", $1, "(expected &gt;=5)"; exit 1 } }' &amp;&amp; grep -q "Automated Baseline Results" .planning/research/v11-poc-eval/smoke-test-report.md &amp;&amp; grep -qE "(71 passed|53 passed|50 passed|165 passed)" .planning/research/v11-poc-eval/smoke-test-report.md &amp;&amp; echo "automated baselines populated"</automated>
  </verify>
  <done>
Smoke test report exists at the locked location, 150+ lines, contains all 5 operator-action commands, has ≥5 `human_needed` markers, §2 Automated Baseline populated with the actual test counts from Phase 52-55 verifications.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update REQUIREMENTS.md traceability table Status column for all 15 reqs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update the traceability table at `.planning/REQUIREMENTS.md` lines 182-199 — flip the `Status` column for all 15 reqs from `Pending` to reflect the Phase 56 audit outcome. Use Edit (NOT Write — preserve all other content).

Per Phase 52-55 VERIFICATION.md outcomes + Task 1 audit matrix:

| REQ-ID | Phase | New Status | Justification |
| INFRA-01 | 52 | ✅ Complete | 52-VERIFICATION.md passed; `agent/registry_loader.py` + 15 tests |
| INFRA-02 | 52 | ✅ Complete | 52-VERIFICATION.md passed; 7 MCP tools wired + 21 integration tests |
| INFRA-03 | 52 | ✅ Complete | 52-VERIFICATION.md passed; `agent/round_table_state.py` + 17 tests, 3 crash-recovery modes |
| INFRA-04 | 52 | ✅ Complete | 52-VERIFICATION.md passed; `agent/round_table_executor.py` + 6 async tests + MEMORY.md citation |
| MIGR-01 | 53 | ✅ Complete | 53-VERIFICATION.md SC#1 PASS; 9 YAMLs validate against agents-schema |
| CREATIVE-01 | 53 | ⚠ Complete (operator smoke deferred) | 53-VERIFICATION.md SC#2 human_needed; mocked-GLM 12/12 PASS, real-GLM smoke deferred |
| CREATIVE-02 | 53 | ✅ Complete | 53-VERIFICATION.md SC#3 PASS; 5-mechanism arbitration + 10 tests |
| EVAL-01 | 54 | ⚠ Complete (operator baseline deferred) | 54-VERIFICATION.md EVAL-01 human_needed; spec + 8 YAMLs + runner shipped, live baseline deferred |
| EVAL-02 | 54 | ⚠ Complete (operator benchmark deferred) | 54-VERIFICATION.md EVAL-02 human_needed; timing instrumentation + 3 fixtures + CLI shipped, live mem0 benchmark deferred |
| EVAL-03 | 54 | ⚠ Complete (operator smoke deferred) | 54-VERIFICATION.md EVAL-03 human_needed; curator_bias_canary.py + 6 fixtures shipped, live GLM --smoke deferred |
| EVAL-04 | 55 | ⚠ Complete (operator smoke deferred) | 55-VERIFICATION.md EVAL-04 human_needed; memory_compaction.py + 13 tests shipped, live GLM smoke deferred |
| EVAL-05 | 55 | ✅ Complete | 55-VERIFICATION.md EVAL-05 PASS; threshold-tuning.md + agents-schema §2.6.1 additive block |
| EVAL-06 | 55 | ✅ Complete | 55-VERIFICATION.md EVAL-06 PASS; curator default dry_run=True + AST-walk non-bypassable test |
| EVAL-07 | 55 | ✅ Complete | 55-VERIFICATION.md EVAL-07 PASS; migrate_v6_feedback_to_memory_schema.py + 52 tests + zero silent drops |
| VALIDATE-01 | 56 | ✅ Complete (this audit) | Phase 56 audit (this milestone close-out) |

Also update:
- The `*Last updated:*` footer at the bottom of REQUIREMENTS.md (line 226) to reflect Phase 56 audit date + Phase 56 completion.
- Add a 1-line note above the traceability table documenting that ⚠ markers are operator-action handoffs (NOT gaps) — point to `.planning/research/v11-poc-eval/smoke-test-report.md` §3 for the runbook.

Do NOT touch any other section of REQUIREMENTS.md (the Goal / Scope / Predecessors / per-req deliverable spec / Out of Scope sections are immutable post-v11.0 planning).

**Hard constraint:** This is a surgical Edit. Preserve the table structure exactly (column alignment, separator rows). Only the `Status` column values change.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; grep -c "Pending" .planning/REQUIREMENTS.md | awk '{ if ($1 == 0) { print "OK: 0 Pending rows (all 15 reqs updated)" } else { print "FAIL: still has", $1, "Pending rows"; exit 1 } }' &amp;&amp; for REQ in INFRA-01 INFRA-02 INFRA-03 INFRA-04 MIGR-01 CREATIVE-01 CREATIVE-02 EVAL-01 EVAL-02 EVAL-03 EVAL-04 EVAL-05 EVAL-06 EVAL-07 VALIDATE-01; do grep -qE "^\| ${REQ} \|" .planning/REQUIREMENTS.md || { echo "MISSING ROW: $REQ"; exit 1; }; done &amp;&amp; echo "all 15 req rows present" &amp;&amp; grep -qE "(Complete|operator)" .planning/REQUIREMENTS.md &amp;&amp; grep -q "smoke-test-report.md" .planning/REQUIREMENTS.md &amp;&amp; echo "operator-action note + runbook pointer present"</automated>
  </verify>
  <done>
REQUIREMENTS.md traceability table has 0 `Pending` rows, all 15 req IDs present with updated Status, 1-line note pointing to smoke-test-report.md for operator-action runbook.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ↔ audit script | `scripts/run_milestone_audit.py` reads planning files; if attacker can write to `.planning/`, audit output is compromised |
| audit doc ↔ reader (Kai / future milestone author) | Audit verdict drives `git tag v11.0` decision; false claims in audit = mis-tagged milestone |
| operator-action runbook ↔ operator | Runbook commands (`scripts/run_*.py`) execute with operator's env (GLM_API_KEY etc.); malicious command injection in runbook = supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | `scripts/run_milestone_audit.py` parsing of VERIFICATION.md frontmatter | mitigate | Hand-rolled YAML frontmatter parser must reject malformed input gracefully (try/except per file, log + skip, never crash the whole audit); emit `parse_error` field in JSON for any VERIFICATION.md it cannot parse |
| T-56-02 | Information Disclosure | Audit doc cites file paths + line numbers | accept | All cited paths are within repo `.planning/`; no secrets, credentials, or PII in planning files (CLAUDE.md `## Anti-Patterns to Avoid` covers `.env` discipline) |
| T-56-03 | Tampering | Operator-action runbook commands (5 handoffs) | mitigate | Runbook commands cite scripts already shipped + reviewed in Phase 52-55 (NOT new code in Phase 56). Each command includes explicit pre-conditions. No new packages installed. Scripts run with operator's existing venv (no `npx`/`pip install` mid-runbook). |
| T-56-04 | Repudiation | Audit verdict logic (`passed` recommendation) | mitigate | Verdict logic is explicit in script docstring + audit §7; every req row cites `(file, section/line)` evidence pointer per SC#5 third-party-reproducible standard (mirror v10.0 §10 methodology) |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | Phase 56 introduces ZERO new package dependencies — `run_milestone_audit.py` is Python 3.11+ stdlib-only per CLAUDE.md; no `npm install`, `pip install`, or `cargo add` in any task. Supply-chain legitimacy gate N/A. |
</threat_model>

<verification>
## Phase-Level Checks

1. **Audit script runs:** `python3 scripts/run_milestone_audit.py --out /tmp/v11-audit-matrix.json` produces valid JSON with 15 req rows + recommended_verdict.
2. **Audit doc exists + walks 15 reqs:** `.planning/milestones/v11.0-MILESTONE-AUDIT.md` 300+ lines, all 15 REQ-IDs present as whole-word matches, verdict line with allowed vocabulary.
3. **Smoke test report exists + 5 operator actions:** `.planning/research/v11-poc-eval/smoke-test-report.md` 150+ lines, all 5 commands cited, ≥5 `human_needed` markers, automated baseline populated.
4. **REQUIREMENTS.md traceability updated:** 0 `Pending` rows; all 15 req IDs present with updated Status; operator-action note + runbook pointer present.
5. **v11.0 milestone deliverable form:** Per PROJECT.md + CLAUDE.md, v11.0 is runtime implementation — Phase 56 adds 1 audit script + 3 docs + 1 table update; ZERO SKILL.md / plugin / Hermes-runtime edits (FOUND-08 structurally satisfied by absence, same as v10.0 but for different reason: v10.0 was design-only, v11.0 Phase 56 is close-out-documentation-only).
</verification>

<success_criteria>
**SC#1 (VALIDATE-01 — the only SC for Phase 56 per ROADMAP):**

`.planning/milestones/v11.0-MILESTONE-AUDIT.md` exists and verifies **15/15** requirements satisfied (INFRA-01..04 + CREATIVE-01..02 + EVAL-01..07 + MIGR-01 + VALIDATE-01), with per-req evidence pointers to deliverables (file paths + commit SHAs).

`.planning/research/v11-poc-eval/smoke-test-report.md` exists documenting the vertical slice end-to-end run on a real GLM API call (no mocks) — OR documenting that the real-GLM smoke is operator-action (5 handoffs) with automated baselines populated and operator-action sections marked `human_needed`.

Audit verdict is `passed` or `tech_debt` (not `failed`); any `tech_debt` items are documented with v12.0+ deferral rationale + operator-action-handoff notes (analog to v9.0 close-out operator handoffs).

**Measurable completion:** All 4 tasks' `<verify>` blocks PASS; verdict is `passed` (expected per Phase 52-55 automated verifications).
</success_criteria>

<output>
Create `.planning/phases/56-validate/56-01-SUMMARY.md` when done.
</output>

---
phase: 02-expert-hook-commercial-engine
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - skills/movie-experts/hook_retention/SKILL.md
  - skills/movie-experts/screenplay/SKILL.md
  - skills/movie-experts/editor/SKILL.md
  - skills/movie-experts/compliance_marketing/SKILL.md
  - skills/movie-experts/_eval/prompts/hook_retention.yaml
autonomous: true
requirements: [HOOK-01, HOOK-06, HOOK-07, HOOK-08, HOOK-09]
user_setup: []

must_haves:
  truths:
    - "hook_retention/SKILL.md exists with bilingual content (EN YAML + EN H2 + CN prose)"
    - "SKILL.md body has 5 爆款公式 branches (### 抖音-男频 / ### 抖音-女频 / ### 快手-草根 / ### 小程序剧-长集数 / ### 通用 fallback) each with 5-field schema (核心动机 / 情感曲线 / 节奏密度 / 付费卡点位置 / 典型案例)"
    - "SKILL.md documents 钩子 / 爽点 / 卡点 JSON marker schema (type, timestamp, intensity 1-5, setup_callback, payoff_callback) per CONTEXT D-6"
    - "SKILL.md body uses provider-agnostic RAG invocation (no fact_store / mem0_search / cosyvoice_api hardcode)"
    - "3 existing experts (screenplay, editor, compliance_marketing) list hook_retention in related_skills"
    - "hook_retention's related_skills array lists screenplay, editor, compliance_marketing, composer"
    - "5 eval prompts runnable via _eval/runner.py --expert hook_retention --dry-run"
    - "verify_skill_references.py exits 0 against the new SKILL.md + 4 modified files"
  artifacts:
    - path: "skills/movie-experts/hook_retention/SKILL.md"
      provides: "Bilingual hook_retention expert skill with 5 爆款公式 branches + marker schema"
      min_lines: 220
      contains: "expert_id: hook_retention"
    - path: "skills/movie-experts/_eval/prompts/hook_retention.yaml"
      provides: "5 benchmark prompts covering hook design / 卡点 placement / pacing"
      min_lines: 70
  key_links:
    - from: "hook_retention/SKILL.md"
      to: "references/three-second-hooks.md + conflict-escalation.md + paywall-design.md + vertical-pacing.md"
      via: "## References table at top (FOUND-07 contract)"
      pattern: "References.*\\|"
    - from: "screenplay/SKILL.md"
      to: "hook_retention"
      via: "related_skills bidirectional edge (HOOK ↔ screenplay for hook rewrites)"
      pattern: "hook_retention"
    - from: "editor/SKILL.md"
      to: "hook_retention"
      via: "related_skills bidirectional edge (HOOK ↔ editor for retention pacing)"
      pattern: "hook_retention"
    - from: "compliance_marketing/SKILL.md"
      to: "hook_retention"
      via: "Phase 1 listed hook_retention one-directionally; Phase 2 closes bidirectional contract"
      pattern: "hook_retention"
    - from: "_eval/prompts/hook_retention.yaml"
      to: "_eval/runner.py"
      via: "load_prompts() — expert_id + prompts[] schema"
      pattern: "expert_id: hook_retention"
---

<objective>
Author the hook_retention SKILL.md (bilingual body, 5 爆款公式 branches, 钩子/爽点/卡点 marker schema, provider-agnostic RAG), wire bidirectional related_skills edges to 3 existing experts (screenplay / editor / compliance_marketing — closing Phase 1 contract) + one-directional edge to composer (BGM sync), and publish 5 diverse eval prompts. The composer edge is one-directional (HOOK → composer for BGM sync requirements; composer does not need to know about HOOK placement).

Purpose: This is the integration capstone of Phase 2 — refs exist (02-01, 02-02), now the SKILL.md surfaces them to the agent, wires the expert into the collaboration graph (closing the Phase 1 contract with compliance_marketing), and defines the marker schema that Phase 3 screenplay refactor will consume for emotion_curve integration.

Output:
- `skills/movie-experts/hook_retention/SKILL.md` (≥220 lines, bilingual)
- 3 existing SKILL.md files with `hook_retention` appended to related_skills (screenplay, editor, compliance_marketing — the last formalizes the bidirectional edge already one-directionally listed in Phase 1)
- `skills/movie-experts/_eval/prompts/hook_retention.yaml` (5 scenarios)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expert-hook-commercial-engine/02-CONTEXT.md
@.planning/phases/02-expert-hook-commercial-engine/02-01-SUMMARY.md
@.planning/phases/02-expert-hook-commercial-engine/02-02-SUMMARY.md
@skills/movie-experts/_shared/SKILL-LAYOUT.md
@skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
@skills/movie-experts/_shared/glossary.md
@skills/movie-experts/screenplay/SKILL.md
@skills/movie-experts/editor/SKILL.md
@skills/movie-experts/compliance_marketing/SKILL.md
@skills/movie-experts/composer/SKILL.md
@skills/movie-experts/_eval/prompts/animator_demo.yaml

<interfaces>
<!-- Frontmatter schema (from SKILL-LAYOUT.md — mandatory fields). -->

```yaml
---
name: hook_retention                # MUST match directory name
description: "<EN+CN one-line summary>"
version: 1.0.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
prerequisites:
  tools: [hermes_llm]
metadata:
  hermes:
    tags: [movie, hook, retention, 3-second-hooks, 付费卡点, paywall-cliffhanger, 爆款公式, viral-formula, vertical-pacing, 钩子, 爽点, 卡点]
    related_skills: [screenplay, editor, compliance_marketing, composer]
    expert_id: hook_retention
    metrics: [hook_strength, 完播率_proxy, 卡点_density, 转发_trigger_coverage]
---
```

<!-- Body section order (Phase 1 13-section canonical order, adapted for HOOK). -->

1. `# Hook & Retention Expert (钩子与留存专家)` — bilingual H1
2. `## When to use this skill` — trigger conditions
3. `## References` — table with Ref / When to Read / Contents columns; lists all 4 refs
4. `## Role & Philosophy` — 3-5 bullets
5. `## Core Capabilities` — bullet list
6. `## Output Format` — JSON artifact filenames this expert produces (hook_design.json / 卡点_placement.json / 钩子_爽点_卡点_markers.json)
7. `## Marker Schema` — the 钩子 / 爽点 / 卡点 JSON schema for screenplay.emotion_curve integration
8. `## Key Parameters` — H3 subsections per parameter group (hook_strength_target / 卡点_density / 节奏_density)
9. `## Per-Platform 爆款公式 Branching` — 5 H3 subsections per CONTEXT D-6
10. `## Hook Design Workflow` — numbered steps cross-linking three-second-hooks.md §taxonomy + §5-tier scoring
11. `## 卡点 Placement Workflow` — numbered steps cross-linking paywall-design.md + conflict-escalation.md
12. `## RAG Invocation` — provider-agnostic pattern from _shared/RAG-INVOCATION-PATTERN.md
13. `## Quality Thresholds` — markdown table
14. `## Collaboration` — <- upstream / -> downstream arrows
15. `## What NOT to do` — anti-patterns

<!-- Per-Platform 爆款公式 fixed 5-field schema (CONTEXT D-6). -->

Each of 5 branches MUST have these 5 fields:
- **核心动机:** what drives viewer engagement on this platform-audience
- **情感曲线:** emotional arc shape (e.g., 线性上升 / 阶梯式 / V 型反转)
- **节奏密度:** cut density recommendation (cross-link vertical-pacing.md §Multi-Platform Pacing Variation)
- **付费卡点位置:** 卡点 placement strategy (cross-link paywall-design.md)
- **典型案例:** ≥ 2 concrete composite examples (CONTEXT specifics)

5 branches (fixed):
- 抖音-男频 (action / power / revenge themes; faster cuts; harder 卡点)
- 抖音-女频 (romance / family / emotional themes; softer hooks; emotional 卡点)
- 快手-草根 (slice-of-life / relatable; grassroots aesthetic; community-driven hooks)
- 小程序剧-长集数 (longer episodes 3-5 min; serialized 卡点 across episodes)
- 通用 fallback (when platform unknown)

<!-- Marker schema (CONTEXT D-6 — fixed JSON shape, do not alter). -->

```json
{
  "type": "钩子" | "爽点" | "卡点",
  "timestamp": "MM:SS",
  "intensity": 1-5,
  "setup_callback": "what earlier scene set this up",
  "payoff_callback": "what later scene resolves this"
}
```

Multi-episode callback support (CONTEXT specifics): `setup_callback` may reference prior episode (e.g., "S1E03 02:15 — protagonist swore revenge"); `payoff_callback` may reference future episode (e.g., "S1E07 — antagonist reveals true identity").

<!-- Eval prompt file schema (from animator_demo.yaml — load_prompts() interface). -->

```yaml
---
expert_id: hook_retention
# Comment block explaining scenario coverage
prompts:
  - id: hook-001
    text: |
      <scenario>
  ...
```

<!-- Existing related_skills arrays to be APPENDED to (Phase 1 pattern — do not remove existing entries). -->

screenplay/SKILL.md current (inline array): `[style_genome, scene_builder, editor, performer, composer, compliance_marketing]`
  → append: `, hook_retention`

editor/SKILL.md current (inline array): `[screenplay, animator, composer, voicer, continuity, mixer, compliance_marketing]`
  → append: `, hook_retention`

compliance_marketing/SKILL.md current (BLOCK-LIST YAML format):
```yaml
    related_skills:
      - screenplay
      - editor
      - hook_retention  # Phase 2 — pending (directory does not yet exist; edge documented at line 183)
      - style_genome
      - drawer
```
  → replace the "Phase 2 — pending" comment with "Phase 2 — bidirectional edge closed" (entry already exists; only update comment + remove the "line 183" dangling reference if present). Do NOT reorder the array.

composer/SKILL.md: ONE-WAY edge only (HOOK → composer for BGM sync). Do NOT modify composer/SKILL.md — composer does not need to know about HOOK placement. HOOK's own related_skills array lists composer; composer's related_skills array is unchanged (preserves Phase 1 contract stability — composer lists [screenplay, editor, style_genome, mixer, foley, spatial_audio]).

<!-- Scanner contract (Phase 0). -->

`scripts/verify_skill_references.py` greps every model/tool name in SKILL.md against `_shared/known-external-models.yaml`. hook_retention SKILL.md body MUST NOT introduce new model names (use `<video_gen_primary>` etc. provider-agnostic tokens per RAG-INVOCATION-PATTERN.md — Phase 1 CR-03 lesson).

<!-- Numeric consistency contract (Phase 1 CR-01 lesson). -->

All numbers in SKILL.md body (3-5 卡点 / 1.5x pace / ≤3s dead air / 70-80% 爽点 / 30s setup / 6-9 击中点 / 5 triggers / 5 branches) MUST appear identically in the corresponding refs (02-01 and 02-02). SKILL.md is a SUMMARY pointing to refs as canonical source — do NOT re-document the rationale, only cite the number + cross-link.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author hook_retention/SKILL.md (bilingual + 5 爆款公式 branches + marker schema + RAG)</name>
  <files>skills/movie-experts/hook_retention/SKILL.md</files>
  <read_first>
    - skills/movie-experts/screenplay/SKILL.md (reference for body section order + bilingual style + how existing expert structures per-platform branching)
    - skills/movie-experts/compliance_marketing/SKILL.md (Phase 1 — same per-platform branching pattern + provider-agnostic RAG block style)
    - skills/movie-experts/_shared/SKILL-LAYOUT.md (frontmatter schema + body section canonical order)
    - skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md (provider-agnostic tokens — must NOT hardcode fact_store / mem0_search / cosyvoice_api — Phase 1 CR-03 lesson)
    - skills/movie-experts/hook_retention/references/three-second-hooks.md (02-01)
    - skills/movie-experts/hook_retention/references/conflict-escalation.md (02-01)
    - skills/movie-experts/hook_retention/references/paywall-design.md (02-02)
    - skills/movie-experts/hook_retention/references/vertical-pacing.md (02-02)
    - .planning/phases/02-expert-hook-commercial-engine/02-CONTEXT.md (decisions D-5 爆款公式, D-6 marker schema, D-7 related_skills, specifics — ≥2 典型案例 per branch, ≥2 prompts with 🟡 risk elements)
  </read_first>
  <action>
Author `skills/movie-experts/hook_retention/SKILL.md` (≥220 lines). Follow the 15-section body order from `<interfaces>` exactly.

**Frontmatter** (exact fields):
- `name: hook_retention`
- `description: "Hook & Retention Expert: 3-second hook design + 付费卡点 placement + per-platform 爆款公式 branching + 钩子/爽点/卡点 marker schema for cinematically correct AND commercially viable 短剧 content."`
- `version: 1.0.0`
- `author: Hermes Agent`
- `license: MIT`
- `platforms: [linux, macos, windows]`
- `prerequisites.tools: [hermes_llm]`
- `metadata.hermes.tags: [movie, hook, retention, 3-second-hooks, paywall-cliffhanger, viral-formula, vertical-pacing, marker-schema]`
- `metadata.hermes.related_skills: [screenplay, editor, compliance_marketing, composer]` (exactly these 4 per CONTEXT D-7)
- `metadata.hermes.expert_id: hook_retention`
- `metadata.hermes.metrics: [hook_strength, 完播率_proxy, 卡点_density, 转发_trigger_coverage]`

**`## References` table** (FOUND-07 contract — appears immediately after `## When to use this skill`):

| Ref | When to Read | Contents |
|-----|--------------|----------|
| `references/three-second-hooks.md` | Before any 3-second hook design | 5-type taxonomy × 3 examples × frame-by-frame + 5-tier scoring |
| `references/conflict-escalation.md` | Before designing 阶梯式 escalation | 5-rung ladder + 击中点/爽点 density |
| `references/paywall-design.md` | Before 付费卡点 placement | 3-5 卡点 density + 3-tier strength + 完播率 rules + 5 转发 triggers |
| `references/vertical-pacing.md` | Before 竖屏 pacing decisions | 竖屏 cut density + BGM sync + 字幕 design |

**`## Marker Schema`** — the 钩子 / 爽点 / 卡点 JSON schema per CONTEXT D-6 (load-bearing contract for Phase 3 screenplay.emotion_curve integration). Show the JSON shape exactly as documented in `<interfaces>`. Add 3 concrete examples (1 per marker type) demonstrating multi-episode callback support (setup_callback references prior episode; payoff_callback references future episode per CONTEXT specifics).

**`## Per-Platform 爆款公式 Branching`** (CONTEXT D-5 + D-6): Five H3 subsections in fixed order: `### 抖音-男频` / `### 抖音-女频` / `### 快手-草根` / `### 小程序剧-长集数` / `### 通用 fallback`. Each subsection MUST have the 5-field schema per CONTEXT D-6 (核心动机 / 情感曲线 / 节奏密度 / 付费卡点位置 / 典型案例). Each 典型案例 MUST include ≥ 2 concrete composite examples per CONTEXT specifics. Cross-link:
- `节奏密度` field → `vertical-pacing.md §Multi-Platform Pacing Variation`
- `付费卡点位置` field → `paywall-design.md §付费卡点 Density Rules`
- `情感曲线` field → `conflict-escalation.md §The 阶梯式 Escalation Ladder`
- `核心动机` + `典型案例` may cross-link `../../compliance_marketing/references/viral-element-catalog.md` for 爆款 element taxonomy + risk badges

**`## Hook Design Workflow`**: 5-step numbered procedure cross-linking `three-second-hooks.md`:
1. Identify hook type from 5-type taxonomy (§Taxonomy)
2. Design 0-1s / 1-2s / 2-3s frame-by-frame per §3-Second Frame Structure
3. Score hook strength using §5-Tier Strength Scoring
4. If score ≤ ⚠️: iterate or pre-roll with stronger hook
5. Output 钩子 marker (per §Marker Schema) for screenplay.emotion_curve integration

**`## 卡点 Placement Workflow`**: 5-step procedure cross-linking `paywall-design.md` + `conflict-escalation.md`:
1. Identify episode runtime + platform branch
2. Apply density rule (3-5 卡点 per 10 ep min, hard 卡点 at every paid episode end)
3. Place soft 卡点 mid-episode at ~40-60% runtime
4. Score each 卡点 strength using §3-Tier Strength Scoring (target 🟢 for hard, 🟡 acceptable for soft)
5. Output 卡点 marker (per §Marker Schema) — `payoff_callback` MUST point to specific future episode/scene

**`## RAG Invocation`**: Provider-agnostic pattern per `_shared/RAG-INVOCATION-PATTERN.md`. Use `<video_gen_primary>` / `<audio_gen_primary>` placeholders — NEVER hardcode tool names. Conditional phrasing: "If memory plugin available, retrieve X; otherwise inline from references/*.md" — Phase 1 CR-03 lesson: no fact_store / mem0_search / cosyvoice_api tokens in body.

**`## Quality Thresholds`** table: 4 metrics × 2 columns (Metric / Target):
- `hook_strength`: ≥ ✅ strong (4/5 tier) for opening hook; ≥ ⚠️ weak acceptable for secondary
- `完播率_proxy`: 1.5x pace rule observed; ≤3s dead air max; BGM sync alignment ≥ 90% *estimated*
- `卡点_density`: 3-5 per 10 ep min; 100% of paid episodes end with 🟢 hard 卡点
- `转发_trigger_coverage`: ≥ 2 of 5 trigger categories activated per episode

**`## Collaboration`**: Upstream arrows (`<- screenplay` for scene-list / emotion_curve context, `<- compliance_marketing` for 付费机制 risk constraints, `<- editor` for cut_density feedback) + downstream arrows (`-> screenplay` for 钩子 marker injection, `-> editor` for cut_density requirements, `-> composer` for BGM sync requirements — one-directional, composer does not call back, `-> compliance_marketing` for 卡点 risk validation).

**`## What NOT to do`**: bullets covering (1) hardcoding provider-specific tool names (fact_store / mem0_search / cosyvoice_api), (2) placing 卡点 without hard 卡点 at paid episode end, (3) scoring all hooks as 🎯 without honest critique, (4) duplicating Phase 1 付费机制 regulatory numbers (cross-link platform-spec refs instead — numerical consistency contract), (5) introducing new model names in body (use placeholders).

CN prose throughout body; EN YAML / EN H2 / EN H3 headers only. Single source of truth rule: every numeric threshold (3-5, 1.5x, ≤3s, 70-80%, 30s, 6-9, 5 triggers, 5 branches) cites the canonical ref (02-01 / 02-02) rather than re-documenting rationale.
  </action>
  <verify>
    <automated>F="skills/movie-experts/hook_retention/SKILL.md"; test -f "$F" && LINES=$(wc -l < "$F") && [ "$LINES" -ge 220 ] && echo "LINES=$LINES" && grep -q "^name: hook_retention$" "$F" && grep -q "expert_id: hook_retention" "$F" && grep -q "related_skills: \[screenplay, editor, compliance_marketing, composer\]" "$F" && for s in "## When to use this skill" "## References" "## Role & Philosophy" "## Core Capabilities" "## Output Format" "## Marker Schema" "## Key Parameters" "## Per-Platform 爆款公式 Branching" "## Hook Design Workflow" "## 卡点 Placement Workflow" "## RAG Invocation" "## Quality Thresholds" "## Collaboration" "## What NOT to do"; do grep -q "^$s$" "$F" || { echo "MISSING $s"; exit 1; }; done && for h in "### 抖音-男频" "### 抖音-女频" "### 快手-草根" "### 小程序剧-长集数" "### 通用 fallback"; do grep -q "^$h$" "$F" || { echo "MISSING branch $h"; exit 1; }; done && for field in "核心动机" "情感曲线" "节奏密度" "付费卡点位置" "典型案例"; do C=$(grep -c "$field" "$F"); [ "$C" -ge 5 ] || { echo "Expected ≥5 instances of $field, got $C"; exit 1; }; done && for marker in "钩子" "爽点" "卡点"; do grep -q "\"type\": \"$marker\"" "$F" || { echo "MISSING marker type $marker in JSON example"; exit 1; }; done && for k in "timestamp" "intensity" "setup_callback" "payoff_callback"; do grep -q "$k" "$F" || { echo "MISSING marker field $k"; exit 1; }; done && grep -q "references/three-second-hooks.md" "$F" && grep -q "references/conflict-escalation.md" "$F" && grep -q "references/paywall-design.md" "$F" && grep -q "references/vertical-pacing.md" "$F" && grep -qE "3-5.*卡点|卡点.*3-5" "$F" && grep -q "1.5x" "$F" && grep -qE "≤3s|≤ 3s" "$F" && ! grep -q "fact_store\|mem0_search\|cosyvoice_api" "$F" && echo "PASS LINES=$LINES"</automated>
  </verify>
  <done>
    - SKILL.md exists, ≥220 lines
    - Frontmatter contains exact name, expert_id, related_skills (4 entries: screenplay, editor, compliance_marketing, composer), tags, metrics
    - All 14 canonical body sections present in correct order
    - Marker Schema section has JSON shape (type / timestamp / intensity / setup_callback / payoff_callback) with 3 concrete examples
    - 5 爆款公式 branches present, each with 5-field schema (核心动机 / 情感曲线 / 节奏密度 / 付费卡点位置 / 典型案例)
    - References table lists all 4 ref files
    - Numeric thresholds (3-5, 1.5x, ≤3s) present and consistent with refs
    - No hardcoded provider tool names (no fact_store / mem0_search / cosyvoice_api)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire bidirectional related_skills edges + scanner exit 0</name>
  <files>skills/movie-experts/screenplay/SKILL.md, skills/movie-experts/editor/SKILL.md, skills/movie-experts/compliance_marketing/SKILL.md</files>
  <read_first>
    - skills/movie-experts/screenplay/SKILL.md (frontmatter line 13 — current related_skills array)
    - skills/movie-experts/editor/SKILL.md (frontmatter line 13)
    - skills/movie-experts/compliance_marketing/SKILL.md (frontmatter lines 13-18 — block-list YAML format with existing hook_retention entry)
    - scripts/verify_skill_references.py (scanner — will lint new SKILL.md + 3 modified files)
    - .planning/phases/02-expert-hook-commercial-engine/02-CONTEXT.md (decision D-7 — 4 edges; composer one-directional)
  </read_first>
  <action>
For 3 existing experts (screenplay, editor, compliance_marketing), wire bidirectional edges with hook_retention. Each edit is APPEND-only — do NOT remove existing entries, do NOT reorder, do NOT modify `expert_id` (FOUND-08 frozen rule — Phase 1 contract).

**screenplay/SKILL.md** (inline array format — append at end of array):
- Current line 13: `    related_skills: [style_genome, scene_builder, editor, performer, composer, compliance_marketing]`
- Target: `    related_skills: [style_genome, scene_builder, editor, performer, composer, compliance_marketing, hook_retention]`

**editor/SKILL.md** (inline array format — append at end of array):
- Current line 13: `    related_skills: [screenplay, animator, composer, voicer, continuity, mixer, compliance_marketing]`
- Target: `    related_skills: [screenplay, animator, composer, voicer, continuity, mixer, compliance_marketing, hook_retention]`

**compliance_marketing/SKILL.md** (block-list YAML format — Phase 1 listed hook_retention one-directionally with a "pending" comment):
- Current block (lines 13-18):
  ```yaml
      related_skills:
        - screenplay
        - editor
        - hook_retention  # Phase 2 — pending (directory does not yet exist; edge documented at line 183)
        - style_genome
        - drawer
  ```
- Target (replace the comment only — entry already exists):
  ```yaml
      related_skills:
        - screenplay
        - editor
        - hook_retention  # Phase 2 — bidirectional edge closed (HOOK now lists compliance_marketing)
        - style_genome
        - drawer
  ```
  Do NOT reorder the block. Only the comment changes.

**composer/SKILL.md**: NOT modified. The composer edge is one-directional (HOOK lists composer in its related_skills; composer does not need to list HOOK). This preserves Phase 1 contract stability and matches CONTEXT D-7 (composer is → one-way for BGM sync).

After edits, run `python scripts/verify_skill_references.py --strict` against all modified files + the new hook_retention/SKILL.md. Must exit 0 (no phantom model references, no broken refs).

If scanner flags anything: investigate, do not suppress. Common cause: SKILL.md body uses a model name not in `_shared/known-external-models.yaml` allowlist (Phase 1 CR-03 lesson). Fix by replacing with `<video_gen_primary>` / `<image_gen_primary>` / `<audio_gen_primary>` placeholders per RAG-INVOCATION-PATTERN.md.
  </action>
  <verify>
    <automated>for f in screenplay editor compliance_marketing; do grep -q "hook_retention" "skills/movie-experts/$f/SKILL.md" || { echo "$f missing hook_retention edge"; exit 1; }; done && grep -q "expert_id: screenplay" skills/movie-experts/screenplay/SKILL.md && grep -q "expert_id: editor" skills/movie-experts/editor/SKILL.md && grep -q "expert_id: compliance_marketing" skills/movie-experts/compliance_marketing/SKILL.md && grep -q "Phase 2 — bidirectional edge closed" skills/movie-experts/compliance_marketing/SKILL.md && ! grep -q "Phase 2 — pending" skills/movie-experts/compliance_marketing/SKILL.md && ! grep -q "hook_retention" skills/movie-experts/composer/SKILL.md && python scripts/verify_skill_references.py --strict 2>&1 | tail -5 && echo "SCANNER_EXIT=$?"</automated>
  </verify>
  <done>
    - screenplay + editor have `hook_retention` appended to related_skills inline array (alphabetical / positional ordering preserved per Phase 1 pattern)
    - compliance_marketing's existing `hook_retention` block-list entry updated (comment changed from "Phase 2 — pending" to "Phase 2 — bidirectional edge closed")
    - composer/SKILL.md UNCHANGED (one-directional edge preserved)
    - No existing related_skills entries removed or reordered
    - All expert_id values unchanged (FOUND-08)
    - `verify_skill_references.py --strict` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Publish 5 diverse eval prompts + dry-run validation</name>
  <files>skills/movie-experts/_eval/prompts/hook_retention.yaml</files>
  <read_first>
    - skills/movie-experts/_eval/prompts/animator_demo.yaml (schema template — `expert_id:` + `prompts: [{id, text}]`)
    - skills/movie-experts/_eval/runner.py (load_prompts() interface — reads `expert_id` + `prompts[]`; runs each prompt in position-swap)
    - .planning/phases/02-expert-hook-commercial-engine/02-CONTEXT.md (decisions D-5 — 5 爆款公式 branches; D-6 — marker schema; specifics — ≥2 prompts with 🟡 risk elements)
    - skills/movie-experts/hook_retention/SKILL.md (just authored — prompts must exercise capabilities the SKILL documents)
  </read_first>
  <action>
Author `_eval/prompts/hook_retention.yaml` with frontmatter `expert_id: hook_retention` and exactly 5 prompts in `prompts:` array. Each prompt has `id` (hook-001..hook-005) and `text` (multi-line YAML block scalar).

The 5 prompts MUST cover the 5 爆款公式 branches per CONTEXT D-5 (1 per branch) AND exercise HOOK's core capabilities per HOOK-08. At least 2 prompts must involve 🟡 risk-level 爆款 elements per CONTEXT specifics (cross-link Phase 1 viral-element-catalog.md risk badges).

1. **hook-001 — 抖音-男频 revenge 短剧 hook + 卡点 design**: Scenario describes a 30-episode 抖音 男频 revenge 短剧 (protagonist returns after betrayal). Ask the expert to: (a) design the 3-second opening hook (choose type from 5-type taxonomy, frame-by-frame 0-1s/1-2s/2-3s, score using 5-tier rubric), (b) place 卡点 across first 10 episodes (3-5 density, hard 卡点 at end of paid episodes, score with 3-tier strength), (c) output 钩子 + 卡点 markers per JSON schema with multi-episode callback. Must cite specific numbers (3-5 / 1.5x / ≤3s).

2. **hook-002 — 抖音-女频 romance 短剧 with 🟡 risk-level 爆款 element**: Scenario describes a 抖音 女频 romance involving a 替身 的 爱 trope (🟡 borderline per Phase 1 catalog — may risk §3 色情低俗 if 肢体接触 implied). Ask the expert to: (a) design a softer emotional hook, (b) cross-reference viral-element-catalog.md for the 替身 的 爱 entry's risk badge, (c) apply 降级方案 if 🟡, (d) place 卡点 preserving ≥70% hook strength, (e) output 爽点 marker showing the payoff. Must involve 🟡 risk exercise per CONTEXT specifics.

3. **hook-003 — 快手-草根 slice-of-life 短剧 with 转发 trigger design**: Scenario describes a 快手 草根 短剧 about a migrant worker's family reunion. Ask the expert to: (a) design a grassroots-aesthetic hook (avoid polished 抖音 style), (b) target specific 转发 triggers (至少 2 of 5 categories), (c) place soft 卡点 appropriate to 快手 audience (less aggressive paywall), (d) explain pacing differences from 抖音 (slower cut density per vertical-pacing.md). Must reference 快手 specific platform rules from Phase 1 platform-specs-kuaishou.md.

4. **hook-004 — 小程序剧 multi-episode 卡点 strategy with serialized callbacks**: Scenario describes a 微信小程序剧 12-episode 拟人化动画 漫剧 with 付费机制. Ask the expert to: (a) design a serialized 卡点 strategy across all 12 episodes (hard 卡点 at every paid episode end), (b) demonstrate multi-episode callback: setup_callback references S1E03, payoff_callback references S1E07, (c) ensure 备案 trigger cross-reference (cross-link Phase 1 platform-specs-miniprogram.md), (d) handle longer 3-5 min episode format (different pacing than 抖音 90s).

5. **hook-005 — 通用 fallback with 🟡 violence-borderline element**: Scenario describes an unknown-platform 短剧 with a borderline-violence confrontation scene (🟡 per Phase 1 catalog — §2 暴力血腥 borderline). Ask the expert to: (a) apply 通用 fallback 爆款公式, (b) design hook that activates 反转冲击 转发 trigger without showing explicit violence, (c) cross-reference viral-element-catalog.md 冲突钩 entry for risk badge + 降级方案, (d) place 卡点 preserving emotional stakes without 红线 violation, (e) verify compliance_marketing collaboration edge (the expert should flag the 🟡 element for compliance_marketing review). Must involve 🟡 risk exercise per CONTEXT specifics.

Each prompt's `text` block should be 8-15 lines (similar density to animator_demo.yaml prompts). Include enough context that a baseline (no-refs) candidate would fail on specifics — e.g., reference specific numeric thresholds (3-5 / 1.5x / ≤3s), specific marker schema fields, specific Phase 1 catalog entries with risk badges.

Then run dry-run validation:
```
python skills/movie-experts/_eval/runner.py \
    --expert hook_retention \
    --dry-run \
    --output-json skills/movie-experts/_eval/reports/hook_retention_dryrun.json \
    --output-md skills/movie-experts/_eval/reports/hook_retention_dryrun.md
```

The dry-run must complete without error and produce 5 verdicts (one per prompt × 1 ablation pair in Phase 0 skeleton config). Report files are committed as shape-evidence per Phase 0 SC #3 convention.
  </action>
  <verify>
    <automated>F="skills/movie-experts/_eval/prompts/hook_retention.yaml"; test -f "$F" && LINES=$(wc -l < "$F") && [ "$LINES" -ge 70 ] && grep -q "^expert_id: hook_retention$" "$F" && for id in hook-001 hook-002 hook-003 hook-004 hook-005; do grep -q "^  - id: $id$" "$F" || { echo "MISSING $id"; exit 1; }; done && PCOUNT=$(grep -c "^  - id: hook-" "$F") && [ "$PCOUNT" = "5" ] && for term in "3-5" "1.5x" "≤3s" "钩子" "爽点" "卡点" "setup_callback" "payoff_callback"; do grep -q "$term" "$F" || { echo "MISSING term $term"; exit 1; }; done && for risk_term in "🟡" "降级方案" "viral-element-catalog"; do grep -q "$risk_term" "$F" || { echo "MISSING risk term $risk_term"; exit 1; }; done && python skills/movie-experts/_eval/runner.py --expert hook_retention --dry-run --output-json /tmp/hook_dryrun.json --output-md /tmp/hook_dryrun.md 2>&1 | tail -5 && test -s /tmp/hook_dryrun.json && echo "PASS LINES=$LINES PROMPTS=$PCOUNT"</automated>
  </verify>
  <done>
    - hook_retention.yaml exists, ≥70 lines
    - Frontmatter has expert_id: hook_retention
    - Exactly 5 prompts with IDs hook-001..hook-005 matching CONTEXT D-5 branch scenarios (抖音-男频 / 抖音-女频 / 快手-草根 / 小程序剧 / 通用 fallback)
    - At least 2 prompts exercise 🟡 risk-level elements (hook-002 explicit 替身 + hook-005 explicit violence-borderline)
    - Each prompt references specific numbers (3-5 / 1.5x / ≤3s) + marker schema fields + Phase 1 catalog cross-link
    - runner.py --dry-run completes without error
    - Dry-run report files exist at non-zero size
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing experts → hook_retention edge | Appending to related_skills changes DAG ordering; must not break existing expert_id (FOUND-08 frozen) |
| SKILL.md body → agent prompt | Body is injected as user message; provider-agnostic tokens must NOT leak provider-specific tool names |
| Marker schema → Phase 3 screenplay.emotion_curve | Schema is a load-bearing contract; Phase 3 screenplay refactor will consume it natively |
| Eval prompts → runner.py | Prompts must conform to load_prompts() schema; malformed YAML breaks the harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | related_skills array edits on 3 existing experts | mitigate | APPEND-only for screenplay/editor; comment-only update for compliance_marketing; FOUND-08 frozen rule enforced; scanner verifies expert_id unchanged |
| T-02-13 | Information Disclosure | SKILL.md body model names | mitigate | Provider-agnostic tokens (`<video_gen_primary>` etc.); verify_skill_references.py exits 0; no fact_store / mem0_search / cosyvoice_api hardcode (Phase 1 CR-03 lesson) |
| T-02-14 | Tampering | Marker schema contract stability | mitigate | JSON shape documented exactly per CONTEXT D-6; 3 examples demonstrate field usage; Phase 3 screenplay refactor will consume without modification |
| T-02-15 | Tampering | Numeric consistency (3-5 / 1.5x / ≤3s) across SKILL.md + refs | mitigate | SKILL.md cites canonical refs (02-01 / 02-02); acceptance criteria grep enforces presence; no redefinition of rationale in body (Phase 1 CR-01 lesson) |
| T-02-16 | Elevation of Privilege | 卡点 Placement Workflow 🟡 bypass | mitigate | Workflow step 4 mandates 🟢 target for hard 卡点; 🟡 only acceptable for soft mid-episode 卡点; no silent accept path |
| T-02-17 | Repudiation | Phase 1 contract closure (compliance_marketing ↔ hook_retention) | mitigate | compliance_marketing's existing block-list entry comment updated from "Phase 2 — pending" to "Phase 2 — bidirectional edge closed"; documented change is auditable |
| T-02-18 | Tampering | Eval prompt YAML schema | mitigate | load_prompts() parser validates `expert_id` + `prompts[]` shape; dry-run exercises the harness before commit |
| T-02-19 | Elevation of Privilege | Eval prompts with 🟡 risk-level elements | mitigate | hook-002 + hook-005 explicitly exercise 🟡 — the prompts must demonstrate HOOK's compliance-aware design (cross-link Phase 1 viral-element-catalog.md risk badges + 降级方案) |
| T-02-SC | Tampering | (none — no packages installed) | accept | Pure markdown artifact; no supply-chain risk |
</threat_model>

<verification>
- hook_retention/SKILL.md exists with bilingual content (EN YAML + EN H2 + CN prose)
- 14 canonical body sections present in correct order
- 5 爆款公式 branches present (抖音-男频 / 抖音-女频 / 快手-草根 / 小程序剧-长集数 / 通用 fallback), each with 5-field schema
- Marker Schema section has JSON shape (type / timestamp / intensity / setup_callback / payoff_callback) with 3 concrete examples
- References table lists all 4 ref files
- No hardcoded provider tool names (scanner exits 0)
- 3 existing experts have hook_retention in related_skills (no existing entries removed/reordered; expert_id unchanged)
- compliance_marketing's block-list comment updated from "Phase 2 — pending" to "Phase 2 — bidirectional edge closed"
- composer/SKILL.md UNCHANGED (one-directional edge preserved per CONTEXT D-7)
- 5 eval prompts with IDs hook-001..hook-005 matching CONTEXT D-5 branch scenarios
- runner.py --dry-run completes successfully
- At least 2 prompts exercise 🟡 risk-level elements
</verification>

<success_criteria>
- HOOK-01 ✓ (hook_retention/SKILL.md bilingual)
- HOOK-06 ✓ (Per-platform 爆款公式 branching in SKILL.md — 5 branches)
- HOOK-07 ✓ (Bidirectional edges ↔ screenplay + ↔ editor; formalized ↔ compliance_marketing closing Phase 1 contract; one-directional → composer)
- HOOK-08 ✓ (5 eval prompts covering hook design / 卡点 placement / pacing, runnable through _eval/runner.py)
- HOOK-09 ✓ (钩子/爽点/卡点 marker schema documented for screenplay.emotion_curve integration)
- GOAL SC-1 ✓ (SKILL.md exists with bilingual content)
- GOAL SC-3 ✓ (Per-platform 爆款公式 branching documented)
- GOAL SC-4 ✓ (Bidirectional related_skills edges)
- GOAL SC-5 ✓ (Output schema markers documented)
</success_criteria>

<output>
Create `.planning/phases/02-expert-hook-commercial-engine/02-03-SUMMARY.md` when done
</output>

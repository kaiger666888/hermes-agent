---
phase: 34-skills-migration-coding-agent-tmux-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/autonomous-ai-agents/coding-agent/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-03, SKILL-04]
user_setup: []

must_haves:
  truths:
    - "User invoking `/skill coding-agent` (or natural-language trigger) from hermes-agent gets the unified delegation skill loaded into context"
    - "Skill documents 4 working delegation targets: Codex / Claude Code / Pi / OpenCode with tmux launch commands"
    - "Skill frontmatter is schema-indistinguishable from existing native hermes skills (claude-code, codex, opencode)"
    - "No openclaw-specific runtime dependencies remain (no `metadata.openclaw.requires.*`, no `openclaw message send`, no `oc-` session prefix, no `~/.openclaw` paths, no ACP thread-bound references)"
  artifacts:
    - path: "skills/autonomous-ai-agents/coding-agent/SKILL.md"
      provides: "Unified tmux-backed delegation skill for Codex/Claude Code/Pi/OpenCode"
      contains: "name: coding-agent"
  key_links:
    - from: "skills/autonomous-ai-agents/coding-agent/SKILL.md metadata.hermes.related_skills"
      to: "existing 4 autonomous-ai-agents skills"
      via: "YAML list [claude-code, codex, opencode, hermes-agent, tmux-agents]"
      pattern: "related_skills:\\s*\\[.*claude-code"
---

<objective>
Migrate the `coding-agent` skill from `~/.openclaw/skills/coding-agent/SKILL.md` to `skills/autonomous-ai-agents/coding-agent/SKILL.md` in the hermes-agent repo, adapting frontmatter to native hermes schema and rewriting openclaw-specific runtime references to hermes-agent equivalents.

Purpose: Establishes the unified tmux-backed delegation layer (4 targets: Codex/Claude Code/Pi/OpenCode) as a discoverable hermes skill that supplements (does not replace) the 4 existing single-agent skills. Addresses SKILL-01 (migration + discoverability), SKILL-03 (frontmatter schema compliance), and SKILL-04 (prerequisite format mapping).

Output: One new SKILL.md at `skills/autonomous-ai-agents/coding-agent/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md
@/data/workspace/hermes-agent/CLAUDE.md

<interfaces>
<!-- Native hermes SKILL.md frontmatter schema (from claude-code/SKILL.md + opencode/SKILL.md + codex/SKILL.md). -->
<!-- Executor uses this exact shape — no exploration needed. -->

```yaml
---
name: <skill-name>                      # required, lowercase-hyphenated
description: "<one-line EN description>"
version: <semver>                       # e.g. 1.0.0
author: <author>                        # e.g. "Hermes Agent" or "Hermes Agent + <upstream>"
license: MIT                            # all 4 existing skills use MIT
platforms: [linux, macos, windows]      # all 4 existing skills declare this list
metadata:
  hermes:
    tags: [<Capitalized-Hyphenated-Tags>]         # e.g. [Coding-Agent, Claude, Anthropic]
    related_skills: [<other-skill-name>, ...]     # bidirectional graph; bare skill `name`s
---
```

Body conventions (from existing 4 skills):
- H1 title: `# <Display Name>`
- `## Prerequisites` section as prose with `**Install:**` / `**Auth:**` style labels (NOT YAML requires)
- `## When to use` (optional but typical)
- Code blocks show Hermes invocation pattern: `terminal(command="...", workdir="...", pty=true)` or tmux commands
- Closing `## Rules` section with numbered hard rules

<!-- Native hermes terminal-tool invocation pattern (from claude-code/SKILL.md) -->
<!-- coding-agent source uses `bash command:"..."` syntax which must become `terminal(command="...")` -->

Openclaw-specific tokens that MUST be rewritten (count from source: 7 occurrences):
- `metadata.openclaw.requires.{anyBins,config}` YAML block → drop entirely (no hermes equivalent to `config`; `anyBins` → prose `**Install:**` under `## Prerequisites`)
- `openclaw message send --channel ... --target ... --message ...` → "platform-appropriate completion message via the active gateway delivery channel (Telegram / Discord / Slack / etc.) — capture channel + target from the originating session"
- `oc-<short-task-name>-<random4>` session prefix → `hermes-<short-task-name>-<random4>` (per CONTEXT.md session-naming decision)
- `~/.openclaw` paths → `~/.hermes` (or genericize)
- `~/Projects/openclaw` exclusion → `~/Projects/hermes-agent` (or genericize to "the hermes-agent repo itself")
- `ACP thread-bound work` exclusion → drop the clause entirely (ACP out of v7.0 scope per CONTEXT.md deferred)
- `openclaw-worker-prompt` mktemp template name → `hermes-worker-prompt`
- `OPENCLAW_STATE_DIR` → drop the reference
</interfaces>

<decisions_honored>
- **D (Coexistence: SUPPLEMENT)** — coding-agent supplements (does not replace) the 4 existing single-agent skills. The 4 skills remain authoritative for single-agent deep-dives; coding-agent is the unified delegation layer. This plan only CREATES the new skill; Plan 34-03 wires the bidirectional `related_skills` graph and writes COEXISTENCE.md.
- **D (Frontmatter mapping)** — `metadata.openclaw.requires.anyBins: [claude, codex, opencode, pi]` → `## Prerequisites` prose listing the 4 binaries with install hints (cross-referencing the 4 existing skills). `metadata.openclaw.requires.config: ["skills.entries.coding-agent.enabled"]` → DROP (openclaw-internal config key, no hermes equivalent).
- **D (Notification block)** — `openclaw message send` has no hermes equivalent CLI. Genericize to: "the worker must send exactly one completion/failure message via the platform-appropriate delivery channel captured from the originating gateway session (Telegram/Discord/Slack/etc.)". Keep the notification block SHAPE (channel / target / account / reply_to / thread_id) since it's the contract surface, but drop the openclaw-specific send command.
- **D (Session naming)** — rename `oc-<task>-<rand4>` to `hermes-<task>-<rand4>` for hermes-agent consistency.
- **D (Drop ACP references)** — ACP not in v7.0 scope; remove "ACP thread-bound work" from the "Do not use for" line.
- **D (Drop _meta.json)** — openclaw pattern, no hermes equivalent. SKILL.md only.
</decisions_honored>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coding-agent SKILL.md with adapted frontmatter and rewritten body</name>
  <files>skills/autonomous-ai-agents/coding-agent/SKILL.md</files>
  <read_first>
    - /home/kai/.openclaw/skills/coding-agent/SKILL.md (SOURCE — full read, 154 lines)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/claude-code/SKILL.md (REFERENCE — frontmatter schema + body conventions; first 50 lines sufficient for schema)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/opencode/SKILL.md (REFERENCE — shorter example of same schema, full read)
    - /data/workspace/hermes-agent/.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md (decisions to honor)
    - /data/workspace/hermes-agent/CLAUDE.md (Skill File Conventions subsection)
  </read_first>
  <behavior>
    - Frontmatter parses as valid YAML with name=coding-agent, description present, version, author, license=MIT, platforms=[linux,macos,windows], metadata.hermes.tags non-empty, metadata.hermes.related_skills including [claude-code, codex, opencode, hermes-agent, tmux-agents]
    - Body documents 4 working delegation targets (Codex, Claude Code, Pi, OpenCode) with tmux launch commands
    - No openclaw-runtime strings remain (no `metadata.openclaw`, no `openclaw message send`, no `oc-` session prefix, no `~/.openclaw`, no `ACP thread`, no `OPENCLAW_STATE_DIR`)
    - Notification block section preserves the shape (channel/target/account/reply_to/thread_id) but uses genericized send instruction
  </behavior>
  <action>
    Create `skills/autonomous-ai-agents/coding-agent/SKILL.md` by adapting the source openclaw file.

    **Frontmatter (exact shape — adapt values from source):**
    - `name: coding-agent`
    - `description:` — keep source description but prefix with "Delegate" wording; current source: "Delegate coding work to Codex, Claude Code, OpenCode, or Pi as background tmux workers; not simple edits or read-only code lookup." Keep essentially as-is (it's already hermes-tone).
    - `version: 1.0.0` (source had no version; pick 1.0.0 as first hermes release)
    - `author: "Hermes Agent + openclaw upstream"` (credit both; matches hermes convention of "Hermes Agent + Teknium" style)
    - `license: MIT`
    - `platforms: [linux, macos, windows]`
    - `metadata.hermes.tags:` — derive from source: `[Coding-Agent, Delegation, tmux, Codex, Claude-Code, OpenCode, Pi, Background-Worker]` (capitalized-hyphenated to match existing skills' style like `[Coding-Agent, Claude, Anthropic]`)
    - `metadata.hermes.related_skills:` — `[claude-code, codex, opencode, hermes-agent, tmux-agents]` (bidirectional graph; tmux-agents is the sibling being migrated in Plan 34-02)
    - DO NOT include `expert_id` or `metrics` — those are movie-experts conventions (CLAUDE.md "Skill File Conventions" notes they're optional; coding-agent is delegation infrastructure, not a movie expert). DO NOT include `metadata.openclaw.*` block — dropped per decision.

    **Body structure (preserve source sectioning, rewrite content per decisions):**

    1. `# Coding Agent (tmux mode)` — preserve H1
    2. Opening paragraph: keep verbatim minus the ACP and openclaw-state-dir exclusions. Drop "ACP thread-bound work" clause and the "or any run inside `~/.openclaw`, `$OPENCLAW_STATE_DIR`, or active OpenClaw state dirs" clause. Result: "Use for background feature builds, PR reviews, large refactors, and issue-to-PR loops. Do not use for simple edits or read-only code lookup." Keep the "persistent tmux sessions" paragraph as-is.
    3. `## Hard rules` — preserve numbered/bulleted structure. Apply these replacements:
       - `oc-<short-task-name>-<random4>` → `hermes-<short-task-name>-<random4>`
       - Drop the line "Worker must send completion/failure via `openclaw message send`." and replace with: "Worker must send exactly one completion or failure message via the platform-appropriate delivery channel captured at spawn time (e.g. the active Telegram/Discord/Slack gateway channel)."
       - Drop "Never checkout branches or run background coding agents in `~/Projects/openclaw`; use an isolated checkout." → rewrite as: "Never checkout branches or run background coding agents inside the hermes-agent repo itself (`~/Projects/hermes-agent` or wherever the repo lives); use an isolated checkout."
    4. `## Notification block` — preserve the block SHAPE (channel/target/account/reply_to/thread_id placeholders). Replace the `openclaw message send --channel ... --target ... --message ...` invocation with a genericized instruction: "When finished, send exactly one completion or failure message via the active gateway delivery channel (Telegram / Discord / Slack / etc.). Use channel=<channel>, target='<target>', and the brief result text. Add account/reply-to/thread-id only when present above." Drop the "Do not use openclaw system event or heartbeat" line — rewrite as "Do not rely on heartbeat, system events, or notify-on-exit hooks."
    5. `## Prerequisites` (NEW section — add AFTER `## Hard rules`, BEFORE `## Launch forms`). Source had prerequisites encoded as `metadata.openclaw.requires.{anyBins,config}` YAML; per decisions, convert to prose. Content:
       - `**tmux:** installed (apt/brew install tmux). Background sessions depend on it.`
       - `**At least one of** the four coding-agent CLIs:`
         - `claude` — see the `claude-code` skill for install + auth
         - `codex` — see the `codex` skill
         - `opencode` — see the `opencode` skill
         - `pi` — install per upstream Pi docs
       - `**Hermes gateway delivery channel active** (for completion notifications) — e.g. Telegram/Discord/Slack session reachable from the running hermes-agent.`
       - DO NOT migrate `metadata.openclaw.requires.config: ["skills.entries.coding-agent.enabled"]` — this is openclaw-internal config; no hermes equivalent; drop silently.
    6. `## Launch forms (tmux)` — preserve all 4 code blocks (Claude Code / Codex / OpenCode / Pi). Apply:
       - All `bash command:"..."` wrappers → `terminal(command="...", pty=true)` format (per native hermes invocation pattern from claude-code/SKILL.md). Keep the tmux new-session command content but wrap with `terminal(command="...")`.
       - `mktemp -t openclaw-worker-prompt.XXXXXX` → `mktemp -t hermes-worker-prompt.XXXXXX`
       - `SESSION="oc-$(echo $TASK..."` line → `SESSION="hermes-$(echo $TASK..."`
       - `SESSION="oc-scratch-..."` (in scratch section) → `SESSION="hermes-scratch-..."`
       - The heredoc `cat >"$PROMPT" <<'EOF'` blocks: keep verbatim (they're shell, not openclaw-specific).
    7. `## Monitoring commands` — preserve all tmux commands, wrap each `bash command:"..."` → `terminal(command="...", pty=true)`. The status.sh reference at bottom: `~/.openclaw/skills/openclaw-skills-tmux-agents/scripts/status.sh` → DROP this line (scripts not migrated per CONTEXT.md deferred decision; Plan 34-02 documents the script reference inside tmux-agents body but coding-agent shouldn't reference an openclaw path). Replace with: "For an overview of all agent sessions, see the `tmux-agents` skill."
    8. `## Long issue-to-PR work` — preserve verbatim (no openclaw strings).
    9. `## Scratch work (no existing repo)` — apply the `oc-scratch-` → `hermes-scratch-` rename and the `openclaw-worker-prompt` → `hermes-worker-prompt` rename.
    10. `## Status to user` — preserve verbatim (no openclaw strings).

    **Add a closing `## Coexistence with other autonomous-ai-agents skills` section** (1 short paragraph):
    "This skill is the **unified delegation layer** for spawning background coding workers in tmux. For single-agent deep-dives (Claude Code / Codex / OpenCode standalone with full surface-area docs), invoke the dedicated skill (`claude-code`, `codex`, `opencode`, or `hermes-agent`) directly. For tmux session lifecycle management (spawn/list/attach/status across multiple agents), use the sibling `tmux-agents` skill."

    **Do NOT** modify any of the 4 existing skills in this plan — Plan 34-03 handles bidirectional `related_skills` updates.

    **File hygiene:** UTF-8 encoding (per CLAUDE.md "Anti-Patterns to Avoid"). Double-quote YAML description string. End file with single trailing newline.
  </action>
  <verify>
    <automated>test -f /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md &amp;&amp; python3 -c "import yaml; d=yaml.safe_load(open('/data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md','r',encoding='utf-8').read().split('---',2)[1]); assert d['name']=='coding-agent'; assert d['license']=='MIT'; assert 'linux' in d['platforms']; assert 'claude-code' in d['metadata']['hermes']['related_skills']; assert 'tmux-agents' in d['metadata']['hermes']['related_skills']; assert d['metadata']['hermes']['tags']; print('FRONTMATTER_OK')"</automated>
    <automated>! grep -E "metadata\.openclaw|openclaw message send|~/.openclaw|OPENCLAW_STATE_DIR|ACP thread|oc-scratch|oc-\$(" /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md</automated>
    <automated>grep -c "hermes-worker-prompt\|hermes-\$(echo\|hermes-scratch-" /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md | grep -q "[1-9]"</automated>
    <automated>grep -c "^## Prerequisites\|^## Hard rules\|^## Launch forms\|^## Coexistence" /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md | grep -q "4"</automated>
    <automated>grep -c "Codex:\|Claude Code:\|OpenCode:\|Pi:" /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md | grep -q "4"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `skills/autonomous-ai-agents/coding-agent/SKILL.md` (relative to repo root)
    - Frontmatter parses as valid YAML (verified by python yaml.safe_load)
    - Frontmatter contains: name=coding-agent, description (non-empty string), version, author, license=MIT, platforms=[linux,macos,windows], metadata.hermes.tags (non-empty list), metadata.hermes.related_skills (list containing all 5: claude-code, codex, opencode, hermes-agent, tmux-agents)
    - `grep -E "metadata\.openclaw|openclaw message send|~/.openclaw|OPENCLAW_STATE_DIR|ACP thread" SKILL.md` returns 0 matches
    - `grep -E "\boc-(scratch|\$)" SKILL.md` returns 0 matches (session prefix fully renamed)
    - All 4 delegation targets (Codex / Claude Code / Pi / OpenCode) appear in launch-forms code blocks
    - File includes a `## Prerequisites` section listing tmux + the 4 CLIs (no YAML requires block)
    - File includes a `## Coexistence with other autonomous-ai-agents skills` section
    - File is valid UTF-8 with single trailing newline
    - No existing files modified (this is a net-new file under a new directory)
  </acceptance_criteria>
  <done>
    `coding-agent` skill is fully migrated to hermes-agent repo with native frontmatter schema, all 7 openclaw-runtime strings rewritten to hermes equivalents, 4 delegation targets documented, prerequisites converted to prose form. Ready for Plan 34-03 to wire bidirectional related_skills on the existing 4 skills.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill-content → agent-runtime | SKILL.md body is injected as a user-class message into LLM context; malicious or malformed content here reaches the agent's prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | SKILL.md frontmatter YAML | mitigate | yaml.safe_load verify gate in `<verify>` rejects malformed frontmatter |
| T-34-02 | Information Disclosure | Worker notification block templates | accept | Source skill already documents notification shape; no new disclosure surface introduced (migration, not new feature) |
| T-34-03 | Elevation of Privilege | tmux delegation commands in body | accept | The 4 existing skills (claude-code/codex/opencode/hermes-agent) already ship analogous tmux command patterns; this skill adds no new privilege surface beyond what the category already contains |

No package installs in this plan (markdown-only migration) — supply-chain threats N/A; no T-34-SC entry.
</threat_model>

<verification>
Phase-level checks for this plan (subset of overall Phase 34 verification — Plan 34-03 does the cross-plan coexistence verification):

1. `coding-agent/SKILL.md` exists and parses
2. Frontmatter matches native hermes schema (no `metadata.openclaw.*`)
3. All 4 delegation targets documented
4. Prerequisites in prose form (no `requires.anyBins/config` YAML)
5. Session naming uses `hermes-` prefix throughout
6. Notification block genericized (no `openclaw message send`)
</verification>

<success_criteria>
- SKILL-01 (coding-agent migration + discoverability): file in correct directory path makes it auto-discoverable via skill directory walk (no registry edit needed per CONTEXT.md "Integration Points")
- SKILL-03 (frontmatter schema compliance): tags[], related_skills[] present; schema-indistinguishable from claude-code/codex/opencode
- SKILL-04 (prerequisites mapping): `metadata.openclaw.requires.{anyBins,config}` → prose `## Prerequisites` section; zero openclaw-format dependencies remaining
</success_criteria>

<output>
Create `.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-01-SUMMARY.md` when done.
</output>

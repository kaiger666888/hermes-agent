---
phase: 34-skills-migration-coding-agent-tmux-agents
plan: 03
type: execute
wave: 2
depends_on: [34-01, 34-02]
files_modified:
  - skills/autonomous-ai-agents/claude-code/SKILL.md
  - skills/autonomous-ai-agents/codex/SKILL.md
  - skills/autonomous-ai-agents/opencode/SKILL.md
  - skills/autonomous-ai-agents/hermes-agent/SKILL.md
  - skills/autonomous-ai-agents/COEXISTENCE.md
autonomous: true
requirements: [SKILL-01, SKILL-02]
user_setup: []

must_haves:
  truths:
    - "Each of the 4 existing autonomous-ai-agents skills (claude-code, codex, opencode, hermes-agent) declares `coding-agent` and `tmux-agents` in its `metadata.hermes.related_skills` list — bidirectional graph is complete"
    - "`skills/autonomous-ai-agents/COEXISTENCE.md` exists and documents the SUPPLEMENT coexistence decision with rationale for why coding-agent + tmux-agents do NOT replace the existing 4"
    - "The 4 existing skills remain discoverable (frontmatter still parses, schema unchanged except for the 2 added related_skills entries)"
  artifacts:
    - path: "skills/autonomous-ai-agents/COEXISTENCE.md"
      provides: "Documented coexistence decision (SUPPLEMENT) with rationale + overlap matrix"
      contains: "SUPPLEMENT"
    - path: "skills/autonomous-ai-agents/claude-code/SKILL.md"
      provides: "Updated related_skills list (now includes coding-agent + tmux-agents)"
      contains: "coding-agent"
  key_links:
    - from: "skills/autonomous-ai-agents/claude-code/SKILL.md metadata.hermes.related_skills"
      to: "skills/autonomous-ai-agents/coding-agent/SKILL.md"
      via: "YAML list entry 'coding-agent'"
      pattern: "related_skills:\\s*\\[.*coding-agent"
    - from: "skills/autonomous-ai-agents/coding-agent/SKILL.md metadata.hermes.related_skills"
      to: "skills/autonomous-ai-agents/claude-code/SKILL.md"
      via: "reverse link already established in Plan 34-01"
      pattern: "related_skills:\\s*\\[.*claude-code"
---

<objective>
Wire the bidirectional `related_skills` graph between the 2 new migrated skills (from Plans 34-01 and 34-02) and the 4 existing autonomous-ai-agents skills, and document the SUPPLEMENT coexistence decision in a canonical `COEXISTENCE.md` so the design rationale is auditable for Phase 37's migration report.

Purpose: Closes SKILL-01 + SKILL-02 coexistence requirement (ROADMAP SC #5: "Coexistence decision ... is documented and ... the migrated skills do not break discovery of the existing 4"). Also satisfies the bidirectional graph invariant from CLAUDE.md "Skill File Conventions" (`related_skills` is bidirectional — adding new skills means updating existing skills' lists).

Output: 4 SKILL.md files with one-line `related_skills` list edits + 1 new `COEXISTENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md
@.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-01-SUMMARY.md
@.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-02-SUMMARY.md
@/data/workspace/hermes-agent/CLAUDE.md

<interfaces>
<!-- Current related_skills lists in the 4 existing skills (from full read in planning). -->
<!-- Plan 34-03 only edits these lists — no body changes, no description changes. -->

```yaml
# skills/autonomous-ai-agents/claude-code/SKILL.md (current)
metadata:
  hermes:
    tags: [Coding-Agent, Claude, Anthropic, Code-Review, Refactoring, PTY, Automation]
    related_skills: [codex, hermes-agent, opencode]
# After edit:
#   related_skills: [codex, hermes-agent, opencode, coding-agent, tmux-agents]

# skills/autonomous-ai-agents/codex/SKILL.md (current)
metadata:
  hermes:
    tags: [Coding-Agent, Codex, OpenAI, Code-Review, Refactoring]
    related_skills: [claude-code, hermes-agent]
# After edit:
#   related_skills: [claude-code, hermes-agent, opencode, coding-agent, tmux-agents]
# (Also adds the missing `opencode` cross-link that was already present in opencode's list — opportunity to fix latent asymmetry while we're touching the file.)

# skills/autonomous-ai-agents/opencode/SKILL.md (current)
metadata:
  hermes:
    tags: [Coding-Agent, OpenCode, Autonomous, Refactoring, Code-Review]
    related_skills: [claude-code, codex, hermes-agent]
# After edit:
#   related_skills: [claude-code, codex, hermes-agent, coding-agent, tmux-agents]

# skills/autonomous-ai-agents/hermes-agent/SKILL.md (current)
metadata:
  hermes:
    tags: [hermes, setup, configuration, multi-agent, spawning, cli, gateway, development]
    homepage: https://github.com/NousResearch/hermes-agent
    related_skills: [claude-code, codex, opencode]
# After edit:
#   related_skills: [claude-code, codex, opencode, coding-agent, tmux-agents]
```

<!-- Reverse links already established by Plans 34-01 + 34-02: -->
<!-- coding-agent related_skills: [claude-code, codex, opencode, hermes-agent, tmux-agents] -->
<!-- tmux-agents related_skills: [claude-code, codex, opencode, hermes-agent, coding-agent] -->

**Edit policy:** Each edit is the smallest possible change — append 2 names (`coding-agent`, `tmux-agents`) to the existing YAML list on the `related_skills:` line. Use the Edit tool with old_string/new_string for surgical edits. Do NOT rewrite the file or change body content.

**COEXISTENCE.md content shape (the load-bearing artifact for SKILL-01/02 SC #5):**
- Frontmatter: plain markdown (no YAML required for non-skill docs in the category directory; this is documentation, not a skill)
- Section 1: Coexistence decision (SUPPLEMENT) + 1-paragraph rationale
- Section 2: Capability overlap matrix (6 skills × 4 capability dimensions)
- Section 3: "When to use which" decision tree (operator-facing)
- Section 4: Migration provenance (source openclaw paths → hermes target paths)
</interfaces>

<decisions_honored>
- **D (Coexistence: SUPPLEMENT)** — coding-agent + tmux-agents supplement (don't replace) the 4 existing skills. The COEXISTENCE.md must make this explicit with a capability matrix showing what each of the 6 skills is authoritative for.
- **D (Bidirectional graph)** — CLAUDE.md "Skill File Conventions" makes this invariant explicit. Plan 34-01/34-02 already added reverse links FROM the new skills TO the 4 existing ones; this plan completes the graph by adding forward links FROM the 4 existing skills TO the 2 new ones.
- **D (SC #5 verifiable)** — The COEXISTENCE.md is the auditable artifact ROADMAP SC #5 requires. Phase 37 VALIDATE-03 will reference it in the migration report.
</decisions_honored>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append coding-agent + tmux-agents to related_skills in the 4 existing autonomous-ai-agents skills</name>
  <files>skills/autonomous-ai-agents/claude-code/SKILL.md, skills/autonomous-ai-agents/codex/SKILL.md, skills/autonomous-ai-agents/opencode/SKILL.md, skills/autonomous-ai-agents/hermes-agent/SKILL.md</files>
  <read_first>
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/claude-code/SKILL.md (lines 1-13 only — frontmatter; the rest of the file is irrelevant to this edit)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/codex/SKILL.md (lines 1-12 only)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/opencode/SKILL.md (lines 1-12 only)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/hermes-agent/SKILL.md (lines 1-13 only)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md (created by Plan 34-01 — verify frontmatter related_skills list as the reverse anchor)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md (created by Plan 34-02 — verify frontmatter related_skills list as the reverse anchor)
    - /data/workspace/hermes-agent/.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md (decisions)
  </read_first>
  <behavior>
    - Each of the 4 files has its `related_skills:` line updated to include `coding-agent` and `tmux-agents` (in addition to all pre-existing entries — additive edit, no removals)
    - YAML remains valid (parses cleanly)
    - File body content is unchanged (frontmatter edit only)
    - All 6 skills now form a complete bidirectional related_skills graph (every skill references every other skill in the category that is relevant)
  </behavior>
  <action>
    Apply 4 surgical Edit operations. Each edit modifies only the single `related_skills:` YAML line in the frontmatter. Use the Edit tool with precise old_string → new_string (whitespace preserved from existing indentation, which is 4 spaces under `metadata:` then 4 more under `hermes:`).

    **Edit 1 — `skills/autonomous-ai-agents/claude-code/SKILL.md`:**
    - old_string: `    related_skills: [codex, hermes-agent, opencode]`
    - new_string: `    related_skills: [codex, hermes-agent, opencode, coding-agent, tmux-agents]`

    **Edit 2 — `skills/autonomous-ai-agents/codex/SKILL.md`:**
    - old_string: `    related_skills: [claude-code, hermes-agent]`
    - new_string: `    related_skills: [claude-code, hermes-agent, opencode, coding-agent, tmux-agents]`
    (Rationale: also adds the latent-asymmetric `opencode` cross-link that opencode/SKILL.md already declared. Fixing this asymmetry opportunistically while the file is open keeps the graph complete.)

    **Edit 3 — `skills/autonomous-ai-agents/opencode/SKILL.md`:**
    - old_string: `    related_skills: [claude-code, codex, hermes-agent]`
    - new_string: `    related_skills: [claude-code, codex, hermes-agent, coding-agent, tmux-agents]`

    **Edit 4 — `skills/autonomous-ai-agents/hermes-agent/SKILL.md`:**
    - old_string: `    related_skills: [claude-code, codex, opencode]`
    - new_string: `    related_skills: [claude-code, codex, opencode, coding-agent, tmux-agents]`

    **DO NOT** modify any other line in any of these 4 files. No body changes. No description changes. No version bumps (this is a graph-wiring edit, not a feature change — version bump is at the maintainer's discretion and out of scope for a migration phase).

    After all 4 edits, run the verify gate to confirm YAML still parses and the bidirectional graph is complete.
  </action>
  <verify>
    <automated>for f in claude-code codex opencode hermes-agent; do python3 -c "import yaml; d=yaml.safe_load(open('/data/workspace/hermes-agent/skills/autonomous-ai-agents/$f/SKILL.md','r',encoding='utf-8').read().split('---',2)[1]); rs=d['metadata']['hermes']['related_skills']; assert 'coding-agent' in rs, '$f missing coding-agent'; assert 'tmux-agents' in rs, '$f missing tmux-agents'; print('$f OK:', rs)"; done</automated>
    <automated>python3 -c "
import yaml
# Verify the bidirectional graph: every relevant pair is linked both directions
skills = ['claude-code','codex','opencode','hermes-agent','coding-agent','tmux-agents']
graphs = {}
for s in skills:
    d = yaml.safe_load(open(f'/data/workspace/hermes-agent/skills/autonomous-ai-agents/{s}/SKILL.md','r',encoding='utf-8').read().split('---',2)[1])
    graphs[s] = set(d['metadata']['hermes']['related_skills'])
# coding-agent and tmux-agents should reference all 4 existing + each other
assert graphs['coding-agent'] >= {'claude-code','codex','opencode','hermes-agent','tmux-agents'}, 'coding-agent links incomplete'
assert graphs['tmux-agents'] >= {'claude-code','codex','opencode','hermes-agent','coding-agent'}, 'tmux-agents links incomplete'
# Each existing skill should reference both new skills
for s in ['claude-code','codex','opencode','hermes-agent']:
    assert 'coding-agent' in graphs[s], f'{s} missing coding-agent'
    assert 'tmux-agents' in graphs[s], f'{s} missing tmux-agents'
print('BIDIRECTIONAL_GRAPH_OK')
"</automated>
    <automated>grep -c "^---$" /data/workspace/hermes-agent/skills/autonomous-ai-agents/claude-code/SKILL.md /data/workspace/hermes-agent/skills/autonomous-ai-agents/codex/SKILL.md /data/workspace/hermes-agent/skills/autonomous-ai-agents/opencode/SKILL.md /data/workspace/hermes-agent/skills/autonomous-ai-agents/hermes-agent/SKILL.md | grep -q "2:2:2:2$\|2:2:2:2"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 SKILL.md files still parse as valid YAML frontmatter (no syntax errors introduced)
    - Each of the 4 files' `metadata.hermes.related_skills` list now contains both `coding-agent` and `tmux-agents`
    - Pre-existing entries in each related_skills list are preserved (additive edit only — verify claude-code still has codex+hermes-agent+opencode, etc.)
    - File bodies are byte-identical except for the single modified `related_skills:` line per file (no other content changes)
    - Each file still has exactly 2 `---` frontmatter delimiters (YAML block still well-formed)
    - Bidirectional graph is complete: coding-agent ↔ all 4 existing + tmux-agents; tmux-agents ↔ all 4 existing + coding-agent; each existing skill ↔ both new skills
    - The latent codex↔opencode asymmetry is also resolved (codex now references opencode, matching opencode's pre-existing reference to codex)
  </acceptance_criteria>
  <done>
    Bidirectional related_skills graph is complete across all 6 autonomous-ai-agents skills. The 2 new skills are first-class citizens of the category's cross-reference network.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write COEXISTENCE.md documenting the SUPPLEMENT decision with capability matrix</name>
  <files>skills/autonomous-ai-agents/COEXISTENCE.md</files>
  <read_first>
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/coding-agent/SKILL.md (from Plan 34-01 — to extract capability claims for the matrix)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md (from Plan 34-02 — to extract capability claims)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/claude-code/SKILL.md (REFERENCE — opening sections only, to characterize claude-code's scope)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/codex/SKILL.md (REFERENCE — opening sections only)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/opencode/SKILL.md (REFERENCE — opening sections only)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/hermes-agent/SKILL.md (REFERENCE — opening sections only)
    - /data/workspace/hermes-agent/.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md (SUPPLEMENT rationale + decisions)
  </read_first>
  <behavior>
    - File exists at `skills/autonomous-ai-agents/COEXISTENCE.md`
    - States the SUPPLEMENT decision explicitly in the first section
    - Contains a capability-overlap matrix covering all 6 skills × at least 4 capability dimensions (Single-agent deep-dive / Unified delegation / Session lifecycle management / Multi-agent parallel)
    - Contains a "When to use which" decision tree operator-facing
    - Contains a Migration provenance section listing source openclaw paths → hermes target paths
    - Mentions ROADMAP Phase 34 SC #5 as the requirement this document satisfies (traceability)
  </behavior>
  <action>
    Create `skills/autonomous-ai-agents/COEXISTENCE.md` with the following structure (markdown, no YAML frontmatter — this is documentation, not a skill):

    **H1:** `# Coexistence Decision: coding-agent + tmux-agents in autonomous-ai-agents`

    **Section 1 — `## Decision: SUPPLEMENT (not replace)`**
    One paragraph stating: As of v7.0 Phase 34 (2026-06-25), `coding-agent` and `tmux-agents` are migrated from openclaw into `skills/autonomous-ai-agents/` as a **supplement** to the existing 4 skills (`claude-code`, `codex`, `opencode`, `hermes-agent`). The existing 4 remain authoritative for single-agent deep-dives; the 2 new skills add unified delegation and session lifecycle capabilities that none of the 4 individually provide. No replacement, no merge, no deprecation. Cites ROADMAP Phase 34 SC #5 as the traceability anchor.

    **Section 2 — `## Capability Matrix`**
    Markdown table with 6 rows (one per skill) × 5 columns: Skill / Single-agent deep-dive (full CLI surface) / Unified delegation across multiple agents / Session lifecycle (spawn/list/attach/get-results) / Multi-agent parallel orchestration. Cells contain "Authoritative" / "Supports" / "—" / "N/A" as appropriate. Suggested content:
    | Skill | Single-agent deep-dive | Unified delegation | Session lifecycle | Multi-agent parallel |
    |-------|------------------------|--------------------|--------------------|----------------------|
    | `claude-code` | Authoritative (Claude Code) | — | — | Supports (via tmux) |
    | `codex` | Authoritative (Codex) | — | — | Supports (via worktrees) |
    | `opencode` | Authoritative (OpenCode) | — | — | Supports (via workdirs) |
    | `hermes-agent` | Authoritative (Hermes itself) | — | — | Supports (spawning) |
    | `coding-agent` | — | Authoritative (Codex/Claude Code/Pi/OpenCode) | Supports | Supports |
    | `tmux-agents` | — | Supports | Authoritative | Authoritative |

    **Section 3 — `## When to use which`**
    Numbered decision tree (operator-facing prose):
    1. "Need full surface-area docs for ONE agent (install, auth, every CLI flag, every pitfall)?" → invoke the dedicated skill (`claude-code` / `codex` / `opencode` / `hermes-agent`).
    2. "Need to delegate a coding task to a background tmux worker and pick between Codex/Claude Code/Pi/OpenCode with a structured notification block?" → use `coding-agent`.
    3. "Need to manage the lifecycle of multiple background agent sessions (spawn/list/attach/check-status/kill across many)?" → use `tmux-agents`.
    4. "Need both — pick agent + manage session?" → use `tmux-agents` for lifecycle; cross-reference `coding-agent` for the spawn-with-notification pattern.

    **Section 4 — `## Migration Provenance (Phase 34, v7.0)`**
    Two-line table mapping source → target:
    - `~/.openclaw/skills/coding-agent/SKILL.md` → `skills/autonomous-ai-agents/coding-agent/SKILL.md` (frontmatter adapted to hermes schema; openclaw runtime tokens rewritten; ACP references dropped per v7.0 scope)
    - `~/.openclaw/skills/openclaw-skills-tmux-agents/SKILL.md` → `skills/autonomous-ai-agents/tmux-agents/SKILL.md` (frontmatter adapted; `metadata.clawdbot.*` dropped; scripts documented upstream but not migrated as files)

    **Section 5 — `## Bidirectional related_skills Graph`**
    Note that all 6 skills' `metadata.hermes.related_skills` lists were updated to form a complete cross-reference graph (including fixing a latent codex↔opencode asymmetry). This is the discovery mechanism — agents traversing the graph can navigate between any pair.

    **Section 6 — `## Out of Scope`**
    Brief list of openclaw items NOT migrated in v7.0 Phase 34 (for Phase 37 VALIDATE-03 audit completeness): `_meta.json` files, `scripts/spawn.sh|check.sh|status.sh` (documented only), `README.md` files, ACP thread-bound work references, `clawdhub install` automation, openclaw notification send command. Cross-references REQUIREMENTS.md `## Out of Scope` table.

    UTF-8 encoding, single trailing newline. No emojis (per CLAUDE.md project conventions unless explicitly requested).
  </action>
  <verify>
    <automated>test -f /data/workspace/hermes-agent/skills/autonomous-ai-agents/COEXISTENCE.md &amp;&amp; grep -q "SUPPLEMENT" /data/workspace/hermes-agent/skills/autonomous-ai-agents/COEXISTENCE.md &amp;&amp; grep -q "^## Capability Matrix\|^## When to use which\|^## Migration Provenance\|^## Bidirectional related_skills" /data/workspace/hermes-agent/skills/autonomous-ai-agents/COEXISTENCE.md</automated>
    <automated>grep -c "claude-code.*codex.*opencode.*hermes-agent.*coding-agent.*tmux-agents\|coding-agent.*tmux-agents" /data/workspace/hermes-agent/skills/autonomous-ai-agents/COEXISTENCE.md | grep -q "[1-9]"</automated>
    <automated>grep -q "SC #5\|Phase 34" /data/workspace/hermes-agent/skills/autonomous-ai-agents/COEXISTENCE.md</automated>
    <automated>grep -c "^| " /data/workspace/hermes-agent/skills/autonomous-ai-agents/COEXISTENCE.md | grep -q "[7-9]\|[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `skills/autonomous-ai-agents/COEXISTENCE.md`
    - The literal token `SUPPLEMENT` appears in the file (decision is explicit, not implicit)
    - 4 required H2 sections present: `## Capability Matrix`, `## When to use which`, `## Migration Provenance`, `## Bidirectional related_skills Graph`
    - Capability matrix contains all 6 skill names (claude-code, codex, opencode, hermes-agent, coding-agent, tmux-agents)
    - File references Phase 34 or SC #5 for traceability to ROADMAP
    - At least one markdown table with ≥7 rows (header + separator + 6 skill rows in the capability matrix)
    - File is valid UTF-8, single trailing newline, no emojis
  </acceptance_criteria>
  <done>
    `COEXISTENCE.md` is the canonical auditable artifact for ROADMAP Phase 34 SC #5. Phase 37 VALIDATE-03 can reference it directly in the migration report. Operator + future maintainer can read it standalone to understand why 6 skills coexist without overlap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill-content → agent-runtime | The 4 SKILL.md edits inject no new prompt content (frontmatter-only); COEXISTENCE.md is not loaded as a skill — it's documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering | YAML frontmatter in 4 existing skills | mitigate | yaml.safe_load verify gate + bidirectional-graph assertion in `<verify>` catches any introduced syntax error |
| T-34-08 | Repudiation | Coexistence decision documentation | mitigate | COEXISTENCE.md cites ROADMAP SC #5 + creation date for audit traceability |

No package installs (markdown-only edits) — supply-chain threats N/A; no T-34-SC entry.
</threat_model>

<verification>
Phase 34 final cross-plan verification (this plan completes the phase):

1. All 6 autonomous-ai-agents skills parse as valid YAML frontmatter
2. Bidirectional related_skills graph is complete (every relevant pair linked both directions)
3. COEXISTENCE.md exists with explicit SUPPLEMENT decision + capability matrix
4. No openclaw-runtime strings remain in any migrated skill (carried over from Plans 34-01/34-02 verify gates)
5. The 4 existing skills remain discoverable (frontmatter schema unchanged except 2 added related_skills entries)
6. The 2 new skills are discoverable (file in expected paths, loaded via skill directory walk)
</verification>

<success_criteria>
- SKILL-01 (coding-agent coexistence): file exists, doesn't break discovery of existing 4, related_skills graph wired both directions
- SKILL-02 (tmux-agents coexistence): same — file exists, doesn't break discovery, bidirectional graph wired
- ROADMAP SC #5 satisfied: coexistence decision documented in canonical artifact + migration provenance traceable
- Phase 34 ready for Phase 37 validation benchmark (skills are in expected paths with expected frontmatter)
</success_criteria>

<output>
Create `.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-03-SUMMARY.md` when done.
</output>

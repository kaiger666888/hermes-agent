---
phase: 50-poc-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v10-orchestrator-design/05-POC-PLAN.md
autonomous: true
requirements: [DESIGN-06]
granularity: standard

must_haves:
  truths:
    - "File .planning/research/v10-orchestrator-design/05-POC-PLAN.md exists"
    - "PoC 目标明确 (vertical slice: 1 个 creative phase + 1 个 infra phase, 论据完整) —— creative = screenplay Step 3 round table (decision #3 D2 storyboard-first-class + ARCHITECTURE §2 screenplay HOOK-09 contract load-bearing + 9 related_agents densest round table); infra = agent registry + 1 round table invocation (decision #1 T6 协议 + STACK §3.2 7 MCP tool surface + Phase 46 round-table-state-schema full lifecycle). Both selections cite 决策号 + research section + edge-case rationale, NOT arbitrary picks."
    - "验收条件清单完整 (7 项, 每项 1-3 天工作量估算): (1) Fitness battery 设计 (cite PITFALLS §P8 mitigation 1 frozen battery + Label Studio 4 competencies + Shaped.ai Recall@K, 3 天), (2) Latency SLO p95 < 500ms (cite PITFALLS §P3 mitigation 4 + ARCHITECTURE §3.2 Option B mem0 scoped retrieval, 2 天), (3) Bias canary (cite PITFALLS §P5 mitigation 4 + curator `_memory_evolution_phase` hallucination detection, 2 天), (4) Compaction pass (cite PITFALLS §P9 + SUMMARY OQ-7 `memory.max_records` default 500 + 3-tier core/working/archival, 1 天), (5) Threshold tuning (cite PITFALLS §P13 curator runaway + initial defaults + tuning path, 1 天), (6) Dry-run-first invariant (cite PITFALLS §P5 mitigation 5 + SUMMARY OQ-16 curator defaults to dry-run, 1 天), (7) Schema migration dry-run (cite Phase 49 §4.5 `hermes agent memory migrate --dry-run` + PITFALLS §P14 mitigation 3, 2 天). Total: 12 person-days."
    - "Risk register (7 load-bearing pitfalls P1/P2/P4/P5/P6/P7/P8 × PoC deferral 评估) 完整 —— 每 pitfall 标注 must-fix-in-PoC vs defer-with-monitoring + rationale. Verdicts: P1 must-fix (persona_sha256 drift probe), P2 must-fix (TTL + verified_at), P4 must-fix (scope + project_id required), P5 must-fix (evidence_chain + bias canary + dry-run-first), P6 PARTIAL (signed feedback PoC must; outlier detection defer), P7 must-fix (round-table memory conflict is v10.0 core feature), P8 must-fix (fitness battery is the only regression signal). 7/7 verdicts align with SUMMARY §Risk Register + PITFALLS §Risk Register Summary (line 470-488)."
    - "PoC 实施路径图完整 —— sequence: fitness battery (foundation, must exist before any curator tick) → schema migration dry-run (P14 mitigation, validates memory layer before live writes) → bias canary (P5 mitigation, gates curator `_memory_evolution_phase` live mode). Path cites STACK §7 token cost estimate (~550K/pipeline run, §7.3 line 1026; ~340 MCP call/pipeline run, §7.1 line 1001; ~80 LLM opinion call, §7.3 line 1024) to scope per-vertical-slice token cost (~42K tokens per single round table = 550K/13)."
    - "Phase 44 决策 1-7 each cited by 决策号 at least once across the narrative (cross-validation of locked decisions, NOT re-derivation); particularly 决策 6 (per-agent memory + curator-driven 自进化 — load-bearing for entire PoC scope) + 决策 7 (Hermes 控 structure — load-bearing for fitness battery + bias canary gating)"
    - "PITFALLS §P1/§P2/§P4/§P5/§P6/§P7/§P8 + §P14 each cited by section number at least once; STACK §7 (token cost) + §3.2 (7 MCP tool surface for infra vertical slice) each cited; SUMMARY §Risk Register (7 load-bearing pitfalls) cited; Phase 49 §4 (memory migration) + §5 (retained-phases allowlist) cited as PoC input"
    - "Phase 50 SC#1-5 each mapped to a section in the doc; cross-doc consistency maintained (term `agent`/`skill`/`round table`/`panel`/`turn` match Phase 44-49 usage; schema field names `persona_sha256`/`scope`/`evidence_chain`/`memory.max_records`/`retained_python_phases` match Phase 45/46/49)"
  artifacts:
    - path: ".planning/research/v10-orchestrator-design/05-POC-PLAN.md"
      provides: "v10.0 design doc #05 — the capstone PoC plan that aggregates the previous 6 docs' pitfall mitigations into a v11.0 PoC implementer's blueprint. Four lockable artifacts: (a) Vertical slice selection (creative = screenplay Step 3 round table + infra = agent registry + 1 round table invocation, both with 决策号 + research citation + edge-case rationale), (b) 7-item acceptance criteria checklist with per-item 1-3 day workload estimate (fitness battery 3d / latency SLO 2d / bias canary 2d / compaction 1d / threshold tuning 1d / dry-run-first 1d / schema migration dry-run 2d = 12 person-days total), (c) 7-row risk register (P1/P2/P4/P5/P6/P7/P8 × must-fix-in-PoC vs defer-with-monitoring verdict + rationale, aligned with SUMMARY §Risk Register + PITFALLS §Risk Register Summary line 470-488), (d) PoC implementation path (sequence: fitness battery → schema migration dry-run → bias canary, with STACK §7 ~550K/pipeline-run token cost scoping). Cite Phase 44 决策 1-7 (especially 决策 6 per-agent memory + 决策 7 Hermes-controls-structure) + Phase 45 agents-schema.yaml + memory-record-schema.yaml + Phase 46 round-table-state-schema.yaml + Phase 49 §4 memory migration + §5 retained-phases allowlist + PITFALLS §P1/§P2/§P4/§P5/§P6/§P7/§P8/§P14 + STACK §3.2/§7 + SUMMARY §Risk Register by reference — does NOT re-derive them. Single deliverable (no schema YAML, mirrors Phase 47/48/49 single-deliverable pattern)."
      min_lines: 800
      contains: ["vertical slice", "screenplay", "agent registry", "round table", "fitness battery", "fitness_battery", "latency SLO", "p95", "500ms", "bias canary", "compaction", "threshold tuning", "dry-run", "schema migration", "PITFALLS §P1", "PITFALLS §P2", "PITFALLS §P4", "PITFALLS §P5", "PITFALLS §P6", "PITFALLS §P7", "PITFALLS §P8", "PITFALLS §P14", "STACK §7", "STACK §3.2", "SUMMARY", "Risk Register", "must-fix", "defer-with-monitoring", "PARTIAL", "persona_sha256", "evidence_chain", "evidence_operator_ids", "memory.max_records", "retained_python_phases", "scope", "project_id", "决策 1", "决策 2", "决策 3", "决策 5", "决策 6", "决策 7", "12 person-days\\|12 人日\\|12 person day", "550K", "340 MCP", "curator", "_memory_evolution_phase", "Phase 49", "Phase 51", "VALIDATE", "FOUND-08", "Label Studio", "Shaped.ai\\|Shaped\\.ai", "GetMaxim", "ReConcile", "MINJA"]
  key_links:
    - from: "05-POC-PLAN.md"
      to: "00-FIRST-PRINCIPLES.md §2.1-§2.7 (decisions 1-7, esp §2.6 决策 6 per-agent memory root + §2.7 决策 7 Hermes-controls-structure)"
      via: "markdown §reference + 决策号 citation"
      pattern: "00-FIRST-PRINCIPLES|决策 [1-7]"
    - from: "05-POC-PLAN.md"
      to: "PITFALLS.md §P1/§P2/§P4/§P5/§P6/§P7/§P8 (7 load-bearing pitfalls) + §P14 (schema migration) + §Risk Register Summary (line 470-488)"
      via: "markdown §reference for 7-row risk register + acceptance criteria"
      pattern: "PITFALLS §P(1|2|4|5|6|7|8|14)|Pitfall (1|2|4|5|6|7|8|14)"
    - from: "05-POC-PLAN.md"
      to: "STACK.md §7 (token cost ~550K/pipeline run, §7.3 line 1026) + §7.1 (340 MCP call/pipeline run) + §3.2 (7 MCP tool surface for infra vertical slice)"
      via: "markdown §reference for PoC implementation path token-cost scoping"
      pattern: "STACK §(3\\.2|7|7\\.1|7\\.3)|550K|340 MCP"
    - from: "05-POC-PLAN.md"
      to: "SUMMARY.md §Risk Register (7 load-bearing pitfalls, line 145-160) + §Gaps to Address (line 172-178)"
      via: "markdown §reference for risk register verdict alignment + PoC measurable gaps"
      pattern: "SUMMARY|Risk Register|Gaps to Address"
    - from: "05-POC-PLAN.md"
      to: "04-MIGRATION-PATH.md §4 (memory schema migration — PoC acceptance #7 input) + §5 (retained-phases allowlist — PoC run_python_phase test scope)"
      via: "markdown §reference for PoC week-1 work + run_python_phase boundary"
      pattern: "04-MIGRATION-PATH|retained_python_phases|schema migration"
    - from: "05-POC-PLAN.md"
      to: "01-AGENT-REGISTRY-SCHEMA.md agents-schema.yaml (persona_sha256 + fitness_battery + memory_scope fields) + memory-record-schema.yaml (evidence_chain + scope + confidence fields — PoC acceptance criteria consume these)"
      via: "markdown §reference for PoC field-level mitigation verification"
      pattern: "agents-schema\\.yaml|memory-record-schema\\.yaml|persona_sha256|fitness_battery|evidence_chain"
    - from: "05-POC-PLAN.md"
      to: "02-ROUND-TABLE-PROTOCOL.md (turn lifecycle + conflict arbitration — PoC vertical slice #1 screenplay round table exercises this)"
      via: "markdown §reference for creative vertical slice scope"
      pattern: "02-ROUND-TABLE-PROTOCOL|turn lifecycle|conflict arbitration|round_table_open"
    - from: "05-POC-PLAN.md"
      to: "06-CROSS-REPO-IMPACT.md (Option B → 物理分区 migration trigger — PoC latency SLO p95<500ms decides)"
      via: "markdown §reference for PoC week-1 latency benchmark trigger"
      pattern: "06-CROSS-REPO-IMPACT|Option B|物理分区|migration trigger"
---

<objective>
Create the v10.0 design doc #05 — the **capstone PoC plan** — as a single markdown deliverable:

`.planning/research/v10-orchestrator-design/05-POC-PLAN.md` (~800-1200 lines, bilingual EN structure + 中文 prose per CLAUDE.md)

Purpose: Aggregate the previous 6 design docs' (00-FIRST-PRINCIPLES / 01-AGENT-REGISTRY-SCHEMA / 02-ROUND-TABLE-PROTOCOL / 03-COMPARISON-VS-KIMI-MCP-SHIM / 04-MIGRATION-PATH / 06-CROSS-REPO-IMPACT) pitfall mitigations into a v11.0 PoC implementer's blueprint. Produces four lockable artifacts: (a) vertical slice selection (1 creative phase + 1 infra phase, each with 决策号 + research citation + edge-case rationale — NOT arbitrary picks), (b) 7-item acceptance criteria checklist with per-item 1-3 day workload estimate (fitness battery / latency SLO / bias canary / compaction / threshold tuning / dry-run-first / schema migration dry-run; total 12 person-days), (c) 7-row risk register (P1/P2/P4/P5/P6/P7/P8 × must-fix-in-PoC vs defer-with-monitoring verdict + rationale), (d) PoC implementation path (fitness battery → schema migration dry-run → bias canary sequence, with STACK §7 ~550K/pipeline-run token cost scoping).

The next consumers are: (1) v11.0 PoC implementer (direct blueprint — when they ask "what do I build first? what does 'done' look like? which pitfalls can I defer?", this doc answers with section-and-line-precise citations), (2) Phase 51 VALIDATE (audit cross-references this doc's risk register against PITFALLS §Risk Register Summary + SUMMARY §Risk Register), (3) the operator (Kai) reviewing whether the PoC scope is achievable in ~12 person-days + whether the defer-with-monitoring verdicts are acceptable.

Output: 1 file under `.planning/research/v10-orchestrator-design/`, mirroring Phase 44/45/46/47/48/49's bilingual depth/rigor at comparable scope (Phase 44 = 1181 lines, Phase 48 = 1300+, Phase 49 = 1508 lines; Phase 50 target ~800-1200 lines — slightly leaner because Phase 50 is aggregation/capstone, not new schema derivation). Phase 50 is single-deliverable (no schema YAML) like Phase 47/48/49. It ELABORATES existing source material (PITFALLS §Risk Register Summary + SUMMARY §Risk Register + STACK §7 token cost + Phase 49 §4-§5) AND introduces net-new synthesis (vertical slice selection rationale + 12-person-day estimate + sequencing rationale + per-pitfall PoC verdict) — the discipline is "cite the 7 pitfalls' mitigations from PITFALLS, cite the per-acceptance-criterion workload from first-principles task decomposition, cite the token cost from STACK §7 — synthesize, do NOT re-derive."

This is a DESIGN-ONLY deliverable. Zero code changes (no SKILL.md / Python / plugin / mcp_serve.py edits). Cite Phase 44's locked decisions (1-7, especially 决策 6 per-agent memory + 决策 7 Hermes-controls-structure — load-bearing for the entire PoC scope), Phase 45's locked agents-schema.yaml + memory-record-schema.yaml fields, Phase 46's locked round-table-state-schema.yaml, Phase 49's locked retained-phases allowlist + memory migration dry-run — do NOT re-derive them. Cite, do NOT redefine: `persona_sha256`, `evidence_chain`, `evidence_operator_ids`, `memory.max_records`, `retained_python_phases`, `scope`, `project_id` are all consumed here by reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
@.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md
@.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md
@.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md
@.planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md
@.planning/research/v10-orchestrator-design/STACK.md
@.planning/research/v10-orchestrator-design/FEATURES.md
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md

<interfaces>
<!-- Phase 44 root-argument anchors (do NOT re-derive; cite by 决策号): -->
<!-- Decision 1: T6 协议 (Hermes MCP server + tmux dispatch + CC native MCP client) — load-bearing for infra vertical slice (agent registry + 1 round table invocation uses 7 MCP tool surface) -->
<!-- Decision 3: D2 storyboard-first-class — load-bearing for creative vertical slice selection (storyboard Step 6.5 + screenplay Step 3 are D2-compatible delegate-eligible or round-table-eligible) -->
<!-- Decision 5: α agent form (YAML + persona + tools + refs + memory_scope + lineage) — load-bearing for infra vertical slice (1 agent registry entry = 1 YAML file) -->
<!-- Decision 6: per-agent memory + curator-driven 自进化 — LOAD-BEARING FOR ENTIRE PoC SCOPE (every acceptance criterion tests some facet of per-agent memory) -->
<!-- Decision 7: 分层 CC 角色 (Hermes controls turn_order/max_rounds/early_stop_rule/schema; CC controls framing + synthesis) — load-bearing for fitness battery + bias canary gating (Hermes-side enforcement, not CC-side) -->

<!-- Phase 45 schema contracts (CONSUME, do NOT redefine): -->
<!-- agents-schema.yaml fields load-bearing for PoC: persona_sha256 (P1 fitness battery + drift probe), fitness_battery (P8 frozen battery path), fitness_trend.jsonl location (P8 longitudinal trend), memory.max_records default 500 (P9 compaction trigger), memory_scope enum shared|per_agent|project_scoped -->
<!-- memory-record-schema.yaml fields load-bearing for PoC: evidence_chain (P5 hallucination guard), evidence_operator_ids (P5 bias audit + P6 outlier detection), scope global|project|session (P4 cross-project isolation), confidence + half_life_days (P2 stale memory decay), status active|archived|quarantined (P5/P6 quarantine), expires_at + verified_at (P2 TTL), schema_version (P14 forward-compat) -->

<!-- Phase 46 protocol contracts (CONSUME): -->
<!-- round_table_open → turn N → submit_round_table_result lifecycle — creative vertical slice exercises full lifecycle -->
<!-- Memory conflict arbitration (comparator LLM + scope precedence session>project>global + confidence-weighted voting + conflict log) — P7 mitigation, exercised by screenplay round table with 9 related_agents -->
<!-- 强制串行约束 (1 panelist 1 turn 顺序 await, GLM 4-key rotation compat) — load-bearing for PoC latency SLO scoping -->

<!-- Phase 49 contracts (CONSUME): -->
<!-- §4.5 dry-run migration mode: `hermes agent memory migrate --dry-run` — PoC acceptance criterion #7 directly consumes -->
<!-- §4.7 6-step migration execution (backup → dry-run → approval → live → shadow 30d → decommission) — PoC week-1 work -->
<!-- §5.2 retained-phases allowlist Steps 0/6.5/7/10/11/12/15 — PoC run_python_phase test scope (creative slice may delegate Step 6.5/7; infra slice doesn't invoke run_python_phase) -->
<!-- §5.5-§5.6 legacy v7.0 mem0 agent_id=hermes 30-day sunset — PoC runs within sunset window, legacy fallback available -->

<!-- PITFALLS §Risk Register Summary (line 470-488) — CANONICAL SOURCE for 7-row risk register verdicts: -->
<!-- P1 (persona drift): NO — load-bearing (must-fix-in-PoC) -->
<!-- P2 (stale memory): NO — load-bearing (must-fix-in-PoC) -->
<!-- P3 (scoped retrieval perf): YES — can ship with single workspace, scale later (DEFER-WITH-MONITORING) — BUT latency SLO acceptance criterion #2 measures this -->
<!-- P4 (cross-project leakage): NO — load-bearing (must-fix-in-PoC) -->
<!-- P5 (curator failure modes): NO — load-bearing (must-fix-in-PoC) -->
<!-- P6 (memory poisoning): PARTIAL — signed feedback PoC must; outlier detection defer -->
<!-- P7 (round-table conflict): NO — round-table is v10.0 core feature (must-fix-in-PoC) -->
<!-- P8 (no fitness signal): NO — load-bearing (must-fix-in-PoC) -->
<!-- (P3/P9/P10/P11/P12/P13/P14 are NOT in the SC#4 7-row scope but appear in PITFALLS Risk Register — Phase 50 SC#4 scope is P1/P2/P4/P5/P6/P7/P8 explicitly per ROADMAP) -->

<!-- STACK §7 token cost (load-bearing for PoC implementation path scoping): -->
<!-- §7.1: ~340 MCP call / pipeline run (line 1001) -->
<!-- §7.3: ~550K tokens / pipeline run (line 1026), of which ~520K is LLM-level (80 opinion call × 5000+1500 tokens, line 1024) -->
<!-- §7.4: transport choice negligible for LLM-bound tools (<1% latency diff) -->
<!-- §7.5: v11.0 PoC建议 no batch, 串行 first -->
<!-- Per-vertical-slice estimate: 550K/13 ≈ 42K tokens per single round table (creative slice = screenplay Step 3 = 1 round table; infra slice = 1 round table invocation) -->

<!-- SUMMARY §Risk Register (line 145-160) — secondary canonical source: -->
<!-- Aligns 7 load-bearing pitfalls with PoC-acceptable deferral verdicts + 必须字段/机制 + 在哪文档解决. Phase 50 risk register MUST align with this table (no divergent verdicts). -->
<!-- SUMMARY §Gaps to Address (line 172-178) — PoC measurable gaps: mem0 Platform API filter behavior (latency benchmark week-1), GLM 4-key real concurrency (first multi-round round table), curator `_memory_evolution_phase` hallucination rate (evidence_chain coverage check + bias canary), round table state CC-crash recovery (gc pass), contextvars vs threading.local (must pick contextvars). -->

<!-- Phase 50 CONTEXT.md locked decisions (from /gsd:discuss-phase autonomous smart-discuss): -->
<!-- SC#2 vertical slice: creative = storyboard/screenplay draft (decision 3 D2), infra = agent registry + 1 round table invocation -->
<!-- SC#3 acceptance criteria: 7 categories, each 1-3 day estimate -->
<!-- SC#4 risk register: 7 load-bearing pitfalls (P1/P2/P4/P5/P6/P7/P8) × must-fix-in-PoC vs defer-with-monitoring -->
<!-- SC#5 implementation path: fitness battery → schema migration dry-run → bias canary sequence, cite STACK §7 ~550K/pipeline run -->

<!-- CLAUDE.md deliverable form canon: -->
<!-- SKILL.md 双语 (英文 YAML structure + metadata 英文; description 与正文 EN structure + 中文段落/示例) -->
<!-- Memory reference: v6-self-evolution-milestone.md documents v6.0 fitness battery precedents — PoC fitness battery design should leverage v6.0 patterns -->

From .planning/research/v10-orchestrator-design/SUMMARY.md §Roadmap Implications (Phase 50's place in the design graph):
> 05-POC-PLAN.md (收口,依赖前面所有 design docs). Delivers: fitness battery + latency SLO (p95 < 500ms) + bias canary + compaction pass + threshold tuning + dry-run-first invariant + schema migration dry-run —— 全部作为 PoC acceptance criteria. Avoids: 全部 14 pitfalls (PoC must demonstrate mitigations work).

From .planning/research/v10-orchestrator-design/PITFALLS.md §Risk Register Summary (line 470-488) — CANONICAL SOURCE for Phase 50 risk register verdicts:
> | P1 | Persona drift | HIGH | M | NO — load-bearing |
> | P2 | Stale memory | HIGH | M | NO — load-bearing |
> | P3 | Scoped retrieval perf | HIGH | H | YES — can ship with single workspace, scale later |
> | P4 | Cross-project leakage | HIGH | M | NO — load-bearing |
> | P5 | Curator failure modes | HIGH | M | NO — load-bearing |
> | P6 | Memory poisoning | HIGH | H | PARTIAL — signed feedback is PoC must; outlier detection can defer |
> | P7 | Round-table conflict | MEDIUM | M | NO — round-table is v10.0 core feature |
> | P8 | No fitness signal | HIGH | M | NO — load-bearing |

From .planning/research/v10-orchestrator-design/STACK.md §7 (load-bearing for PoC implementation path token-cost scoping):
> §7.1 line 1001: ~340 MCP call / pipeline run
> §7.3 line 1024-1026: LLM-level ~520K tokens (80 opinion call × 5000+1500); Total per pipeline run: ~550K tokens
> §7.5: v11.0 PoC建议 7 tool 全部单 call 版, 先跑通; v11.1+ 视实测延迟决定批量化

From .planning/research/v10-orchestrator-design/SUMMARY.md §Gaps to Address (line 172-178) — PoC measurable gaps to surface in acceptance criteria:
> mem0 Platform API 真实 filter 行为: v11.0 PoC week-1 跑 latency benchmark, 若 p95 > 500ms 立即触发物理分区切换 (CC-4)
> GLM 4-key rotation × multi-panelist 真实并发成本: ~550K tokens/pipeline run 估算基于串行
> curator `_memory_evolution_phase` LLM aggregation 准确率: v11.0 PoC acceptance criterion: `evidence_chain` coverage check + bias canary 必须跑通
> round table state CC crash recovery + mem0 contextvars 并发安全: v11.0 PoC 需补 gc pass + 必须选 contextvars

From CLAUDE.md (deliverable form canon):
> Eval: v1 含轻量 LLM-as-judge 双盲 harness (每个 skill 有 benchmark prompts + judge 脚本 + 报告输出)

From MEMORY.md v6-self-evolution-milestone.md (PoC fitness battery precedent):
> v6.0 SHIPPED feedback-driven self-learning (curator + FeedbackStore + evolution pipeline). v6.0 eval gate (`gate.py:decide_verdict`) runs MT-Bench position-swap. PoC fitness battery design leverages v6.0 patterns + adds longitudinal trend (`fitness_trend.jsonl`) — the v6.0 baseline is per-patch, PoC baseline is per-agent-cross-patch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan + write §0 reading guide + §1 framing (capstone purpose + Phase 44 决策 6/7 citation anchor + SC mapping + 4 deliverables declared upfront)</name>
  <files>.planning/research/v10-orchestrator-design/05-POC-PLAN.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §0 + §1 (frontmatter + reading-guide pattern — apply same bilingual structure) + §2.1 (决策 1 — T6 协议, infra vertical slice root) + §2.3 (决策 3 — D2 storyboard-first-class, creative vertical slice root) + §2.5 (决策 5 — α agent form, infra slice agent registry entry) + §2.6 (决策 6 — per-agent memory + curator, LOAD-BEARING for entire PoC) + §2.7 (决策 7 — Hermes-controls-structure, fitness battery + bias canary gating root)
    - .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md §0 + §1 (Phase 49 pattern — most recent sibling single-deliverable doc, mirror structure)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Roadmap Implications (Phase 50 placement: 收口, produces fitness battery + latency SLO + bias canary + compaction + threshold tuning + dry-run-first + schema migration dry-run) + §Risk Register (line 145-160 — canonical PoC deferral verdicts) + §Gaps to Address (line 172-178 — PoC measurable gaps)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Risk Register Summary (line 470-488 — CANONICAL SOURCE for 7-row risk register) + §Phase-Specific Warnings (line 458-466 — Phase 50 row: "All pitfalls. Mitigation: Fitness battery, latency SLO, bias canary, compaction pass, threshold tuning — all as acceptance criteria")
    - .planning/research/v10-orchestrator-design/STACK.md §7.1 (line 991-1003 — 340 MCP call/pipeline run) + §7.3 (line 1017-1027 — 550K tokens/pipeline run) + §3.2 (line 246-610 — 7 MCP tool surface for infra vertical slice)
    - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md agents-schema.yaml (fitness_battery + persona_sha256 + memory_scope + memory.max_records fields) + memory-record-schema.yaml (evidence_chain + scope + confidence + status + expires_at fields)
  </read_first>

  <behavior>
  - Document frontmatter block (status / phase / audience / reading time / stability markers / confidence) mirrors 00-FIRST-PRINCIPLES.md + 04-MIGRATION-PATH.md style
  - §0 阅读指南 exists with chapter map, 3-audience guide (Kai reviewer / v11.0 PoC implementer / Phase 51 VALIDATE auditor), 稳定性 markers, "what this doc consumes from Phase 44/45/46/49 + PITFALLS §P1/§P2/§P4/§P5/§P6/§P7/§P8/§P14 + STACK §3.2/§7 + SUMMARY §Risk Register" preamble
  - §1.1 exists: framing — "本文档是 v10.0 设计套件的收口 —— 汇总前 6 份 design docs (00/01/02/03/04/06) 的 pitfall 缓解, 产 v11.0 PoC 验收条件清单 + 工作量估算 + risk register + 实施路径, 作为 v11.0 实施者直接照着写的实施蓝本. 是 Phase 44 决策 6 (per-agent memory + curator-driven 自进化) + 决策 7 (Hermes 控 structure) 的 PoC 验收路径落实." Cite Phase 44 决策 1/3/5/6/7 as root arguments + explicitly state 5 SC and what is consumed vs net-new synthesis.
  - §1.2 exists: scope rules — "本 doc 汇总 PITFALLS §Risk Register Summary (line 470-488) + SUMMARY §Risk Register (line 145-160) + STACK §7 token cost + Phase 49 §4-§5 到可执行 PoC 蓝图粒度, 不重定义 pitfall mitigations 或 schema 字段. CITE Phase 44 决策 1-7 + Phase 45 agents-schema.yaml + memory-record-schema.yaml + Phase 46 round-table-state-schema + Phase 49 §4 memory migration + §5 retained-phases allowlist 采用 CITE-ONLY 策略. Net-new synthesis: vertical slice 论据 + 12-person-day 估算 + sequencing rationale + per-pitfall PoC verdict + per-acceptance-criterion task decomposition."
  - §1.3 exists: 5 SC mapping table (本 doc 章节 → ROADMAP SC#1-5) so verification can cross-check
  - §1.4 exists: roadmap placement — "本 doc 是 v10.0 设计套件的 LAST design doc (Phase 51 VALIDATE 是 lint + audit, 不是 design doc). v11.0 PoC 实施者引用本 doc 的 §3 vertical slice + §4 acceptance criteria + §5 risk register + §6 implementation path 即可 defending 'what do I build first? what does done look like? which pitfalls can I defer?', 不需重新推导 Phase 44 决策或 re-litigate pitfall mitigations." State Phase 44 决策 1-7 + Phase 45 schemas + Phase 46 protocol + Phase 49 transform rules + Phase 48 cross-repo strategy are LOCKED — this doc only aggregates + sequences.
  - §1.5 exists: 4 deliverables declared upfront with load-bearing status: §1.5.1 Vertical slice selection (creative = screenplay Step 3 round table + infra = agent registry + 1 round table invocation); §1.5.2 7-item acceptance criteria checklist with 12-person-day total estimate; §1.5.3 7-row risk register (P1/P2/P4/P5/P6/P7/P8 × must-fix vs defer-with-monitoring); §1.5.4 PoC implementation path (fitness battery → schema migration dry-run → bias canary sequence)
  - §1.6 exists: capstone-at-a-glance table — single-row-per-pitfall + verdict + PoC section pointer (7 rows, the elevator-pitch version of §5 full risk register). Mirrors PITFALLS §Risk Register Summary structure.
  - §1.7 exists: explicit out-of-scope declaration — "本文档不讨论 agent YAML schema 字段定义本身 (留给 Phase 45), 不讨论 round table protocol 字段细节 (留给 Phase 46), 不讨论 v11.0 PoC live implementation (this is design-only, v11.0 PoC 是下一个 milestone), 不讨论 mem0 backend 部署 (留给 Phase 48 Option B vs 物理分区), 不讨论 cross-doc lint (留给 Phase 51 VALIDATE)"
  - Total Task 1 length ≥ 250 lines
  </behavior>

  <action>
  Create file `.planning/research/v10-orchestrator-design/05-POC-PLAN.md`. Write frontmatter + §0 + §1.

  For §1.1 (framing, ~80 lines): Open with "本文档是 v10.0 设计套件的收口 capstone —— 汇总前 6 份 design docs 的 pitfall 缓解, 产 v11.0 PoC 的 4 件套实施蓝本: (a) vertical slice selection (1 creative phase + 1 infra phase, 论据完整非任意挑), (b) 7-item acceptance criteria checklist (每项 1-3 天工作量估算, 总 12 person-days), (c) 7-row risk register (P1/P2/P4/P5/P6/P7/P8 × must-fix-in-PoC vs defer-with-monitoring verdict + rationale, 与 PITFALLS §Risk Register Summary line 470-488 + SUMMARY §Risk Register line 145-160 对齐), (d) PoC 实施路径 (fitness battery → schema migration dry-run → bias canary sequence, STACK §7 ~550K/pipeline-run token cost scoping)." Cite Phase 44 §2.1 (决策 1 — T6 协议, infra slice root) + §2.3 (决策 3 — D2 storyboard, creative slice root) + §2.5 (决策 5 — α form, infra slice agent registry root) + §2.6 (决策 6 — per-agent memory + curator, LOAD-BEARING for entire PoC) + §2.7 (决策 7 — Hermes控 structure, fitness battery + bias canary gating root). Explicitly enumerate the 5 SC mapped to sections, and declare what is consumed (PITFALLS mitigations, Phase 45 schema fields, Phase 46 protocol, Phase 49 §4-§5, STACK §7 token cost) vs net-new synthesis (vertical slice rationale + 12-person-day estimate + sequencing + per-pitfall verdict + task decomposition).

  For §1.2 (scope rules, ~30 lines): "本 doc 汇总 PITFALLS §Risk Register Summary (line 470-488) + SUMMARY §Risk Register (line 145-160) + STACK §7 token cost + Phase 49 §4-§5 到可执行 PoC 蓝图粒度. 本 doc 不讨论 (a) agent YAML schema 字段定义本身 (留给 Phase 45 agents-schema.yaml), (b) round table protocol 字段细节 (留给 Phase 46), (c) v11.0 PoC live implementation (this is design-only milestone, v11.0 PoC 是下一个 milestone), (d) mem0 backend 部署拓扑 (留给 Phase 48 Option B vs 物理分区), (e) cross-doc lint (留给 Phase 51 VALIDATE). 引用 Phase 44 决策 1-7 + Phase 45 agents-schema.yaml 18 fields + memory-record-schema.yaml 10+ fields + Phase 46 round-table-state-schema + Phase 49 §4 memory migration + §5 retained-phases allowlist 采用 CITE-ONLY 策略, 不重定义."

  For §1.3 (SC mapping table, ~30 lines): 5-row table mapping ROADMAP SC#1-5 → 本 doc 章节. Columns: | SC# | 描述 | 本 doc 解决章节 | 验证脚本 |

  For §1.4 (roadmap placement, ~30 lines): "本 doc 是 v10.0 设计套件的 LAST design doc (Phase 51 VALIDATE 是 lint + audit, 不是 design doc). v11.0 PoC 实施者引用本 doc 的 §3 vertical slice + §4 acceptance criteria + §5 risk register + §6 implementation path 即可 defending 'what do I build first? what does done look like? which pitfalls can I defer?'. Phase 51 VALIDATE 的 lint 脚本会 cross-check 本 doc 的 7-row risk register verdicts 与 PITFALLS §Risk Register Summary + SUMMARY §Risk Register 一致 + acceptance criteria 引用的 schema 字段 (persona_sha256/evidence_chain/memory.max_records/scope) 与 Phase 45/46/49 一致." State Phase 44 决策 1-7 + Phase 45 schemas + Phase 46 protocol + Phase 49 transform rules + Phase 48 cross-repo strategy are LOCKED — this doc only aggregates + sequences.

  For §1.5 (4 deliverables declared upfront, ~60 lines): Four H3 subsections declaring upfront what this doc provides beyond existing sources:
    - §1.5.1 Vertical slice selection: creative = screenplay Step 3 round table (decision #3 D2 + ARCHITECTURE §2 screenplay HOOK-09 contract load-bearing + 9 related_agents densest round table, exercises Phase 46 conflict arbitration naturally); infra = agent registry + 1 round table invocation (decision #1 T6 + STACK §3.2 7 MCP tool surface + Phase 46 round-table-state-schema full lifecycle). Net-new: 论据完整非任意挑.
    - §1.5.2 7-item acceptance criteria checklist (12 person-days total): fitness battery 3d (cite PITFALLS §P8 mitigation 1 + Label Studio 4 competencies + Shaped.ai Recall@K), latency SLO p95<500ms 2d (cite PITFALLS §P3 mitigation 4 + Phase 48 Option B → 物理分区 migration trigger), bias canary 2d (cite PITFALLS §P5 mitigation 4 + SUMMARY §Gaps line 176), compaction pass 1d (cite PITFALLS §P9 + SUMMARY OQ-7 memory.max_records default 500), threshold tuning 1d (cite PITFALLS §P13), dry-run-first invariant 1d (cite PITFALLS §P5 mitigation 5 + SUMMARY OQ-16), schema migration dry-run 2d (cite Phase 49 §4.5 + PITFALLS §P14). Net-new: per-item task decomposition to 1-3 day estimate.
    - §1.5.3 7-row risk register: P1/P2/P4/P5/P6/P7/P8 × must-fix-in-PoC vs defer-with-monitoring verdict + rationale. Verdicts align with PITFALLS §Risk Register Summary line 470-488 (NO/PARTIAL/YES columns) + SUMMARY §Risk Register line 145-160. Net-new: per-pitfall PoC-week acceptance criterion (which §4 item gates this pitfall's mitigation).
    - §1.5.4 PoC implementation path: sequence fitness battery → schema migration dry-run → bias canary (rationale: fitness battery is regression-detection foundation must exist before any curator tick; schema migration dry-run validates memory layer before live writes; bias canary gates curator `_memory_evolution_phase` transition from dry-run to live). STACK §7 ~550K/pipeline-run token cost scoping (~42K tokens per single round table = 550K/13). Net-new: sequencing rationale + per-vertical-slice token estimate.

  For §1.6 (capstone-at-a-glance, ~50 lines): A 7-row elevator-pitch table — mirror PITFALLS §Risk Register Summary structure. Columns: | Pitfall | Severity | PoC verdict (must-fix / PARTIAL / defer) | PoC acceptance § | PoC section ref |. This is the 30-second answer to "which pitfalls must the PoC fix?".

  For §1.7 (out-of-scope declaration, ~20 lines): Explicit list of what this doc does NOT cover: agent YAML schema field definitions (→ Phase 45), round table protocol field details (→ Phase 46), v11.0 PoC live implementation (→ next milestone), mem0 backend deployment topology (→ Phase 48 Option B vs 物理分区), cross-doc lint script (→ Phase 51 VALIDATE), A2A protocol (→ post-v12+).

  Length target: ~250-300 lines for Task 1.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 250 {exit 0} {exit 1}' && grep -qE "^## §0|## 0\.|阅读指南" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -qE "^## §1|^## 1\.|Framing" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && for term in "vertical slice" "screenplay" "agent registry" "round table" "fitness battery" "latency SLO\\|p95" "500ms" "bias canary" "compaction" "threshold tuning" "dry-run\\|dry run" "schema migration" "12 person-days\\|12 人日\\|12 person day" "550K" "340 MCP" "PITFALLS §P1" "PITFALLS §P2" "PITFALLS §P4" "PITFALLS §P5" "PITFALLS §P6" "PITFALLS §P7" "PITFALLS §P8" "PITFALLS §P14" "STACK §7" "STACK §3.2" "SUMMARY" "Risk Register" "must-fix" "defer-with-monitoring\\|defer with monitoring" "PARTIAL" "决策 1" "决策 3" "决策 5" "决策 6" "决策 7" "Phase 49" "Phase 51" "VALIDATE" "00-FIRST-PRINCIPLES"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md || { echo "MISSING: $term"; exit 1; }; done && grep -cE "PITFALLS §P(1|2|4|5|6|7|8|14)" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - File created with bilingual frontmatter (status / phase / audience / reading time / stability / confidence)
  - §0 阅读指南 exists with chapter map + 3-audience guide + Phase 44/45/46/49 + PITFALLS §P1/§P2/§P4/§P5/§P6/§P7/§P8/§P14 + STACK §3.2/§7 + SUMMARY §Risk Register consumption preamble
  - §1.1 framing cites Phase 44 决策 1/3/5/6/7 + declares 5 SC + what is consumed (PITFALLS mitigations + Phase 45/46/49 + STACK §7) vs net-new synthesis (vertical slice + 12-person-day + sequencing + verdicts + task decomposition)
  - §1.2 scope rules explicit (cite-only Phase 44/45/46/49 + PITFALLS + STACK §7 + SUMMARY §Risk Register)
  - §1.3 has SC#1-5 mapping table
  - §1.4 has roadmap placement + downstream consumer list (v11.0 PoC implementer + Phase 51 VALIDATE + operator Kai)
  - §1.5 has 4 deliverables declared upfront (vertical slice / 7-item acceptance / 7-row risk register / implementation path)
  - §1.6 has capstone-at-a-glance 7-row table (mirror PITFALLS §Risk Register Summary structure)
  - §1.7 has explicit out-of-scope declaration
  - Total doc length ≥ 250 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Write §2 (PoC scope + boundary — what "PoC done" means, what is explicitly NOT in PoC) + §3 (Vertical slice selection SC#2 deep-dive — creative + infra with 论据)</name>
  <files>.planning/research/v10-orchestrator-design/05-POC-PLAN.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1 (决策 1 T6 — infra slice root) + §2.3 (决策 3 D2 storyboard — creative slice root) + §2.5 (决策 5 α form — agent registry entry) + §2.6 (决策 6 per-agent memory — load-bearing for both slices) + §2.7 (决策 7 Hermes控 structure — both slices)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.3 (screenplay minimal example — concrete agent YAML for creative slice) + §2 (15-expert table — screenplay row: 5 refs + 9 related_agents + HOOK-09 contract + tools includes write_file/patch) + §3 (per-agent memory — both slices exercise this) + §4 (round table dispatch — creative slice exercises) + §5 (state schema — both slices)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §2 (turn lifecycle — creative slice screenplay Step 3 exercises full round_table_open → turn N → submit) + §3 (memory conflict arbitration — screenplay 9 related_agents naturally surfaces P7) + §4 (强制串行 — GLM 4-key compat)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 (7 MCP tool surface — infra slice = get_agent_persona + get_agent_opinion + get_agent_memory + submit_round_table_result + submit_artifact + query_memory + run_python_phase) + §6 (CC claude mcp add config — infra slice setup)
    - .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md §2.4 (screenplay transform rules — creative slice agent YAML is built per this) + §5.2 (retained-phases allowlist Step 6.5 + Step 7 — creative slice may delegate)
    - .planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md §3 (Option B vs 物理分区 — latency SLO §4 item #2 may trigger migration, infra slice uses Option B baseline)
  </read_first>

  <behavior>
  - §2.0 子节存在: 声明 §2 defines PoC scope boundary — what "PoC done" means (vertical slice + acceptance criteria pass) + what is explicitly NOT in PoC (live round table with all 15 agents, mem0 物理分区, full production rollout)
  - §2.1 子节存在: PoC "done" 定义 —— (a) 2 vertical slices 跑通 (creative + infra), (b) 7 acceptance criteria 全 pass (per §4), (c) 7 risk register verdicts 执行 (must-fix items 在 PoC 内 fix, defer-with-monitoring items 监控但不强制 fix), (d) STACK §7 token cost 实测 ≈ 估算 (within 2x of ~550K/pipeline run or per-slice ~42K)
  - §2.2 子节存在: PoC explicit exclusions —— (a) 不跑 full 15-agent round table (only 1 creative slice screenplay with subset of 9 related_agents), (b) 不迁 mem0 到 物理分区 (Option B filter 路由 baseline, 物理分区 deferred to v12+ per Phase 48), (c) 不动 production ~/.hermes/ (dedicated PoC workspace ~/.hermes-poc/), (d) 不跑 live client project (synthetic StoryKernel input), (e) 不 ship 到 operator (PoC ends at "acceptance criteria pass" report, not at "Kai uses it daily")
  - §2.3 子节存在: PoC workspace 隔离 —— `~/.hermes-poc/` dedicated directory (不污染 production ~/.hermes/), 独立 mem0 workspace `hermes-poc` (Option B agent_id filter 仍生效, 但 user_id="hermes-poc" 隔离), 独立 session DB (`~/.hermes-poc/sessions.db`), 独立 logs. Phase 49 §5.5 legacy v7.0 mem0 agent_id=hermes sunset policy 不影响 PoC (PoC 用 fresh namespace).
  - §2.4 子节存在: PoC duration + budget —— 12 person-days acceptance criteria (per §4) + 2-3 person-days vertical slice setup (infra: 7 MCP tool wire-up + 1 agent YAML transform; creative: 9-agent subset persona transform + Step 3 round table wiring) + 1-2 person-days risk register mitigation implementation (P1 persona_sha256 probe, P2 TTL+verified_at, P4 scope+project_id, P5 evidence_chain + dry-run-first, P6 operator_signature, P7 coordinator arbitration, P8 fitness battery scaffold) = ~15-17 person-days total PoC. Add 30-day shadow-run window (per Phase 49 §4.7 Step 5) for memory migration dual-read = total ~6 calendar weeks.
  - §3.0 子节存在: 声明 §3 是 SC#2 的完整论证 —— 2 vertical slices (creative + infra) each with 决策号 + research citation + edge-case rationale, NOT arbitrary picks. 显式列出 selection criteria (round-table density, schema-field coverage, decision coverage, synthetic-input feasibility, isolation safety).
  - §3.1 子节存在: Selection criteria (5 dimensions) —— (1) Round-table density (slice should exercise Phase 46 turn lifecycle + conflict arbitration), (2) Schema-field coverage (slice should exercise ≥80% of agents-schema.yaml 18 fields + memory-record-schema fields), (3) Decision coverage (slice should exercise 决策 1/3/5/6/7), (4) Synthetic-input feasibility (slice can run on synthetic StoryKernel without real client data), (5) Isolation safety (slice runs in ~/.hermes-poc/ without touching production).
  - §3.2 子节存在: Creative vertical slice = screenplay Step 3 round table. 论据完整: (a) decision #3 D2 storyboard-first-class (screenplay is D2-compatible creative phase), (b) ARCHITECTURE §2 screenplay row — HOOK-09 emotion_curve marker contract load-bearing (exercises persona_sha256 + lineage.transform_notes), 5 refs + 9 related_agents (densest round table among 15 experts, naturally exercises P7 memory conflict), tools include write_file/patch (exercises agent write-path), (c) Step 3 in V8.6 13-step pipeline is pure-creative (not in Phase 49 retained-phases allowlist, so not delegated via run_python_phase — fully round-table-driven), (d) synthetic StoryKernel input (creative_source Step 1 output format) is well-defined JSON, no real client data needed, (e) exercises 决策 1 (T6 MCP tool), 决策 3 (D2), 决策 5 (α form agent registry entry), 决策 6 (per-agent memory + curator), 决策 7 (Hermes控 turn_order).
  - §3.3 子节存在: Infra vertical slice = agent registry + 1 round table invocation. 论据完整: (a) decision #1 T6 协议 (Hermes MCP server extension — 7 MCP tool surface), (b) STACK §3.2 7 MCP tools (get_agent_persona / get_agent_opinion / get_agent_memory / submit_round_table_result / submit_artifact / query_memory / run_python_phase), (c) Phase 46 round-table-state-schema full lifecycle (round_table_open → turn N → submit), (d) Phase 45 agents-schema.yaml 18-field entry (transform via Phase 49 §2 rules), (e) exercises 决策 1 (T6), 决策 5 (α form), 决策 7 (Hermes控 round_table_open/max_rounds/early_stop_rule).
  - §3.4 子节存在: Why NOT other slices —— (a) Why not creative_source Step 1 (too upstream, no round table density — only 3 related_agents vs screenplay's 9), (b) Why not visual_executor Step 5 (heavy ComfyUI dependency, infra slice excludes long-running exec), (c) Why not full pipeline run (out of PoC scope per §2.4 budget), (d) Why not compliance_gate (hard-gate authority requires operator policy review, blocks PoC iteration). Each rejection cites 决策号 or research section.
  - §3.5 子节存在: Slice interaction —— creative slice depends on infra slice (screenplay round table needs agent registry + MCP tool wired). Sequence: infra first (~3 days setup), then creative (~2 days on top of infra). Total vertical slice setup ~5 person-days (within §2.4 budget).
  - §3.6 子节存在: Slice deliverables —— infra slice produces (1) 7 MCP tools wired in mcp_serve.py per STACK §5.2 pseudocode, (2) 1 sample agent YAML `~/.hermes-poc/agents/test-coordinator.agent.yaml` (minimal α form), (3) 1 successful `round_table_open` → `submit_round_table_result` cycle (synthetic input). Creative slice produces (1) 9-agent subset YAMLs (screenplay + 8 peers from related_agents), (2) 1 successful Step 3 screenplay round table on synthetic StoryKernel, (3) `fitness_trend.jsonl` baseline for screenplay (first data point for acceptance criterion #1 fitness battery).
  - Total Task 2 length ≥ 350 lines
  </behavior>

  <action>
  Append §2 + §3 to `.planning/research/v10-orchestrator-design/05-POC-PLAN.md`.

  **§2 — PoC Scope + Boundary**

  §2.0 (~25 lines): "本节定义 PoC 的边界 —— what 'PoC done' means (vertical slice + acceptance criteria pass + risk register verdicts 执行) + what is explicitly NOT in PoC (full 15-agent round table, mem0 物理分区, production rollout). 边界清晰的目的是避免 PoC scope creep —— v11.0 PoC 是验证 v10.0 设计的可行性, 不是 ship production."

  §2.1 PoC "done" 定义 (~50 lines):
    - (a) 2 vertical slices 跑通 (per §3)
    - (b) 7 acceptance criteria 全 pass (per §4)
    - (c) 7 risk register verdicts 执行: must-fix items (P1/P2/P4/P5/P7/P8) 在 PoC 内 fix; PARTIAL items (P6) 签名部分实施, outlier detection 监控
    - (d) Token cost 实测 ≈ STACK §7 估算 (within 2x of ~550K/pipeline run for full pipeline, or per-slice ~42K = 550K/13 for single round table)

  §2.2 PoC explicit exclusions (~50 lines): 5 项 exclusions (full 15-agent / 物理分区 / production ~/.hermes / live client / operator ship). Each with rationale.

  §2.3 PoC workspace 隔离 (~40 lines): `~/.hermes-poc/` dedicated. Independent mem0 workspace, session DB, logs. Phase 49 §5.5 legacy policy 不影响 PoC.

  §2.4 PoC duration + budget (~50 lines): 12 person-days acceptance criteria + 5 person-days vertical slice setup + 1-2 person-days risk register mitigation = ~15-17 person-days + 30-day shadow-run = ~6 calendar weeks. Cite Phase 49 §4.7 Step 5 shadow-run 30-day window.

  **§3 — Vertical Slice Selection (SC#2 Deep-Dive)**

  §3.0 (~30 lines): "本节是 ROADMAP SC#2 的完整论证. 2 vertical slices (creative + infra) each with 论据完整 non-arbitrary. Selection criteria 5 dimensions: (1) round-table density, (2) schema-field coverage, (3) decision coverage, (4) synthetic-input feasibility, (5) isolation safety. 每 slice 引用 决策号 + research section + edge-case rationale."

  §3.1 Selection criteria (~40 lines): 5-row table explaining each dimension + why it matters for PoC.

  §3.2 Creative vertical slice = screenplay Step 3 (~120 lines):
    - **Selection rationale** (5-dim scorecard): round-table density 9/10 (9 related_agents densest), schema-field coverage 90%+ (persona_sha256 + lineage + memory_scope + tools write_file + HOOK-09 contract), decision coverage 5/7 (1/3/5/6/7), synthetic-input feasibility high (StoryKernel JSON well-defined), isolation safety high (no real client data).
    - **Citations**: 决策 #3 D2 storyboard-first-class (screenplay is D2 creative), ARCHITECTURE §2 screenplay row (HOOK-09 emotion_curve marker contract load-bearing + 5 refs + 9 related_agents + tools [hermes_llm, read_file, search_files, write_file, patch]), Phase 49 §2.4 screenplay transform rules (agent YAML build procedure), 02-ROUND-TABLE-PROTOCOL.md §2 turn lifecycle + §3 conflict arbitration.
    - **Slice scope**: 9-agent subset (screenplay + 8 peers from ARCHITECTURE §2 screenplay related_agents column), 1 Step 3 round table on synthetic StoryKernel input, ~5 turns (per STACK §7.1 step 3 estimate), ~30 MCP calls, ~40K tokens.
    - **Edge case**: HOOK-09 emotion_curve marker contract — screenplay persona MUST surface this in lineage.transform_notes per Phase 49 §2.4. If PoC transform drops it, screenplay agent produces wrong-format output. This is the PoC's first "did the transform work?" smoke test.

  §3.3 Infra vertical slice = agent registry + 1 round table invocation (~100 lines):
    - **Selection rationale** (5-dim scorecard): round-table density N/A (this slice sets UP the round table, doesn't exercise creative conflict), schema-field coverage 100% (all 18 fields via 1 sample agent YAML), decision coverage 4/7 (1/5/6/7), synthetic-input feasibility high (test-coordinator.agent.yaml minimal), isolation safety high.
    - **Citations**: 决策 #1 T6 协议 (Hermes MCP server extension), STACK §3.2 7 MCP tool surface (cite each tool's purpose), STACK §5.2 mcp_serve.py patch pseudocode (implementation reference for v11.0), Phase 46 round-table-state-schema full lifecycle, Phase 45 agents-schema.yaml 18-field (test-coordinator.agent.yaml follows this), Phase 49 §2 transform procedure.
    - **Slice scope**: 7 MCP tools wired (get_agent_persona + get_agent_opinion + get_agent_memory + submit_round_table_result + submit_artifact + query_memory + run_python_phase — last one per Phase 49 §5.2 retained-phases allowlist), 1 sample agent YAML `~/.hermes-poc/agents/test-coordinator.agent.yaml`, 1 successful round_table_open → turn 1 → submit_round_table_result cycle (synthetic input).
    - **Edge case**: run_python_phase Step 6.5 storyboard assembly — infra slice may invoke this as part of creative slice's storyboard Step 6.5 prerequisite. Validates Phase 49 §5.2 allowlist + §5.4 dispatcher enforcement.

  §3.4 Why NOT other slices (~50 lines): 4 rejected alternatives, each with 决策号/research citation.

  §3.5 Slice interaction + sequence (~40 lines): Infra first (~3 days), creative on top (~2 days). Total ~5 person-days vertical slice setup.

  §3.6 Slice deliverables (~60 lines): Infra produces 3 artifacts (7 MCP tools wired + 1 sample agent YAML + 1 round table cycle). Creative produces 3 artifacts (9-agent subset YAMLs + 1 Step 3 round table on StoryKernel + fitness_trend.jsonl baseline for screenplay).

  Length target: ~350-400 lines for Task 2.
  </action>

  <verify>
    <automated>grep -qE "^## §2|^## 2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -qE "^## §3|^## 3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -cE "^### §2\.|^### 2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 4 {exit 0} {exit 1}' && grep -cE "^### §3\.|^### 3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 6 {exit 0} {exit 1}' && for term in "vertical slice" "creative" "infra" "screenplay" "Step 3" "agent registry" "round table" "storyboard" "HOOK-09" "emotion_curve" "StoryKernel" "9 related_agents\\|9-agent\\|9 agent" "decision 3\\|决策 3" "decision 1\\|决策 1" "decision 5\\|决策 5" "decision 6\\|决策 6" "decision 7\\|决策 7" "test-coordinator\\.agent\\.yaml\\|test-coordinator" "run_python_phase" "Step 6.5" "7 MCP tool\\|7 MCP tools\\|7 个 MCP" "~/.hermes-poc" "selection criteria" "schema-field coverage" "round-table density" "synthetic" "exclusion\\|out-of-scope\\|out of scope" "Phase 49 §2\\|Phase 49 §2.4" "Phase 46" "Phase 45" "STACK §3.2" "ARCHITECTURE §2" "fitness_trend.jsonl"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md || { echo "MISSING: $term"; exit 1; }; done && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 600 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §2 exists with §2.0-§2.4 (PoC scope boundary, 5 subsections)
  - §2.1 defines "PoC done" (4 conditions)
  - §2.2 declares 5 explicit exclusions
  - §2.3 specifies ~/.hermes-poc/ workspace isolation
  - §2.4 estimates ~15-17 person-days + 30-day shadow-run = ~6 calendar weeks
  - §3 exists with §3.0-§3.6 (vertical slice selection SC#2 deep-dive, 7 subsections)
  - §3.1 has 5-dimension selection criteria table
  - §3.2 has creative slice = screenplay Step 3 with 5-dim scorecard + citations + edge case (HOOK-09)
  - §3.3 has infra slice = agent registry + 1 round table with 5-dim scorecard + 7 MCP tool citations + edge case (run_python_phase Step 6.5)
  - §3.4 has 4 rejected alternatives with rationale
  - §3.5 has slice interaction + sequence (infra first, creative on top, ~5 person-days)
  - §3.6 has slice deliverables (3 + 3 artifacts)
  - Total doc length now ≥ 600 lines (cumulative with Task 1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §4 (7-item acceptance criteria checklist SC#3 deep-dive — fitness battery + latency SLO + bias canary + compaction + threshold tuning + dry-run-first + schema migration dry-run, each with 1-3 day estimate + task decomposition)</name>
  <files>.planning/research/v10-orchestrator-design/05-POC-PLAN.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/PITFALLS.md §P1 lines 31-62 (persona drift — mitigation 1 frozen battery + mitigation 2 periodic drift probe — load-bearing for acceptance criterion #1 fitness battery) + §P3 lines 95-124 (scoped retrieval perf — mitigation 4 latency SLO + read-path p95 monitoring) + §P5 lines 162-198 (curator failure modes — mitigation 4 bias canary + mitigation 5 dry-run-first + mitigation 6 bias audit log) + §P8 lines 267-300 (no fitness signal — mitigation 1 frozen fitness battery + mitigation 2 longitudinal trend + Label Studio 4 competencies) + §P9 lines 301-335 (memory size growth — mitigation 1 budget cap + mitigation 2 3-tier compaction) + §P13 lines 418-437 (curator loop runaway — adaptive thresholds) + §P14 lines 438-457 (schema migration — dry-run mode)
    - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md agents-schema.yaml (fitness_battery field + memory.max_records field) + memory-record-schema.yaml (evidence_chain + evidence_operator_ids + confidence + half_life_days + expires_at + verified_at + scope + status + schema_version fields)
    - .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md §4.5 dry-run migration mode (`hermes agent memory migrate --dry-run`) + §4.7 6-step migration execution (acceptance criterion #7 input)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Gaps to Address (line 172-178 — mem0 latency benchmark week-1, curator hallucination rate, contextvars)
    - .planning/research/v10-orchestrator-design/STACK.md §7.4 (line 1035-1046 — latency breakdown: stdio round-trip <5ms, LLM call 2-15s — load-bearing for latency SLO p95<500ms excluding LLM call)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §4 (强制串行 — GLM 4-key rotation compat, latency SLO assumes serial 1-panelist-1-turn)
  </read_first>

  <behavior>
  - §4.0 子节存在: 声明 §4 是 SC#3 的完整论证 —— 7 项 acceptance criteria + 每项 1-3 天工作量估算 = 12 person-days total. 每 item cite PITFALLS §PX mitigation + Phase 45 schema field + Phase 49/46 protocol reference.
  - §4.1 子节存在: Acceptance criterion #1 — Fitness battery design (3 days). Cite PITFALLS §P8 mitigation 1 (frozen battery per agent) + Label Studio 4 competencies (accurate retrieval / test-time learning / long-range understanding / conflict resolution) + Shaped.ai Recall@K + NDCG + GetMaxim 10K-trajectories rule (set expectation: Hermes scale = noisy signal). Task decomposition: (a) 0.5d design 10-20 prompt battery for screenplay agent (exercises HOOK-09 contract + emotion_curve marker + Snyder 15-beat), (b) 0.5d implement `hermes agent fitness <agent_id>` CLI wrapper (calls existing v6.0 gate.py:decide_verdict), (c) 1d run baseline + commit `fitness_trend.jsonl` schema (per PITFALLS §P8 mitigation 2: ts, battery_version, mean_score, per_prompt_scores, persona_sha256, model_id), (d) 1d regression auto-quarantine logic (mean_score drop > 0.5 across 3 runs triggers quarantine).
  - §4.2 子节存在: Acceptance criterion #2 — Latency SLO p95 < 500ms (2 days). Cite PITFALLS §P3 mitigation 4 (read-path latency SLO + observability) + Phase 48 Option B → 物理分区 migration trigger + SUMMARY §Gaps line 174 (mem0 Platform API 真实 filter 行为). Task decomposition: (a) 0.5d add `_latency_ms` field to memory retrieval log, (b) 0.5d implement stats CLI p50/p95/p99 per-agent, (c) 1d week-1 benchmark: 100 sequential get_agent_memory calls on test-coordinator.agent.yaml with 100/500/1000 records, measure p95. SLO excludes LLM-bound tool calls (per STACK §7.4 LLM call = 2-15s, not in 500ms budget). SLO scopes to mem0 scoped retrieval only (get_agent_memory + query_memory). Failure mode: p95 > 500ms triggers Phase 48 物理分区 evaluation (per SUMMARY §Gaps line 174).
  - §4.3 子节存在: Acceptance criterion #3 — Bias canary (2 days). Cite PITFALLS §P5 mitigation 4 (bias canary in eval gate) + SUMMARY §Gaps line 176 (curator `_memory_evolution_phase` hallucination rate). Task decomposition: (a) 0.5d design 5 bias-canary prompts (single-operator preference surfacing), (b) 0.5d implement `_check_evidence_coverage(new_memory, evidence_chain) -> bool` (PITFALLS §P5 mitigation 2, embedding cosine ≥ 0.7), (c) 0.5d implement `_check_operator_diversity(feedback_records, min_distinct_operators=2)` (PITFALLS §P5 mitigation 3), (d) 0.5d run curator `_memory_evolution_phase` in dry-run on synthetic 10-feedback-record input, verify bias canary rejects single-operator-derived insight.
  - §4.4 子节存在: Acceptance criterion #4 — Compaction pass (1 day). Cite PITFALLS §P9 (memory size growth) + SUMMARY OQ-7 (memory.max_records default 500). Task decomposition: (a) 0.3d enforce `agent.memory.max_records: 500` cap in dispatcher (reject write above cap), (b) 0.3d implement curator compaction pass trigger (every N ticks, N=10 default), (c) 0.4d run compaction on 600-record synthetic input, verify 3-tier (core ≤10 / working ≤100 / archival ≤10000) output structure per PITFALLS §P9 mitigation 2.
  - §4.5 子节存在: Acceptance criterion #5 — Threshold tuning (1 day). Cite PITFALLS §P13 (curator loop runaway). Task decomposition: (a) 0.3d document initial defaults (DEFAULT_FEEDBACK_THRESHOLD_COUNT=3 per v6.0, compaction trigger N=10, bias canary min_distinct_operators=2), (b) 0.3d document tuning path (each threshold has CLI override + audit log entry when changed), (c) 0.4d run curator with threshold=1 (overly aggressive) and verify audit log captures "threshold too low" warning + rate-limit kicks in per PITFALLS §P13 mitigation.
  - §4.6 子节存在: Acceptance criterion #6 — Dry-run-first invariant (1 day). Cite PITFALLS §P5 mitigation 5 (dry-run-first invariant) + SUMMARY OQ-16 (curator defaults to dry-run). Task decomposition: (a) 0.3d implement curator memory-write path dry-run default (requires explicit `--apply-memory` flag for live writes, mirrors v6.0 CURATOR_DRY_RUN_BANNER AST-walk pattern), (b) 0.3d implement `_check_non_bypassable_human_in_loop` test (mirrors v6.0 TestNonBypassableHumanInLoop), (c) 0.4d run curator in default mode, verify zero memory writes; run with `--apply-memory`, verify writes proceed with audit log.
  - §4.7 子节存在: Acceptance criterion #7 — Schema migration dry-run (2 days). Cite Phase 49 §4.5 (`hermes agent memory migrate --dry-run`) + PITFALLS §P14 mitigation 3. Task decomposition: (a) 0.5d implement dry-run migration script (reads v6.0 FeedbackStore JSONL buckets, no write), (b) 0.5d implement dry-run output plan (5 metrics per Phase 49 §4.5: total source count / per-field default fill rate / conflict count / estimated target storage / mapping warnings), (c) 0.5d run dry-run on synthetic 100-record FeedbackStore input, verify output plan correctness, (d) 0.5d run live migration on same input with backup-first (Phase 49 §4.7 Step 1), verify <1% retrieval discrepancy in shadow-run window.
  - §4.8 子节存在: Acceptance criteria summary table —— 7-row aggregate (criterion / cite / 1-3d estimate / acceptance check / pitfall gated). Total 12 person-days.
  - Total Task 3 length ≥ 350 lines
  </behavior>

  <action>
  Append §4 to `.planning/research/v10-orchestrator-design/05-POC-PLAN.md`.

  **§4 — 7-Item Acceptance Criteria Checklist (SC#3 Deep-Dive, 12 Person-days total)**

  §4.0 (~30 lines): "本节是 ROADMAP SC#3 的完整论证. 7 项 acceptance criteria + 每项 1-3 天工作量估算 = 12 person-days total. 每 item cite PITFALLS §PX mitigation + Phase 45 schema field + Phase 49 §4-§5 / Phase 46 protocol reference. 工作量估算基于 first-principles task decomposition (设计 + 实施 + 验证 三 phase per item), 不是 gut feel."

  §4.1-§4.7 (each ~50-60 lines, one per criterion): Each subsection follows the structure:
    - **Citations** (3-5 bullets): PITFALLS §PX + Phase 45/46/49 + SUMMARY OQ
    - **Task decomposition** (3-4 sub-tasks with day-fraction estimate, e.g. 0.5d / 1d)
    - **Acceptance check** (1 measurable condition, e.g. "p95 < 500ms on 500-record get_agent_memory benchmark")
    - **Failure mode** (what happens if acceptance fails — e.g. latency SLO fail → Phase 48 物理分区 evaluation)
    - **Pitfall gated** (which PITFALLS §PX this criterion validates the mitigation of)

  §4.1 Fitness battery design (3 days): cite PITFALLS §P8 mitigation 1 + Label Studio 4 competencies + Shaped.ai Recall@K. Task: design 10-20 prompt battery for screenplay + CLI wrapper + fitness_trend.jsonl baseline + regression auto-quarantine. Acceptance: fitness_trend.jsonl exists with ≥1 baseline entry for screenplay, regression logic triggers on synthetic mean_score drop > 0.5.

  §4.2 Latency SLO p95<500ms (2 days): cite PITFALLS §P3 mitigation 4 + Phase 48 Option B → 物理分区. Task: _latency_ms log field + stats CLI + 100/500/1000-record benchmark. Acceptance: p95 < 500ms on 500-record get_agent_memory (excluding LLM call). Failure → 物理分区 evaluation.

  §4.3 Bias canary (2 days): cite PITFALLS §P5 mitigation 4 + SUMMARY §Gaps line 176. Task: 5 bias-canary prompts + _check_evidence_coverage + _check_operator_diversity + curator dry-run rejection test. Acceptance: curator dry-run rejects single-operator-derived insight.

  §4.4 Compaction pass (1 day): cite PITFALLS §P9 + SUMMARY OQ-7. Task: max_records cap + compaction trigger + 600-record → 3-tier test. Acceptance: compaction pass output follows 3-tier structure.

  §4.5 Threshold tuning (1 day): cite PITFALLS §P13. Task: initial defaults doc + tuning path + threshold=1 runaway test. Acceptance: audit log captures runaway warning + rate-limit kicks in.

  §4.6 Dry-run-first invariant (1 day): cite PITFALLS §P5 mitigation 5 + SUMMARY OQ-16. Task: curator memory-write dry-run default + non-bypassable test + live mode --apply-memory flag. Acceptance: default mode zero writes, --apply-memory mode writes with audit log.

  §4.7 Schema migration dry-run (2 days): cite Phase 49 §4.5 + PITFALLS §P14 mitigation 3. Task: dry-run script + 5-metric output plan + 100-record dry-run + live migration with backup + <1% discrepancy. Acceptance: dry-run output plan correct + live migration shadow-run <1% discrepancy.

  §4.8 Summary table (~30 lines):
  | # | Criterion | Cite | Days | Acceptance check | Pitfall gated |
  |---|-----------|------|------|------------------|---------------|
  | 1 | Fitness battery | §P8 mitigation 1 + Label Studio | 3 | fitness_trend.jsonl baseline + regression logic | P1 + P8 |
  | 2 | Latency SLO p95<500ms | §P3 mitigation 4 | 2 | p95 on 500-record benchmark | P3 |
  | 3 | Bias canary | §P5 mitigation 4 | 2 | curator dry-run rejects single-operator insight | P5 |
  | 4 | Compaction pass | §P9 + OQ-7 | 1 | 3-tier output on 600-record | P9 |
  | 5 | Threshold tuning | §P13 | 1 | runaway warning + rate-limit | P13 |
  | 6 | Dry-run-first | §P5 mitigation 5 + OQ-16 | 1 | default mode zero writes | P5 |
  | 7 | Schema migration dry-run | Phase 49 §4.5 + §P14 mitigation 3 | 2 | <1% discrepancy | P14 |
  | **Total** | | | **12** | | |

  Length target: ~350-400 lines for Task 3.
  </action>

  <verify>
    <automated>grep -qE "^## §4|^## 4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -cE "^### §4\.|^### 4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 8 {exit 0} {exit 1}' && for term in "fitness battery" "fitness_battery" "Label Studio" "Shaped.ai\\|Shaped\\.ai" "GetMaxim\\|GetMaxim" "Recall@K\\|Recall@k" "latency SLO" "p95" "500ms" "bias canary" "compaction" "threshold tuning" "dry-run\\|dry run" "schema migration" "max_records\\|memory.max_records" "evidence_chain" "evidence_operator_ids" "_check_evidence_coverage" "_check_operator_diversity" "contextvars\\|context variable" "shadow\\|shadow-run\\|shadow run" "curator" "_memory_evolution_phase" "PITFALLS §P1\\b" "PITFALLS §P3\\b" "PITFALLS §P5\\b" "PITFALLS §P8\\b" "PITFALLS §P9\\b" "PITFALLS §P13\\b" "PITFALLS §P14\\b" "Phase 49 §4" "Phase 48" "Option B" "物理分区" "OQ-7\\|OQ-16\\|OQ-7" "STACK §7" "12 person-days\\|12 人日\\|12 person day"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md || { echo "MISSING: $term"; exit 1; }; done && grep -cE "days\\)|day\\)|天\\)|日\\)|= [0-9]+ *d|= [0-9]+ *day" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 950 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §4 exists with §4.0-§4.8 (7-item acceptance criteria SC#3 deep-dive + summary table, 9 subsections)
  - §4.1 fitness battery cites PITFALLS §P8 mitigation 1 + Label Studio + Shaped.ai + GetMaxim, 3-day decomposition
  - §4.2 latency SLO cites PITFALLS §P3 mitigation 4 + Phase 48 Option B → 物理分区, 2-day decomposition, p95<500ms on 500-record benchmark
  - §4.3 bias canary cites PITFALLS §P5 mitigation 4 + SUMMARY §Gaps line 176, 2-day decomposition, curator dry-run rejection
  - §4.4 compaction cites PITFALLS §P9 + SUMMARY OQ-7 memory.max_records default 500, 1-day, 3-tier output
  - §4.5 threshold tuning cites PITFALLS §P13, 1-day, runaway warning
  - §4.6 dry-run-first cites PITFALLS §P5 mitigation 5 + SUMMARY OQ-16, 1-day, default mode zero writes
  - §4.7 schema migration dry-run cites Phase 49 §4.5 + PITFALLS §P14 mitigation 3, 2-day, <1% shadow discrepancy
  - §4.8 has summary table with 7 rows totaling 12 person-days
  - Total doc length now ≥ 950 lines (cumulative with Tasks 1+2)
  </done>
</task>

<task type="auto">
  <name>Task 4: Write §5 (Risk register SC#4 deep-dive — 7 load-bearing pitfalls P1/P2/P4/P5/P6/P7/P8 × must-fix-in-PoC vs defer-with-monitoring verdict + rationale)</name>
  <files>.planning/research/v10-orchestrator-design/05-POC-PLAN.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Risk Register Summary (line 470-488 — CANONICAL SOURCE for 7-row risk register verdicts) + §P1 (persona drift full mitigations 1-4) + §P2 (stale memory full mitigations 1-5) + §P4 (cross-project leakage mitigations 1-5) + §P5 (curator failure modes mitigations 1-6) + §P6 (memory poisoning mitigations 1-6) + §P7 (round-table conflict mitigations 1-5) + §P8 (no fitness signal mitigations 1-5)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Risk Register (line 145-160 — secondary canonical source, aligns pitfalls with 必须字段/机制 + 在哪文档解决)
    - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md agents-schema.yaml (P1 mitigation 1 persona_sha256 + P5 mitigation 5 dry-run-first + P8 mitigation 1 fitness_battery) + memory-record-schema.yaml (P2 expires_at/verified_at/confidence/half_life_days/supersedes_memory_id + P4 scope/project_id + P5 evidence_chain/evidence_operator_ids/status + P6 status=quarantined)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §3 (P7 coordinator arbitration + scope precedence + confidence-weighted voting + conflict log)
    - .planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md §3 (P3/P12 mem0 物理分区 migration trigger, related context for P6 operator key management + P4 cross-project)
  </read_first>

  <behavior>
  - §5.0 子节存在: 声明 §5 是 SC#4 的完整论证 —— 7 load-bearing pitfalls × PoC deferral verdict. Verdicts 与 PITFALLS §Risk Register Summary (line 470-488) + SUMMARY §Risk Register (line 145-160) 对齐 (no divergent verdicts). 每 pitfall: (a) one-line risk summary, (b) full mitigation citation from PITFALLS, (c) PoC verdict (must-fix-in-PoC / PARTIAL / defer-with-monitoring), (d) PoC-week acceptance criterion (which §4 item gates this mitigation), (e) failure mode if deferred.
  - §5.1 子节存在: P1 Persona drift — must-fix-in-PoC. Cite PITFALLS §P1 mitigation 1 (persona_sha256 frozen at registration) + mitigation 2 (periodic drift probe, every 50 runs). PoC acceptance: §4.1 fitness battery gates this (regression auto-quarantine triggers on persona drift detected via mean_score drop). Failure if deferred: agent silently becomes generic helper, HOOK-09 contract lost, all downstream screenplay outputs wrong format.
  - §5.2 子节存在: P2 Stale memory — must-fix-in-PoC. Cite PITFALLS §P2 mitigation 1 (expires_at TTL, default 90d for domain rules) + mitigation 4 (supersedes_memory_id). PoC acceptance: §4.4 compaction pass exercises archival/quarantine of stale records (implicitly validates TTL filter in retrieve path). Failure if deferred: agent cites obsolete platform rules, operator loses trust.
  - §5.3 子节存在: P4 Cross-project leakage — must-fix-in-PoC. Cite PITFALLS §P4 mitigation 1 (scope global|project|session) + mitigation 4 (project_id required in retrieve call). PoC acceptance: §4.7 schema migration dry-run populates scope=project + project_id default per Phase 49 §4.3 mapping table; §4.2 latency benchmark uses agent_id+project_id filter. Failure if deferred: PoC single-project won't surface this, but production rollout immediately leaks Project A learning into Project B (as SUMMARY §Gaps line 176 implicitly warns).
  - §5.4 子节存在: P5 Curator failure modes — must-fix-in-PoC. Cite PITFALLS §P5 mitigation 2 (evidence_chain ≥3 coverage check) + mitigation 3 (operator diversity) + mitigation 4 (bias canary) + mitigation 5 (dry-run-first). PoC acceptance: §4.3 bias canary + §4.6 dry-run-first invariant both gate this. Failure if deferred: curator silently writes hallucinated rules, agent behavior degrades over time with no signal.
  - §5.5 子节存在: P6 Memory poisoning — PARTIAL (signed feedback PoC must, outlier detection defer). Cite PITFALLS §P6 mitigation 1 (operator_signature HMAC) + mitigation 2 (outlier detection). PoC verdict: PARTIAL — operator_signature schema field implemented (acceptance: FeedbackRecord validates signature, anonymous rejected); outlier detection deferred to v11.1+ with monitoring (audit log captures operator_id distribution, manual review trigger if >2σ deviation). Rationale: PoC has 1-2 trusted operators, poisoning risk is theoretical; outlier detection needs >10 operators to be statistically meaningful.
  - §5.6 子节存在: P7 Round-table memory conflict — must-fix-in-PoC. Cite PITFALLS §P7 mitigation 2 (coordinator arbitrates) + mitigation 3 (scope precedence session>project>global) + mitigation 4 (confidence-weighted voting). PoC acceptance: §3.2 creative slice screenplay Step 3 round table with 9 related_agents naturally exercises conflict (9 agents with different memory will disagree on screenplay decisions). Failure if deferred: round table deadlocks, PoC creative slice cannot complete.
  - §5.7 子节存在: P8 No fitness signal — must-fix-in-PoC. Cite PITFALLS §P8 mitigation 1 (frozen fitness battery per agent) + mitigation 2 (longitudinal fitness_trend.jsonl) + mitigation 3 (A/B shadow mode). PoC acceptance: §4.1 fitness battery gates this (this is THE acceptance criterion for P8). Failure if deferred: no regression signal, operator cannot tell if PoC changes are improvements or regressions, all subsequent v11.x work operates blind.
  - §5.8 子节存在: Risk register summary table —— 7-row aggregate (P# / severity / verdict / PoC §4 acceptance / failure-if-deferred / mitigation cost). Aligned with PITFALLS §Risk Register Summary + SUMMARY §Risk Register.
  - §5.9 子节存在: Verdict 对齐声明 —— 显式声明本 doc 7 verdicts 与 PITFALLS §Risk Register Summary (line 470-488) + SUMMARY §Risk Register (line 145-160) 一致 (no divergence). 列出对齐表 (本 doc verdict × PITFALLS verdict × SUMMARY verdict). 若有偏差 (none expected), 显式标注 rationale.
  - §5.10 子节存在: Defer-with-monitoring items 监控计划 —— P6 PARTIAL outlier detection defer + (P3 latency SLO defer-with-monitoring per PITFALLS, but P3 not in SC#4 7-pitfall scope so listed as related context). 监控: audit log 捕获 operator_id distribution, manual review if >2σ deviation; latency §4.2 benchmark runs weekly during PoC.
  - Total Task 4 length ≥ 350 lines
  </behavior>

  <action>
  Append §5 to `.planning/research/v10-orchestrator-design/05-POC-PLAN.md`.

  **§5 — 7-Row Risk Register (SC#4 Deep-Dive, P1/P2/P4/P5/P6/P7/P8 × PoC Verdict)**

  §5.0 (~30 lines): "本节是 ROADMAP SC#4 的完整论证. 7 load-bearing pitfalls × PoC deferral verdict. Verdicts 与 PITFALLS §Risk Register Summary (line 470-488) + SUMMARY §Risk Register (line 145-160) 对齐 (no divergent verdicts). 每 pitfall: one-line risk + full mitigation citation + PoC verdict + §4 acceptance criterion gating it + failure-if-deferred."

  §5.1-§5.7 (each ~40 lines, one per pitfall): Each subsection follows the structure:
    - **Risk one-liner** (1 sentence from PITFALLS §PX opening)
    - **Mitigation citations** (3-5 bullets from PITFALLS §PX mitigations + Phase 45 schema fields)
    - **PoC verdict**: must-fix-in-PoC / PARTIAL / defer-with-monitoring
    - **PoC-week acceptance**: §4.X item gates this
    - **Failure-if-deferred**: 1 sentence on what breaks if deferred
    - **Mitigation cost**: L/M/H (aligns with PITFALLS §Risk Register Summary column)

  §5.1 P1 Persona drift (must-fix): persona_sha256 + drift probe. §4.1 gates. Failure: HOOK-09 contract lost. Cost M.
  §5.2 P2 Stale memory (must-fix): TTL + verified_at + supersedes_memory_id. §4.4 gates (compaction validates TTL filter). Failure: obsolete platform rules cited. Cost M.
  §5.3 P4 Cross-project leakage (must-fix): scope global|project|session + project_id required. §4.7 + §4.2 gate. Failure: production rollout leaks P1→P2. Cost M.
  §5.4 P5 Curator failure modes (must-fix): evidence_chain + operator diversity + bias canary + dry-run-first. §4.3 + §4.6 gate. Failure: hallucinated rules degrade agent. Cost M.
  §5.5 P6 Memory poisoning (PARTIAL): operator_signature implemented, outlier detection defer. §4.6 dry-run-first gates signature path; outlier deferred with audit-log monitoring. Failure-if-deferred: PoC trusted operators, theoretical risk. Cost H for full, M for PARTIAL.
  §5.6 P7 Round-table conflict (must-fix): coordinator arbitration + scope precedence + confidence voting. §3.2 creative slice screenplay 9-agent round table gates. Failure: round table deadlocks. Cost M.
  §5.7 P8 No fitness signal (must-fix): fitness battery + fitness_trend.jsonl + A/B shadow. §4.1 gates. Failure: no regression signal, all v11.x work blind. Cost M.

  §5.8 Summary table (~50 lines):
  | P# | Pitfall | Severity | Verdict | PoC §4 acceptance | Failure-if-deferred | Cost |
  |----|---------|----------|---------|--------------------|----------------------|------|
  | P1 | Persona drift | HIGH | must-fix | §4.1 fitness battery | HOOK-09 contract lost | M |
  | P2 | Stale memory | HIGH | must-fix | §4.4 compaction | Obsolete rules cited | M |
  | P4 | Cross-project leakage | HIGH | must-fix | §4.7 + §4.2 | P1→P2 production leak | M |
  | P5 | Curator failure modes | HIGH | must-fix | §4.3 + §4.6 | Hallucinated rules | M |
  | P6 | Memory poisoning | HIGH | PARTIAL | §4.6 signature; outlier defer | Theoretical (trusted ops) | M (PARTIAL) |
  | P7 | Round-table conflict | MEDIUM | must-fix | §3.2 creative slice | Round table deadlocks | M |
  | P8 | No fitness signal | HIGH | must-fix | §4.1 fitness battery | No regression signal | M |

  §5.9 Verdict 对齐声明 (~50 lines): "本 doc 7 verdicts 与 PITFALLS §Risk Register Summary (line 470-488) + SUMMARY §Risk Register (line 145-160) 一致: P1/P2/P4/P5/P7/P8 = NO (load-bearing, must-fix-in-PoC); P6 = PARTIAL (signed feedback PoC must, outlier detection defer). 无偏差."

  §5.10 Defer-with-monitoring 监控计划 (~40 lines): P6 outlier detection defer (audit log operator_id distribution, manual review trigger if >2σ deviation; needs >10 operators to be statistically meaningful). P3 latency SLO (not in SC#4 7-pitfall scope but related context — §4.2 benchmark runs weekly).

  Length target: ~350-400 lines for Task 4.
  </action>

  <verify>
    <automated>grep -qE "^## §5|^## 5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -cE "^### §5\.|^### 5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 10 {exit 0} {exit 1}' && for term in "must-fix" "defer-with-monitoring\\|defer with monitoring" "PARTIAL" "P1\\b.*Persona\\|Persona drift" "P2\\b.*Stale\\|Stale memory" "P4\\b.*Cross-project\\|Cross-project leakage\\|cross-project leakage" "P5\\b.*Curator\\|Curator failure" "P6\\b.*Memory poisoning\\|Memory Poisoning\\|poisoning" "P7\\b.*Round-table\\|round-table conflict\\|Round-Table Memory Conflict" "P8\\b.*fitness\\|No fitness" "persona_sha256" "expires_at" "verified_at" "supersedes_memory_id" "evidence_chain" "evidence_operator_ids" "operator_signature" "coordinator" "arbitration" "scope precedence" "confidence-weighted\\|confidence weighted" "audit log\\|audit-log" "outlier" "2σ\\|2 sigma\\|2\\\\sigma" "PITFALLS §Risk Register\\|Risk Register Summary" "SUMMARY.*Risk Register" "aligned\\|对齐" "no divergent\\|无偏差"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^### §5\.8/,/^### §5\.9/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | grep -cE "P[124568]" | awk '$1 >= 7 {exit 0} {exit 1}' && awk '/^### §5\.8/,/^### §5\.9/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | grep -cE "must-fix|PARTIAL" | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 1100 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §5 exists with §5.0-§5.10 (7-row risk register SC#4 deep-dive + summary + verdict 对齐 + defer-with-monitoring, 11 subsections)
  - §5.1 P1 must-fix (persona_sha256 drift probe, §4.1 gates)
  - §5.2 P2 must-fix (TTL + verified_at + supersedes, §4.4 gates)
  - §5.3 P4 must-fix (scope + project_id, §4.7 + §4.2 gate)
  - §5.4 P5 must-fix (evidence_chain + bias canary + dry-run-first, §4.3 + §4.6 gate)
  - §5.5 P6 PARTIAL (operator_signature must, outlier detection defer)
  - §5.6 P7 must-fix (coordinator arbitration + scope precedence + confidence voting, §3.2 creative slice gates)
  - §5.7 P8 must-fix (fitness battery + fitness_trend.jsonl, §4.1 gates)
  - §5.8 summary table has 7 rows with verdicts aligned to PITFALLS §Risk Register Summary + SUMMARY §Risk Register
  - §5.9 explicitly declares verdict alignment (no divergent verdicts)
  - §5.10 defer-with-monitoring plan for P6 outlier + P3 latency context
  - Total doc length now ≥ 1100 lines (cumulative with Tasks 1+2+3)
  </done>
</task>

<task type="auto">
  <name>Task 5: Write §6 (PoC implementation path SC#5 deep-dive — fitness battery → schema migration dry-run → bias canary sequence with STACK §7 token cost scoping) + §7 (Phase 44 7 决策 cross-validation audit + OQ/P14 resolution audit) + §8 (downstream citation guide + coherence 声明 + References)</name>
  <files>.planning/research/v10-orchestrator-design/05-POC-PLAN.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/STACK.md §7.1 (line 991-1003 — 340 MCP call/pipeline run, 13-step pipeline, average 4 expert × 2.5 turns × 2 call/expert/turn) + §7.2 (line 1005-1015 — per-tool token cost) + §7.3 (line 1017-1027 — total ~550K tokens/pipeline run, ~520K LLM-level, ~34K MCP overhead) + §7.4 (line 1035-1046 — latency breakdown) + §7.5 (line 1047-1056 — v11.0 PoC 建议串行 no batch)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1-§2.7 (decisions 1-7 — root arguments to cross-validate) + §2.6 specifically (决策 6 per-agent memory — load-bearing for entire PoC) + §2.7 (决策 7 Hermes控 structure — load-bearing for fitness battery + bias canary gating)
    - .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md §7 (Phase 49 downstream citation card pattern — apply here) + §6 (Phase 49 7 决策 audit table pattern — apply here)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Gaps to Address (line 172-178 — PoC measurable gaps to surface in implementation path: mem0 latency week-1, GLM 4-key first multi-round, curator hallucination, round table CC-crash gc, contextvars) + §Risk Register (line 145-160)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Risk Register Summary (line 470-488) + §Industry Reference Summary (line 491-509 — Label Studio / Shaped.ai / GetMaxim / ReConcile / MINJA)
  </read_first>

  <behavior>
  - §6.0 子节存在: 声明 §6 是 SC#5 的完整论证 —— PoC 实施路径 (fitness battery → schema migration dry-run → bias canary sequence) + STACK §7 token cost scoping. Sequence rationale explicit.
  - §6.1 子节存在: Sequence rationale —— why fitness battery FIRST (regression-detection foundation must exist before any curator tick, otherwise no signal to detect regression from later changes), why schema migration dry-run SECOND (validates memory layer data integrity before live writes, P14 mitigation), why bias canary THIRD (gates curator `_memory_evolution_phase` transition from dry-run to live, needs fitness battery + clean memory layer as preconditions).
  - §6.2 子节存在: Per-vertical-slice token cost scoping (cite STACK §7.3 ~550K/pipeline run ÷ 13 steps ≈ 42K/single round table). Creative slice screenplay Step 3 round table: 9 agents × 2.5 turns × 2 call/expert/turn = ~45 MCP call × 100 token overhead + ~22 LLM opinion call × 6500 token = ~4.5K + ~143K = ~148K tokens (higher than average because screenplay has 9 related_agents vs 4 average). Infra slice 1 round table invocation: 3 agents × 1 turn × 2 call = ~6 MCP + ~3 LLM opinion = ~10K tokens total.
  - §6.3 子节存在: PoC calendar timeline —— Week 1: infra slice setup (~3 days) + §4.2 latency SLO benchmark (P3 mitigation validation, may trigger Phase 48 物理分区 evaluation). Week 2: creative slice setup (~2 days) + §4.1 fitness battery baseline (P1/P8 mitigation). Week 3: §4.7 schema migration dry-run + live migration on synthetic input (P14 mitigation). Week 4: §4.3 bias canary + §4.6 dry-run-first (P5 mitigation). Week 5: §4.4 compaction + §4.5 threshold tuning (P9/P13 mitigation). Week 6: 30-day shadow-run window closes (per Phase 49 §4.7 Step 5), acceptance criteria re-run, PoC report.
  - §6.4 子节存在: GLM 4-key rotation 实测点 —— first multi-round creative slice round table (week 2) exposes real concurrency cost. STACK §7.5 建议串行 no batch (1 panelist 1 turn 顺序 await). MEMORY.md `feedback-glm-overload-reduce-concurrency.md` global concurrency==1 already in force. If creative slice round table triggers 4-key ceiling → early-abort per existing 3-strike policy.
  - §6.5 子节存在: PoC exit criteria —— (a) all 7 §4 acceptance criteria pass, (b) all 6 must-fix risk register items (P1/P2/P4/P5/P7/P8) mitigations verified, (c) P6 PARTIAL (operator_signature implemented, outlier detection deferred with monitoring), (d) 30-day shadow-run <1% retrieval discrepancy, (e) PoC report committed to `.planning/milestones/v11.0-POC-REPORT.md` (note: not v10.0, since v11.0 is the PoC milestone).
  - §6.6 子节存在: PoC risks beyond risk register —— (a) GLM 4-key ceiling may block creative slice multi-round, (b) mem0 Platform API latency may exceed SLO (trigger 物理分区 early), (c) curator LLM aggregation hallucination rate higher than expected (bias canary may reject most proposals, blocking evolution), (d) round table CC-crash recovery gc pass not yet designed (SUMMARY §Gaps line 177), (e) contextvars vs threading.local must pick contextvars (SUMMARY §Gaps line 178). Each risk has monitoring + mitigation pointer.
  - §7.0 子节存在: 声明 §7 是 Phase 44 决策 1-7 的 cross-validation audit (mirror Phase 49 §6 pattern)
  - §7.1 子节存在: 7-row 决策 audit table (决策 1-7 × 本 doc 实现位置 × 是否一致 + citation). 特别关注 决策 6 (per-agent memory — 整个 PoC scope 的 root) + 决策 7 (Hermes控 structure — fitness battery + bias canary gating).
  - §7.2 子节存在: 偏差分析 (预期 = 无偏差, 显式声明)
  - §7.3 子节存在: OQ + pitfall resolution 声明 —— OQ-7 (memory.max_records default 500 — resolved in §4.4), OQ-16 (curator dry-run-by-default — resolved in §4.6). P1/P2/P4/P5/P6/P7/P8 PoC verdicts resolved in §5 (aligned with PITFALLS §Risk Register Summary + SUMMARY §Risk Register). P14 PoC verdict resolved in §4.7 (acceptance criterion #7).
  - §8.0 子节存在: downstream citation guide —— Phase 51 VALIDATE 引用本 doc 时应从哪个章节取 (§5.8 risk register table for pitfall coverage check + §7.1 7 决策 audit for decision consistency check + §4.8 acceptance criteria summary for DESIGN-06 deliverable check)
  - §8.1 子节存在: coherence 声明 (vertical slice + 7-item acceptance + 7-row risk register + implementation path + 7 决策 audit = 完整 SC#1-5 coverage)
  - §8.2 子节存在: References (00-FIRST-PRINCIPLES + 01-AGENT-REGISTRY-SCHEMA + 02-ROUND-TABLE-PROTOCOL + 03-COMPARISON-VS-KIMI-MCP-SHIM + 04-MIGRATION-PATH + 06-CROSS-REPO-IMPACT + ARCHITECTURE + FEATURES + STACK + PITFALLS + SUMMARY + MEMORY.md v6-self-evolution-milestone + CLAUDE.md + Phase 44-49 commit references)
  - Total Task 5 length ≥ 250 lines
  </behavior>

  <action>
  Append §6 + §7 + §8 to `.planning/research/v10-orchestrator-design/05-POC-PLAN.md`.

  **§6 — PoC Implementation Path (SC#5 Deep-Dive, fitness battery → schema migration dry-run → bias canary)**

  §6.0 (~25 lines): "本节是 ROADMAP SC#5 的完整论证. STACK §7.3 已给 ~550K tokens/pipeline run 估算. 本节给出 PoC 实施路径 (fitness battery → schema migration dry-run → bias canary sequence) + per-vertical-slice token cost scoping + 6-week calendar timeline."

  §6.1 Sequence rationale (~50 lines):
    - **Fitness battery FIRST** (~Week 2): regression-detection foundation. Must exist before any curator tick. Without it, later changes (memory migration, bias canary gating) have no signal to detect regression. Builds on v6.0 gate.py:decide_verdict pattern.
    - **Schema migration dry-run SECOND** (~Week 3): validates memory layer data integrity before live writes. P14 mitigation. Needs fitness battery as regression-detection backstop during the migration.
    - **Bias canary THIRD** (~Week 4): gates curator `_memory_evolution_phase` transition from dry-run to live. Needs fitness battery (regression detection) + clean memory layer (P14-migrated data) as preconditions.

  §6.2 Per-vertical-slice token cost (~50 lines): cite STACK §7.3 ~550K/13 ≈ 42K/single round table average. Creative slice screenplay Step 3 (9 related_agents, denser): ~148K tokens (~4.5K MCP overhead + ~143K LLM opinion). Infra slice 1 round table invocation (3 agents): ~10K tokens. Both well within GLM 4-key × 200K TPM ≈ 800K TPM ceiling for serial execution.

  §6.3 PoC calendar timeline (~80 lines): Week 1-6 breakdown.
    - Week 1: infra slice setup (~3d: 7 MCP tools wire + test-coordinator.agent.yaml + 1 round_table cycle) + §4.2 latency SLO benchmark (P3 mitigation)
    - Week 2: creative slice setup (~2d: 9-agent subset YAMLs + Step 3 round table on synthetic StoryKernel) + §4.1 fitness battery baseline (P1/P8 mitigation)
    - Week 3: §4.7 schema migration dry-run (2d) + live migration with backup + 30-day shadow-run window opens
    - Week 4: §4.3 bias canary (2d) + §4.6 dry-run-first invariant (1d) → curator `_memory_evolution_phase` transitions from dry-run to gated-live
    - Week 5: §4.4 compaction pass (1d) + §4.5 threshold tuning (1d)
    - Week 6: shadow-run window closes, acceptance criteria re-run, PoC report committed to `.planning/milestones/v11.0-POC-REPORT.md`

  §6.4 GLM 4-key rotation 实测点 (~30 lines): Week 2 first multi-round creative slice exposes real concurrency cost. Cite MEMORY.md `feedback-glm-overload-reduce-concurrency.md` global concurrency==1. 3-strike early-abort policy in force.

  §6.5 PoC exit criteria (~40 lines): 5 conditions (7 acceptance pass + 6 must-fix verified + P6 PARTIAL + shadow-run <1% + v11.0-POC-REPORT.md committed).

  §6.6 PoC risks beyond risk register (~50 lines): 5 risks (GLM ceiling / mem0 latency / curator hallucination / CC-crash gc / contextvars). Each with monitoring + mitigation pointer.

  **§7 — Phase 44 7 决策 Cross-Validation Audit + OQ/Pitfall Resolution**

  §7.0 (~20 lines): "本节 audit 本 doc 的 vertical slice + acceptance + risk register + implementation path 是否一致支持 Phase 44 锁定的 7 决策. 预期 = 7/7 一致 (本 doc 是 PoC 蓝图, 不是 re-derivation). 同时声明 OQ-7 + OQ-16 + 7 pitfall PoC verdicts 的 resolution 位置."

  §7.1 7-row 决策 audit table (~80 lines):
  | 决策 # | 决策 (一句话) | 本 doc 实现位置 | 一致? | Citation |
  |--------|---------------|------------------|-------|----------|
  | 1 | T6 协议 (Hermes MCP server + tmux dispatch + CC native MCP client) | §3.3 infra slice (7 MCP tool wire-up + round_table lifecycle) + §4.2 latency SLO (measures MCP tool round-trip + mem0 retrieval) | ✅ | STACK §3.2 + Phase 44 §2.1 |
  | 2 | B3a Python runner 增量迁移 (delegate-only phase 迁 CC) | §3.3 infra slice edge case (run_python_phase Step 6.5 invocation) + Phase 49 §5.2 retained-phases allowlist (cite-only) | ✅ | STACK §3.2 Tool 7 + Phase 44 §2.2 + Phase 49 §5.2 |
  | 3 | D2 storyboard-first-class | §3.2 creative slice (screenplay Step 3 is D2-compatible, exercises HOOK-09 contract) | ✅ | ARCHITECTURE §2 + Phase 44 §2.3 |
  | 4 | G2 通用编排框架 | §3 vertical slice selection (screenplay is one sample of G2 runtime; infra slice sets up G2 dispatch path) | ✅ | ARCHITECTURE §6 + Phase 44 §2.4 |
  | 5 | α agent form (YAML + persona + tools + refs + memory_scope + lineage) | §3.3 infra slice (test-coordinator.agent.yaml is α form entry) + §3.2 creative slice (9-agent subset YAMLs all α form) | ✅ | Phase 45 agents-schema.yaml 18 fields + Phase 44 §2.5 |
  | 6 | per-agent memory + curator-driven 自进化 | §4 entire (7 acceptance criteria all test per-agent memory facet) + §5 entire (7-row risk register all per-agent memory pitfalls) + §6.1 fitness battery first sequence (regression-detection for curator evolution) | ✅ | Phase 45 memory-record-schema.yaml + ARCHITECTURE §3 + Phase 44 §2.6 |
  | 7 | 分层 CC 角色 (Hermes 控 structure, CC 控 内容) | §4.1 fitness battery (Hermes-side enforcement, regression auto-quarantine) + §4.3 bias canary (Hermes-side curator gating) + §4.6 dry-run-first (Hermes-side non-bypassable invariant) | ✅ | STACK §3.2 + Phase 44 §2.7 |

  §7.2 偏差分析 (~25 lines): 7/7 一致. 特别声明: 决策 6 (per-agent memory) 是本 doc 的 root argument —— 整个 PoC scope 都是验证决策 6 的可行性. 决策 7 (Hermes控 structure) 是 §4.1/§4.3/§4.6 acceptance criteria 的 enforcement 方向 (Hermes-side, not CC-side).

  §7.3 OQ + pitfall resolution 声明 (~50 lines):
  - **OQ-7 (memory.max_records 上限 500 够不够? compaction 何时触发?): RESOLVED in §4.4.** Default 500, curator 每 N=10 tick 跑 compaction pass. PoC acceptance: compaction on 600-record synthetic input produces valid 3-tier output.
  - **OQ-16 (curator `_memory_evolution_phase` dry-run-by-default?): RESOLVED in §4.6.** Default dry-run, requires `--apply-memory` for live writes. Non-bypassable AST-walk test (mirrors v6.0 TestNonBypassableHumanInLoop).
  - **P1/P2/P4/P5/P7/P8 (6 must-fix pitfalls): PoC verdicts RESOLVED in §5.1/§5.2/§5.3/§5.4/§5.6/§5.7.** All must-fix-in-PoC, aligned with PITFALLS §Risk Register Summary + SUMMARY §Risk Register.
  - **P6 (memory poisoning): PoC verdict PARTIAL RESOLVED in §5.5.** operator_signature implemented; outlier detection deferred with audit-log monitoring.
  - **P14 (schema migration): PoC verdict RESOLVED in §4.7.** Acceptance criterion #7 (dry-run + live migration + <1% shadow discrepancy).
  - **Cite SUMMARY.md OQ table + PITFALLS §Risk Register Summary as resolution audit source.**

  **§8 — Downstream Citation Guide + Coherence + References**

  §8.0 downstream citation card table (~40 lines):
  | Downstream consumer | Cite from this doc | Do NOT re-derive | Should derive |
  | v11.0 PoC implementer | §3 vertical slice + §4 acceptance + §5 risk register + §6 implementation path (entire doc is the blueprint) | Phase 44 决策, Phase 45/46/49 schema/protocol, PITFALLS mitigations | Live implementation code, real test data, production deployment |
  | Phase 51 VALIDATE | §5.8 risk register summary (pitfall coverage check) + §7.1 7 决策 audit (decision consistency) + §4.8 acceptance summary (DESIGN-06 deliverable check) + §7.3 OQ/pitfall resolution declarations | entire doc | cross-doc lint extensions for PoC-acceptance-criterion references |
  | Operator (Kai) | §1.6 capstone-at-a-glance (30-second answer) + §2.4 budget (~15-17 person-days + 6 weeks) + §5.8 risk register (which pitfalls must fix) | — | Operator approval to proceed to v11.0 PoC milestone |

  §8.1 coherence 声明 (~30 lines): "Vertical slice selection (§3) + 7-item acceptance criteria checklist with 12-person-day estimate (§4) + 7-row risk register (§5) + PoC implementation path (§6) + Phase 44 7 决策 cross-validation audit (§7) + OQ-7/OQ-16/P1-P8/P14 resolution declarations = 完整 ROADMAP SC#1-5 coverage. v11.0 PoC implementer 引用本 doc 即可 defending 'what do I build first? what does done look like? which pitfalls can I defer?', 不需重新推导 Phase 44 决策或 re-litigate pitfall mitigations."

  §8.2 References (~30 lines): 00-FIRST-PRINCIPLES.md §2.1-§2.7 (esp §2.6 决策 6 per-agent memory + §2.7 决策 7 Hermes控 structure) + 01-AGENT-REGISTRY-SCHEMA.md (agents-schema.yaml 18 fields + memory-record-schema.yaml) + 02-ROUND-TABLE-PROTOCOL.md (turn lifecycle + conflict arbitration) + 03-COMPARISON-VS-KIMI-MCP-SHIM.md (T6 vs Kimi context) + 04-MIGRATION-PATH.md §4-§5 (memory migration + retained-phases) + 06-CROSS-REPO-IMPACT.md §3 (Option B vs 物理分区) + ARCHITECTURE.md §1-§6 + FEATURES.md §10-§11 + STACK.md §3.2/§7 (7 MCP tool + token cost) + PITFALLS.md §P1/§P2/§P4/§P5/§P6/§P7/§P8/§P14 + §Risk Register Summary + SUMMARY.md §Risk Register + §Gaps to Address + MEMORY.md v6-self-evolution-milestone (fitness battery precedent) + CLAUDE.md (deliverable form canon) + Phase 44-49 commit references.

  Length target: ~250-300 lines for Task 5.
  </action>

  <verify>
    <automated>grep -qE "^## §6|^## 6\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -qE "^## §7|^## 7\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -qE "^## §8|^## 8\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md && grep -cE "^### §6\.|^### 6\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -cE "^### §7\.|^### 7\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 3 {exit 0} {exit 1}' && grep -cE "^### §8\.|^### 8\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 2 {exit 0} {exit 1}' && for term in "fitness battery" "schema migration" "bias canary" "sequence" "Week 1" "Week 2" "Week 3" "Week 4" "Week 5" "Week 6" "shadow-run\\|shadow run\\|shadowrun" "GLM 4-key\\|GLM 4 key\\|4-key rotation" "concurrency==1\\|concurrency == 1" "42K\\|~42K\\|≈ 42K" "550K" "148K" "exit criteria" "v11.0-POC-REPORT\\|v11.0-POC\\|v11\\.0-POC-REPORT" "决策 1" "决策 2" "决策 3" "决策 4" "决策 5" "决策 6" "决策 7" "audit" "一致" "OQ-7" "OQ-16" "P1" "P2" "P4" "P5" "P6" "P7" "P8" "P14" "RESOLVED\\|resolved" "downstream" "coherence" "References" "00-FIRST-PRINCIPLES" "01-AGENT-REGISTRY-SCHEMA" "02-ROUND-TABLE-PROTOCOL" "04-MIGRATION-PATH" "06-CROSS-REPO-IMPACT" "Phase 51" "VALIDATE" "v6-self-evolution-milestone"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^### §7\.1/,/^### §7\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | grep -cE "决策 [1-7]" | awk '$1 >= 7 {exit 0} {exit 1}' && awk '/^### §7\.1/,/^### §7\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | grep -c "✅" | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/05-POC-PLAN.md | awk '$1 >= 1300 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §6 exists with §6.0-§6.6 (PoC implementation path SC#5 deep-dive, 7 subsections)
  - §6.1 sequence rationale (fitness battery FIRST → schema migration dry-run SECOND → bias canary THIRD) with explicit precondition logic
  - §6.2 per-vertical-slice token cost (~148K creative / ~10K infra, cite STACK §7.3 ~550K/13≈42K average)
  - §6.3 6-week calendar timeline (Week 1 infra+latency → Week 2 creative+fitness → Week 3 schema migration → Week 4 bias canary+dry-run-first → Week 5 compaction+threshold → Week 6 shadow close + report)
  - §6.4 GLM 4-key rotation 实测点 (week 2 creative slice exposes real cost, concurrency==1 + 3-strike early-abort in force)
  - §6.5 PoC exit criteria (5 conditions including v11.0-POC-REPORT.md)
  - §6.6 PoC risks beyond risk register (5 risks: GLM ceiling / mem0 latency / curator hallucination / CC-crash gc / contextvars)
  - §7 exists with §7.0-§7.3 (7 决策 cross-validation audit + 偏差分析 + OQ/pitfall resolution)
  - §7.1 has 7-row audit table with 决策 1-7 each marked ✅ + citation column
  - §7.2 declares 7/7 一致 (no Phase 44 revision needed), 决策 6 root + 决策 7 enforcement direction
  - §7.3 declares OQ-7 + OQ-16 + P1/P2/P4/P5/P6/P7/P8 + P14 all RESOLVED with section pointer
  - §8 exists with §8.0 downstream citation card + §8.1 coherence 声明 + §8.2 References
  - All 7 决策 (1-7) appear in §7.1 audit table
  - MEMORY.md v6-self-evolution-milestone reference appears (fitness battery precedent)
  - Total doc length ≥ 1300 lines (final, mirrors Phase 48/49 ~1300-1500 line depth at capstone rigor)
  - Phase 51 VALIDATE appears in downstream citation card
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc → v11.0 PoC implementer | The deliverable is a design doc (PoC blueprint). Misinterpretation by v11.0 PoC implementers (e.g. "all 7 acceptance criteria must pass before any work begins" or "defer-with-monitoring means ignore P6 outlier detection") is the primary risk. Mitigated by §7 cross-validation audit + §8.0 downstream citation card + §6.3 week-by-week calendar + §6.5 exit criteria + §5.10 defer-with-monitoring plan (P6 outlier audit-log monitoring explicit). |
| 05-POC-PLAN.md → PITFALLS §Risk Register Summary + SUMMARY §Risk Register | This doc CONSUMES the canonical 7-row risk register verdicts from PITFALLS line 470-488 + SUMMARY line 145-160. Divergent verdicts would create cross-doc drift. Mitigated by §5.9 explicit verdict-对齐 declaration (no divergent verdicts) + §7.3 resolution declarations cross-check. |
| 7 acceptance criteria → v11.0 PoC implementation | The 7 criteria (§4) are the PoC's "done" definition. Ambiguous criteria = implementer ships incomplete PoC. Mitigated by §4.1-§4.7 per-criterion task decomposition + measurable acceptance check + pitfall gated column. |
| Vertical slice selection → creative slice screenplay Step 3 | §3.2 selects screenplay Step 3 round table as creative slice. Wrong selection = PoC exercises wrong code paths. Mitigated by §3.1 5-dimension selection criteria + §3.4 rejected alternatives with rationale + §3.2 5-dim scorecard (round-table density 9/10, schema coverage 90%+, etc.). |
| Risk register P6 PARTIAL verdict → signed-feedback implementation | §5.5 marks P6 PARTIAL (operator_signature PoC must, outlier detection defer). Implementer may over-defer (skip operator_signature too). Mitigated by §5.5 explicit "operator_signature schema field implemented (acceptance: FeedbackRecord validates signature, anonymous rejected)" + §5.10 monitoring plan (audit log operator_id distribution, manual review if >2σ deviation). |
| PoC implementation path sequencing → fitness battery first | §6.1 sequences fitness battery FIRST. Wrong sequence = no regression detection during schema migration / bias canary. Mitigated by §6.1 explicit precondition logic (fitness battery = regression-detection foundation must exist before any curator tick) + §6.3 week-by-week calendar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | §4 acceptance criteria — implementer may quietly lower the bar (e.g. "p95 < 500ms on 10 records" instead of 500) to ship faster | mitigate | §4.2 explicit "100/500/1000-record benchmark" + §4.8 summary table with measurable acceptance check column + §6.5 PoC exit criteria (all 7 must pass) + Phase 51 VALIDATE cross-check |
| T-50-02 | Information Disclosure | §5.5 P6 operator_signature — without HMAC, PoC trusted-operator assumption breaks if workspace shared | accept | PoC workspace ~/.hermes-poc/ is single-operator (Kai); §2.3 isolation declaration; if shared, operator_signature schema field is implemented per acceptance criterion |
| T-50-03 | Denial of Service | §6.4 GLM 4-key rotation ceiling — creative slice multi-round may block on 4-key exhaust | mitigate | §6.4 cites MEMORY.md global concurrency==1 + 3-strike early-abort policy in force; STACK §7.5 串行 no-batch recommendation followed; creative slice 9-agent × 2.5 turns within 800K TPM ceiling for serial |
| T-50-04 | Elevation of Privilege | §3.3 infra slice run_python_phase — dispatcher may allow non-allowlist step if enforcement weak | mitigate | §3.3 edge case cites Phase 49 §5.4 dispatcher-layer validation (reject with explicit error, no silent fallback); acceptance criterion implicit in infra slice 1-round-table cycle |
| T-50-05 | Repudiation | §4.7 schema migration dry-run — implementer may run live migration without dry-run, then blame design for data corruption | mitigate | §4.7 task decomposition (a) dry-run script + (b) output plan + (c) dry-run on synthetic input BEFORE (d) live migration; mirrors Phase 49 §4.7 6-step backup-first; §5.4 P5 dry-run-first invariant gates this |
| T-50-06 | Spoofing | §5.3 P4 cross-project leakage — without scope=project + project_id required, PoC single-project won't surface, production leaks P1→P2 | mitigate | §5.3 must-fix verdict + §4.7 schema migration dry-run populates scope=project + project_id per Phase 49 §4.3 mapping table; §4.2 latency benchmark uses agent_id+project_id filter |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-50-SC (supply chain from package installs) is not applicable.
</threat_model>

<verification>
**Phase 50 DESIGN-06 success criteria (from ROADMAP §Phase 50):**

1. SC#1 — File `05-POC-PLAN.md` exists (verified by Task 1)
2. SC#2 — PoC 目标明确 (vertical slice: 1 creative phase = screenplay Step 3 round table + 1 infra phase = agent registry + 1 round table invocation, 论据完整) (verified by Task 2 §3 full deep-dive)
3. SC#3 — 验收条件清单完整 (7 项: fitness battery / latency SLO p95<500ms / bias canary / compaction pass / threshold tuning / dry-run-first invariant / schema migration dry-run, 每项 1-3 天工作量估算, total 12 person-days) (verified by Task 3 §4 full deep-dive)
4. SC#4 — Risk register (7 load-bearing pitfalls P1/P2/P4/P5/P6/P7/P8 × PoC deferral 评估) complete, 每 pitfall must-fix-in-PoC vs defer-with-monitoring + rationale, aligned with PITFALLS §Risk Register Summary + SUMMARY §Risk Register (verified by Task 4 §5 full deep-dive)
5. SC#5 — PoC 实施路径图 (fitness battery → schema migration dry-run → bias canary sequence, cite STACK §7 token cost estimate ~550K/pipeline run) (verified by Task 5 §6 full deep-dive)

**Final deliverable target:** ~1300+ lines for the single markdown deliverable (mirrors Phase 44's 1181 lines + Phase 48's ~1300 + Phase 49's 1508 at comparable rigor; Phase 50 is single-deliverable so the entire budget concentrates in the capstone synthesis narrative).

**Methodology audit (self-check before commit):**
- Every section cites PITFALLS §PX / SUMMARY §X / Phase 44 决策号 / Phase 45 schema source / Phase 46 protocol / Phase 49 §X / STACK §X (not invented)
- Phase 44 decisions 1-7 cited by 决策号, never re-derived (esp 决策 6 per-agent memory — load-bearing for entire PoC, 决策 7 Hermes控 structure — load-bearing for fitness battery + bias canary gating)
- Phase 45 schema fields (persona_sha256, fitness_battery, memory.max_records, evidence_chain, evidence_operator_ids, scope, project_id, expires_at, verified_at, confidence, half_life_days, status, schema_version) cited by name, never redefined
- Phase 46 protocol invariants (round_table_open lifecycle, conflict arbitration, 强制串行) cited by section
- Phase 49 §4 (memory migration dry-run) + §5 (retained-phases allowlist) cited by section
- PITFALLS §P1/§P2/§P4/§P5/§P6/§P7/§P8 + §P14 + §Risk Register Summary (line 470-488) each cited by section/line
- STACK §3.2 (7 MCP tool) + §7.1/§7.3 (~340 MCP call, ~550K token) each cited
- SUMMARY §Risk Register (line 145-160) + §Gaps to Address (line 172-178) each cited
- Bilingual: EN structure (headers, schemas, citation tags) + 中文 prose (rationale, examples) per CLAUDE.md
- §7 audit confirms 7/7 决策 cross-validation consistent (no silent Phase 44 revision)
- §7.3 declares OQ-7 + OQ-16 + P1/P2/P4/P5/P6/P7/P8 + P14 RESOLVED with section pointer (not deferred)
- v6.0 fitness battery precedent cited from MEMORY.md v6-self-evolution-milestone (PoC design leverages v6.0 patterns)
- §5.9 risk register verdicts aligned with PITFALLS §Risk Register Summary + SUMMARY §Risk Register (no divergent verdicts)
</verification>

<success_criteria>
**Phase 50 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/50-poc-plan/50-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) The 1 deliverable file exists:
   - `.planning/research/v10-orchestrator-design/05-POC-PLAN.md` (≥ 1300 lines, §0-§8, vertical slice selection + 7-item acceptance criteria with 12-person-day estimate + 7-row risk register + PoC implementation path + Phase 44 7 决策 audit + OQ-7/OQ-16/P1-P8/P14 resolution)
3. ROADMAP.md Phase 50 entry updated (Plans: 1 plan, plan list with checkmark).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/50-poc-plan/50-01-SUMMARY.md` when done. SUMMARY should record:
- Final line count of the deliverable
- Vertical slice selection lock (creative = screenplay Step 3 round table + 9-agent subset; infra = agent registry + 1 round table invocation + 7 MCP tools wired)
- 7-item acceptance criteria lock (fitness battery 3d / latency SLO 2d / bias canary 2d / compaction 1d / threshold tuning 1d / dry-run-first 1d / schema migration dry-run 2d = 12 person-days total)
- 7-row risk register lock (P1/P2/P4/P5/P7/P8 must-fix-in-PoC; P6 PARTIAL with operator_signature implemented + outlier detection deferred; aligned with PITFALLS §Risk Register Summary + SUMMARY §Risk Register)
- PoC implementation path lock (fitness battery → schema migration dry-run → bias canary sequence; 6-week calendar timeline; ~148K tokens creative slice / ~10K tokens infra slice; GLM 4-key ceiling monitored at week 2)
- Phase 44 决策 1-7 cross-validation lock (7/7 ✅ 一致, 决策 6 root + 决策 7 enforcement direction)
- OQ-7 + OQ-16 + P1/P2/P4/P5/P6/P7/P8 + P14 RESOLVED declarations
- SC#1-5 coverage confirmed
- Cross-validation: every section cites PITFALLS §PX / SUMMARY §X / Phase 44 决策号 / Phase 45/46/49 schema/protocol source / STACK §X
</output>

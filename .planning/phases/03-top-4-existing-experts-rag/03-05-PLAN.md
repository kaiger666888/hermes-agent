---
phase: 03-top-4-existing-experts-rag
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - skills/movie-experts/_eval/reports/phase3-ablation-dryrun.json
  - skills/movie-experts/_eval/reports/phase3-ablation-dryrun.md
  - skills/movie-experts/_eval/reports/phase3-go-nogo.json
  - skills/movie-experts/_eval/reports/phase3-go-nogo.md
  - skills/movie-experts/_eval/config.yaml.example
  - .planning/STATE.md
autonomous: true
requirements: [REFACTOR-06, REFACTOR-07]

must_haves:
  truths:
    - "3-condition ablation (old-no-refs / new-no-refs / new-with-refs) runnable via `_eval/runner.py` for all 4 experts (screenplay / editor / colorist / style_genome)"
    - "Dry-run produces 4 expert × 3 conditions × 3 prompts pairwise = 4 × C(3,2) × 3 = 36 verdicts per judge; with 2 judges (config declares qwen3-235b + deepseek-v3) expected 72 verdicts total once live run executes"
    - "GO/NO-GO report exists in both machine-readable JSON + human-readable Markdown at `_eval/reports/phase3-go-nogo.{json,md}`"
    - "GO/NO-GO criteria documented: GO if ≥2/3 prompts improve with new-with-refs vs new-no-refs across ≥3/4 experts (CONTEXT decision #9)"
    - "Post-refactor eval comparison vs Phase 0 baseline produced for all 4 experts"
    - "Live run flag: dry-run default; live run deferred to Phase 6 if OPENROUTER_API_KEY absent"
  artifacts:
    - path: "skills/movie-experts/_eval/reports/phase3-ablation-dryrun.json"
      provides: "Aggregated dry-run ablation results across 4 experts × 3 conditions"
      min_lines: 1
    - path: "skills/movie-experts/_eval/reports/phase3-ablation-dryrun.md"
      provides: "Human-readable dry-run ablation report"
      min_lines: 50
    - path: "skills/movie-experts/_eval/reports/phase3-go-nogo.json"
      provides: "Machine-readable GO/NO-GO recommendation with criteria + deferred-live-run status"
      contains: "\"phase\": \"3\""
    - path: "skills/movie-experts/_eval/reports/phase3-go-nogo.md"
      provides: "Human-readable GO/NO-GO report with recommendation + criteria + dry-run evidence + Phase 6 live-run instructions"
      min_lines: 80
    - path: "skills/movie-experts/_eval/config.yaml.example"
      provides: "Updated config with 3 ablation conditions (old_no_refs / new_no_refs / new_with_refs) replacing Phase 0 baseline/candidate"
      contains: "new_with_refs"
    - path: ".planning/STATE.md"
      provides: "STATE.md Phase 3 status updated to mark GO/NO-GO gate artifact produced"
      contains: "Phase 3"
  key_links:
    - from: "skills/movie-experts/_eval/reports/phase3-go-nogo.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 4 GO/NO-GO gate decision per Phase 4 SC #102 (after Phase 4 not after Phase 3)"
      pattern: "GO/NO-GO"
    - from: "skills/movie-experts/_eval/reports/phase3-go-nogo.md"
      to: ".planning/phases/03-top-4-existing-experts-rag/{03-01,03-02,03-03,03-04}-SUMMARY.md"
      via: "4 refactored experts are the new-with-refs condition source"
      pattern: "SUMMARY"
    - from: "skills/movie-experts/_eval/config.yaml.example"
      to: "skills/movie-experts/_eval/runner.py"
      via: "conditions array drives run_ablation pair enumeration"
      pattern: "conditions"
---

<objective>
Run the 3-condition ablation eval (old-no-refs / new-no-refs / new-with-refs) across all 4 refactored experts, produce the GO/NO-GO report at `_eval/reports/phase3-go-nogo.{json,md}`, and update STATE.md to mark the gate artifact produced.

Purpose: Phase 3 is the GO/NO-GO gate for Phase 5. CONTEXT D-7 mandates the ablation matrix (3 conditions × 4 experts × 3 prompts × 2 orderings × 2 judges ≈ 72 verdicts from this plan; 192 verdicts in Phase 6 live run). CONTEXT D-8 defines GO criteria: ≥2/3 prompts improve with new-with-refs vs new-no-refs across ≥3/4 experts. CONTEXT D-11 mandates dry-run default for Phase 3; live run deferred to Phase 6 if OPENROUTER_API_KEY absent. This plan ships the harness validation + report template; the actual statistical GO/NO-GO verdict is produced by the Phase 6 live run.

Output:
- 3-condition ablation config in `_eval/config.yaml.example`
- 4 dry-run reports (one per expert) producing aggregated ablation matrix
- `phase3-ablation-dryrun.{json,md}` aggregated report
- `phase3-go-nogo.{json,md}` GO/NO-GO report with deferred-live-run status
- STATE.md updated to mark Phase 3 gate artifact produced
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-top-4-existing-experts-rag/03-CONTEXT.md
@.planning/phases/03-top-4-existing-experts-rag/03-01-SUMMARY.md
@.planning/phases/03-top-4-existing-experts-rag/03-02-SUMMARY.md
@.planning/phases/03-top-4-existing-experts-rag/03-03-SUMMARY.md
@.planning/phases/03-top-4-existing-experts-rag/03-04-SUMMARY.md
@skills/movie-experts/_eval/runner.py
@skills/movie-experts/_eval/config.yaml.example
@skills/movie-experts/_eval/judge_prompt.md
@skills/movie-experts/_eval/reports/README.md
@skills/movie-experts/_eval/reports/hook_retention_dryrun.md

<interfaces>
<!-- Runner.py CLI contract (Phase 0 + ablation extension point): -->
```
python skills/movie-experts/_eval/runner.py \
    --config skills/movie-experts/_eval/config.yaml.example \
    --expert <expert_id> \
    --dry-run \
    --output-json skills/movie-experts/_eval/reports/<expert>_phase3.json \
    --output-md   skills/movie-experts/_eval/reports/<expert>_phase3.md
```

The runner reads `config.conditions` array (≥2 labels); ablation enumerates all C(N,2) pairs.
For 3 conditions: pairs = (old_no_refs vs new_no_refs), (old_no_refs vs new_with_refs), (new_no_refs vs new_with_refs) = 3 pairs × 3 prompts = 9 verdicts per expert per judge.

<!-- Runner.py ablation output shape (per run_ablation + format_results): -->
```json
{
  "total_comparisons": <int>,
  "verdicts": [
    {"prompt_id": "<id>", "pair": ["<c_i>", "<c_j>"], "final": "A_wins|B_wins|tie", "judge": "<model>", "ordering_ab": "A|B|tie", "ordering_ba": "A|B|tie"}
  ]
}
```

<!-- Dry-run contract: _StubJudgeClient returns A on swap=False, B on swap=True -> final='tie' for every pair (per runner.py:453-498). All-verdicts-tie is the dry-run signature. Live run replaces stub with openai.OpenAI client. -->

<!-- OPENROUTER_API_KEY check (CONTEXT D-11): absent => dry-run default; present => live run option (still defaults to dry-run unless explicitly enabled). -->

<!-- CONTEXT decisions relevant to this plan: -->
- D-7: 3 ablation conditions (old-no-refs / new-no-refs / new-with-refs)
- D-8: N=12 verdicts per condition per expert (3 prompts × 2 orderings × 2 judges)
- D-9: GO criteria ≥2/3 prompts improve with new-with-refs vs new-no-refs across ≥3/4 experts
- D-10: Judge panel Qwen3-235B + DeepSeek-V3, temp=0, CoT + <decision> tag
- D-11: Default dry-run; live only if OPENROUTER_API_KEY set
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config to 3 ablation conditions + run 4 dry-run ablations + aggregate results</name>
  <files>
skills/movie-experts/_eval/config.yaml.example,
skills/movie-experts/_eval/reports/phase3-ablation-dryrun.json,
skills/movie-experts/_eval/reports/phase3-ablation-dryrun.md,
skills/movie-experts/_eval/reports/screenplay_phase3.json,
skills/movie-experts/_eval/reports/screenplay_phase3.md,
skills/movie-experts/_eval/reports/editor_phase3.json,
skills/movie-experts/_eval/reports/editor_phase3.md,
skills/movie-experts/_eval/reports/colorist_phase3.json,
skills/movie-experts/_eval/reports/colorist_phase3.md,
skills/movie-experts/_eval/reports/style_genome_phase3.json,
skills/movie-experts/_eval/reports/style_genome_phase3.md
  </files>
  <read_first>
    - skills/movie-experts/_eval/config.yaml.example (current 2-condition baseline/candidate config; replace with 3 conditions)
    - skills/movie-experts/_eval/runner.py (CLI contract; how --expert resolves to prompts file; how --dry-run produces stub answers; how config.conditions drives run_ablation)
    - skills/movie-experts/_eval/judge_prompt.md (4 evaluation dimensions: industry_accuracy / professional_depth / actionability / language_quality)
    - skills/movie-experts/_eval/reports/hook_retention_dryrun.md (existing dry-run report format reference)
    - skills/movie-experts/_eval/reports/README.md (existing report directory conventions)
    - .planning/phases/03-top-4-existing-experts-rag/03-CONTEXT.md (D-7: 3 conditions; D-8: N=12; D-11: dry-run default)
  </read_first>
  <action>
**Step 1: Update `config.yaml.example` to 3 ablation conditions.** Replace the current `conditions: [baseline, candidate]` with `conditions: [old_no_refs, new_no_refs, new_with_refs]`. Add a comment block above the conditions array documenting the Phase 3 ablation semantics:
- `old_no_refs` = Phase 0 baseline SKILL.md (no RAG block, no refs) — loaded from `_eval/baseline/<expert>/SKILL.md`
- `new_no_refs` = refactored Phase 3 SKILL.md with `## Knowledge Retrieval` block stripped (RAG-aware but no refs loaded)
- `new_with_refs` = refactored Phase 3 SKILL.md + bundled `references/*.md` injected as retrieval context

Preserve the existing judge models array `[qwen/qwen3-235b-a22b:free, deepseek/deepseek-chat-v3:free]` (D-10 panel). Preserve `judge.base_url`. Do NOT change the `temperature` field — runner.py hard-pins it to 0.0 (EVAL-03).

**Step 2: Run 4 dry-run ablations (one per expert).** For each of `screenplay`, `editor`, `colorist`, `style_genome`, invoke:
```
python skills/movie-experts/_eval/runner.py \
    --config skills/movie-experts/_eval/config.yaml.example \
    --expert <expert_id> \
    --dry-run \
    --output-json skills/movie-experts/_eval/reports/<expert_id>_phase3.json \
    --output-md   skills/movie-experts/_eval/reports/<expert_id>_phase3.md
```
Each invocation produces 9 verdicts (3 pairs × 3 prompts) since `main()` uses only `judges[0]` (qwen3-235b). The dry-run signature is `final="tie"` for every pair (per `_StubJudgeClient` design at runner.py:453-498).

If `OPENROUTER_API_KEY` is present in the environment, attempt a live run for one expert (e.g., screenplay) and document the live-run results alongside the dry-run. If the live run fails (rate-limit, model unavailable, network), fall back to dry-run for that expert and log the failure in the GO/NO-GO report's "Deferred Live Run" section.

**Step 3: Aggregate results into `phase3-ablation-dryrun.{json,md}`.** The aggregated JSON shape:
```json
{
  "phase": 3,
  "mode": "dry-run",
  "experts": ["screenplay", "editor", "colorist", "style_genome"],
  "conditions": ["old_no_refs", "new_no_refs", "new_with_refs"],
  "judges_expected_in_phase6": ["qwen/qwen3-235b-a22b:free", "deepseek/deepseek-chat-v3:free"],
  "judges_run_this_phase": ["qwen/qwen3-235b-a22b:free (stub)"],
  "total_verdicts_this_phase": 36,
  "total_verdicts_expected_phase6_live": 144,
  "per_expert": {
    "<expert>": {
      "total_verdicts": 9,
      "pairs": [
        {"pair": ["old_no_refs", "new_no_refs"], "verdicts": [...]},
        {"pair": ["old_no_refs", "new_with_refs"], "verdicts": [...]},
        {"pair": ["new_no_refs", "new_with_refs"], "verdicts": [...]}
      ]
    }
  },
  "live_run_attempted": <bool>,
  "live_run_experts_completed": [],
  "live_run_errors": []
}
```
Embed the per-expert verdict lists by reading the 4 `<expert>_phase3.json` files produced in Step 2.

The aggregated Markdown (`phase3-ablation-dryrun.md`) presents:
1. Header: "Phase 3 Ablation — Dry-Run Results" + date + 4-expert matrix
2. Per-expert section: a 3-row table (one per pair) showing prompt_id × winner × judge
3. Aggregate "Harness Validation" section: confirm the runner accepted 3 conditions, produced C(3,2)×3=9 verdicts per expert, all final="tie" (the stub signature)
4. "Live Run Status" section: OPENROUTER_API_KEY present? Live attempted? Live succeeded? Deferred to Phase 6?

**Single source of truth for verdict data:** do NOT re-run the runner or recompute verdicts in the aggregator; embed from the 4 per-expert JSON files.

**Step 4: Verify ablation matrix is complete.** Assert 4 experts × 9 verdicts = 36 total verdicts in the aggregated JSON. If any expert produced <9 verdicts (e.g., prompts file missing, runner failed), stop and document the failure rather than shipping an incomplete matrix.
  </action>
  <verify>
    <automated>cd skills/movie-experts/_eval && grep -q "new_with_refs" config.yaml.example && grep -q "old_no_refs" config.yaml.example && grep -q "new_no_refs" config.yaml.example && for EXP in screenplay editor colorist style_genome; do python runner.py --expert $EXP --dry-run --output-json reports/${EXP}_phase3.json --output-md reports/${EXP}_phase3.md 2>&1 | grep -q "total_comparisons=9" || { echo "$EXP dry-run failed or wrong verdict count"; exit 1; }; done && test -f reports/phase3-ablation-dryrun.json && test -f reports/phase3-ablation-dryrun.md && python -c "import json; d=json.load(open('reports/phase3-ablation-dryrun.json',encoding='utf-8')); assert d['phase']==3; assert d['mode']=='dry-run'; assert len(d['experts'])==4; assert d['total_verdicts_this_phase']==36, f\"expected 36 got {d['total_verdicts_this_phase']}\"; print('aggregate JSON valid')" && grep -q "Harness Validation" reports/phase3-ablation-dryrun.md && grep -q "Live Run Status" reports/phase3-ablation-dryrun.md && echo PASS</automated>
  </verify>
  <done>
    - config.yaml.example updated with 3 ablation conditions (old_no_refs / new_no_refs / new_with_refs)
    - 4 per-expert dry-run reports exist (screenplay/editor/colorist/style_genome) with 9 verdicts each
    - Aggregated phase3-ablation-dryrun.json contains 36 total verdicts across 4 experts
    - Aggregated phase3-ablation-dryrun.md has Harness Validation + Live Run Status sections
    - All dry-run verdicts are `tie` (stub signature; expected)
    - OPENROUTER_API_KEY presence documented; live run either attempted or explicitly deferred
  </done>
</task>

<task type="auto">
  <name>Task 2: Author GO/NO-GO report + update STATE.md</name>
  <files>
skills/movie-experts/_eval/reports/phase3-go-nogo.json,
skills/movie-experts/_eval/reports/phase3-go-nogo.md,
.planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 GO/NO-GO GATE section — gate decision formally occurs AFTER Phase 4 per ROADMAP; this plan produces the Phase 3 evidence + preliminary recommendation, not the final gate decision)
    - .planning/STATE.md (current Phase 3 status; GO/NO-GO Gate section in STATE)
    - .planning/REQUIREMENTS.md (REFACTOR-06: post-refactor eval comparison vs baseline; REFACTOR-07: ablation comparison done)
    - skills/movie-experts/_eval/reports/phase3-ablation-dryrun.json (produced by Task 1 — embed summary in GO/NO-GO report)
    - skills/movie-experts/_eval/judge_prompt.md (4 evaluation dimensions for the recommendation context)
    - .planning/phases/03-top-4-existing-experts-rag/03-CONTEXT.md (D-9 GO criteria: ≥2/3 prompts improve with new-with-refs vs new-no-refs across ≥3/4 experts; D-11 dry-run default)
    - .planning/phases/03-top-4-existing-experts-rag/03-01-SUMMARY.md
    - .planning/phases/03-top-4-existing-experts-rag/03-02-SUMMARY.md
    - .planning/phases/03-top-4-existing-experts-rag/03-03-SUMMARY.md
    - .planning/phases/03-top-4-existing-experts-rag/03-04-SUMMARY.md
  </read_first>
  <action>
**Step 1: Author `phase3-go-nogo.json` (machine-readable).** Shape:
```json
{
  "phase": 3,
  "report_type": "phase3_go_nogo",
  "generated_at": "<ISO 8601 date>",
  "recommendation_status": "deferred_to_phase6_live_run",
  "preliminary_recommendation": "CONDITIONAL_GO_pending_phase6_evidence",
  "go_criteria": {
    "threshold": "≥2/3 prompts improve with new-with-refs vs new-no-refs across ≥3/4 experts",
    "source": "CONTEXT D-9"
  },
  "evidence_status": {
    "phase3_dry_run_complete": true,
    "phase3_live_run_attempted": <bool from Task 1>,
    "phase3_live_run_experts_completed": [<list from Task 1>],
    "phase6_live_run_required_for_final_verdict": true
  },
  "experts_evaluated": ["screenplay", "editor", "colorist", "style_genome"],
  "conditions_evaluated": ["old_no_refs", "new_no_refs", "new_with_refs"],
  "expected_phase6_verdict_count": 144,
  "phase3_dry_run_verdict_count": 36,
  "phase3_dry_run_signature": "all_tie",
  "rationale_for_deferral": "Phase 3 dry-run uses _StubJudgeClient (runner.py:453) which deterministically returns 'tie' for every pair; no statistical signal possible. Live run requires OPENROUTER_API_KEY + live model calls; per CONTEXT D-11 deferred to Phase 6 budget decision.",
  "ref_corpus_summary": {
    "screenplay": "5 refs (Snyder / McKee / CN shortdrama / academic / dialogue) — REFACTOR-08 satisfied with ≥3 heuristics per ref",
    "editor": "5 refs (Murch / classical / montage / fxrxt / CN cutting) — REFACTOR-08 satisfied",
    "colorist": "5 refs (Bellantoni / Hurkman / cross-cultural / CN audience / digital science) — REFACTOR-08 satisfied",
    "style_genome": "5 refs (director archive 30-50 / genre DNA / cross-cultural / auteur / CN director) — REFACTOR-08 satisfied"
  },
  "phase6_live_run_prerequisites": [
    "OPENROUTER_API_KEY configured in ~/.hermes/.env",
    "config.yaml (not .example) committed locally with live credentials redacted from git",
    "Phase 6 expand N from 3 to ≥20 prompts per expert (EVAL-05)",
    "Phase 6 multi-judge ensemble (both qwen3-235b + deepseek-v3 invoked, not just judges[0])"
  ],
  "go_decision_authority": "Phase 4 (per ROADMAP Phase 4 GO/NO-GO GATE note); this report provides Phase 3 evidence, Phase 4 produces the formal gate decision after Phase 4 completes its own scope."
}
```

**Step 2: Author `phase3-go-nogo.md` (human-readable).** Structure:
1. `# Phase 3 GO/NO-GO Report` + generated date + report status (deferred / conditional)
2. `## Recommendation` — CONDITIONAL GO pending Phase 6 live-run evidence. Explicitly state: Phase 3 dry-run produces no statistical signal (all-tie stub signature); the GO/NO-GO verdict requires Phase 6 live run.
3. `## GO Criteria` — quote CONTEXT D-9 verbatim ("≥2/3 prompts improve with new-with-refs vs new-no-refs across ≥3/4 experts"). Document the operationalization: per-expert improvement rate computed as (new_with_refs wins / total non-tie verdicts) on the (new_no_refs vs new_with_refs) pair.
4. `## Phase 3 Evidence` — 4-expert summary table:
   - Expert | Refs Authored | REFACTOR-08 Heuristics/Ref | Dry-Run Verdicts | Live-Run Status
   - Each row cross-links to the corresponding per-expert dry-run report + Phase plan SUMMARY
5. `## Harness Validation` — confirm: (a) runner.py accepts 3 conditions, (b) C(3,2)=3 pairs enumerated per expert, (c) 9 verdicts per expert × 4 experts = 36 total, (d) dry-run stub signature observed (all-tie). This is the harness-validity proof — the runner would have crashed if any prompts file were missing or if config were malformed.
6. `## Deferred Live Run` — OPENROUTER_API_KEY presence + whether live was attempted. Document Phase 6 prerequisites (4 items per the JSON).
7. `## Phase 4 Gate Authority` — explicitly state that the formal GO/NO-GO gate decision occurs at the end of Phase 4 (per ROADMAP note after Phase 4 description), not at end of Phase 3. Phase 3 ships evidence + preliminary recommendation; Phase 4 ships the formal verdict.
8. `## Ref Corpus Audit (REFACTOR-08)` — for each of 4 experts, list the 5 ref filenames + the ≥3 concrete heuristics per ref (one-line each). This is the human-readable proof that REFACTOR-08 (≥1 heuristic NOT in base model training) is satisfied per ref, with the ≥3 floor exceeded per CONTEXT decision #2.
9. `## Next Steps` — `/gsd:plan-phase 4` to plan EXPERT-CINE (the next phase); OR `/gsd:execute-phase 6 --live-eval` if Phase 6 should run first to produce the live evidence required for a Phase 4 gate verdict.

**Step 3: Update `.planning/STATE.md`.** Modify the Phase 3 row in `## Phase Statuses` to status "Gate artifact produced" with notes "Dry-run complete; live deferred to Phase 6. GO/NO-GO report at `_eval/reports/phase3-go-nogo.{json,md}`. Formal gate decision deferred to Phase 4 per ROADMAP." Update `### Progress` ASCII bar to `Phase 3 [ ########## ] 100% Gate artifact produced`. Update `next action` line to "Plan Phase 4 (EXPERT-CINE) — boundary doc with scene_builder/animator/editor MUST be produced before SKILL.md". Update `accumulated_context` to reference the new GO/NO-GO report.

Preserve all other STATE.md content (Phase 0/1/2 completion notes, decisions, blockers). Do NOT modify Phase 4/5/6 status rows.
  </action>
  <verify>
    <automated>cd skills/movie-experts/_eval/reports && test -f phase3-go-nogo.json && test -f phase3-go-nogo.md && python -c "import json; d=json.load(open('phase3-go-nogo.json',encoding='utf-8')); assert d['phase']==3; assert d['recommendation_status']=='deferred_to_phase6_live_run'; assert d['expected_phase6_verdict_count']==144; assert len(d['experts_evaluated'])==4; print('JSON valid')" && grep -q "## Recommendation" phase3-go-nogo.md && grep -q "## GO Criteria" phase3-go-nogo.md && grep -q "## Phase 3 Evidence" phase3-go-nogo.md && grep -q "## Harness Validation" phase3-go-nogo.md && grep -q "## Deferred Live Run" phase3-go-nogo.md && grep -q "## Phase 4 Gate Authority" phase3-go-nogo.md && grep -q "## Ref Corpus Audit" phase3-go-nogo.md && S="../../.planning/STATE.md" && test -f "$S" && grep -q "Phase 3" "$S" && grep -q "Gate artifact produced" "$S" && grep -q "phase3-go-nogo" "$S" && echo PASS</automated>
  </verify>
  <done>
    - phase3-go-nogo.json exists with recommendation_status=deferred_to_phase6_live_run + 144 expected Phase 6 verdicts
    - phase3-go-nogo.md has 8 required sections (Recommendation / GO Criteria / Phase 3 Evidence / Harness Validation / Deferred Live Run / Phase 4 Gate Authority / Ref Corpus Audit / Next Steps)
    - STATE.md Phase 3 row updated to "Gate artifact produced" with reference to phase3-go-nogo report
    - STATE.md preserves Phase 0/1/2 completion notes + decisions + blockers
    - STATE.md next action updated to Phase 4 (or Phase 6 live run if operator prioritizes evidence first)
    - Formal GO/NO-GO gate authority explicitly documented as Phase 4 (per ROADMAP), NOT Phase 3
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dry-run ablation report → operator decision | all-tie dry-run signature must NOT be misread as "no quality difference"; explicitly documented as stub artifact in the report |
| GO/NO-GO recommendation → Phase 4 gate | this plan ships PRELIMINARY recommendation + evidence; Phase 4 ships formal gate verdict. Misalignment of authorities would cause premature scrapping of Phase 5 |
| OPENROUTER_API_KEY → live model API | key handling must not log the key value; runner.py already enforces this (T-00-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-25 | Information Disclosure | Dry-run all-tie signature misread as no RAG uplift | mitigate | GO/NO-GO report explicitly documents stub signature; "Phase 3 Evidence" section calls out the deferral to Phase 6 |
| T-03-26 | Tampering | GO criteria misoperationalized (≥2/3 prompts rule computed wrong) | mitigate | GO Criteria section quotes CONTEXT D-9 verbatim and provides the per-expert improvement-rate formula |
| T-03-27 | Elevation of Privilege | Phase 3 report treated as the formal gate decision (should be Phase 4) | mitigate | Phase 4 Gate Authority section explicitly defers formal verdict; STATE.md notes the same |
| T-03-28 | Information Disclosure | OPENROUTER_API_KEY leak through report or logs | mitigate | runner.py never logs key value (T-00-09 hard-pinned); reports reference key presence/absence only, never value |
| T-03-29 | Repudiation | Live run failure (rate-limit / model unavailable) masked as success | mitigate | Live Run Status section captures per-expert success/failure; failures logged with error code for Phase 6 retry |
| T-03-30 | Tampering | STATE.md update overwrites Phase 0/1/2 completion notes | mitigate | <action> Step 3 explicitly preserves all other STATE.md content; only Phase 3 row + next-action line updated |
| T-03-31 | Tampering | Aggregated JSON miscounts verdicts (e.g., 35 instead of 36) | mitigate | Verify gate asserts d['total_verdicts_this_phase']==36; per-expert JSON files preserved as single source of truth |
| T-03-SC | Tampering | (none — no packages installed) | accept | Pure JSON + Markdown + YAML artifact; no supply-chain risk |
</threat_model>

<verification>
- config.yaml.example updated with 3 ablation conditions
- 4 per-expert dry-run reports exist with 9 verdicts each (36 total)
- Aggregated phase3-ablation-dryrun.{json,md} validates harness end-to-end
- phase3-go-nogo.{json,md} present with 8 required sections + 144 expected Phase 6 verdict count
- STATE.md Phase 3 row updated to "Gate artifact produced"
- Formal GO/NO-GO gate authority documented as Phase 4 (per ROADMAP) — this plan ships evidence only
</verification>

<success_criteria>
- REFACTOR-06 ✓ (post-refactor eval comparison vs baseline produced for all 4 experts — dry-run harness validated; live comparison deferred to Phase 6 with documented prerequisites)
- REFACTOR-07 ✓ (ablation comparison done — 3-condition × 4-expert matrix runnable via runner.py; dry-run produces valid output shape; live run is operator-budget decision per CONTEXT D-11)
- Phase 3 GO/NO-GO gate artifact ✓ (report exists in JSON + Markdown; preliminary recommendation documented; Phase 4 formal gate authority preserved)
</success_criteria>

<output>
Create `.planning/phases/03-top-4-existing-experts-rag/03-05-SUMMARY.md` when done
</output>

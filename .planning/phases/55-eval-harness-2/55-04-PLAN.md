---
phase: 55-eval-harness-2
plan: 04
type: execute
wave: 2
depends_on:
  - 55-03
files_modified:
  - scripts/migrate_v6_feedback_to_memory_schema.py
  - tests/v11-schema-migration/__init__.py
  - tests/v11-schema-migration/conftest.py
  - tests/v11-schema-migration/test_field_mapping.py
  - tests/v11-schema-migration/test_dry_run_zero_writes.py
  - tests/v11-schema-migration/test_zero_silent_drops.py
  - tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/screenplay/cli.jsonl
  - tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/screenplay/kais_aigc.jsonl
  - tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/cinematographer/manual.jsonl
  - tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/index.json
  - .planning/research/v11-poc-eval/migration-dry-run-format.md
autonomous: true
requirements: [EVAL-07, EVAL-06]
tags: [eval, schema-migration, p14-mitigation, dry-run, feedbackstore, memory-record-schema]

must_haves:
  truths:
    - "Running migrate_v6_feedback_to_memory_schema.py --dry-run produces a diff report (what would change) without writing to mem0"
    - "Running the script without --dry-run also defaults to dry-run (dry_run=True as Python default — EVAL-06 invariant applies to BOTH curator and this script)"
    - "Every source FeedbackRecord appears in the dry-run output (zero silent drops — P14 mitigation)"
    - "Dry-run output format matches the spec in migration-dry-run-format.md (5 metrics: total count / per-field default fill rate / conflict count / estimated target storage / mapping warnings)"
    - "Source FeedbackStore is NEVER mutated — read-only access (append-only source preserved)"
    - "Field mapping follows Phase 49 §4.3 17-row table (skill_id → agent_id, verdict→status: good/needs_work→active, bad→quarantined, etc.)"
  artifacts:
    - path: "scripts/migrate_v6_feedback_to_memory_schema.py"
      provides: "CLI with --dry-run flag (default) + --apply flag (explicit opt-in for live migration); reads v6.0 FeedbackStore JSONL → maps to memory-record-schema via Phase 49 §4.3 table"
      min_lines: 250
    - path: "tests/v11-schema-migration/test_field_mapping.py"
      provides: "Per-field mapping tests for the 17-row Phase 49 §4.3 table"
      min_lines: 100
    - path: "tests/v11-schema-migration/test_dry_run_zero_writes.py"
      provides: "Dry-run produces zero mem0 writes + zero audit entries"
      min_lines: 50
    - path: "tests/v11-schema-migration/test_zero_silent_drops.py"
      provides: "P14 mitigation test — every source record accounted for in dry-run output"
      min_lines: 60
    - path: "tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/"
      provides: "Sample v6.0 FeedbackStore (3 buckets × ~10 records = 30 records) covering all verdict values + edge cases"
    - path: ".planning/research/v11-poc-eval/migration-dry-run-format.md"
      provides: "Dry-run output format spec (5 metrics) — authoritative reference for the diff report shape"
      min_lines: 100
  key_links:
    - from: "scripts/migrate_v6_feedback_to_memory_schema.py"
      to: "agent/feedback_store.FeedbackStore"
      via: "reads v6.0 JSONL buckets (read-only — never writes)"
      pattern: "FeedbackStore|buckets/.*\\.jsonl"
    - from: "scripts/migrate_v6_feedback_to_memory_schema.py"
      to: "agent/feedback_schema.FeedbackRecord"
      via: "source schema (Pydantic) parsed per line"
      pattern: "FeedbackRecord"
    - from: "scripts/migrate_v6_feedback_to_memory_schema.py"
      to: ".planning/research/v10-orchestrator-design/memory-record-schema.yaml"
      via: "target schema (10 mandated fields + 10 optional fields)"
      pattern: "memory-record-schema|record_id|persona_sha256|schema_version"
    - from: "scripts/migrate_v6_feedback_to_memory_schema.py"
      to: "agent/curator_audit.append_audit"
      via: "migration audit log entry (ONLY in --apply mode, never in --dry-run)"
      pattern: "append_audit"
---

<objective>
Build the v6.0 FeedbackStore → memory-record-schema migration script with `--dry-run` default mode. Dry-run produces a 5-metric diff report (what would change) without writing to mem0. Every source FeedbackRecord appears in the dry-run output (zero silent drops — P14 mitigation). Default is dry-run-first per EVAL-06 invariant (cited from 55-03 pattern).

Purpose: P14 mitigation (Schema Migration Breaks Memory Store — silent drop / unsafe default pollution of memory store). Without this script, the v6.0 → v10.0 schema migration has no safe "preview before apply" path. Hard constraint per `05-POC-PLAN.md` §4.7 + Phase 49 `04-MIGRATION-PATH.md` §4.5 + planning_context contract #6 ("P14 mitigation: Zero silent drops").

Output: `scripts/migrate_v6_feedback_to_memory_schema.py` (NEW CLI script, ~250 lines) + `tests/v11-schema-migration/` test suite + `.planning/research/v11-poc-eval/migration-dry-run-format.md` format spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-eval-harness-2/55-CONTEXT.md
@.planning/phases/55-eval-harness-2/55-03-PLAN.md (dry-run-first pattern reference — the same `dry_run: bool = True` default + defensive None-check + AST-walk test approach applies here)

@.planning/research/v10-orchestrator-design/05-POC-PLAN.md (§4.7 Schema Migration Dry-Run contract)
@.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md (§4.3 17-row mapping table + §4.5 dry-run output format + §4.6 safe defaults)
@.planning/research/v10-orchestrator-design/memory-record-schema.yaml (target schema — 10 mandated + 10 optional fields)

<interfaces>
<!-- Source schema: agent/feedback_schema.py:184 — ACTUAL implementation (verified 2026-07-07).
     NOTE: This differs slightly from MIGRATION-PATH §4.1 documentation, which lists
     a simpler `verdict: positive|negative|neutral`. The actual code has 3 verdict
     values: good | needs_work | bad. Follow the CODE (not the doc) for verdict mapping.
     Document the divergence in the migration-dry-run-format.md spec. -->

```python
class FeedbackRecord(BaseModel):
    skill_id: str                              # → maps to target agent_id
    expert_id: str                             # = skill_id (kept separate for future-proofing)
    source: Literal["cli", "kais_aigc", "manual"]   # provenance — cli slash cmd, kais_aigc inbox, manual JSONL
    verdict: Literal["good", "needs_work", "bad"]   # qualitative rating
    correction: str = ""                       # free-text operator feedback
    revised_output: str | None = None          # optional full replacement output
    output_snapshot: OutputSnapshot            # provenance of the LLM output being reviewed
    ts: datetime                               # when feedback was recorded (timezone-aware ISO-8601)
```

FeedbackStore filesystem layout (agent/feedback_store.py:199):
```
~/.hermes/feedback/
├── buckets/
│   └── <skill_id>/
│       └── <source>.jsonl      # append-only, one FeedbackRecord per line, encoding="utf-8"
├── dedup/
│   └── sha256-registry.jsonl    # audit log of supersession events
└── index.json                    # versioned via _INDEX_VERSION = 1
```

<!-- Target schema: memory-record-schema.yaml — 10 mandated fields + 10 optional fields -->

```yaml
# Mandated (all 10 required):
record_id: string (UUID v4)
agent_id: string ^[a-z0-9_-]+$
scope: enum [global, project, session]
status: enum [active, archived, quarantined, superseded]
confidence: float 0.0-1.0
evidence_chain: list (≥3 entries per P5 mitigation 2)
created_at: ISO-8601
persona_sha256: string ^[a-f0-9]{64}$
schema_version: string ^[0-9]+\.[0-9]+\.[0-9]+$  (default "1.0.0")
content: string

# Optional (have defaults — safe-default-on-unknown-field per §4.6):
project_id, session_id, expires_at, verified_at, supersedes_memory_id,
evidence_operator_ids, half_life_days, last_recalled_at, recall_count, confidentiality
```

<!-- Phase 49 §4.3 mapping table (17 rows) — adapted to ACTUAL FeedbackRecord fields: -->

| Source FeedbackRecord field | Target memory-record field | Mapping rule |
|-----------------------------|----------------------------|--------------|
| `skill_id` | `agent_id` | Verbatim copy. `agent_id = skill_id` per ARCHITECTURE §3.2 Option B. |
| `expert_id` | (unused — same as skill_id) | Document in mapping warnings if `expert_id != skill_id`. |
| `source` | `evidence_chain[0].source_type` | "cli" → "operator"; "kais_aigc" → "auto_eval"; "manual" → "operator". |
| `verdict` | `status` | `good` → `active`; `needs_work` → `active` (informational); `bad` → `quarantined` (P14 mitigation — never auto-activate negative-feedback records). |
| `correction` | (consumed into `content` if non-empty) | If `correction` is non-empty AND `feedback_text` equivalent is absent, content = correction. Else content = `output_snapshot.revised_output` or `<no feedback text>`. |
| `revised_output` | (consumed into `content` if present) | If present, append to content as "Suggested revision: ...". |
| `output_snapshot` | `evidence_chain[0].evidence_sha256` + `evidence_chain[0].evidence_text` | Preserve as evidence provenance. |
| `ts` | `created_at` | Verbatim copy (timezone-aware ISO-8601 preserved). |
| *(no source)* | `record_id` | Generated UUID v4 at migration time. |
| *(no source)* | `scope` | Default `"project"` (P14 mitigation 2 — tightest scope). |
| *(no source)* | `project_id` | From bucket path if encoded; else default `"unknown"`. |
| *(no source)* | `session_id` | Default `null`. |
| *(no source)* | `confidence` | Default `0.5` (OQ-4 neutral). |
| *(no source)* | `evidence_chain` | Constructed: `[{source_type, evidence_sha256, evidence_text, ts}]`. Length=1 (below P5 ≥3 threshold — flag as mapping warning). |
| *(no source)* | `evidence_operator_ids` | `["unknown"]` (source FeedbackRecord has no operator_id field — divergence from MIGRATION-PATH §4.1 doc; document in format spec). |
| *(no source)* | `half_life_days` | Default `180`. |
| *(no source)* | `expires_at` | Default `created_at + 180 days`. |
| *(no source)* | `verified_at` | Default `created_at`. |
| *(no source)* | `supersedes_memory_id` | Default `null`. |
| *(no source)* | `confidentiality` | Default `"confidential"` (P14 mitigation 2 — safest not most permissive). |
| *(no source)* | `persona_sha256` | Computed from agent YAML persona at migration time. If agent YAML not found → `0`×64 (zero-hash) + mapping warning. |
| *(no source)* | `schema_version` | Default `"1.0.0"`. |

<!-- 5-metric dry-run output plan (Phase 49 §4.5): -->

```
(a) Total source records: N FeedbackRecord lines across M buckets/
    Breakdown by skill_id: <skill>: <count> (<%>)
(b) Per-target-field default fill rate: % source-derived vs default-backed for each new memory-record field
(c) Conflict count: records mapping to status=quarantined (verdict=bad) + records with mapping warnings
(d) Estimated target storage: ~X MB after migration (source size + ~80% growth for added fields)
(e) Mapping warnings: per-record warnings (missing project_id, ambiguous verdict→status, etc.)
```

From agent/curator_audit.py:158 — append_audit is the live-mode write signal. Tests assert ZERO append_audit calls in --dry-run mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration-dry-run-format.md spec (5-metric output format)</name>
  <files>.planning/research/v11-poc-eval/migration-dry-run-format.md</files>
  <action>
    Create `.planning/research/v11-poc-eval/migration-dry-run-format.md` per Phase 49 `04-MIGRATION-PATH.md` §4.5 + POC-PLAN §4.7. This is the AUTHORITATIVE format spec that Task 2's script implements and Task 3's tests verify against.

    Doc structure (6 sections):

    **Section 1 — Purpose + Scope:** P14 mitigation (zero silent drops / safe defaults). This spec defines the dry-run output format for `scripts/migrate_v6_feedback_to_memory_schema.py`. Live migration is OUT OF SCOPE for v11.0 PoC (per `04-MIGRATION-PATH.md` §4.7 Step 4 — that's v12+).

    **Section 2 — Source vs Target Schema Summary:** Brief recap of FeedbackRecord (v6.0, 8 fields) → memory-record (v10.0, 10 mandated + 10 optional) transformation. Cite Phase 49 §4.3 mapping table. **Document the divergence** between MIGRATION-PATH §4.1 doc (which says `verdict: positive|negative|neutral`) and the ACTUAL code in `agent/feedback_schema.py:206` (`verdict: good|needs_work|bad`). The code is authoritative.

    **Section 3 — Dry-Run Output Format (5 metrics):** Adapted from Phase 49 §4.5 example. The script outputs BOTH:
    - **Human-readable Markdown report** to stdout (the §4.5 example shape — operator reviews this).
    - **Machine-readable JSON summary** to a `--out <path>` file (CI integration). JSON shape:

    ```json
    {
      "source": {"path": "~/.hermes/feedback/", "index_version": 1, "total_records": N, "bucket_breakdown": {"<skill_id>": <count>, ...}},
      "target": {"schema_version": "1.0.0", "estimated_storage_mb": X.Y},
      "metrics": {
        "total_source_count": N,
        "per_field_default_fill_rate": {"content": 1.0, "agent_id": 1.0, "status": 0.96, "scope": 0.0, "confidence": 0.0, ...},
        "conflict_count": {"quarantined_verdict_bad": K, "evidence_chain_below_min": L, ...},
        "estimated_target_storage_mb": X.Y,
        "mapping_warnings": [{"record_id": "...", "line": N, "field": "project_id", "warning": "bucket path without project_id — defaulting to unknown"}, ...]
      },
      "source_record_ids_accounted": ["<uuid>", "<uuid>", ...],
      "dry_run": true,
      "timestamp": "2026-07-07T..."
    }
    ```

    **Section 4 — Zero Silent Drops Guarantee (P14 mitigation):** The `source_record_ids_accounted` array in the JSON summary MUST contain every source FeedbackRecord's computed `record_id` (UUID generated deterministic from source content hash for reproducibility — `record_id = uuid.uuid5(NAMESPACE_OID, sha256(source_line))`). The test `test_zero_silent_drops` asserts `len(source_record_ids_accounted) == total_source_count`.

    **Section 5 — Safe Defaults (P14 mitigation 2):** Document the 6 safe-default-on-unknown-field rules from Phase 49 §4.6:
    - Unknown `confidentiality` → `"confidential"` (NOT `"public"`)
    - Unknown `scope` → `"project"` (NOT `"global"`)
    - Unknown `status` (verdict mapping fails) → `"archived"` (NOT `"active"`)
    - Unknown `confidence` → `0.3` (low — requires re-verification). NOTE: v11.0 PoC uses 0.5 default per OQ-4 neutral baseline; 0.3 is for ambiguous cases only.
    - Unknown `half_life_days` → `180` (conservative — expire sooner)
    - Unknown `expires_at` → `created_at + 180 days`

    **Section 6 — v11.0 PoC Acceptance (cite POC-PLAN §4.7 acceptance check):** Dry-run output plan correct on synthetic input (5 metrics reasonable + zero silent drops). Manual spot-check 5 random records: does the mapped memory record make sense? Live migration + 30-day shadow-run are OUT OF SCOPE for v11.0 (deferred to v12+ per REQUIREMENTS.md).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "
content = open('.planning/research/v11-poc-eval/migration-dry-run-format.md', encoding='utf-8').read()
for needle in ['total_source_count', 'per_field_default_fill_rate', 'conflict_count', 'estimated_target_storage', 'mapping_warnings', 'source_record_ids_accounted', 'zero silent drops', 'safe', 'confidential', 'project']:
    assert needle in content, f'missing: {needle}'
print('migration-dry-run-format.md OK — all required tokens present')
"</automated>
  </verify>
  <done>
    - `.planning/research/v11-poc-eval/migration-dry-run-format.md` exists with all 6 sections.
    - Both output formats documented (Markdown human-readable + JSON machine-readable).
    - Zero silent drops guarantee documented (P14 mitigation).
    - Safe defaults documented (6 rules from Phase 49 §4.6).
    - Divergence between MIGRATION-PATH §4.1 doc + actual code documented (verdict values: good|needs_work|bad).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement migrate_v6_feedback_to_memory_schema.py with --dry-run default</name>
  <files>scripts/migrate_v6_feedback_to_memory_schema.py</files>
  <behavior>
    - Test 1: `python scripts/migrate_v6_feedback_to_memory_schema.py` (no args) runs in dry-run mode by default. Zero mem0 writes. Zero `append_audit` calls. Outputs Markdown report to stdout + JSON summary to default `--out` path.
    - Test 2: `python scripts/migrate_v6_feedback_to_memory_schema.py --dry-run` (explicit) is identical to default — dry-run is the default mode.
    - Test 3: `python scripts/migrate_v6_feedback_to_memory_schema.py --apply` performs live migration (writes to mem0 + appends audit entry). For v11.0 PoC the `--apply` path is gated by a confirmation prompt ("Type 'apply' to confirm live migration:") — defense-in-depth.
    - Test 4: Source FeedbackStore is NEVER mutated in any mode. `git status` after running --dry-run shows zero changes to `~/.hermes/feedback/`. (Source is append-only; script opens read-only.)
    - Test 5: Running the script on the sample fixture (3 buckets × ~10 records = 30 records) produces a JSON summary where `metrics.total_source_count == 30` AND `len(source_record_ids_accounted) == 30` (zero silent drops).
    - Test 6: All 5 metrics fields are present in the JSON summary: `total_source_count`, `per_field_default_fill_rate`, `conflict_count`, `estimated_target_storage_mb`, `mapping_warnings`.
    - Test 7: Field mapping follows Phase 49 §4.3 table — `verdict=good → status=active`, `verdict=needs_work → status=active`, `verdict=bad → status=quarantined`. `skill_id=screenplay → agent_id=screenplay`.
    - Test 8: Safe defaults applied — every migrated record has `scope="project"`, `confidence=0.5`, `confidentiality="confidential"`, `schema_version="1.0.0"` (unless source provides a value).
  </behavior>
  <action>
    Create `scripts/migrate_v6_feedback_to_memory_schema.py` per `04-MIGRATION-PATH.md` §4.3+§4.5+§4.6 + `05-POC-PLAN.md` §4.7.

    Script structure (~250 lines):

    1. Module docstring citing `04-MIGRATION-PATH.md §4` + `05-POC-PLAN.md §4.7` + `from __future__ import annotations`.
    2. `argparse` CLI:
       - `--source <path>` (default: `$HERMES_HOME/feedback`) — source FeedbackStore path.
       - `--out <path>` (default: `./migration-dryrun-report.json`) — JSON summary output path.
       - `--dry-run` (default: True) — dry-run mode flag. Mutually exclusive with `--apply`.
       - `--apply` — live migration flag. Triggers confirmation prompt.
       - `--no-prompt` — skip confirmation (for scripting; use with care). Only valid with `--apply`.
       - If NEITHER `--dry-run` NOR `--apply` is passed → default to `--dry-run` (EVAL-06 invariant).
       - If BOTH are passed → argparse error.
    3. `def main(argv=None) -> int:` entry point. Parses args, dispatches to `run_dry_run()` or `run_live_migration()`.
    4. `def run_dry_run(source_path: Path, out_path: Path) -> dict:` — the primary v11.0 PoC path.
       - Validate source path exists + has expected layout (`buckets/`, `index.json`).
       - Walk `buckets/<skill_id>/<source>.jsonl` files. For each line: parse via `agent.feedback_schema.FeedbackRecord` Pydantic model. Skip + log malformed lines to mapping_warnings (do NOT silently drop — P14 mitigation).
       - For each parsed FeedbackRecord: apply Phase 49 §4.3 mapping table to produce a target memory-record dict. Apply safe defaults per §4.6.
       - Compute `record_id` via deterministic UUIDv5 from source content hash (so re-runs produce identical IDs for reproducibility): `record_id = str(uuid.uuid5(uuid.NAMESPACE_OID, hashlib.sha256(line.encode()).hexdigest()))`.
       - Aggregate the 5 metrics: total count, per-field default fill rate, conflict count (verdict=bad → quarantined), estimated target storage (sum of record sizes), mapping warnings list.
       - Build `source_record_ids_accounted` list with every computed record_id (zero silent drops).
       - Write Markdown report to stdout (the §4.5 example format — header, 5 sections, footer "NO WRITE OCCURRED. To apply: ... --apply").
       - Write JSON summary to `out_path`.
       - Return the JSON dict for test inspection.
    5. `def run_live_migration(source_path: Path, out_path: Path, *, no_prompt: bool = False) -> dict:` — v12+ path. For v11.0 PoC: gate behind confirmation prompt; if confirmed, write records to mem0 + append audit entry via `curator_audit.append_audit(action="auto_apply", patch_id="memory-migration-{ts}", skill_id="_migration", eval_score={"migration": {"source_count": N, "target_count": M, "conflict_count": K}})`. The live path is implemented but not exercised by v11.0 PoC tests (deferred to v12+).
    6. `def map_record(record: FeedbackRecord, *, agent_persona_sha256: dict[str, str]) -> dict:` — pure function applying Phase 49 §4.3 mapping table. Returns target memory-record dict. This is the unit-test surface for field mapping.
    7. `def apply_safe_defaults(target_record: dict, *, source_record: FeedbackRecord) -> dict:` — applies the 6 safe-default rules from §4.6. Mutates target_record in place (called by `map_record`).
    8. `def compute_metrics(source_records: list[FeedbackRecord], target_records: list[dict]) -> dict:` — pure function producing the 5-metric summary.
    9. `def render_markdown_report(metrics: dict, *, source_path: Path) -> str:` — pure function rendering the §4.5 example format.

    CRITICAL — read-only enforcement:
    - The script opens all source files with `open(path, "r", encoding="utf-8")`. Never `"w"` / `"a"` / `"r+"` on source.
    - The `dedup/sha256-registry.jsonl` audit log of source FeedbackStore is NEVER appended to in dry-run mode. Only the target audit log (curator_audit) is appended to in `--apply` mode.

    GLM dispatch — this script does NOT make LLM calls. Migration is deterministic field mapping. (GLM dispatch is the curator compaction path in 55-01, not the schema migration path. Document this divergence in the docstring.)
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/v11-schema-migration/test_field_mapping.py tests/v11-schema-migration/test_dry_run_zero_writes.py tests/v11-schema-migration/test_zero_silent_drops.py -x -v</automated>
  </verify>
  <done>
    - `scripts/migrate_v6_feedback_to_memory_schema.py` exists with `--dry-run` (default), `--apply`, `--out`, `--source` flags.
    - All 8 behavior tests pass.
    - `grep -c "dry_run.*True\|--dry-run" scripts/migrate_v6_feedback_to_memory_schema.py` returns ≥ 2 (default visible in argparse + main dispatch).
    - `grep -c "append_audit" scripts/migrate_v6_feedback_to_memory_schema.py` returns ≥ 1 (audit log entry in --apply path).
    - `grep -c "FeedbackRecord" scripts/migrate_v6_feedback_to_memory_schema.py` returns ≥ 2 (Pydantic source schema parsed).
    - Default mode (no args) runs without writing to source FeedbackStore (`git status` clean after run on fixture).
    - Script exits with code 0 on dry-run success, non-zero on validation errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build sample FeedbackStore fixture + 3 test files (field mapping / dry-run zero writes / zero silent drops)</name>
  <files>tests/v11-schema-migration/__init__.py, tests/v11-schema-migration/conftest.py, tests/v11-schema-migration/test_field_mapping.py, tests/v11-schema-migration/test_dry_run_zero_writes.py, tests/v11-schema-migration/test_zero_silent_drops.py, tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/screenplay/cli.jsonl, tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/screenplay/kais_aigc.jsonl, tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/cinematographer/manual.jsonl, tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/index.json</files>
  <action>
    Create the test scaffolding + fixture + 3 test files:

    1. `tests/v11-schema-migration/__init__.py` — empty (package marker).

    2. `tests/v11-schema-migration/conftest.py` — pytest fixtures:
       - `hermes_home_tmp` (autouse) — tmp HERMES_HOME per test.
       - `sample_feedbackstore` — fixture that copies `tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/` into `hermes_home_tmp / "feedback"` so the script can read it.
       - `mock_audit_chain` — intercepts `curator_audit.append_audit` to count calls (for `test_dry_run_zero_writes`).
       - `run_migration` — fixture returning a helper that invokes `scripts/migrate_v6_feedback_to_memory_schema.py` via `subprocess.run([sys.executable, "scripts/migrate_v6_feedback_to_memory_schema.py", ...])` with the right env. Returns the parsed JSON summary.

    3. `tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/` — sample v6.0 FeedbackStore covering edge cases:
       - `buckets/screenplay/cli.jsonl` — 12 FeedbackRecord lines: 4 `verdict=good`, 4 `verdict=needs_work`, 4 `verdict=bad`. Varied `correction` text + `output_snapshot` shapes.
       - `buckets/screenplay/kais_aigc.jsonl` — 8 FeedbackRecord lines: mix of verdicts.
       - `buckets/cinematographer/manual.jsonl` — 10 FeedbackRecord lines: mix of verdicts.
       - Total: 30 records. Every record must be valid FeedbackRecord JSON (validate via Pydantic at fixture-load time).
       - `index.json` — `{"_INDEX_VERSION": 1, "buckets": {...}}` mirroring the v6.0 layout.
       - Edge cases included:
         - 1 record with empty `correction` + `verdict=bad` → ambiguous, generates mapping warning.
         - 1 record with `correction` containing Chinese text → verifies UTF-8 round-trip.
         - 1 record with very long `output_snapshot.prompt` (>1KB) → verifies large record handling.

    4. `tests/v11-schema-migration/test_field_mapping.py` — per-field mapping tests (the 17-row Phase 49 §4.3 table). Each test asserts a single field mapping:
       - `test_skill_id_to_agent_id` — `skill_id="screenplay"` → `agent_id="screenplay"`.
       - `test_verdict_good_to_status_active` — `verdict="good"` → `status="active"`.
       - `test_verdict_needs_work_to_status_active` — `verdict="needs_work"` → `status="active"`.
       - `test_verdict_bad_to_status_quarantined` — `verdict="bad"` → `status="quarantined"` (P14 mitigation — never auto-activate).
       - `test_ts_to_created_at` — `ts` verbatim → `created_at`.
       - `test_default_scope_is_project` — `scope="project"` (safe default).
       - `test_default_confidence_is_0_5` — `confidence=0.5` (OQ-4 neutral).
       - `test_default_confidentiality_is_confidential` — `confidentiality="confidential"` (safe default).
       - `test_default_schema_version_1_0_0` — `schema_version="1.0.0"`.
       - `test_evidence_chain_constructed_from_source` — `evidence_chain=[{source_type, evidence_sha256, evidence_text, ts}]` length 1.
       - `test_default_half_life_180_days` — `half_life_days=180`.
       - `test_default_expires_at_created_plus_180` — `expires_at == created_at + timedelta(days=180)`.
       - `test_record_id_deterministic_uuid5` — same source line → same `record_id` across runs.
       - (more if needed to cover all 17 mapping rows)

    5. `tests/v11-schema-migration/test_dry_run_zero_writes.py` — dry-run zero-mutation tests:
       - `test_default_mode_is_dry_run` — invoke script with no args; assert JSON summary `"dry_run": true` + exit code 0.
       - `test_explicit_dry_run_flag` — invoke with `--dry-run`; identical behavior to default.
       - `test_dry_run_zero_audit_entries` — invoke with `--dry-run`; assert `mock_audit_chain.call_count == 0`.
       - `test_source_feedbackstore_unchanged` — invoke with `--dry-run`; assert no files under `<HERMES_HOME>/feedback/buckets/` were modified (compare file mtimes + content hashes before vs after).
       - `test_apply_requires_confirmation` — invoke with `--apply` (no `--no-prompt`); assert script prompts + aborts if confirmation not provided. (v11.0 PoC: --apply path is exercised only by this test, which validates the prompt; actual live migration is v12+.)
       - `test_apply_with_no_prompt_performs_writes` — invoke with `--apply --no-prompt`; assert audit entries appended + target records written. (This test exercises the live path for coverage; document in test docstring that v12+ is when this becomes operator-facing.)

    6. `tests/v11-schema-migration/test_zero_silent_drops.py` — P14 mitigation tests:
       - `test_every_source_record_appears_in_summary` — invoke with sample fixture (30 records); assert `len(source_record_ids_accounted) == 30` AND `metrics.total_source_count == 30`.
       - `test_malformed_line_logged_not_dropped` — add a deliberately malformed JSON line to one bucket; invoke dry-run; assert the line appears in `mapping_warnings` (NOT silently dropped) AND `total_source_count` reflects only valid records (the malformed line is counted separately as `metrics.malformed_lines`).
       - `test_record_id_uuid5_deterministic` — run dry-run twice; assert the `source_record_ids_accounted` arrays are identical (deterministic UUIDv5 from content hash).
       - `test_all_5_metrics_fields_present` — assert JSON summary has `total_source_count`, `per_field_default_fill_rate`, `conflict_count`, `estimated_target_storage_mb`, `mapping_warnings` keys.

    Use `subprocess.run` to invoke the script (NOT direct Python import) — this matches the operator-facing CLI invocation pattern and catches `sys.exit` / stdout buffering correctly. Parse the JSON summary from the `--out` file.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/v11-schema-migration/ -x -v</automated>
  </verify>
  <done>
    - `tests/v11-schema-migration/__init__.py` + `conftest.py` + 3 test files all exist.
    - `tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/` has 3 buckets + 30 records + index.json.
    - All field mapping tests pass (17-row Phase 49 §4.3 table covered).
    - Dry-run zero-writes tests pass (zero audit entries, source unchanged).
    - Zero silent drops tests pass (every source record in summary, malformed lines logged not dropped).
    - `pytest tests/v11-schema-migration/ -x` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source FeedbackStore → migration script | Script reads append-only JSONL. A corrupted source (partial write per v6.0 WR-01 mitigation) could produce malformed lines. |
| Migration script → mem0 target | Live migration (--apply) writes records to mem0. A buggy mapping could pollute the memory store with wrong-tier or wrong-scope records. |
| Migration script → curator_audit | Live migration appends audit entries. A buggy dry-run/default could write audit entries when it shouldn't. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-11 | Tampering | source FeedbackStore mutation | mitigate | Script opens source files read-only (`"r"` mode). Source is append-only per v6.0 design. Test `test_source_feedbackstore_unchanged` verifies file mtimes + hashes unchanged. |
| T-55-12 | Information Disclosure | unsafe default value (e.g. confidentiality=public) | mitigate | Safe-default-on-unknown-field per Phase 49 §4.6 — 6 rules documented in migration-dry-run-format.md §5. Test `test_default_confidentiality_is_confidential` enforces. |
| T-55-13 | Denial of Service | silent drop of source records (P14 core risk) | mitigate | Zero silent drops invariant — `source_record_ids_accounted` array in JSON summary contains every source record. Test `test_every_source_record_appears_in_summary` enforces. Malformed lines logged to mapping_warnings, not dropped. |
| T-55-14 | Elevation of Privilege | dry-run bypass (script writes despite --dry-run) | mitigate | `dry_run: bool = True` default (EVAL-06 invariant, pattern from 55-03). Test `test_dry_run_zero_audit_entries` enforces zero audit calls. AST-walk pattern from 55-03 applies (the script's write paths are inside `if not dry_run:` guards — document in script docstring). |
| T-55-15 | Repudiation | live migration without audit trail | mitigate | `--apply` mode appends `curator_audit.append_audit(action="auto_apply", ...)` with full migration metrics in eval_score. Test `test_apply_with_no_prompt_performs_writes` verifies audit entry appended. |
| T-55-SC | Tampering | no new packages installed | accept | Zero packages installed. |
</threat_model>

<verification>
Phase 55 SC#4 (EVAL-07) verification:

1. **Script exists with --dry-run flag:** `ls scripts/migrate_v6_feedback_to_memory_schema.py` + `python scripts/migrate_v6_feedback_to_memory_schema.py --help` shows `--dry-run` (default), `--apply`, `--out`, `--source` flags.
2. **Fixtures exist:** `ls tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/buckets/` shows 3 skill_id subdirs with .jsonl files.
3. **Dry-run produces diff report without writing:** Run `python scripts/migrate_v6_feedback_to_memory_schema.py --source tests/v11-schema-migration/fixtures/sample_v6_feedbackstore/ --out /tmp/migration-report.json`; verify exit code 0 + JSON report exists + `"dry_run": true` in report + zero mem0 writes (verified via `mock_audit_chain.call_count == 0`).
4. **Zero silent drops:** Verify `len(source_record_ids_accounted) == metrics.total_source_count == 30` in the JSON report.
5. **Format spec exists:** `ls .planning/research/v11-poc-eval/migration-dry-run-format.md` + content covers all 5 metrics + zero-silent-drops guarantee + safe defaults.
</verification>

<success_criteria>
- EVAL-07 acceptance: dry-run output plan correct on synthetic input (5 metrics reasonable + zero silent drops). P14 mitigation satisfied.
- Default mode (no args) is dry-run — EVAL-06 invariant applies to this script too.
- Live migration (--apply) path exists but is gated by confirmation prompt; not exercised by v11.0 PoC operators.
- Source FeedbackStore NEVER mutated in any mode (read-only access).
- All 30 sample fixture records accounted for in dry-run output (zero silent drops).
- Format spec at `migration-dry-run-format.md` is the authoritative reference.
</success_criteria>

<output>
Create `.planning/phases/55-eval-harness-2/55-04-SUMMARY.md` when done
</output>

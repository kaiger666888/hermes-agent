---
phase: 55-eval-harness-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v11-poc-eval/threshold-tuning.md
  - .planning/research/v10-orchestrator-design/agents-schema.yaml
autonomous: true
requirements: [EVAL-05]
tags: [eval, threshold, tuning, p13-mitigation, documentation, schema]

must_haves:
  truths:
    - "3 thresholds (memory.max_records, confidence_threshold_for_promotion, evidence_chain_min_for_acceptance) have documented initial defaults in threshold-tuning.md"
    - "Each threshold has a documented tuning methodology for v12.0 operators"
    - "Each threshold has a documented failure mode (what breaks if misconfigured) + audit log entry schema for overrides"
    - "The 3 thresholds appear as schema fields in agents-schema.yaml (verified via grep)"
    - "Runaway protection (P13 mitigation) is documented: adaptive threshold formula + queue-depth backpressure + 30-day auto-reject"
  artifacts:
    - path: ".planning/research/v11-poc-eval/threshold-tuning.md"
      provides: "Initial defaults + tuning methodology + runaway protection for 3 thresholds"
      min_lines: 200
    - path: ".planning/research/v10-orchestrator-design/agents-schema.yaml"
      provides: "memory.thresholds config block with max_records + confidence_threshold_for_promotion + evidence_chain_min_for_acceptance"
      contains: "max_records"
  key_links:
    - from: ".planning/research/v11-poc-eval/threshold-tuning.md"
      to: ".planning/research/v10-orchestrator-design/agents-schema.yaml"
      via: "documentation references schema fields by name"
      pattern: "max_records|confidence_threshold_for_promotion|evidence_chain_min_for_acceptance"
    - from: ".planning/research/v11-poc-eval/threshold-tuning.md"
      to: "agent/curator.py"
      via: "DEFAULT_FEEDBACK_THRESHOLD_COUNT = 3 (v6.0 baseline) cited"
      pattern: "DEFAULT_FEEDBACK_THRESHOLD_COUNT"
---

<objective>
Document the 3 operator-tunable thresholds — `memory.max_records`, `confidence_threshold_for_promotion`, `evidence_chain_min_for_acceptance` — with initial defaults sourced from v10.0 schemas + tuning methodology for v12.0 operators. Verify (and additively amend) the config schema fields in `agents-schema.yaml`. Document P13 mitigation (curator loop runaway protection) covering adaptive threshold formula, queue-depth backpressure, and 30-day auto-reject.

Purpose: P13 mitigation (curator loop runaway). Without documented thresholds + runaway protection, operators drown in low-quality proposals and start rubber-stamping approvals → feedback loop degrades (per `05-POC-PLAN.md` §4.5).

Output: `.planning/research/v11-poc-eval/threshold-tuning.md` (NEW doc) + additive `memory.thresholds:` block in `agents-schema.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-eval-harness-2/55-CONTEXT.md

@.planning/research/v10-orchestrator-design/05-POC-PLAN.md (§4.5 Threshold Tuning contract)
@.planning/research/v10-orchestrator-design/agents-schema.yaml (target schema)

<interfaces>
<!-- Verified facts from codebase scan (2026-07-07): -->

From .planning/research/v10-orchestrator-design/agents-schema.yaml (current state):
- The `memory_scope` field exists at line 126 (shared | per_agent | project_scoped) — routing convention, NOT thresholds.
- The 3 threshold fields (`max_records`, `confidence_threshold_for_promotion`, `evidence_chain_min_for_acceptance`) are NOT currently present in agents-schema.yaml. They are referenced in POC-PLAN §4.4 / §4.5 as "agents-schema.yaml field: memory.max_records (default 500)" — implying a `memory:` sub-block that was not yet authored.
- This plan will additively amend the schema (no field renames, no enum removals) to add a `memory:` sub-object containing the 3 threshold fields with defaults + descriptions citing POC-PLAN §4.4 + §4.5.

From agent/curator.py:211 (v6.0 baseline):
```python
DEFAULT_FEEDBACK_THRESHOLD_COUNT = 3          # baseline for adaptive threshold floor
DEFAULT_FEEDBACK_THRESHOLD_SESSIONS = 2
DEFAULT_AUTO_APPLY_ENABLED = False
DEFAULT_AUTO_APPLY_MIN_DELTA = 0.1
DEFAULT_AUTO_APPLY_MIN_EVIDENCE = 3            # cite this as evidence_chain_min_for_acceptance precedent
```

From 05-POC-PLAN.md §4.5 (P13 mitigation, adaptive formula):
```
feedback_threshold_count = max(3, active_projects * 2)
```
At PoC scale (active_projects=1): threshold = max(3, 2) = 3 (floor wins).
At production scale (active_projects=10): threshold = max(3, 20) = 20.

From 05-POC-PLAN.md §4.5 (P13 mitigation 2+3):
- Queue-depth backpressure: curator pauses proposal generation when review queue > 50 pending patches; auto-resumes when queue drains below 25 (hysteresis prevents flapping).
- Auto-reject old patches: pending patches older than 30 days auto-rejected (`status="expired"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend agents-schema.yaml with additive memory.thresholds block</name>
  <files>.planning/research/v10-orchestrator-design/agents-schema.yaml</files>
  <action>
    Add a `memory:` sub-block to `agents-schema.yaml` (insert after the existing `memory_scope:` field at line ~140, before the `lineage:` block at line ~146). The block defines the 3 operator-tunable thresholds as additive fields (no renames, no enum changes — purely additive, preserving schema backward compat).

    New block content (citing POC-PLAN §4.4 + §4.5 + PITFALLS §P9 + §P13):

    ```yaml
      # ──────────────────────────────────────────────────────────────────────
      # §2.6.1 — Memory thresholds (P9 memory cap + P5 promotion gating + P13 runaway)
      # ──────────────────────────────────────────────────────────────────────

      memory:
        type: object
        description: |
          Operator-tunable memory thresholds. Source: 05-POC-PLAN.md §4.4 + §4.5
          (Phase 55 EVAL-04 + EVAL-05 deliverable). Initial defaults frozen at
          v11.0 PoC baseline; tuning path documented in
          .planning/research/v11-poc-eval/threshold-tuning.md for v12.0 operators.
        default: {}
        properties:
          max_records:
            type: integer
            minimum: 10
            default: 500
            description: |
              Hard cap on active memory records per agent. Above this cap,
              compaction is triggered (lazy on next retrieve). Source:
              05-POC-PLAN.md §4.4 + SUMMARY OQ-7. PITFALLS §P9 mitigation 1.
          confidence_threshold_for_promotion:
            type: number
            minimum: 0.0
            maximum: 1.0
            default: 0.7
            description: |
              Confidence floor for promoting a working-tier record to core-tier.
              Records with confidence ≥ this threshold are eligible for
              promotion during compaction or curator review. Source:
              05-POC-PLAN.md §4.5.
          evidence_chain_min_for_acceptance:
            type: integer
            minimum: 1
            default: 3
            description: |
              Minimum evidence_chain length for a memory record to be accepted
              as active. Records with shorter evidence chains are quarantined
              pending additional corroboration. Source: 05-POC-PLAN.md §4.5 +
              DEFAULT_AUTO_APPLY_MIN_EVIDENCE precedent (agent/curator.py:215).
              PITFALLS §P5 mitigation 2 (evidence coverage ≥3).
        additionalProperties: false
    ```

    Rules:
    - Use `additionalProperties: false` on the `memory` object (whitelist — no surprise keys).
    - All 3 properties have `default:` matching POC-PLAN §4.4 + §4.5 (500 / 0.7 / 3).
    - Every `description:` cites a source (POC-PLAN section + PITFALLS mitigation when applicable).
    - DO NOT modify the existing `memory_scope` field — it remains at line ~126 as the routing convention (separate concern from thresholds).

    After the edit, the schema remains valid JSON Schema Draft 2020-12. Verify by loading with `jsonschema.Draft202012Validator.check_schema()`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "
import yaml, jsonschema
with open('.planning/research/v10-orchestrator-design/agents-schema.yaml', encoding='utf-8') as f:
    schema = yaml.safe_load(f)
jsonschema.Draft202012Validator.check_schema(schema)
props = schema.get('properties', {})
memory = props.get('memory', {})
sub = memory.get('properties', {})
assert 'max_records' in sub, 'missing max_records'
assert sub['max_records']['default'] == 500
assert 'confidence_threshold_for_promotion' in sub, 'missing confidence_threshold_for_promotion'
assert sub['confidence_threshold_for_promotion']['default'] == 0.7
assert 'evidence_chain_min_for_acceptance' in sub, 'missing evidence_chain_min_for_acceptance'
assert sub['evidence_chain_min_for_acceptance']['default'] == 3
print('agents-schema.yaml memory.thresholds block OK')
"</automated>
  </verify>
  <done>
    - `agents-schema.yaml` has a `memory:` sub-object with all 3 threshold properties.
    - All 3 properties have the correct defaults (500 / 0.7 / 3).
    - Schema still validates as Draft 2020-12 (`check_schema()` passes).
    - The existing `memory_scope` field is untouched.
    - All 3 properties cite POC-PLAN §4.4 or §4.5 in their description.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author threshold-tuning.md with defaults + tuning methodology + runaway protection</name>
  <files>.planning/research/v11-poc-eval/threshold-tuning.md</files>
  <action>
    Create `.planning/research/v11-poc-eval/threshold-tuning.md` per POC-PLAN §4.5. The doc MUST cover 6 sections:

    **Section 1 — Initial Defaults (frozen at v11.0 PoC baseline):**
    A 4-row table (one row per threshold + one for the v6.0 baseline `DEFAULT_FEEDBACK_THRESHOLD_COUNT=3`):

    | Threshold | Default | Source | PoC Rationale |
    |-----------|---------|--------|---------------|
    | `memory.max_records` | 500 | agents-schema.yaml §2.6.1; POC-PLAN §4.4; SUMMARY OQ-7 | Generous cap for single-agent PoC; triggers compaction well before context-window pressure. |
    | `confidence_threshold_for_promotion` | 0.7 | agents-schema.yaml §2.6.1; POC-PLAN §4.5 | Conservative — curator must be reasonably sure before promoting working-tier to core-tier. |
    | `evidence_chain_min_for_acceptance` | 3 | agents-schema.yaml §2.6.1; POC-PLAN §4.5; PITFALLS §P5 mitigation 2 | Mirrors v6.0 `DEFAULT_AUTO_APPLY_MIN_EVIDENCE=3` precedent (agent/curator.py:215). Three independent sources = reasonable corroboration floor. |
    | `DEFAULT_FEEDBACK_THRESHOLD_COUNT` | 3 | agent/curator.py:211 (v6.0 ship) | Floor for adaptive formula `max(3, active_projects * 2)`. At PoC (active_projects=1) this stays at 3. |

    **Section 2 — Tuning Methodology (for v12.0 operators):**
    For each threshold, document:
    - (a) **Symptom of too-low value** (e.g. `max_records=50` → compaction runs constantly, GLM cost spikes; `confidence_threshold=0.3` → low-quality records flood core-tier; `evidence_chain_min=1` → single-source rumors become memory).
    - (b) **Symptom of too-high value** (e.g. `max_records=10000` → context overflow before compaction triggers; `confidence_threshold=0.95` → curator almost never promotes, core-tier stagnates; `evidence_chain_min=10` → almost nothing accepted, system appears broken).
    - (c) **Recommended tuning increment** (e.g. adjust `max_records` in steps of ±100; adjust `confidence_threshold` in steps of ±0.05; adjust `evidence_chain_min` in steps of ±1).
    - (d) **Measurement signal** (e.g. for `max_records`: monitor `compact_memory` invocation count per week via curator_audit `action="auto_apply"` entries; for `confidence_threshold`: monitor promotion rate from `eval_score.compaction.summary_record_ids` length; for `evidence_chain_min`: monitor quarantine rate).
    - (e) **CLI override** — `hermes curator set --max-records=N --confidence-threshold=F --evidence-chain-min=N` (proposed v12.0 surface; for v11.0 PoC the values are read from agents-schema.yaml defaults). Override captured in `~/.hermes-poc/curator_thresholds.yaml` for persistence.

    **Section 3 — Audit Log Entry Schema for Threshold Overrides:**
    When an operator changes a threshold (v12.0 CLI), append an audit log entry via `curator_audit.append_audit(action="auto_apply", patch_id="threshold-override-{ts}", skill_id="_operator", eval_score={"threshold_override": {"threshold_name": "max_records", "old_value": 500, "new_value": 750, "rationale_text": "increased for production scale", "operator_id": "kai"}})`. Document the 5 required fields: `operator_id`, `threshold_name`, `old_value`, `new_value`, `rationale_text`.

    **Section 4 — Runaway Protection (P13 mitigation, all 3 sub-mechanisms):**
    - (a) **Adaptive threshold formula** (PITFALLS §P13 mitigation 1): `feedback_threshold_count = max(3, active_projects * 2)`. PoC (active_projects=1) = 3; production (active_projects=10) = 20. Document the warning condition: when an operator manually sets `feedback_threshold_count < max(3, active_projects * 2)`, curator_audit appends a "threshold too low, runaway risk" warning entry.
    - (b) **Queue-depth backpressure** (PITFALLS §P13 mitigation 2): curator pauses proposal generation when review queue > 50 pending patches; auto-resumes when queue drains below 25 (hysteresis). Document the `hermes curator stats --queue-depth` surface (v12.0).
    - (c) **Auto-reject old patches** (PITFALLS §P13 mitigation 3): pending patches older than 30 days auto-rejected (`status="expired"`). Document the cleanup cron + the audit log entry type.

    **Section 5 — Cross-Criterion Dependencies:**
    Document the §4.4 ↔ §4.5 sharing of `memory.max_records` (per POC-PLAN §4.5 final paragraph "Cross-criterion note"): compaction trigger threshold is the SAME parameter as the max_records threshold; tuning one tunes both. They are not independent.

    **Section 6 — v11.0 PoC Acceptance (cite POC-PLAN §4.5 acceptance check):**
    "Audit log captures runaway warning + rate-limit kicks in when threshold set to overly-aggressive value (threshold=1 with active_projects=10)." For v11.0 PoC this acceptance is satisfied by documentation (the CLI override + queue-depth stats are v12.0 surfaces); the test scenarios are documented for v12.0 operator validation.

    Style: Bilingual EN structure + 中文段落 for operator-facing rationale (per CLAUDE.md language convention). Headings in English; rationale paragraphs may be 中文.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "
import re
content = open('.planning/research/v11-poc-eval/threshold-tuning.md', encoding='utf-8').read()
for needle in ['max_records', 'confidence_threshold_for_promotion', 'evidence_chain_min_for_acceptance', 'DEFAULT_FEEDBACK_THRESHOLD_COUNT', 'max(3,', 'queue-depth', '30-day', 'auto-reject', 'audit log']:
    assert needle in content, f'missing: {needle}'
print('threshold-tuning.md OK — all required tokens present')
"</automated>
  </verify>
  <done>
    - `.planning/research/v11-poc-eval/threshold-tuning.md` exists with all 6 sections.
    - All 3 thresholds (max_records / confidence_threshold_for_promotion / evidence_chain_min_for_acceptance) appear with defaults + tuning methodology.
    - DEFAULT_FEEDBACK_THRESHOLD_COUNT=3 v6.0 baseline cited.
    - Adaptive formula `max(3, active_projects * 2)` present.
    - Queue-depth backpressure + 30-day auto-reject documented (P13 mitigation).
    - Audit log entry schema for threshold overrides documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator config → curator runtime | Threshold values flow from `~/.hermes-poc/curator_thresholds.yaml` (v12.0) or `agents-schema.yaml` defaults (v11.0) into curator decision logic. A misconfigured threshold (e.g. `max_records=1`) could brick the system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Tampering | threshold values | mitigate | Schema enforces `minimum: 10` on max_records, `[0.0, 1.0]` range on confidence_threshold, `minimum: 1` on evidence_chain_min. Out-of-range values rejected at agents-schema.yaml validation time. |
| T-55-06 | Denial of Service | runaway threshold (low max_records → constant compaction) | mitigate | P13 mitigation 1 adaptive formula + 2 queue-depth backpressure + 3 30-day auto-reject. Documented in threshold-tuning.md §4. |
| T-55-07 | Repudiation | threshold override without audit trail | mitigate | Documented audit log entry schema (5 required fields) in threshold-tuning.md §3. Every override captured via `curator_audit.append_audit()`. |
| T-55-SC | Tampering | no new packages installed | accept | Zero packages installed — pure docs + YAML edit. |
</threat_model>

<verification>
Phase 55 SC#2 (EVAL-05) verification:

1. **Thresholds documented:** Open `threshold-tuning.md`. Verify all 3 thresholds (memory.max_records, confidence_threshold_for_promotion, evidence_chain_min_for_acceptance) have documented initial defaults + tuning methodology.
2. **Schema fields verified:** Open `agents-schema.yaml`. Verify the `memory:` sub-block exists with `max_records` (default 500), `confidence_threshold_for_promotion` (default 0.7), `evidence_chain_min_for_acceptance` (default 3) properties. `jsonschema.Draft202012Validator.check_schema()` passes.
3. **Runaway protection:** Verify threshold-tuning.md §4 documents all 3 P13 mitigation sub-mechanisms (adaptive formula + queue-depth backpressure + 30-day auto-reject).
4. **Audit trail:** Verify threshold-tuning.md §3 documents the audit log entry schema for threshold overrides.
</verification>

<success_criteria>
- EVAL-05 acceptance: 3 thresholds documented with initial defaults (from v10.0 schemas) + tuning methodology for v12.0 operators; config schema fields verified present in `agents-schema.yaml`.
- `threshold-tuning.md` exists with all 6 required sections.
- `agents-schema.yaml` `memory:` block is additive (no existing fields renamed or removed) — backward-compatible.
- Schema still validates as Draft 2020-12.
</success_criteria>

<output>
Create `.planning/phases/55-eval-harness-2/55-02-SUMMARY.md` when done
</output>

---
phase: 18-validation-documentation
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - .planning/research/v2-pipeline-design/skills-mapping.yaml
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/18-validation-documentation/18-VERIFICATION.md
autonomous: true
requirements: [VALIDATE-01, VALIDATE-02, DOC-01, DOC-02]
user_setup: []

must_haves:
  truths:
    - "A reader opens skills-mapping.yaml and finds sign_off_status: signed_off on every mapping entry (preserved 9 + renamed 2 + merged 2 + NEW 1 + not_in_new_dag 3); the 2 N:1 merged entries that previously used revisit_in_phase now have a Phase 18 sign-off annotation resolving the revisit"
    - "A reader opens REQUIREMENTS.md and finds VALIDATE-01, VALIDATE-02, DOC-01, DOC-02 all marked Complete in the Traceability table, with v3.0 coverage shown as 12/12"
    - "A reader opens STATE.md and finds v3.0 milestone progress shown as 100% complete (6/6 phases), milestone status ready_to_close or closed"
    - "A reader opens 18-VERIFICATION.md and finds an explicit per-criterion verdict (PASS/FAIL) for all 7 ROADMAP §18 success criteria, with the FAIL-defect resolution path documented if any criterion is not yet met"
    - "Any defects surfaced by 18-01 VALIDATION-REPORT.md are either patched in this plan or explicitly deferred to FUTURE-* with a documented rationale"
  artifacts:
    - path: ".planning/research/v2-pipeline-design/skills-mapping.yaml"
      provides: "All entries signed_off; Phase 18 finalizes the 2 revisit_in_phase annotations"
      contains: "sign_off_status: signed_off"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VALIDATE-01/02 + DOC-01/02 marked Complete; v3.0 coverage 12/12"
      contains: "VALIDATE-01"
    - path: ".planning/STATE.md"
      provides: "v3.0 milestone 100% complete; Phase 18 fully done"
      contains: "Phase 18"
    - path: ".planning/phases/18-validation-documentation/18-VERIFICATION.md"
      provides: "Per-criterion verdict for all 7 ROADMAP §18 success criteria"
      contains: "## Verification Matrix"
      min_lines: 80
  key_links:
    - from: "skills-mapping.yaml sign_off_status"
      to: "ROADMAP §18 criterion #2"
      via: "all entries signed_off"
      pattern: "sign_off_status: signed_off"
    - from: "REQUIREMENTS.md Traceability"
      to: "v3.0 milestone close"
      via: "12/12 requirements Complete"
      pattern: "VALIDATE-01.*Complete"
    - from: "18-VERIFICATION.md"
      to: "ROADMAP §18 success criteria #1-#7"
      via: "per-criterion verdict"
      pattern: "PASS|FAIL"
---

<objective>
Close out the v3.0 milestone: final sign-off on skills-mapping.yaml, mark VALIDATE/DOC requirements complete in REQUIREMENTS.md, advance STATE.md to milestone-complete, and produce the per-criterion VERIFICATION.md that the milestone audit will reference.

Purpose: After 18-01 (audit) and 18-02 (docs) complete, 18-03 is the integration-close. It patches any defects surfaced by 18-01, flips all skills-mapping.yaml entries to signed_off, and produces the canonical verification matrix that satisfies VALIDATE-01 + VALIDATE-02 + DOC-01 + DOC-02 traceability.

Output: Patched skills-mapping.yaml, updated REQUIREMENTS.md + STATE.md + ROADMAP.md, and 18-VERIFICATION.md (the milestone-close artifact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-validation-documentation/18-CONTEXT.md
@.planning/phases/18-validation-documentation/18-01-PLAN.md
@.planning/phases/18-validation-documentation/18-02-PLAN.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml

<interfaces>
<!-- Current skills-mapping.yaml sign_off state (from initial read): -->
SIGNED OFF (6 entries):
  - continuity_auditor (Phase 13, signed_off_at: 2026-06-17, signed_off_by: phase-13)
  - compliance_gate (Phase 13, signed_off_at: 2026-06-17, signed_off_by: phase-13)
  - prompt_injector (Phase 16, signed_off_at: 2026-06-17, signed_off_by: phase-16)
  - performer not_in_new_dag (Phase 17, signed_off_at: 2026-06-17, signed_off_by: phase-17)
  - scene_builder not_in_new_dag (Phase 17, signed_off_at: 2026-06-17, signed_off_by: phase-17)
  - storyboard_designer not_in_new_dag (Phase 17, signed_off_at: 2026-06-17, signed_off_by: phase-17)

NOT YET SIGNED OFF (entries lacking sign_off_status: signed_off):
  - 9 one_to_one_preserved entries (creative_source, style_genome, screenplay, script_auditor, character_designer, cinematographer, editor, colorist, hook_retention) — these have NO sign_off_status field currently because they are 1:1 preserved (no rename action); Phase 18 must add sign_off_status: signed_off for completeness per ROADMAP §18 #2 "all entries have sign_off_status: signed_off"
  - quality_gate (one_to_one_preserved) — same; needs sign_off annotation
  - theory_critic (one_to_one_preserved, consultative) — same; needs sign_off annotation
  - visual_executor (n_to_one_merged) — uses `revisit_in_phase: "Phase 11 handoff review..."` NOT signed_off; Phase 18 resolves the revisit (Phase 11 was a v2.0 PRFP design phase that has shipped; the handoff review is satisfied by the cross-repo migration matrix at kais-migration-matrix.yaml + 06-COMPARISON-VS-26-SKILLS.md). Add sign_off_status: signed_off + signed_off_at + signed_off_by: phase-18 + a note resolving the revisit_in_phase.
  - audio_pipeline (n_to_one_merged) — same pattern as visual_executor; resolve revisit_in_phase + sign off.
  - production (not_in_new_dag, disposition: deferred) — NOT a sign-off target; the deferred disposition is final for v3.0 (FUTURE-09). Document that production's deferred status is the v3.0 final disposition (no sign_off action needed because there is no migration action to sign off — it remains as-is).

<!-- REQUIREMENTS.md Traceability rows to update (4): -->
- VALIDATE-01 | 18 | Pending → Complete
- VALIDATE-02 | 18 | Pending → Complete
- DOC-01     | 18 | Pending → Complete
- DOC-02     | 18 | Pending → Complete

<!-- ROADMAP §18 success criteria (7) for 18-VERIFICATION.md: -->
#1: grep expert_id count reconciled (per VALIDATION-REPORT.md, after 18-01 ROADMAP edit)
#2: skills-mapping.yaml all entries signed_off
#3: README.md Mermaid DAG + reconciled inventory (per 18-02 Task 1)
#4: glossary.md 5 new-expert terms (per 18-02 Task 2)
#5: known-external-models.yaml §2.17 dated annex (per 18-02 Task 3)
#6: FOUND-08 compliance (per VALIDATION-REPORT.md §FOUND-08 Compliance Audit)
#7: Backward compat (per VALIDATION-REPORT.md §Backward Compatibility Verification)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch any defects from VALIDATION-REPORT.md + flip all skills-mapping.yaml entries to signed_off</name>
  <files>.planning/research/v2-pipeline-design/skills-mapping.yaml</files>
  <read_first>
    - .planning/phases/18-validation-documentation/VALIDATION-REPORT.md (Task 1 + 2 output from 18-01 — find the INVENTORY DEFECTS list, if any)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml (full read — understand current sign_off state per the interfaces block above)
  </read_first>
  <action>
    Two-part action:

    **Part A — Patch defects from VALIDATION-REPORT.md.** Open VALIDATION-REPORT.md §FOUND-08 Compliance Audit + §Backward Compatibility Verification. If any row has verdict FAIL, identify the fix:
    - Missing redirect stub → out of scope to create in 18-03 (FOUND-08 violation would require surfacing as a v3.0 blocker; document in 18-VERIFICATION.md as a criterion-#6 FAIL with explicit remediation plan).
    - Missing `status:` field on a redirect stub → patch the offending SKILL.md frontmatter directly (small additive edit, in scope for 18-03 close-out).
    - Missing `aliases:` field on a renamed/merged expert's frontmatter → patch directly (in scope).
    - Stranded `related_skills` reference to a deprecated expert → patch directly per Phase 17-01's rewire pattern (in scope).
    - Missing `inheritance_targets` on a deprecated expert → patch directly (in scope).

    If VALIDATION-REPORT.md shows zero FAIL verdicts (the expected outcome given Phases 13-17 each had their own close-out verification), skip Part A entirely and note "No defects to patch — all 13 migration rows PASS" in 18-VERIFICATION.md.

    **Part B — Flip all skills-mapping.yaml entries to sign_off_status: signed_off.** For each mapping entry that does not already have `sign_off_status: signed_off`, add three fields:
    ```yaml
    sign_off_status: signed_off
    signed_off_at: 2026-06-17
    signed_off_by: phase-18
    ```

    Entries to sign off (13 total):
    - 9 one_to_one_preserved entries (creative_source, style_genome, screenplay, script_auditor, character_designer, cinematographer, editor, colorist, hook_retention) — add sign_off_status + timestamp + signer. Note in a comment: "Phase 18 finalizes 1:1 preserved entries with sign_off for ROADMAP §18 #2 completeness; these entries required no migration action but benefit from explicit sign-off for audit traceability."
    - quality_gate (one_to_one_preserved) — same.
    - theory_critic (one_to_one_preserved, consultative) — same.
    - visual_executor (n_to_one_merged) — IMPORTANT: this entry currently has `revisit_in_phase: "Phase 11 handoff review — may revisit split if specialization > consistency"`. Phase 11 of v2.0 PRFP shipped (cross-repo handoff suite at `.planning/research/v2-pipeline-design/kais-migration-matrix.yaml` + `06-COMPARISON-VS-26-SKILLS.md`); the handoff review is complete and did not recommend a split. Add sign_off_status + timestamp + signer, AND add a `revisit_resolution: "Phase 11 v2.0 PRFP handoff review complete (2026-06-16); no split recommended — specialization gain did not exceed consistency loss per PITFALLS §2.1. Signed off in Phase 18 v3.0."` field to resolve the revisit_in_phase explicitly.
    - audio_pipeline (n_to_one_merged) — same pattern as visual_executor. Add sign_off + revisit_resolution referencing Phase 11 + PITFALLS §2.6 (5-task compression rationale upheld).

    For production (not_in_new_dag, disposition: deferred) — do NOT add sign_off_status. Instead, add a clarifying note: `v3_0_disposition: "deferred is the final v3.0 state; FUTURE-09 owns production expert resolution in a future milestone. No sign_off action required for v3.0 close (no migration was performed)."`. The ROADMAP §18 #2 criterion "all entries have sign_off_status: signed_off" applies to entries that had a migration action; production's deferred state is itself the final decision.

    Update the file's top-of-file comment block to note: "Phase 18 v3.0 finalization: all migration entries signed_off; production deferred per FUTURE-09."

    Commit message: `docs(18): skills-mapping.yaml all entries signed_off (v3.0 milestone close)`
  </action>
  <verify>
    <automated>python3 -c "
import yaml
d = yaml.safe_load(open('.planning/research/v2-pipeline-design/skills-mapping.yaml', encoding='utf-8'))
mappings = d.get('mappings', [])
not_in = d.get('not_in_new_dag', [])
signed = 0
total_actionable = 0
for m in mappings:
    total_actionable += 1
    if m.get('sign_off_status') == 'signed_off':
        signed += 1
prod = [n for n in not_in if n.get('v1_expert_id') == 'production'][0]
dep_candidates = [n for n in not_in if n.get('disposition') == 'deprecate_candidate']
dep_signed = sum(1 for n in dep_candidates if n.get('sign_off_status') == 'signed_off')
assert signed == total_actionable, f'{signed}/{total_actionable} mappings signed_off'
assert dep_signed == 3, f'{dep_signed}/3 deprecate candidates signed_off'
assert 'v3_0_disposition' in prod, 'production v3_0_disposition missing'
print(f'OK: {signed}/{total_actionable} mappings + {dep_signed}/3 deprecates signed_off; production deferred annotated')
"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 mapping entries (9 preserved + quality_gate + theory_critic + visual_executor + audio_pipeline) have sign_off_status: signed_off + signed_off_at: 2026-06-17 + signed_off_by: phase-18.
    - visual_executor + audio_pipeline entries have revisit_resolution field resolving the Phase 11 revisit_in_phase annotation.
    - production entry has v3_0_disposition field documenting the deferred status as final for v3.0.
    - 3 deprecate_candidate entries (performer, scene_builder, storyboard_designer) remain signed_off from Phase 17 (no regression).
    - Any defect surfaced by VALIDATION-REPORT.md is either patched (if small additive SKILL.md fix) or documented as a FAIL in 18-VERIFICATION.md with remediation plan.
    - File remains valid YAML.
  </acceptance_criteria>
  <done>
    skills-mapping.yaml satisfies ROADMAP §18 #2: all migration entries signed_off; v3.0 sign-off chain complete end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md + STATE.md + ROADMAP.md for v3.0 milestone close</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Traceability table — currently shows VALIDATE-01/02 + DOC-01/02 as Pending; flip to Complete)
    - .planning/STATE.md (current state — Phase 18 in progress; advance to complete + v3.0 milestone 100%)
    - .planning/ROADMAP.md (Phase 18 section — currently "Plans: TBD"; update to "Plans: 3/3 complete"; v3.0 milestone header from "in planning" to "complete")
  </read_first>
  <action>
    Three coordinated updates marking v3.0 milestone close:

    **REQUIREMENTS.md — Traceability table:** Flip the 4 Pending rows to Complete:
    - VALIDATE-01 | 18 | Pending → Complete
    - VALIDATE-02 | 18 | Pending → Complete
    - DOC-01     | 18 | Pending → Complete
    - DOC-02     | 18 | Pending → Complete

    Update the Coverage summary block: "v3.0 requirements: 12 total ... Mapped to phases: 12/12 ✓" stays the same, but add a completion line: "Completed: 12/12 ✓ (v3.0 milestone closed 2026-06-17)".

    Update the "Last updated" footer: "2026-06-17 — v3.0 milestone closed; all 12 requirements Complete".

    **STATE.md — milestone close:**
    - Update frontmatter: `status: milestone_complete` (or `ready_to_close` if human audit gate is desired); `last_updated: 2026-06-17T...`; `last_activity: Phase 18 Plan 03 COMPLETE — v3.0 milestone closed. All 4 Phase 18 requirements (VALIDATE-01/02 + DOC-01/02) marked Complete. skills-mapping.yaml 13 entries signed_off. 18-VERIFICATION.md produced with per-criterion verdicts.`
    - Progress block: `total_phases: 6`, `completed_phases: 6`, `total_plans: 16` (13 prior + 3 Phase 18), `completed_plans: 16`, `percent: 100`.
    - Update the v3.0 progress bar to show Phase 18 [██████████] 100% Complete.
    - Add a Phase 18 row to the phase-status note (or update the Phase 18 line in the progress block to reflect all 3 plans complete).
    - "Current Position" block: Phase 18 complete; Plan 03 complete; Status: v3.0 milestone closed.
    - "Session Continuity" → "Last action" + "Next action": last action = Phase 18-03 close (signed_off + REQUIREMENTS + STATE + ROADMAP + VERIFICATION); next action = `/gsd:plan-milestone v4` OR operator review of v3.0 + FUTURE-* items OR `/gsd:audit v3.0` if audit workflow exists.
    - Add a "## v3.0 Milestone Close Summary" H2 section near the bottom summarizing: 6 phases (13-18), 16 plans, 12 requirements, all signed off. Reference VALIDATION-REPORT.md + 18-VERIFICATION.md as audit artifacts. Note FUTURE-06/07/08/09/10 deferred items remain (per REQUIREMENTS.md Future Requirements section).

    **ROADMAP.md — Phase 18 + v3.0 header:**
    - Phase 18 "Plans: TBD" → "Plans: 3/3 plans complete" + enumerate the 3 plans with checkboxes:
      ```
      Plans:
      - [x] 18-01-PLAN.md — Validation + reconciliation (VALIDATION-REPORT.md + ROADMAP §18 #1 reconciliation; VALIDATE-01/02 audit)
      - [x] 18-02-PLAN.md — Documentation finalization (README Mermaid DAG + glossary + known-external-models.yaml §2.17 annex; DOC-01/02)
      - [x] 18-03-PLAN.md — Sign-off + close (skills-mapping.yaml all signed_off + REQUIREMENTS + STATE + ROADMAP + 18-VERIFICATION.md)
      ```
    - v3.0 milestone header (top of file, line 16): "🚧 v3.0 Skills-to-DAG Alignment — Phases 13-18 (in planning)" → "✅ v3.0 Skills-to-DAG Alignment — Phases 13-18 (shipped 2026-06-17)".
    - Add a new entry to the Milestones list: "- ✅ **v3.0 Skills-to-DAG Alignment** — Phases 13-18 (shipped 2026-06-17) — audit artifacts at `.planning/phases/18-validation-documentation/VALIDATION-REPORT.md` + `18-VERIFICATION.md`".

    Per CONTEXT D-06 ("no silent sign-off"), the milestone close note in STATE.md must explicitly enumerate what was signed off and reference the audit artifacts — no silent close.

    Commit message: `docs(18): v3.0 milestone close (REQUIREMENTS + STATE + ROADMAP; 12/12 Complete)`
  </action>
  <verify>
    <automated>grep -q "VALIDATE-01.*Complete" .planning/REQUIREMENTS.md && grep -q "VALIDATE-02.*Complete" .planning/REQUIREMENTS.md && grep -q "DOC-01.*Complete" .planning/REQUIREMENTS.md && grep -q "DOC-02.*Complete" .planning/REQUIREMENTS.md && grep -q "milestone_complete\|ready_to_close" .planning/STATE.md && grep -q "v3.0 Skills-to-DAG Alignment.*shipped 2026-06-17" .planning/ROADMAP.md && grep -q "18-03-PLAN.md" .planning/ROADMAP.md && echo "REQUIREMENTS + STATE + ROADMAP updated for v3.0 close"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md Traceability shows VALIDATE-01/02 + DOC-01/02 as Complete; coverage summary documents 12/12 Complete.
    - STATE.md frontmatter status reflects milestone complete/ready-to-close; progress block shows 100%; Phase 18 line in progress bar updated to 100%; a v3.0 Milestone Close Summary section is present.
    - ROADMAP.md Phase 18 plans block shows 3/3 complete with checkboxes; v3.0 milestone header flipped from in-planning to shipped.
    - All three files reference 2026-06-17 as the milestone ship date.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md + STATE.md + ROADMAP.md reflect v3.0 milestone close; traceability chain complete end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 3: Produce 18-VERIFICATION.md (per-criterion verdict for all 7 ROADMAP §18 success criteria)</name>
  <files>.planning/phases/18-validation-documentation/18-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 18 Success Criteria #1-#7)
    - .planning/phases/18-validation-documentation/VALIDATION-REPORT.md (the audit artifact that backs criteria #1, #6, #7)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml (Task 1 output — backs criterion #2)
    - skills/movie-experts/README.md (18-02 Task 1 output — backs criterion #3)
    - skills/movie-experts/_shared/glossary.md (18-02 Task 2 output — backs criterion #4)
    - skills/movie-experts/_shared/known-external-models.yaml (18-02 Task 3 output — backs criterion #5)
  </read_first>
  <action>
    Produce the canonical verification matrix at `.planning/phases/18-validation-documentation/18-VERIFICATION.md`. This is the artifact the milestone audit will reference. Structure:

    **H1: Phase 18 Verification (v3.0 Milestone Close)**

    **Intro paragraph:** Phase 18 verifies that all ROADMAP §18 success criteria are met, drawing on VALIDATION-REPORT.md (criteria #1, #6, #7), skills-mapping.yaml post-18-03 (#2), README.md post-18-02 (#3), glossary.md post-18-02 (#4), and known-external-models.yaml post-18-02 (#5).

    **H2: Verification Matrix** — a 7-row table with columns: Criterion # | ROADMAP §18 text (verbatim) | Evidence artifact | Verdict (PASS/FAIL) | Notes.

    For each of the 7 criteria, fill the row:

    #1 — grep expert_id count reconciled:
    - Evidence: VALIDATION-REPORT.md §Reconciliation Arithmetic + ROADMAP §18 #1 (post-18-01 edit).
    - Verdict: PASS if 18-01's reconciliation arithmetic produces a coherent 4-bucket total that matches the actual find count; FAIL if any defect is unresolved.
    - Notes: cite the reconciled total (16 + 3 + 3 + 10 = 32 expected; actual find count from 18-01); cite the 21-target discrepancy documentation.

    #2 — skills-mapping.yaml all entries signed_off:
    - Evidence: skills-mapping.yaml post-18-03 Task 1; cite the signed count (13 mappings + 3 deprecate_candidates = 16 signed entries; production deferred).
    - Verdict: PASS if 18-03 Task 1's verify command passes.
    - Notes: production's deferred state is the final v3.0 disposition, not a sign-off target.

    #3 — README.md 21-expert inventory + v2.0 PRFP Mermaid DAG:
    - Evidence: README.md post-18-02 Task 1; cite the Mermaid block presence + reconciled inventory table + footer count.
    - Verdict: PASS if 18-02 Task 1's verify command passes.
    - Notes: original 21-target revised to reconciled count per VALIDATION-REPORT.md (CONTEXT D-06 no-silent-sign-off compliance).

    #4 — glossary.md 5 new terms:
    - Evidence: glossary.md post-18-02 Task 2; cite the Phase 18 DOC-02 verification section + line numbers for each of the 5 terms.
    - Verdict: PASS if all 5 terms (visual_executor, audio_pipeline, prompt_injector, continuity_auditor, compliance_gate) have dedicated H3 entries.

    #5 — known-external-models.yaml Phase 8 §2.17 dated annex:
    - Evidence: known-external-models.yaml post-18-02 Task 3; cite the §2.17 annex section + verified_date: 2026-06-17 entries + entry count.
    - Verdict: PASS if 18-02 Task 3's verify command passes (YAML valid + ≥35 models + §2.17 section present + verified_date present).

    #6 — FOUND-08 compliance (zero silent renames):
    - Evidence: VALIDATION-REPORT.md §FOUND-08 Compliance Audit.
    - Verdict: PASS if all 13 migration rows (10 redirects + 3 deprecations) have verdict PASS in the audit table; FAIL if any row is FAIL with an unresolved defect.

    #7 — Backward compat (old expert_id references resolve):
    - Evidence: VALIDATION-REPORT.md §Backward Compatibility Verification.
    - Verdict: PASS if the verification matrix shows all legacy expert_id references resolve via alias/redirect; FAIL if any stranded reference remains.

    **H2: Overall Phase 18 Verdict** — a single line: "Phase 18 verdict: PASS (7/7 criteria met) v3.0 milestone ready to close" or "Phase 18 verdict: FAIL (N/7 criteria met) — defects documented below".

    **H2: Defects + Remediation (only if any FAIL)** — if any criterion is FAIL, document the defect + remediation owner + target phase. If all PASS, write "Zero defects — all 7 criteria PASS".

    **H2: v3.0 Milestone Audit Artifacts** — bullet list of the canonical artifacts an auditor should review:
    - VALIDATION-REPORT.md (inventory classification + FOUND-08 audit + backward compat)
    - 18-VERIFICATION.md (this file)
    - skills-mapping.yaml (sign-off chain)
    - README.md (user-facing inventory + DAG)
    - glossary.md + known-external-models.yaml (terminology + model allowlist)
    - Per-phase SUMMARYs (13-01 through 18-03) for granular change history

    **H2: Deferred Items (carried forward)** — list FUTURE-06 through FUTURE-10 with one-sentence rationale, referencing REQUIREMENTS.md §Future Requirements. These are NOT v3.0 failures; they are explicitly out-of-scope future work.

    Commit message: `docs(18): 18-VERIFICATION.md — per-criterion verdict for v3.0 milestone close`
  </action>
  <verify>
    <automated>test -f .planning/phases/18-validation-documentation/18-VERIFICATION.md && grep -q "## Verification Matrix" .planning/phases/18-validation-documentation/18-VERIFICATION.md && grep -q "## Overall Phase 18 Verdict" .planning/phases/18-validation-documentation/18-VERIFICATION.md && grep -cE "PASS|FAIL" .planning/phases/18-validation-documentation/18-VERIFICATION.md | head -1 && echo "18-VERIFICATION.md produced"</automated>
  </verify>
  <acceptance_criteria>
    - 18-VERIFICATION.md exists with the required H2 structure (Verification Matrix, Overall Phase 18 Verdict, Defects + Remediation or Zero Defects, v3.0 Milestone Audit Artifacts, Deferred Items).
    - All 7 ROADMAP §18 criteria have a row in the Verification Matrix with explicit PASS/FAIL verdict + evidence artifact citation.
    - Overall verdict line is explicit ("7/7 PASS" or "N/7 PASS with defects documented").
    - The Deferred Items section enumerates FUTURE-06/07/08/09/10 (these are NOT failures — explicit forward-reference).
    - The audit artifacts bullet list references VALIDATION-REPORT.md as the primary audit entry point.
  </acceptance_criteria>
  <done>
    18-VERIFICATION.md is the canonical v3.0 milestone close artifact with per-criterion verdicts; auditor-ready.
  </done>
</task>

</tasks>

<verification>
- skills-mapping.yaml has sign_off_status: signed_off on all 13 mapping entries + 3 deprecate_candidates (16 total); production has v3_0_disposition annotation.
- visual_executor + audio_pipeline revisit_in_phase annotations resolved with revisit_resolution field.
- REQUIREMENTS.md Traceability shows VALIDATE-01/02 + DOC-01/02 as Complete; 12/12 v3.0 requirements Complete.
- STATE.md status reflects milestone complete; progress 100%; v3.0 Milestone Close Summary section present.
- ROADMAP.md Phase 18 shows 3/3 plans complete; v3.0 milestone header flipped to shipped 2026-06-17.
- 18-VERIFICATION.md exists with per-criterion verdicts for all 7 ROADMAP §18 criteria + overall verdict + deferred-items list.
- Any defect from VALIDATION-REPORT.md is either patched (Task 1 Part A) or documented as a FAIL in 18-VERIFICATION.md.
</verification>

<success_criteria>
v3.0 Skills-to-DAG Alignment milestone is closed: all 12 requirements Complete, all skills-mapping.yaml entries signed_off, all 7 ROADMAP §18 success criteria verified PASS, audit artifacts produced (VALIDATION-REPORT.md + 18-VERIFICATION.md). The milestone is ready for operator review and the next milestone (v4 or FUTURE-* items) can begin.
</success_criteria>

<output>
Create `.planning/phases/18-validation-documentation/18-03-SUMMARY.md` when done
</output>

---
phase: 00-audit-eval-skeleton-blocker-gate
plan: 04
type: execute
wave: 3
depends_on: [00-01]
files_modified:
  - skills/movie-experts/_shared/glossary.md
  - skills/movie-experts/_shared/SKILL-LAYOUT.md
  - skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
  - skills/movie-experts/animator/GAP-REPORT.md
  - skills/movie-experts/colorist/GAP-REPORT.md
  - skills/movie-experts/composer/GAP-REPORT.md
  - skills/movie-experts/continuity/GAP-REPORT.md
  - skills/movie-experts/drawer/GAP-REPORT.md
  - skills/movie-experts/editor/GAP-REPORT.md
  - skills/movie-experts/foley/GAP-REPORT.md
  - skills/movie-experts/mixer/GAP-REPORT.md
  - skills/movie-experts/performer/GAP-REPORT.md
  - skills/movie-experts/scene_builder/GAP-REPORT.md
  - skills/movie-experts/screenplay/GAP-REPORT.md
  - skills/movie-experts/spatial_audio/GAP-REPORT.md
  - skills/movie-experts/style_genome/GAP-REPORT.md
  - skills/movie-experts/voicer/GAP-REPORT.md
  - skills/movie-experts/animator/SKILL.md
  - skills/movie-experts/performer/SKILL.md
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-05, FOUND-06, FOUND-07, FOUND-08, FOUND-09]
user_setup: []

must_haves:
  truths:
    - "Every one of the 14 existing expert directories contains a GAP-REPORT.md with sections: <phantoms>, <knowledge_gaps>, <prompt_weak_points>, <stale_metrics>, <missing_refs_topics>"
    - "Running `python scripts/verify_skill_references.py --strict` exits 0 (zero phantoms found) across all 14 SKILL.md files"
    - "`skills/movie-experts/_shared/glossary.md` exists with >= 16 EN<->CN term pairs covering the SC #5 list"
    - "`skills/movie-experts/_shared/SKILL-LAYOUT.md` documents the standard layout (SKILL.md + references/*.md + _eval/prompts/)"
    - "`skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md` documents the provider-agnostic pattern with conditional phrasing + graceful degradation"
    - "All 14 existing expert_id values unchanged (verified by re-running snapshot.py verify)"
  artifacts:
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "EN<->CN term dictionary with >= 16 core entries"
      contains: "运镜"
    - path: "skills/movie-experts/_shared/SKILL-LAYOUT.md"
      provides: "Standard skill directory layout spec (FOUND-05)"
      min_lines: 40
    - path: "skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md"
      provides: "Provider-agnostic RAG invocation contract (FOUND-06)"
      min_lines: 40
    - path: "skills/movie-experts/animator/GAP-REPORT.md"
      provides: "Per-expert audit findings for animator"
      contains: "wan22_video"
    - path: "skills/movie-experts/performer/GAP-REPORT.md"
      provides: "Per-expert audit findings for performer"
      contains: "168K"
  key_links:
    - from: "skills/movie-experts/<expert>/GAP-REPORT.md"
      to: "scripts/verify_skill_references.py"
      via: "Audit JSON output is the source of <phantoms> section"
      pattern: "phantoms"
    - from: "skills/movie-experts/_shared/glossary.md"
      to: "skills/movie-experts/<expert>/SKILL.md"
      via: "Future experts reference glossary for EN<->CN consistency"
      pattern: "glossary"
    - from: "skills/movie-experts/<expert>/SKILL.md (post-cleanup)"
      to: "scripts/verify_skill_references.py --strict"
      via: "Lint gate passes (exit 0)"
      pattern: "strict"
---

<objective>
Close out Phase 0: consume the 00-01 audit JSON to write 14 GAP-REPORT.md files, strip the confirmed phantom references from animator + performer SKILL.md (and any others surfaced), publish the `_shared/` foundation docs (glossary, layout spec, RAG invocation pattern), and verify the audit gate passes.

Purpose: This is the gate that proves Phase 0 is done. Once `verify_skill_references.py --strict` exits 0 and all 14 GAP-REPORTs exist, downstream phases can build on a clean foundation.
Output: 14 GAP-REPORTs, 3 _shared docs, 2+ cleaned SKILL.md files, green audit gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-audit-eval-skeleton-blocker-gate/00-CONTEXT.md
@.planning/research/SUMMARY.md
@CLAUDE.md

@.planning/phases/00-audit-eval-skeleton-blocker-gate/00-01-PLAN.md

<interfaces>
<!-- 00-01 audit JSON output shape (consumed here as input) -->
```json
{
  "total": 2,
  "findings": [
    {"file": "skills/movie-experts/animator/SKILL.md", "line": 3, "token": "wan22_video", "context": "description: ..."},
    {"file": "skills/movie-experts/animator/SKILL.md", "line": 12, "token": "wan22_video", "context": "tags: [..., wan22, ...]"},
    {"file": "skills/movie-experts/performer/SKILL.md", "line": 3, "token": "168K controlled tokens", "context": "description: ..."}
  ]
}
```
Note: 00-01 produces this via `--output-json`. This plan reads it (or invokes the scanner directly) to populate the `<phantoms>` section of each GAP-REPORT.

<!-- GAP-REPORT.md required structure (per CONTEXT.md decisions + FOUND-01) -->
```markdown
# GAP-REPORT: <expert_id>

**Audited:** YYYY-MM-DD
**Audit tool:** scripts/verify_skill_references.py (sha256: ...)
**Baseline tag:** eval-baseline-v1

## <phantoms>
<!-- Model/tool/sampler/concept names not in plugins/ inventory or known-external-models.yaml -->
- ...

## <knowledge_gaps>
<!-- Topics the expert should know but SKILL.md doesn't cover -->

## <prompt_weak_points>
<!-- Vague instructions, missing thresholds, hand-wavy guidance -->

## <stale_metrics>
<!-- metrics: [...] values that don't map to measurable behavior -->

## <missing_refs_topics>
<!-- Subjects that should become references/*.md in Phase 3/5 refactor -->
```

<!-- Provider-agnostic token convention (per CONTEXT.md decisions) -->
Phantom replacement strategy:
- `wan22_video` (vendor model name, concept valid) → `<video_gen_primary>` placeholder
- `wan22_video_turbo` → `<video_gen_preview>`
- "168K controlled tokens" (fabricated concept) → strip entirely; replace with concrete description of the ExBxSxP matrix dimensions
- FLUX 1.x samplers (if any surface) → `<image_gen_primary>` placeholder

<!-- SC #5 glossary required terms (>= 16) -->
运镜 / hook / 卡点 / 爆款 / 男频 / 女频 / 完播率 / 付费卡点 / 钩子 / 爽点 / 击中点 / 镜头语言 / 景别 / 视角 / 轴线 / 调度

Project conventions (CLAUDE.md):
- Every `open()` passes `encoding="utf-8"`
- Markdown files are documentation — no Python code style applies
- Expert_id preservation (FOUND-08): the `metadata.hermes.expert_id` field MUST NOT change in any SKILL.md edit; only prompt body / metrics / thresholds / phantom tokens get edited
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 14 GAP-REPORT.md files from audit findings + manual craft review</name>
  <files>skills/movie-experts/animator/GAP-REPORT.md, skills/movie-experts/colorist/GAP-REPORT.md, skills/movie-experts/composer/GAP-REPORT.md, skills/movie-experts/continuity/GAP-REPORT.md, skills/movie-experts/drawer/GAP-REPORT.md, skills/movie-experts/editor/GAP-REPORT.md, skills/movie-experts/foley/GAP-REPORT.md, skills/movie-experts/mixer/GAP-REPORT.md, skills/movie-experts/performer/GAP-REPORT.md, skills/movie-experts/scene_builder/GAP-REPORT.md, skills/movie-experts/screenplay/GAP-REPORT.md, skills/movie-experts/spatial_audio/GAP-REPORT.md, skills/movie-experts/style_genome/GAP-REPORT.md, skills/movie-experts/voicer/GAP-REPORT.md</files>
  <read_first>
    - All 14 SKILL.md files (animator, colorist, composer, continuity, drawer, editor, foley, mixer, performer, scene_builder, screenplay, spatial_audio, style_genome, voicer) — full reads to assess knowledge_gaps, prompt_weak_points, stale_metrics, missing_refs_topics
    - Output of `python scripts/verify_skill_references.py --output-json /tmp/audit.json` (from 00-01) — to populate `<phantoms>` sections
    - .planning/research/SUMMARY.md sections covering "Top-4 deep refactor needs" + "Remaining 10 classification" — informs missing_refs_topics per expert
  </read_first>
  <action>
    For EACH of the 14 experts, write `<expert>/GAP-REPORT.md` per the required structure (5 sections: <phantoms>, <knowledge_gaps>, <prompt_weak_points>, <stale_metrics>, <missing_refs_topics>).

    <phantoms> section: populate from the 00-01 audit JSON. Run `python scripts/verify_skill_references.py --output-json /tmp/audit.json` first, then for each expert, list the findings (token + line + context) under their file. If zero findings for that expert, write `None detected.`

    <knowledge_gaps>: based on reading the SKILL.md, identify topics the expert SHOULD know per the research SUMMARY.md classification (e.g., screenplay should know Save the Cat beat sheet, 短剧 pacing density, hook integration; animator should know current video gen model behavior matrix). 3-6 concrete gaps per expert.

    <prompt_weak_points>: identify vague phrasing, missing numeric thresholds, hand-wavy guidance. Examples: "make it cinematic" without criteria; "use appropriate pacing" without range; metric names that don't tie to measurable behavior. 2-5 weak points per expert.

    <stale_metrics>: examine the `metadata.hermes.metrics` list in each SKILL.md frontmatter. Flag any metric that lacks a measurement definition (e.g., `motion_smoothness` — how is it measured?). 1-3 stale metrics per expert.

    <missing_refs_topics>: propose 2-6 reference file topics for Phase 3/5 refactor based on research SUMMARY.md table-stakes list per expert. Be specific (e.g., for editor: "Murch Rule of Six reference", "FxRxT axis compliance reference", "短剧 cut density reference").

    Honor the per-expert research classification from SUMMARY.md: screenplay/editor/colorist/style_genome = "needs deep refs (4-6)"; continuity/scene_builder/animator/drawer/composer/mixer/foley/voicer/spatial_audio/performer = "needs light-to-medium refs (2-4)".

    Use bilingual content: EN structure + CN prose for craft-specific gaps where natural (e.g., 短剧 pacing gaps use CN term, then EN explanation).

    Each GAP-REPORT must reference the baseline tag `eval-baseline-v1` in its header (per FOUND-04 traceability).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && for expert in animator colorist composer continuity drawer editor foley mixer performer scene_builder screenplay spatial_audio style_genome voicer; do f="skills/movie-experts/$expert/GAP-REPORT.md"; if [ ! -f "$f" ]; then echo "MISSING: $f"; exit 1; fi; for section in "<phantoms>" "<knowledge_gaps>" "<prompt_weak_points>" "<stale_metrics>" "<missing_refs_topics>"; do grep -q "^## $section" "$f" || { echo "MISSING SECTION $section in $f"; exit 1; }; done; done && echo "ALL 14 GAP-REPORTs present with 5 sections each" && grep -l "wan22_video" skills/movie-experts/animator/GAP-REPORT.md && grep -l "168K" skills/movie-experts/performer/GAP-REPORT.md</automated>
  </verify>
  <done>
    All 14 `GAP-REPORT.md` files exist. Each contains all 5 required section headers. animator GAP-REPORT references `wan22_video`; performer GAP-REPORT references `168K`. Each file references baseline tag `eval-baseline-v1`. Commit message: `docs(00-04): publish 14 GAP-REPORTs from audit findings`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip phantom refs from animator + performer SKILL.md (+ any others surfaced by audit)</name>
  <files>skills/movie-experts/animator/SKILL.md, skills/movie-experts/performer/SKILL.md</files>
  <read_first>
    - skills/movie-experts/animator/SKILL.md (full — to find every `wan22_video` occurrence across frontmatter + body)
    - skills/movie-experts/performer/SKILL.md (full — to find every "168K controlled tokens" occurrence)
    - Output of `python scripts/verify_skill_references.py --output-json /tmp/audit.json` (full findings list — may surface phantoms in other skills beyond the 2 known)
    - skills/movie-experts/_shared/known-external-models.yaml (from 00-01 — to know which tokens are legit)
    - .planning/phases/00-audit-eval-skeleton-blocker-gate/00-CONTEXT.md (Phantom Reference Handling decisions)
  </read_first>
  <action>
    For animator/SKILL.md:
    - Replace every `wan22_video` occurrence with `<video_gen_primary>` placeholder (vendor-specific name → provider-agnostic token per CONTEXT.md decision).
    - Replace `wan22_video_turbo` with `<video_gen_preview>`.
    - In the frontmatter `tags: [..., wan22, ...]` list, replace `wan22` with `video-gen` (or remove if it duplicates an existing tag).
    - Update the description frontmatter to reference "current video generation models" instead of "Wan2.2 video generation".
    - Update the H1 and Role sections similarly.
    - PRESERVE `metadata.hermes.expert_id: animator` UNCHANGED (FOUND-08 hard rule). DO NOT touch the expert_id field.
    - PRESERVE `metadata.hermes.related_skills` list UNCHANGED (collaboration graph integrity).

    For performer/SKILL.md:
    - Strip every "168K controlled tokens" occurrence (fabricated concept — no real industry term matches).
    - Replace with a concrete description: "Performance-4D matrix (ExBxSxP) — a parametric dispatch across Emotion, Body mechanics, Spatial staging, and Prompt engineering dimensions." (This is the real concept the fabrication was gesturing at.)
    - Update description frontmatter, H1 subtitle, Role bullets, and any Key Parameters section that referenced the count.
    - PRESERVE `metadata.hermes.expert_id: performer` UNCHANGED (FOUND-08 hard rule).

    For any OTHER phantoms surfaced by the audit (FLUX 1.x in drawer, additional fabricated tokens in others — TBD based on audit run): apply the same placeholder strategy. Use `<image_gen_primary>` for FLUX 1.x. Strip entirely if no real concept maps.

    After edits, re-run `python scripts/verify_skill_references.py --strict` — it MUST exit 0 (zero phantoms). If it still reports findings, iterate until clean.

    CRITICAL: do NOT introduce new model names that aren't in the allowlist. Use placeholders (`<video_gen_primary>`, `<image_gen_primary>`, etc.) only.

    CRITICAL: edit ONLY the prompt body and phantom tokens. Do NOT change expert_id, related_skills, or other structural frontmatter. Use surgical Edit tool calls, not file rewrites.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python scripts/verify_skill_references.py --strict 2>&1 | tail -10 && echo "EXIT:$?" && grep -c "wan22_video\|168K controlled" skills/movie-experts/animator/SKILL.md skills/movie-experts/performer/SKILL.md && echo "---EXPERT_ID PRESERVED---" && grep "expert_id:" skills/movie-experts/animator/SKILL.md skills/movie-experts/performer/SKILL.md</automated>
  </verify>
  <done>
    `verify_skill_references.py --strict` exits 0 (zero findings). `grep -c "wan22_video"` in animator/SKILL.md returns 0; `grep -c "168K controlled"` in performer/SKILL.md returns 0. expert_id values unchanged (`animator`, `performer`). Placeholder tokens (`<video_gen_primary>` etc.) present where vendor names were stripped. Commit message: `fix(00-04): strip phantom refs from animator + performer SKILL.md (FOUND-03, FOUND-08)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Publish _shared foundation docs (glossary + layout + RAG invocation pattern)</name>
  <files>skills/movie-experts/_shared/glossary.md, skills/movie-experts/_shared/SKILL-LAYOUT.md, skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md</files>
  <read_first>
    - .planning/phases/00-audit-eval-skeleton-blocker-gate/00-CONTEXT.md (specifics section — glossary required term list)
    - .planning/research/SUMMARY.md (RAG invocation hard rule + provider-agnostic phrasing requirement)
    - One existing SKILL.md (e.g., screenplay/SKILL.md) — to confirm the layout pattern that's already informally established
    - skills/movie-experts/_shared/known-external-models.yaml (from 00-01 — sibling _shared file)
  </read_first>
  <action>
    Create three foundation docs under `skills/movie-experts/_shared/`:

    (1) `glossary.md` — EN<->CN term dictionary. MUST include all 16 required terms from SC #5 with bilingual definitions. Structure per entry:
    ```markdown
    ### 运镜 / cinematography / camera movement
    **CN:** 运镜 — 摄影机移动方式的统称,包括推拉摇移升降跟。
    **EN:** The collective term for camera movement techniques (dolly, pan, tilt, crane, tracking, handheld).
    **Context:** Used by EXPERT-CINE (Phase 4) and references `animator.camera_type`.
    ```
    Cover at minimum: 运镜, 钩子/hook, 卡点/paywall cliffhanger, 爆款/viral formula, 男频, 女频, 完播率/completion rate, 付费卡点, 钩子/hook (alt: opening hook), 爽点, 击中点, 镜头语言/shot grammar, 景别/shot size, 视角/angle, 轴线/axis line (180 rule), 调度/blocking. Add 4-8 more terms as natural (e.g., 转发率/share rate, 竖屏/vertical screen, 备案/filing, 标识/labeling).

    (2) `SKILL-LAYOUT.md` — Standard skill directory layout spec (FOUND-05). Document:
    - Required files: `SKILL.md` (uppercase, mandatory), `references/*.md` (curated RAG corpus), `_eval/prompts/<expert>.yaml` (eval prompts)
    - Optional: `_eval/baseline/` (snapshots), `LICENSE.md` (per ref if copyright requires)
    - Forbidden: any Python/JS code in skill dirs (eval scripts live in `_eval/` at suite level, not per-expert)
    - Naming: expert_id MUST match directory name; lowercase; snake_case
    - Frontmatter schema: link to CLAUDE.md "Skill File Conventions" section; restate the mandatory fields (name, description, version, metadata.hermes.{tags, related_skills, expert_id, metrics})
    - Example tree for a hypothetical expert

    (3) `RAG-INVOCATION-PATTERN.md` — Provider-agnostic RAG invocation contract (FOUND-06). Document:
    - The hard rule: never hardcode `fact_store`, `mem0_search`, or any specific tool name in SKILL.md body
    - Pattern: use conditional phrasing like "If a memory/RAG tool is available in your current runtime (e.g., `mem0_search`, `fact_store`, or similar), retrieve the following context before proceeding: [list of ref topics]. If no such tool is available, fall back to the references/*.md files bundled with this skill."
    - Graceful degradation: always fall back to bundled refs; never fail if memory plugin absent
    - Why: Hermes runs against multiple providers; hardcoding breaks portability
    - Example before/after snippet showing hardcoded vs provider-agnostic phrasing
    - Reference to `_shared/known-external-models.yaml` for vendor token conventions
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "
import re
g = open('skills/movie-experts/_shared/glossary.md', encoding='utf-8').read()
required = ['运镜', '钩子', '卡点', '爆款', '男频', '女频', '完播率', '付费卡点', '爽点', '击中点', '镜头语言', '景别', '视角', '轴线', '调度']
missing = [t for t in required if t not in g]
assert not missing, f'GLOSSARY MISSING: {missing}'
print(f'Glossary OK: {len(required)} required terms present')
h3_count = len(re.findall(r'^### ', g, re.MULTILINE))
print(f'Total glossary entries (H3): {h3_count}')
assert h3_count >= 16, f'Expected >= 16 entries, got {h3_count}'
" && echo "---LAYOUT---" && grep -cE "^## (Required|Optional|Forbidden|Naming|Frontmatter)" skills/movie-experts/_shared/SKILL-LAYOUT.md && echo "---RAG---" && grep -cE "mem0_search|fact_store|conditional|graceful" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md</automated>
  </verify>
  <done>
    `_shared/glossary.md` exists with >= 16 H3 term entries, all 15 required terms present (verified by python check). `_shared/SKILL-LAYOUT.md` exists with >= 5 spec sections. `_shared/RAG-INVOCATION-PATTERN.md` exists and references conditional phrasing + graceful degradation + the forbidden tool names. Commit message: `docs(00-04): publish _shared foundation docs (glossary, layout, RAG pattern) (FOUND-05/06/09)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit JSON → GAP-REPORT | structured data flows into human-readable report |
| SKILL.md edit → behavior | prompt body changes affect downstream LLM behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-14 | Tampering | expert_id modified during phantom cleanup | mitigate | verify post-edit via `grep expert_id:` returns original values; snapshot.py verify confirms no unintended drift |
| T-00-15 | Repudiation | GAP-REPORT content not traceable to audit run | mitigate | each GAP-REPORT header records audit tool sha256 + baseline tag + audit date |
| T-00-16 | Information Disclosure | glossary references future-phase expert names | accept | all referenced experts are in public ROADMAP.md; no sensitive info |
| T-00-17 | Tampering | phantom replacement token breaks LLM comprehension | mitigate | use documented `<video_gen_primary>` placeholder convention (defined in RAG-INVOCATION-PATTERN.md); Phase 3/5 fills in real model names via refs |
| T-00-18 | Spoofing | new phantom introduced during cleanup | mitigate | re-run `verify_skill_references.py --strict` after edits; must exit 0 |
| T-00-SC | Tampering | no package installs | accept | pure markdown + Python script edits; zero pip/npm operations |
</threat_model>

<verification>
- `ls skills/movie-experts/*/GAP-REPORT.md | wc -l` == 14
- Each GAP-REPORT has all 5 required sections (`grep -c "^## <" $f` == 5 per file)
- `python scripts/verify_skill_references.py --strict` exits 0 (the BLOCKER GATE — SC #1)
- `grep -c "wan22_video" skills/movie-experts/animator/SKILL.md` == 0
- `grep -c "168K controlled" skills/movie-experts/performer/SKILL.md` == 0
- `grep "expert_id:" skills/movie-experts/animator/SKILL.md` returns `expert_id: animator` (UNCHANGED)
- `grep "expert_id:" skills/movie-experts/performer/SKILL.md` returns `expert_id: performer` (UNCHANGED)
- `python skills/movie-experts/_eval/snapshot.py verify` still detects NO drift on baseline snapshots IF baselines were captured before this cleanup — OR if baselines were captured after 00-02 ran (which was pre-cleanup), the verify will report drift on animator + performer, which is EXPECTED and correct (baseline = pre-cleanup state; current = post-cleanup state). Document this in the SUMMARY.
- `_shared/glossary.md` has >= 16 H3 entries with all 15 required terms
- `_shared/SKILL-LAYOUT.md` and `_shared/RAG-INVOCATION-PATTERN.md` exist with required content
- Ruff clean on any Python touched (none expected in this plan — pure docs + markdown edits)
</verification>

<success_criteria>
- All 14 GAP-REPORT.md files exist with 5 sections each — SC #4, FOUND-01
- `verify_skill_references.py --strict` exits 0 — SC #1, FOUND-02, FOUND-03
- Phantom tokens (`wan22_video`, `168K controlled tokens`) absent from all 14 SKILL.md files — FOUND-03
- All 14 expert_id values unchanged — FOUND-08
- `_shared/glossary.md` published with >= 16 EN<->CN term pairs — SC #5, FOUND-09
- `_shared/SKILL-LAYOUT.md` documents standard layout — FOUND-05
- `_shared/RAG-INVOCATION-PATTERN.md` documents provider-agnostic pattern — FOUND-06
- Phase 0 BLOCKER GATE passed; Phase 1 unblocked
</success_criteria>

<output>
Create `.planning/phases/00-audit-eval-skeleton-blocker-gate/00-04-SUMMARY.md` when done. Include: total phantoms stripped (per expert), GAP-REPORT section counts, glossary entry count, expert_id preservation proof (grep output), final `verify_skill_references.py --strict` exit code (must be 0), and the snapshot.py verify drift report (expected: animator + performer show drift between baseline and current — this is the PROOF that cleanup happened after baseline was captured).
</output>

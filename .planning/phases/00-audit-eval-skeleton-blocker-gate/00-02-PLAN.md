---
phase: 00-audit-eval-skeleton-blocker-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/movie-experts/_eval/snapshot.py
  - skills/movie-experts/_eval/tests/test_snapshot.py
  - skills/movie-experts/_eval/baseline/.gitkeep
autonomous: true
requirements: [FOUND-04, EVAL-02]
user_setup: []

must_haves:
  truths:
    - "Running `python skills/movie-experts/_eval/snapshot.py capture` creates `_eval/baseline/<expert>/{SKILL.md,PROVENANCE.json}` for all 14 existing experts"
    - "Each baseline SKILL.md is a byte-exact copy of the current skills/movie-experts/<expert>/SKILL.md"
    - "Each PROVENANCE.json contains sha256, git_sha, captured_at (ISO 8601), tag=eval-baseline-v1, source_path, and expert_id"
    - "Running `python skills/movie-experts/_eval/snapshot.py verify` exits 0 when baselines match current skills, exits 1 with a diff report when any have drifted"
    - "expert_id values in all 14 baselines are unchanged from the source skills (FOUND-08 preservation gate)"
  artifacts:
    - path: "skills/movie-experts/_eval/snapshot.py"
      provides: "Capture and verify baseline snapshots of SKILL.md files"
      min_lines: 120
      exports: ["capture_baselines", "verify_baselines", "compute_provenance"]
    - path: "skills/movie-experts/_eval/tests/test_snapshot.py"
      provides: "Pytest coverage for capture + verify + provenance computation"
      min_lines: 50
    - path: "skills/movie-experts/_eval/baseline/.gitkeep"
      provides: "Directory placeholder so git tracks baseline tree"
  key_links:
    - from: "skills/movie-experts/_eval/snapshot.py"
      to: "skills/movie-experts/<expert>/SKILL.md"
      via: "Path read at capture time; copied byte-exact"
      pattern: "SKILL\\.md"
    - from: "skills/movie-experts/_eval/snapshot.py"
      to: "git rev-parse HEAD"
      via: "subprocess to capture current commit SHA"
      pattern: "rev-parse"
---

<objective>
Capture a reproducible pre-refactor baseline of all 14 existing SKILL.md files with cryptographic provenance. This snapshot is the "before" anchor for every Phase 3/5/6 ablation comparison.

Purpose: Per RESEARCH.md PITFALLS #8 and Phase 3 REFACTOR-06/07, every "RAG uplift" claim requires a before-vs-after comparison. Without a baseline captured NOW (before any refactor touches the skills), no future claim is defensible.
Output: `_eval/snapshot.py` + 14 baseline directories with `SKILL.md` + `PROVENANCE.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-audit-eval-skeleton-blocker-gate/00-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Existing expert directory list (must snapshot all 14) -->
14 experts (alphabetical): animator, colorist, composer, continuity, drawer, editor, foley, mixer, performer, scene_builder, screenplay, spatial_audio, style_genome, voicer

<!-- SKILL.md frontmatter carries expert_id — must preserve unchanged -->
From skills/movie-experts/animator/SKILL.md frontmatter:
```yaml
metadata:
  hermes:
    expert_id: animator  # frozen value — preserved across refactor
```

<!-- Provenance JSON shape (this plan defines it; downstream consumers expect it) -->
```json
{
  "expert_id": "animator",
  "tag": "eval-baseline-v1",
  "source_path": "skills/movie-experts/animator/SKILL.md",
  "sha256": "<64 hex chars>",
  "git_sha": "<40 hex chars or 'uncommitted'>",
  "captured_at": "2026-06-15T12:34:56+08:00",
  "byte_size": 12345
}
```

Project conventions (from CLAUDE.md):
- Every `open()` passes `encoding="utf-8"` (Ruff PLW1514, CI-blocking)
- `from __future__ import annotations` at module top
- `subprocess.run` for git — pass explicit `capture_output=True, text=True, encoding="utf-8"`
- Tests live next to module: `skills/movie-experts/_eval/tests/test_snapshot.py`
- `_eval/` is NOT registered in Hermes tool registry (loader only finds SKILL.md files; safe)
- No new packages; use stdlib only (hashlib, json, pathlib, subprocess, datetime, argparse)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for capture + verify + provenance</name>
  <files>skills/movie-experts/_eval/tests/test_snapshot.py</files>
  <read_first>
    - skills/movie-experts/animator/SKILL.md (real skill content — for byte-exact test)
    - CLAUDE.md (Code Style, Error Handling, Test conventions)
    - scripts/tests/test_verify_skill_references.py (from 00-01 if landed — pattern reference for tempfile fixtures)
  </read_first>
  <behavior>
    - Test `test_compute_provenance_returns_required_fields`: given a known skill path and a fake git SHA, `compute_provenance(path, expert_id="animator", git_sha="abc123")` returns dict with keys: expert_id, tag, source_path, sha256, git_sha, captured_at, byte_size. `tag` == "eval-baseline-v1". `sha256` is 64 lowercase hex chars. `captured_at` parses as ISO 8601 via `datetime.fromisoformat`.
    - Test `test_compute_provenance_hash_is_deterministic`: same file content → same sha256. Mutate content → different sha256.
    - Test `test_capture_baselines_creates_14_dirs`: in a temp skills tree with 14 expert subdirs each containing a SKILL.md, `capture_baselines(skills_dir, baseline_dir)` creates 14 subdirectories under `baseline_dir`, each with `SKILL.md` and `PROVENANCE.json`.
    - Test `test_capture_is_byte_exact`: content of `baseline/<expert>/SKILL.md` equals content of source `<expert>/SKILL.md` (bytes equal).
    - Test `test_verify_returns_clean_when_unchanged`: immediately after capture, `verify_baselines(skills_dir, baseline_dir)` returns `(True, [])` (no drift).
    - Test `test_verify_detects_drift`: after capture, mutate source SKILL.md; `verify_baselines` returns `(False, [drift_report])` where drift_report includes expert_id, expected_sha256, actual_sha256.
  </behavior>
  <action>
    Create `skills/movie-experts/_eval/tests/test_snapshot.py`. Use `tempfile.TemporaryDirectory` + `pathlib.Path` to build synthetic skill trees (do NOT mutate real skills during tests). Tests MUST fail at import (module `snapshot` not yet implemented) — RED phase. Create empty `skills/movie-experts/_eval/tests/__init__.py` if needed for pytest discovery.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/movie-experts/_eval/tests/test_snapshot.py -x --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>
    pytest collects 6 tests; all fail with ImportError (module not yet implemented). Commit message: `test(00-02): add failing tests for snapshot capture/verify`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement snapshot.py and capture 14 baselines</name>
  <files>skills/movie-experts/_eval/snapshot.py, skills/movie-experts/_eval/baseline/.gitkeep</files>
  <read_first>
    - skills/movie-experts/_eval/tests/test_snapshot.py (the contract — implement to pass)
    - One real SKILL.md to confirm hash computation logic (e.g., animator/SKILL.md)
  </read_first>
  <action>
    Implement `skills/movie-experts/_eval/snapshot.py` to GREEN. Structure:
    (1) `BASELINE_TAG = "eval-baseline-v1"` module constant.
    (2) `EXPERT_DIRS = ["animator", "colorist", "composer", "continuity", "drawer", "editor", "foley", "mixer", "performer", "scene_builder", "screenplay", "spatial_audio", "style_genome", "voicer"]` — the 14 frozen expert IDs.
    (3) `compute_provenance(skill_path: Path, expert_id: str, git_sha: str) -> dict` — reads bytes (encoding="utf-8"), computes `hashlib.sha256`, gets `byte_size`, stamps `captured_at` via `datetime.now(timezone.utc).isoformat()`, returns provenance dict per interface shape above.
    (4) `_current_git_sha() -> str` — runs `git rev-parse HEAD` via `subprocess.run(["git", "rev-parse", "HEAD"], capture_output=True, text=True, encoding="utf-8", cwd=repo_root)`; returns stripped stdout or `"uncommitted"` on error.
    (5) `capture_baselines(skills_dir: Path, baseline_dir: Path, git_sha: str | None = None) -> list[Path]` — iterates `EXPERT_DIRS`, for each: reads `skills_dir/<expert>/SKILL.md`, writes byte-exact copy to `baseline_dir/<expert>/SKILL.md`, writes `PROVENANCE.json`. Creates parent dirs. Returns list of created baseline dirs.
    (6) `verify_baselines(skills_dir: Path, baseline_dir: Path) -> tuple[bool, list[dict]]` — for each expert: recompute sha256 of current source; if it differs from PROVENANCE.json sha256, append drift dict `{expert_id, expected, actual, source_path}`. Return `(len(drifts)==0, drifts)`.
    (7) `main()` — argparse with `mode` positional (`capture` | `verify`), `--skills-dir` (default `<script_parent>/../`), `--baseline-dir` (default `<script_parent>/baseline/`). For capture: also verify all 14 dirs exist (raise with missing list if not), then capture + print summary. For verify: print drift report and exit 1 if drifted.

    Honor CLAUDE.md: `from __future__ import annotations`; every `open(...)` passes `encoding="utf-8"` (use `"rb"` and `.read_bytes()` for hashing to avoid encoding ambiguity — or read_text with explicit encoding); specific exceptions bound to names.

    After implementation: RUN `python skills/movie-experts/_eval/snapshot.py capture` from repo root. This creates all 14 baseline directories. Verify the count with `ls skills/movie-experts/_eval/baseline/ | wc -l` == 14.

    Create empty `skills/movie-experts/_eval/baseline/.gitkeep` so the directory tree is tracked even before capture runs (capture itself creates the 14 subdirs).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/movie-experts/_eval/tests/test_snapshot.py -x --tb=short 2>&1 | tail -10 && echo "---CAPTURE---" && python skills/movie-experts/_eval/snapshot.py capture 2>&1 | tail -5 && echo "---COUNT---" && ls skills/movie-experts/_eval/baseline/ | wc -l && echo "---VERIFY---" && python skills/movie-experts/_eval/snapshot.py verify 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 6 tests pass. Capture creates 14 baseline subdirs under `skills/movie-experts/_eval/baseline/`. Each contains `SKILL.md` (byte-exact copy) and `PROVENANCE.json` (with sha256, git_sha, captured_at, tag=eval-baseline-v1). Verify reports "all baselines match" (since capture just ran). expert_id in each PROVENANCE.json matches the directory name (FOUND-08 preservation). Commit message: `feat(00-02): implement snapshot.py and capture 14 baselines`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git → script | git_sha comes from local git; trusted |
| SKILL.md → baseline | byte-exact copy preserves all source semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-05 | Tampering | baseline SKILL.md edited manually post-capture | mitigate | PROVENANCE.json sha256 + verify subcommand catches drift |
| T-00-06 | Repudiation | "uncommitted" git_sha for unsaved work | accept | provenance records "uncommitted" verbatim; downstream comparison acknowledges this state |
| T-00-07 | Information Disclosure | git_sha / file size in PROVENANCE | accept | both are non-sensitive metadata; commit SHAs are public |
| T-00-08 | Spoofing | synthetic expert dir injected to inflate count | mitigate | EXPERT_DIRS hardcoded list — unknown dirs silently skipped, not auto-included |
| T-00-SC | Tampering | stdlib only; no package installs | accept | zero pip/npm operations in this plan |
</threat_model>

<verification>
- `python -m pytest skills/movie-experts/_eval/tests/test_snapshot.py` — 6/6 passing
- `python skills/movie-experts/_eval/snapshot.py capture` exits 0; prints "Captured 14 baselines" or equivalent
- `ls skills/movie-experts/_eval/baseline/` shows exactly 14 subdirs matching the EXPERT_DIRS list
- Each `<expert>/PROVENANCE.json` parses as valid JSON with required 7 fields
- `python skills/movie-experts/_eval/snapshot.py verify` exits 0 immediately after capture (no drift)
- `python skills/movie-experts/_eval/snapshot.py verify` exits 1 after manually mutating any source SKILL.md
- No new entries in `pyproject.toml` dependencies
- Ruff clean: `ruff check skills/movie-experts/_eval/snapshot.py skills/movie-experts/_eval/tests/test_snapshot.py`
</verification>

<success_criteria>
- 14 baseline snapshots captured with cryptographic provenance (sha256 + git SHA + timestamp)
- Tag `eval-baseline-v1` present on every PROVENANCE.json
- Verify subcommand reliably detects drift (RED→GREEN regression coverage)
- Zero new packages added (stdlib only)
</success_criteria>

<output>
Create `.planning/phases/00-audit-eval-skeleton-blocker-gate/00-02-SUMMARY.md` when done. Include: baseline capture timestamp, git SHA, list of 14 expert_ids (must match EXPERT_DIRS exactly), byte-size total, provenance schema example.
</output>

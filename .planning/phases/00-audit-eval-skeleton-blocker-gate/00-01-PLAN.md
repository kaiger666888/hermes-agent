---
phase: 00-audit-eval-skeleton-blocker-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_skill_references.py
  - skills/movie-experts/_shared/known-external-models.yaml
  - scripts/tests/test_verify_skill_references.py
autonomous: true
requirements: [FOUND-02, FOUND-06, FOUND-08]
user_setup: []

must_haves:
  truths:
    - "Running `python scripts/verify_skill_references.py` exits 0 when zero phantoms found, non-zero otherwise"
    - "Script produces both JSON (machine-readable) and Markdown (human-readable) reports in stdout/files"
    - "Allowlist auto-builds from plugins/image_gen/*/plugin.yaml and plugins/video_gen/*/plugin.yaml at runtime"
    - "Known phantom `wan22_video` is flagged in animator/SKILL.md on first run"
    - "Known phantom `168K controlled tokens` is flagged in performer/SKILL.md on first run"
  artifacts:
    - path: "scripts/verify_skill_references.py"
      provides: "CI lint that greps model/tool/sampler names against allowlist"
      min_lines: 150
      exports: ["main", "build_allowlist", "scan_skill_file"]
    - path: "skills/movie-experts/_shared/known-external-models.yaml"
      provides: "Manual override allowlist for non-plugin external models (Sora, Veo, Kling, Runway Gen-3, etc.)"
      contains: "sora"
    - path: "scripts/tests/test_verify_skill_references.py"
      provides: "Pytest coverage for allowlist builder, scanner, and reporter"
      min_lines: 60
  key_links:
    - from: "scripts/verify_skill_references.py"
      to: "plugins/*/plugin.yaml"
      via: "PyYAML load at runtime"
      pattern: "plugin\\.yaml"
    - from: "scripts/verify_skill_references.py"
      to: "skills/movie-experts/_shared/known-external-models.yaml"
      via: "PyYAML load manual overrides"
      pattern: "known-external-models"
---

<objective>
Build the phantom-reference detector: a standalone CI lint that flags any model/tool/sampler/concept name appearing in any `skills/movie-experts/*/SKILL.md` that is not present in `plugins/*/plugin.yaml` or a curated manual override list.

Purpose: This is the credibility anchor — without it, "phantom refs" (e.g., `wan22_video`, "168K controlled tokens", FLUX 1.x samplers) keep shipping silently. It feeds the GAP-REPORT generator in 00-04 and survives as a CI gate for every future refactor.
Output: `scripts/verify_skill_references.py`, `_shared/known-external-models.yaml`, pytest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-audit-eval-skeleton-blocker-gate/00-CONTEXT.md
@.planning/research/SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Real plugin.yaml schema parsed at runtime -->
From plugins/video_gen/fal/plugin.yaml (representative):
```yaml
name: fal
version: 1.0.0
description: "FAL.ai video generation backend. Multi-model — Veo 3.1, Kling, Pixverse — covering text-to-video and image-to-video via fal_client's queue API."
kind: backend
requires_env:
  - FAL_KEY
```
Notes: plugin.yaml files vary in shape; `name` and `description` are the consistent fields carrying model/tool vocabulary. Parse both.

Known phantoms (confirmed in audit, must be caught by scanner):
- `skills/movie-experts/animator/SKILL.md:3,12` — `wan22_video` (no such plugin/manifest exists)
- `skills/movie-experts/performer/SKILL.md:3,20,35` — `168K controlled tokens` (fabricated concept)

Project conventions to honor (from CLAUDE.md):
- Every `open()` MUST pass `encoding="utf-8"` (Ruff PLW1514, CI-blocking)
- snake_case functions/modules; PascalCase classes; UPPER_SNAKE constants
- No `from x import *`; explicit imports only
- Specific exceptions only; bind with `as exc`; log with `%s` positional args
- Tests: `tests/test_<module>.py` (here: `scripts/tests/test_verify_skill_references.py` — co-locate with script since scripts/ has its own tests dir)
- No new packages; only `openai`, `pyyaml`, `jinja2` already in pyproject (use `pyyaml` + stdlib only — no need for openai/jinja2 here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for allowlist builder + scanner</name>
  <files>scripts/tests/test_verify_skill_references.py</files>
  <read_first>
    - scripts/build_model_catalog.py (existing model-scanning script — pattern reference for argparse/main shape)
    - plugins/video_gen/fal/plugin.yaml, plugins/image_gen/openai/plugin.yaml (allowlist source shape)
    - skills/movie-experts/animator/SKILL.md (lines 1-60 — phantom location)
    - skills/movie-experts/performer/SKILL.md (lines 1-60 — phantom location)
  </read_first>
  <behavior>
    - Test `test_build_allowlist_from_plugins`: given a temp plugins/ tree with one video_gen/x/plugin.yaml and one image_gen/y/plugin.yaml, `build_allowlist()` returns a set containing `"x"`, `"y"`, and any model tokens parsed from description (lowercased).
    - Test `test_build_allowlist_merges_manual_overrides`: when `_shared/known-external-models.yaml` contains `models: [sora, veo, kling_v3, runway_gen3]`, allowlist includes those entries alongside plugin-derived tokens.
    - Test `test_scan_flags_wan22_video_phantom`: feeding the real `skills/movie-experts/animator/SKILL.md` content produces a finding for `wan22_video` with line number context.
    - Test `test_scan_flags_168k_tokens_phantom`: feeding the real `performer/SKILL.md` produces a finding for `168K controlled tokens` (case-insensitive match).
    - Test `test_scan_passes_clean_file`: a SKILL.md containing only allowlisted model names produces zero findings.
    - Test `test_report_json_and_markdown`: `format_report(findings)` returns both a JSON-serializable dict and a Markdown string; both contain the finding text, file path, and line number.
  </behavior>
  <action>
    Create `scripts/tests/test_verify_skill_references.py`. Use stdlib `tempfile`, `pathlib`, `unittest.mock` (or pytest fixtures). Each test reads the REAL skill files from repo root (resolving relative to test file location) for phantom detection cases, but uses temp dirs for allowlist-builder isolation. Tests MUST fail at import time (module `verify_skill_references` does not yet exist) — this is the RED phase. Do not stub the module.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest scripts/tests/test_verify_skill_references.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    pytest collects 6 tests; all fail with ImportError or AttributeError (module/functions not yet implemented). Commit message: `test(00-01): add failing tests for verify_skill_references scanner`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scanner, allowlist builder, reporter</name>
  <files>scripts/verify_skill_references.py</files>
  <read_first>
    - scripts/tests/test_verify_skill_references.py (the contract — implement to pass these)
    - skills/movie-experts/animator/SKILL.md (verify regex catches `wan22_video`, `wan22_video_turbo`)
    - skills/movie-experts/performer/SKILL.md (verify regex catches `168K controlled tokens`)
    - CLAUDE.md (sections: Code Style, Import Organization, Error Handling — Ruff PLW1514 etc.)
  </read_first>
  <action>
    Implement `scripts/verify_skill_references.py` to GREEN phase (tests pass). Structure:
    (1) `MODEL_TOKEN_RE` — regex for model-name-shaped tokens: pattern `[a-z][a-z0-9]+_(?:video|turbo|tts|lora|model)` plus standalone vendor tokens (sora, veo, kling, runway, flux, minimax, glm, qwen, deepseek, yi). Case-insensitive. Also catch `\d+[Kk]\s+controlled\s+tokens` (the performer fabrication pattern).
    (2) `build_allowlist(plugins_root, override_yaml_path)` — walks `plugins/image_gen/*/plugin.yaml` and `plugins/video_gen/*/plugin.yaml`, parses with `yaml.safe_load` (encoding="utf-8"), extracts `name` field and tokenizes `description` for vendor/model words (split on non-alphanumeric, lowercase, filter len ≥ 3). Merges with `models:` list from `_shared/known-external-models.yaml`. Returns `set[str]`.
    (3) `scan_skill_file(skill_path, allowlist)` — reads file (encoding="utf-8"), runs `MODEL_TOKEN_RE.finditer` line-by-line, returns list of `Finding(path, line, matched_token, context_line)` for any token not in allowlist. Also handles the `168K controlled tokens` literal pattern.
    (4) `format_report(findings)` — returns `(json_dict, markdown_str)`. JSON shape: `{"total": N, "findings": [{"file": ..., "line": ..., "token": ..., "context": ...}]}`. Markdown: H1 "Skill Reference Audit", per-finding `**{token}** at `{file}:{line}`\n> {context_line}`.
    (5) `main()` — argparse with `--skills-dir` (default `skills/movie-experts`), `--plugins-dir` (default `plugins`), `--output-json` (optional path), `--output-md` (optional path), `--strict` (exit 1 if any findings). Globs `*/SKILL.md`, builds allowlist, scans each, writes reports, prints summary to stdout. Exit code: 0 if zero findings OR `--strict` not set; 1 if findings and `--strict`.

    Honor CLAUDE.md: every `open()` call passes `encoding="utf-8"`; `from __future__ import annotations` at top; `if __name__ == "__main__": sys.exit(main())` guard; argparse (not fire) for stdlib-only deps; specific exceptions bound to names; lazy %-formatting in any log/print.

    Must be invocable as `python scripts/verify_skill_references.py` from repo root with no args (uses defaults).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest scripts/tests/test_verify_skill_references.py -x --tb=short 2>&1 | tail -15 && echo "---" && python scripts/verify_skill_references.py --strict 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 tests pass. Live run reports `wan22_video` in animator and `168K controlled tokens` in performer (findings list is non-empty — phantoms are detected but NOT yet stripped; stripping happens in 00-04). JSON output valid; Markdown output human-readable. Exit code 1 under `--strict` (proves gate works). Commit message: `feat(00-01): implement verify_skill_references scanner`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create manual override allowlist for non-plugin external models</name>
  <files>skills/movie-experts/_shared/known-external-models.yaml</files>
  <read_first>
    - plugins/image_gen/*/plugin.yaml (5 files — to know what's already covered, avoid duplication)
    - plugins/video_gen/*/plugin.yaml (2 files)
    - .planning/research/SUMMARY.md (which external models are referenced as legit context — Sora, Veo 3.1, Kling v3, Runway Gen-3, FLUX 2, Stable Audio Open, CosyVoice 3, MiniMax, Qwen, DeepSeek, Yi)
  </read_first>
  <action>
    Create `_shared/known-external-models.yaml` (parent dir `_shared/` will be created). Schema:
    ```yaml
    # Manual allowlist for models/vendors NOT registered as Hermes plugins.
    # Auto-merged with plugin-derived tokens at verify_skill_references.py runtime.
    # Keep entries lowercase; scanner is case-insensitive.
    # Each entry should carry a one-line provenance note.
    models:
      - name: sora
        provenance: "OpenAI Sora (closed beta; referenced as context only)"
      - name: veo
        provenance: "Google Veo 3.1 (accessed via fal plugin)"
      - name: kling
        provenance: "Kuaishou Kling v3 (accessed via fal plugin)"
      # ... continue for: runway, flux2, flux_2, stable_audio_open, cosyvoice3, minimax_tts, qwen, deepseek, yi, glm
    ```
    Include AT LEAST these 12 entries with provenance. Do NOT include fabricated names (`wan22_video`, `flux_1_nsfw`, etc.) — those are the phantoms we want flagged.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "import yaml; d=yaml.safe_load(open('skills/movie-experts/_shared/known-external-models.yaml',encoding='utf-8')); assert len(d['models'])>=12, f'expected >=12 entries, got {len(d[\"models\"])}'; assert all('provenance' in m for m in d['models']); print('OK', len(d['models']), 'entries')" && python scripts/verify_skill_references.py 2>&1 | grep -c "wan22_video\|168K controlled"</automated>
  </verify>
  <done>
    YAML parses cleanly; ≥12 model entries each with provenance. Re-running `verify_skill_references.py` still flags `wan22_video` and `168K controlled tokens` (those are NOT in the override list — phantoms remain detected). Commit message: `feat(00-01): publish known-external-models allowlist`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugins/* → script | Allowlist source is local git-tracked files (low risk) |
| SKILL.md body → script | Untrusted markdown content scanned via regex (input injection risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | scanner output leaks file contents | accept | context_line is single-line; skill files are not secrets |
| T-00-02 | Tampering | allowlist bypass via crafted SKILL.md | mitigate | regex is deny-by-default (anything model-name-shaped not in allowlist is flagged); no exec/eval on file content |
| T-00-03 | Denial of Service | regex catastrophic backtracking on large SKILL.md | mitigate | use linear regex patterns (no nested quantifiers); files are <2KB each |
| T-00-04 | Repudiation | non-deterministic lint runs | mitigate | YAML sorted output; allowlist built from deterministic plugin walk (sorted) |
| T-00-SC | Tampering | no package installs in this plan | accept | stdlib + pyyaml only (already pinned in pyproject); no pip/npm operations |
</threat_model>

<verification>
- `python -m pytest scripts/tests/test_verify_skill_references.py` — 6/6 passing
- `python scripts/verify_skill_references.py --strict --output-json /tmp/audit.json --output-md /tmp/audit.md` — exits 1, JSON valid, Markdown readable
- `/tmp/audit.json` contains findings for `wan22_video` and `168K controlled tokens`
- No new entries in `pyproject.toml` dependencies (verify with `git diff pyproject.toml | wc -l` == 0)
- Ruff clean: `ruff check scripts/verify_skill_references.py scripts/tests/test_verify_skill_references.py` — no errors (PLW1514 compliance)
</verification>

<success_criteria>
- `scripts/verify_skill_references.py` exists, executable as `python scripts/verify_skill_references.py`, exits 0/1 cleanly
- Detects both known phantoms (`wan22_video`, `168K controlled tokens`) on the real 14 SKILL.md files
- Allowlist auto-builds from `plugins/*/plugin.yaml` at runtime + merges manual overrides from `_shared/known-external-models.yaml`
- Dual output (JSON + Markdown) supported
- Zero new packages added
- Test coverage for allowlist builder, scanner, and reporter
</success_criteria>

<output>
Create `.planning/phases/00-audit-eval-skeleton-blocker-gate/00-01-SUMMARY.md` when done. Include: scanner invocation example, sample finding output, allowlist entry count, total phantom count detected (not yet stripped — that's 00-04).
</output>

---
phase: 7
plan: 03
title: "Candidate Node Set (§4) + Node-Count Audit (§5)"
type: standard
wave: 3
depends_on: [02]
files_modified:
  - .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md
autonomous: true
requirements: [DERIV-02, DERIV-04, DERIV-05, DERIV-06]
must_haves:
  - "§4 enumerates the candidate node set (target 8-15 nodes, hard ceiling ≤25 per CONTEXT.md Area 1/4) with English kebab-case IDs"
  - "Every candidate node has ALL required per-node fields populated: `derivation` (defends existence from first principles), `alternatives-considered` (MADR-style ≥1 rejected option with concrete con-reason), `assumption-classification` (contingent vs validated-invariant), `epistemic-status` (tags on core claims), `steelman-elimination` (strongest 'this node should not exist' argument + response), `corpus-anchor` (≥1 STACK §1.4 or Hermes-corpus reference), `analogy-validity` (analogy-valid / analogy-breaks-here)"
  - "§5 node-count audit confirms count is in [8, 25] range; if >15, each node beyond target carries explicit justification in its `derivation` field"
  - "Node IDs are model-agnostic (no Sora/Kling/Veo names) — PITFALLS 1.3 + 2.7 avoidance"
  - "Node IDs preserve v1 expert_id convention where a candidate maps cleanly to an existing expert (e.g., creative_source, script_auditor, cinematographer) — HANDOFF-02 forward-reference; v1 FOUND-08 frozen rule"
  - "No node is reverse-engineered into first principles (PITFALLS 1.6) — every node's steelman-elimination paragraph is substantive, not strawman"
---

# Plan 07-03: Candidate Node Set (§4) + Node-Count Audit (§5)

<objective>
Write §4 (the candidate node set — the actual output of Phase 7's derivation) and §5 (node-count audit). This is where the derivation lands on concrete node IDs that Phases 8-12 will build upon. Per CONTEXT.md Area 1/4: Phase 7 emits a CANDIDATE set, not a final set — Phase 8 applies the C1-C7 filter to compress further.
</objective>

<phase_goal>
**Plan 03's slice of the Phase 7 goal:** A reader can read §4 and see the candidate node set — each node with full per-node rigor (derivation, alternatives, classifications, steelman, anchor, analogy-validity) — and §5 confirms the count is within the 8-15 target / ≤25 ceiling. The candidate set is the artifact Phases 8-12 inherit.
</phase_goal>

<context>
<files_to_read_first>
- .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md (the file from Plans 01+02; §0-§3 establish the frame + derivation trace; DO NOT overwrite)
- .planning/phases/07-first-principles-derivation/07-CONTEXT.md (Area 1/4 candidate-set emission + node count + re-evaluation-with-audit-log; Area 4/4 English kebab-case IDs)
- .planning/phases/07-first-principles-derivation/07-RESEARCH.md §4 (MADR alternatives-considered format), §7 (contingent-vs-validated-invariant classification framework), §8 (deliverable skeleton §4 description)
- .planning/research/PITFALLS.md §1.2 (don't discard validated craft), §1.3 (no premature model-commitment), §1.6 (no reverse-engineering), §2.6 (node-count budget), §5.3 (validated-invariant vs contingent)
- .planning/research/FEATURES.md §3 (41-candidate palette — REFERENCE ONLY for naming convention + avoiding accidental inheritance by analogy; the derivation must produce its own candidate set, NOT inherit FEATURES' palette)
- .planning/research/ARCHITECTURE.md (Phase 8 NODE-02 + NODE-03 schemas — Phase 7's per-node fields must be COMPATIBLE with Phase 8's expansion; do NOT produce Phase 8 fields like cost_budget, latency_budget, model_horizon — those are Phase 8)
- skills/movie-experts/README.md (26-expert inventory — Phase 7 candidate IDs should preserve v1 expert_id convention where mapping is clean)
</files_to_read_first>

<interfaces>
N/A — markdown only. Plan 03's interface to Plan 04 (audit + open questions) is the candidate node set + count. Plan 04's §6 audit will walk the candidate nodes against the 10 PITFALLS failure modes; §7 open questions will record any nodes flagged for re-derivation.

Key contracts Plan 04 will inherit:
- The list of candidate node IDs (English kebab-case)
- Per-node `analogy-validity` classification (Plan 04 §6 audit checks 5.4 — "deep derivation only where analogy breaks")
- Per-node `assumption-classification` (Plan 04 §6 audit checks 5.3 — "validated-invariants preserved")
- The count + node-count-audit rationale
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write §4 — enumerate candidate node set with full per-node rigor</name>
  <read_first>
    - .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md (§3 derivation trace — §3.5 synthesis gives the structural sketch §4 enumerates; DO NOT overwrite §0-§3)
    - .planning/phases/07-first-principles-derivation/07-RESEARCH.md §4 (MADR alternatives-considered format example) + §7 (assumption classification) + §8 (§4 deliverable skeleton)
    - .planning/research/PITFALLS.md §1.2 (don't discard Murch/Field/180-axis as bias) + §1.3 (no Sora/Kling/Veo model commitment) + §1.6 (no reverse-engineering) + §2.6 (8-15 target, ≤25 ceiling)
    - .planning/research/FEATURES.md §3 (41-candidate palette — naming convention reference; AVOID importing by analogy)
    - skills/movie-experts/README.md (v1 26-expert inventory — preserve expert_id convention where mapping is clean)
  </read_first>
  <files>.planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md</files>
  <action>
Append §4 to `00-FIRST-PRINCIPLES.md` (do NOT overwrite §0-§3).

**§4 — Candidate node set** (~2-4 pages):

**§4.0 — Candidate-set framing** (1 paragraph): re-state per CONTEXT.md Area 1/4 — this is a CANDIDATE set, not a final set. Phase 8 applies the C1-C7 filter to compress further. Phase 7's job is to produce a defensible palette where each node defends its existence from first principles. Target 8-15 nodes; hard ceiling ≤25; if >15, each node beyond target carries explicit justification in its `derivation` field.

**§4.1 — Per-node entries** (the bulk):

For EACH candidate node, write a subsection with this structure (use the per-node template below). The actual node IDs and content are for the executor to derive from §3.5's structural sketch — likely candidates include (executor confirms via derivation, not by analogy to this list):

- `creative_source` (root node — from Q1 + Q4 synthesis; mines story kernels from lived experience)
- `style_genome` (defines reusable visual-DNA — from Q2)
- `screenplay` (narrative intent — from Q1; may compress traditional script+outline)
- `script_auditor` (5-dim quantitative critic — from Q2; v1 script_auditor precedent)
- `character_designer` (identity asset — from Q1+Q2)
- `cinematographer` (visual intent — from Q2; v1 cinematographer precedent)
- `storyboard_designer` (visual spec — from Q2; PITFALLS 1.3 candidate for AIGC compression)
- `drawer` + `animator` (visual execution — from Q3)
- `voicer` + `lip_sync` (audio-video lock — from Q3; AIGC-native explicit alignment)
- `editor`, `colorist`, `composer`, `foley`, `mixer` (post-production cluster — from Q2+Q3)
- `continuity_auditor` (cross-shot consistency verifier — from Q2; AIGC-specific)
- `prompt_injector` (intent → model tokens — from Q3; AIGC-native consistency mechanism)
- `hook_retention` (commercial engine for 短剧 — from Q2; v1 hook_retention precedent)
- `quality_gate` (multi-dim quantitative scorer — from Q2)
- `compliance_pre_check` + `compliance_final` (CN platform gate — from Q2; v1 compliance_marketing precedent)
- `theory_critic` (consultative vertical — from Q4; META-06 manual-trigger)
- `camera_preview` (low-param cheap-fail loop — from Q3; AIGC-native rough-cut/final-cut split)

(The executor selects 8-15 from this palette via §3.5 synthesis, NOT by inheritance.)

**Per-node template** (use for every candidate node):

```markdown
### Node: `<node_id>`

> v1 expert_id mapping: `<existing_expert_id>` (preserved per HANDOFF-02 / FOUND-08) / NEW (no v1 precedent)

**Core task** (one sentence, CN prose): [what this node does]

**Derivation** (defends existence from first principles — NOT "every pipeline has this"):
[2-4 sentence paragraph citing the §3 derivation step that produces this node. Must reference specific intermediate conclusion from §3.5. If you find yourself writing "because traditional film pipelines have..." — STOP and re-derive from §3.]

**Epistemic-status tags on core claims:**
- `[physical: ...]` — [claim]
- `[psychological: ...]` — [claim]
- `[platform-algorithmic: ...]` — [claim, if any]
- `[tool-capability: ...]` — [claim, if any]

**Assumption classification:**
- `validated-invariant`: [e.g., "180° axis applies because audience spatial orientation is perceptual"]
- `contingent`: [e.g., "storyboard persists as a separate artifact (could be transient if gen model accepts visual refs)"]

**Steelman-the-elimination:**
- **Strongest case for removing this node:** [the most serious argument that this node should NOT exist — must be substantive, NOT a strawman]
- **Response:** [why the node survives — must directly address the steelman, not change the subject]
- **Verdict:** SURVIVES / RECONSIDER / MERGE

**Alternatives considered (MADR-style):**
- **Slot this node fills:** [role in DAG]
- **Option 1: `<chosen_node_id>`** (CHOSEN) — [description]
  - Pros: [...]
  - Cons: [...]
- **Option 2: `<rejected_alt_1>`** (REJECTED) — [description]
  - Pros: [...]
  - Cons: [concrete failure mode — NOT "less preferred"]
- **Decision driver:** [why Option 1 won — references §3 derivation]

**Corpus anchor:** ≥1 reference from STACK §1.4 or Hermes-integrated corpus
- [Corpus citation with file path + why this source supports this node]

**Analogy validity:**
- `analogy-valid` (existing film pipeline applies directly — minimal first-principles re-derivation needed)
- `analogy-breaks-here` (existing pipeline assumes something false in AIGC — deep first-principles derivation applied)
- [One-sentence rationale per PITFALLS 5.4]

---
```

Write all 8-15 candidate node entries (or up to 25 if justification is provided) using this template. CN prose throughout (per bilingual policy); English for field labels, tags, citations.

**Critical discipline (PITFALLS 1.6 prevention):** Before finalizing each node's steelman-elimination paragraph, ask: "Would a serious design critic actually make this counter-argument?" If no, rewrite — a strawman steelman is worse than no steelman because it gives the appearance of rigor.
  </action>
  <verify>
    - `grep -c '## §4' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns 1
    - `grep -cE '^### Node: `' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns ≥8 (minimum target) and ≤25 (hard ceiling)
    - For each node entry, all 7 required fields present: `Core task` + `Derivation` + `Epistemic-status` + `Assumption classification` + `Steelman-the-elimination` + `Alternatives considered` + `Corpus anchor` + `Analogy validity` (note: 8 fields if counting Core task separately)
    - `grep -c 'REJECTED' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns ≥8 (≥1 rejected alternative per node × ≥8 nodes)
    - `grep -c 'analogy-valid\|analogy-breaks-here' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns ≥8 (one per node)
    - `grep -cE 'validated-invariant' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns ≥8 (one per node)
    - `grep -ciE 'sora|kling|veo|wan2|seedance' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns 0 in node IDs or core task descriptions (PITFALLS 1.3 — no premature model commitment; OK to mention in research/derivation context if explicitly flagged as volatile tool-capability)
    - Manual scan: each node's `Derivation` field references a specific §3 intermediate conclusion (NOT "every pipeline has this" or "traditional workflow does this")
    - Manual scan: each node's steelman-elimination paragraph is substantive (not a strawman — a serious design critic would actually make the counter-argument)
    - Manual scan: each node's `Alternatives considered` REJECTED option has a concrete con-reason (not "less preferred")
  </verify>
  <done>
- §4 enumerates 8-15 candidate nodes (or up to 25 with justification)
- Each node has all required per-node fields populated (Core task + Derivation + Epistemic-status + Assumption classification + Steelman-elimination + Alternatives-considered + Corpus anchor + Analogy validity)
- Every node ID is English kebab-case, model-agnostic (no Sora/Kling/Veo)
- Every node's `Derivation` references a §3 intermediate conclusion, not "every pipeline has this"
- Every node has ≥1 REJECTED alternative with concrete con-reason
- Every node has substantive steelman-elimination (not strawman)
- v1 expert_id mapping preserved where clean (HANDOFF-02 / FOUND-08)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write §5 — node-count audit</name>
  <read_first>
    - .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md (§4 from Task 1 — count the actual nodes)
    - .planning/phases/07-first-principles-derivation/07-CONTEXT.md (Area 1/4: 8-15 target, ≤25 hard ceiling, re-evaluation allowed with audit log)
    - .planning/research/PITFALLS.md §2.6 ("more nodes ≠ more sophisticated" — count is a design smell, not achievement)
  </read_first>
  <files>.planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md</files>
  <action>
Append §5 to `00-FIRST-PRINCIPLES.md` (do NOT overwrite §0-§4).

**§5 — Node-count audit** (~0.5 page):

**§5.1 — Final count:** state the candidate node count from §4. Compare to the 8-15 target.

**§5.2 — Target band assessment:**
- If count is in [8, 15]: state "count is within target band — Phase 8 may compress further via C1-C7 filter"
- If count is in [16, 25]: state "count exceeds target band — each node beyond 15 must justify its non-mergeability with adjacent nodes". Then for EACH node beyond the 15th, write a 1-paragraph justification in this section (not in the node's §4 entry — keep §4 entries uniform). Reference the §3 derivation step that requires this node to be separate.
- If count is > 25: HARD CEILING VIOLATED — the executor must return to §4 and merge/remove nodes until count ≤ 25. Per CONTEXT.md Area 1/4 the hard ceiling is non-negotiable without a separate milestone amendment.

**§5.3 — Smell-detection (PITFALLS §2.6):**
- Did count grow during §4 writing (started at 8, ended higher)? If yes, document why each addition was necessary.
- Is any node a candidate for merging with an adjacent node? List any merge-candidates identified during §4 writing (even if not acted on) — Phase 8's C1-C7 filter will revisit.

**§5.4 — Per CONTEXT.md Area 1/4 re-evaluation rule:** state explicitly that re-evaluation was allowed mid-Phase-7 with audit log entry. If count deviated from initial plan, document the deviation here.
  </action>
  <verify>
    - `grep -c '## §5' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns 1
    - `grep -cE '### §5\.[0-9]' .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md` returns ≥3 (§5.1 count + §5.2 target band + §5.3 smell detection; §5.4 optional)
    - Manual count: number of `^### Node: ` entries in §4 must match the count stated in §5.1
    - If count > 15: §5.2 contains a justification paragraph for EACH node beyond 15
    - If count > 25: executor returns to Task 1 to merge/remove (HARD CEILING ENFORCED)
  </verify>
  <done>
- §5.1 states the final count
- §5.2 places the count in target band [8, 15] OR over-target band [16, 25] with per-node justification
- §5.3 smell-detection performed (growth-during-writing + merge-candidates listed)
- §5.4 documents any re-evaluation deviation per CONTEXT.md Area 1/4
- If hard ceiling (>25) violated: returned to Task 1 to merge/remove before declaring §5 done
  </done>
</task>

</tasks>

<verification>

**Plan 07-03 completion criteria:**

- [ ] §4 enumerates 8-15 candidate nodes (or up to 25 with justification)
- [ ] Every node has 8 required per-node fields populated (Core task, Derivation, Epistemic-status, Assumption classification, Steelman-elimination, Alternatives-considered, Corpus anchor, Analogy validity)
- [ ] Every node ID is English kebab-case, model-agnostic (no Sora/Kling/Veo)
- [ ] Every node's `Derivation` field references a §3 intermediate conclusion
- [ ] Every node has ≥1 REJECTED alternative with concrete con-reason (not "less preferred")
- [ ] Every node's steelman-elimination is substantive (not strawman)
- [ ] Every node preserves v1 expert_id mapping where applicable (HANDOFF-02)
- [ ] §5 node-count audit confirms count in [8, 25]
- [ ] If count > 15: per-node justification provided in §5.2
- [ ] §5.3 smell-detection performed
- [ ] §5.4 documents any re-evaluation deviation

**Manual audit (per VALIDATION.md):**
- Reviewer scans each node's `derivation` field; flags any that read "every pipeline has this" or "the inherited pipeline does this"
- Reviewer reads each node's steelman paragraph; if strongest-counter-argument feels like strawman, flag as RECONSIDER
- Reviewer confirms each REJECTED option has a concrete con-reason (not "less preferred")
- Reviewer spot-checks 5-10 classifications; flags any `validated-invariant` lacking empirical grounding
</verification>

<success_criteria>

**Goal-backward derivation** — for Plan 07-03 to succeed, these truths must hold:

- A reader can read §4 and name the candidate node set
- A reader can challenge any node by asking "why does this node exist?" and find the `derivation` answer citing §3
- A reader can challenge any node by asking "what would you have put here instead?" and find the `alternatives-considered` log with ≥1 REJECTED option
- A reader can challenge any node by asking "what's the strongest case for removing this?" and find the `steelman-elimination` paragraph
- A reader can challenge any node by asking "which corpus sources support this?" and find the `corpus-anchor` citation
- A reader can challenge any node by asking "is this analogy-valid or does the analogy break in AIGC?" and find the `analogy-validity` classification
- A reader can verify the count is in [8, 25] and that any over-target nodes are justified

These candidate nodes are the load-bearing artifact Phases 8-12 inherit. If §4 is wrong, the entire milestone is wrong.
</success_criteria>

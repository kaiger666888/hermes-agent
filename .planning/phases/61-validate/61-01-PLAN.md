---
phase: 61-validate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_milestone_audit.py
  - .planning/milestones/v12.0-MILESTONE-AUDIT.md
  - .planning/research/v12-poc-eval/production-smoke-report.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VALIDATE-01]

must_haves:
  truths:
    - "All 8 v12.0 reqs verified at automated level with documented evidence pointers"
    - "v12.0 audit verdict declared: passed | tech_debt | gaps_found | fail"
    - "3 operator-action handoffs aggregated in §3 runbook (P57 SC#2 smoke + P60 EVAL-01 + P60 EVAL-02)"
    - "Production smoke report exists; SC#2 either measured against real GLM or marked human_needed"
    - "REQUIREMENTS.md traceability table shows all 8 Status cells flipped from Pending"
  artifacts:
    - path: "scripts/run_milestone_audit.py"
      provides: "Milestone-aware audit coverage matrix producer (--milestone v11.0|v12.0)"
      contains: "HUMAN_VERIFICATION_REQ_MAP_V12"
    - path: ".planning/milestones/v12.0-MILESTONE-AUDIT.md"
      provides: "v12.0 close-out audit (8 reqs walked + 3 operator-action runbook + verdict)"
      min_lines: 200
    - path: ".planning/research/v12-poc-eval/production-smoke-report.md"
      provides: "v12.0 production smoke report (SC#2 measurement or human_needed handoff)"
      min_lines: 120
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with 8 Status cells updated"
  key_links:
    - from: "scripts/run_milestone_audit.py"
      to: ".planning/phases/57-endpoint-routing/57-VERIFICATION.md"
      via: "PHASE_VERIFICATION_FILES_V12 dict + REQ_ID_PATTERN_V12 regex"
      pattern: "PHASE_VERIFICATION_FILES_V12|57-endpoint-routing"
    - from: ".planning/milestones/v12.0-MILESTONE-AUDIT.md §0 Scorecard"
      to: "audit-matrix.json output"
      via: "cited counts (satisfied_reqs, human_needed_reqs, operator_action_count)"
      pattern: "satisfied_reqs|operator_action_count"
    - from: ".planning/research/v12-poc-eval/production-smoke-report.md"
      to: "scripts/run_screenplay_step3_roundtable.py --smoke"
      via: "operator-action runbook entry"
      pattern: "run_screenplay_step3_roundtable.py --smoke"
---

<objective>
v12.0 Production Hardening milestone close-out — produce the audit + production-smoke
evidence package that declares VALIDATE-01 satisfied.

Purpose: Mirror the v11.0 Phase 56 / v10.0 Phase 51 close-out pattern. Walk all 8 v12.0
reqs (ENDPOINT-01 + THROTTLE-01/02 + POOL-01/02 + EVAL-01/02 + VALIDATE-01) against
Phase 57-60 VERIFICATION.md evidence; aggregate the 3 operator-action handoffs (P57 SC#2
live GLM smoke + P60 EVAL-01 mem0 benchmark + P60 EVAL-02 fitness battery); declare a
verdict; and either measure or defer the SC#2 production smoke.

Output:
- `scripts/run_milestone_audit.py` extended with `--milestone v11.0|v12.0` flag
- `.planning/milestones/v12.0-MILESTONE-AUDIT.md` (NEW — verdict: passed expected)
- `.planning/research/v12-poc-eval/production-smoke-report.md` (NEW — SC#2 result or human_needed)
- `.planning/REQUIREMENTS.md` traceability table (8 Status cells flipped)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-validate/61-CONTEXT.md
@.planning/milestones/v11.0-MILESTONE-AUDIT.md

# Phase VERIFICATIONs — evidence source for §2 traceability
@.planning/phases/57-endpoint-routing/57-VERIFICATION.md
@.planning/phases/58-rpm-throttling/58-VERIFICATION.md
@.planning/phases/59-aux-pool-isolation/59-VERIFICATION.md
@.planning/phases/60-live-eval/60-VERIFICATION.md

# Existing audit script + smoke driver (TASK 1 modifies script; TASK 3 may invoke driver)
@scripts/run_milestone_audit.py
@scripts/run_screenplay_step3_roundtable.py

<interfaces>
<!-- Contracts the executor needs. Extracted from source files. -->

From scripts/run_milestone_audit.py (the script Task 1 extends):
```python
# CURRENT — v11.0-hardcoded. Task 1 MUST refactor to milestone-aware dispatch.
REQUIREMENTS_FILE = REPO_ROOT / ".planning" / "REQUIREMENTS.md"
PHASE_VERIFICATION_FILES = {  # hardcoded v11.0 phases 52-55
    "52": REPO_ROOT / ".planning" / "phases" / "52-infra-foundation" / "52-VERIFICATION.md",
    ...
}
REQ_ID_PATTERN = re.compile(
    r"^(INFRA-0[1-4]|CREATIVE-0[1-2]|EVAL-0[1-7]|MIGR-01|VALIDATE-01)$"
)
TRACE_ROW_PATTERN = re.compile(
    r"^\|\s*([A-Z]+-[0-9]{2})\s*\|\s*([0-9]{2})\s*\|\s*([A-Z]+)\s*\|"
)
HUMAN_VERIFICATION_REQ_MAP: dict[str, list[str]] = {
    "53": ["CREATIVE-01"],
    "54": ["EVAL-01", "EVAL-02", "EVAL-03"],
    "55": ["EVAL-04"],
}

def run_audit(out_path: Path | None = None) -> dict[str, Any]: ...
def main(argv: list[str] | None = None) -> int: ...
```

v12.0 facts the executor needs:
- v12.0 phases: 57, 58, 59, 60, 61 (5 phases — last is this audit itself)
- v12.0 req IDs: ENDPOINT-01, THROTTLE-01, THROTTLE-02, POOL-01, POOL-02, EVAL-01, EVAL-02, VALIDATE-01 (8 reqs)
- v12.0 VERIFICATION files (confirmed by reading 57-60):
  - 57-VERIFICATION.md: status `human_needed`, 1 handoff (SC#2 live GLM smoke deferred to P61) → maps to ENDPOINT-01
  - 58-VERIFICATION.md: status `passed`, 0 handoffs → THROTTLE-01 + THROTTLE-02 fully satisfied
  - 59-VERIFICATION.md: status `passed` (re-verified after gap closure), 0 handoffs → POOL-01 + POOL-02 fully satisfied
  - 60-VERIFICATION.md: status `human_needed`, 2 handoffs (EVAL-01 mem0 + EVAL-02 fitness battery) → maps to EVAL-01, EVAL-02
- Phase 61 VALIDATE-01 = this audit (self-confirming per Phase 56 precedent)
- v12.0 Requirements Coverage status-cell text uses: `SATISFIED` (58/59), `STRUCTURALLY SATISFIED (infra) — live run DEFERRED` (60), `SATISFIED` for ENDPOINT-01 row in P57 with explicit "SC#2 smoke target live measurement deferred to Phase 61" note in evidence cell

Expected counts (for verdict = `passed`):
- total_reqs = 8
- satisfied_reqs = 5 (ENDPOINT-01 + THROTTLE-01 + THROTTLE-02 + POOL-01 + POOL-02 + VALIDATE-01 = 6, OR 5 if ENDPOINT-01 treated as human_needed because of P57 SC#2 handoff)
- human_needed_reqs = 2 or 3 (EVAL-01 + EVAL-02 from P60; plus ENDPOINT-01 if P57's SC#2 handoff is attributed to it)
- operator_action_count = 3 (P57×1 + P60×2; VALIDATE-01 self has none)

Verdict = `passed` if (satisfied + human_needed) == 8 AND operator_action_count >= 3 AND no `failed`.

Verdict logic convention (per CONTEXT.md D-3 + v11.0 §7):
- operator-action items count as `passed-with-operator-deferral` (NOT blocking design gaps)
- runtime validations of code already verified by automated tests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend run_milestone_audit.py with --milestone v11.0|v12.0 dispatch</name>
  <files>scripts/run_milestone_audit.py</files>
  <action>
Extend the existing stdlib-only audit script so the same file serves both v11.0
(Phase 56 precedent) and v12.0 (Phase 61 — this plan) close-outs via a new
`--milestone` CLI flag (default `v11.0` to preserve backward compatibility).

Refactor strategy — extract milestone-specific data into two module-level dicts
keyed by milestone version, dispatch on the CLI arg:

1. `MILESTONE_PHASE_VERIFICATION_FILES: dict[str, dict[str, Path]]` — keyed by
   `"v11.0"` (existing 52-55 map, unchanged) and `"v12.0"` (NEW: 57-60 map for
   `57-endpoint-routing`, `58-rpm-throttling`, `59-aux-pool-isolation`,
   `60-live-eval`). Phase 61 (this audit) is NOT in the map — its verdict is
   self-derived like Phase 56 was.

2. `MILESTONE_REQ_ID_PATTERNS: dict[str, re.Pattern[str]]` — keyed by milestone.
   v11.0 keeps the existing regex unchanged. v12.0 uses:
   `r"^(ENDPOINT-01|THROTTLE-0[1-2]|POOL-0[1-2]|EVAL-0[1-2]|VALIDATE-01)$"`.

3. `MILESTONE_HUMAN_VERIFICATION_REQ_MAP: dict[str, dict[str, list[str]]]` —
   keyed by milestone. v11.0 keeps the existing map (53→[CREATIVE-01],
   54→[EVAL-01,EVAL-02,EVAL-03], 55→[EVAL-04]). v12.0 NEW:
   - `"57": ["ENDPOINT-01"]` — P57 has 1 handoff (SC#2 live GLM smoke, deferred
     to P61 per ROADMAP Phase 57 SC#2). Per CONTEXT.md decision #6, this handoff
     aggregates into the §3 runbook. The audit attributes it to ENDPOINT-01
     (the only req Phase 57 owns; SC#2 is its production smoke).
   - `"60": ["EVAL-01", "EVAL-02"]` — P60 has 2 handoffs (mem0 benchmark +
     fitness battery real-mode) per 60-VERIFICATION.md frontmatter
     `human_verification:` block. Order matches the 2 handoff entries.

4. `MILESTONE_AUDIT_SELF_PHASE: dict[str, str]` — v11.0→"56", v12.0→"61".
   Used to mark VALIDATE-01 as `passed` (self-confirming per Phase 56 precedent).

5. `MILESTONE_OPERATOR_ACTION_FLOOR: dict[str, int]` — v11.0→5, v12.0→3.
   Operator-action-count threshold for verdict-logic gate.

Update `_build_coverage_matrix` to accept `milestone: str` parameter and
dispatch via these dicts. Update the JSON output `milestone` field to reflect
the chosen milestone. Update verdict-logic sub-check naming to be
milestone-agnostic (e.g. `all_reqs_have_automated_verification` instead of
`all_15_have_automated_verification`).

CLI additions:
- New `--milestone {v11.0,v12.0}` arg (default `v11.0` — backward compat).
- Pass `milestone` into `run_audit(milestone=..., out_path=...)`.
- `--help` updated to describe both modes.

CRITICAL preservation rules:
- v11.0 behavior MUST be byte-identical when invoked without `--milestone` (or
  with `--milestone v11.0`). The v11.0 audit shipped as
  `.planning/milestones/v11.0-MILESTONE-AUDIT.md` must remain reproducible.
- All `open()` calls continue to pass `encoding="utf-8"` (Ruff PLW1514).
- No new third-party deps (stdlib-only per CLAUDE.md).
- Keep `_extract_frontmatter` + `_parse_req_coverage_table` + `_parse_verification`
  UNCHANGED — they already handle arbitrary VERIFICATION.md formats; only the
  DATA they consume (which file, which req pattern) becomes milestone-aware.

After refactor, the script must produce correct v12.0 output:
`total_reqs=8, satisfied_reqs>=5, human_needed_reqs>=2 (likely 3 with ENDPOINT-01),
failed_reqs=0, operator_action_count=3, recommended_verdict=passed`.

Test both invocations:
1. `python3 scripts/run_milestone_audit.py --milestone v11.0 --out /tmp/v11-audit-regression.json`
   → must produce identical counts to the v11.0 baseline (15 reqs, 5 handoffs).
2. `python3 scripts/run_milestone_audit.py --milestone v12.0 --out /tmp/v12-audit-matrix.json`
   → must produce 8 reqs, 3 handoffs, verdict `passed`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python3 scripts/run_milestone_audit.py --milestone v12.0 --out /tmp/v12-audit-matrix.json && python3 -c "import json; m=json.load(open('/tmp/v12-audit-matrix.json', encoding='utf-8')); assert m['milestone']=='v12.0', f'milestone={m[\"milestone\"]}'; assert m['total_reqs']==8, f'total_reqs={m[\"total_reqs\"]}'; assert m['failed_reqs']==0, f'failed_reqs={m[\"failed_reqs\"]}'; assert m['operator_action_count']==3, f'operator_action_count={m[\"operator_action_count\"]}'; assert m['verdict_logic']['recommended_verdict']=='passed', f\"verdict={m['verdict_logic']['recommended_verdict']}\"; print('OK: v12.0 audit matrix verified:', {k: m[k] for k in ('total_reqs','satisfied_reqs','human_needed_reqs','failed_reqs','operator_action_count')})"</automated>
    <automated>cd /data/workspace/hermes-agent && python3 scripts/run_milestone_audit.py --milestone v11.0 --out /tmp/v11-audit-regression.json && python3 -c "import json; m=json.load(open('/tmp/v11-audit-regression.json', encoding='utf-8')); assert m['milestone']=='v11.0'; assert m['total_reqs']==15; assert m['operator_action_count']==5; assert m['verdict_logic']['recommended_verdict']=='passed'; print('OK: v11.0 regression preserved (15 reqs, 5 handoffs, verdict=passed)')"</automated>
  </verify>
  <done>
- `scripts/run_milestone_audit.py` accepts `--milestone {v11.0,v12.0}` (default v11.0)
- v11.0 regression: 15 reqs / 5 handoffs / verdict=passed (byte-identical counts)
- v12.0 new: 8 reqs / 3 handoffs / verdict=passed
- Ruff PLW1514 clean (no bare `open()`)
- Stdlib-only (no new third-party deps)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author .planning/milestones/v12.0-MILESTONE-AUDIT.md with verdict</name>
  <files>.planning/milestones/v12.0-MILESTONE-AUDIT.md</files>
  <action>
Author the v12.0 Production Hardening milestone audit, mirroring the v11.0
audit structure (10 sections) with v12.0-specific content. Output to
`.planning/milestones/v12.0-MILESTONE-AUDIT.md` (NEW file, 200+ lines).

YAML frontmatter (mirror v11.0 shape, v12.0 values):
```
milestone: v12.0
milestone_name: Production Hardening
audited: 2026-07-08T...Z   (use actual completion time)
status: passed
verdict: `passed`
baseline_commit: <first v12.0 commit, e.g. the Phase 57 PLAN commit>
head_commit: <commit hash of Task 1 of this plan>
scores:
  requirements: 8/8
  phases: 5/5           # Phase 57-61
  plans: 7/7            # 57(1) + 58(2) + 59(2) + 60(2) + 61(1)
  operator_action_handoffs: 3   # 1 from P57 + 2 from P60
  hardening_gaps_closed: 4/4    # the 4 from ROADMAP §"Hardening gaps targeted"
  pitfalls_mitigated: 7/7       # carried from v11.0, runtime-validated in v12.0
gaps:
  requirements: []
  integration: []
tech_debt:
  - phase: 57
    items:
      - "SC#2 live GLM latency measurement deferred to Phase 61 production-smoke-report.md (proxy signal Test 13 verified)"
  - phase: 60
    items:
      - "EVAL-01 live mem0 p95 — infrastructure shipped, operator run deferred (MEM0_API_KEY)"
      - "EVAL-02 real-mode fitness battery — infrastructure shipped, operator run deferred (GLM_AUX_API_KEY_1..4)"
  - v12.0_scope_deferrals:
      - "15-expert full transform (only 9 sample agents shipped in v11.0; 6 deferred to v13.0)"
      - "Option B → 物理分区 migration (v12.0 still uses mem0 single backend + agent_id filter; physical partition migration is v13+)"
      - "Curator self-evolution live tick (v12.0 keeps dry-run default per v11.0 EVAL-06)"
      - "Round table 异步并发 (v12.0 keeps INFRA-04 serial constraint per GLM upstream capacity)"
      - "Production deployment + live traffic (v12.0 is hardening, not deployment)"
nyquist:
  compliant_phases: [57, 58, 59, 60, 61]
  overall: compliant
design_only: false
runtime_implementation: true
found08_preserved: n/a          # v12.0 touches zero SKILL.md (same as v11.0)
```

Body sections (10 — mirror v11.0, v12.0 content):

**§0 — Scorecard.** Table with: Requirements 8/8 (cite satisfied + human_needed
split from audit-matrix.json); Phases 5/5; Plans 7/7; Operator-action handoffs
3; Hardening gaps closed 4/4 (synthesis timeout + RPM burst + aux pool sharing
+ live eval — each cross-referenced to the req that closed it); Automated test
count aggregate from Phase 57-60 VERIFICATIONs (P57: 259 PASS, P58: 264 PASS
cross-phase, P59: 15 PASS after fix, P60: 30 PASS); Audit script 1 (extended
in this phase); FOUND-08 n/a (zero SKILL.md edits — v12.0 is runtime
hardening only).

**§1 — Phase Verification Summary.** Table: 5 rows (57-61). Columns: Phase,
Name, Plans, Reqs, Status, Commit Range (lookup via `git log --oneline
.planning/phases/<N>-*`), SUMMARY pointer. Status pulled from each
VERIFICATION.md frontmatter `status:` field (57=human_needed; 58=passed;
59=passed after re-verification; 60=human_needed; 61=passed per this audit).

**§2 — Requirements Traceability (8/8).** Table: 8 rows. Each row: REQ-ID,
Phase, Plan, Status, Verification Source (file:section:1-line quote). Pull
evidence from each VERIFICATION.md Requirements Coverage row. Status
vocabulary: ✅ Complete / ⚠ Complete (operator smoke deferred). Expected
breakdown: 5 ✅ Complete (THROTTLE-01, THROTTLE-02, POOL-01, POOL-02,
VALIDATE-01) + 3 ⚠ Complete-with-operator-deferral (ENDPOINT-01, EVAL-01,
EVAL-02).

**§3 — Operator-Action Runbook (3 handoffs).** Table with 3 rows:
- Row 1: P57 ENDPOINT-01 SC#2 live GLM smoke. Command: `python scripts/run_screenplay_step3_roundtable.py --smoke` (with `GLM_API_KEY` + 4-key rotation + aux pool configured). Expected: total latency <240s vs v11.0's 490s; synthesis routes to `open.bigmodel.cn/api/anthropic` (no z.ai 30s cap, no 5x retry storm); zero `RateLimitError` (Phase 58 throttle absorbs RPM pacing); build/screenplay-step3-output.json with 6 HOOK-09 fields. Why human: requires real GLM budget + gateway paused.
- Row 2: P60 EVAL-01 live mem0 benchmark. Command: `python scripts/seed_mem0_backend.py --agent-id screenplay --count 500 && python scripts/run_latency_benchmark.py --backend mem0 --record-count 500 --runs 100 --out /tmp/v12-bench-live.json`. Expected: latency-baseline.md §2.2 500-record row filled with empirical p50/p95/p99 + Date + Operator; if p95 ≥ 500ms, `.planning/research/v12-poc-eval/物理分区-triggers.md` authored per Phase 48 §3. Why human: requires `MEM0_API_KEY`.
- Row 3: P60 EVAL-02 real-mode fitness battery. Command: `python scripts/compute_fitness_baseline.py --battery tests/v11-fitness-battery/scenarios --out /tmp/v12-fitness-baseline.json`. Expected: 2 new entries in `~/.hermes/eval/fitness_trend.jsonl` with `battery_version=v1-screenplay-baseline-real` + `mode` field; fitness-battery-baseline.md exists with 7 sections + discrimination verdict (PASS if delta ≥ 0.3 AND persona mean ≥ 0.7). Why human: requires `GLM_AUX_API_KEY_1..4` + ~10-25min wall-clock + ~400K tokens + ~0.20 CNY.

Include **Pre-conditions for all 3** (shared): main repo venv, working
directory, 9 agent YAMLs present, cli-config.yaml `auxiliary.*` provider
blocks, MEMORY.md `feedback-glm-overload-reduce-concurrency.md` (global
concurrency==1) honored, MEMORY.md `hermes-gateway-systemd.md` (recommend
`systemctl --user stop hermes-gateway.service` during run to free GLM quota).

Include **Verdict triage after operator runs** (3-row table: all 3 PASS →
`passed`; 2/3 PASS + 1 documented partial → `passed` with note; any
fundamental FAIL → re-classify).

**§4 — Hardening Risk Register Reconciliation.** Table: 4 rows (one per
hardening gap from ROADMAP §"Hardening gaps targeted"). Each row: Gap ID
(cite ROADMAP source row), Risk name, Severity, Runtime mitigation shipped in
v12.0, Verification evidence (file:section pointer). The 4 gaps:
1. Synthesis endpoint timeout (z.ai 30s cap) → ENDPOINT-01 —
   `agent/auxiliary_client.py:_select_endpoint_by_prompt_length` +
   `_get_cached_client` wire-in + Test 13 (routed vs unrouted latency regression).
2. RPM rate limit burst (hardcoded `asyncio.sleep` Strategy A) → THROTTLE-01 —
   `agent/glm_throttle.py` TokenBucket + `acquire_slot` integration in
   `auxiliary_client.call_llm` + 9 TDD tests + zero executable `asyncio.sleep`
   in driver (AST-walked).
3. Auxiliary traffic shares credential pool (gateway burst exhausts keys) →
   POOL-01 + POOL-02 — `load_named_pool("auxiliary")` + `GLM_AUX_API_KEY_1..4`
   + `select_freshest_tpm` wire-in + `record_usage` post-call + `tpm_warning`
   emission.
4. Fitness battery real-mode + live mem0 benchmark unrun → EVAL-01 + EVAL-02 —
   `_BackendAdapter` singleton + idempotent seeder + `--backend mem0` flag +
   `baseline_mode` kwarg on `agent.fitness_battery` + dual-mode orchestrator +
   30 new tests + `BATTERY_VERSION=v1-screenplay-baseline-real`.

Also include a carried-forward note: "7 load-bearing pitfalls from v10.0
(P1/P2/P4/P5/P8/P10/P14) remain runtime-mitigated per v11.0 §4; v12.0
hardening does not regress any of them."

**§5 — Implementation Path Adherence.** v12.0 has no equivalent of
`05-POC-PLAN.md §6.1` strict sequencing, but ROADMAP §"Phase Numbering"
establishes 57→58→59→60→61 ordering by dependency. Document: Phase 58 SC#3
verifies zero `asyncio.sleep` in driver — only true after Phase 58 lands;
Phase 59 isolation tests pass only after Phase 58 throttle wiring exists;
Phase 60 `baseline_mode` dispatch uses Phase 58/59 infra; Phase 61 audit cites
all four prior VERIFICATIONs. Adherence: 4/4 phases committed in declared
order; no premature Phase 60 work before Phase 59 wired `select_freshest_tpm`.

**§6 — Vertical Slice Smoke Test Result.** Pointer:
`.planning/research/v12-poc-eval/production-smoke-report.md` (authored in
Task 3 of this plan). Summary table: per-phase test counts from
VERIFICATION.md Behavioral Spot-Checks (P57: 259 PASS, P58: 264 PASS, P59: 15
PASS, P60: 30 PASS) + the migration / fitness / latency / bias regression
probes (cite specific row + file). Real-GLM smoke = operator-action per §3
(cite operator-action handoff #1).

**§7 — Verdict + Tech Debt + FOUND-08.** Verdict: `passed` (expected). Verdict
logic (per CONTEXT.md D-3 + v11.0 §7 precedent): 6 criteria — (1) §2 8/8 reqs
verified at automated level, (2) §3 3 operator-action items documented, (3)
§4 4 hardening gaps closed, (4) §5 implementation path adhered, (5) §6 553+
automated tests green (aggregate), (6) no `failed` in any VERIFICATION.md.
v11.0 precedent: operator-action items count as
`passed-with-operator-deferral` (runtime validations, NOT blocking design
gaps). FOUND-08 n/a: zero SKILL.md edits (v12.0 is runtime hardening; per
v11.0 declaration pattern). Tech debt: enumerate the 5 scope deferrals
(15-expert full transform; 物理分区 migration; curator live tick; round table
async; production deployment) — all to v13.0+.

**§8 — Next-Step Action Items.** 4 items: (1) operator runs §3 handoffs + tags
`v12.0`; (2) v13.0 author consumes §3 numbers as productionization input; (3)
v13.0 scope planning uses §7 tech_debt as candidate work items; (4) close-out
pattern precedent extended: v9.0 P43 → v10.0 P51 (design-only) → v11.0 P56
(runtime PoC) → v12.0 P61 (runtime hardening).

**§9 — References.** Lists: REQUIREMENTS.md, ROADMAP.md, PROJECT.md,
`scripts/run_milestone_audit.py`, `scripts/run_screenplay_step3_roundtable.py`,
the 4 VERIFICATION.md files (57-60), the v11.0 audit (precedent), the
v12-poc-eval/production-smoke-report.md (Task 3 deliverable),
audit-matrix.json (machine-readable run artifact).

**§10 — Methodology Notes.** Audit scope + evidence standards + audit script
methodology + verdict logic (mirror v11.0 §10). Note: the v11.0 + v12.0
milestones both use the same script with `--milestone` flag — third-party
reproducibility improved over v11.0 (which was hardcoded).

Canonical frontmatter line: add `verdict: \`passed\`` (lowercase, backticked)
per v11.0 §7 precedent so machine-readable parsers can find it.

Cite `audit-matrix.json` counts (satisfied_reqs / human_needed_reqs /
operator_action_count / recommended_verdict) explicitly in §0 Scorecard AND §2
traceability AND §7 verdict logic — these are the script's authoritative
output per Task 1.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && test -f .planning/milestones/v12.0-MILESTONE-AUDIT.md && LINES=$(wc -l &lt; .planning/milestones/v12.0-MILESTONE-AUDIT.md) && [ "$LINES" -ge 200 ] && echo "OK: v12.0-MILESTONE-AUDIT.md exists ($LINES lines)"</automated>
    <automated>cd /data/workspace/hermes-agent && grep -c "^## §" .planning/milestones/v12.0-MILESTONE-AUDIT.md | grep -qw 10 && echo "OK: 10 sections present" || (echo "FAIL: section count != 10"; grep -c "^## §" .planning/milestones/v12.0-MILESTONE-AUDIT.md)</automated>
    <automated>cd /data/workspace/hermes-agent && grep -E "^verdict:\s*\`passed\`" .planning/milestones/v12.0-MILESTONE-AUDIT.md &amp;&amp; echo "OK: verdict declared in frontmatter"</automated>
    <automated>cd /data/workspace/hermes-agent && grep -c "ENDPOINT-01\|THROTTLE-01\|THROTTLE-02\|POOL-01\|POOL-02\|EVAL-01\|EVAL-02\|VALIDATE-01" .planning/milestones/v12.0-MILESTONE-AUDIT.md | grep -qE "^[8-9]|[1-9][0-9]+" &amp;&amp; echo "OK: all 8 reqs cited" || echo "FAIL: not all 8 reqs cited"</automated>
    <automated>cd /data/workspace/hermes-agent && grep -c "scripts/run_screenplay_step3_roundtable.py --smoke\|seed_mem0_backend.py\|compute_fitness_baseline.py" .planning/milestones/v12.0-MILESTONE-AUDIT.md | grep -qw 0 &amp;&amp; echo "FAIL: §3 operator-action commands missing" || echo "OK: §3 operator-action commands present"</automated>
  </verify>
  <done>
- `.planning/milestones/v12.0-MILESTONE-AUDIT.md` exists, 200+ lines
- 10 sections present (§0 Scorecard through §10 Methodology Notes)
- Frontmatter `verdict: \`passed\`` (lowercase, machine-readable)
- All 8 reqs cited by REQ-ID across §2 traceability + §0 scorecard
- §3 Operator-Action Runbook has 3 rows with command + expected + why-human
- §4 lists all 4 hardening gaps closed with verification evidence pointers
- §7 verdict logic cites audit-matrix.json counts from Task 1
- FOUND-08 n/a declaration present (zero SKILL.md edits)
- v11.0 milestone audit precedent cited (single-repo, single-script pattern)
  </done>
</task>

<task type="auto">
  <name>Task 3: Author production-smoke-report.md + update REQUIREMENTS.md traceability</name>
  <files>.planning/research/v12-poc-eval/production-smoke-report.md, .planning/REQUIREMENTS.md</files>
  <action>
Two deliverables in this task — both pure documentation, no code changes:

### Part A — Author `.planning/research/v12-poc-eval/production-smoke-report.md`

Mirror the v11.0 smoke-test-report.md structure but with v12.0 hardening
context. Create directory `mkdir -p .planning/research/v12-poc-eval` first.
Document body sections (120+ lines):

1. **Header** — milestone + scope statement: "v12.0 production smoke with
   hardening (Phase 57 endpoint routing + Phase 58 throttle + Phase 59 aux pool
   isolation) in place. Target: <240s round table latency + zero
   RateLimitError vs v11.0's 490s + 5x retry storm."

2. **§1 — Automated Test Aggregation.** Table aggregating Phase 57-60
   Behavioral Spot-Checks results from each VERIFICATION.md:
   - P57: 259 PASS in 87.29s (test_auxiliary_endpoint_routing 13 + v11-*/ 87 + 9 cross-cutting 159)
   - P58: 37 PASS (test_glm_throttle 9 + test_round_table_budget 28) + 264 cross-phase PASS
   - P59: 15 PASS (test_auxiliary_client_aux_pool 6 + test_credential_pool_tpm_tracking 9, after re-verification)
   - P60: 30 PASS (test_seed_mem0_backend 12 + test_run_latency_benchmark_backend_flag 7 + test_baseline_mode_dispatch 11)
   - Total: 586+ automated tests green across v12.0 phases.

3. **§2 — Production Smoke Targets (SC#2 result).** Two-row table per
   ROADMAP Phase 61 SC#2:
   - Target 1: <240s round table latency vs v11.0's 490s. Status:
     `human_needed` (requires real GLM budget). Reference: §3 operator-action
     row 1.
   - Target 2: zero `RateLimitError` (vs v11.0's 5x retry storm). Status:
     `human_needed` (same run as target 1). Reference: §3 operator-action row 1.

4. **§3 — Operator-Action Runbook (3 handoffs).** Same 3 handoffs as
   milestone-audit §3, but in smoke-report format: each handoff has 7 fields
   (Status, Command, Pre-conditions, Expected, Why human, Timestamp, Result).
   Reuse the command/expected/why content from Task 2's audit §3 but expand
   each row into the 7-field runbook shape (per v11.0 smoke-test-report.md
   precedent).

5. **§4 — v11.0 vs v12.0 Hardening Delta.** Before/after table showing what
   v12.0 changed at each layer:
   - Endpoint routing: v11.0 hardcoded z.ai coding plan → v12.0
     prompt-length-aware routing (Phase 57)
   - RPM pacing: v11.0 hardcoded `asyncio.sleep(2.5)` Strategy A → v12.0
     TokenBucket per task (Phase 58 THROTTLE-01)
   - TPM budget: v11.0 unbounded → v12.0 per-round-table 100K default +
     budget_warning/exceeded events (Phase 58 THROTTLE-02)
   - Credential pool: v11.0 shared main pool → v12.0 isolated aux pool with
     `GLM_AUX_API_KEY_1..4` + per-key TPM sliding window (Phase 59)
   - Live eval: v11.0 fixture-only → v12.0 live backend infrastructure
     shipped (Phase 60, operator-run handoffs)

6. **§5 — Verdict Triage.** 3-row table mirroring v11.0 smoke-test-report §3
   verdict triage: all 3 handoffs PASS → `passed`; 2/3 PASS + 1 documented
   partial → `passed` with note; any fundamental FAIL → re-classify.

7. **§6 — References.** List: 4 VERIFICATION.md (57-60), v11.0 smoke-test-report
   (precedent), v12.0-MILESTONE-AUDIT.md (Task 2), the 5 operator-action scripts.

Footer: timestamp + verifier line. Single operator-action smoke command in
short form for easy copy-paste: `python scripts/run_screenplay_step3_roundtable.py --smoke`
(cited verbatim in §3 row 1 to satisfy v11.0 §3 token-adjacency precedent).

**Real-GLM attempt policy (per CONTEXT.md D-2 "Claude's Discretion"):** The
executor MAY attempt to run the SC#2 smoke if (and only if) `GLM_API_KEY` is
available in the current environment AND the hermes-gateway is not actively
consuming quota (check `systemctl --user is-active hermes-gateway.service` —
must return `inactive` or `failed`). If attempted and it succeeds, populate §2
with empirical numbers. If attempted and rate-limited (HTTP 429 or
`RateLimitError`), mark `human_needed` per v11.0 convention. If not attempted
(no key / gateway running), mark `human_needed` from the start. **Default
behavior: mark human_needed** — the SC#2 smoke requires real GLM budget that
the planner cannot assume is available. The smoke report documents the
handoff; the operator runs it.

### Part B — Update `.planning/REQUIREMENTS.md` traceability table

Surgical Edit (preserve all other content). Two changes only:

1. **Update 8 Status cells** in the traceability table (lines ~152-159) from
   `Pending` to milestone-status-appropriate values:
   - ENDPOINT-01: `Pending` → `⚠ Complete (SC#2 smoke deferred)` (Phase 57
     shipped all infra; live GLM smoke deferred to operator per §3 runbook)
   - THROTTLE-01: `Pending` → `Complete` (Phase 58 fully verified)
   - THROTTLE-02: `Pending` → `Complete` (Phase 58 fully verified)
   - POOL-01: `Pending` → `Complete` (Phase 59 fully verified after re-verification)
   - POOL-02: `Pending` → `Complete` (Phase 59 fully verified after re-verification)
   - EVAL-01: `Pending` → `⚠ Complete (live mem0 deferred)` (Phase 60 infra
     shipped; live `MEM0_API_KEY` run deferred to operator per §3)
   - EVAL-02: `Pending` → `⚠ Complete (live fitness deferred)` (Phase 60 infra
     shipped; live `GLM_AUX_API_KEY_1..4` run deferred to operator per §3)
   - VALIDATE-01: `Pending` → `Complete (this audit — verdict passed)`

2. **Update footer** (line ~185) — change `*Last updated: 2026-07-07 — v12.0
   Production Hardening requirements defined ... Next: /gsd:plan-phase 57.*` to
   new timestamp + new "Next" pointer:
   `*Last updated: 2026-07-08 — v12.0 milestone audit complete. All 8 reqs verified
   (5 Complete + 3 Complete-with-operator-deferral). Audit verdict: passed. See
   .planning/milestones/v12.0-MILESTONE-AUDIT.md. Next: operator runs §3 handoffs
   + git tag v12.0.*`

CRITICAL preservation: do NOT change any other cell in the traceability table
(req-id, phase, category, person-days columns all unchanged). Do NOT change
the prose sections above the table (Goals / Predecessors / req description
blocks). Use Edit tool for surgical cell-by-cell updates, not Write for the
whole file.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && mkdir -p .planning/research/v12-poc-eval &amp;&amp; test -f .planning/research/v12-poc-eval/production-smoke-report.md &amp;&amp; LINES=$(wc -l &lt; .planning/research/v12-poc-eval/production-smoke-report.md) &amp;&amp; [ "$LINES" -ge 120 ] &amp;&amp; echo "OK: production-smoke-report.md exists ($LINES lines)"</automated>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; grep -c "^## §\|^## [0-9]" .planning/research/v12-poc-eval/production-smoke-report.md | awk '{ if ($1 &gt;= 5) print "OK: section count &gt;= 5 (" $1 ")"; else print "FAIL: too few sections (" $1 ")"; exit ($1 &lt; 5) }'</automated>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; grep -c "run_screenplay_step3_roundtable.py --smoke\|seed_mem0_backend.py\|compute_fitness_baseline.py" .planning/research/v12-poc-eval/production-smoke-report.md | awk '{ if ($1 &gt;= 3) print "OK: all 3 operator-action commands present (" $1 ")"; else print "FAIL: missing operator-action commands (" $1 ")"; exit ($1 &lt; 3) }'</automated>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; grep -c "Pending" .planning/REQUIREMENTS.md | awk '{ if ($1 &gt; 1) print "FAIL: more than expected Pending occurrences (found " $1 ", expected &lt;= 1 in prose)"; else print "OK: no Pending cells left in traceability table"; exit ($1 &gt; 1) }'</automated>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; grep -cE "ENDPOINT-01 \| 57\|THROTTLE-01 \| 58\|THROTTLE-02 \| 58\|POOL-01 \| 59\|POOL-02 \| 59\|EVAL-01 \| 60\|EVAL-02 \| 60\|VALIDATE-01 \| 61" .planning/REQUIREMENTS.md | awk '{ if ($1 == 8) print "OK: all 8 reqs still mapped to correct phases"; else print "FAIL: req-to-phase mapping broken (" $1 "/8)"; exit ($1 != 8) }'</automated>
    <automated>cd /data/workspace/hermes-agent &amp;&amp; grep -cE "Complete \(SC#2 smoke deferred\)|Complete \(live mem0 deferred\)|Complete \(live fitness deferred\)|Complete \(this audit" .planning/REQUIREMENTS.md | awk '{ if ($1 &gt;= 4) print "OK: 4 milestone-status-appropriate deferral markers present (" $1 ")"; else print "FAIL: missing deferral markers (" $1 "/4 expected)"; exit ($1 &lt; 4) }'</automated>
  </verify>
  <done>
- `.planning/research/v12-poc-eval/production-smoke-report.md` exists, 120+ lines
- 5+ sections (Automated Aggregation, Smoke Targets, Operator-Action Runbook, v11.0 vs v12.0 Delta, Verdict Triage, References)
- §3 operator-action runbook has 3 rows with 7-field format (Status/Command/Pre-conditions/Expected/Why human/Timestamp/Result)
- All 3 operator-action commands cited verbatim (run_screenplay_step3_roundtable.py --smoke / seed_mem0_backend.py / compute_fitness_baseline.py)
- `.planning/REQUIREMENTS.md` 8 Status cells flipped from Pending to milestone-appropriate status
- 4 deferral markers (⚠ Complete-with-operator-deferral) present for ENDPOINT-01 + EVAL-01 + EVAL-02 + VALIDATE-01
- Req-to-phase mapping preserved (8 rows still map to 57/58/58/59/59/60/60/61)
- Footer timestamp updated to 2026-07-08 + "Next: operator runs §3 handoffs + git tag v12.0"
- Zero "Pending" cells remain in traceability table (footer's historical prose excluded)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → planning files | `scripts/run_milestone_audit.py` reads `.planning/REQUIREMENTS.md` + 4 VERIFICATION.md files (trusted source) |
| operator → audit script CLI | Operator invokes the script via `python3 scripts/run_milestone_audit.py --milestone v12.0`. CLI args parsed via argparse (safe). |
| Task 3 real-GLM attempt → external API | IF executor attempts SC#2 smoke, GLM_API_KEY crosses boundary to open.bigmodel.cn. Risk: rate-limit / credential leak. Mitigation: default = human_needed; only attempt if key + inactive gateway both verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | audit script CLI arg parsing | accept | argparse handles arg validation; no untrusted input crosses into subprocess calls; `--milestone` enum-constrained |
| T-61-02 | Information Disclosure | audit-matrix.json may embed VERIFICATION.md evidence quotes | accept | evidence quotes are from already-public planning files; no secrets cross the boundary |
| T-61-03 | Denial of Service | Task 3 SC#2 smoke attempt could exhaust GLM quota | mitigate | default = human_needed (no attempt); if attempted, requires `systemctl --user is-active hermes-gateway.service` returns non-active first; honors MEMORY.md `feedback-glm-overload-reduce-concurrency.md` (global_concurrency==1) |
| T-61-04 | Tampering | REQUIREMENTS.md Status cell updates | mitigate | surgical Edit tool use (not Write); verify regex enforces 8 req-to-phase rows preserved + zero "Pending" cells left in traceability table |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | zero package installs in this plan (stdlib-only audit script extension); no slopcheck checkpoint needed |
</threat_model>

<verification>
Phase 61 complete when ALL of:

1. **Task 1 verify passes** — `python3 scripts/run_milestone_audit.py --milestone v12.0` produces `total_reqs=8, operator_action_count=3, recommended_verdict=passed` AND `--milestone v11.0` regression preserved (15 reqs, 5 handoffs).

2. **Task 2 verify passes** — `.planning/milestones/v12.0-MILESTONE-AUDIT.md` exists with 200+ lines, 10 sections, `verdict: \`passed\`` frontmatter, all 8 req IDs cited, §3 with 3 operator-action commands, §4 with 4 hardening-gap rows.

3. **Task 3 verify passes** — `.planning/research/v12-poc-eval/production-smoke-report.md` exists with 120+ lines, 5+ sections, all 3 operator-action commands cited; `.planning/REQUIREMENTS.md` traceability table has zero Pending cells, 8 req-to-phase rows preserved, 4 deferral markers present.

4. **Git tag v12.0** — operator (Kai) tags after reviewing the audit + (optionally) running §3 operator-action smoke tests. This is OUTSIDE the autonomous scope of this plan — VALIDATE-01 deliverable spec explicitly allows documenting handoffs without running them (mirror v11.0 Phase 56 precedent).

5. **Commits** — each task in its own atomic commit per GSD convention:
   - `feat(61-01): extend run_milestone_audit.py with --milestone v11.0|v12.0 dispatch`
   - `docs(61-01): ship v12.0 milestone audit (verdict=passed, 8/8 reqs, 3 operator-action handoffs)`
   - `docs(61-01): ship v12.0 production-smoke-report + flip REQUIREMENTS.md traceability (8/8)`

6. **GSD plan SUMMARY** — `.planning/phases/61-validate/61-01-SUMMARY.md` committed after all 3 task commits.
</verification>

<success_criteria>
**VALIDATE-01 deliverable complete.** All of:

- `.planning/milestones/v12.0-MILESTONE-AUDIT.md` exists with verdict declared (expected: `passed`)
- `.planning/research/v12-poc-eval/production-smoke-report.md` exists with SC#2 documented (measured or `human_needed`)
- `scripts/run_milestone_audit.py` extended to support both v11.0 (regression preserved) and v12.0 (NEW)
- 3 operator-action handoffs aggregated: P57 SC#2 smoke + P60 EVAL-01 + P60 EVAL-02
- `.planning/REQUIREMENTS.md` traceability table updated (8 Status cells flipped, 0 Pending remaining in traceability rows)
- All 8 v12.0 reqs walked in §2 traceability with evidence pointers to Phase 57-60 VERIFICATION.md
- FOUND-08 n/a declared (zero SKILL.md edits — v12.0 is runtime hardening only)
- Ruff PLW1514 clean (every `open()` passes `encoding="utf-8"`)
- Stdlib-only (no new third-party deps)
- v11.0 regression preserved: `--milestone v11.0` produces byte-identical counts to the v11.0 audit (15 reqs, 5 handoffs)

**Operator next steps (outside autonomous scope):**
1. Run §3 operator-action handoffs (any order)
2. Populate empirical numbers in production-smoke-report.md §2 if SC#2 succeeds
3. `git tag v12.0` once satisfied with audit + (optionally) handoff results
</success_criteria>

<output>
Create `.planning/phases/61-validate/61-01-SUMMARY.md` when done (per GSD convention).
</output>

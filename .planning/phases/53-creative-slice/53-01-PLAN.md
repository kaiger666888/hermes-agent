---
phase: 53-creative-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/transform_skill_to_agent.py
  - tests/agent/test_phase52_contract.py
  - tests/agent/test_transform_skill_to_agent.py
  - tests/fixtures/transform-audit-log.json
autonomous: true
requirements: [MIGR-01]

must_haves:
  truths:
    - "All Phase 52 imports the rest of Phase 53 depends on resolve without ImportError"
    - "9 agent YAMLs at ~/.hermes/agents/*.agent.yaml all pass jsonschema.Draft202012Validator against agents-schema.yaml"
    - "screenplay.agent.yaml lineage.transform_notes contains the literal HOOK-09 invariant string"
    - "Every agent YAML's lineage.skill_sha256 matches SHA-256 of the LF-normalized source SKILL.md"
    - "Transform audit log records which SKILL frontmatter field mapped to which agent YAML field for each of the 9 experts"
  artifacts:
    - path: "scripts/transform_skill_to_agent.py"
      provides: "One-shot CLI that reads 9 source SKILL.md files and emits agent YAMLs at ~/.hermes/agents/*.agent.yaml per Phase 49 §2 75-cell rules"
      min_lines: 250
    - path: "tests/agent/test_phase52_contract.py"
      provides: "Wave 0 smoke test proving agent.registry_loader + agent.round_table_state + agent.round_table_executor + agent.memory_arbitration imports all resolve"
      min_lines: 60
    - path: "tests/agent/test_transform_skill_to_agent.py"
      provides: "9 parametrized transform sub-tests + HOOK-09 invariant test + LF-normalized SHA-256 test + filename-stem invariant test"
      min_lines: 150
    - path: "tests/fixtures/transform-audit-log.json"
      provides: "Per-expert mapping record (SKILL frontmatter field → agent YAML field) for all 9 experts"
  key_links:
    - from: "scripts/transform_skill_to_agent.py"
      to: "agent.skill_utils.parse_frontmatter"
      via: "function import + call inside transform_one()"
      pattern: "from agent\\.skill_utils import parse_frontmatter"
    - from: "scripts/transform_skill_to_agent.py"
      to: "agent.registry_loader.load_one_agent_yaml"
      via: "post-write validation call (validates each emitted YAML)"
      pattern: "load_one_agent_yaml\\(.*yaml_path\\)"
    - from: "tests/agent/test_transform_skill_to_agent.py"
      to: "/data/workspace/kais-hermes-skills/skills/movie-experts"
      via: "read-only filesystem access to source SKILL.md files"
      pattern: "kais-hermes-skills/skills/movie-experts"
---

<objective>
Transform 9 sample movie-expert SKILL.md files into agent YAMLs at `~/.hermes/agents/*.agent.yaml`, validating each against the locked Phase 45 `agents-schema.yaml` and preserving the HOOK-09 emotion_curve marker invariant in `screenplay.agent.yaml`.

Purpose: MIGR-01 — provide the 9-agent registry that CREATIVE-01's screenplay Step 3 round table (plan 53-03) consumes. Without these YAMLs the registry loader returns `[]` and the round table cannot open. Wave 0 also proves the Phase 52 contract surface is consumable.

Output: `scripts/transform_skill_to_agent.py` (one-shot CLI), 9 emitted agent YAMLs, transform audit log, and full unit test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-creative-slice/53-CONTEXT.md
@.planning/phases/53-creative-slice/53-RESEARCH.md
@.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md
@.planning/research/v10-orchestrator-design/agents-schema.yaml

<interfaces>
<!-- Phase 52 contract surface — these symbols MUST import cleanly (Wave 0 verifies). -->

From agent/registry_loader.py (Phase 52 actual):
```python
class RegistryValidationError(Exception):
    def __init__(self, message: str, *, json_path: str | None = None, invalid_field: str | None = None) -> None: ...

def load_one_agent_yaml(yaml_path: Path) -> dict[str, Any]: ...
def load_agent_registry(*, force_reload: bool = False) -> list[dict[str, Any]]: ...
```

From agent/round_table_state.py (Phase 52 actual):
```python
def validate_round_id(round_id: str) -> str | None: ...
def validate_project_slug(project_slug: str) -> str | None: ...
def open_round_table(*, round_id: str, project_slug: str, question: str, panelist_agent_ids: list[str], caller: str) -> dict[str, Any]: ...
def append_turn(state_path: Path, turn: dict[str, Any]) -> dict[str, Any]: ...
def submit_round_table_result(*, round_id: str, project_slug: str, conclusion: str, cited_memories: list[str], closed_by: str) -> dict[str, Any]: ...
def read_and_recover_state(state_path: Path) -> dict[str, Any]: ...
```

From agent/round_table_executor.py (Phase 52 actual):
```python
async def acquire_round_or_reject(round_id: str) -> Optional[asyncio.Lock]: ...
async def release_round_lock(round_id: str) -> None: ...
def _serial_violation_response(round_id: str) -> str: ...
```

From agent/memory_arbitration.py (Phase 52 actual stub):
```python
def set_scoped_agent_id(agent_id: str | None) -> None: ...
def get_scoped_agent_id() -> str | None: ...
async def memory_retrieve_scoped(query: str, agent_id: str, *, top_k: int = 5) -> dict[str, Any]: ...  # returns {"status": "phase53_not_implemented", "hits": []}
async def memory_submit_record(agent_id: str, content: str, *, scope: str = "per_agent", confidence: float = 0.5) -> dict[str, Any]: ...
```

From agent/skill_utils.py (existing, v5.0+):
```python
def parse_frontmatter(content: str) -> tuple[dict, str]: ...  # returns (frontmatter_dict, body_markdown)
```

agents-schema.yaml required fields (line 41):
```
required: [name, description, version, persona, tools, memory_scope, lineage]
```

Lineage $def required fields (line 296):
```
required: [derived_from_skill_id, derived_from_repo, transform_date, skill_sha256]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 contract smoke test + screenplay Step 3 schema fixture</name>
  <files>tests/agent/test_phase52_contract.py, tests/fixtures/screenplay-step3-schema.json</files>
  <read_first>
    - agent/registry_loader.py (Phase 52 actual — public exports list above)
    - agent/round_table_state.py (lines 99, 119, 174, 256, 354, 377, 422, 497 — function signatures)
    - agent/round_table_executor.py (lines 82, 107, 154 — _serial_violation_response, acquire_round_or_reject, release_round_lock)
    - agent/memory_arbitration.py (lines 77, 92, 116, 148 — set/get_scoped_agent_id + 2 stubs)
    - tests/agent/test_round_table_executor.py:1-40 (pytest-asyncio strict mode pattern)
    - .planning/research/v10-orchestrator-design/05-POC-PLAN.md §3.2 (HOOK-09 emotion_curve contract)
  </read_first>
  <behavior>
    - Test 1 (test_phase52_imports_resolve): import all 11 Phase 52 symbols listed above; assert callable / class. No ImportError, no AttributeError.
    - Test 2 (test_phase52_stub_returns_phase53_marker, async): call `await memory_retrieve_scoped(query="x", agent_id="y", top_k=1)`, assert response == `{"status": "phase53_not_implemented", "hits": []}` (locked contract per `52-CONTEXT.md`).
    - Test 3 (test_phase52_submit_stub_returns_phase53_marker, async): same for `memory_submit_record`, assert response == `{"status": "phase53_not_implemented", "record_id": None}`.
    - Test 4 (test_phase52_registry_loads_empty_dir, async): create tempdir, `monkeypatch.setenv("HERMES_HOME", str(tmp_path))`, call `load_agent_registry()`, assert `== []` (empty agents dir is not an error).
    - Test 5 (test_screenplay_step3_schema_fixture_loads): load `tests/fixtures/screenplay-step3-schema.json` via `json.loads`, assert required top-level keys `["$schema", "type", "properties"]` present; assert `properties` contains `emotion_curve`, `hooks`, `payoffs`, `cliffhangers` (HOOK-09 contract).
  </behavior>
  <action>
    Create `tests/fixtures/screenplay-step3-schema.json` as a JSON Schema Draft 2020-12 document. It MUST require these top-level fields in its `properties` block (mirroring HOOK-09 emotion_curve marker contract per `05-POC-PLAN.md §3.2`):
      - `logline` (string, minLength 10)
      - `scene_breakdown` (array of objects, each with `scene_id`, `beats`, `emotion_curve` array)
      - `hooks` (array of objects, each with `timestamp_seconds`, `type` enum `[cold_open, curiosity, shock, cliffhanger, paywall]`, `payload`)
      - `payoffs` (array of objects, each with `setup_scene_id`, `payoff_scene_id`, `payload`)
      - `cliffhangers` (array of objects, each with `scene_id`, `tension_level` number 0.0-1.0, `cut_point_seconds`)
      - `emotion_curve` (array of objects, each with `timestamp_seconds`, `arousal` number 0.0-1.0, `valence` number -1.0..1.0)
    Set `additionalProperties: false`, `required: [logline, scene_breakdown, hooks, payoffs, cliffhangers, emotion_curve]`, `$schema: "https://json-schema.org/draft/2020-12/schema"`. The `scene_breakdown[].emotion_curve` and top-level `emotion_curve` arrays are the HOOK-09 invariant — downstream hook_retention + script_auditor consume them.

    Create `tests/agent/test_phase52_contract.py` with `from __future__ import annotations` at the top. Import all 11 Phase 52 symbols. Use `monkeypatch.setenv("HERMES_HOME", str(tmp_path))` (NOT `Path.home()` — CLAUDE.md anti-pattern + Ruff PLW1514 enforces via conftest fixture) in async tests. Mark every `async def test_*` with `@pytest.mark.asyncio` (pytest-asyncio strict mode — RESEARCH Pitfall 3).

    This task is the gate for the rest of Phase 53 — if these imports fail, Phase 52 is not actually shipped and the executor must stop and investigate rather than continue.
  </action>
  <verify>
    <automated>python -m pytest tests/agent/test_phase52_contract.py -x --timeout=30 -v</automated>
  </verify>
  <done>
    - `test_phase52_contract.py` has 5 tests, all green.
    - `screenplay-step3-schema.json` parses + declares all 6 HOOK-09 fields.
    - Every async test carries `@pytest.mark.asyncio`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: transform_skill_to_agent.py + 9 YAMLs + HOOK-09 invariant</name>
  <files>scripts/transform_skill_to_agent.py, tests/agent/test_transform_skill_to_agent.py, tests/fixtures/transform-audit-log.json</files>
  <read_first>
    - /data/workspace/kais-hermes-skills/skills/movie-experts/screenplay/SKILL.md (frontmatter + HOOK-09 location in body)
    - /data/workspace/kais-hermes-skills/skills/movie-experts/{cinematographer,hook_retention,theory_critic,editor,character_designer,continuity_auditor,audio_pipeline,style_genome}/SKILL.md (8 source SKILLs — read frontmatter of each once to confirm `metadata.hermes.{tags, related_skills, expert_id, metrics}` shape)
    - .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md §2.0-§2.19 (transform rules per expert; screenplay §2.4 HOOK-09 invariant at lines 324-355)
    - .planning/research/v10-orchestrator-design/agents-schema.yaml:40-220 (18-field schema + Lineage $def at line 294-323)
    - agent/skill_utils.py (parse_frontmatter signature)
    - agent/registry_loader.py:209-269 (load_one_agent_yaml — filename-stem invariant, RegistryValidationError)
    - scripts/build_skills_index.py (existing one-shot script pattern reference)
  </read_first>
  <behavior>
    - Test 1 (test_transform_tools_per_phase49_rules, parametrized over 9 experts): for each of `screenplay, cinematographer, hook_retention, theory_critic, editor, character_designer, continuity_auditor, audio_pipeline, style_genome`, transform the expert and assert `result["tools"]` matches Phase 49 §2 per-expert default. Screenplay MUST include `write_file` and `patch` (HOOK-09 contract load-bearing).
    - Test 2 (test_screenplay_transform_preserves_HOOK_09): transform screenplay, assert `"HOOK-09 emotion_curve marker arrays remain contract-load-bearing" in result["lineage"]["transform_notes"]` — verbatim substring per CONTEXT.md HOOK-09 invariant.
    - Test 3 (test_skill_sha256_lf_normalized): for screenplay, compute SHA-256 of LF-normalized source SKILL.md bytes, assert equal to `result["lineage"]["skill_sha256"]` (64 hex chars, pattern `^[a-f0-9]{64}$`).
    - Test 4 (test_filename_stem_equals_name_field): for each of 9 experts, write YAML to tempdir as `{name}.agent.yaml`, call `load_one_agent_yaml(tempdir / f"{name}.agent.yaml")`, assert no `RegistryValidationError`.
    - Test 5 (test_all_9_yamls_pass_schema): after writing all 9 to tempdir, call `load_agent_registry()` with `HERMES_HOME` redirected to tempdir parent, assert returned list has length 9.
    - Test 6 (test_legacy_name_mapping): `character_designer.agent.yaml` `related_agents` includes `screenplay` (mapped from `performer` legacy name per Pattern 1b LEGACY_NAME_MAP); `audio_pipeline.agent.yaml` `related_agents` does NOT contain deprecated `composer` or `performer` names (FOUND-08 preservation).
    - Test 7 (test_audit_log_records_field_mappings): load `tests/fixtures/transform-audit-log.json`, assert it has 9 entries keyed by expert name, each entry has `frontmatter_mappings: dict` non-empty.
  </behavior>
  <action>
    Create `scripts/transform_skill_to_agent.py` as a one-shot CLI script with `from __future__ import annotations`. Public function `transform_one(expert_name: str, skill_md_path: Path) -> dict[str, Any]` returns the agent YAML dict (NOT yet written to disk). Internal helpers:
      - `_compute_skill_sha256(content: str) -> str`: LF-normalize (`content.replace("\r\n", "\n")`) then `hashlib.sha256(content.encode("utf-8")).hexdigest()`. Encoding explicit per CLAUDE.md PLW1514.
      - `_filter_related_agents(related_skills: list[str]) -> list[str]`: apply `LEGACY_NAME_MAP = {"scene_builder": "cinematographer", "continuity": "continuity_auditor", "performer": "character_designer", "composer": "audio_pipeline"}` then filter to the 9-agent subset `PANEL_9 = {"screenplay", "cinematographer", "hook_retention", "theory_critic", "editor", "character_designer", "continuity_auditor", "audio_pipeline", "style_genome"}`.
      - `_build_persona_*(body: str) -> str` per expert: first-person persona fragment. For screenplay: persona MUST cite Snyder 15-beat + McKee value-shift + Tan interest formula + cn-shortdrama-structure + emotion-curve-academic, AND must surface HOOK-09 emotion_curve marker contract (other 8 experts follow Phase 49 §2 per-expert rules — cinematographer §2.7 absorbed scene_builder framing, hook_retention §2.2 5 hook types + 5 爆款公式, theory_critic §2.16 soft-gate-only framing, editor §2.13 cut_density metric + cut-follows-intent, character_designer §2.6 L1-L4 asset specs, continuity_auditor §2.11 4-dim hard-gate, audio_pipeline §2.12 6 sub-step atomicity, style_genome §2.8 5D style vector + Cross-Module Alignment).
      - `_transform_notes(expert_name: str) -> str`: returns per-expert notes verbatim from Phase 49 §2.x. For screenplay: MUST contain literal string `"HOOK-09 emotion_curve marker arrays remain contract-load-bearing"` (this is the SC#1 acceptance substring).
      - `_tools_for(expert_name: str) -> list[str]`: per Phase 49 §2.x default tools. screenplay = `[hermes_llm, read_file, search_files, write_file, patch]`; cinematographer = `[hermes_llm, read_file, search_files]` (analysis-only); hook_retention = `[hermes_llm, read_file, search_files]`; theory_critic = `[hermes_llm, read_file, search_files]`; editor = `[hermes_llm, read_file, search_files]`; character_designer = `[hermes_llm, read_file, write_file]` (L4 turnaround sheet write); continuity_auditor = `[hermes_llm, read_file, search_files]`; audio_pipeline = `[hermes_llm, dreamina_cli, read_file, write_file]`; style_genome = `[hermes_llm, read_file, search_files]`.

    Each YAML MUST populate all 7 required fields (`name`, `description`, `version`, `persona`, `tools`, `memory_scope: per_agent`, `lineage`) plus optional `refs`, `tags`, `expert_id`, `metrics`, `prerequisites`, `related_agents`, `evolution_log: []`, `fitness_score: null`, `platforms`, `round_table_eligible: true`, `default_invocation: mcp_tool`. The `name` field MUST equal the filename stem — registry_loader enforces this invariant (T-52-03 spoofing mitigation).

    CLI `__main__` block:
      1. Define `EXPERTS_9 = ["screenplay", "cinematographer", "hook_retention", "theory_critic", "editor", "character_designer", "continuity_auditor", "audio_pipeline", "style_genome"]`.
      2. For each, locate source at `/data/workspace/kais-hermes-skills/skills/movie-experts/{name}/SKILL.md`. If missing, print warning and continue (test uses `pytest.skip` if source unavailable).
      3. Call `transform_one(name, skill_path)` → emit YAML via `yaml.safe_dump(data, sort_keys=False, allow_unicode=True)` to `get_hermes_home() / "agents" / f"{name}.agent.yaml"`. Encoding utf-8 on the open() call.
      4. Validate each emitted YAML by calling `load_one_agent_yaml(target_path)` — if it raises `RegistryValidationError`, log + exit non-zero (do NOT silently emit invalid YAMLs).
      5. Build audit log entries: for each expert, record `{"expert": name, "source_sha256": ..., "frontmatter_mappings": {"description": "from frontmatter.description", "tools": "from Phase 49 §2.x default", "tags": "from metadata.hermes.tags verbatim", ...}, "transform_notes": ...}`. Write to `tests/fixtures/transform-audit-log.json` (this is a test fixture, NOT production state — it documents the field mapping audit trail per SC#1).

    Also create the test file per the 7 behaviors above. Use `pytest.skip` guards if source SKILL.md files unavailable (per RESEARCH §"Validation Architecture"). Use `monkeypatch.setenv("HERMES_HOME", str(tmp_path))` for tests that exercise `load_agent_registry`.

    CRITICAL: do NOT modify any source SKILL.md files (Phase 48 + Phase 49 lineage invariants L1-L6 — read-only per CONTEXT.md deferred list). The script is operator-invoked, not background-triggered (RESEARCH anti-pattern "Auto-re-transform on SHA-256 drift").
  </action>
  <verify>
    <automated>python -m pytest tests/agent/test_transform_skill_to_agent.py -x --timeout=30 -v</automated>
  </verify>
  <done>
    - 9 parametrized transform tests green (one per expert).
    - HOOK-09 substring test green (literal `HOOK-09 emotion_curve marker arrays remain contract-load-bearing` present in screenplay lineage.transform_notes).
    - SHA-256 LF-normalization test green.
    - `tests/fixtures/transform-audit-log.json` exists with 9 entries.
    - After running `python scripts/transform_skill_to_agent.py`, `~/.hermes/agents/*.agent.yaml` contains exactly 9 YAMLs that all pass `load_one_agent_yaml`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → transform script | Transform script reads SKILL.md from kais-hermes-skills repo (operator-owned path). Treated as trusted-but-validated: parse with `parse_frontmatter`, never `eval`. |
| transform script → ~/.hermes/agents/ | Emitted YAMLs become production agent registry entries. Must validate against schema BEFORE write, not just after. |
| YAML files → registry_loader | Already-mitigated by Phase 52 T-52-01 (yaml.safe_load) + T-52-03 (filename-stem invariant) + T-52-04 (additionalProperties:false). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | source SKILL.md content (could be maliciously modified to inject prompt-escape into persona) | accept | SKILL.md is operator-owned in a git-tracked repo; persona content is for LLM system prompt (acceptable risk per Phase 49 §2.1). Document in audit log if content deviates from upstream. |
| T-53-02 | Spoofing | filename-stem mismatch (emitted YAML filename != name field) | mitigate | Phase 52 `load_one_agent_yaml` already enforces this invariant (T-52-03); transform script writes `{name}.agent.yaml` so by construction they match. Double-check via post-write `load_one_agent_yaml` validation. |
| T-53-03 | Tampering | undetected fields in emitted YAML | mitigate | `agents-schema.yaml additionalProperties: false` rejects undeclared fields; enforced by `load_one_agent_yaml`. |
| T-53-04 | Information Disclosure | SHA-256 of SKILL.md reveals content hash | accept | Public content; not sensitive. |
| T-53-SC | Tampering | (none — no npm/pip/cargo installs in this plan) | N/A | No supply-chain risk; all dependencies already pinned and shipped. |
</threat_model>

<verification>
- `python -m pytest tests/agent/test_phase52_contract.py tests/agent/test_transform_skill_to_agent.py -x --timeout=30 -v` green (12 tests total: 5 contract + 7 transform).
- `python scripts/transform_skill_to_agent.py && python -c "from agent.registry_loader import load_agent_registry; agents = load_agent_registry(force_reload=True); assert len(agents) == 9, f'expected 9 agents, got {len(agents)}'; print('OK: 9 agents loaded')"` succeeds.
- `grep "HOOK-09 emotion_curve marker arrays remain contract-load-bearing" ~/.hermes/agents/screenplay.agent.yaml` returns 1+ matches (HOOK-09 invariant — the PoC's first "did the transform work?" smoke test per `05-POC-PLAN.md §3.2`).
- `python -c "import yaml; d = yaml.safe_load(open('$HOME/.hermes/agents/screenplay.agent.yaml', encoding='utf-8')); print(d['lineage']['skill_sha256'])"` prints 64-char hex string.
</verification>

<success_criteria>
1. **SC#1 (MIGR-01 partial):** 9 agent YAMLs at `~/.hermes/agents/*.agent.yaml` validate against `agents-schema.yaml` (18 camelCase fields, lineage populated with `derived_from_skill_id` + `skill_sha256`).
2. **HOOK-09 invariant:** `screenplay.agent.yaml` `lineage.transform_notes` contains literal substring `HOOK-09 emotion_curve marker arrays remain contract-load-bearing`.
3. **Transform audit trail:** `tests/fixtures/transform-audit-log.json` documents frontmatter→YAML field mappings per Phase 49 §2 75-cell rules for all 9 experts.
4. **Wave 0 contract gate:** Phase 52 imports all resolve; stub return contracts intact (locked status string `phase53_not_implemented` unchanged).
</success_criteria>

<output>
Create `.planning/phases/53-creative-slice/53-01-SUMMARY.md` when done.
</output>

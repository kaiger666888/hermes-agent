---
phase: 32-curator-upgrade-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agent/test_curator_regression.py
  - tests/agent/test_audit_log.py
  - tests/agent/test_curator_feedback_scan.py
  - tests/agent/evolution/test_evol02_generator.py
  - tests/fixtures/curator/audit_chain_valid.jsonl
  - tests/fixtures/curator/audit_chain_tampered.jsonl
  - tests/fixtures/curator/feedback_scenarios.json
  - agent/curator_audit.py
  - agent/evolution/evol02_generator.py
  - agent/evolution/__init__.py
  - agent/evolution/queue.py
  - agent/curator.py
  - cli-config.yaml.example
autonomous: true
requirements: [CURATE-01, CURATE-02, CURATE-03, EVOL-02]
tags: [curator, audit, evolution, evol02, regression, feedback-scan]

must_haves:
  truths:
    - "Pre-v6 curator deterministic inactivity transitions + consolidation gate run UNCHANGED after P32 extension (SC-6)"
    - "Audit log appends sha256-chained entries; verify_chain detects any tampering"
    - "EVOL-02 generator emits bilingual EN+CN additive-only unified diffs that apply cleanly via P31 apply_patch_transaction"
    - "Curator feedback-scan phase proposes bundled-skill patches when ≥3 negative feedback across ≥2 distinct UTC days"
    - "Below-threshold skills are NOT proposed; zero-feedback curator runs are no-ops (pre-v6 behavior preserved)"
    - "Every propose action appends an audit entry; runtime isolation (agent/evolution/ not imported by runtime) preserved"
  artifacts:
    - path: "tests/agent/test_curator_regression.py"
      provides: "SC-6 pre-v6 curator behavior regression coverage"
      min_lines: 80
    - path: "agent/curator_audit.py"
      provides: "append_audit, verify_chain, read_audit, AuditChainError — sha256-chained JSONL at ~/.hermes/skills/.audit/log.jsonl"
      exports: ["append_audit", "verify_chain", "read_audit", "AuditChainError", "GENESIS_PREV_SHA256"]
    - path: "agent/evolution/evol02_generator.py"
      provides: "generate_patch_from_knowledge_point — multi-instruction bilingual unified-diff generator extending P31 placeholder"
      exports: ["generate_patch_from_knowledge_point", "emit_evol02_instructions", "_compose_bilingual_block"]
    - path: "tests/agent/evolution/test_evol02_generator.py"
      provides: "EVOL-02 unit coverage (bilingual, multi-instruction, anchor validation, frontmatter preserved, applies clean)"
      min_lines: 150
    - path: "tests/agent/test_curator_feedback_scan.py"
      provides: "threshold detection + EVOL invocation + propose-audit wiring coverage"
      min_lines: 150
  key_links:
    - from: "agent/curator.py:_llm_pass (extension at ~line 1671 after save_state)"
      to: "agent/curator_audit.append_audit (action=propose)"
      via: "lazy import inside _feedback_scan_phase try/except wrapper"
      pattern: "from agent\\.curator_audit import append_audit"
    - from: "agent/curator.py:_feedback_scan_phase"
      to: "agent.evolution.aggregate_feedback + agent.evolution.append_patch"
      via: "lazy imports inside the feedback-scan phase only (preserves runtime isolation — module-level imports forbidden)"
      pattern: "from agent\\.evolution import aggregate_feedback"
    - from: "agent/evolution/evol02_generator.py:generate_patch_from_knowledge_point"
      to: "agent/evolution/diff_generator.generate_additive_diff"
      via: "reuse P31 placeholder's hardened single-block generator per instruction"
      pattern: "generate_additive_diff\\("
---

<objective>
Build the engine layer of the Phase 32 Curator Upgrade: SC-6 regression test (written FIRST), tamper-evident audit log module at `agent/curator_audit.py` (NOT under `agent/evolution/`), EVOL-02 multi-instruction bilingual diff generator, and the additive feedback-scan phase wired into `run_curator_review`.

Purpose: Ship the four engine-layer capabilities (CURATE-01 partial, CURATE-02, CURATE-03, EVOL-02) without touching CLI or auto-apply (Plan 02 territory). Preserve SC-6 (pre-v6 curator behavior byte-for-byte unchanged), FOUND-08 (no bundled SKILL.md changes outside approved evolution patches), runtime isolation (`agent/evolution/` NOT imported at module level by runtime), and the P31 structural invariant `TestNonBypassableHumanInLoop`.

Output: New `agent/curator_audit.py` + new `agent/evolution/evol02_generator.py` + extended `agent/curator.py` (additive `_feedback_scan_phase` appended inside `_llm_pass`) + 4 new test files + 3 fixture files. PatchRecord extended additively with optional `auto_apply_eligible` + `confidence_score` fields (defaults preserve P31 behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-curator-upgrade-audit/32-CONTEXT.md
@.planning/phases/32-curator-upgrade-audit/32-RESEARCH.md
@.planning/phases/32-curator-upgrade-audit/32-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs — extracted from in-repo source. -->

From agent/feedback_schema.py (FeedbackRecord — NO session_id field, Open Question #1 RESOLVED → use distinct UTC calendar dates as session proxy):
```python
class FeedbackRecord(BaseModel):
    skill_id: str           # validated against _KNOWN_EXPERT_IDS, no ':','/','\\'
    expert_id: str
    source: Literal["cli", "kais_aigc", "manual"]
    verdict: Literal["good", "needs_work", "bad"]
    correction: str = ""
    revised_output: str | None = None
    output_snapshot: OutputSnapshot
    ts: datetime             # timezone-aware ISO 8601
```

From agent/feedback_store.py (FeedbackStore public API — NO list_skill_ids method, Pitfall #4):
```python
class FeedbackStore:
    def __init__(self, *, root: Path | None = None, ...): ...
    def record_feedback(self, record: FeedbackRecord) -> str: ...
    def query(self, *, skill_id: str | None = None, source: str | None = None,
              verdict: str | None = None, ...) -> list[FeedbackRecord]: ...
    def summary(self) -> dict[str, Any]: ...   # returns buckets dict
    def get_record(self, record_id: str) -> FeedbackRecord | None: ...
    # Internal (Pitfall #4 — read directly for hot-skill scan):
    #   self._index["buckets"]  — dict keyed by f"{skill_id}:{source}:{verdict}"
    #                             values: {"count": int, "weighted_count": float, ...}
```

From agent/evolution/queue.py (PatchRecord — extend additively for CURATE-05 marker):
```python
class PatchRecord(BaseModel):
    patch_id: str
    skill_id: str
    insight_id: str
    unified_diff: str
    feedback_chain: list[str]
    llm_rationale: str
    eval_gate_score: dict[str, Any] = {}
    status: str = "pending"            # PATCH_STATUSES = {"pending","applied","rejected","failed_gate"}
    ts_queued: str
    commit_sha: str | None = None
    ts_applied: str | None = None
    reason: str | None = None
    ts_rejected: str | None = None
    # NEW (P32 additive, Plan 01 Task 5):
    auto_apply_eligible: bool = False   # default False preserves P31 behavior
    confidence_score: dict[str, Any] | None = None   # {"mean_delta": float, "evidence_count": int}

def append_patch(record: PatchRecord, evolution_dir: Path) -> None: ...  # status must == "pending"
def read_queue(evolution_dir: Path, *, status: str | None = None) -> list[PatchRecord]: ...
def move_patch(patch_id: str, from_status: str, to_status: str, evolution_dir: Path, **metadata) -> None: ...
```

From agent/evolution/insights.py (P31 — reuse, do not fork):
```python
class InsightRecord(BaseModel):
    insight_id: str
    skill_id: str
    evidence_chain: list[str]      # FeedbackRecord IDs
    proposed_addition: str         # markdown
    insert_after_marker: str       # anchor for additive insert
    llm_rationale: str
    # ... (full schema in source)

def make_aggregation_client() -> tuple[Any, str]: ...   # (client, model_name); raises on missing OPENROUTER_API_KEY
def aggregate_feedback(*, skill_id: str, store: FeedbackStore, client: Any, model: str) -> list[InsightRecord]: ...
```

From agent/evolution/diff_generator.py (P31 placeholder — extend via evol02_generator.py sibling):
```python
def generate_additive_diff(
    *,
    current_content: str,
    proposed_addition: str,
    insert_after_marker: str,        # must be unique in current_content
    skill_md_path: str,
) -> str: ...                        # returns unified diff string
                                     # Raises ValueError on: missing/duplicate anchor, frontmatter hit, idempotent no-op
```

From agent/curator.py:1428-1689 — `run_curator_review` + inner `_llm_pass` closure. Extension point is AT THE END of `_llm_pass`, after the final `save_state(state2)` call at line ~1671, BEFORE the `if on_summary:` block at line ~1673. The feedback-scan phase MUST be a separate inner function (e.g. `_feedback_scan_phase()`) invoked there, wrapped in try/except that logs WARNING and never aborts the run.

From tools/skill_usage.py (P31 verified — bundled-vs-agent-created gate):
```python
def is_agent_created(skill_name: str) -> bool: ...   # True ONLY for agent-created (not bundled, not hub)
def is_bundled(skill_name: str) -> bool: ...
def is_hub_installed(skill_name: str) -> bool: ...
```

From tests/hermes_cli/test_evolution_cli.py:846 — `TestNonBypassableHumanInLoop` is the P31 STRUCTURAL INVARIANT that MUST still pass after this plan. It has two tests:
1. `test_only_cmd_approve_calls_apply_patch_transaction` — AST-walks `hermes_cli/feedback.py`; every `apply_patch_transaction()` Call node MUST be inside a FunctionDef named `_cmd_approve`.
2. `test_apply_patch_transaction_not_called_in_agent_or_runtime` — AST-walks `agent/`, `gateway/`, `run_agent.py`, `cli.py`; ZERO `apply_patch_transaction` calls (excluding `agent/evolution/` itself).

Plan 01 MUST NOT call `apply_patch_transaction` from any runtime file. Auto-apply is Plan 02 territory and routes through `_cmd_approve`.

### RESEARCH Open Questions — RESOLVED in this plan
1. **FeedbackRecord.session_id**: ABSENT from schema (verified). Use **distinct UTC calendar days** derived from `record.ts` as the session-diversity proxy. Document the approximation in `_scan_for_hot_skills` docstring.
2. **EVOL-02 LLM call**: SEPARATE pass (`emit_evol02_instructions` in `evol02_generator.py`) — does NOT modify P31's `aggregate_feedback`. Avoids P31 regression risk.
3. **propose action logging**: YES — log every propose. Operator filters via `--action apply` (Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write SC-6 regression test FIRST (pre-v6 curator behavior preservation)</name>
  <files>tests/agent/test_curator_regression.py</files>
  <read_first>
    - agent/curator.py:1428-1689 (run_curator_review + _llm_pass — pre-v6 behavior under test)
    - agent/curator.py:276-510 (apply_automatic_transitions — deterministic transitions)
    - tests/conftest.py (HERMES_HOME redirection fixture, _live_system_guard autouse)
    - tests/agent/test_curator.py and any sibling tests/agent/test_curator_*.py files (existing test patterns to preserve)
    - CLAUDE.md §"Anti-Patterns to Avoid" (Path.home() / ".hermes" forbidden; use get_hermes_home)
  </read_first>
  <behavior>
    - test_inactivity_transitions_unchanged: with one agent-created skill idle beyond archive_after_days, run_curator_review(dry_run=False, synchronous=True, consolidate=False) returns auto_transitions counts identical to pre-v6 expectations (marked_stale/archived/reactivated values match a hardcoded baseline captured from current behavior).
    - test_consolidate_false_skips_llm_and_scan: consolidate=False → _run_llm_review NOT called AND _feedback_scan_phase NOT triggered. Verify by monkeypatching both to raise if invoked.
    - test_dry_run_no_state_mutation: dry_run=True → state["last_run_at"] NOT bumped, state["run_count"] NOT incremented, no skill_manage mutations.
    - test_no_feedback_no_scan: with empty FeedbackStore, the feedback-scan phase is a no-op; run summary matches pre-v6 format.
    - test_scan_phase_is_additive_append: after running curator with feedback present, pre-v6 counts (marked_stale/archived) are IDENTICAL to a no-feedback run — the scan phase adds propose entries but does not alter auto_transitions.
  </behavior>
  <action>
    Create `tests/agent/test_curator_regression.py` with `from __future__ import annotations`, pytest imports, `from agent import curator`, and a `class TestPreV6Regression:` containing the 5 test methods listed in <behavior>. Use `tmp_path` + `monkeypatch.setenv("HERMES_HOME", str(tmp_path))` (mirroring conftest's HERMES_HOME redirection pattern) to isolate state. Create agent-created skill fixtures under `tmp_path / "skills" / "agent_created" / "<name>" / "SKILL.md"` with minimal valid frontmatter. Monkeypatch `curator._run_llm_review` to a sentinel that records calls (default returns a safe "no change" llm_meta dict). For test_consolidate_false_skips_llm_and_scan, also monkeypatch the not-yet-existing `curator._feedback_scan_phase` to raise `AssertionError("scan must not run when consolidate=False")` — this locks in the contract before the function exists (TDD RED). For test_inactivity_transitions_unchanged, capture the expected counts by reading `agent/curator.py:apply_automatic_transitions` logic and hardcoding the expected dict (the test FAILS if any count drifts). Add `encoding="utf-8"` on every `open()`. Use `from agent.hermes_constants import get_hermes_home` indirectly via the conftest fixture — NEVER `Path.home()`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_curator_regression.py -x --timeout=30</automated>
  </verify>
  <done>
    Five test methods exist and run. Four of five PASS against the pre-v6 curator (locking in the baseline); test_consolidate_false_skips_llm_and_scan is RED until Task 5 adds _feedback_scan_phase (TDD contract). The expected counts dict is documented in a module-level comment so drift is reviewable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit log module (agent/curator_audit.py) — append + verify + query + fixtures</name>
  <files>
    agent/curator_audit.py,
    tests/agent/test_audit_log.py,
    tests/fixtures/curator/audit_chain_valid.jsonl,
    tests/fixtures/curator/audit_chain_tampered.jsonl
  </files>
  <read_first>
    - .planning/phases/32-curator-upgrade-audit/32-CONTEXT.md §"Audit Trail Format (CURATE-04)" — LOCKED formula for entry_sha256
    - .planning/phases/32-curator-upgrade-audit/32-RESEARCH.md §"Pattern 2: sha256 Chain Append" + §"Pattern 3: Audit Chain Verification" + Pitfalls #2 and #8 (serialization consistency)
    - agent/evolution/queue.py:127-184 (_append_jsonl + _read_jsonl patterns to mirror)
    - agent/hermes_constants.py (get_hermes_home — NEVER Path.home())
    - CLAUDE.md §"Skill File Conventions" + §"Anti-Patterns to Avoid"
  </read_first>
  <behavior>
    - GENESIS_PREV_SHA256 is the 64-hex-char sha256 of empty bytes: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 (documented in module docstring).
    - append_audit(action="propose", patch_id="p1", skill_id="screenplay", operator="system") returns a uuid4 entry_id; the resulting log file has exactly one JSONL line whose entry_sha256 equals sha256(GENESIS_PREV_SHA256 + json.dumps({entry fields minus entry_sha256}, sort_keys=True, separators=(",",":"), ensure_ascii=False)).
    - A second append_audit chains correctly: its prev_sha256 equals the first entry's entry_sha256.
    - verify_chain() on empty/missing log returns [] (valid).
    - verify_chain() on tests/fixtures/curator/audit_chain_valid.jsonl returns [] (no breaks).
    - verify_chain() on tests/fixtures/curator/audit_chain_tampered.jsonl returns at least one break dict {"line": int, "error": str} describing the tampering.
    - verify_chain() detects prev_sha256 mismatch when an entry's prev_sha256 field is edited.
    - read_audit(action="apply", since="2026-06-01", skill="screenplay") filters correctly; None filters return all.
    - AuditChainError raised when append_audit detects a corrupt tail (malformed JSON on the last line) — NEVER silently chain from a broken entry.
    - Tamper-evident: deleting an interior line produces a prev_sha256 mismatch on all subsequent entries.
    - `ensure_ascii=False` pinned in BOTH append and verify (Pitfall #8 mitigation); CN content in feedback_ids round-trips cleanly.
  </behavior>
  <action>
    Create `agent/curator_audit.py` with `from __future__ import annotations` and module docstring documenting: (a) genesis prev = sha256("").hexdigest() literal, (b) single-operator trust model, (c) append-only contract (no edit/delete API; retraction = append new entry with action="retract" referencing original entry_id), (d) Pitfall #6 concurrency note (single-process). Define `GENESIS_PREV_SHA256` constant. Define `class AuditChainError(Exception): pass`. Implement a single private `_serialize_entry_for_sha256(entry: dict) -> str` returning `json.dumps({k:v for k,v in entry.items() if k != "entry_sha256"}, sort_keys=True, separators=(",",":"), ensure_ascii=False)` — used by BOTH `_compute_entry_sha256` and `verify_chain` (Pitfall #2 mitigation). Implement `_audit_log_path() -> Path` returning `get_hermes_home() / "skills" / ".audit" / "log.jsonl"` with `mkdir(parents=True, exist_ok=True)` on the parent. Implement `append_audit(*, action: str, patch_id: str, skill_id: str, operator: str = "system", feedback_ids: list[str] | None = None, eval_score: dict | None = None, commit_sha: str | None = None) -> str` per CONTEXT.md CURATE-04 schema (entry_id uuid4, prev_sha256 from last line or GENESIS, ISO-8601 UTC ts via datetime.now(timezone.utc).isoformat(), entry_sha256 computed via _serialize helper). Read prev via `path.read_text(encoding="utf-8").splitlines()`; on malformed last line raise AuditChainError with line number. Append via `with path.open("a", encoding="utf-8") as f: f.write(json.dumps(entry, ensure_ascii=False) + "\n")`. Implement `verify_chain() -> list[dict]` per RESEARCH Pattern 3 (walk, recompute, collect breaks, DO NOT break on first mismatch — enumerate all). Implement `read_audit(*, action: str | None = None, since: str | None = None, skill: str | None = None) -> list[dict]` applying filters (since = ISO date lower bound, parse via datetime.fromisoformat). Create the two fixture files under `tests/fixtures/curator/`: audit_chain_valid.jsonl with 3 sequential propose→apply→rollback entries (use the SAME serialization helper to generate valid entry_sha256 values — compute via a one-shot Python invocation if needed, then commit the static file); audit_chain_tampered.jsonl = copy of valid with the middle entry's action field mutated from "apply" to "applyy" (one character addition — breaks entry_sha256 recomputation but keeps valid JSON). Every `open()` passes `encoding="utf-8"`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_audit_log.py -x --timeout=30 && ruff check agent/curator_audit.py</automated>
  </verify>
  <done>
    Module imports cleanly. All behavior tests pass. Fixture files committed. `grep -c "ensure_ascii" agent/curator_audit.py` returns >= 2 (append + verify paths both pin it). `grep -c "encoding=" agent/curator_audit.py` equals the number of open() calls (Ruff PLW1514 clean). verify_chain detects the tampered fixture. AuditChainError raised on corrupt tail.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EVOL-02 generator (agent/evolution/evol02_generator.py) — bilingual multi-instruction diff</name>
  <files>
    agent/evolution/evol02_generator.py,
    agent/evolution/__init__.py,
    tests/agent/evolution/test_evol02_generator.py
  </files>
  <read_first>
    - agent/evolution/diff_generator.py (P31 placeholder — generate_additive_diff signature, _frontmatter_end_offset, anchor-uniqueness check, idempotent guard)
    - agent/evolution/insights.py:96-242 (InsightRecord schema + aggregate_feedback + make_aggregation_client)
    - .planning/phases/32-curator-upgrade-audit/32-CONTEXT.md §"EVOL-02 Generator Design" — LOCKED instruction schema
    - .planning/phases/32-curator-upgrade-audit/32-RESEARCH.md §"EVOL-02 Generator Extension" + Pitfalls #3 (frontmatter drift) + §"Don't Hand-Roll" (centralize bilingual composition)
    - CLAUDE.md §"Skill File Conventions" (bilingual EN heading + body, then CN heading + body)
  </read_first>
  <behavior>
    - _compose_bilingual_block(content_en="# Heading\nEN body", content_zh="### 中文标题\n中文正文") returns "# Heading\nEN body\n\n### 中文标题\n中文正文\n" (EN block, blank line, CN block, trailing newline).
    - generate_patch_from_knowledge_point with a single instruction {"file": "skills/.../references/x.md", "anchor_section": "## Three-Act Structure", "add_after": True, "content_en": "...", "content_zh": "..."} returns a unified diff that adds the bilingual block immediately after the anchor; output is parseable by P31 apply_patch_transaction without error.
    - Multi-instruction single-file case: two instructions against the same file produce a combined diff where the SECOND instruction's anchor is found in the already-mutated content (anchor offsets shift correctly). Choose the build-final-state-then-diff-once approach (RESEARCH note at the bottom of §"EVOL-02 Generator Extension"): mutate a working copy of the file content in-memory for each instruction sequentially, then emit ONE unified_diff via difflib.unified_diff(original_lines, final_lines) for that file. Multi-file case: one diff per file, joined by "\n".
    - ValueError raised on: unknown file path, anchor_section not found in current content, anchor_section appears more than once (duplicate), anchor inside frontmatter block, idempotent no-op (addition already present), content_en or content_zh empty.
    - Frontmatter (lines between leading `---` fences) is NEVER touched — reuse _frontmatter_end_offset from diff_generator.py to validate anchor is outside frontmatter (Pitfall #3).
    - emit_evol02_instructions(*, insight: InsightRecord, current_files: dict[str, str], client: Any, model: str) -> list[dict]: invokes the LLM with a system prompt that EXPLICITLY requires: (a) additive-only content, (b) preserve frontmatter byte-for-byte, (c) bilingual EN+CN per CLAUDE.md convention, (d) output strictly the JSON schema {instructions: [{file, anchor_section, add_after, content_en, content_zh}]}. Raises AggregationError (reuse from insights.py) on malformed LLM output. Mocked in tests; never makes a real API call in the test suite.
    - The output diff applies cleanly: feed it to apply_patch_transaction in an integration test (test_diff_applies_clean) using a tmp git repo with a sample SKILL.md.
    - __init__.py re-exports generate_patch_from_knowledge_point, emit_evol02_instructions, _compose_bilingual_block.
  </behavior>
  <action>
    Create `agent/evolution/evol02_generator.py` with `from __future__ import annotations`, imports of `difflib`, `json`, `logging`, `pathlib.Path`, and `from agent.evolution.insights import InsightRecord, AggregationError`, `from agent.evolution.diff_generator import generate_additive_diff, _frontmatter_end_offset` (reuse, do not fork — RESEARCH "Don't Hand-Roll" row 2). Define module logger. Define `EVOL02_SYSTEM_PROMPT` constant enforcing the 4 LLM constraints listed in <behavior>. Implement `_compose_bilingual_block(content_en: str, content_zh: str) -> str` returning the EN-then-CN format with explicit `.strip()` on each input and a single blank line separator. Implement `generate_patch_from_knowledge_point(*, insight: InsightRecord, current_files: dict[str, str], instructions: list[dict]) -> str` using the build-final-state-then-diff approach: for each instruction, validate file exists in current_files, validate anchor_section exists exactly once in that file's content AND lies after _frontmatter_end_offset (raise ValueError otherwise), compose the bilingual block, splice it after the anchor line in a working copy; collect final-state strings per file; emit one `difflib.unified_diff(original_lines, final_lines, fromfile=..., tofile=...)` per touched file; join multi-file diffs with "\n". Implement `emit_evol02_instructions(*, insight, current_files, client, model) -> list[dict]` mirroring the `aggregate_feedback` LLM-call pattern from insights.py: build user prompt from insight.proposed_addition + list of current_files keys, call client.chat.completions.create(model=model, response_format={"type": "json_object"}, messages=[{system},{user}]), parse JSON, validate each instruction dict has required keys (file, anchor_section, add_after, content_en, content_zh), raise AggregationError on missing keys or wrong types. Extend `agent/evolution/__init__.py` `__all__` and the bottom import block to re-export the three new symbols. Create `tests/agent/evolution/test_evol02_generator.py` covering every <behavior> bullet: TestBilingualComposition, TestSingleInstruction, TestMultiInstructionSameFile, TestAnchorValidation (missing/duplicate/frontmatter), TestIdempotentNoop, TestFrontmatterPreserved, TestEmitInstructions (mocked LLM via monkeypatch), TestDiffAppliesClean (integration with apply_patch_transaction on a tmp git repo — create a sample skills/movie-experts/screenplay/references/structure.md, commit it, generate a diff, apply, assert file mutated as expected). All `open()` calls pass `encoding="utf-8"`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/evolution/test_evol02_generator.py -x --timeout=30 && ruff check agent/evolution/evol02_generator.py agent/evolution/__init__.py</automated>
  </verify>
  <done>
    Module imports cleanly. generate_patch_from_knowledge_point + _compose_bilingual_block + emit_evol02_instructions exported from agent.evolution. All unit tests pass including the apply_patch_transaction integration test. `grep -c "from agent.evolution.diff_generator import" agent/evolution/evol02_generator.py` >= 1 (reuse confirmed). Bilingual format verified by assertion on the CN marker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Feedback threshold scan + propose/audit wiring (extends agent/curator.py)</name>
  <files>
    agent/curator.py,
    tests/agent/test_curator_feedback_scan.py,
    tests/fixtures/curator/feedback_scenarios.json
  </files>
  <read_first>
    - agent/curator.py:1428-1689 (run_curator_review + _llm_pass — locate the extension point at ~line 1671 AFTER save_state(state2))
    - agent/curator.py:124-189 (_load_config + threshold getters — mirror pattern for new feedback threshold getters)
    - agent/feedback_store.py:199 FeedbackStore, :693 _load_or_init_index, :919 query, :990 summary (Pitfall #4 — iterate self._index["buckets"] keys directly)
    - agent/evolution/insights.py:242 aggregate_feedback, :147 make_aggregation_client
    - agent/evolution/queue.py:78 PatchRecord, :192 append_patch
    - tools/skill_usage.py:415 is_agent_created, :426 is_bundled
    - .planning/phases/32-curator-upgrade-audit/32-CONTEXT.md §"Curator Trigger Integration (CURATE-01)" + §"Confidence Score for Auto-Apply (CURATE-05)"
    - .planning/phases/32-curator-upgrade-audit/32-RESEARCH.md Pitfall #4 (FeedbackStore has no list_skill_ids), Pitfall #5 (regression — additive only), Pitfall #7 (bundled NEVER auto)
  </read_first>
  <behavior>
    - _load_feedback_config() reads from cli-config.yaml under feedback.curator.* with defaults: feedback_threshold_count=3, feedback_threshold_sessions=2, auto_apply_enabled=False, auto_apply_min_delta=0.1, auto_apply_min_evidence=3.
    - get_feedback_threshold_count(), get_feedback_threshold_sessions(), get_auto_apply_enabled(), get_auto_apply_min_delta(), get_auto_apply_min_evidence() getters mirror the existing curator config getter pattern.
    - _scan_for_hot_skills(store: FeedbackStore, *, threshold_count: int, threshold_sessions: int) -> list[str] iterates store._index["buckets"] keys of form "{skill_id}:{source}:{verdict}", filters to verdict in {"needs_work","bad"}, sums negative counts across all sources per skill, returns skills with neg_total >= threshold_count AND >= threshold_sessions distinct UTC calendar days (derived from record.ts via query(); Open Question #1 RESOLVED — session_id absent, use distinct dates). NEVER calls store.list_skill_ids() (does not exist — Pitfall #4).
    - _compute_confidence(patch: PatchRecord, *, eval_score: dict, evidence_count: int, min_delta: float, min_evidence: int) -> dict returns {"eligible": bool, "mean_delta": float, "evidence_count": int, "reason": str}. Eligible requires BOTH eval_score.mean_delta >= min_delta AND evidence_count >= min_evidence (two-signal per CONTEXT.md CURATE-05 LOCKED decision).
    - _feedback_scan_phase(start: datetime) -> dict[str, Any] is the ADDITIVE phase: imports lazy (FeedbackStore, aggregate_feedback, make_aggregation_client, generate_patch_from_knowledge_point, emit_evol02_instructions, append_patch, append_audit) INSIDE the function body (preserves runtime isolation — module-level agent.evolution imports FORBIDDEN). For each hot skill: call aggregate_feedback → for each insight → call emit_evol02_instructions → generate_patch_from_knowledge_point → construct PatchRecord (status="pending", auto_apply_eligible=False initially) → append_patch → append_audit(action="propose", patch_id, skill_id, feedback_ids=insight.evidence_chain, operator="system"). Wrapped in try/except Exception that logs WARNING and returns {"scanned": 0, "proposed": [], "error": str(e)} — NEVER aborts the curator run (Pitfall: scan failure must not break pre-v6 phases that already completed).
    - Bundled NEVER auto: _feedback_scan_phase NEVER sets auto_apply_eligible=True on a bundled skill patch. Confidence scoring happens but the eligible flag stays False for bundled (Plan 02 CLI handler enforces the apply-side gate; Plan 01 just records the score). Use tools.skill_usage.is_bundled(skill_id) check before considering auto-apply marking.
    - run_curator_review: at the extension point (~line 1671, after the final save_state(state2) in _llm_pass), invoke feedback_result = _feedback_scan_phase(start) and append its summary to final_summary (e.g., `f"{final_summary}; feedback-scan: {feedback_result.get('proposed', [])}"`). The invocation MUST be inside `if not dry_run:` guard (dry-run is preview-only, pre-v6 contract) AND inside `if consolidate:` is NOT required — feedback-scan is independent of consolidation (per CONTEXT.md the scan is a separate post-v6 phase, not gated by consolidate). When dry_run=True OR FeedbackStore empty OR no skill crosses threshold, the scan is a no-op and final_summary format is unchanged (test_no_feedback_no_scan + test_dry_run_no_state_mutation in Task 1 enforce this).
    - Pre-v6 code paths (apply_automatic_transitions counts, _run_llm_review invocation, report write, state.last_run_at/run_count bumps) are byte-for-byte unchanged — Task 1's test_curator_regression.py enforces this.
  </behavior>
  <action>
    First, extend `agent/evolution/queue.py` `PatchRecord` additively: add `auto_apply_eligible: bool = False` and `confidence_score: dict[str, Any] | None = None` AFTER the existing `ts_rejected` field. Defaults preserve P31 behavior (existing tests unaffected). Add a short docstring note. Re-run P31 queue tests to confirm zero regression. Second, extend `agent/curator.py`: add 5 config getters mirroring `get_consolidate()` / `get_stale_after_days()` pattern (lines 124-209), reading from `cfg.get("feedback", {}).get("curator", {})` with documented defaults. Add `_load_feedback_config()` helper. Add `_scan_for_hot_skills(store, *, threshold_count, threshold_sessions) -> list[str]` implementing Pitfall #4 sketch from RESEARCH (iterate `store._index.get("buckets", {})`, split key on ":", filter verdicts, sum across sources, derive distinct UTC days from `store.query(skill_id=..., verdict="needs_work")` + `verdict="bad"` results via `r.ts.date()` — document the session_id-absent approximation in the docstring). Add `_compute_confidence(patch, *, eval_score, evidence_count, min_delta, min_evidence) -> dict`. Add `_feedback_scan_phase(start: datetime) -> dict[str, Any]` with all imports lazy inside the body (FeedbackStore, aggregate_feedback, make_aggregation_client, generate_patch_from_knowledge_point, emit_evol02_instructions, append_patch, append_audit, is_bundled). Wrap the body in try/except Exception as exc: logger.warning(...) and return error dict. Construct PatchRecord with status="pending", ts_queued=datetime.now(timezone.utc).isoformat(), auto_apply_eligible=False (always for Plan 01; Plan 02 may flip this for agent-created skills via the CLI handler). Then locate the extension point in `run_curator_review`'s `_llm_pass` closure — AFTER the final `save_state(state2)` at ~line 1671, BEFORE `if on_summary:` at ~line 1673. Insert: `if not dry_run: try: fb = _feedback_scan_phase(start); if fb.get("proposed"): final_summary = f"{final_summary}; feedback-scan: proposed {len(fb['proposed'])} patch(es)" except Exception as exc: logger.warning("curator feedback-scan phase failed: %s", exc)`. This is ADDITIVE — the existing save_state, report write, and on_summary calls run exactly as before. Update cli-config.yaml.example with a new documented `feedback.curator.*` block mirroring existing comment style. Create `tests/fixtures/curator/feedback_scenarios.json` with 3 scenarios: (a) below-threshold (2 needs_work same day — not proposed), (b) above-threshold (3 needs_work across 2 distinct UTC days — proposed), (c) bundled-skill-crosses-threshold (proposed but auto_apply_eligible stays False). Create `tests/agent/test_curator_feedback_scan.py` with TestThresholdDetection, TestBelowThresholdNoPropose, TestProposeHotBundledSkill (verifies append_patch + append_audit invoked with action="propose"), TestScanFailureDoesNotAbortCurator (monkeypatch _feedback_scan_phase to raise; verify run_curator_review still completes), TestBundledNeverAutoApplyEligible (bundled patch always auto_apply_eligible=False even if confidence passes), TestEvolInvocation (verifies aggregate_feedback + emit_evol02_instructions + generate_patch_from_knowledge_point + append_patch call sequence via monkeypatched fakes). All tests use tmp_path HERMES_HOME redirection, mock LLM via monkeypatch (no real API calls), and `encoding="utf-8"` on every open().
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_curator_regression.py tests/agent/test_curator_feedback_scan.py tests/agent/evolution/ -x --timeout=60 && python -m pytest tests/hermes_cli/test_evolution_cli.py::TestNonBypassableHumanInLoop -x --timeout=30 && grep -c "from agent.evolution" agent/curator.py | head -1</automated>
  </verify>
  <done>
    All Task 1 regression tests now PASS (including test_consolidate_false_skips_llm_and_scan — the _feedback_scan_phase function now exists and the scan is correctly gated). All Task 4 behavior tests pass. P31 TestNonBypassableHumanInLoop STILL PASSES (apply_patch_transaction not called from runtime — `_feedback_scan_phase` only calls append_patch, NOT apply_patch_transaction). `grep -c "from agent.evolution" agent/curator.py` returns 0 at MODULE LEVEL (lazy imports are INSIDE _feedback_scan_phase function body — runtime isolation preserved). Pre-v6 curator tests in tests/agent/test_curator*.py (existing files) still green. cli-config.yaml.example has the new documented block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→CLI config | cli-config.yaml feedback.curator.* keys cross into runtime via _load_feedback_config — untrusted-ish (operator-owned file but mutable) |
| FeedbackStore→curator | FeedbackRecord data (potentially user-submitted corrections with PII) crosses into curator scan |
| LLM→EVOL-02 | LLM emits structured instructions crossing into patch generation (prompt injection vector) |
| curator→evolution queue | Curator-proposed patches land in P31 queue (separate trust boundary from operator-approved) |
| audit log file→verify | File on disk is mutable by any process with filesystem access (tampering surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | audit log file (~/.hermes/skills/.audit/log.jsonl) | mitigate | sha256 chain: every entry's entry_sha256 depends on prev entry's entry_sha256; verify_chain detects any edit/delete; append-only API (no edit/delete in code); AuditChainError on corrupt tail (Task 2) |
| T-32-02 | Elevation of Privilege | LLM emits malicious EVOL-02 instructions (prompt injection) | mitigate | (1) generate_additive_diff rejects non-additive patches (P31 hardened); (2) _frontmatter_end_offset guard rejects frontmatter insertion (Pitfall #3); (3) FOUND-08 byte-intact check in apply_patch_transaction rejects frontmatter drift; (4) human-in-loop approve via _cmd_approve still required for bundled (P31 invariant preserved — Task 4 does NOT call apply_patch_transaction) |
| T-32-03 | Denial of Service | _feedback_scan_phase crashes abort the curator run | mitigate | try/except Exception around the entire scan phase body; logs WARNING, returns error dict, does NOT raise — pre-v6 phases already completed and their state is preserved (Task 4 test_scan_failure_does_not_abort_curator) |
| T-32-04 | Repudiation | propose actions go unlogged → operator cannot trace why a patch entered the queue | mitigate | Every append_patch call is paired with an append_audit(action="propose", ...) call inside _feedback_scan_phase (Task 4); audit log is append-only and sha256-chained (T-32-01) |
| T-32-05 | Tampering | Operator config sets auto_apply_enabled=true via cli-config.yaml | accept | Default OFF (LOCKED in CONTEXT.md CURATE-05); operator explicitly opts in; bundled NEVER auto regardless of config (is_bundled check in _feedback_scan_phase). Config is operator-owned file — trust boundary is operator's machine. |
| T-32-06 | Information Disclosure | feedback_ids in audit log may contain user corrections with PII | accept | v6 assumes trusted single-operator environment per PROJECT.md (FUTURE-V6-06 auto-redaction deferred). Audit log lives under HERMES_HOME which is operator-controlled. |
| T-32-07 | Tampering | Curator runs in daemon thread while operator invokes CLI (concurrent JSONL writes) | accept | v6 is single-operator per CONTEXT.md Claude's Discretion; worst case = duplicate patch_id (content-addressed, detectable) or corrupt audit line (caught by verify_chain). File locking deferred. Documented in audit module docstring (Task 2). |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 01 installs ZERO external packages (all stdlib + existing in-repo modules per RESEARCH §"Package Legitimacy Audit"). No [SLOP]/[SUS]/[ASSUMED] packages introduced. |
</threat_model>

<verification>
Plan 01 is complete when ALL of the following hold:

1. `python -m pytest tests/agent/test_curator_regression.py tests/agent/test_audit_log.py tests/agent/test_curator_feedback_scan.py tests/agent/evolution/test_evol02_generator.py -x --timeout=60` is green.
2. `python -m pytest tests/agent/test_curator*.py tests/agent/evolution/ -x --timeout=120` is green (all pre-existing + new curator + evolution tests).
3. `python -m pytest tests/hermes_cli/test_evolution_cli.py::TestNonBypassableHumanInLoop -x` is green (P31 structural invariant preserved).
4. `ruff check agent/curator_audit.py agent/evolution/evol02_generator.py agent/evolution/__init__.py agent/evolution/queue.py agent/curator.py` is clean.
5. `grep -c "from agent.evolution\|import agent.evolution" agent/curator.py` shows ZERO matches at column 0 (module-level) — lazy imports are INSIDE _feedback_scan_phase function body only.
6. `grep -c "apply_patch_transaction" agent/curator.py agent/curator_audit.py agent/evolution/evol02_generator.py` returns 0 (no new callers — Plan 01 only proposes, never applies).
7. FOUND-08 byte-intact: `git diff --name-only HEAD -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` returns 0 (no bundled SKILL.md changes — Plan 01 only writes tests + new modules + extends curator.py).
8. `grep -c "encoding=" agent/curator_audit.py agent/evolution/evol02_generator.py` matches the open() call count in each file (PLW1514 clean).
</verification>

<success_criteria>
- SC-6 regression test (Task 1) PASSES — pre-v6 curator behavior byte-for-byte preserved.
- Audit log module (Task 2) — append/verify/query/tamper-detection all behave per LOCKED formula; both fixtures committed.
- EVOL-02 generator (Task 3) — bilingual EN+CN multi-instruction diffs apply cleanly via P31 apply_patch_transaction; __init__.py re-exports.
- Feedback threshold scan (Task 4) — ≥3 neg across ≥2 distinct UTC days triggers propose; bundled NEVER auto_apply_eligible; scan failures never abort the curator run; runtime isolation preserved (lazy imports inside function body only).
- FOUND-08 + P31 TestNonBypassableHumanInLoop + runtime isolation all preserved (verification commands 3, 5, 6, 7).
- CURATE-01 (propose path), CURATE-02 (threshold), CURATE-03 (audit log), EVOL-02 (generator) all covered. CURATE-04 (CLI) and CURATE-05 (apply-side auto path) deferred to Plan 02.
</success_criteria>

<output>
Create `.planning/phases/32-curator-upgrade-audit/32-01-SUMMARY.md` when done. The SUMMARY must record: test count added, confirmation that P31 TestNonBypassableHumanInLoop still passes, confirmation of zero module-level `from agent.evolution` imports in agent/curator.py, the resolved Open Question #1 answer (distinct UTC calendar days as session proxy because FeedbackRecord has no session_id field), and the PatchRecord additive extension (auto_apply_eligible + confidence_score fields with P31-preserving defaults).
</output>

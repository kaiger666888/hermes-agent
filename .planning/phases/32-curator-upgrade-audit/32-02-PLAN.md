---
phase: 32-curator-upgrade-audit
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - tests/hermes_cli/test_curator_cli.py
  - hermes_cli/curator.py
  - hermes_cli/feedback.py
  - cli-config.yaml.example
autonomous: true
requirements: [CURATE-04, CURATE-05]
tags: [curator, cli, auto-apply, audit-log, delegation, p31-invariant]

must_haves:
  truths:
    - "Operator can run `hermes curator queue [--skill X] [--status pending|applied|rejected]` and see pending patches (delegates to P31 _cmd_review_queue)"
    - "Operator can run `hermes curator approve <patch_id> --yes` and the patch applies via the P31 _cmd_approve path (single source of truth for apply_patch_transaction)"
    - "Operator can run `hermes curator reject <patch_id> <reason>` and the patch moves to rejected.jsonl (delegates to P31 _cmd_reject)"
    - "Operator can run `hermes curator audit-log [--action apply] [--since DATE] [--skill X] [--verify]` to query or verify the audit trail"
    - "CURATE-05 auto-apply fires for agent-created skills ONLY when (1) auto_apply_enabled=true in config AND (2) two-signal confidence met (mean_delta >= +0.1 AND evidence_count >= 3); bundled skills NEVER auto-apply"
    - "apply_patch_transaction remains called ONLY from _cmd_approve (P31 structural invariant TestNonBypassableHumanInLoop preserved)"
    - "Every approve / reject / auto_apply action appends an audit entry"
  artifacts:
    - path: "hermes_cli/curator.py"
      provides: "register_cli extension with queue/approve/reject/audit-log subparsers; _cmd_queue / _cmd_approve_curator / _cmd_reject_curator / _cmd_audit_log handlers"
      exports: ["_cmd_queue", "_cmd_approve_curator", "_cmd_reject_curator", "_cmd_audit_log"]
    - path: "tests/hermes_cli/test_curator_cli.py"
      provides: "CLI smoke coverage for queue/approve/reject/audit-log + CURATE-05 auto-apply refuse/fire/default-off + delegation-to-P31 structural assertions"
      min_lines: 200
    - path: "hermes_cli/feedback.py"
      provides: "_cmd_approve extended to call append_audit(action='apply') on success — single source of truth for the apply→audit wiring (A4 from RESEARCH); optional _cmd_auto_apply_eligible handler for CURATE-05 batch auto-apply"
  key_links:
    - from: "hermes_cli/curator.py:_cmd_approve_curator"
      to: "hermes_cli/feedback.py:_cmd_approve"
      via: "direct function call delegation (thin wrapper)"
      pattern: "from hermes_cli\\.feedback import _cmd_approve"
    - from: "hermes_cli/feedback.py:_cmd_approve (after successful move_patch)"
      to: "agent.curator_audit.append_audit(action='apply', commit_sha=...)"
      via: "lazy import + try/except WARNING (best-effort — never block approve on audit failure per RESEARCH A4)"
      pattern: "from agent\\.curator_audit import append_audit"
    - from: "hermes_cli/feedback.py:_cmd_auto_apply_eligible (NEW)"
      to: "agent.evolution.apply.apply_patch_transaction"
      via: "DIRECT call from this CLI handler (NOT from agent/) — this is the second legitimate caller alongside _cmd_approve OR a wrapper around _cmd_approve depending on design choice"
      pattern: "(apply_patch_transaction\\(|_cmd_approve\\()"
---

<objective>
Build the CLI + auto-apply layer of Phase 32: extend `hermes_cli/curator.py:register_cli` with `queue`/`approve`/`reject`/`audit-log` subparsers, wire the audit-logging into `_cmd_approve` (single source of truth per RESEARCH A4), and implement the CURATE-05 auto-apply path that routes through a CLI handler (NOT through `agent/curator.py` — Architectural Constraint #1).

Purpose: Ship CURATE-04 (operator CLI) and CURATE-05 (semi-automatic agent-created-skill path) without breaking the P31 structural invariant `TestNonBypassableHumanInLoop`. The auto-apply path is the single hardest design constraint in this phase — it MUST call `apply_patch_transaction` from a CLI handler, never from runtime code.

Output: Extended `hermes_cli/curator.py` + extended `hermes_cli/feedback.py` (`_cmd_approve` gains audit-log call; new `_cmd_auto_apply_eligible` for CURATE-05) + new `tests/hermes_cli/test_curator_cli.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-curator-upgrade-audit/32-CONTEXT.md
@.planning/phases/32-curator-upgrade-audit/32-RESEARCH.md
@.planning/phases/32-curator-upgrade-audit/32-VALIDATION.md
@.planning/phases/32-curator-upgrade-audit/32-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Plan 02 contracts. The Plan 01 artifacts below are assumed shipped. -->

From Plan 01 (must be COMPLETE before Plan 02 starts):
```python
# agent/curator_audit.py
GENESIS_PREV_SHA256: str   # e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
class AuditChainError(Exception): ...
def append_audit(*, action: str, patch_id: str, skill_id: str,
                 operator: str = "system", feedback_ids: list[str] | None = None,
                 eval_score: dict | None = None, commit_sha: str | None = None) -> str: ...
def verify_chain() -> list[dict]: ...     # [] = valid
def read_audit(*, action: str | None = None, since: str | None = None,
               skill: str | None = None) -> list[dict]: ...

# agent/curator.py — Plan 01 added (read via hermes_cli, NOT modified by Plan 02):
def get_feedback_threshold_count() -> int: ...      # default 3
def get_feedback_threshold_sessions() -> int: ...   # default 2
def get_auto_apply_enabled() -> bool: ...           # default False
def get_auto_apply_min_delta() -> float: ...        # default 0.1
def get_auto_apply_min_evidence() -> int: ...       # default 3
def _compute_confidence(patch, *, eval_score, evidence_count, min_delta, min_evidence) -> dict: ...
#   returns {"eligible": bool, "mean_delta": float, "evidence_count": int, "reason": str}

# agent/evolution/queue.py — Plan 01 extended additively:
class PatchRecord(BaseModel):
    # ... existing fields ...
    auto_apply_eligible: bool = False           # NEW (Plan 01)
    confidence_score: dict[str, Any] | None = None  # NEW (Plan 01)
```

From hermes_cli/feedback.py:828-919 (P31 shipped — Plan 02 EXTENDS _cmd_approve, does NOT fork):
```python
def _cmd_approve(args) -> int:
    # ... existing: --yes gate, lazy import apply_patch_transaction from agent.evolution.apply,
    #     apply_patch_transaction(...), move_patch(patch_id, "pending", "applied", commit_sha=...) ...
    # Plan 02 adds: after successful move_patch, call append_audit(action="apply", patch_id=..., 
    #               skill_id=..., operator=_get_operator(), commit_sha=...) inside try/except WARNING
```

From hermes_cli/curator.py:45 (P31 shipped — Plan 02 EXTENDS register_cli):
```python
def register_cli(parent: argparse.ArgumentParser) -> None:
    # ... existing subparsers: run, status, enable, disable, consolidate, rollback ...
    # Plan 02 appends AFTER the existing p_rollback block:
    #   queue / approve / reject / audit-log subparsers
```

From tests/hermes_cli/test_evolution_cli.py:846 (P31 STRUCTURAL INVARIANT — MUST stay green):
```python
class TestNonBypassableHumanInLoop:
    def test_only_cmd_approve_calls_apply_patch_transaction(self):
        # AST-walks hermes_cli/feedback.py: every apply_patch_transaction() Call node
        # MUST be inside a FunctionDef named _cmd_approve.
        # Plan 02 design decision: if we add a new _cmd_auto_apply_eligible caller, this test
        # MUST be amended to allowlist it as a second legitimate caller. See <action> Task 2.
    def test_apply_patch_transaction_not_called_in_agent_or_runtime(self):
        # ZERO apply_patch_transaction calls in agent/, gateway/, run_agent.py, cli.py
        # (excluding agent/evolution/ itself). Plan 02 does NOT touch these — auto-apply
        # lives in hermes_cli/feedback.py (CLI layer), not runtime.
```

From tools/skill_usage.py (Plan 01 verified):
```python
def is_agent_created(skill_name: str) -> bool: ...   # True ONLY for agent-created
def is_bundled(skill_name: str) -> bool: ...
```

### Architectural Constraint #1 (LOAD-BEARING — read before planning Task 2)
The P31 test `test_only_cmd_approve_calls_apply_patch_transaction` AST-walks `hermes_cli/feedback.py` and asserts every `apply_patch_transaction()` Call node is inside a FunctionDef named EXACTLY `_cmd_approve`. Two design options for CURATE-05 auto-apply (per RESEARCH §"Architectural Constraint #1"):

**Option A (RECOMMENDED — minimal blast radius):** The `_cmd_auto_apply_eligible` handler in `hermes_cli/feedback.py` is a thin wrapper that delegates to `_cmd_approve` for each eligible patch. apply_patch_transaction is still called only from _cmd_approve. P31 test passes UNCHANGED.

**Option B (full auto, requires P31 test amendment):** `_cmd_auto_apply_eligible` calls apply_patch_transaction directly. The P31 test must be amended to allowlist `_cmd_auto_apply_eligible` as a second legitimate caller, with a code comment documenting why this is safe (CURATE-05 fenced: agent-created only, two-signal confidence, default OFF, bundled NEVER).

**This plan uses Option A** unless the executor discovers Option A is infeasible (e.g. _cmd_approve's interactive --yes prompt cannot be bypassed programmatically). If Option B becomes necessary, the executor MUST amend the P31 test with a documented exception comment and re-run it green before proceeding. This is the ONLY legitimate reason to modify a P31-shipped structural test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curator CLI subcommands (queue/approve/reject/audit-log) — thin delegation + audit wiring</name>
  <files>
    hermes_cli/curator.py,
    hermes_cli/feedback.py,
    tests/hermes_cli/test_curator_cli.py,
    cli-config.yaml.example
  </files>
  <read_first>
    - hermes_cli/curator.py:45-200 (register_cli existing pattern — p_run, p_status, p_rollback; mirror style for new subparsers)
    - hermes_cli/feedback.py:828-919 (_cmd_approve — Plan 02 extends with append_audit call AFTER move_patch success)
    - hermes_cli/feedback.py:731-765 (_cmd_review_queue — the function _cmd_queue delegates to)
    - hermes_cli/feedback.py:805-828 (_cmd_reject — the function _cmd_reject_curator delegates to)
    - agent/curator_audit.py (Plan 01 — append_audit, verify_chain, read_audit signatures)
    - tests/hermes_cli/test_evolution_cli.py:1-130 (existing CLI test patterns — TestRegisterCliPreservesExistingSubcommands to mirror)
    - tests/hermes_cli/test_evolution_cli.py:385-465 (TestReviewQueueCmd pattern — reuse for TestQueueCmd)
    - tests/hermes_cli/test_evolution_cli.py:569-828 (TestApproveCmdRequiresYes + TestApproveCmd patterns — reuse for TestApproveCmdCurator)
    - CLAUDE.md §"Anti-Patterns" (no shell=True; lazy %-logging)
  </read_first>
  <behavior>
    - `hermes curator queue` (no args) lists all pending patches via delegation to _cmd_review_queue; exit 0.
    - `hermes curator queue --skill screenplay --status pending` filters via the same delegation (args pass through).
    - `hermes curator approve <patch_id>` WITHOUT --yes exits non-zero with a confirmation prompt (mirrors P31 _cmd_approve_requires_yes).
    - `hermes curator approve <patch_id> --yes` delegates to _cmd_approve; on success, _cmd_approve (in hermes_cli/feedback.py) appends an audit entry with action="apply" and the resolved commit_sha; exit 0.
    - `hermes curator reject <patch_id> "<reason>"` delegates to _cmd_reject; on success, an audit entry action="reject" is appended (extend _cmd_reject analogously OR append in the curator wrapper).
    - `hermes curator audit-log` (no filters) prints all entries: "{ts} {action:10s} {patch_id} skill={skill_id}".
    - `hermes curator audit-log --action apply --since 2026-06-01 --skill screenplay` filters correctly.
    - `hermes curator audit-log --verify` on a valid chain prints "audit chain: OK (all entries verify)" exit 0; on a broken chain prints "audit chain: N break(s) detected:" with per-line errors exit 1.
    - `hermes curator audit-log --verify` on empty/missing log prints "audit chain: OK (no entries)" exit 0.
    - test_approve_delegates_to_p31: monkeypatch hermes_cli.feedback._cmd_approve to a sentinel; invoke _cmd_approve_curator(args); assert sentinel was called with the same args object.
    - test_existing_six_subcommands_still_resolve: pre-existing curator subcommands (run/status/enable/disable/consolidate/rollback) still parse without regression.
    - test_audit_log_append_is_best_effort: monkeypatch append_audit to raise; _cmd_approve still returns 0 (the apply succeeded; audit failure is logged WARNING but does not block the apply — RESEARCH A4 rationale).
    - _get_operator() helper returns the OS username (via getpass.getuser() with fallback to "unknown") for the audit log operator field.
  </behavior>
  <action>
    Extend `hermes_cli/curator.py:register_cli` AFTER the existing p_rollback block. Add four subparsers per the CONTEXT.md §"Curator CLI Surface" spec: p_queue (--skill, --status choices=[pending,applied,rejected] default=pending), p_approve (patch_id positional, -y/--yes flag), p_reject (patch_id positional, reason positional), p_audit (--action choices, --since, --skill, --verify store_true). Set defaults(func=...) to _cmd_queue, _cmd_approve_curator, _cmd_reject_curator, _cmd_audit_log. Implement `_cmd_queue(args) -> int`: lazy `from hermes_cli.feedback import _cmd_review_queue`; return _cmd_review_queue(args). Implement `_cmd_approve_curator(args) -> int`: lazy `from hermes_cli.feedback import _cmd_approve`; return _cmd_approve(args) — audit append is wired INSIDE _cmd_approve (Task 1 also extends _cmd_approve, see below). Implement `_cmd_reject_curator(args) -> int`: lazy `from hermes_cli.feedback import _cmd_reject`; rc = _cmd_reject(args); if rc == 0: lazy `from agent.curator_audit import append_audit`; try: append_audit(action="reject", patch_id=args.patch_id, skill_id=_resolve_skill_from_patch(args.patch_id), operator=_get_operator()) except Exception as exc: logging.getLogger(__name__).warning("audit log append failed: %s", exc); return rc. Implement `_cmd_audit_log(args) -> int`: lazy `from agent.curator_audit import verify_chain, read_audit`; if getattr(args, "verify", False): breaks = verify_chain(); if breaks: print breaks, return 1; else print "audit chain: OK", return 0; else entries = read_audit(action=args.action, since=args.since, skill=args.skill); print each; return 0. Implement `_get_operator() -> str`: try `import getpass; return getpass.getuser()` except Exception: return "unknown". Implement `_resolve_skill_from_patch(patch_id) -> str`: lazy import read_queue from agent.evolution.queue; iterate evolution_dir pending+applied+rejected; return the matching patch's skill_id or "unknown" if not found. Extend `hermes_cli/feedback.py:_cmd_approve` (the P31 shipped function at line 828): AFTER the successful move_patch call (search for the line that writes commit_sha to state), insert a try/except block: lazy `from agent.curator_audit import append_audit`; append_audit(action="apply", patch_id=args.patch_id, skill_id=<resolved>, operator=_get_operator(), commit_sha=<commit_sha>, eval_score=<eval_score if available>); except Exception as exc: logger.warning("audit log append failed: %s", exc). This makes EVERY approve caller (curator wrapper AND direct `hermes feedback approve`) get the audit entry — single source of truth per RESEARCH A4. The lazy import + try/except ensures audit failure NEVER blocks the apply (T-32-04 mitigation). Add `_get_operator` to hermes_cli/feedback.py as well (or import from curator — single source). Update cli-config.yaml.example: under the existing `feedback:` block (or create one if absent), document the `curator:` sub-block with feedback_threshold_count, feedback_threshold_sessions, auto_apply_enabled, auto_apply_min_delta, auto_apply_min_evidence keys + inline comments. Create `tests/hermes_cli/test_curator_cli.py` with: TestRegisterCliPreservesExistingSubcommands (mirrors P31 pattern), TestQueueCmd (empty + populated + filter), TestApproveCmdCurator (requires --yes, delegates to _cmd_approve, audit appended), TestRejectCmdCurator (delegates, audit appended), TestAuditLogCmd (no-filter, action-filter, since-filter, skill-filter, --verify OK, --verify detects break, --verify empty), TestDelegation (monkeypatch _cmd_approve / _cmd_reject / _cmd_review_queue to sentinels and assert call forwarding), TestAuditAppendBestEffort (monkeypatch append_audit to raise; approve still succeeds). Use tmp_path HERMES_HOME redirection, argparse.parse_args(...) for subparser tests (mirror TestRegisterCliPreservesExistingSubcommands pattern at tests/hermes_cli/test_evolution_cli.py:47-63). All `open()` passes `encoding="utf-8"`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/hermes_cli/test_curator_cli.py -x --timeout=30 && python -m pytest tests/hermes_cli/test_evolution_cli.py -x --timeout=60 && ruff check hermes_cli/curator.py hermes_cli/feedback.py</automated>
  </verify>
  <done>
    All four new subcommands parse and delegate correctly. audit-log --verify distinguishes valid/tampered/empty. _cmd_approve in hermes_cli/feedback.py now calls append_audit(action="apply") on success. test_approve_delegates_to_p31 confirms _cmd_approve_curator is a pure delegation wrapper. All existing P31 evolution CLI tests still green (test_evolution_cli.py full suite).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CURATE-05 auto-apply path — agent-created only, two-signal confidence, default OFF, routes through _cmd_approve (Architectural Constraint #1)</name>
  <files>
    hermes_cli/feedback.py,
    tests/hermes_cli/test_curator_cli.py
  </files>
  <read_first>
    - .planning/phases/32-curator-upgrade-audit/32-RESEARCH.md §"Architectural Constraint #1 (CRITICAL)" + §"Auto-Apply Path (CURATE-05)" — Option A vs Option B
    - .planning/phases/32-curator-upgrade-audit/32-CONTEXT.md §"Confidence Score for Auto-Apply (CURATE-05)" — LOCKED two-signal definition
    - hermes_cli/feedback.py:828-919 (_cmd_approve — the function _cmd_auto_apply_eligible delegates to)
    - hermes_cli/feedback.py:45-200 (register_cli — where to add the new auto-apply-eligible subparser under feedback, OR extend curator.register_cli — design decision)
    - agent/evolution/queue.py:78 PatchRecord (now with auto_apply_eligible + confidence_score fields from Plan 01)
    - agent/curator.py (Plan 01 added _compute_confidence, get_auto_apply_enabled, get_auto_apply_min_delta, get_auto_apply_min_evidence)
    - tools/skill_usage.py:415 is_agent_created, :426 is_bundled
    - tests/hermes_cli/test_evolution_cli.py:846 TestNonBypassableHumanInLoop (MUST stay green; Option A preserves it unchanged)
  </read_first>
  <behavior>
    - test_auto_apply_default_off: when cli-config.yaml feedback.curator.auto_apply_enabled is absent OR false, _cmd_auto_apply_eligible scans the queue, finds zero eligible patches (because the config gate is closed), prints "auto-apply disabled in config", exit 0. No apply_patch_transaction call.
    - test_auto_apply_refuses_bundled: with auto_apply_enabled=true, a bundled skill patch has auto_apply_eligible=True (incorrectly marked by a buggy proposer) — _cmd_auto_apply_eligible SKIPS it because `is_agent_created(skill_id)` returns False. Prints "skipped bundled skill <id>: auto-apply forbidden for bundled skills". No apply_patch_transaction call. (Defense-in-depth: even if Plan 01's _feedback_scan_phase incorrectly marks a bundled patch, this CLI gate refuses it — T-32-05 mitigation.)
    - test_auto_apply_fires_for_agent_created: with auto_apply_enabled=true, an agent-created skill patch has auto_apply_eligible=True AND confidence_score={"mean_delta": 0.15, "evidence_count": 4} (both signals pass: 0.15 >= 0.1 AND 4 >= 3) — _cmd_auto_apply_eligible invokes _cmd_approve for that patch (Option A delegation — apply_patch_transaction still called only from _cmd_approve, P31 test passes UNCHANGED). On success, an audit entry action="auto_apply" is appended (distinct from action="apply" so operators can filter). Prints "auto-applied: <patch_id>".
    - test_auto_apply_skips_low_confidence: agent-created patch with confidence_score={"mean_delta": 0.05, "evidence_count": 4} — mean_delta below threshold; SKIPPED. Same for evidence_count=2 (below threshold). Either signal failing prevents auto-apply.
    - test_auto_apply_skips_not_marked_eligible: agent-created patch with auto_apply_eligible=False — SKIPPED regardless of confidence.
    - test_p31_structural_invariant_unchanged: tests/hermes_cli/test_evolution_cli.py::TestNonBypassableHumanInLoop still passes (Option A: _cmd_auto_apply_eligible delegates to _cmd_approve, never calls apply_patch_transaction directly).
    - test_two_signal_required (negative): patches meeting only ONE signal (delta passes but evidence fails, OR evidence passes but delta fails) are SKIPPED. Both signals must pass simultaneously.
    - The auto-apply subcommand surface: `hermes curator auto-apply-eligible` (under curator, mirroring the curator-owns-curate-scope mental model) OR `hermes feedback auto-apply-eligible` (under feedback, mirroring P31's feedback-owns-evolution model). **Decision: `hermes curator auto-apply-eligible`** because CURATE-05 is a curator-scope requirement and operators think of auto-apply as a curator behavior. The handler lives in hermes_cli/curator.py and delegates to _cmd_approve in hermes_cli/feedback.py for each eligible patch.
  </behavior>
  <action>
    Extend `hermes_cli/curator.py:register_cli` with a fifth subparser p_auto_apply (action="auto-apply-eligible", help="Apply all agent-created patches meeting two-signal confidence (CURATE-05; default OFF; bundled NEVER auto)"). Add `--dry-run` flag (lists eligible patches without applying). set_defaults(func=_cmd_auto_apply_eligible). Implement `_cmd_auto_apply_eligible(args) -> int`: lazy imports (read_queue from agent.evolution.queue, is_agent_created from tools.skill_usage, get_auto_apply_enabled/get_auto_apply_min_delta/get_auto_apply_min_evidence from agent.curator, append_audit from agent.curator_audit, _cmd_approve from hermes_cli.feedback). Step 1: config gate — if not get_auto_apply_enabled(): print "auto-apply disabled in config (feedback.curator.auto_apply_enabled=false)"; return 0. Step 2: scan — pending = read_queue(evolution_dir=_resolve_evolution_dir(), status="pending"). Step 3: filter — eligible = [p for p in pending if p.auto_apply_eligible AND is_agent_created(p.skill_id) AND (p.confidence_score or {}).get("mean_delta", 0) >= get_auto_apply_min_delta() AND (p.confidence_score or {}).get("evidence_count", 0) >= get_auto_apply_min_evidence()]. Log skipped patches at INFO with reason (bundled / not-marked / low-delta / low-evidence). Step 4: if args.dry_run: print eligible list; return 0. Step 5: for each patch in eligible: construct an argparse.Namespace with patch_id=p.patch_id, yes=True (programmatically bypass the prompt — this is the operator's explicit opt-in via running the command); rc = _cmd_approve(namespace); if rc == 0: lazy append_audit(action="auto_apply", patch_id=p.patch_id, skill_id=p.skill_id, operator=_get_operator(), commit_sha=<resolved from move_patch state>, eval_score=p.eval_gate_score) inside try/except WARNING; else: logger.error("auto-apply failed for %s: rc=%d", p.patch_id, rc); continue (best-effort — one failure does not abort the batch). Step 6: print summary "auto-applied N patch(es), skipped M"; return 0 if all succeeded else 1. CRITICAL (Architectural Constraint #1 Option A): _cmd_auto_apply_eligible does NOT call apply_patch_transaction directly — it delegates to _cmd_approve which is the sole legitimate caller per the P31 structural test. This keeps TestNonBypassableHumanInLoop green UNCHANGED. Add tests to tests/hermes_cli/test_curator_cli.py: class TestAutoApplyCmd with the 7 behavior methods above. Use monkeypatch to fake read_queue (return controlled PatchRecord lists), fake get_auto_apply_enabled (return True/False), fake _cmd_approve (sentinel that records calls). Verify via AST-walk assertion in test_p31_structural_invariant_unchanged that the test_only_cmd_approve_calls_apply_patch_transaction assertion still holds (run the actual P31 test as the verification). Document in _cmd_auto_apply_eligible docstring: "CURATE-05 — routes through _cmd_approve per Architectural Constraint #1 (Option A). apply_patch_transaction is still called ONLY from _cmd_approve." Add `encoding="utf-8"` on any open() if needed.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/hermes_cli/test_curator_cli.py::TestAutoApplyCmd tests/hermes_cli/test_evolution_cli.py::TestNonBypassableHumanInLoop -x --timeout=30 && grep -c "apply_patch_transaction" hermes_cli/curator.py</automated>
  </verify>
  <done>
    All 7 TestAutoApplyCmd methods pass. P31 TestNonBypassableHumanInLoop passes UNCHANGED (no test amendment needed — Option A chosen). `grep -c "apply_patch_transaction" hermes_cli/curator.py` returns 0 (the curator CLI handler delegates, never calls apply directly). Two-signal confidence enforced (both delta AND evidence thresholds). Bundled skills NEVER auto-applied (defense-in-depth via is_agent_created check even if Plan 01 incorrectly marked a bundled patch). Default OFF (config gate). Audit entries distinguish "auto_apply" from "apply" so operators can filter.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→CLI | Operator invokes `hermes curator {queue, approve, reject, audit-log, auto-apply-eligible}` — untrusted args (patch_id, reason strings) |
| config→runtime | cli-config.yaml feedback.curator.auto_apply_enabled crosses into runtime auto-apply decision |
| CLI→apply machinery | _cmd_approve_curator / _cmd_auto_apply_eligible delegate to _cmd_approve which calls apply_patch_transaction — the single non-bypassable apply gate |
| audit log file→read_audit | Operator-filtered reads; --verify recomputes sha256 (file is mutable on disk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Elevation of Privilege | CURATE-05 auto-apply fires on a bundled skill (SC-3 violation) | mitigate | Defense-in-depth: (1) Plan 01 _feedback_scan_phase never sets auto_apply_eligible=True for bundled skills; (2) Plan 02 _cmd_auto_apply_eligible re-checks is_agent_created(skill_id) and skips bundled even if the flag was incorrectly set; (3) test_auto_apply_refuses_bundled regression test (Task 2) |
| T-32-09 | Elevation of Privilege | CURATE-05 auto-apply fires when config is default-OFF | mitigate | Step 1 of _cmd_auto_apply_eligible: `if not get_auto_apply_enabled(): print disabled-message; return 0` — config gate is the first check, before any queue scan. test_auto_apply_default_off (Task 2). |
| T-32-10 | Repudiation | apply_patch_transaction called outside _cmd_approve (EVOL-04 bypass) | mitigate | Option A design: _cmd_auto_apply_eligible DELEGATES to _cmd_approve, never calls apply_patch_transaction directly. P31 structural test TestNonBypassableHumanInLoop passes UNCHANGED. If Option B were chosen, the test would require amendment with a documented exception — Plan 02 avoids this. |
| T-32-11 | Tampering | Operator passes a malicious patch_id to `hermes curator approve` | mitigate | _cmd_approve (P31) validates patch_id exists in queue.jsonl before applying; apply_patch_transaction (P31) runs the 6-step atomic transaction including additive-only + FOUND-08 byte-intact + path-traversal guards. Plan 02 adds no new attack surface here — delegation only. |
| T-32-12 | Denial of Service | audit append fails and blocks the approve | mitigate | try/except WARNING around every append_audit call in _cmd_approve / _cmd_reject_curator / _cmd_auto_apply_eligible. Apply succeeds even if audit write fails (audit is best-effort, RESEARCH A4). test_audit_log_append_is_best_effort (Task 1). |
| T-32-13 | Information Disclosure | audit-log command prints feedback_ids that may contain PII | accept | v6 single-operator trust model; feedback_ids are record IDs (not free-text PII themselves); the linked FeedbackRecord.correction may contain PII but is not printed by the audit-log command (only patch_id + skill_id + ts + action). |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 02 installs ZERO external packages. No new deps. |
</threat_model>

<verification>
Plan 02 is complete when ALL of the following hold:

1. `python -m pytest tests/hermes_cli/test_curator_cli.py -x --timeout=60` is green (all 5 Test classes: RegisterCli, QueueCmd, ApproveCmdCurator, RejectCmdCurator, AuditLogCmd, Delegation, AuditAppendBestEffort, AutoApplyCmd).
2. `python -m pytest tests/hermes_cli/test_evolution_cli.py -x --timeout=120` is green (full P31 CLI suite — regression preserved).
3. `python -m pytest tests/hermes_cli/test_evolution_cli.py::TestNonBypassableHumanInLoop -x` is green UNCHANGED (P31 structural invariant — Option A preserves it without amendment).
4. `ruff check hermes_cli/curator.py hermes_cli/feedback.py` is clean (PLW1514 — every open() has encoding="utf-8").
5. `grep -c "apply_patch_transaction" hermes_cli/curator.py` returns 0 (curator CLI delegates, never calls apply directly).
6. `grep -c "apply_patch_transaction" hermes_cli/feedback.py` returns the SAME count as before Plan 02 (no new callers added — Option A; the only Call node remains inside _cmd_approve per the P31 AST test).
7. `python -m pytest tests/agent/test_curator*.py tests/agent/evolution/ tests/agent/test_audit_log.py -x --timeout=120` is green (Plan 01 engine tests still pass after Plan 02 changes).
8. FOUND-08 byte-intact: `git diff --name-only HEAD -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` returns 0.
</verification>

<success_criteria>
- CURATE-04 (CLI): all four subcommands queue/approve/reject/audit-log work end-to-end; delegation to P31 confirmed by structural test; audit-log --verify distinguishes valid/tampered/empty.
- CURATE-05 (auto-apply): default OFF; two-signal confidence (delta >= 0.1 AND evidence >= 3); bundled NEVER auto (defense-in-depth via is_agent_created recheck); routes through _cmd_approve (Architectural Constraint #1 Option A — P31 test passes UNCHANGED).
- Audit wiring: _cmd_approve extended to call append_audit(action="apply") — single source of truth for the apply→audit link. Best-effort (audit failure does not block apply).
- P31 TestNonBypassableHumanInLoop + all pre-existing evolution CLI tests preserved.
- FOUND-08 + runtime isolation preserved (Plan 02 does not touch agent/ runtime code).
</success_criteria>

<output>
Create `.planning/phases/32-curator-upgrade-audit/32-02-SUMMARY.md` when done. The SUMMARY must record: which Option (A or B) was chosen for CURATE-05 and why, confirmation that P31 TestNonBypassableHumanInLoop passes UNCHANGED (or, if Option B was necessary, the exact test-amendment diff and rationale), confirmation that apply_patch_transaction is still called only from _cmd_approve, the new CLI subcommand surface, and the final test count for Plan 02.
</output>

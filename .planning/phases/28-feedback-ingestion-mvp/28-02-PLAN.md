---
phase: 28-feedback-ingestion-mvp
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - hermes_cli/feedback.py
  - hermes_cli/main.py
  - hermes_cli/commands.py
  - cli.py
  - agent/feedback_ingest.py
  - tests/hermes_cli/test_feedback_cli.py
  - tests/agent/test_feedback_ingest.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-05]
user_setup:
  - service: "kais-aigc-platform (optional, INGEST-02 integration test)"
    why: "End-to-end test of file-exchange ingestion. Operator drops JSON files into the inbox dir."
    env_vars:
      - name: HERMES_FEEDBACK_INBOX_KAIS
        source: "Operator-chosen mount point for kais-aigc-platform export. Default: ~/.hermes/skills/.feedback/inbox-kais/"

must_haves:
  truths:
    - "Operator can type /feedback good 'comment' (or needs_work / bad) inside a Hermes REPL conversation and see a confirmation message + a persisted JSON file under ~/.hermes/skills/.feedback/incoming/"
    - "Operator can type /feedback with no prior movie-expert skill output in the conversation and receive a clear error message (no silent default, no crash)"
    - "JSON files dropped into ~/.hermes/skills/.feedback/inbox-kais/ are picked up by `hermes feedback watch` within ~2 seconds, validated, written to incoming/, and the source file moved to processed-kais/"
    - "Partially-written files in inbox-kais/ are NOT ingested until size is stable across 2 consecutive polls"
    - "`hermes feedback import <file.jsonl>` writes ALL records (atomic) or NONE and lists every validation error with line numbers"
    - "`hermes feedback import tests/fixtures/feedback/valid_10.jsonl` succeeds as a cold-start path (INGEST-05)"
    - "`hermes feedback --help` lists import, watch, submit subcommands"
    - "No bundled SKILL.md or references/*.md bytes are modified (FOUND-08)"
  artifacts:
    - path: "hermes_cli/feedback.py"
      provides: "register_cli(parent) + cmd_feedback_import + cmd_feedback_watch + cmd_feedback_submit (subcommand dispatcher)"
      contains: "def register_cli"
    - path: "hermes_cli/main.py"
      provides: "1-line wiring: feedback_parser + register_cli call"
      contains: "register_cli"
    - path: "hermes_cli/commands.py"
      provides: "CommandDef entry for /feedback slash command alias"
      contains: "CommandDef"
    - path: "cli.py"
      provides: "_handle_feedback_command method + process_command elif branch for canonical == 'feedback'"
      contains: "_handle_feedback_command"
    - path: "agent/feedback_ingest.py"
      provides: "import_jsonl() atomic batch + watch_inbox_kais() polling watcher (extends Plan 01 write_feedback_record)"
      contains: "def import_jsonl"
    - path: "tests/hermes_cli/test_feedback_cli.py"
      provides: "slash command integration + CLI subcommand smoke tests"
  key_links:
    - from: "hermes_cli/main.py"
      to: "hermes_cli/feedback.py"
      via: "register_cli call"
      pattern: "from hermes_cli\\.feedback import register_cli"
    - from: "cli.py process_command"
      to: "cli.py _handle_feedback_command"
      via: "elif canonical == feedback"
      pattern: "canonical == [\"']feedback[\"']"
    - from: "cli.py _handle_feedback_command"
      to: "agent/feedback_snapshot.build_output_snapshot"
      via: "snapshot capture"
      pattern: "build_output_snapshot"
    - from: "hermes_cli/feedback.py cmd_feedback_import"
      to: "agent/feedback_ingest.import_jsonl"
      via: "batch import"
      pattern: "import_jsonl"
    - from: "hermes_cli/feedback.py cmd_feedback_watch"
      to: "agent/feedback_ingest.watch_inbox_kais"
      via: "watcher loop"
      pattern: "watch_inbox_kais"
    - from: "agent/feedback_ingest.import_jsonl"
      to: "agent/feedback_ingest.write_feedback_record"
      via: "single write path"
      pattern: "write_feedback_record"
---

<objective>
Wire the three feedback sources (CLI `/feedback`, kais-aigc file-exchange watcher, manual JSONL batch import) onto the Plan 01 schema + write-path foundation.

Purpose: This plan ships the operator-facing ingestion paths required by INGEST-01/02/03/05. Plan 01 built the contract + persistence primitive; this plan builds the three doors that produce records conforming to that contract. Per CONTEXT.md locked decisions: `/feedback` slash command (D-INGEST-01), file exchange (D-INGEST-02), JSONL format (D-INGEST-03).

Output: New `hermes_cli/feedback.py` subcommand module, new ingest functions (JSONL importer + kais watcher) in `agent/feedback_ingest.py`, `/feedback` slash handler in `cli.py`, single-line wirings in `hermes_cli/main.py` + `hermes_cli/commands.py`, full test coverage for all three sources. Zero bundled-skill byte changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-feedback-ingestion-mvp/28-CONTEXT.md
@.planning/phases/28-feedback-ingestion-mvp/28-RESEARCH.md
@.planning/phases/28-feedback-ingestion-mvp/28-VALIDATION.md
@.planning/phases/28-feedback-ingestion-mvp/28-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 the executor uses directly. -->

From `agent/feedback_schema.py` (Plan 01):
```python
class OutputSnapshot(BaseModel):
    sha256: str; output_text: str; prompt: str; model: str; provider: str
    api_mode: str = ""; params: dict; captured_at: datetime
    @field_validator("sha256") ...

class FeedbackRecord(BaseModel):
    skill_id: str; expert_id: str
    source: Literal["cli", "kais_aigc", "manual"]
    verdict: Literal["good", "needs_work", "bad"]
    correction: str = ""; revised_output: str | None = None
    output_snapshot: OutputSnapshot; ts: datetime
    @field_validator("skill_id", "expert_id") ...
    @field_validator("ts") ...
```

From `agent/feedback_snapshot.py` (Plan 01):
```python
def build_output_snapshot(
    agent: Any, conversation_history: list[dict[str, Any]], assistant_idx: int
) -> OutputSnapshot: ...
```

From `agent/feedback_ingest.py` (Plan 01, to be EXTENDED in this plan):
```python
def write_feedback_record(record: FeedbackRecord) -> Path: ...
```

From `agent/skill_commands.py:47,73`:
```python
_SKILL_INVOCATION_PREFIX = "[IMPORTANT: The user has invoked the "
# Detection: message["content"].startswith(_SKILL_INVOCATION_PREFIX)
```

From `hermes_cli/curator.py:495-603` (the EXACT register_cli pattern to mirror):
```python
def register_cli(parent: argparse.ArgumentParser) -> None:
    parent.set_defaults(func=lambda a: (parent.print_help(), 0)[1])
    subs = parent.add_subparsers(dest="curator_command")
    p_run = subs.add_parser("run", ...)
    p_run.set_defaults(func=_cmd_run)
    ...
```

From `hermes_cli/commands.py:46,64` (CommandDef + COMMAND_REGISTRY):
```python
@dataclass
class CommandDef:
    name: str; ...  # canonical name without slash
COMMAND_REGISTRY: list[CommandDef] = [...]
```

From `cli.py:7265-7415` (process_command if/elif canonical chain):
```python
# Add: elif canonical == "feedback": self._handle_feedback_command(cmd_original)
```

From `cli.py:3490,3496` (REPL state read by /feedback handler):
```python
self.agent                  # the AIAgent instance
self.conversation_history   # list[dict[str, Any]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JSONL batch importer + kais-aigc file-exchange polling watcher in agent/feedback_ingest.py</name>
  <files>agent/feedback_ingest.py, tests/agent/test_feedback_ingest.py</files>
  <read_first>
    - agent/feedback_ingest.py (Plan 01 Task 3 output — write_feedback_record already defined; this task ADDS import_jsonl + watch_inbox_kais to the same module)
    - agent/feedback_schema.py (Plan 01 — FeedbackRecord, ValidationError)
    - .planning/phases/28-feedback-ingestion-mvp/28-RESEARCH.md (Code Examples §"Atomic JSONL Batch Import" full draft; Pattern 4 §"Portable Filesystem Polling" full draft; Pitfall #2 partial-write detection, #6 watcher orphan, #7 JSONL atomicity)
    - utils.py:111-153 (atomic_json_write — already used by write_feedback_record)
    - tests/agent/test_feedback_ingest.py (Plan 01 — extend the existing class or add new classes TestImportJsonl + TestWatchInboxKais alongside TestWriteFeedbackRecord)
    - tests/fixtures/feedback/valid_10.jsonl + invalid_verdict.jsonl + invalid_skill.jsonl (Plan 01 — the test inputs)
    - CLAUDE.md §"Error Handling" (specific exceptions, `as exc` binding, %-formatting in logs)
  </read_first>
  <behavior>
    - test_import_jsonl_all_valid: import_jsonl(valid_10.jsonl) returns (10, []) and 10 files appear under <hermes_home>/skills/.feedback/incoming/
    - test_import_jsonl_atomic_reject: import_jsonl(invalid_verdict.jsonl) returns (0, errors) where errors contains one entry mentioning "line 2" + "verdict"; ZERO files written to incoming/ (atomicity)
    - test_import_jsonl_invalid_skill_reject: import_jsonl(invalid_skill.jsonl) returns (0, errors) where errors mentions "line 2" + "skill_id"; ZERO files written
    - test_import_jsonl_dry_run: import_jsonl(valid_10.jsonl, dry_run=True) returns (10, []) and ZERO files written (validation only)
    - test_import_jsonl_skips_blank_and_comment_lines: a fixture with blank lines and lines starting with `#` is parsed correctly (skipped, not errored)
    - test_import_jsonl_cold_start_10: same as test_import_jsonl_all_valid but asserts the cold-start use case (operator seeding baseline) — this is the INGEST-05 success criterion
    - test_watch_inbox_ingests_new_file: drop a valid FeedbackRecord JSON into a fake inbox dir, run one or two iterations of watch_inbox_kais (or factor out a single-pass _scan_once helper) → file appears under <hermes_home>/skills/.feedback/incoming/ with source="kais_aigc", and the original is moved to processed-kais/
    - test_watch_inbox_waits_for_stable_size: write a partial file (size 100), call _scan_once, then append more bytes (size 200), call _scan_once again → file is NOT ingested on the first call (size not stable); only on the third call (size unchanged) is it ingested
    - test_watch_inbox_rejects_path_traversal: filename `"../../etc/passwd.json"` in inbox is NOT used as the write target — write target is always generated from skill_id + source + ts (sanitized)
    - test_watch_inbox_continues_on_malformed: a malformed JSON file is moved to an errors/ directory (or logged and skipped) but does NOT crash the scan loop
  </behavior>
  <action>
    Extend `agent/feedback_ingest.py` (do not remove the existing write_feedback_record from Plan 01).

    Define `def import_jsonl(file_path: Path, *, dry_run: bool = False) -> tuple[int, list[str]]:` per RESEARCH Code Examples §"Atomic JSONL Batch Import". Implementation steps: read `file_path.read_text(encoding="utf-8")` (RESEARCH Pitfall #1 — encoding MUST be explicit), split lines via `.splitlines()`. For each non-blank non-`#`-prefixed line (1-indexed), attempt `json.loads(line)`; on `json.JSONDecodeError as exc` append `f"line {i}: invalid JSON: {exc}"` to errors and continue. On parse success, attempt `FeedbackRecord(**raw)`; on `pydantic.ValidationError as exc` append `f"line {i}: validation failed: {exc}"`. After all lines: if errors, return `(0, errors)` WITHOUT writing anything (CONTEXT.md D-INGEST-03 atomic batch). If `dry_run`, return `(len(records), [])`. Otherwise iterate `records` and call `write_feedback_record(r)` for each, then return `(len(records), [])`.

    Define `def watch_inbox_kais(inbox_dir: Path | None = None, *, interval: float = 1.0, stop_event: threading.Event | None = None, max_iterations: int | None = None) -> None:` per RESEARCH Pattern 4. If `inbox_dir is None`, compute `inbox_dir = get_hermes_home() / "skills" / ".feedback" / "inbox-kais"`; allow override via `os.environ.get("HERMES_FEEDBACK_INBOX_KAIS")` (CONTEXT.md specifics line 74). Compute `processed_dir = inbox_dir.parent / "processed-kais"` and `errors_dir = inbox_dir.parent / "errors-kais"`; mkdir all three with `parents=True, exist_ok=True`. Maintain `seen: dict[str, tuple[float, int]]` (name → mtime+size) and `pending: dict[str, int]` (name → last observed size). Loop `while max_iterations is None or iter_count < max_iterations`: check `stop_event.is_set()` → break; `os.scandir(inbox_dir)`; for each `.json` entry skip directories; compute `current = (stat.st_mtime, stat.st_size)`; skip if `seen.get(key) == current`; if `pending.get(key) != stat.st_size` set `pending[key] = stat.st_size` and continue (write in progress, RESEARCH Pitfall #2); otherwise ingest. For ingestion: read bytes, `json.loads`, construct FeedbackRecord with `source="kais_aigc"` override (force the source regardless of what the JSON says — anti-spoofing), call write_feedback_record, then move the source file to `processed_dir / entry.name` via `Path.rename`. On `json.JSONDecodeError` OR `pydantic.ValidationError` OR `OSError` — log `logger.warning("kais inbox ingest failed for %s: %s", entry.name, exc)`, move file to `errors_dir`, set `seen[key] = current` (do not retry). After scan, `time.sleep(interval)`. Support `max_iterations` to make the loop testable without spawning threads.

    Refactor the inner per-file logic into `def _scan_once(inbox_dir: Path, processed_dir: Path, errors_dir: Path, seen: dict, pending: dict) -> int:` returning count ingested — this lets tests call `_scan_once` directly instead of sleeping through `watch_inbox_kais`. `watch_inbox_kais` becomes a thin wrapper that loops calling `_scan_once` + `time.sleep`. Path-traversal defense: when computing write target, ALWAYS use `write_feedback_record(record)` which derives the filename from `record.skill_id + record.source + record.ts` — NEVER from the source filename. When moving the processed file, use `processed_dir / Path(entry.name).name` (the `.name` strips any directory components).

    Create `threading` import. Install SIGINT/SIGTERM-to-stop_event translation at the top of watch_inbox_kais via `signal.signal(signal.SIGINT, lambda *_: stop_event.set() if stop_event else None)` — wrap in try/except for Windows compatibility (SIGTERM missing on Windows).

    Extend `tests/agent/test_feedback_ingest.py` (do NOT remove Plan 01's TestWriteFeedbackRecord class). Add `class TestImportJsonl:` and `class TestWatchInboxKais:` with methods matching the <behavior> bullets. For watcher tests, factor the inbox setup into a fixture that yields `(inbox_dir, processed_dir, errors_dir)` paths under tmp_path. For `test_watch_inbox_waits_for_stable_size`, write the file in three steps with explicit `_scan_once` calls between writes (no actual sleeping). For `test_watch_inbox_continues_on_malformed`, drop a malformed JSON file, call `_scan_once`, assert it returns 0 ingested and the file is now under `errors_dir`, AND a second valid file in the same scan IS ingested (loop did not crash). For `test_watch_inbox_rejects_path_traversal`, create a file with the literal name `../../etc/passwd.json` inside the inbox (use `tmp_path` so the traversal is contained) and assert that after `_scan_once`, no file was created outside the incoming/ dir tree.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_ingest.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def import_jsonl" agent/feedback_ingest.py` returns 1
    - `grep -c "def watch_inbox_kais" agent/feedback_ingest.py` returns 1
    - `grep -c "def _scan_once" agent/feedback_ingest.py` returns 1
    - `grep -c "def write_feedback_record" agent/feedback_ingest.py` returns 1 (Plan 01 function preserved)
    - `grep -c 'source="kais_aigc"' agent/feedback_ingest.py` returns at least 1 (anti-spoofing override in watcher)
    - `grep -c "HERMES_FEEDBACK_INBOX_KAIS" agent/feedback_ingest.py` returns at least 1
    - `grep -v '^ *#' agent/feedback_ingest.py | grep -c "Path.home"` returns 0
    - `python -m pytest tests/agent/test_feedback_ingest.py -x` exits 0 (covers Plan 01 + Plan 02 ingest tests)
    - `python -m pytest tests/agent/test_feedback_ingest.py::TestImportJsonl -x` exits 0
    - `python -m pytest tests/agent/test_feedback_ingest.py::TestWatchInboxKais -x` exits 0
    - `ruff check agent/feedback_ingest.py` exits 0
  </acceptance_criteria>
  <done>
    import_jsonl does atomic all-or-nothing batch import with line-numbered errors and dry-run support. watch_inbox_kais polls inbox-kais/ at 1s, ingests stable files with source="kais_aigc" override, moves processed files to processed-kais/, moves malformed files to errors-kais/ without crashing the loop. Path-traversal safe. All ingest tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hermes_cli/feedback.py register_cli + /feedback slash command wiring</name>
  <files>hermes_cli/feedback.py, hermes_cli/main.py, hermes_cli/commands.py, cli.py, tests/hermes_cli/test_feedback_cli.py</files>
  <read_first>
    - hermes_cli/curator.py:495-603 (the EXACT register_cli pattern to mirror line-for-line in style)
    - hermes_cli/main.py:11670 (where _secrets_cli.register_cli is wired — copy this call style for feedback)
    - hermes_cli/main.py:11972 (where _register_curator_cli is wired — second reference)
    - hermes_cli/commands.py:42-80 (CommandDef dataclass + COMMAND_REGISTRY list — see how existing commands are declared)
    - cli.py:7265-7415 (process_command if/elif canonical chain — add the feedback branch here)
    - cli.py:3490 and 3496 (self.agent + self.conversation_history — REPL state the handler reads)
    - agent/skill_commands.py:47,73,245-280 (_SKILL_INVOCATION_PREFIX marker + _build_skill_message — for skill_id resolution; per RESEARCH Open Question #1, scan conversation_history backward for this marker)
    - agent/feedback_schema.py (Plan 01), agent/feedback_snapshot.py (Plan 01), agent/feedback_ingest.py (Plan 01 + Task 1 of this plan)
    - CLAUDE.md §"Skill File Conventions" (no edits to bundled skills — this plan adds slash command only, no skill changes)
  </read_first>
  <behavior>
    - test_register_cli_creates_subcommands: building a parent ArgumentParser and calling register_cli(parent) yields `import`, `watch`, `submit` subcommands
    - test_cmd_feedback_import_valid_file: invoking the import subcommand func with args.file=valid_10.jsonl → returns 0 and 10 files written
    - test_cmd_feedback_import_invalid_file_returns_nonzero: invoking import with invalid_verdict.jsonl → returns nonzero exit code and prints errors to stdout/stderr
    - test_cmd_feedback_watch_invokes_watch_inbox_kais: invoking watch subcommand func (with max_iterations injected via test fixture) calls agent.feedback_ingest.watch_inbox_kais
    - test_slash_feedback_persists: _handle_feedback_command("good nice work") with a FakeAgent + a conversation_history containing a prior skill-invoked assistant turn → writes one FeedbackRecord file with source="cli", verdict="good", correction="nice work"
    - test_slash_feedback_no_skill_output: _handle_feedback_command("good") with empty conversation_history or no assistant message → prints an error containing "No" (movie-expert output / conversation) and writes ZERO files
    - test_slash_feedback_unknown_verdict: _handle_feedback_command("excellent ...") → prints usage showing the 3 valid verdicts; writes nothing
    - test_slash_feedback_skill_id_resolution: when the preceding user message starts with _SKILL_INVOCATION_PREFIX containing the skill name "screenplay", the resulting FeedbackRecord.skill_id == "screenplay"
    - test_slash_feedback_usage_when_no_args: _handle_feedback_command("") → prints usage line containing "/feedback"
    - test_feedback_command_in_command_registry: COMMAND_REGISTRY contains a CommandDef with name == "feedback"
  </behavior>
  <action>
    Create `hermes_cli/feedback.py` with `from __future__ import annotations`. Import `argparse`, `sys`, `from pathlib import Path`. Mirror `hermes_cli/curator.py:495-603` structure exactly: define `def register_cli(parent: argparse.ArgumentParser) -> None:` that sets a default help func, creates `subs = parent.add_subparsers(dest="feedback_command")`, and registers three subcommands.

    `p_import = subs.add_parser("import", help="Batch-import feedback records from a JSONL file")`. Add `p_import.add_argument("file", help="Path to .jsonl file (one FeedbackRecord per line)")` and `p_import.add_argument("--dry-run", action="store_true", help="Validate without writing")`. `p_import.set_defaults(func=_cmd_import)`.

    `p_watch = subs.add_parser("watch", help="Watch inbox-kais/ for new kais-aigc feedback files")`. Add `p_watch.add_argument("--interval", type=float, default=1.0, help="Poll interval (seconds)")`. `p_watch.set_defaults(func=_cmd_watch)`.

    `p_submit = subs.add_parser("submit", help="Submit a single feedback record (scripting-friendly)")`. Add positional `skill_id`, positional `verdict` with `choices=["good", "needs_work", "bad"]`, `--correction` default `""`, `--revised` default `None`, `--output-text` (required — scripting path doesn't have live conversation state), `--prompt-text` default `""`, `--model` default `""`, `--provider` default `""`. `p_submit.set_defaults(func=_cmd_submit)`.

    Define `_cmd_import(args) -> int:` that calls `from agent.feedback_ingest import import_jsonl`, invokes `count, errors = import_jsonl(Path(args.file), dry_run=args.dry_run)`. If errors: print each to stderr, return 1. Else: print `f"Imported {count} records"` (or `f"Validated {count} records (dry-run)"` if dry_run), return 0.

    Define `_cmd_watch(args) -> int:` that calls `from agent.feedback_ingest import watch_inbox_kais`, constructs a `threading.Event()` for stop_event, installs signal handlers (inside watch_inbox_kais already), prints startup banner with PID + inbox path + log location (RESEARCH Pitfall #6), and calls `watch_inbox_kais(interval=args.interval, stop_event=stop_event)`. Return 0 on clean exit.

    Define `_cmd_submit(args) -> int:` that constructs an `OutputSnapshot` (compute sha256 from args.output_text.encode utf-8), wraps it in a `FeedbackRecord(source="manual", ...)`, calls `write_feedback_record`, prints the written path, returns 0. On `pydantic.ValidationError`, print field-level errors and return 1.

    Edit `hermes_cli/main.py` near line 11972 (where _register_curator_cli is wired): add `feedback_parser = subparsers.add_parser("feedback", help="Feedback ingestion subcommands")` and `from hermes_cli.feedback import register_cli as _register_feedback_cli; _register_feedback_cli(feedback_parser)`. Match the existing 2-line style.

    Edit `hermes_cli/commands.py` `COMMAND_REGISTRY` list: add one `CommandDef(name="feedback", ...)` entry mirroring existing entries (read lines 64-80 first to confirm the exact dataclass fields — likely `name`, `help`, aliases, etc.). Do NOT break the Telegram-parity test (RESEARCH flagged this test as sensitive to registry additions — read it before editing if uncertain).

    Edit `cli.py`: add an `elif canonical == "feedback":` branch inside `process_command()` (line 7265+) that calls `self._handle_feedback_command(cmd_original)`. Add a new method `_handle_feedback_command(self, cmd_original: str) -> None:` per RESEARCH Code Examples §"Pattern 2: Slash Command Dispatch in process_command()". Implementation: parse `cmd_original` via simple split OR `shlex.split` — extract verdict (must be one of the 3 valid values) and correction (rest of the line); support `--revised "<text>"` optional flag. If no agent or no conversation_history, print error "No active conversation to attach feedback to." and return. Scan `self.conversation_history` backward for the most recent assistant message; if none, print "No assistant output found to attach feedback to." and return. Resolve skill_id by scanning backward for the most recent user message whose content (string form) starts with `_SKILL_INVOCATION_PREFIX` (import from `agent.skill_commands`), parse the skill name from the marker; if no marker found, print "No movie-expert skill output found in this conversation. /feedback attaches to the most recent skill output." (RESEARCH Pitfall #4 — DO NOT silently default). If skill_id resolved, call `build_output_snapshot(self.agent, self.conversation_history, last_assistant_idx)`, construct `FeedbackRecord(skill_id=resolved, expert_id=resolved, source="cli", verdict=verdict, correction=correction, output_snapshot=snapshot, ts=datetime.now(timezone.utc))`, call `write_feedback_record(record)`, print confirmation with the written path. Wrap the whole body in try/except `pydantic.ValidationError as exc` printing field-level errors.

    Create `tests/hermes_cli/test_feedback_cli.py` with `class TestFeedbackCli:` containing pytest methods matching the <behavior> bullets. For `_handle_feedback_command` tests, build a minimal fake REPL class with just `agent` and `conversation_history` attributes OR mock `cli.HermesCLI` — prefer constructing a small stub class `_StubCLI:` with the method copied onto it (or use `unittest.mock`). For `test_slash_feedback_skill_id_resolution`, build conversation_history with a user message whose content starts with the exact `_SKILL_INVOCATION_PREFIX` string followed by something containing the skill name "screenplay" — verify the resulting record's skill_id. For `test_cmd_feedback_watch_invokes_watch_inbox_kais`, use `unittest.mock.patch("agent.feedback_ingest.watch_inbox_kais")` and assert it was called once with the expected interval.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/hermes_cli/test_feedback_cli.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def register_cli" hermes_cli/feedback.py` returns 1
    - `grep -c "def _cmd_import\|def _cmd_watch\|def _cmd_submit" hermes_cli/feedback.py` returns 3
    - `grep -c "from hermes_cli.feedback import register_cli" hermes_cli/main.py` returns 1
    - `grep -c 'add_parser("feedback"' hermes_cli/main.py` returns 1
    - `grep -c "feedback" hermes_cli/commands.py` returns at least 1 (new CommandDef entry)
    - `grep -c 'canonical == "feedback"' cli.py` returns at least 1
    - `grep -c "def _handle_feedback_command" cli.py` returns 1
    - `python -m pytest tests/hermes_cli/test_feedback_cli.py -x` exits 0
    - `python -m pytest tests/hermes_cli/test_feedback_cli.py::TestFeedbackCli::test_slash_feedback_persists -x` exits 0
    - `python -m pytest tests/hermes_cli/test_feedback_cli.py::TestFeedbackCli::test_slash_feedback_no_skill_output -x` exits 0
    - `python -m pytest tests/agent/test_feedback_schema.py::test_source_enum_same_schema -x` exits 0 (cross-source parity — INGEST-03 contract holds)
    - `ruff check hermes_cli/feedback.py` exits 0
    - `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` returns 0 (FOUND-08)
  </acceptance_criteria>
  <done>
    hermes feedback {import,watch,submit} subcommands work end-to-end via register_cli pattern. /feedback slash command resolves skill_id from prior skill invocation, captures OutputSnapshot, persists FeedbackRecord with source="cli". Clear errors when no skill output, unknown verdict, or validation failure. All CLI tests pass. FOUND-08 preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kais-aigc-platform filesystem → Hermes watcher | External process writes JSON files to inbox-kais/. Hermes reads + validates them. Trust assumption: HERMES_FEEDBACK_INBOX_KAIS points to an operator-controlled mount (PROJECT.md trusted-operator assumption). |
| Operator REPL → cli.py /feedback handler | Operator types arbitrary text; handler parses verdict + correction. Bounded by Literal["good","needs_work","bad"] validation in Pydantic. |
| CLI args → hermes_cli/feedback.py subcommands | `argparse` choices constrain verdict; file paths are validated by Pydantic + atomic write (no path traversal possible — write target always derived from skill_id+source+ts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06 | Tampering | kais inbox filename → write target path traversal | mitigate | write_feedback_record derives filename ONLY from record.skill_id + record.source + record.ts. Source file rename uses `Path(entry.name).name` to strip directory components. Path traversal test asserts no file escapes the incoming/ tree. |
| T-28-07 | Spoofing | kais inbox JSON claims source="cli" | mitigate | watch_inbox_kais OVERRIDES source to "kais_aigc" regardless of JSON content (anti-spoofing). Test asserts override. |
| T-28-08 | Denial of Service | Malformed JSON in inbox crashes watcher loop | mitigate | try/except json.JSONDecodeError + pydantic.ValidationError per file; move to errors-kais/; continue scanning. Test asserts loop survives + subsequent valid file still ingested. |
| T-28-09 | Denial of Service | Partially-written file read mid-write | mitigate | 2-poll stability check (record size, ingest only when size unchanged on next poll). Test asserts partial files wait. |
| T-28-10 | Denial of Service | Watcher process orphaned on Ctrl+C | mitigate | SIGINT/SIGTERM handlers set stop_event; PID printed on startup. Operator can kill. (Windows SIGTERM absent — wrapped in try/except.) |
| T-28-11 | Denial of Service | JSONL batch import writes partial on line-N failure | mitigate | All-or-nothing: parse + validate ALL lines first, write only if errors is empty. Test asserts zero files written on invalid_verdict.jsonl. |
| T-28-12 | Information Disclosure | Pydantic ValidationError in /feedback reveals schema | accept | Schema is in-repo public. Helps operator correct input. Low risk. |
| T-28-13 | Repudiation | Feedback record lacks operator identity | accept | Out of P28 scope (CONTEXT.md deferred: "source enum {cli,kais_aigc,manual} is sufficient for v6"). Operator identity tracking deferred. |
| T-28-14 | Elevation | /feedback handler mutates conversation_history | mitigate | _handle_feedback_command only READS self.conversation_history; build_output_snapshot's _extract_text does not mutate input. Code review assertion. |
</threat_model>

<verification>
Plan 02 verification (run after both tasks complete):

1. **Full feedback test suite green:** `python -m pytest tests/agent/test_feedback_schema.py tests/agent/test_feedback_snapshot.py tests/agent/test_feedback_ingest.py tests/hermes_cli/test_feedback_cli.py -x` exits 0
2. **CLI subcommand wiring:** `python -c "from hermes_cli.feedback import register_cli; import argparse; p=argparse.ArgumentParser(); register_cli(p); print('ok')"` prints `ok`
3. **Command registry entry:** `python -c "from hermes_cli.commands import COMMAND_REGISTRY; assert any(c.name == 'feedback' for c in COMMAND_REGISTRY); print('ok')"` prints `ok`
4. **Slash command branch present:** `grep -c 'canonical == "feedback"' cli.py` returns at least 1
5. **Cold-start smoke (INGEST-05):** in a Python REPL, `from agent.feedback_ingest import import_jsonl; from pathlib import Path; print(import_jsonl(Path("tests/fixtures/feedback/valid_10.jsonl")))` returns `(10, [])`
6. **Atomic reject smoke (INGEST-03):** in a Python REPL, `import_jsonl(Path("tests/fixtures/feedback/invalid_verdict.jsonl"))` returns `(0, [...])` with a non-empty error list mentioning line 2 + verdict
7. **Cross-source parity (INGEST-03):** `python -m pytest tests/agent/test_feedback_schema.py::test_source_enum_same_schema -x` exits 0
8. **Ruff clean:** `ruff check agent/feedback_schema.py agent/feedback_snapshot.py agent/feedback_ingest.py hermes_cli/feedback.py` exits 0
9. **FOUND-08 byte-intact:** `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` returns 0
10. **No new dependencies:** `git diff --name-only v5.0 -- pyproject.toml uv.lock | wc -l` returns 0
</verification>

<success_criteria>
- `/feedback <verdict> [correction]` slash command works in REPL, resolves skill_id from prior skill invocation, captures OutputSnapshot, persists FeedbackRecord with source="cli" (INGEST-01)
- `/feedback` with no prior skill output prints clear error, writes nothing (INGEST-01 negative path)
- `hermes feedback watch` polls inbox-kais/, ingests stable JSON files with source="kais_aigc" override, moves to processed-kais/ (INGEST-02)
- Partial-write detection (2-poll stability) prevents truncated-file ingestion (INGEST-02 robustness)
- `hermes feedback import <file.jsonl>` does atomic all-or-nothing batch import with line-numbered errors + dry-run support (INGEST-03)
- All three sources emit the same FeedbackRecord schema — cross-source parity verified by test_source_enum_same_schema (INGEST-03)
- `hermes feedback import tests/fixtures/feedback/valid_10.jsonl` cold-start path succeeds — 10 records written (INGEST-05)
- `hermes feedback submit` provides a scripting-friendly manual-ingest path (INGEST-05)
- Zero bundled SKILL.md or references/*.md bytes changed (FOUND-08)
- Zero new dependencies introduced
</success_criteria>

<output>
Create `.planning/phases/28-feedback-ingestion-mvp/28-02-SUMMARY.md` when done.
</output>

# Phase 19 Plan — Snowflake Method Integration

**Plan ID:** 19-01
**Phase:** 19 — Snowflake Method Integration
**Mode:** Autonomous / full-automation (skip_discuss)
**Created:** 2026-06-18
**Source context:** [`19-CONTEXT.md`](./19-CONTEXT.md)
**Requirements:** SNOWFLAKE-01..04

---

## Goal

把 Randy Ingermanson 的雪花法 10 步递进管线挂载到 `creative_source` + `screenplay` 两个 active expert,填补 StoryKernel → Snyder 15-beat 之间的"展开塌陷"。Phase 19 不创建新 expert_id,只在 `skills/movie-experts/` 下做 4 处增量(1 新 ref + 2 SKILL.md body patch + 4 glossary entries)。

---

## Success Criteria ↔ Deliverables Mapping

| SC # | Requirement | Deliverable | Verification |
|------|-------------|-------------|--------------|
| 1 | SNOWFLAKE-01 | `creative_source/references/snowflake-method.md` ≥ 200 行,含 10 Ingermanson 步骤 + 短剧 60-180s 单集 step scaling + 10-80 集连续剧 step scaling + StoryKernel→Snowflake 桥接协议 | `wc -l ≥ 200` + grep 10 step headings + bridge 协议 section 存在 |
| 2 | SNOWFLAKE-02 | `creative_source/SKILL.md` body 引用 snowflake 调用点(StoryKernel → Snowflake-3 → Snowflake-4 → screenplay)+ 输出 schema 声明 | grep "Snowflake" ≥ 3 处 + Output schema 段落含 Snowflake artifact |
| 3 | SNOWFLAKE-03 | `screenplay/SKILL.md` body 在 Beat Planning 步骤前新增 "Consume Snowflake-4" 子步骤 + 字段映射表 | workflow 步骤序列调整 + 映射表 markdown 表格存在 |
| 4 | SNOWFLAKE-04 | `_shared/glossary.md` 新增 4 个 H3 词条(Snowflake Method / Story Spine / Premise Sentence / Scene List),中英对照 + Ingermanson 出处 | grep 4 个 `### ` 标题 + 出处标注 |
| 5 | (架构约束) | 无新 expert_id 目录;`expert_id` + `related_skills` 不变 | diff 不触及 frontmatter |

---

## File-level Plan

### F1: NEW `skills/movie-experts/creative_source/references/snowflake-method.md`

**Target length:** 280-380 行(对齐 screenplay 现有 refs 密度,如 save-the-cat-beat-sheet.md 303 行)。

**Structure:**
1. Header + Source + Copyright (Fair Use paraphrase + Ingermanson 出处 + LICENSE.md 引用,对齐 save-the-cat-beat-sheet.md §1-6 行模式)
2. Summary — 雪花法为什么填补 StoryKernel → Snyder 之间的展开塌陷
3. ## The 10 Ingermanson Steps — 表格列出 10 步原文 + 输入/输出 + 递进关系
4. ## 短剧 Step Scaling (60-180s 单集) — 哪些 step 必做 / 可选 / 延后到 screenplay 内部
   - 强制 minimum: Step 1 (premise) + Step 2 (paragraph) + Step 3 (character synopsis) + Step 4 (one-page synopsis)
   - 可选: Step 5-6 (character bios + 4-page synopsis)
   - 延后: Step 7-10 移至 screenplay 内部 Beat Planning 之后(避免与 Snyder beat sheet 重复)
5. ## 短剧 Step Scaling (10-80 集连续剧) — series-level vs episode-level step split
6. ## StoryKernel → Snowflake Bridge Protocol — 显式协议
   - 触发条件: `unspeakability_score ≥ 7` (CONTEXT decision §3) OR `dramatic_potential.overall ≥ 0.75`
   - Step 1 输入: StoryKernel `structural_formula` (1-2 个最强冲突)
   - 输出 artifact: `snowflake_artifacts.json` (含 step 1-6 全部字段)
7. ## Snowflake-4 → Snyder 15-Beat Field Mapping — 字段映射表 (CONTEXT decision §4)
8. ## 与 McMahon 6 弧线的隐含耦合 — step 2 paragraph expansion 选择 1 个 polarity template (CONTEXT specifics §75)
9. ## Output Schema (`snowflake_artifacts.json`)
10. ## License — Fair Use paraphrase 声明 + LICENSE.md 引用

**License status:** Fair Use paraphrase(对齐 save-the-cat-beat-sheet.md §4 行的模式),Ingermanson 出处标注。

### F2: PATCH `skills/movie-experts/creative_source/SKILL.md`

**修改范围:** 只动 body(不动 frontmatter)。新增 2 处:
1. 在 `## References` 表新增 1 行(snowflake-method.md 引用)
2. 在 `## Workflow` 末尾(Step 11 之后)新增 step 12-13(Snowflake 展开 + 输出 snowflake_artifacts.json),并在 `## Output Format` 后追加 `## Snowflake Expansion Output` 小节,声明 snowflake_artifacts.json schema

**禁改:** frontmatter (`name` / `expert_id` / `related_skills` / `tags`)。

### F3: PATCH `skills/movie-experts/screenplay/SKILL.md`

**修改范围:** 只动 body。新增 1 处:
1. 在 `## Workflow` 步骤 1(Knowledge Retrieval)与步骤 2(Beat Planning)之间,新增步骤 1.5 "Consume Snowflake-4 一页大纲"(声明消费 creative_source 的 snowflake_artifacts.json.step_4SynopsisOnePage 作为 scaffold + 字段映射表)

**禁改:** frontmatter。

### F4: PATCH `skills/movie-experts/_shared/glossary.md`

**修改范围:** 文件末尾追加 4 个 H3 词条(对齐现有 `### 运镜 / cinematography / camera movement` 格式):
1. `### Snowflake Method / 雪花法`
2. `### Story Spine / 故事脊`
3. `### Premise Sentence / 一句话前提`
4. `### Scene List / 场景列表`

每条含 CN / EN / Context(Ingermanson 2000s 出处)。

### F5: Verification

写 `19-VERIFICATION.md`,逐 SC 验证 + status 字段。

---

## Execution Order (sequential, no waves)

1. F1 — Write snowflake-method.md
2. F2 — Edit creative_source/SKILL.md body
3. F3 — Edit screenplay/SKILL.md body
4. F4 — Edit _shared/glossary.md
5. F5 — Verify & write VERIFICATION.md

**Sequential 理由:** F2/F3 引用 F1 中的术语,F4 glossary 引用 F1 step 名称。线性顺序避免术语漂移。

---

## Constraints / Backward-compat

- ❌ 不创建新 expert 目录(`snowflake/` 等)
- ❌ 不修改任何 `expert_id` 字段(FOUND-08 HARD RULE)
- ❌ 不修改任何 `related_skills` 数组(FOUND-08 backward-compat)
- ❌ 不动 screenplay 现有 6 个 ref(snowflake 是补充,不替换 Snyder/McKee/Tan/McMahon)
- ❌ 不动 creative_source 现有 Bourdieu 6 层结构
- ❌ 不创建新 eval 维度(知识层增量,_eval/ 不更新)
- ✅ 文档语言:中文为主,方法论术语保留英文(对齐 v3.0 关闭后所有 refs 风格)
- ✅ 新 ref 必须含 LICENSE.md 引用(Fair Use paraphrase 模式)

---

## Out of Scope (留待 Phase 21 DOC)

- `skills/movie-experts/README.md` corpus tree 更新 → Phase 21 DOC-01
- `skills-mapping.yaml` sign-off → Phase 21 DOC-02

---

## Risks

| Risk | Mitigation |
|------|------------|
| F1 行数 < 200 | 写作时实时 `wc -l` 校验 |
| F2/F3 编辑破坏 frontmatter | 仅 Edit tool 精确字符串匹配,不动 frontmatter 区 |
| F4 词条名重复 | 写前 `grep -i "snowflake\|story spine\|premise sentence\|scene list"` |
| 字段映射表与 Snyder 15-beat 不对齐 | 显式引用 save-the-cat-beat-sheet.md beat 编号 |

---

**Plan author:** Claude (autonomous mode)
**Plan date:** 2026-06-18

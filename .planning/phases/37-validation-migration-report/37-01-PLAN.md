---
phase: 37-validation-migration-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
  - .planning/milestones/v7.0-MIGRATION-REPORT.md
  - .planning/phases/37-validation-migration-report/37-VERIFICATION.md
autonomous: true
requirements: [VALIDATE-01, VALIDATE-02, VALIDATE-03]

must_haves:
  truths:
    - "Each migrated skill (coding-agent + tmux-agents) has at least 1 benchmark prompt whose structural preconditions are verified (frontmatter valid + delegation/operation invocation forms documented + discoverable via skill directory walk)"
    - "Enhanced ~/.hermes/SOUL.md has all 4 routing categories (immediate / cognitive / expert-management / default) verified present + source-tagged, with the rule forms needed for runtime routing"
    - ".planning/milestones/v7.0-MIGRATION-REPORT.md exists and documents every transform decision + every explicitly skipped item (feishu-* / acp-router / models.json / sessions / multi-profile) each with a one-line rationale"
    - "Operator smoke-test commands are documented in the migration report (MEM0_API_KEY config + live mem0 ingestion + SOUL.md routing smoke-test + skill invocation smoke-test)"
  artifacts:
    - path: .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      provides: "Structural benchmark results for VALIDATE-01 + VALIDATE-02 (grep-based, runtime-deferred items flagged)"
      min_lines: 80
    - path: .planning/milestones/v7.0-MIGRATION-REPORT.md
      provides: "Canonical v7.0 migration close-out report — transform decisions + skipped items + operator action items"
      contains: ["Transform Decisions", "Explicitly Skipped", "MEM0_API_KEY"]
      min_lines: 200
    - path: .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      provides: "Phase 37 verification report with must-haves + requirements coverage"
  key_links:
    - from: .planning/milestones/v7.0-MIGRATION-REPORT.md
      to: .planning/phases/34-skills-migration-coding-agent-tmux-agents/34-VERIFICATION.md
      via: "Phase-by-Phase Summary references 34-VERIFICATION status"
      pattern: "34-VERIFICATION"
    - from: .planning/milestones/v7.0-MIGRATION-REPORT.md
      to: .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
      via: "Transform Decisions table references SOUL adaptation source"
      pattern: "35-01-TRANSFORMATION-NOTE"
    - from: .planning/milestones/v7.0-MIGRATION-REPORT.md
      to: .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md
      via: "Operator Action Items references ingestion commands"
      pattern: "36-01-INGESTION-NOTE"
---

<objective>
Validate Phases 34-36 deliverables are regression-free (structural preconditions) and produce the canonical `.planning/milestones/v7.0-MIGRATION-REPORT.md` documenting every v7.0 transform decision + every explicitly skipped item with rationale. This is the FINAL phase of v7.0 — closes the milestone and feeds v8.0 planning.

Purpose: VALIDATE-01 (skill benchmark) + VALIDATE-02 (SOUL routing) cannot exercise live runtime (no hermes conversation / tmux session in this validation context), so the plan honestly scopes to **structural preconditions + invocation forms + rule completeness** and hands off runtime smoke-tests to the operator with documented commands. VALIDATE-03 (migration report) is the load-bearing, fully-achievable deliverable.

Output:
- `.planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md` — structural benchmark validation
- `.planning/milestones/v7.0-MIGRATION-REPORT.md` — canonical migration report
- `.planning/phases/37-validation-migration-report/37-VERIFICATION.md` — phase verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-validation-migration-report/37-CONTEXT.md

# Phase 34-36 deliverables (read-only inputs)
@.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-VERIFICATION.md
@.planning/phases/35-soul-md-identity-enhancement/35-VERIFICATION.md
@.planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
@.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-VERIFICATION.md
@.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md
@skills/autonomous-ai-agents/coding-agent/SKILL.md
@skills/autonomous-ai-agents/tmux-agents/SKILL.md
@skills/autonomous-ai-agents/COEXISTENCE.md

# Reference for audit-style writing (matching format conventions)
@.planning/milestones/v6.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key facts the executor needs without re-discovery. Verified at plan-time. -->

## Phase 34 verified facts (from 34-VERIFICATION.md, status=passed 5/5)
- coding-agent/SKILL.md: 160 lines, valid YAML frontmatter, 4 launch blocks at L78/84/90/96 (Claude Code / Codex / OpenCode / Pi), each with `terminal(command="tmux new-session -d -s '$SESSION' -x 200 -y 50 ...", pty=true)` form
- tmux-agents/SKILL.md: 166 lines, valid YAML frontmatter, 4 H3 operations at L49/73/81/93 (Spawn / List / Check / Attach)
- COEXISTENCE.md: 61 lines, SUPPLEMENT decision, 6×4 capability matrix
- Bidirectional related_skills graph COMPLETE for all 6 autonomous-ai-agents skills (verified at commit a62c1178d)
- Zero unresolved openclaw-format dependencies (`metadata.openclaw.*`, `OPENCLAW_STATE_DIR`, `ACP thread` all absent)

## Phase 35 verified facts (from 35-VERIFICATION.md, status=human_needed; structural=all-pass)
- ~/.hermes/SOUL.md: 4519 bytes, 4 routing categories present:
  - `### 即时执行命令 (Immediate Execution — Local Skill)` at L9
  - `### 认知类命令 (Cognitive — MCP / Memory / Skill)` at L17
  - `### 专家管理命令 (Expert Management)` at L61
  - `### 默认 (Default)` at L69
- Original 515-byte Hermes Nous-Research identity preserved byte-for-byte as Section A
- 9× `openclaw 迁移` source tags + 10× `2026-06-25` date tags (both ≥ 5)
- `GLM-4-flash` and `你是"手"` framing absent (correctly dropped)
- Backup: ~/.hermes/SOUL.md.openclaw-backup-2026-06-25 (975 bytes, mtime preserved, cmp=0)
- SC #1 + SC #2 explicitly require runtime observation — Phase 37 validates rule presence + completeness, defers live routing to operator

## Phase 36 verified facts (from 36-VERIFICATION.md, status=human_needed; tooling-only)
- ~/.hermes/memories/USER.md exists with openclaw-origin frontmatter (3 keys), body byte-identical to source
- plugins/memory/mem0/scripts/batch_ingest.py: 282 lines, dry-run enumerates 124 files (NOT 133 — planning estimate corrected in INGESTION-NOTE)
- plugins/memory/mem0/scripts/spot_check.py: 206 lines, 5 queries (AIGC deployment / ComfyUI / Trellis / ACE-Step / CosyVoice)
- Idempotency mechanism in code: `_get_existing_hashes` + `metadata.content_hash` + skip at L178-180
- MEM0_API_KEY NOT configured — live ingestion DEFERRED to operator
- 124 .md files / 837,136 bytes / 817 KB (NOT 133 / 1.3MB — doc-consistency discrepancy flagged for operator decision)

## Skipped items required in VALIDATE-03 (5 categories, each needs one-line rationale)
1. feishu-* (feishu-doc / feishu-drive / feishu-perm / feishu-wiki skills) — deferred to v8.0+ (FEISHU-01/02 in REQUIREMENTS.md Future Requirements)
2. acp-router skill — openclaw-internal scheduler; no hermes analog (ACP-01 deferred)
3. models.json / provider keys — Kai handles manually, out-of-milestone (operator-handled, REQUIREMENTS.md Out of Scope)
4. agents/<name>/sessions/ runtime state — no migration value, would risk state corruption (REQUIREMENTS.md Out of Scope)
5. Multi-profile mechanism — v7.0 uses single SOUL.md; multi-profile deferred (AGT-01 deferred)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Structural benchmark validation for VALIDATE-01 + VALIDATE-02</name>
  <files>.planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md</files>

  <read_first>
    - skills/autonomous-ai-agents/coding-agent/SKILL.md
    - skills/autonomous-ai-agents/tmux-agents/SKILL.md
    - skills/autonomous-ai-agents/COEXISTENCE.md
    - ~/.hermes/SOUL.md
    - .planning/phases/34-skills-migration-coding-agent-tmux-agents/34-VERIFICATION.md
    - .planning/phases/35-soul-md-identity-enhancement/35-VERIFICATION.md
    - .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
    - .planning/phases/37-validation-migration-report/37-CONTEXT.md
  </read_first>

  <behavior>
    - VALIDATE-01 structural: coding-agent SKILL.md exists + YAML frontmatter parses + exactly 4 delegation targets documented (Claude Code / Codex / OpenCode / Pi) each with `terminal(command="tmux new-session ...")` form
    - VALIDATE-01 structural: tmux-agents SKILL.md exists + YAML frontmatter parses + exactly 4 operations documented (Spawn / List / Check / Attach) each with `terminal(command="...", pty=true)` form
    - VALIDATE-01 structural: both skills discoverable via `find skills/autonomous-ai-agents -maxdepth 2 -name SKILL.md` (all 6 skills present)
    - VALIDATE-01 RUNTIME DEFERRED: live tmux spawn + delegation chain execution requires operator environment (documented honestly as deferred)
    - VALIDATE-02 structural: ~/.hermes/SOUL.md contains all 4 routing categories (即时执行 / 认知 / 专家管理 / 默认) + each rule category has source tag (`openclaw 迁移`) + date tag (`2026-06-25`)
    - VALIDATE-02 structural: original Hermes identity preserved (first paragraph pre-H2 contains "Nous Research") — non-destructive contract holds
    - VALIDATE-02 RUNTIME DEFERRED: live hermes conversation + observing routing split requires operator (documented honestly as deferred)
  </behavior>

  <action>
    Create `.planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md` with these required sections (use the exact H2 headers below):

    1. **`# Phase 37 — Structural Benchmark Results`** — frontmatter (`phase: 37-validation-migration-report`, `authored: 2026-06-25`, `status: structural-pass / runtime-deferred-to-operator`).

    2. **`## Scope Clarification`** — explicitly acknowledge per 37-CONTEXT.md: VALIDATE-01 + VALIDATE-02 live runtime testing (hermes conversation + tmux + agent binaries) is operator-scope; Phase 37 validates STRUCTURAL preconditions (file presence, frontmatter validity, invocation-form presence, rule completeness) and documents the runtime smoke-test commands for operator handoff. State this honestly as a SCOPED BOUNDARY, not as a gap.

    3. **`## VALIDATE-01 — Skill Benchmarks (Structural)`** with three subsections:
       - **`### coding-agent`** — run and record the output of:
         - `[ -f skills/autonomous-ai-agents/coding-agent/SKILL.md ] && wc -l skills/autonomous-ai-agents/coding-agent/SKILL.md` (existence + line count, expect 160)
         - `python3 -c "import yaml; yaml.safe_load(open('skills/autonomous-ai-agents/coding-agent/SKILL.md').read().split('---')[1])"` (frontmatter parses; wrap in try/except reporting pass/fail)
         - `grep -cE '^\*\*(Claude Code|Codex|OpenCode|Pi):\*\*$' skills/autonomous-ai-agents/coding-agent/SKILL.md` (expect 4)
         - `grep -cE 'terminal\(command="tmux new-session' skills/autonomous-ai-agents/coding-agent/SKILL.md` (expect ≥ 4 — invocation forms present)
         - For each of the 4 delegation targets, state the benchmark prompt (per CONTEXT.md decisions): "use claude code to fix a typo in README.md" → expected behavior = skill discoverable + delegation form documented. Mark `STRUCTURAL: PASS` and `RUNTIME: OPERATOR-SMOKE-TEST` for each.
       - **`### tmux-agents`** — same structure. Grep checks:
         - `[ -f skills/autonomous-ai-agents/tmux-agents/SKILL.md ] && wc -l ...` (expect 166)
         - YAML parse check
         - `grep -cE '^### (Spawn|List|Check|Attach)' skills/autonomous-ai-agents/tmux-agents/SKILL.md` (expect 4)
         - `grep -cE 'terminal\(command="tmux' skills/autonomous-ai-agents/tmux-agents/SKILL.md` (expect ≥ 8 — multiple tmux invocations across operations)
         - Benchmark prompt per CONTEXT.md: "spawn a coding agent named test-session to do X" → expected = spawn form documented + skill discoverable. Mark `STRUCTURAL: PASS` and `RUNTIME: OPERATOR-SMOKE-TEST`.
       - **`### Discoverability (both skills)`** — run `find skills/autonomous-ai-agents -maxdepth 2 -name SKILL.md | sort` and confirm all 6 skills present (claude-code, coding-agent, codex, hermes-agent, opencode, tmux-agents).

    4. **`## VALIDATE-02 — SOUL.md Routing (Structural)`** with subsections per category. For each, grep ~/.hermes/SOUL.md and record hit count + line content:
       - **`### Immediate-execution`** — `grep -nE '即时执行' ~/.hermes/SOUL.md` + `grep -cE 'draw|video|tts|run|status|queue' ~/.hermes/SOUL.md`. Benchmark prompts (per CONTEXT.md): "draw a cat", "tts hello world" — expected = rule present + trigger tokens listed. Mark STRUCTURAL: PASS.
       - **`### Cognitive`** — `grep -nE '认知' ~/.hermes/SOUL.md`. Prompts: "帮我设计剧本", "记住这个角色" — expected = rule present with skill-discovery/mem0/curator routing guidance.
       - **`### Expert-management`** — `grep -nE '专家管理' ~/.hermes/SOUL.md`. Prompt: "/expert" — expected = rule present with curator/skill-discovery routing.
       - **`### Default`** — `grep -nE '默认' ~/.hermes/SOUL.md`. Prompt: "今天天气怎么样" — expected = default route to configured LLM (NOT GLM-4-flash hardcoded — verify `! grep -q "GLM-4-flash" ~/.hermes/SOUL.md`).
       - **`### Non-destructive contract`** — verify original Hermes identity preserved: `head -1 ~/.hermes/SOUL.md` should contain "Nous Research" (or whatever the operator-state file's first line says — read and report verbatim). Source/date tagging: `grep -c "openclaw 迁移" ~/.hermes/SOUL.md` (expect ≥ 5) + `grep -c "2026-06-25" ~/.hermes/SOUL.md` (expect ≥ 5).

    5. **`## Operator Smoke-Test Commands (Deferred Runtime Validation)`** — a checklist the operator runs after this phase. Each item: command + expected output + why-deferred.
       - coding-agent live spawn: pick a delegation target, run the documented `terminal(command="tmux new-session ...")` form from coding-agent/SKILL.md, expect `tmux has-session` returns 0
       - tmux-agents live spawn/list/attach: run the spawn form from tmux-agents/SKILL.md L49, then `tmux list-sessions`, then `tmux capture-pane -t <name> -p -S -50`
       - SOUL.md live routing: from a hermes conversation issue "draw a cat" (immediate) vs "今天天气怎么样" (default) — observe different routing
       - All three with explicit "WHY DEFERRED: requires operator environment (live hermes runtime + tmux + agent CLIs)" annotations.

    6. **`## Summary`** — table with two columns (Check | Result) and one row per check, all marked either `STRUCTURAL: PASS` or `RUNTIME: OPERATOR-SMOKE-TEST`. Closing paragraph: structural preconditions for VALIDATE-01 + VALIDATE-02 are met; runtime verification is honestly handed off to operator per CONTEXT.md scoped-boundary decision.

    Tone: factual, audit-style (mirror v6.0-MILESTONE-AUDIT.md's evidence-with-numbers style). No marketing language. Every claim has a grep/wc evidence line. The runtime deferral must read as a SCOPED BOUNDARY honoring operator-context constraints, NOT as a gap or failure.
  </action>

  <verify>
    <automated>
      # File exists and has required sections
      test -f .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      grep -q "^# Phase 37 — Structural Benchmark Results" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      grep -q "^## VALIDATE-01" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      grep -q "^## VALIDATE-02" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      grep -q "STRUCTURAL: PASS" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      grep -q "OPERATOR-SMOKE-TEST" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
      # Line count substantive (not a stub)
      test $(wc -l < .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md) -ge 80
    </automated>
  </verify>

  <done>
    - 37-01-BENCHMARK-RESULTS.md exists with all 6 sections
    - Each VALIDATE-01 check shows concrete grep output (not "it works")
    - Each VALIDATE-02 routing category confirmed present with evidence line
    - Runtime smoke-test commands documented for operator handoff
    - Honest scope acknowledgment present (structural vs runtime split)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write canonical .planning/milestones/v7.0-MIGRATION-REPORT.md (VALIDATE-03)</name>
  <files>.planning/milestones/v7.0-MIGRATION-REPORT.md</files>

  <read_first>
    - .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md (just produced in Task 1)
    - .planning/phases/34-skills-migration-coding-agent-tmux-agents/34-VERIFICATION.md
    - .planning/phases/35-soul-md-identity-enhancement/35-01-TRANSFORMATION-NOTE.md
    - .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md
    - .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-VERIFICATION.md
    - .planning/phases/37-validation-migration-report/37-CONTEXT.md
    - .planning/milestones/v6.0-MILESTONE-AUDIT.md (format reference only)
    - .planning/STATE.md (deferred-items table at lines 150-162)
    - .planning/REQUIREMENTS.md (Out of Scope table at lines 61-74)
  </read_first>

  <behavior>
    - Report covers all 6 required sections per 37-CONTEXT.md decisions (Executive Summary / Transform Decisions / Explicitly Skipped Items / Operator Action Items / Phase-by-Phase Summary / Forward-Looking Notes)
    - Transform Decisions table covers the 4 transform categories: skill frontmatter mapping / prerequisite mapping / SOUL rule adaptation / memory ingestion strategy — each with rationale sourced from 34/35/36 transformation notes
    - Explicitly Skipped Items table covers all 5 required categories (feishu-* / acp-router / models.json / sessions / multi-profile), each with a one-line rationale (NOT just a name)
    - Operator Action Items section includes MEM0_API_KEY config + live mem0 ingestion + SOUL.md routing smoke-test + skill invocation smoke-test
    - Phase-by-Phase Summary cites each phase's VERIFICATION status (34=passed 5/5, 35=human_needed structural-pass, 36=human_needed tooling-only) and the human_needed items
    - Forward-Looking Notes suggest v8.0 priorities drawn from deferred items (feishu-* / multi-profile / acp-router evaluation)
    - Length: 200-400 lines (per CONTEXT.md Claude's Discretion) — comprehensive but not bloated
  </behavior>

  <action>
    Create `.planning/milestones/v7.0-MIGRATION-REPORT.md` with YAML frontmatter and the following sections, using the EXACT H2 headers shown. Mirror the audit-style format of v6.0-MILESTONE-AUDIT.md (factual, evidence-cited, no marketing language).

    **Frontmatter:**
    ```
    ---
    milestone: v7.0
    milestone_name: openclaw → hermes-agent Primary Agent Migration
    authored: 2026-06-25
    status: shipped (structural); runtime smoke-tests deferred to operator
    phases: 4 (34, 35, 36, 37)
    requirements: 14 (SKILL×4 + SOUL×3 + MEM×4 + VALIDATE×3)
    ---
    ```

    **Required sections (exact H2 headers):**

    1. `# v7.0 openclaw → hermes-agent Migration Report` — title + 1-paragraph milestone purpose statement (migrate openclaw primary-agent capabilities into hermes-agent).

    2. `## Executive Summary` — what v7.0 accomplished. 3-5 bullet points covering: 2 skills migrated (coding-agent + tmux-agents) into skills/autonomous-ai-agents/; SOUL.md enhanced non-destructively with 4 AIGC routing categories; memory tooling built (batch_ingest + spot_check) with USER.md migrated + 124-file dry-run validated; coexistence decision (SUPPLEMENT) documented; migration report (this document) canonicalized. Closing statement: "v7.0 ships the structural migration; live runtime verification (tmux spawn + mem0 ingestion + SOUL routing observation) is handed off to operator per scoped-boundary decisions in Phase 37 CONTEXT.md."

    3. `## Transform Decisions` — a markdown table with columns **`| Decision Category | Source Convention | Target Convention | Rationale | Source Artifact |`**. Rows:
       - Skill frontmatter mapping | openclaw `metadata.openclaw.{requires.anyBins, requires.config}` | hermes `metadata.hermes.{tags[], related_skills[]}` | Hermes uses directory-walk skill discovery + tag-based search; openclaw's binary-config prerequisites recast as prose `## Prerequisites` sections | skills/autonomous-ai-agents/{coding-agent,tmux-agents}/SKILL.md
       - Prerequisite mapping (coding-agent) | `requires.anyBins: [claude, codex, opencode, pi]` | Prose section listing 4 CLIs + cross-references to claude-code/codex/opencode skills for install+auth | Hermes lacks a binary-registry prerequisite schema; prose preserves the "at-least-one-of" semantics | coding-agent/SKILL.md L52-60
       - Prerequisite mapping (tmux-agents) | `requires.anyBins: [tmux]` | Prose section `## Prerequisites` (L18-22) | Same rationale | tmux-agents/SKILL.md L18-22
       - SOUL rule adaptation (immediate-execution) | openclaw regex `^(?i)(draw|video|tts|run|status|queue)` → `local_skill` | Natural-language section `### 即时执行命令` listing all 6 tokens + routing to local AIGC skill execution | Hermes is a single-agent model with skill discovery, not a regex-dispatch router; regex rewritten as natural-language routing guidance | 35-01-TRANSFORMATION-NOTE.md §3
       - SOUL rule adaptation (cognitive MCP) | 5 openclaw MCP routes (`mcp:hermes_plan`, `_memory_write`, `_memory_read`, `_reflect`, `_learn`) | 5 hermes-native routes (skill-discovery / mem0 plugin / mem0 plugin / curator / curator+memory) | Hermes' actual MCP server (`mcp_serve.py`) exposes a different vocabulary (conversations_list, messages_read, etc.); closest surfaces are skill discovery + mem0 + curator | 35-01-TRANSFORMATION-NOTE.md §4
       - SOUL rule adaptation (default) | openclaw hardcoded `GLM-4-flash` | Generic "configured LLM" (cli-config.yaml `model:`) | Hermes is model-agnostic; hardcoding a single model would constrain the operator | 35-01-TRANSFORMATION-NOTE.md §6
       - SOUL identity adaptation | openclaw two-agent "手/脑" framing | DROPPED (no single-agent analog); hermes Section A Nous-Research identity preserved byte-for-byte | Two-agent framing contradicts hermes single-agent model | 35-01-TRANSFORMATION-NOTE.md §2
       - Memory ingestion strategy | openclaw workspace/memory/*.md static files | mem0 backend with idempotent SHA-256 dedup + per-file metadata.content_hash | Operator-queryable semantic search; idempotent re-ingest prevents duplication | 36-01-INGESTION-NOTE.md

    4. `## Explicitly Skipped Items` — markdown table with columns **`| Item | Category | Rationale | Disposition |`**. EXACTLY these 5 rows (per VALIDATE-03 SC#3 spec — the report MUST cover all 5 categories):
       - `feishu-* skills (feishu-doc / feishu-drive / feishu-perm / feishu-wiki)` | Skills | Deferred to v8.0+ — needs design decision on merge-vs-keep-4 (FEISHU-02); may bundle with hermes feishu plugin | Deferred — FEISHU-01/02 in REQUIREMENTS.md Future Requirements
       - `acp-router skill` | Skills | openclaw-internal scheduler; hermes has no analog (hermes is itself the primary agent, not an ACP client) | Deferred — ACP-01 in REQUIREMENTS.md; AGT-02 evaluation pending
       - `models.json / provider keys` | Configuration | Operator-handled manually (provider dashboard credentials, no programmatic migration value) | Out-of-milestone — Kai handles (REQUIREMENTS.md Out of Scope)
       - `agents/<name>/sessions/ runtime state` | Runtime state | No migration value; would risk state corruption across agent implementations | Out-of-milestone — REQUIREMENTS.md Out of Scope
       - `Multi-profile mechanism` | Architecture | v7.0 uses single SOUL.md enhancement; multi-profile is a v8.0+ design decision | Deferred — AGT-01 in REQUIREMENTS.md Future Requirements

    5. `## Operator Action Items` — numbered list with concrete commands. MUST include all 4 items:
       1. **Configure MEM0_API_KEY** — obtain from https://app.mem0.ai → API Keys; add to `~/.hermes/.env` as `MEM0_API_KEY=<key>` OR create `~/.hermes/mem0.json` with `{"api_key":"<key>","user_id":"hermes-user","agent_id":"hermes"}`. (Source: 36-01-INGESTION-NOTE.md "Operator Action Required".)
       2. **Run live mem0 ingestion** — `python3 plugins/memory/mem0/scripts/batch_ingest.py`; expected `Ingestion complete: total=124 ingested=124 skipped=0 failed=0`. Then `python3 plugins/memory/mem0/scripts/spot_check.py` (5 queries). Then re-run batch_ingest.py to verify idempotency (`ingested=0 skipped=124`).
       3. **SOUL.md routing smoke-test** — from a hermes conversation, issue `draw a cat` (expect local skill invocation) vs `今天天气怎么样` (expect default LLM chat) vs `帮我设计剧本` (expect skill-discovery / memory surface). Observe routing split.
       4. **Skill invocation smoke-test** — from a hermes conversation, trigger coding-agent (e.g., "use claude code to fix a typo in README.md") and tmux-agents (e.g., "spawn a coding agent named test-session"). Verify skill discovery + delegation form fires.

       Include a brief `### Why Deferred` sub-paragraph: these items require operator-environment resources (cloud credentials, live hermes runtime, tmux + agent CLIs) that cannot be exercised by automated phase verification. The structural preconditions are verified (see Phase 37 BENCHMARK-RESULTS.md); these commands are the runtime confirmation.

    6. `## Phase-by-Phase Summary` — markdown table with columns **`| Phase | Name | Verification Status | Requirements Closed | Human-Needed Items | Key Artifacts |`**. Rows for Phase 34, 35, 36, 37:
       - Phase 34 | Skills Migration (coding-agent + tmux-agents) | passed (5/5 must-haves) | SKILL-01..04 | None (runtime skill invocation deferred to P37 VALIDATE-01) | skills/autonomous-ai-agents/{coding-agent,tmux-agents}/SKILL.md + COEXISTENCE.md
       - Phase 35 | SOUL.md Identity Enhancement | human_needed (structural all-pass; 2 SC require runtime) | SOUL-01..03 | 2 items: immediate-execution live routing observation + cognitive/default routing split observation | ~/.hermes/SOUL.md + backup + 35-01-TRANSFORMATION-NOTE.md
       - Phase 36 | Memory Ingestion | human_needed (tooling-only; 3 SC require live backend) | MEM-01..04 | 3 items: live ingest of 124 files + 5-query spot-check + idempotency re-run + (optional) doc-consistency patch on 133-vs-124 discrepancy | batch_ingest.py + spot_check.py + 36-01-INGESTION-NOTE.md + ~/.hermes/memories/USER.md
       - Phase 37 | Validation & Migration Report | (this phase) structural-pass / runtime-deferred | VALIDATE-01..03 | Operator smoke-tests (4 items, see Operator Action Items) | 37-01-BENCHMARK-RESULTS.md + this report

       Add a `### Notable Cross-Phase Findings` subsection with 2-3 bullets: (a) 124-vs-133 file count discrepancy from planning estimate (INGESTION-NOTE File Count Correction), (b) SOUL.md transformation dropped both `GLM-4-flash` literal and two-agent "手/脑" framing (TRANSFORMATION-NOTE §2 + §6), (c) bidirectional related_skills graph made complete across all 6 autonomous-ai-agents skills (codex↔opencode asymmetry resolved as bonus).

    7. `## Forward-Looking Notes for v8.0` — 4-6 bullets suggesting v8.0 priorities drawn from deferred items:
       - feishu-* skill migration + FEISHU-02 merge-vs-keep-4 design decision (likely needs hermes feishu plugin bundling)
       - Multi-profile mechanism evaluation (AGT-01) — does Kai need 7 profiles like openclaw, or is single-SOUL.md sufficient?
       - acp-router re-evaluation (AGT-02 / ACP-01) — could hermes serve as ACP server instead of client?
       - Operator smoke-test outcomes from v7.0 — if any structural preconditions fail at runtime, feed back into v8.0 scope
       - Movie-experts subsystem continues in sibling repo (kais-movie-agent); v8.0 should NOT touch skills/movie-experts/ in this repo
       - Potential cross-AI peer review of this migration report via `/gsd:review` (deferred per 37-CONTEXT.md)

    Tone: factual, no marketing. Every rationale should cite a source artifact (34/35/36 transformation notes or VERIFICATION reports). The "Explicitly Skipped Items" section is load-bearing for preventing v8.0 scope-creep reversal — each rationale must be specific enough that re-litigating it in v8.0 requires new information, not just re-asking the question.
  </action>

  <verify>
    <automated>
      # File exists at canonical path
      test -f .planning/milestones/v7.0-MIGRATION-REPORT.md
      # All 6 required H2 sections present (exact headers from CONTEXT.md)
      grep -q "^## Executive Summary"           .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "^## Transform Decisions"         .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "^## Explicitly Skipped Items"    .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "^## Operator Action Items"       .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "^## Phase-by-Phase Summary"      .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "^## Forward-Looking Notes"       .planning/milestones/v7.0-MIGRATION-REPORT.md
      # All 5 explicitly-skipped categories mentioned with rationale (NOT just names)
      grep -q "feishu-"        .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "acp-router"     .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "models.json"    .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "sessions"       .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "multi-profile"  .planning/milestones/v7.0-MIGRATION-REPORT.md
      # Operator action item: MEM0_API_KEY config documented
      grep -c "MEM0_API_KEY"   .planning/milestones/v7.0-MIGRATION-REPORT.md | grep -qE '^[1-9]'
      # Length substantive (200-400 line target per CONTEXT.md)
      LINES=$(wc -l < .planning/milestones/v7.0-MIGRATION-REPORT.md)
      test "$LINES" -ge 200
      # Source artifact traceability (at least one of each phase cited)
      grep -q "34-VERIFICATION\|34-01-PLAN\|Phase 34" .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "35-01-TRANSFORMATION-NOTE\|35-VERIFICATION\|Phase 35" .planning/milestones/v7.0-MIGRATION-REPORT.md
      grep -q "36-01-INGESTION-NOTE\|36-VERIFICATION\|Phase 36" .planning/milestones/v7.0-MIGRATION-REPORT.md
    </automated>
  </verify>

  <done>
    - v7.0-MIGRATION-REPORT.md exists at the canonical milestone path
    - All 6 required sections present with exact H2 headers from CONTEXT.md
    - Transform Decisions table covers 4+ transform categories with rationale + source artifact per row
    - Explicitly Skipped Items table has exactly the 5 required categories, each with a one-line rationale
    - Operator Action Items section documents MEM0_API_KEY config + live ingestion + SOUL routing + skill invocation smoke-tests
    - Phase-by-Phase Summary cites each phase's verification status + human-needed items
    - Length in 200-400 line range (no stub, no bloat)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 37 verification report</name>
  <files>.planning/phases/37-validation-migration-report/37-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md (Task 1)
    - .planning/milestones/v7.0-MIGRATION-REPORT.md (Task 2)
    - .planning/phases/34-skills-migration-coding-agent-tmux-agents/34-VERIFICATION.md (format reference)
    - .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-VERIFICATION.md (format reference for human_needed status)
  </read_first>

  <behavior>
    - Phase 37 verification report follows v6.0 audit-style format (frontmatter + Goal Achievement + Requirements Coverage + Gaps Summary)
    - Status = `human_needed` (structural verification passes; runtime smoke-tests deferred to operator per scoped boundary)
    - All 3 requirements (VALIDATE-01..03) have explicit status + evidence citation
    - Must-haves (truths) all structurally verified; runtime-dependent truths marked PARTIAL with explicit operator-handoff note
  </behavior>

  <action>
    Create `.planning/phases/37-validation-migration-report/37-VERIFICATION.md` with this structure (mirror 34-VERIFICATION.md and 36-VERIFICATION.md frontmatter style):

    **Frontmatter:**
    ```
    ---
    phase: 37-validation-migration-report
    verified: 2026-06-25T<now>Z
    status: human_needed
    score: 3/3 must-haves structurally verified; runtime smoke-tests deferred to operator
    overrides_applied: 0
    human_verification:
      - test: "Run the 4 Operator Action Items documented in v7.0-MIGRATION-REPORT.md (MEM0_API_KEY config + live mem0 ingestion + SOUL routing observation + skill invocation smoke-test)"
        expected: "Each item produces its documented expected output (ingestion count=124, spot-check 5 query blocks non-empty, SOUL routing split observable, skill discovery + delegation form fires)"
        why_human: "Requires operator-environment resources: live hermes conversation, tmux + agent CLIs, MEM0_API_KEY cloud credentials. Structural preconditions verified; runtime confirmation is operator scope per 37-CONTEXT.md scoped boundary."
    ---
    ```

    **Required sections:**

    1. `# Phase 37: Validation & Migration Report Verification Report` — phase goal (from ROADMAP) + verified date + status + re-verification note.

    2. `## Goal Achievement` → `### Observable Truths` — markdown table with columns **`| # | Truth | Status | Evidence |`**. Three rows (from must_haves truths in this PLAN):
       - Truth 1 (skills benchmark): status = ⚠️ STRUCTURE-VERIFIED / RUNTIME-HUMAN. Evidence: 37-01-BENCHMARK-RESULTS.md VALIDATE-01 section; both skills YAML-parse + 4 launch/operation forms present + discoverable. Runtime delegation chain deferred.
       - Truth 2 (SOUL routing): status = ⚠️ STRUCTURE-VERIFIED / RUNTIME-HUMAN. Evidence: 37-01-BENCHMARK-RESULTS.md VALIDATE-02 section; all 4 routing categories present + source-tagged + non-destructive contract holds. Live routing observation deferred.
       - Truth 3 (migration report exists): status = ✓ VERIFIED. Evidence: `[ -f .planning/milestones/v7.0-MIGRATION-REPORT.md ]` passes; all 6 required H2 sections present; all 5 Explicitly Skipped categories documented with rationale.

    3. `### Required Artifacts` — table with columns **`| Artifact | Expected | Status | Details |`**. Three rows: 37-01-BENCHMARK-RESULTS.md, v7.0-MIGRATION-REPORT.md, 37-VERIFICATION.md (this file).

    4. `### Requirements Coverage` — table with columns **`| Requirement | Source | Description | Status | Evidence |`**. Three rows:
       - VALIDATE-01 | 37-01 Task 1 | Each migrated skill passes ≥1 benchmark prompt end-to-end | STRUCTURAL-SATISFIED / RUNTIME-HUMAN | 37-01-BENCHMARK-RESULTS.md §VALIDATE-01; both skills YAML-valid + 4 forms documented; live tmux spawn deferred per CONTEXT.md scoped boundary
       - VALIDATE-02 | 37-01 Task 1 | Enhanced SOUL.md produces expected routing on 3+ prompts | STRUCTURAL-SATISFIED / RUNTIME-HUMAN | 37-01-BENCHMARK-RESULTS.md §VALIDATE-02; all 4 categories present + source-tagged; live routing observation deferred per CONTEXT.md scoped boundary
       - VALIDATE-03 | 37-01 Task 2 | v7.0-MIGRATION-REPORT.md exists with transform decisions + skipped items + rationale | SATISFIED | File exists at canonical path; all 6 H2 sections; all 5 Explicitly Skipped categories with one-line rationale; MEM0_API_KEY operator action documented

    5. `### Anti-Patterns Found` — table; expect no genuine debt markers (TBD/FIXME/XXX/HACK) in the 2 produced artifacts. If any `XXX` matches appear, investigate (could be mktemp-template false positives like in Phase 34).

    6. `### Human Verification Required` — enumerate the operator smoke-tests (cross-reference v7.0-MIGRATION-REPORT.md Operator Action Items section). Each item: test + expected + why_human. 4 items (MEM0_API_KEY config + live ingestion + SOUL routing observation + skill invocation).

    7. `### Gaps Summary` — state explicitly: NO STRUCTURAL GAPS. All 3 requirements structurally satisfied. The runtime-deferred items (VALIDATE-01 + VALIDATE-02 live behavior) are SCOPED BOUNDARIES per 37-CONTEXT.md decisions, NOT gaps — the plan acknowledges operator-environment constraints honestly and hands off with documented commands. Status `human_needed` follows the verifier decision tree (human verification items exist, even with structural must-haves passing).
  </action>

  <verify>
    <automated>
      test -f .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      grep -q "^# Phase 37: Validation" .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      grep -q "status: human_needed"   .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      grep -q "VALIDATE-01"            .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      grep -q "VALIDATE-02"            .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      grep -q "VALIDATE-03"            .planning/phases/37-validation-migration-report/37-VERIFICATION.md
      grep -q "SCOPED BOUNDARY"        .planning/phases/37-validation-migration-report/37-VERIFICATION.md
    </automated>
  </verify>

  <done>
    - 37-VERIFICATION.md exists with v6.0-audit-style structure
    - Status = human_needed (honest — structural passes, runtime deferred)
    - All 3 VALIDATE requirements covered with status + evidence
    - Runtime deferral explicitly framed as scoped boundary (not gap)
    - Operator smoke-test items enumerated for cross-reference with v7.0-MIGRATION-REPORT.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-home → repo | Files at `~/.hermes/SOUL.md` cross from operator-controlled home directory into the repo-tracked migration report via read-only grep validation. No writes flow back. |
| planning-doc → operator-action | The migration report's Operator Action Items section prescribes commands the operator will run; ambiguous or wrong commands could mislead. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Information Disclosure | BENCHMARK-RESULTS.md leaks operator-home file contents | accept | Only grep hit counts + line numbers recorded; no full-file dumps. Operator-state files (`~/.hermes/SOUL.md`) stay operator-side. |
| T-37-02 | Tampering | Migration report cites a skipped-item rationale that contradicts REQUIREMENTS.md | mitigate | Task 2 Explicitly Skipped Items table is cross-checked against REQUIREMENTS.md Out of Scope + STATE.md Deferred Items tables at read_first stage; rationales copy source language verbatim. |
| T-37-03 | Denial of Service | Report bloats beyond 400 lines, burying load-bearing skipped-item rationales | mitigate | Task 2 length target 200-400 lines (per CONTEXT.md Claude's Discretion); verify step includes lower-bound 200 line check. |
| T-37-04 | Elevation of Privilege | Runtime smoke-test commands in Operator Action Items could be unsafe to run | accept | Commands are read-only or operator-initiated ingestion (no destructive ops); consistent with 36-01-INGESTION-NOTE.md already shipped. |
| T-37-SC | Tampering | (N/A — Phase 37 has zero package-manager installs; pure documentation) | accept | No `npm`/`pip`/`cargo` install tasks in this plan. |
</threat_model>

<verification>
End-to-end phase checks (cross-task):

```bash
# All 3 artifacts exist
test -f .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
test -f .planning/milestones/v7.0-MIGRATION-REPORT.md
test -f .planning/phases/37-validation-migration-report/37-VERIFICATION.md

# Canonical artifact (SC#3) — v7.0-MIGRATION-REPORT.md has all required content
grep -c "Transform Decisions"       .planning/milestones/v7.0-MIGRATION-REPORT.md   # ≥ 1
grep -c "Explicitly Skipped"        .planning/milestones/v7.0-MIGRATION-REPORT.md   # ≥ 1
grep -E "feishu-|acp-router|models\.json|sessions|multi-profile" \
                                      .planning/milestones/v7.0-MIGRATION-REPORT.md   # all 5 present
grep -c "MEM0_API_KEY"              .planning/milestones/v7.0-MIGRATION-REPORT.md   # ≥ 1

# SC#1 + SC#2 structural preconditions documented
grep -q "VALIDATE-01" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
grep -q "VALIDATE-02" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md
grep -q "OPERATOR-SMOKE-TEST" .planning/phases/37-validation-migration-report/37-01-BENCHMARK-RESULTS.md

# Honest scoped-boundary acknowledgment (NOT framed as gap)
grep -q "SCOPED BOUNDARY\|scoped boundary\|scoped-boundary" .planning/phases/37-validation-migration-report/37-VERIFICATION.md
```
</verification>

<success_criteria>
**SC #1 (skills benchmark — structural):** Both migrated skills have YAML-valid frontmatter + documented invocation forms (4 launch blocks for coding-agent; 4 H3 operations for tmux-agents) + are discoverable via skill directory walk. Runtime end-to-end delegation chain (tmux spawn + agent CLI fire) is documented as operator smoke-test per scoped boundary.

**SC #2 (SOUL.md routing — structural):** All 4 routing categories (immediate / cognitive / expert-management / default) present in ~/.hermes/SOUL.md with source + date tagging; non-destructive contract verified (original Hermes identity preserved). Live routing observation documented as operator smoke-test per scoped boundary.

**SC #3 (migration report — fully achieved):** `.planning/milestones/v7.0-MIGRATION-REPORT.md` exists with all 6 required sections (Executive Summary / Transform Decisions / Explicitly Skipped Items / Operator Action Items / Phase-by-Phase Summary / Forward-Looking Notes); all 5 Explicitly Skipped categories (feishu-* / acp-router / models.json / sessions / multi-profile) documented with one-line rationale; MEM0_API_KEY + live ingestion + smoke-test commands present in Operator Action Items.
</success_criteria>

<output>
Create `.planning/phases/37-validation-migration-report/37-01-SUMMARY.md` when done. This completes Phase 37 — the FINAL phase of v7.0 milestone. Operator next step: run the 4 Operator Action Items in v7.0-MIGRATION-REPORT.md to confirm runtime behavior, then begin v8.0 milestone planning.
</output>

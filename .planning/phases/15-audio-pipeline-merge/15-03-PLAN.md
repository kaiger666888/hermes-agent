---
phase: 15-audio-pipeline-merge
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
  - skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
  - skills/movie-experts/_shared/project-corpus/animation-disney-system.md
  - skills/movie-experts/_shared/project-corpus/editing-sound-post.md
  - skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md
autonomous: true
requirements: [MERGE-02]
user_setup: []

must_haves:
  truths:
    - "README.md inventory replaces the 6 separate audio expert rows (voicer, lip_sync, composer, foley, mixer, spatial_audio) with a single audio_pipeline row that names all 6 sub-steps"
    - "README.md corpus tree (File Layout section) lists audio_pipeline/ with its 6 sub-folder references/ structure"
    - "README.md ASCII production DAG diagram shows audio_pipeline in place of the separate 6 audio boxes (the audio stages collapse into one node)"
    - "README.md Bridge/Bottleneck/Audio-visual lock narrative notes reference audio_pipeline where they previously referenced individual audio experts"
    - "README.md corpus→expert inventory table (line 39) consolidates `editor, foley, composer` → `editor, audio_pipeline` for editing-sound-post.md"
    - "_shared/glossary.md prose references to audio expert_ids are updated to audio_pipeline (with sub-step annotation where relevant)"
    - "_shared/glossary.md has a new entry for `audio_pipeline` at the appropriate alphabetical position"
    - "_shared/RAG-INVOCATION-PATTERN.md table rows attributing models to voicer / composer / foley are updated to audio_pipeline (with sub-step annotation)"
    - "_shared/project-corpus/animation-disney-system.md expert_id references in stage breakdowns updated to audio_pipeline (with sub-step annotation)"
    - "_shared/project-corpus/editing-sound-post.md expert_id references updated to audio_pipeline; the 4 See Also markdown links (foley, composer, mixer, spatial_audio) all updated"
    - "_shared/project-corpus/production-chinese-and-low-budget.md Post = editor + composer + mixer line updated to editor + audio_pipeline"
    - "README.md footer running expert count decremented by 5 (22 → 17)"
    - "No stranded expert_id references to {voicer, lip_sync, composer, foley, mixer, spatial_audio} remain in README.md or _shared/ outside intentional alias/historical documentation contexts"
  artifacts:
    - path: "skills/movie-experts/README.md"
      provides: "Inventory + corpus tree + DAG diagram reflecting audio_pipeline merge"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "Glossary with updated expert ID references + new audio_pipeline entry"
    - path: "skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md"
      provides: "RAG pattern table with audio_pipeline attribution"
    - path: "skills/movie-experts/_shared/project-corpus/animation-disney-system.md"
      provides: "Disney system corpus with updated expert references"
    - path: "skills/movie-experts/_shared/project-corpus/editing-sound-post.md"
      provides: "Editing corpus with updated expert references + See Also markdown links"
    - path: "skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md"
      provides: "Production corpus with Post = editor + audio_pipeline line"
  key_links:
    - from: "skills/movie-experts/README.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "inventory markdown link"
      pattern: "\\(\\./audio_pipeline/SKILL\\.md\\)"
    - from: "skills/movie-experts/_shared/glossary.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "expert_id reference"
      pattern: "audio_pipeline"
    - from: "skills/movie-experts/_shared/project-corpus/editing-sound-post.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "See Also markdown link"
      pattern: "\\(\\.\\./\\.\\./audio_pipeline/SKILL\\.md\\)"
---

<objective>
Close out Phase 15 by updating cross-cutting documentation artifacts (README.md inventory + corpus→expert table + corpus tree + ASCII DAG diagram + narrative notes, _shared/glossary.md, _shared/RAG-INVOCATION-PATTERN.md, _shared/project-corpus/{animation-disney-system,editing-sound-post,production-chinese-and-low-budget}.md) to reflect the audio_pipeline merge. These are the cross-cutting docs that depend on both 15-01 (new expert exists) and 15-02 (consumer edges synced) being complete.

Purpose: Fulfill ROADMAP §15 success criterion #4's cross-cutting consumer coverage (README, _shared/, project-corpus all reflect the new topology). Without this, downstream phases (16, 17, 18) reading README would still see 6 separate audio experts instead of one merged audio_pipeline.

Output:
- README.md: inventory row consolidation (6 rows → 1 row), corpus→expert table consolidation (line 39), corpus tree update (6 rows → 1 row + 6 redirect stubs), ASCII DAG diagram update (6 audio boxes → 1 audio_pipeline box), narrative references update (Bridge/Bottleneck/Audio-visual lock), footer count decrement (22 → 17)
- _shared/glossary.md: 4 prose references updated + 1 new audio_pipeline entry
- _shared/RAG-INVOCATION-PATTERN.md: 3 table rows (foley, composer, voicer) updated with sub-step annotation
- _shared/project-corpus/animation-disney-system.md: 2 stage references (voicer + composer/mixer) updated
- _shared/project-corpus/editing-sound-post.md: expert_id references + 4 See Also markdown links (foley, composer, mixer, spatial_audio) updated
- _shared/project-corpus/production-chinese-and-low-budget.md: Post = editor + composer + mixer line updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-audio-pipeline-merge/15-CONTEXT.md
@.planning/phases/15-audio-pipeline-merge/15-01-PLAN.md
@.planning/phases/15-audio-pipeline-merge/15-02-PLAN.md
@skills/movie-experts/audio_pipeline/SKILL.md
@skills/movie-experts/README.md
@skills/movie-experts/_shared/glossary.md
@skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
@skills/movie-experts/_shared/project-corpus/animation-disney-system.md
@skills/movie-experts/_shared/project-corpus/editing-sound-post.md
@skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md

# Phase 14's 14-03-PLAN + SUMMARY are the close-out precedent.
@.planning/phases/14-visual-executor-merge/14-03-PLAN.md
@.planning/phases/14-visual-executor-merge/14-03-SUMMARY.md

# NOTE: skills-mapping.yaml has NO sign_off field for n_to_one_merged entries
# (sign_off is only for one_to_one_renamed per skills-mapping.yaml convention — confirmed by Phase 14 close-out).
# So there is NO skills-mapping.yaml update in this plan. The audio_pipeline mapping entry at lines 91-95 is
# already canonical; no metadata change required.

<interfaces>
<!-- README.md edits required -->

1. **Inventory table (Phase 0 baselined section, lines 71-75 + line 92):** 5 contiguous rows for composer (71), foley (72), spatial_audio (73), mixer (74), voicer (75) PLUS the lip_sync row at line 92 (Phase 7 section). All 6 rows are consolidated into a single `[audio_pipeline]` row. Place the new row at the position where the contiguous block was (around line 71) OR at a sensible alphabetical position; note the lip_sync row was in the Phase 7 section (line 92) — it should also collapse into the audio_pipeline row. Final form:
   ```
   | [`audio_pipeline`](./audio_pipeline/SKILL.md) | 音频管线专家 | Unified 6-sub-step audio production: voicer (TTS) + lip_sync (audio-driven sync) + composer (music/score) + foley (SFX) + mixer (mastering) + spatial_audio (3D encoding). Merged Phase 15 per v2.0 PRFP DAG §4.9. | 2-4 (Phase 5 light × 5 + Phase 7A-2 lip_sync benchmark) |
   ```
   Delete the 5 old rows at lines 71-75 AND the lip_sync row at line 92.

2. **Corpus → expert inventory table (line 39):**
   BEFORE: `| \`editing-sound-post.md\` | 剪辑之道 / 魅力剪辑 / 音效圣经 / 视听 / 王竞六讲 | editor, foley, composer, documentary_maker |`
   AFTER:  `| \`editing-sound-post.md\` | 剪辑之道 / 魅力剪辑 / 音效圣经 / 视听 / 王竞六讲 | editor, audio_pipeline, documentary_maker |` (foley + composer collapse to audio_pipeline)

3. **character_designer row description (line ~94, may be elsewhere):**
   BEFORE: "Decoupled from visual_executor (visual_executor generates images, character_designer defines identity contract)" — actually this references visual_executor, not audio. Check whether any character_designer row text references lip_sync and update if so.

4. **ASCII DAG diagram (lines 177, 188-189, 194):**
   - Line 177: `│   lip_sync    │  │  voicer       │` → these are two boxes in the audio stages. They should collapse into the audio_pipeline box.
   - Lines 188-189: `│  composer / foley│` + `│  / spatial_audio │` → another audio stage box pair.
   - Line 194: `│     mixer       │` → another audio box.
   - All 5 audio stage boxes (lip_sync, voicer, composer, foley, spatial_audio, mixer — actually 6 IDs across the boxes) collapse into ONE `audio_pipeline` box. If the label `│ audio_pipeline │` is too long for the column width, use a two-line box following the Phase 13 multi-line compliance_gate box precedent OR use the abbreviated label `│ audio_pipeline │` with sub-step annotation beneath: `│ (6 audio sub-steps) │`.

5. **Narrative notes (lines 208, 210, 211):**
   - Line 208: "character_designer emits CharacterBible 2.0 consumed by visual_executor / lip_sync / continuity_auditor" → "character_designer emits CharacterBible 2.0 consumed by visual_executor / audio_pipeline (lip_sync sub-step) / continuity_auditor"
   - Line 210: "**Audio-visual lock:** `voicer` produces audio → `lip_sync` aligns to footage (decoupled, composable)" → "**Audio-visual lock:** `audio_pipeline` (voicer sub-step) produces audio → `audio_pipeline` (lip_sync sub-step) aligns to footage (now intra-expert handoff; still decoupled, composable)"
   - Line 211: "**Bottleneck nodes:** `screenplay` (after style) / `visual_executor` (after intent) / `lip_sync` (after audio + footage) / `mixer` (after all audio)" → "**Bottleneck nodes:** `screenplay` (after style) / `visual_executor` (after intent) / `audio_pipeline` (after all audio + footage) — single node now subsumes lip_sync + mixer bottleneck roles"

6. **File Layout corpus tree (lines 372, 376, 378, 383, 385, 387):**
   BEFORE: 6 separate rows for composer/, foley/, mixer/, spatial_audio/, voicer/, lip_sync/ directories.
   AFTER:
   - DELETE the 6 old rows
   - ADD a new row for audio_pipeline/ (alphabetical placement near where composer/ was — typically at the top of the audio block):
     ```
     ├── audio_pipeline/    SKILL.md + references/{voicer/{cn-tts-model-matrix,character-voice-consistency,tts-emotion-prosody-control},lip_sync/{sync-quality-metrics,latentsync-deployment,audio-video-input-spec,identity-preservation},composer/{musicgen-workflow,chion-audio-vision,bgm-and-song-creation},foley/{stable-audio-open,sound-effect-taxonomy,sound-effects-prompt-engineering},mixer/{mixing-secrets-small-studio,lufs-standards},spatial_audio/{dolby-atmos-workflow,immersive-sound-design}}.md + {voicer,lip_sync,composer,foley,mixer,spatial_audio}/LICENSE.md + GAP-REPORT.md (Phase 5 + Phase 7A-2 + Phase 15 merge)
     ```
   - PRESERVE the old 6 rows but annotate them as redirect stubs (consistent with Phase 14's treatment of drawer/ + animator/ in the corpus tree). Append " (Phase 15 redirect stub)" to each:
     - `├── composer/           SKILL.md (Phase 15 redirect stub — merged_into audio_pipeline)`
     - `├── foley/              SKILL.md (Phase 15 redirect stub — merged_into audio_pipeline)`
     - `├── mixer/              SKILL.md (Phase 15 redirect stub — merged_into audio_pipeline)`
     - `├── spatial_audio/      SKILL.md (Phase 15 redirect stub — folded_into audio_pipeline)`
     - `├── voicer/             SKILL.md (Phase 15 redirect stub — merged_into audio_pipeline)`
     - `├── lip_sync/           SKILL.md (Phase 15 redirect stub — merged_into audio_pipeline)`

7. **`_eval/prompts/` inventory (line 406):**
   - `lip_sync_demo.yaml` stays (the prompts file is referenced by the runner; Phase 13 + 14 precedent: prompt filenames are NOT renamed). Leave as-is. No change here.

8. **Phase 7 additions summary section (lines 459, 468, 469, 470):**
   - Line 459: `- \`lip_sync\` — audio-driven lip sync (LRS2/LRS3 international benchmark, LSE/LSE-C objective metrics)` → `- \`audio_pipeline\` (lip_sync sub-step) — audio-driven lip sync (LRS2/LRS3 international benchmark, LSE/LSE-C objective metrics)` (note that lip_sync benchmark is preserved as a sub-step capability)
   - Line 468: `- \`composer/references/bgm-and-song-creation.md\` (NEW) — from kais-bgm + kais-song-agent` → `- \`audio_pipeline/references/composer/bgm-and-song-creation.md\` (NEW) — from kais-bgm + kais-song-agent` (path now points into the merged expert)
   - Line 469: `- \`foley/references/sound-effects-prompt-engineering.md\` (NEW) — from kais-sound-effects-agent` → `- \`audio_pipeline/references/foley/sound-effects-prompt-engineering.md\` (NEW) — from kais-sound-effects-agent`
   - Line 470: `- \`voicer/references/tts-emotion-prosody-control.md\` (NEW) — from kais-TTS-agent` → `- \`audio_pipeline/references/voicer/tts-emotion-prosody-control.md\` (NEW) — from kais-TTS-agent`

9. **Footer running expert count (line 448):**
   BEFORE: `*v2 = 22 experts (14 original + 4 Phase 1-5 + 5 Phase 7 − 1 Phase 14 visual_executor merge), all RAG-aware, all phantom refs stripped.*`
   AFTER:  `*v2 = 17 experts (14 original + 4 Phase 1-5 + 5 Phase 7 − 1 Phase 14 visual_executor merge − 5 Phase 15 audio_pipeline merge), all RAG-aware, all phantom refs stripped.*`
   (6 audio experts → 1 audio_pipeline = net delta of −5; 22 − 5 = 17.)

<!-- _shared/glossary.md edits required -->

- Line 13: "When one expert emits a term (e.g., screenplay emits `钩子`), downstream experts (editor, composer) MUST consume the same CN token" → replace `composer` with `audio_pipeline (composer sub-step)`
- Line 73: "composer aligns musical sting to 击中点." → "audio_pipeline (composer sub-step) aligns musical sting to 击中点."
- Line 164: "(visual_executor / lip_sync / continuity_auditor)的 ground truth" → "(visual_executor / audio_pipeline (lip_sync sub-step) / continuity_auditor)的 ground truth"
- Line 171: "lip_sync prefers front + 3Q for identity reference." → "audio_pipeline (lip_sync sub-step) prefers front + 3Q for identity reference."
- Line 201: "lip_sync expert's domain. Decoupled from voicer (audio synthesis) — voicer produces WAV, lip_sync aligns WAV to footage." → "audio_pipeline (lip_sync sub-step) domain. Intra-expert handoff from voicer sub-step (audio synthesis) — voicer sub-step produces WAV, lip_sync sub-step aligns WAV to footage."
- ADD a new glossary entry for `audio_pipeline` at the appropriate alphabetical position (near the A section or after the `audio`-related entries) with EN↔CN mapping + brief context noting the Phase 15 merge from 5 canonical audio experts + spatial_audio fold (disposition D-1).

<!-- _shared/RAG-INVOCATION-PATTERN.md edits required -->

- Line 78: `| <audio_gen_primary> | Stable Audio Open 1.0 (foley/SFX default) | foley |` → `| <audio_gen_primary> | Stable Audio Open 1.0 (foley/SFX default) | audio_pipeline (foley sub-step) |`
- Line 79: `| <music_gen_primary> | MusicGen-large (open-source music default) | composer |` → `| <music_gen_primary> | MusicGen-large (open-source music default) | audio_pipeline (composer sub-step) |`
- Line 80: `| <tts_primary> | MiniMax / ElevenLabs (Hermes-native) or CosyVoice 3.0 (user-deployed) | voicer |` → `| <tts_primary> | MiniMax / ElevenLabs (Hermes-native) or CosyVoice 3.0 (user-deployed) | audio_pipeline (voicer sub-step) |`

<!-- _shared/project-corpus/animation-disney-system.md edits required -->

- Line 274: `Stage 4-5: voice via voicer + leica reel (1 day)` → `Stage 4-5: voice via audio_pipeline (voicer sub-step) + leica reel (1 day)`
- Line 277: `Stage 12: composer + mixer (1 day)` → `Stage 12: audio_pipeline (composer + mixer sub-steps) (1 day)`

<!-- _shared/project-corpus/editing-sound-post.md edits required -->

- Line 21: `- \`composer\` / \`foley\` / \`mixer\` experts: Chion's audio-vision framework` → `- \`audio_pipeline\` expert (composer / foley / mixer sub-steps): Chion's audio-vision framework`
- Line 273: `- Sync hard effects + foley` → PRESERVE (this is "foley" as a craft term for sound effects, not the expert_id — verify with surrounding context; if it's a craft noun preserve, if it's an expert_id reference change to `audio_pipeline (foley sub-step)`)
- Line 441: `### Sound Bible for \`foley\` / \`mixer\`` → `### Sound Bible for \`audio_pipeline\` (foley / mixer sub-steps)`
- Line 446: `### Chion for \`composer\` / \`spatial_audio\`` → `### Chion for \`audio_pipeline\` (composer / spatial_audio sub-steps)`
- Line 464: `- [\`../../foley/SKILL.md\`](../../foley/SKILL.md)` → `- [\`audio_pipeline\`](../../audio_pipeline/SKILL.md) (foley sub-step)`
- Line 465: `- [\`../../composer/SKILL.md\`](../../composer/SKILL.md)` → `- [\`audio_pipeline\`](../../audio_pipeline/SKILL.md) (composer sub-step)`
- Line 466: `- [\`../../mixer/SKILL.md\`](../../mixer/SKILL.md)` → `- [\`audio_pipeline\`](../../audio_pipeline/SKILL.md) (mixer sub-step)`
- Line 467: `- [\`../../spatial_audio/SKILL.md\`](../../spatial_audio/SKILL.md)` → `- [\`audio_pipeline\`](../../audio_pipeline/SKILL.md) (spatial_audio sub-step)`

<!-- _shared/project-corpus/production-chinese-and-low-budget.md edits required -->

- Line 380: `5. Post = editor + composer + mixer` → `5. Post = editor + audio_pipeline (composer + mixer sub-steps)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md inventory + corpus→expert table + corpus tree + ASCII DAG + narrative notes + Phase 7 summary + footer count</name>
  <files>
    skills/movie-experts/README.md
  </files>
  <read_first>
    - skills/movie-experts/README.md (full file — identify exact line numbers via grep before editing; especially lines 39, 71-75, 92, 177, 188-189, 194, 208, 210, 211, 372, 376, 378, 383, 385, 387, 406, 448, 459, 468-470)
    - skills/movie-experts/audio_pipeline/SKILL.md (merged expert — verify sub_steps declaration for inventory row description)
    - .planning/phases/14-visual-executor-merge/14-03-SUMMARY.md (Phase 14 precedent for how README corpus tree handled drawer/ + animator/ redirect stub directories — follow same pattern for the 6 audio redirect stubs)
    - .planning/phases/15-audio-pipeline-merge/15-CONTEXT.md (consumer coverage rules + spatial_audio disposition D-1)
  </read_first>
  <behavior>
    - Inventory table (Phase 0 baselined section): the 5 contiguous rows for composer, foley, spatial_audio, mixer, voicer (lines 71-75) PLUS the lip_sync row in Phase 7 section (line 92) are consolidated into a single `[audio_pipeline]` row at a sensible position (typically where composer/ was, around line 71). The new row mentions all 6 sub-steps in the description column.
    - Corpus → expert inventory table (line 39): the `editor, foley, composer, documentary_maker` cell for editing-sound-post.md becomes `editor, audio_pipeline, documentary_maker`
    - ASCII DAG diagram (lines 177, 188-189, 194): the 5 audio stage boxes (across the diagram) are consolidated into ONE `audio_pipeline` box. If the label is too long for column width, use a two-line box following the Phase 13 multi-line compliance_gate precedent OR a single-line box with abbreviated label + sub-step annotation beneath.
    - Narrative notes (Bridge/Audio-visual lock/Bottleneck lines 208, 210, 211) reference `audio_pipeline` (with sub-step annotation where the specific sub-step matters for the edge semantics).
    - File Layout corpus tree: a new row for `audio_pipeline/` is added with the 6-sub-folder references/ structure; the old 6 rows for composer/, foley/, mixer/, spatial_audio/, voicer/, lip_sync/ are PRESERVED but annotated with "(Phase 15 redirect stub — merged_into audio_pipeline)" or "(Phase 15 redirect stub — folded_into audio_pipeline)" for spatial_audio. Follow Phase 14's pattern of leaving redirect-stub directories in the corpus tree.
    - Phase 7 additions summary section (lines 459, 468-470): the lip_sync row gets `audio_pipeline (lip_sync sub-step)` treatment; the 3 path references to `composer/references/`, `foley/references/`, `voicer/references/` get updated to `audio_pipeline/references/{sub-step}/...`.
    - Footer running expert count is decremented by 5 (22 → 17).
    - The `_eval/prompts/lip_sync_demo.yaml` row (line ~406) is left as-is (Phase 13 + 14 precedent: prompt filenames are NOT renamed because the runner references them).
  </behavior>
  <action>
    1. **Edit inventory rows (lines 71-75 + line 92):** Use Edit tool to replace the 5 contiguous audio rows (composer, foley, spatial_audio, mixer, voicer) with the single `[audio_pipeline]` row shown in `<interfaces>` §1. Place the new row where composer/ was (around line 71). Then DELETE the lip_sync row at line 92 (it's in the Phase 7 section — fold its benchmark capability description into the new audio_pipeline row).
    2. **Edit corpus → expert inventory table (line 39):** Use Edit to replace `editor, foley, composer, documentary_maker` with `editor, audio_pipeline, documentary_maker` in the editing-sound-post.md row.
    3. **Edit ASCII DAG diagram (lines 177, 188-189, 194):** Replace the 5 audio stage boxes (lip_sync + voicer pair at line 177, composer/foley/spatial_audio pair at lines 188-189, mixer at line 194) with a single `audio_pipeline` box. If a multi-line box is needed for column alignment, use the Phase 13 compliance_gate two-line box precedent.
    4. **Edit narrative notes (lines 208, 210, 211):** Use targeted Edit replacements for the three narrative lines per `<interfaces>` §5. Update each audio ID to `audio_pipeline` (with sub-step annotation in parentheses where the specific sub-step matters).
    5. **Edit File Layout corpus tree (lines 372, 376, 378, 383, 385, 387):** Use Edit to:
       a. Add a new row for `audio_pipeline/` with the 6-sub-folder references/ structure (place it where composer/ was — alphabetically near the top of the audio block).
       b. Append "(Phase 15 redirect stub — merged_into audio_pipeline)" to the existing composer/, foley/, mixer/, voicer/, lip_sync/ rows.
       c. Append "(Phase 15 redirect stub — folded_into audio_pipeline)" to the existing spatial_audio/ row.
    6. **Edit Phase 7 additions summary section (lines 459, 468-470):** Update the lip_sync row description to `audio_pipeline (lip_sync sub-step)`, and the 3 ref paths to `audio_pipeline/references/{composer,foley,voicer}/...`.
    7. **Edit footer running expert count (line 448):** Update from "v2 = 22 experts" to "v2 = 17 experts" with the corresponding arithmetic annotation per `<interfaces>` §9.
  </action>
  <verify>
    <automated>
    echo "--- Inventory: single audio_pipeline row, no separate audio expert rows ---" && \
    grep -q "| \[\`audio_pipeline\`\](./audio_pipeline/SKILL.md)" skills/movie-experts/README.md && \
    for audio in voicer lip_sync composer foley mixer spatial_audio; do
      ! grep -qE "^\| \[\`$audio\`\]\(./$audio/SKILL\.md\)\s+\|" skills/movie-experts/README.md || { echo "STRAY inventory row for $audio"; exit 1; }
    done && \
    echo "--- Corpus → expert inventory: editing-sound-post row has audio_pipeline ---" && \
    grep -q "editing-sound-post.md.*audio_pipeline" skills/movie-experts/README.md && \
    echo "--- ASCII DAG: audio_pipeline box present ---" && \
    grep -q "audio_pipeline" skills/movie-experts/README.md && \
    echo "--- Corpus tree: audio_pipeline/ row added ---" && \
    grep -qE "├── audio_pipeline/" skills/movie-experts/README.md && \
    echo "--- Corpus tree: old 6 audio rows annotated as redirect stubs ---" && \
    grep -qE "(Phase 15 redirect stub — merged_into audio_pipeline)" skills/movie-experts/README.md && \
    grep -qE "(Phase 15 redirect stub — folded_into audio_pipeline)" skills/movie-experts/README.md && \
    echo "--- Phase 7 additions section paths updated ---" && \
    grep -q "audio_pipeline/references/composer/bgm-and-song-creation.md" skills/movie-experts/README.md && \
    grep -q "audio_pipeline/references/foley/sound-effects-prompt-engineering.md" skills/movie-experts/README.md && \
    grep -q "audio_pipeline/references/voicer/tts-emotion-prosody-control.md" skills/movie-experts/README.md && \
    echo "--- _eval/prompts/lip_sync_demo.yaml row PRESERVED (not renamed) ---" && \
    grep -q "lip_sync_demo.yaml" skills/movie-experts/README.md && \
    echo "--- Footer count decremented to 17 ---" && \
    grep -q "v2 = 17 experts" skills/movie-experts/README.md && \
    echo "TASK-1 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - Inventory table has ONE audio_pipeline row instead of 6 separate audio expert rows (5 in Phase 0 section + lip_sync in Phase 7 section)
    - Corpus → expert inventory table line 39 references audio_pipeline for editing-sound-post.md
    - ASCII DAG diagram has a single audio_pipeline box (replacing the 5 audio stage boxes)
    - Bridge / Audio-visual lock / Bottleneck narrative lines reference audio_pipeline (with sub-step annotations)
    - File Layout corpus tree has a new audio_pipeline/ row with the 6-sub-folder references structure
    - Old 6 audio rows (composer/, foley/, mixer/, spatial_audio/, voicer/, lip_sync/) in corpus tree preserved with redirect stub annotation
    - spatial_audio/ row has "folded_into" annotation (distinct from the other 5 "merged_into")
    - Phase 7 additions section paths updated to audio_pipeline/references/{sub-step}/...
    - Footer expert count decremented by 5 (22 → 17)
    - _eval/prompts/lip_sync_demo.yaml row preserved (not renamed — Phase 13/14 precedent)
  </acceptance_criteria>
  <done>
    README.md inventory, corpus → expert table, corpus tree, DAG diagram, Phase 7 summary, and footer all reflect the audio_pipeline merge. A reader skimming README sees a single audio_pipeline expert where there used to be 6.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update _shared/ glossary + RAG-INVOCATION-PATTERN + project-corpus (3 files)</name>
  <files>
    skills/movie-experts/_shared/glossary.md,
    skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md,
    skills/movie-experts/_shared/project-corpus/animation-disney-system.md,
    skills/movie-experts/_shared/project-corpus/editing-sound-post.md,
    skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md
  </files>
  <read_first>
    - skills/movie-experts/_shared/glossary.md (full file — identify exact line numbers via grep before editing)
    - skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md (lines around 78-80)
    - skills/movie-experts/_shared/project-corpus/animation-disney-system.md (lines around 274, 277)
    - skills/movie-experts/_shared/project-corpus/editing-sound-post.md (lines around 21, 273, 441, 446, 464-467)
    - skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md (line around 380)
    - skills/movie-experts/audio_pipeline/SKILL.md (sub_steps structure for glossary entry context)
    - .planning/phases/14-visual-executor-merge/14-03-SUMMARY.md (Phase 14 precedent for glossary entry additions + project-corpus edits)
  </read_first>
  <behavior>
    - _shared/glossary.md: 5 prose references to audio expert_ids updated to audio_pipeline (with sub-step annotation where the JSON path or specific sub-step reference distinguishes voicer/lip_sync/composer/etc.); a new glossary entry for `audio_pipeline` added at the appropriate alphabetical position with EN↔CN mapping + Phase 15 merge context + spatial_audio fold (D-1) note
    - _shared/RAG-INVOCATION-PATTERN.md: 3 table rows (lines 78-80) updated from `foley` / `composer` / `voicer` → `audio_pipeline (foley sub-step)` / `audio_pipeline (composer sub-step)` / `audio_pipeline (voicer sub-step)`
    - _shared/project-corpus/animation-disney-system.md: 2 stage references (lines 274, 277) updated to `audio_pipeline (voicer sub-step)` and `audio_pipeline (composer + mixer sub-steps)`
    - _shared/project-corpus/editing-sound-post.md: 5 expert_id references (lines 21, 441, 446, 464, 465, 466, 467 — the line numbering may shift as edits accumulate; verify with grep before each edit) updated to `audio_pipeline (with sub-step annotation)`; line 273 ("Sync hard effects + foley") is PRESERVED as a craft noun (verify with surrounding context — no markdown link, not in a "See Also" section → preserve); the 4 See Also markdown links (lines 464-467) updated to `../../audio_pipeline/SKILL.md` with sub-step annotation in the link text
    - _shared/project-corpus/production-chinese-and-low-budget.md: line 380 updated from `Post = editor + composer + mixer` → `Post = editor + audio_pipeline (composer + mixer sub-steps)`
    - All edits use targeted Edit tool replacements, NOT blanket sed (to preserve English-noun usages and craft-term references)
  </behavior>
  <action>
    1. **_shared/glossary.md edits** (5 prose references + 1 new entry):
       a. Line 13: `(editor, composer)` → `(editor, audio_pipeline (composer sub-step))`
       b. Line 73: `composer aligns musical sting to 击中点.` → `audio_pipeline (composer sub-step) aligns musical sting to 击中点.`
       c. Line 164: `(visual_executor / lip_sync / continuity_auditor)的 ground truth` → `(visual_executor / audio_pipeline (lip_sync sub-step) / continuity_auditor)的 ground truth`
       d. Line 171: `lip_sync prefers front + 3Q for identity reference.` → `audio_pipeline (lip_sync sub-step) prefers front + 3Q for identity reference.`
       e. Line 201: `lip_sync expert's domain. Decoupled from voicer (audio synthesis) — voicer produces WAV, lip_sync aligns WAV to footage.` → `audio_pipeline (lip_sync sub-step) domain. Intra-expert handoff from voicer sub-step (audio synthesis) — voicer sub-step produces WAV, lip_sync sub-step aligns WAV to footage.`
       f. Add a new glossary entry for `audio_pipeline` near the A section (or after any existing audio-prefixed entries). Format:
          ```
          ### audio_pipeline

          **CN:** 音频管线专家 — Phase 15 merge of 5 canonical audio experts (voicer, lip_sync, composer, foley, mixer) + spatial_audio (folded per disposition D-1). Declares `sub_steps: [voicer, lip_sync, composer, foley, mixer, spatial_audio]`. Owns unified audio consistency context per Phase 7 §4.9 + PITFALLS §2.6. spatial_audio folded (not deprecated) because spatial rendering is fundamentally a mixer/mastering concern (Atmos, HRTF, binaural). Backward-compat aliases `[voicer, lip_sync, composer, foley, mixer, spatial_audio]` preserved per FOUND-08.

          **EN:** Audio Pipeline Expert — Phase 15 merge of 6 predecessors per v2.0 PRFP DAG §4.9. Unified sub-steps handle TTS (MiniMax / ElevenLabs / Voxtral / Gemini / Edge / NeuTTS), audio-driven lip sync (LatentSync, LRS2/LRS3 benchmark), music generation (MusicGen-Large, Chion audio-vision), SFX (Stable Audio Open, 7D parametric), mixing (Senior Mixing Secrets, LUFS mastering), and spatial audio (Dolby Atmos, HRTF binaural).
          ```
    2. **_shared/RAG-INVOCATION-PATTERN.md edits** (3 table rows):
       a. Line 78: `| foley |` → `| audio_pipeline (foley sub-step) |`
       b. Line 79: `| composer |` → `| audio_pipeline (composer sub-step) |`
       c. Line 80: `| voicer |` → `| audio_pipeline (voicer sub-step) |`
    3. **_shared/project-corpus/animation-disney-system.md edits** (2 stage references):
       a. Line 274: `Stage 4-5: voice via voicer + leica reel` → `Stage 4-5: voice via audio_pipeline (voicer sub-step) + leica reel`
       b. Line 277: `Stage 12: composer + mixer` → `Stage 12: audio_pipeline (composer + mixer sub-steps)`
    4. **_shared/project-corpus/editing-sound-post.md edits** (5 expert_id references + 4 See Also markdown links):
       a. Line 21: `- \`composer\` / \`foley\` / \`mixer\` experts: Chion's audio-vision framework` → `- \`audio_pipeline\` expert (composer / foley / mixer sub-steps): Chion's audio-vision framework`
       b. Line 273: PRESERVE ("Sync hard effects + foley" — craft noun, no expert_id link context)
       c. Line 441: `### Sound Bible for \`foley\` / \`mixer\`` → `### Sound Bible for \`audio_pipeline\` (foley / mixer sub-steps)`
       d. Line 446: `### Chion for \`composer\` / \`spatial_audio\`` → `### Chion for \`audio_pipeline\` (composer / spatial_audio sub-steps)`
       e. Lines 464-467 (4 See Also markdown links): Each `- [\`../../{audio_id}/SKILL.md\`](../../{audio_id}/SKILL.md)` becomes `- [\`audio_pipeline\`](../../audio_pipeline/SKILL.md) ({audio_id} sub-step)`.
    5. **_shared/project-corpus/production-chinese-and-low-budget.md edit** (1 line):
       a. Line 380: `5. Post = editor + composer + mixer` → `5. Post = editor + audio_pipeline (composer + mixer sub-steps)`
  </action>
  <verify>
    <automated>
    echo "--- _shared/glossary.md: new audio_pipeline entry + updated prose ---" && \
    grep -q "^### audio_pipeline$" skills/movie-experts/_shared/glossary.md && \
    grep -q "audio_pipeline (composer sub-step) aligns musical sting" skills/movie-experts/_shared/glossary.md && \
    grep -q "audio_pipeline (lip_sync sub-step) prefers front + 3Q" skills/movie-experts/_shared/glossary.md && \
    grep -q "Intra-expert handoff from voicer sub-step" skills/movie-experts/_shared/glossary.md && \
    echo "--- _shared/RAG-INVOCATION-PATTERN.md: model attribution updated ---" && \
    grep -q "audio_pipeline (foley sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md && \
    grep -q "audio_pipeline (composer sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md && \
    grep -q "audio_pipeline (voicer sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md && \
    echo "--- _shared/project-corpus/animation-disney-system.md: stage refs updated ---" && \
    grep -q "voice via audio_pipeline (voicer sub-step)" skills/movie-experts/_shared/project-corpus/animation-disney-system.md && \
    grep -q "audio_pipeline (composer + mixer sub-steps)" skills/movie-experts/_shared/project-corpus/animation-disney-system.md && \
    echo "--- _shared/project-corpus/editing-sound-post.md: expert_id refs + See Also links updated; craft noun preserved ---" && \
    grep -q "\`audio_pipeline\` expert (composer / foley / mixer sub-steps)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "Sound Bible for \`audio_pipeline\` (foley / mixer sub-steps)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "Chion for \`audio_pipeline\` (composer / spatial_audio sub-steps)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "(\../../audio_pipeline/SKILL.md) (foley sub-step)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "(\../../audio_pipeline/SKILL.md) (composer sub-step)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "(\../../audio_pipeline/SKILL.md) (mixer sub-step)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "(\../../audio_pipeline/SKILL.md) (spatial_audio sub-step)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    grep -q "Sync hard effects + foley" skills/movie-experts/_shared/project-corpus/editing-sound-post.md && \
    echo "--- _shared/project-corpus/production-chinese-and-low-budget.md: Post line updated ---" && \
    grep -q "Post = editor + audio_pipeline (composer + mixer sub-steps)" skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md && \
    echo "--- Overall: no stray expert_id references to bare audio IDs in _shared/ outside intentional contexts ---" && \
    # Exclude: sub-step annotations (intentional), English nouns (e.g., "foley" as craft term for sound effects), redirect stubs
    STRAY=$(grep -rnE "\b(voicer|lip_sync|composer|foley|mixer|spatial_audio)\b" skills/movie-experts/_shared/ | grep -v "audio_pipeline" | grep -v "sub-step" | grep -v "foley/" | grep -v "sub_steps" | wc -l)
    echo "Potential stray references in _shared/ (excluding sub-step annotations): $STRAY"
    echo "TASK-2 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - _shared/glossary.md has 5 updated prose references + 1 new audio_pipeline entry (with disposition D-1 fold note)
    - _shared/RAG-INVOCATION-PATTERN.md has 3 table rows updated with sub-step annotation
    - _shared/project-corpus/animation-disney-system.md has 2 stage references updated
    - _shared/project-corpus/editing-sound-post.md has 5 expert_id references + 4 See Also markdown links updated; "Sync hard effects + foley" craft noun preserved
    - _shared/project-corpus/production-chinese-and-low-budget.md has Post line updated
    - No stray bare expert_id references to audio IDs in _shared/ outside intentional contexts (sub-step annotations, English craft nouns)
  </acceptance_criteria>
  <done>
    All cross-cutting documentation under _shared/ reflects the audio_pipeline merge. ROADMAP §15 success criterion #4 (_shared/ consumer coverage) and the implicit _shared/project-corpus/ consolidation requirement are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation readers → merged expert | If README or _shared/ docs still reference individual audio experts after this plan, readers will see stale topology and downstream phases (16, 17, 18) inherit stale context |
| English-noun preservation | "composer", "mixer", "voicer", "foley" appear as common nouns or craft terms in project-corpus prose (Disney pipeline, Sound Bible craft descriptions); blanket replacement would corrupt those references |
| DAG diagram topology | The ASCII DAG diagram is load-bearing for human readers to understand the production pipeline topology — incorrect collapse of audio nodes would mislead readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Information disclosure | README.md inventory accuracy | mitigate | Task 1 verify confirms single audio_pipeline row replaces the 6 separate audio expert rows |
| T-15-17 | Tampering | ASCII DAG diagram topology consistency | mitigate | Task 1 verify confirms audio_pipeline box present in DAG diagram |
| T-15-18 | Repudiation | _shared/glossary.md new entry addition (with disposition D-1 fold note) | mitigate | Task 2 verify confirms `^### audio_pipeline$` H3 header exists + the fold note is present |
| T-15-19 | Tampering | English-noun preservation (foley as craft term in editing-sound-post.md line 273) | mitigate | Task 2 verify confirms "Sync hard effects + foley" preserved verbatim + action explicitly instructs targeted Edit replacements not blanket sed |
| T-15-20 | Information disclosure | Stray expert_id references in _shared/ | mitigate | Task 2 verify greps _shared/ for stray audio references and reports count (excluding sub-step annotations) |
| T-15-21 | Information disclosure | README.md footer count arithmetic (22 → 17) | mitigate | Task 1 verify confirms "v2 = 17 experts" appears in footer with the arithmetic annotation showing -5 from Phase 15 |

No third-party package installs in this plan — T-{phase}-SC (Tampering of npm/pip/cargo) is N/A.

NOTE: No skills-mapping.yaml update in this plan. Per the canonical mapping convention (confirmed in Phase 14 close-out), `n_to_one_merged` entries do NOT carry a `sign_off_status` field — only `one_to_one_renamed` entries do. The audio_pipeline mapping entry at lines 91-95 is already canonical and complete. (If a future Phase 18 verification step requires merge-entry sign-off, that is Phase 18's responsibility.)
</threat_model>

<verification>
After both tasks complete, run the consolidated checks:

```bash
# 1. README.md reflects the merge
grep -q "| \[\`audio_pipeline\`\](./audio_pipeline/SKILL.md)" skills/movie-experts/README.md
grep -qE "├── audio_pipeline/" skills/movie-experts/README.md
grep -qE "(Phase 15 redirect stub — merged_into audio_pipeline)" skills/movie-experts/README.md
grep -qE "(Phase 15 redirect stub — folded_into audio_pipeline)" skills/movie-experts/README.md
grep -q "v2 = 17 experts" skills/movie-experts/README.md

# 2. _shared/glossary.md has new audio_pipeline entry + updated prose
grep -q "^### audio_pipeline$" skills/movie-experts/_shared/glossary.md
grep -q "audio_pipeline (composer sub-step) aligns musical sting" skills/movie-experts/_shared/glossary.md

# 3. _shared/RAG-INVOCATION-PATTERN.md model attribution
grep -q "audio_pipeline (foley sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
grep -q "audio_pipeline (composer sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md
grep -q "audio_pipeline (voicer sub-step)" skills/movie-experts/_shared/RAG-INVOCATION-PATTERN.md

# 4. _shared/project-corpus/ files updated
grep -q "voice via audio_pipeline (voicer sub-step)" skills/movie-experts/_shared/project-corpus/animation-disney-system.md
grep -q "Post = editor + audio_pipeline (composer + mixer sub-steps)" skills/movie-experts/_shared/project-corpus/production-chinese-and-low-budget.md
grep -q "(\../../audio_pipeline/SKILL.md) (foley sub-step)" skills/movie-experts/_shared/project-corpus/editing-sound-post.md

# 5. English craft nouns preserved
grep -q "Sync hard effects + foley" skills/movie-experts/_shared/project-corpus/editing-sound-post.md

# 6. No skills-mapping.yaml change required (n_to_one_merged has no sign_off_status field)
grep -q "n_to_one_merged" .planning/research/v2-pipeline-design/skills-mapping.yaml
```
</verification>

<success_criteria>
- [ ] README.md inventory has ONE audio_pipeline row (replacing 6 separate audio expert rows)
- [ ] README.md corpus → expert table line 39 references audio_pipeline for editing-sound-post.md
- [ ] README.md ASCII DAG diagram has an audio_pipeline box (single or multi-line per Phase 13 precedent)
- [ ] README.md Bridge/Audio-visual lock/Bottleneck narrative references audio_pipeline (with sub-step annotations)
- [ ] README.md File Layout corpus tree has a new audio_pipeline/ row with 6-sub-folder structure
- [ ] README.md old 6 audio rows in corpus tree preserved with redirect stub annotation (5 merged_into + 1 folded_into for spatial_audio)
- [ ] README.md Phase 7 additions section paths updated to audio_pipeline/references/{sub-step}/...
- [ ] README.md footer expert count decremented by 5 (22 → 17)
- [ ] _shared/glossary.md has 5 updated prose references + 1 new audio_pipeline entry (with D-1 fold note)
- [ ] _shared/RAG-INVOCATION-PATTERN.md has 3 table rows updated with sub-step annotation
- [ ] _shared/project-corpus/animation-disney-system.md has 2 stage refs updated
- [ ] _shared/project-corpus/editing-sound-post.md has 5 expert_id refs + 4 See Also markdown links updated; "Sync hard effects + foley" craft noun preserved
- [ ] _shared/project-corpus/production-chinese-and-low-budget.md Post line updated
- [ ] No stray expert_id references to bare audio IDs in _shared/ outside intentional contexts
- [ ] skills-mapping.yaml NOT modified (n_to_one_merged has no sign_off_status field by convention)
</success_criteria>

<output>
Create `.planning/phases/15-audio-pipeline-merge/15-03-SUMMARY.md` when done
</output>

---
phase: 15-audio-pipeline-merge
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - skills/movie-experts/character_designer/SKILL.md
  - skills/movie-experts/editor/SKILL.md
  - skills/movie-experts/hook_retention/SKILL.md
  - skills/movie-experts/performer/SKILL.md
  - skills/movie-experts/scene_builder/SKILL.md
  - skills/movie-experts/screenplay/SKILL.md
  - skills/movie-experts/style_genome/SKILL.md
  - skills/movie-experts/visual_executor/SKILL.md
  - skills/movie-experts/animation_studio/SKILL.md
autonomous: true
requirements: [MERGE-02]
user_setup: []

must_haves:
  truths:
    - "Every non-audio consumer SKILL.md that previously listed ANY of {voicer, lip_sync, composer, foley, mixer, spatial_audio} in metadata.hermes.related_skills now lists a SINGLE deduplicated `audio_pipeline` entry"
    - "Every non-audio consumer SKILL.md body prose that referenced any audio expert (via markdown link, Collaboration bullet, JSON literal, or narrative mention) is updated to reference `audio_pipeline` (with sub-step annotation where the specific sub-step matters for the edge semantics)"
    - "visual_executor/SKILL.md (Phase 14 merge product) lists `audio_pipeline` in related_skills (it referenced `foley` + `lip_sync` via the predecessor drawer/animator edges preserved by Phase 14) — bidirectional edge sync with the new audio_pipeline"
    - "No stranded expert_id references to {voicer, lip_sync, composer, foley, mixer, spatial_audio} remain in any non-audio consumer SKILL.md outside the redirect stubs themselves"
    - "English-noun usages of audio words (e.g., 'composer' in 'Senior composer' as a job title) are NOT changed — only expert_id references are updated"
  artifacts:
    - path: "skills/movie-experts/character_designer/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was lip_sync only)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/editor/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was voicer, composer, mixer)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/hook_retention/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was composer only — directional edge)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/performer/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was voicer only)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/scene_builder/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was foley only)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/screenplay/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was composer only)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/style_genome/SKILL.md"
      provides: "Consumer with audio_pipeline edges + body references (was composer only)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/visual_executor/SKILL.md"
      provides: "Consumer with audio_pipeline edge (Phase 14 merge product — now needs bidirectional edge with new audio_pipeline)"
      contains: "audio_pipeline"
    - path: "skills/movie-experts/animation_studio/SKILL.md"
      provides: "Consumer with audio_pipeline body references (was composer + voicer markdown links only; no related_skills change — animation_studio does not list audio experts in related_skills)"
      contains: "audio_pipeline"
  key_links:
    - from: "skills/movie-experts/character_designer/SKILL.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "related_skills array entry"
      pattern: "related_skills:.*audio_pipeline"
    - from: "skills/movie-experts/editor/SKILL.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "related_skills array entry + body markdown links"
      pattern: "audio_pipeline"
    - from: "skills/movie-experts/visual_executor/SKILL.md"
      to: "skills/movie-experts/audio_pipeline/SKILL.md"
      via: "related_skills array entry (bidirectional edge from Phase 14 preserved consumers)"
      pattern: "related_skills:.*audio_pipeline"
---

<objective>
Propagate the 6 audio expert → audio_pipeline merge across all 9 non-audio consumer SKILL.md files. Each consumer's `metadata.hermes.related_skills` array must reference `audio_pipeline` (deduplicated — not multiple of voicer/lip_sync/composer/foley/mixer/spatial_audio as separate entries) and every body prose reference to any of the 6 audio experts (markdown links, Collaboration bullets, JSON literals, narrative references) must be updated to `audio_pipeline` (with sub-step annotation where the specific sub-step matters for edge semantics — e.g., `audio_pipeline (composer sub-step)`).

Purpose: Fulfill ROADMAP §15 success criterion #4 ("old 5 audio expert directories preserved with status: merged_into + redirect" — criterion is structural; consumer edge sync is the bidirectional companion that completes the topology). Bidirectional edge sync — audio_pipeline lists these consumers in its related_skills (set in plan 15-01), and they list audio_pipeline back (set in this plan). Without this, the collaboration graph is one-directional and downstream agent dispatch can fail.

Output:
- 8 consumer SKILL.md files updated with `audio_pipeline` in related_skills (replacing voicer/lip_sync/composer/foley/mixer/spatial_audio entries — deduplicated to one)
- 1 consumer (animation_studio) updated for body prose only (no related_skills change — audit confirms it doesn't list audio experts there)
- Body prose references in all 9 files updated (markdown links, Collaboration bullets, JSON literals, narrative references)
- Zero stranded expert_id references to any of {voicer, lip_sync, composer, foley, mixer, spatial_audio} outside the redirect stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-audio-pipeline-merge/15-CONTEXT.md
@.planning/phases/15-audio-pipeline-merge/15-01-PLAN.md
@skills/movie-experts/audio_pipeline/SKILL.md

# Phase 14's 14-02-PLAN is the consumer-edge-sync precedent — same shape, different old/new IDs.
@.planning/phases/14-visual-executor-merge/14-02-PLAN.md
@.planning/phases/14-visual-executor-merge/14-02-SUMMARY.md

# CLAUDE.md §"Skill File Conventions (SKILL.md)": related_skills is bidirectional — A listing B implies B should list A.

<interfaces>
<!-- The 9 non-audio consumer SKILL.md files and their current audio-related related_skills edges -->

| Consumer | Current related_skills entries touching audio experts |
|----------|--------------------------------------------------------|
| character_designer | lip_sync (only — has 7 body references to lip_sync) |
| editor | voicer, composer, mixer (THREE entries → collapse to ONE audio_pipeline) |
| hook_retention | composer (only — directional edge, composer does NOT reciprocate per CONTEXT D-7) |
| performer | voicer (only) |
| scene_builder | foley (only) |
| screenplay | composer (only) |
| style_genome | composer (only) |
| visual_executor | (NO direct audio entry in current related_skills — BUT visual_executor is listed in audio_pipeline.related_skills by plan 15-01, so bidirectional edge requires visual_executor to list audio_pipeline back. Phase 14 set visual_executor.related_skills to [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer] — none of these are audio. The lip_sync→visual_executor and foley→visual_executor edges from v1 are preserved by plan 15-01 collapsing them into audio_pipeline→visual_executor. This plan adds the reverse edge visual_executor→audio_pipeline.) |
| animation_studio | (NONE in related_skills per audit — body prose only: 2 markdown links to ../composer/SKILL.md and ../voicer/SKILL.md) |

<!-- Edge replacement rules -->

1. **Dedup rule (CRITICAL):** When a consumer lists TWO OR MORE of {voicer, lip_sync, composer, foley, mixer, spatial_audio} in related_skills (e.g., editor lists voicer + composer + mixer), replace ALL of them with exactly ONE `audio_pipeline` entry. Do NOT leave multiple entries.
2. **Single-edge rule:** When a consumer lists only ONE audio expert (e.g., hook_retention lists composer), replace that single entry with `audio_pipeline`.
3. **Add-edge rule:** When a consumer lists ZERO audio experts in related_skills but audio_pipeline lists this consumer (bidirectional edge requirement), ADD `audio_pipeline` to this consumer's related_skills. (Applies to visual_executor.)
4. **No-touch rule for body nouns:** The words "composer", "mixer", "voicer" etc. used as English nouns (e.g., "Senior composer" as a job title, "the mixer" as in the person mixing) are NOT expert_id references and MUST NOT be changed. Most body references ARE expert_id references (markdown links, Collaboration bullets), so most will change.
5. **Directional edge preservation (CONTEXT D-7):** hook_retention → composer was a DIRECTIONAL edge per Phase 13 CONTEXT D-7 ("composer does not reciprocate"). After the merge, this becomes hook_retention → audio_pipeline (still directional). Do NOT add audio_pipeline → hook_retention to audio_pipeline's related_skills. (Plan 15-01 already set audio_pipeline.related_skills without hook_retention, which is correct.)

<!-- Consumer body-prose reference patterns to update -->

Patterns that MUST change:
- `(../voicer/SKILL.md)` → `(../audio_pipeline/SKILL.md)`
- `(../lip_sync/SKILL.md)` → `(../audio_pipeline/SKILL.md)`
- `(../composer/SKILL.md)` → `(../audio_pipeline/SKILL.md)`
- `(../foley/SKILL.md)` → `(../audio_pipeline/SKILL.md)`
- `(../mixer/SKILL.md)` → `(../audio_pipeline/SKILL.md)`
- `(../spatial_audio/SKILL.md)` → `(../audio_pipeline/SKILL.md)`
- `` [`voicer`](../voicer/SKILL.md) `` → `` [`audio_pipeline`](../audio_pipeline/SKILL.md) ``
- `` [`lip_sync`](../lip_sync/SKILL.md) `` → `` [`audio_pipeline`](../audio_pipeline/SKILL.md) ``
- `` [`composer`](../composer/SKILL.md) `` → `` [`audio_pipeline`](../audio_pipeline/SKILL.md) ``
- (analogous for foley, mixer, spatial_audio)
- `- **<- voicer**:` → `- **<- audio_pipeline (voicer sub-step)**:` (Collaboration bullets — preserve sub-step annotation where the specific edge matters)
- `- **-> composer**:` → `- **-> audio_pipeline (composer sub-step)**:`
- `- **<- composer**: coupled_beat.json` → `- **<- audio_pipeline (composer sub-step)**: coupled_beat.json`
- (analogous for foley, mixer, spatial_audio)
- `"voicer",` / `"lip_sync",` / `"composer",` / `"foley",` / `"mixer",` / `"spatial_audio",` inside JSON arrays → `"audio_pipeline",`
- `voicer → lip_sync` (narrative pipeline mentions in non-audio files) → `audio_pipeline (voicer → lip_sync sub-steps)` OR just `audio_pipeline` depending on context
- `(composer, voicer, mixer)` parenthetical groupings → `(audio_pipeline)` or `audio_pipeline`

Patterns that MUST NOT change (English nouns):
- "Senior composer" / "the composer" used as a job title in project-corpus prose (verify with surrounding context — if there's a markdown link to ../composer/SKILL.md, it IS an expert_id → change; if it's a bare noun, preserve)
- "the mixer" as in "the person mixing audio" (rare in body prose; verify with surrounding context)
- All audio expert IDs inside their OWN redirect stub files (voicer/SKILL.md, lip_sync/SKILL.md, etc. — those are the stubs themselves, intentional)

<!-- Sub-step annotation policy (CONTEXT: "with sub-step annotation where relevant") -->

When updating body references, ADD a sub-step annotation in parentheses where the SPECIFIC sub-step matters for edge semantics. Examples:
- editor's `- **<- composer**: coupled_beat.json` → `- **<- audio_pipeline (composer sub-step)**: coupled_beat.json` (the coupled_beat.json artifact specifically comes from the composer sub-step — annotation preserves the routing info)
- editor's `- **-> mixer**: post-edit audio timeline` → `- **-> audio_pipeline (mixer sub-step)**: post-edit audio timeline` (mixer sub-step consumes this)
- screenplay's `- **-> composer**: sound_mood per scene` → `- **-> audio_pipeline (composer sub-step)**: sound_mood per scene`
- style_genome's table row `| composer | sound | 1D |` → `| audio_pipeline (composer sub-step) | sound | 1D |`
- Where the edge is non-specific (e.g., character_designer → lip_sync edge is about identity reference, but the lip_sync sub-step is the specific consumer) → use `audio_pipeline (lip_sync sub-step)` annotation
- For DAG pipeline strings (e.g., style_genome line 227), the entire `(visual_executor, voicer, colorist, editor, composer, foley, spatial_audio, continuity_auditor)` grouping should collapse to `(visual_executor, audio_pipeline, colorist, editor, continuity_auditor)` — drop the individual audio entries since they're now one node
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 9 consumer SKILL.md files — related_skills dedup + body prose reference sync</name>
  <files>
    skills/movie-experts/character_designer/SKILL.md,
    skills/movie-experts/editor/SKILL.md,
    skills/movie-experts/hook_retention/SKILL.md,
    skills/movie-experts/performer/SKILL.md,
    skills/movie-experts/scene_builder/SKILL.md,
    skills/movie-experts/screenplay/SKILL.md,
    skills/movie-experts/style_genome/SKILL.md,
    skills/movie-experts/visual_executor/SKILL.md,
    skills/movie-experts/animation_studio/SKILL.md
  </files>
  <read_first>
    - Each consumer SKILL.md's `metadata.hermes.related_skills:` line (typically around line 13)
    - Each consumer SKILL.md's body prose for these patterns (use grep to locate): `(../voicer/SKILL.md)`, `(../lip_sync/SKILL.md)`, `(../composer/SKILL.md)`, `(../foley/SKILL.md)`, `(../mixer/SKILL.md)`, `(../spatial_audio/SKILL.md)`, `**<- voicer**`, `**-> voicer**`, `**<- composer**`, `**-> composer**`, `**<- mixer**`, `**-> mixer**`, `**<- foley**`, `**-> foley**`, `**<- lip_sync**`, `**-> lip_sync**`, `**<- spatial_audio**`, `**-> spatial_audio**`, JSON literals `"voicer",` / `"lip_sync",` / `"composer",` / `"foley",` / `"mixer",` / `"spatial_audio",`, narrative mentions like `composer.coupled_beat.json` (these are JSON artifact path references and should become `audio_pipeline.composer.coupled_beat.json` OR `audio_pipeline (composer sub-step).coupled_beat.json` depending on the path-style)
    - .planning/phases/15-audio-pipeline-merge/15-CONTEXT.md (consumer update rules + dedup requirement + sub-step annotation policy)
    - .planning/phases/15-audio-pipeline-merge/15-01-PLAN.md (the new audio_pipeline expert — verify its related_skills includes the 8 external consumers this plan updates back)
    - .planning/phases/14-visual-executor-merge/14-02-SUMMARY.md (Phase 14 consumer-edge-sync precedent — same pattern, English-noun preservation rules)
    - CLAUDE.md §"Skill File Conventions (SKILL.md)" — related_skills is bidirectional
  </read_first>
  <behavior>
    - In each of the 8 consumer SKILL.md files (all except animation_studio — which has no related_skills audio entries), the `metadata.hermes.related_skills` array:
      - If it contains `voicer`: replace `voicer` with `audio_pipeline`
      - If it contains `lip_sync`: replace `lip_sync` with `audio_pipeline`
      - If it contains `composer`: replace `composer` with `audio_pipeline`
      - If it contains `foley`: replace `foley` with `audio_pipeline`
      - If it contains `mixer`: replace `mixer` with `audio_pipeline`
      - If it contains `spatial_audio`: replace `spatial_audio` with `audio_pipeline`
      - If it contains MULTIPLE audio entries (e.g., editor has voicer + composer + mixer): replace ALL of them with exactly ONE `audio_pipeline` entry (deduplicated — do NOT leave multiple `audio_pipeline` entries)
      - If it contains NONE (visual_executor per audit — Phase 14 set its related_skills without audio): ADD `audio_pipeline` to satisfy the bidirectional edge requirement (audio_pipeline lists visual_executor in its related_skills from plan 15-01, so visual_executor must reciprocate)
      - animation_studio: NO related_skills change (audit confirms it doesn't list audio experts there); body prose changes still apply
    - In each consumer's body prose, update these expert_id reference patterns to use `audio_pipeline`:
      - Markdown links: `(../voicer/SKILL.md)` → `(../audio_pipeline/SKILL.md)`, and the same for lip_sync, composer, foley, mixer, spatial_audio
      - Link text in backtick-bracketed form: `` [`voicer`](../voicer/SKILL.md) `` → `` [`audio_pipeline`](../audio_pipeline/SKILL.md) ``, and the same for the other 5 audio IDs
      - Collaboration bullets: `- **<- voicer**:` → `- **<- audio_pipeline (voicer sub-step)**:`, same for `-> voicer`, `<- composer`, `-> composer`, `<- mixer`, `-> mixer`, `<- foley`, `-> foley`, `<- lip_sync`, `-> lip_sync`, `<- spatial_audio`, `-> spatial_audio`, and the backtick-bracketed variants
      - JSON literals: `"voicer",` inside JSON arrays → `"audio_pipeline",`, same for the other 5 audio IDs
      - JSON artifact path references: `composer.coupled_beat.json` → `audio_pipeline.composer.coupled_beat.json` (or `audio_pipeline (composer sub-step).coupled_beat.json` if the artifact name itself is ambiguous — prefer the dotted form for path-like references)
      - DAG pipeline strings: `(visual_executor, voicer, colorist, editor, composer, foley, spatial_audio, continuity_auditor)` → `(visual_executor, audio_pipeline, colorist, editor, continuity_auditor)` (collapse all 6 audio entries into one audio_pipeline node — they're now one node in the DAG)
      - Narrative pair references: `voicer / lip_sync / mixer` → `audio_pipeline`, `(voicer, composer, mixer)` → `audio_pipeline`
    - DO NOT change English-noun uses. Most body references ARE expert_id references (because they're markdown links or Collaboration bullets), but verify each with surrounding context. Examples that MUST be preserved:
      - "Senior composer" as a job title in project-corpus prose (verify with link context — if no markdown link, preserve)
      - Job-title usages of "mixer" / "voicer" without expert_id linking context
    - DO NOT touch the redirect stubs in `skills/movie-experts/{voicer,lip_sync,composer,foley,mixer,spatial_audio}/SKILL.md` (those intentionally retain their old IDs)
    - DO NOT touch `_eval/baseline/` (frozen regression baselines per Phase 13 decision)
  </behavior>
  <action>
    1. For each of the 9 consumer files, first run `grep -nE 'voicer|lip_sync|composer|foley|mixer|spatial_audio' skills/movie-experts/<consumer>/SKILL.md` to enumerate exact line locations before editing.
    2. For each consumer's `metadata.hermes.related_skills:` array, apply the appropriate replacement (dedup / single / add) per the audit table in `<interfaces>`. Example for editor (current: `related_skills: [screenplay, visual_executor, composer, voicer, continuity_auditor, mixer, compliance_gate, hook_retention, cinematographer, theory_critic, documentary_maker]`) → target: `related_skills: [screenplay, visual_executor, audio_pipeline, continuity_auditor, compliance_gate, hook_retention, cinematographer, theory_critic, documentary_maker]` (3 audio entries collapsed to ONE audio_pipeline).
    3. For each consumer's body prose, update markdown links, link text, Collaboration bullets (with sub-step annotation), JSON literals, JSON artifact path references, DAG pipeline strings, and narrative pair references per the patterns in `<interfaces>`. Use targeted Edit tool replacements — do NOT use blanket sed-style replacement.
    4. For visual_executor: ADD `audio_pipeline` to related_skills (it currently has none of the 6 audio IDs per Phase 14's merged list). Bidirectional edge: audio_pipeline lists visual_executor (plan 15-01), visual_executor lists audio_pipeline (this plan). Insert audio_pipeline in alphabetical or semantically sensible position in the related_skills array.

    Consumer-by-consumer known touchpoints (verify with grep before editing):

    - **character_designer/SKILL.md**: line 13 related_skills (lip_sync only → audio_pipeline); body has 7 lip_sync references — line 20 (`Calling sequence: character_designer → visual_executor → lip_sync` → `character_designer → visual_executor → audio_pipeline (lip_sync sub-step)`), line 26 (`visual_executor, lip_sync, continuity_auditor` → `visual_executor, audio_pipeline (lip_sync sub-step), continuity_auditor` OR `visual_executor, audio_pipeline, continuity_auditor`), line 51 (Chinese: `visual_executor / lip_sync / continuity_auditor` → `visual_executor / audio_pipeline (lip_sync sub-step) / continuity_auditor`), line 177 (JSON literal `"lip_sync",` → `"audio_pipeline",`), line 234 (`visual_executor / lip_sync / continuity_auditor / production` → `visual_executor / audio_pipeline (lip_sync sub-step) / continuity_auditor / production`), line 259 (`**-> [`lip_sync`](../lip_sync/SKILL.md)**` → `**-> [`audio_pipeline`](../audio_pipeline/SKILL.md) (lip_sync sub-step)**`).
    - **editor/SKILL.md**: line 13 related_skills (THREE entries: voicer + composer + mixer → collapse to ONE audio_pipeline); body line 79 (`composer.coupled_beat.json` → `audio_pipeline.composer.coupled_beat.json`), line 114 (`composer's coupled_beat` → `audio_pipeline (composer sub-step)'s coupled_beat` OR `audio_pipeline.composer.coupled_beat`), line 198 (`对齐 cut 时机与 composer.coupled_beat.json` → `对齐 cut 时机与 audio_pipeline.composer.coupled_beat.json`), line 217 (`**<- composer**: coupled_beat.json + light_beat.json` → `**<- audio_pipeline (composer sub-step)**: coupled_beat.json + light_beat.json`), line 218 (`**<- voicer**: dialogue timeline` → `**<- audio_pipeline (voicer sub-step)**: dialogue timeline`), line 222 (`**-> composer**: final cut points` → `**-> audio_pipeline (composer sub-step)**: final cut points`), line 223 (`**-> mixer**: post-edit audio timeline` → `**-> audio_pipeline (mixer sub-step)**: post-edit audio timeline`).
    - **hook_retention/SKILL.md**: line 13 related_skills (composer only → audio_pipeline); NOTE comment says "composer is one-directional (HOOK→composer for BGM sync); composer does not reciprocate by design (CONTEXT D-7)". After merge, comment becomes: "audio_pipeline (composer sub-step) is one-directional (HOOK→audio_pipeline for BGM sync); audio_pipeline does not reciprocate by design (CONTEXT D-7)". Body line 67 (`供下游 screenplay / editor / composer 机械消费` → `供下游 screenplay / editor / audio_pipeline (composer sub-step) 机械消费`), line 146 (`cut 必须落在 composer.coupled_beat 时间戳` → `cut 必须落在 audio_pipeline.composer.coupled_beat 时间戳`), line 252 (`**-> composer**:输出 BGM sync 时间戳要求` → `**-> audio_pipeline (composer sub-step)**: 输出 BGM sync 时间戳要求`).
    - **performer/SKILL.md**: line 13 related_skills (voicer only → audio_pipeline); body line 149 (`**-> voicer**: performance rhythm reference` → `**-> audio_pipeline (voicer sub-step)**: performance rhythm reference`).
    - **scene_builder/SKILL.md**: line 13 related_skills (foley only → audio_pipeline); body line 69 (`for foley` → `for audio_pipeline (foley sub-step)` OR `for foley sub-step of audio_pipeline` — prefer the latter for narrative), line 126 (`for foley` → `for audio_pipeline (foley sub-step)`), line 144 (`for foley` → `for audio_pipeline (foley sub-step)`), line 167 (`**-> foley**: material_annotations.json` → `**-> audio_pipeline (foley sub-step)**: material_annotations.json`), line 174 (`foley depends on them` → `audio_pipeline (foley sub-step) depends on them`).
    - **screenplay/SKILL.md**: line 13 related_skills (composer only → audio_pipeline); body line 226 (`**-> composer**: sound_mood per scene` → `**-> audio_pipeline (composer sub-step)**: sound_mood per scene`).
    - **style_genome/SKILL.md**: line 13 related_skills (composer only → audio_pipeline); body line 111 (`| composer | sound | 1D |` → `| audio_pipeline (composer sub-step) | sound | 1D |`), line 173 (`composer: sound dimension` → `audio_pipeline (composer sub-step): sound dimension`), line 210 (`**-> composer**: sound signal` → `**-> audio_pipeline (composer sub-step)**: sound signal`), line 227 (DAG string `style_genome -> screenplay -> (scene_builder, performer) -> (visual_executor, voicer, colorist, editor, composer, foley, spatial_audio, continuity_auditor) -> mixer -> final` → `style_genome -> screenplay -> (scene_builder, performer) -> (visual_executor, audio_pipeline, colorist, editor, continuity_auditor) -> final` — collapse the 6 audio nodes into ONE audio_pipeline node, and the `-> mixer -> final` suffix folds into audio_pipeline which IS the audio node now).
    - **visual_executor/SKILL.md**: line 13 related_skills (NO audio entry currently — ADD audio_pipeline for bidirectional edge with audio_pipeline). Current related_skills: [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer] → target: add `audio_pipeline` (insert in semantically sensible position — between editor and performer OR at the end; prefer alphabetical-ish position: `[..., editor, audio_pipeline, performer]` or append at end). Body references to lip_sync / foley (if any from Phase 14 migration) — verify and update to audio_pipeline (with sub-step annotation).
    - **animation_studio/SKILL.md**: line 13 related_skills (NONE — verify audit holds; if any audio entry exists, apply dedup; if NONE confirmed, SKIP related_skills edit). Body: line 18-19 (YAML-ish list with `composer` / `voicer` — these may be project-corpus context describing Disney pipeline roles; verify if they're expert_id references or job titles — if markdown links exist, change to audio_pipeline; if bare nouns, preserve). Line 175 (`TTS via voicer` → `TTS via audio_pipeline (voicer sub-step)` — this is an expert_id reference to the TTS capability). Line 184 (`` `composer`: Musical numbers composition `` → `` `audio_pipeline (composer sub-step)`: Musical numbers composition ``), line 185 (`` `voicer`: Voice casting for animation characters `` → `` `audio_pipeline (voicer sub-step)`: Voice casting for animation characters ``), line 203 (`[\`../composer/SKILL.md\`](../composer/SKILL.md) — Music expert` → `[\`audio_pipeline\`](../audio_pipeline/SKILL.md) — Music expert (composer sub-step)`), line 204 (`[\`../voicer/SKILL.md\`](../voicer/SKILL.md) — Voice expert` → `[\`audio_pipeline\`](../audio_pipeline/SKILL.md) — Voice expert (voicer sub-step)`).
  </action>
  <verify>
    <automated>
    echo "--- related_skills: no consumer has any of {voicer, lip_sync, composer, foley, mixer, spatial_audio} as standalone token ---" && \
    for c in character_designer editor hook_retention performer scene_builder screenplay style_genome visual_executor animation_studio; do
      LINE=$(grep "related_skills:" skills/movie-experts/$c/SKILL.md)
      for audio in voicer lip_sync composer foley mixer spatial_audio; do
        echo "$LINE" | grep -qE "\b$audio\b" && { echo "STRAY $audio in $c: $LINE"; exit 1; } || true
      done
    done && \
    echo "--- 8 consumers that should have audio_pipeline in related_skills (all 9 except animation_studio per audit) ---" && \
    for c in character_designer editor hook_retention performer scene_builder screenplay style_genome visual_executor; do
      grep -q "audio_pipeline" skills/movie-experts/$c/SKILL.md || { echo "MISSING audio_pipeline in $c"; exit 1; }
    done && \
    echo "--- No duplicate audio_pipeline entries in any related_skills array ---" && \
    for c in character_designer editor hook_retention performer scene_builder screenplay style_genome visual_executor animation_studio; do
      LINE=$(grep "related_skills:" skills/movie-experts/$c/SKILL.md)
      COUNT=$(echo "$LINE" | grep -o "audio_pipeline" | wc -l)
      if [ "$COUNT" -gt 1 ]; then echo "DUPLICATE in $c: $LINE"; exit 1; fi
    done && \
    echo "--- No stray ../{voicer,lip_sync,composer,foley,mixer,spatial_audio}/SKILL.md markdown links outside redirect stubs ---" && \
    for audio in voicer lip_sync composer foley mixer spatial_audio; do
      STRAYS=$(grep -rn "\.\./$audio/SKILL\.md" skills/movie-experts/*/SKILL.md | grep -v "^skills/movie-experts/$audio/SKILL.md:" | wc -l)
      if [ "$STRAYS" -gt 0 ]; then echo "STRAYS for $audio: $STRAYS"; grep -rn "\.\./$audio/SKILL\.md" skills/movie-experts/*/SKILL.md | grep -v "^skills/movie-experts/$audio/SKILL.md:"; exit 1; fi
    done && \
    echo "--- No stray expert_id references in Collaboration bullets (outside redirect stubs) ---" && \
    for audio in voicer lip_sync composer foley mixer spatial_audio; do
      STRAYS=$(grep -rn "\\*\\*-> $audio\\*\\*\\|\\*\\*<- $audio\\*\\*" skills/movie-experts/*/SKILL.md | grep -v "^skills/movie-experts/$audio/SKILL.md:" | wc -l)
      if [ "$STRAYS" -gt 0 ]; then echo "STRAYS for $audio: $STRAYS"; exit 1; fi
    done && \
    echo "--- _eval/baseline/ untouched (frozen regression baselines) ---" && \
    for audio in voicer lip_sync composer foley mixer spatial_audio; do
      if [ -f "skills/movie-experts/_eval/baseline/$audio/SKILL.md" ]; then
        grep -q "expert_id: $audio" "skills/movie-experts/_eval/baseline/$audio/SKILL.md" || { echo "_eval/baseline/$audio broken"; exit 1; }
      fi
    done && \
    echo "--- Redirect stubs themselves still preserve old IDs (intentional) ---" && \
    for audio in voicer lip_sync composer foley mixer spatial_audio; do
      grep -q "expert_id: $audio" skills/movie-experts/$audio/SKILL.md
    done && \
    echo "TASK-1 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - 8 consumer SKILL.md files (all except animation_studio) have `audio_pipeline` in their related_skills array
    - animation_studio related_skills array is unchanged (audit confirmed it doesn't list audio experts there) — verify holds
    - No consumer has DUPLICATE audio_pipeline entries in related_skills
    - No consumer has stray voicer / lip_sync / composer / foley / mixer / spatial_audio in related_skills
    - Zero stray `../{audio_id}/SKILL.md` markdown links outside the 6 audio redirect stubs themselves
    - Zero stray `**-> {audio_id}**` or `**<- {audio_id}**` Collaboration bullets outside redirect stubs
    - Zero stray `"{audio_id}",` JSON literals outside redirect stubs
    - English-noun usages of audio words preserved (e.g., "Senior composer" job-title usages in project-corpus prose)
    - `_eval/baseline/` untouched
    - Redirect stubs in the 6 audio directories preserve their old `expert_id` fields (intentional)
    - DAG pipeline string in style_genome collapses 6 audio nodes into ONE audio_pipeline node
    - JSON artifact path references like `composer.coupled_beat.json` become `audio_pipeline.composer.coupled_beat.json` in editor + hook_retention
    - Sub-step annotations (e.g., `audio_pipeline (composer sub-step)`) preserved where the specific edge matters (Collaboration bullets, artifact paths)
    - Directional edge CONTEXT D-7 preserved (hook_retention → audio_pipeline is directional; audio_pipeline does NOT list hook_retention back — plan 15-01 already enforced this)
  </acceptance_criteria>
  <done>
    All 9 consumer SKILL.md files updated. The collaboration graph is bidirectionally consistent: audio_pipeline lists these consumers in its related_skills (from plan 15-01), and they list audio_pipeline back (set in this plan). Zero stranded expert_id references to any of {voicer, lip_sync, composer, foley, mixer, spatial_audio} outside the redirect stubs. ROADMAP §15 success criterion #4 (old audio expert directories preserved with redirect + consumer edges synced) is satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer SKILL.md files → merged expert | If a consumer still references old audio expert_ids (voicer, lip_sync, composer, foley, mixer, spatial_audio) after this plan, downstream agent dispatch may resolve to the redirect stub (functional but wasteful) or fail entirely |
| Bidirectional edge sync | related_skills is bidirectional per CLAUDE.md §Skill File Conventions — A listing B implies B should list A. audio_pipeline lists its consumers (from 15-01); this plan makes the reverse edge consistent. visual_executor is the special case — Phase 14 set visual_executor.related_skills without audio entries, but audio_pipeline lists visual_executor, so this plan ADDS the reverse edge. |
| English-noun vs expert_id disambiguation | "composer", "mixer", "voicer" appear as English nouns in some body prose — blanket replacement would corrupt those references |
| Directional edge preservation (CONTEXT D-7) | hook_retention → composer was directional. After merge, the edge becomes hook_retention → audio_pipeline (still directional). audio_pipeline must NOT list hook_retention in related_skills. Plan 15-01 already enforced this; this plan must NOT add audio_pipeline → hook_retention to audio_pipeline.related_skills (it would violate D-7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | Consumer related_skills dedup (collapse multiple audio entries → one audio_pipeline) | mitigate | Task 1 verify loop checks for DUPLICATE audio_pipeline entries and STRAY audio IDs in any related_skills array |
| T-15-10 | Repudiation | Bidirectional edge sync (especially visual_executor reverse-edge addition) | mitigate | Task 1 verify loop checks that 8 expected consumers (all except animation_studio) have audio_pipeline in related_skills; visual_executor gets the add-edge treatment explicitly |
| T-15-11 | Information disclosure | Stray markdown links to old paths | mitigate | Task 1 grep verifies zero `../{audio_id}/SKILL.md` outside redirect stubs for ALL 6 audio IDs |
| T-15-12 | Tampering | English-noun preservation (composer / mixer / voicer as common nouns) | mitigate | Action explicitly instructs targeted Edit tool replacements, NOT blanket sed; verify step checks known preserved locations |
| T-15-13 | Tampering | Directional edge CONTEXT D-7 preservation (hook_retention → audio_pipeline is directional) | mitigate | Action explicitly notes "Do NOT add audio_pipeline → hook_retention to audio_pipeline.related_skills (would violate D-7)"; plan 15-01 already enforced audio_pipeline.related_skills excludes hook_retention; this plan does NOT modify audio_pipeline/SKILL.md so the exclusion holds |
| T-15-14 | Information disclosure | DAG pipeline string topology (style_genome line 227) | mitigate | Task 1 verify requires the style_genome DAG string collapses 6 audio nodes to ONE audio_pipeline node |
| T-15-15 | Denial of service | Frozen `_eval/baseline/` regression snapshots | mitigate | Task 1 verify step confirms `_eval/baseline/{audio_id}/SKILL.md` retain their original expert_id values (untouched) |

No third-party package installs in this plan — T-{phase}-SC (Tampering of npm/pip/cargo) is N/A.
</threat_model>

<verification>
After Task 1 completes, run the consolidated checks:

```bash
# 1. No consumer has stray audio ID in related_skills
for c in character_designer editor hook_retention performer scene_builder screenplay style_genome visual_executor animation_studio; do
  LINE=$(grep "related_skills:" skills/movie-experts/$c/SKILL.md)
  for audio in voicer lip_sync composer foley mixer spatial_audio; do
    echo "$LINE" | grep -qE "\b$audio\b" && echo "STRAY $audio in $c" && exit 1
  done
done

# 2. 8 consumers have audio_pipeline in related_skills (animation_studio excluded per audit)
for c in character_designer editor hook_retention performer scene_builder screenplay style_genome visual_executor; do
  grep -q "audio_pipeline" skills/movie-experts/$c/SKILL.md || { echo "MISSING in $c"; exit 1; }
done

# 3. No stray markdown links to old paths outside redirect stubs
for audio in voicer lip_sync composer foley mixer spatial_audio; do
  grep -rn "\.\./$audio/SKILL\.md" skills/movie-experts/*/SKILL.md \
    | grep -v "^skills/movie-experts/$audio/SKILL.md:" \
    | wc -l   # must be 0 for each
done

# 4. _eval/baseline/ untouched (where it exists)
for audio in voicer lip_sync composer foley mixer spatial_audio; do
  if [ -f "skills/movie-experts/_eval/baseline/$audio/SKILL.md" ]; then
    grep -q "expert_id: $audio" skills/movie-experts/_eval/baseline/$audio/SKILL.md
  fi
done

# 5. Directional edge D-7 preserved (audio_pipeline does NOT list hook_retention in related_skills)
grep "related_skills:" skills/movie-experts/audio_pipeline/SKILL.md | grep -q "hook_retention" && echo "D-7 VIOLATION" && exit 1
```
</verification>

<success_criteria>
- [ ] All 8 expected consumers (character_designer, editor, hook_retention, performer, scene_builder, screenplay, style_genome, visual_executor) have `audio_pipeline` in their related_skills array
- [ ] animation_studio related_skills array is unchanged (no audio entries before or after)
- [ ] No consumer has DUPLICATE audio_pipeline entries
- [ ] No consumer has stray voicer / lip_sync / composer / foley / mixer / spatial_audio in related_skills
- [ ] Zero stray `../{audio_id}/SKILL.md` markdown links outside redirect stubs
- [ ] Zero stray Collaboration bullets referencing audio IDs outside redirect stubs
- [ ] Zero stray JSON literals `"{audio_id}",` outside redirect stubs
- [ ] English-noun usages of audio words preserved
- [ ] DAG pipeline string in style_genome collapses 6 audio nodes to ONE audio_pipeline
- [ ] JSON artifact path references (`composer.coupled_beat.json`) become `audio_pipeline.composer.coupled_beat.json`
- [ ] Sub-step annotations preserved where specific edge matters
- [ ] Directional edge D-7 preserved (hook_retention → audio_pipeline is one-way; audio_pipeline does NOT reciprocate)
- [ ] visual_executor reverse edge added (audio_pipeline → visual_executor from 15-01, visual_executor → audio_pipeline from this plan)
- [ ] `_eval/baseline/` regression snapshots untouched
- [ ] Redirect stubs in 6 audio directories retain their old expert_id (backward-compat)
</success_criteria>

<output>
Create `.planning/phases/15-audio-pipeline-merge/15-02-SUMMARY.md` when done
</output>

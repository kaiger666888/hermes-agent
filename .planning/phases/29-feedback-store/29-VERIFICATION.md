---
phase: 29-feedback-store
verified: 2026-06-24T20:10:00Z
status: passed
score: 4/4 must-have truths verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: N/A
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 29: Feedback Store Verification Report

**Phase Goal:** All ingested feedback persists durably, is queryable by Curator and dashboards, decays in weight over time, and is deduplicated so the same output snapshot cannot skew the learning signal.
**Verified:** 2026-06-24T20:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| #   | Truth (SC)                                                                                                                                                                                                                     | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `~/.hermes/skills/.feedback/` directory exists with `skill_id/source/` sub-directory layout; feedback records append as jsonl files (one file per skill_id+source bucket)                                                       | ✓ VERIFIED | `agent/feedback_store.py:230-247` resolves `_buckets_root = _root / "buckets"` + `mkdir(parents=True, exist_ok=True)`. `bucket_path_for` (line 284) returns `self._buckets_root / record.skill_id / f"{record.source}.jsonl"`. `record_feedback` (line 336) opens with `open(bucket_file, "a", encoding="utf-8")` + `f.flush()` + `os.fsync(f.fileno())` — true O(1) append. TestRecordFeedback::test_bucket_layout + test_append_only_semantics + test_multiple_verdicts_same_bucket_file all PASS.                                                                                                                                                                                                                                                                                |
| 2   | `index.json` queryable by `skill_id` / `verdict` / `source` / `timestamp`; Curator + dashboard consume this single index                                                                                                      | ✓ VERIFIED | `_load_or_init_index` (line 693) reads `index.json` schema `{version, decay_window_days, updated_ts, buckets{}, by_sha256{}}`. Bucket key format `"<skill_id>:<source>:<verdict>"` (line 329, colon separator per RESEARCH Pitfall #6). `query()` (line 919) filters by skill_id/source/verdict/since/until. `summary()` (line 990) returns per-bucket `{count, weighted_count, first_ts, last_ts}`. TestQuery (8 tests) + TestSummary (6 tests) all PASS. `_write_index` (line 682) uses `atomic_json_write` (temp + fsync + os.replace) — atomic single source of truth.                                                                                                                                                                                                                          |
| 3   | Feedback older than N days (configurable in `config.yaml`) is marked with reduced weight — `weighted_count` differs from raw `count` for old buckets                                                                            | ✓ VERIFIED | `compute_weight` (line 73-120) implements `weight = max(_WEIGHT_FLOOR=0.1, 1.0 - age_days/decay_window_days)`. `_load_decay_window_days_from_config` (line 158-193) reads `feedback.decay_window_days` from config.yaml, falls back to `DEFAULT_DECAY_WINDOW_DAYS = 180` on ANY exception. `cli-config.yaml.example:528-531` documents the section. `record_feedback` (line 377) computes `weighted_count = self._recompute_bucket_weighted_count(active_records)` at query time. TestDecay (10 tests incl. test_weight_at_birth / test_weight_at_half / test_weight_floor_at_window / test_config_override_decay_window) + TestRecordFeedback::test_weighted_count_differs_from_count_for_old_record all PASS. **Note:** Default is 180 (not 90 as ROADMAP SC literally states); this is a documented decision per CONTEXT.md D-STORE-03 + REQUIREMENTS.md note on STORE-03. |
| 4   | Duplicate detection: same sha256+same verdict NOT double-counted; same sha256+different verdict demotes older (correction)                                                                                                     | ✓ VERIFIED | `_handle_dedup` (line 404-489) implements the 3-branch decision matrix: new sha256 → write; same sha256+same verdict → return existing record_id (DUPLICATE, no write); same sha256+different verdict → call `_mark_superseded` (line 544) then proceed with newer write. `_mark_superseded` appends supersession event to `dedup/sha256-registry.jsonl` + updates `_sha256_index[sha]` + adds older_record_id to `_superseded_record_ids`. `_recompute_single_bucket` (line 491) refreshes older bucket weighted_count (drops superseded record weight). TestDedup (5 tests) + TestCorrection (7 tests incl. test_correction_chain_three_records + test_correction_supersession_persists_across_reload) all PASS.                                                                                                                                              |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                                    | Expected                                                                                          | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent/feedback_store.py`                   | FeedbackStore class + compute_weight + dedup branch + rebuild_index                              | ✓ VERIFIED | EXISTS (1147 LOC, well above 280-line min). Contains: `compute_weight`, `recompute_bucket_weighted_count`, `_load_decay_window_days_from_config`, `class FeedbackStore` with `__init__`, `_make_record_id`, `bucket_path_for`, `record_feedback`, `_handle_dedup`, `_recompute_single_bucket`, `_mark_superseded`, `_recompute_bucket_weighted_count`, `_read_bucket_records`, `_write_index`, `_load_or_init_index`, `_load_sha256_index`, `rebuild_index`, `query`, `summary`, `get_record`, `_maybe_migrate_phase28_incoming`. SUBSTANTIVE + WIRED. |
| `agent/feedback_ingest.py`                  | write_feedback_record delegates to FeedbackStore                                                  | ✓ VERIFIED | EXISTS (562 LOC). `inspect.getsource(write_feedback_record)` contains `FeedbackStore` — delegation wired (confirmed via runtime introspection).                                                                                                                                                                                                                                                                                                                                                                                                                |
| `hermes_cli/feedback.py`                    | rebuild-index subparser + handler                                                                 | ✓ VERIFIED | EXISTS (279 LOC). `register_cli` includes `rebuild-index` subparser (confirmed via runtime argparse introspection).                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `agent/feedback_schema.py`                  | FeedbackRecord + OutputSnapshot (frozen) + path-safety validators (WR-06)                        | ✓ VERIFIED | EXISTS (240 LOC). `_known_expert` rejects `:` / `/` / `\` in skill_id/expert_id (WR-06 fix).                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `cli-config.yaml.example`                   | `feedback:` section documenting `decay_window_days`                                               | ✓ VERIFIED | EXISTS at line 528 with full inline comment explaining the weight formula.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `tests/agent/test_feedback_store.py`        | 9 test classes covering every must-have                                                           | ✓ VERIFIED | EXISTS (1186 LOC, well above 350-line min). Contains TestFeedbackStoreInit, TestIndex, TestDecay, TestRecordFeedback, TestQuery, TestSummary, TestGetRecord, TestMigration, TestConfig, TestDedup, TestCorrection, TestRebuildIndex. 65 tests pass.                                                                                                                                                                                                                                                                                                            |
| `tests/agent/test_feedback_store_integration.py` | TestDelegation + TestRebuildIndexCLI                                                         | ✓ VERIFIED | EXISTS (381 LOC). Contains `class TestDelegation` + TestRebuildIndexCLI. 10 integration tests pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### Key Link Verification

| From                                              | To                                                                        | Via                                                                                     | Status   | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent/feedback_ingest.py` (write_feedback_record) | `agent.feedback_store.FeedbackStore().record_feedback`                    | wrapper delegation; returns `store.bucket_path_for(record)`                            | ✓ WIRED  | Runtime introspection confirms `FeedbackStore` in source. TestDelegation::test_write_feedback_record_routes_to_store + test_write_feedback_record_no_incoming_file PASS — records land in `buckets/<skill_id>/<source>.jsonl` not `incoming/*.json`.                                                                                                                                                                                                                                                  |
| `agent/feedback_store.py` (record_feedback)        | `_handle_dedup` → `dedup/sha256-registry.jsonl`                           | dedup check before bucket append                                                       | ✓ WIRED  | `_handle_dedup` called at line 324 before bucket append (line 336). TestDedup::test_duplicate_does_not_append_registry confirms registry has exactly 1 line after duplicate attempt.                                                                                                                                                                                                                                                                                 |
| `agent/feedback_store.py` (record_feedback)        | `index.json` (atomic)                                                     | `utils.atomic_json_write` via `_write_index`                                            | ✓ WIRED  | `_write_index` (line 682) calls `atomic_json_write(self._index_path, self._index, indent=2, sort_keys=True)`. TestRecordFeedback::test_index_updated_atomic confirms mtime bump.                                                                                                                                                                                                                                                                                       |
| `agent/feedback_store.py` (FeedbackStore.__init__) | Phase 28 `incoming/*.json`                                                | `_maybe_migrate_phase28_incoming` (lazy, gated by `_migrated`)                          | ✓ WIRED  | Method at line 1039, called at line 269. Append-first-then-archive order (RESEARCH Pitfall #4). CR-02 hardening: pre-migration dedup check + duplicate/ archive subdir + filename collision disambiguation. TestMigration (6 tests) PASS.                                                                                                                                                                                                                              |
| `agent/feedback_store.py` (compute_weight)         | CONTEXT.md Time-Decay Function — `max(0.1, 1.0 - age_days/180)`           | linear decay at query time                                                              | ✓ WIRED  | `compute_weight` line 120 returns `max(_WEIGHT_FLOOR, raw_weight)` where `_WEIGHT_FLOOR = 0.1`. TestDecay::test_weight_floor_at_window + test_weight_floor_default_window PASS.                                                                                                                                                                                                                                                                                       |
| `hermes_cli/feedback.py` (register_cli)            | `agent.feedback_store.FeedbackStore().rebuild_index()`                    | `_cmd_rebuild_index` handler + subparser                                                | ✓ WIRED  | Runtime argparse introspection confirms `rebuild-index` subcommand present. TestRebuildIndexCLI (4 tests incl. test_rebuild_index_subcommand_present) PASS.                                                                                                                                                                                                                                                                                                            |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable                | Source                                                          | Produces Real Data | Status     |
| --------------------------------- | ---------------------------- | --------------------------------------------------------------- | ------------------ | ---------- |
| `record_feedback`                 | bucket_file content          | direct append of `record.model_dump_json()`                     | Yes                | ✓ FLOWING  |
| `record_feedback`                 | `self._index["buckets"]`     | computed from `_read_bucket_records` (real disk read)           | Yes                | ✓ FLOWING  |
| `query()`                         | returned records             | `_read_bucket_records` filtered by index status                 | Yes                | ✓ FLOWING  |
| `summary()`                       | bucket dicts                 | reads from in-memory `self._index["buckets"]` (mirrors disk)    | Yes                | ✓ FLOWING  |
| `rebuild_index()`                 | regenerated index            | walks `buckets/*/*.jsonl` + replays `dedup/sha256-registry.jsonl` | Yes                | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                                | Command                                                                                                                                                       | Result                                                                 | Status  |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------- |
| Store unit tests (all SC-relevant classes)                             | `python -m pytest tests/agent/test_feedback_store.py -x`                                                                                                       | 65 passed                                                              | ✓ PASS  |
| Targeted SC-1..SC-4 test classes                                       | `python -m pytest tests/agent/test_feedback_store.py::TestRecordFeedback tests/.../TestQuery tests/.../TestSummary tests/.../TestDecay tests/.../TestDedup tests/.../TestCorrection tests/.../TestRebuildIndex -v` | 48 passed (verbose)                                                    | ✓ PASS  |
| Full feedback subsystem regression                                     | `python -m pytest tests/agent/test_feedback_store.py tests/agent/test_feedback_store_integration.py tests/agent/test_feedback_ingest.py tests/agent/test_feedback_schema.py tests/agent/test_feedback_snapshot.py tests/hermes_cli/test_feedback_cli.py` | 150 passed, 1 skipped (documented — see Phase 28 test in test_feedback_ingest.py) | ✓ PASS  |
| Ruff PLW1514 on all modified modules                                   | `ruff check agent/feedback_store.py agent/feedback_ingest.py hermes_cli/feedback.py agent/feedback_schema.py`                                                  | All checks passed                                                      | ✓ PASS  |
| FOUND-08 byte-intact                                                  | `git diff --name-only v5.0 -- skills/movie-experts/ \| grep -v _eval \| grep -v _shared \| wc -l`                                                              | 0                                                                      | ✓ PASS  |
| Phase 28 delegation wiring (runtime)                                  | `python -c "import inspect; from agent.feedback_ingest import write_feedback_record; print('FeedbackStore' in inspect.getsource(write_feedback_record))"`      | True                                                                   | ✓ PASS  |
| rebuild-index CLI registration (runtime)                              | `python -c "import argparse; from hermes_cli.feedback import register_cli; p = argparse.ArgumentParser(); register_cli(p); print(any('rebuild-index' in (a.choices or {}) for a in p._actions))"` | True                                                                   | ✓ PASS  |
| compute_weight math at boundaries                                     | (covered by TestDecay)                                                                                                                                         | test_weight_at_birth=1.0, test_weight_at_half=0.5, test_weight_floor_at_window=0.1 all PASS | ✓ PASS  |
| STORE-04 duplicate rejection                                          | (covered by TestDedup)                                                                                                                                         | test_duplicate_rejected + test_duplicate_no_double_count PASS          | ✓ PASS  |
| STORE-04 correction demotion                                          | (covered by TestCorrection)                                                                                                                                    | test_correction_demotes_older + test_correction_weighted_count_older_bucket_drops PASS | ✓ PASS  |

### Probe Execution

Phase 29 is not a migration/tooling phase; no probe scripts declared. Test suite IS the probe — executed above. SKIPPED (no probe scripts).

### Requirements Coverage

| Requirement | Source Plan    | Description                                                                                                                                                | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| STORE-01    | 29-01-PLAN.md  | `~/.hermes/skills/.feedback/` layout, jsonl append                                                                                                          | ✓ SATISFIED | `bucket_path_for` produces `buckets/<skill_id>/<source>.jsonl`. TestRecordFeedback::test_bucket_layout + test_append_only_semantics PASS.                                                                                                                                                                                                                                                                                                                              |
| STORE-02    | 29-01 + 29-02  | `index.json` queryable by skill_id/verdict/source/ts; single source of truth for Curator + dashboard                                                        | ✓ SATISFIED | `_load_or_init_index` + `_write_index` (atomic). `query()` + `summary()` read from in-memory mirror of disk. Bucket key `<skill_id>:<source>:<verdict>`. P32 Curator + P33 Dashboard contract honored (JSON-on-disk schema). TestQuery (8) + TestSummary (6) PASS.                                                                                                                                                                                                 |
| STORE-03    | 29-01-PLAN.md  | Time-decay weight, configurable                                                                                                                             | ✓ SATISFIED | `compute_weight` = `max(0.1, 1.0 - age_days/decay_window_days)`. Default 180 (per CONTEXT.md D-STORE-03 — ROADMAP SC literally says "default 90" but the controlling CONTEXT decision + REQUIREMENTS.md note both say 180; the 180 default is the actual implementation). Configurable via `feedback.decay_window_days` in cli-config.yaml. TestDecay (10 tests) + TestConfig (3 tests) PASS.                                                                       |
| STORE-04    | 29-02-PLAN.md  | Dedup by sha256+verdict; correction demotion                                                                                                                | ✓ SATISFIED | `_handle_dedup` 3-branch decision matrix. `_mark_superseded` zero out older weight + audit-trail append. `_recompute_single_bucket` refreshes older bucket after correction. TestDedup (5) + TestCorrection (7) PASS.                                                                                                                                                                                                                                                  |

**Orphaned requirements check:** REQUIREMENTS.md lines 95-98 map STORE-01..04 exclusively to Phase 29; all 4 are claimed by plans 29-01 and/or 29-02. No orphaned requirements.

### Anti-Patterns Found

| File                          | Line  | Pattern         | Severity | Impact                                                                                                                                                                                                                                                                                                                              |
| ---------------------------- | ----- | --------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent/feedback_store.py:662` | 662   | `return []`     | ℹ️ Info  | Legitimate empty-file guard in `_read_bucket_records` (`if not bucket_file.exists(): return []`). Not a stub — the function reads + parses real records from disk when the file exists. Verified by TestRecordFeedback::test_append_only_semantics (3 sequential appends produce 3 readable records). No data-flow disconnection. |

**Debt markers (TBD/FIXME/XXX/PLACEHOLDER):** None found in any Phase 29 modified file (`agent/feedback_store.py`, `agent/feedback_ingest.py`, `hermes_cli/feedback.py`, `agent/feedback_schema.py`).

**TODO comments:** None in Phase 29 modified files (Plan 01's documented TODO marker for the STORE-04 insertion point was resolved by Plan 02's `_handle_dedup` implementation — the TODO was the insertion contract, not a debt marker).

### Code Review Fix Commits (9/9 confirmed)

All 9 review findings (3 Critical + 6 Warning) were fixed and committed. Confirmed via `git log`:

| # | Finding | Severity | Commit    | Status    |
| - | ------- | -------- | --------- | --------- |
| 1 | CR-01: CORRECTION path recomputes wrong bucket when older has different skill_id/source | Critical | `a580e99ff` | ✓ Fixed |
| 2 | CR-02: Migration can spuriously fire CORRECTION on same record_id after crash-retry     | Critical | `6442f1303` | ✓ Fixed |
| 3 | CR-03: `_scan_once` seen cache misses sub-second-same-size rewrites                     | Critical | `bd753e398` | ✓ Fixed |
| 4 | WR-01: Malformed f-string with no placeholder                                           | Warning  | `9ace66b30` | ✓ Fixed |
| 5 | WR-02: `_move_to_errors` overwrites distinct files with same basename                   | Warning  | `ce081eb87` | ✓ Fixed |
| 6 | WR-03: Symlink check TOCTOU window                                                     | Warning  | `3d40d054d` | ✓ Fixed |
| 7 | WR-04: `_recompute_single_bucket` silently drops zero-active bucket from index          | Warning  | `a580e99ff` | ✓ Fixed (combined with CR-01) |
| 8 | WR-05: Config-load failures swallowed at debug level                                   | Warning  | `aabe4b70b` | ✓ Fixed |
| 9 | WR-06: `summary()` parses bucket keys by splitting on `:`                              | Warning  | `3b3253177` | ✓ Fixed |

All 9 commits exist in git log. (CR-01 + WR-04 were combined into a single commit `a580e99ff` since both touched the same code path — documented in REVIEW-FIX.md.)

### Human Verification Required

**None required.** All 4 SCs are fully covered by automated tests with concrete evidence (not just file existence). The VALIDATION.md "Manual-Only Verifications" section flags two operator-UX items, but these are downstream concerns (P33 dashboard display, live-data `rebuild-index`) — not gates for the Phase 29 goal of "feedback persists durably, is queryable, decays, and is deduplicated". The underlying math, persistence, query, and dedup behaviors are all verified by passing automated tests against real on-disk artifacts.

### Gaps Summary

**No gaps found.** All 4 roadmap success criteria are verified VERIFIED with codebase evidence. All 4 STORE requirements are SATISFIED. All artifacts exist, are substantive (well above min_line thresholds), are wired (verified by both static grep AND runtime introspection), and have flowing data (verified by passing tests that exercise real append/read/decay/dedup paths). All 9 code-review findings are fixed with confirmed commits. Ruff PLW1514 clean. FOUND-08 byte-intact (0 changes to bundled skills/movie-experts vs v5.0). Phase 28 regression intact (no Phase 28 functional regressions; the 1 documented skip is a delegation-triggered side effect on an unwritable-dir test, with the underlying invariant still tested directly on FeedbackStore).

**Phase 29 goal achieved.** Ready to proceed to Phase 30/31 (parallel-eligible per ROADMAP critical path).

---

_Verified: 2026-06-24T20:10:00Z_
_Verifier: Claude (gsd-verifier)_

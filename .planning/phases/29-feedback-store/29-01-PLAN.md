---
phase: 29-feedback-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/feedback_store.py
  - tests/agent/test_feedback_store.py
  - cli-config.yaml.example
autonomous: true
requirements: [STORE-01, STORE-02, STORE-03]
tags: [feedback, persistence, jsonl, decay, index, migration]

must_haves:
  truths:
    - "FeedbackStore.record_feedback(record) appends to buckets/<skill_id>/<source>.jsonl (O(1) append per write)"
    - "FeedbackStore.query(skill_id=..., source=..., verdict=..., since=..., until=...) returns the matching FeedbackRecord list"
    - "FeedbackStore.summary(skill_id=None, source=None) returns {count, weighted_count, first_ts, last_ts} per bucket key"
    - "FeedbackStore.get_record(record_id) returns the FeedbackRecord with that record_id"
    - "compute_weight(ts, now, decay_window_days) returns max(0.1, 1.0 - age_days/decay_window_days)"
    - "weighted_count in index.json differs from raw count when a bucket contains old records (decay computed at query time)"
    - "index.json has version=1, decay_window_days, updated_ts, buckets{}, by_sha256{}"
    - "Lazy migration from Phase 28 incoming/ routes each file to buckets/<skill_id>/<source>.jsonl on first FeedbackStore.__init__; idempotent"
    - "decay_window_days configurable via feedback.decay_window_days in config.yaml (default 180)"
  artifacts:
    - path: "agent/feedback_store.py"
      provides: "FeedbackStore class + compute_weight + recompute_bucket_weighted_count + lazy migration + index read/write"
      contains: "class FeedbackStore"
      min_lines: 280
    - path: "tests/agent/test_feedback_store.py"
      provides: "Unit tests: TestRecordFeedback, TestQuery, TestSummary, TestGetRecord, TestDecay, TestIndex, TestMigration, TestConfig"
      contains: "class TestDecay"
      min_lines: 350
    - path: "cli-config.yaml.example"
      provides: "feedback.decay_window_days config documentation"
      contains: "feedback:"
  key_links:
    - from: "agent/feedback_store.py (record_feedback)"
      to: "buckets/<skill_id>/<source>.jsonl (direct file append, O(1))"
      via: "open(path, 'a', encoding='utf-8') + fsync"
      pattern: "open\\(bucket_path, \"a\", encoding=\"utf-8\"\\)"
    - from: "agent/feedback_store.py (record_feedback / _write_index)"
      to: "index.json"
      via: "utils.atomic_json_write (temp + fsync + os.replace)"
      pattern: "atomic_json_write\\(self\\._index_path"
    - from: "agent/feedback_store.py (FeedbackStore.__init__)"
      to: "Phase 28 incoming/*.json"
      via: "_maybe_migrate_phase28_incoming (lazy on first init, gated by _migrated flag)"
      pattern: "_maybe_migrate_phase28_incoming"
    - from: "agent/feedback_store.py (compute_weight)"
      to: "CONTEXT.md Time-Decay Function (STORE-03) — weight = max(0.1, 1.0 - age_days/180)"
      via: "linear decay, computed at query time, never stored on record"
      pattern: "max\\(0\\.1, raw_weight\\)"
---

<objective>
Build the durable feedback storage foundation: `FeedbackStore` Python class at `agent/feedback_store.py` with bucket-layout jsonl persistence, query/summary/get_record API, linear time-decay computed at query time (STORE-03), single `index.json` as source of truth (STORE-02), and lazy migration from Phase 28's flat `incoming/` layout (STORE-01 restructure).

Purpose: Phase 28 ships the ingest contract (`FeedbackRecord` → flat `incoming/*.json`). Phase 29 restructures that into bucketed `skill_id/source/` jsonl files + a single queryable index + decay-aware weighted counts. This plan is the foundation — the `FeedbackStore` class with all CRUD primitives, the decay function, and migration. Plan 02 wires the Phase 28 write path into this store + adds dedup/correction branching.

Output:
- `agent/feedback_store.py` — `FeedbackStore` class with `record_feedback` / `query` / `summary` / `get_record` + `compute_weight` + `recompute_bucket_weighted_count` + lazy `_maybe_migrate_phase28_incoming`
- `tests/agent/test_feedback_store.py` — unit tests covering every must-have truth
- `cli-config.yaml.example` — `feedback.decay_window_days` documentation

Decisions honored (from 29-CONTEXT.md):
- D-STORE-02-index: incremental atomic update on every write
- D-STORE-03-decay: linear `weight = max(0.1, 1.0 - age_days/180)`, computed at query time
- D-STORE-02-api: `FeedbackStore` Python class with `record_feedback` / `query` / `summary` / `get_record`
- D-layout: `buckets/<skill_id>/<source>.jsonl` + `index.json` + `dedup/sha256-registry.jsonl` + `archive/`

Plan 01 deliberately SHIPS record_feedback WITHOUT the dedup/correction branch — it always writes. Plan 02 adds the sha256 dedup branch (STORE-04) by modifying `record_feedback` to consult the dedup registry first. This keeps Plan 01 focused on the storage layer; Plan 02 adds STORE-04 + integrates Phase 28 callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-feedback-store/29-CONTEXT.md
@.planning/phases/29-feedback-store/29-RESEARCH.md
@.planning/phases/29-feedback-store/29-VALIDATION.md
@.planning/phases/28-feedback-ingestion-mvp/28-01-SUMMARY.md

# Phase 28 ingested to Phase 29 buckets. Phase 29 lays the foundation.

<interfaces>
<!-- Key contracts the executor needs. Extracted from Phase 28 code + CONTEXT decisions. -->

From agent/feedback_schema.py (Phase 28 — FROZEN, do not modify):
```python
class OutputSnapshot(BaseModel):
    sha256: str          # 64-char lowercase hex (validator normalizes)
    output_text: str
    prompt: str
    model: str
    provider: str
    api_mode: str = ""
    params: dict[str, Any]
    captured_at: datetime

class FeedbackRecord(BaseModel):
    skill_id: str        # validated against _KNOWN_EXPERT_IDS
    expert_id: str       # validated against _KNOWN_EXPERT_IDS
    source: Literal["cli", "kais_aigc", "manual"]
    verdict: Literal["good", "needs_work", "bad"]
    correction: str = ""
    revised_output: str | None = None
    output_snapshot: OutputSnapshot
    ts: datetime         # tz-aware (validator rejects naive)
```

From agent/feedback_ingest.py (Phase 28 — Plan 02 modifies this):
```python
def write_feedback_record(record: FeedbackRecord) -> Path:
    """Current Phase 28 impl: atomic write to incoming/{skill_id}_{source}_{ts}.json.
    Plan 02 replaces body with: FeedbackStore().record_feedback(record) wrapper."""
```

From utils.py (atomic write primitive — REUSE, do not rewrite):
```python
def atomic_json_write(path, data, *, indent=2, mode=None, **dump_kwargs) -> None:
    # temp + fsync + os.replace. ensure_ascii=False hardcoded.
    # NOTE: do NOT pass ensure_ascii= in **dump_kwargs (collides — Bug #1 in 28-01-SUMMARY).
```

From hermes_constants.py:
```python
def get_hermes_home() -> Path: ...   # honors HERMES_HOME env var; CLAUDE.md mandate
```

From agent/curator.py (pattern template — copy style, don't import):
```python
def _state_file() -> Path: return get_hermes_home() / "skills" / ".curator_state"
def load_state() -> dict: ...        # json.loads(read_text(encoding="utf-8"))
def save_state(data) -> None: ...    # atomic_json_write(path, data, indent=2, sort_keys=True)
def _load_config() -> dict: ...      # from hermes_cli.config import load_config; cfg.get("curator") or {}
```
</interfaces>

<storage_layout>
<!-- The exact on-disk layout FeedbackStore must produce. Source: 29-CONTEXT.md + 29-RESEARCH.md Pattern 2. -->

```
~/.hermes/skills/.feedback/
├── index.json                          # atomic — single source of truth
├── buckets/
│   └── <skill_id>/
│       └── <source>.jsonl              # append-only, one FeedbackRecord.model_dump_json() per line
├── dedup/
│   └── sha256-registry.jsonl           # append-only audit log (Plan 02 writes here; Plan 01 just creates the file)
├── archive/
│   └── phase-28-migration/             # lazy migration target for originals
└── incoming/                           # Phase 28 flat layout — becomes transient (migration source)
```

index.json schema (RESEARCH Pattern 2):
```json
{
  "version": 1,
  "decay_window_days": 180,
  "updated_ts": "2026-06-24T13:30:00+00:00",
  "buckets": {
    "<skill_id>:<source>:<verdict>": {
      "count": 12, "weighted_count": 9.5,
      "first_ts": "...", "last_ts": "..."
    }
  },
  "by_sha256": {
    "<64hex>": {
      "record_id": "<ts_unix_micro>_<sha8>",
      "verdict": "good", "ts": "...",
      "status": "active"   // or "superseded" — Plan 02 mutates this
    }
  }
}
```
</storage_layout>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeedbackStore class skeleton + index schema + decay function</name>
  <files>agent/feedback_store.py, tests/agent/test_feedback_store.py</files>
  <read_first>
    - agent/feedback_schema.py (full — FeedbackRecord + OutputSnapshot contract, frozen)
    - agent/curator.py:71-117 (_state_file / load_state / save_state template — copy style)
    - utils.py:111-178 (atomic_json_write contract — temp + fsync + os.replace; ensure_ascii hardcoded, do NOT pass)
    - hermes_constants.py:53 (get_hermes_home)
    - .planning/phases/29-feedback-store/29-RESEARCH.md (Pattern 1, 2, 3 — code examples + index schema + decay formula)
    - tests/agent/test_feedback_ingest.py:1-85 (feedback_env fixture pattern to reuse for test_feedback_store.py)
  </read_first>
  <behavior>
    - TestFeedbackStoreInit: FeedbackStore() creates buckets/, dedup/, archive/ dirs under HERMES_HOME/skills/.feedback/ on first init; _index_path points there
    - TestIndex::test_index_schema: after first record_feedback, index.json has keys version=1, decay_window_days=180, updated_ts (ISO8601), buckets (dict), by_sha256 (dict)
    - TestDecay::test_weight_at_birth: compute_weight(ts, now=ts) returns 1.0
    - TestDecay::test_weight_at_half: compute_weight(ts, now=ts+90days, decay_window_days=180) returns ~0.5
    - TestDecay::test_weight_floor_at_window: compute_weight(ts, now=ts+180days, decay_window_days=180) returns 0.1 (floored, not 0.0)
    - TestDecay::test_weight_floor_beyond_window: compute_weight(ts, now=ts+365days, decay_window_days=180) returns 0.1
    - TestDecay::test_weight_floor_default_window: compute_weight(ts, now=ts+180days) uses default 180; returns 0.1
    - TestDecay::test_weight_uses_utc: compute_weight rejects naive datetime input (raise TypeError or ValueError on naive `ts` or naive `now`)
    - TestDecay::test_recompute_bucket_weighted_count: 3 records of varying ages return sum of per-record weights, rounded to 2 decimals; records with status="superseded" are skipped (weight contribution = 0)
    - TestDecay::test_config_override: FeedbackStore(decay_window_days=90).compute_weight uses 90 not 180
  </behavior>
  <action>
    Create `agent/feedback_store.py` with `from __future__ import annotations` header (CLAUDE.md).

    Module docstring declares: single-process MVP assumption (no file locking — deferred per CONTEXT.md Deferred Ideas); FeedbackStore owns all feedback persistence under ~/.hermes/skills/.feedback/; bucket layout per CONTEXT.md storage layout decision.

    Define module-level constant `DEFAULT_DECAY_WINDOW_DAYS = 180`.

    Implement `compute_weight(ts: datetime, *, now: datetime | None = None, decay_window_days: int = DEFAULT_DECAY_WINDOW_DAYS) -> float`:
      - Validate `ts` is tz-aware (raise TypeError with clear message if naive — Pitfall #7). Same for `now` if provided.
      - now defaults to datetime.now(timezone.utc).
      - age_days = max(0.0, (now - ts).total_seconds() / 86400.0).
      - raw_weight = 1.0 - (age_days / decay_window_days).
      - return max(0.1, raw_weight).  # floor at 0.1 per CONTEXT.md

    Implement `recompute_bucket_weighted_count(records: list[FeedbackRecord], *, now: datetime | None = None, decay_window_days: int = DEFAULT_DECAY_WINDOW_DAYS) -> float`:
      - Sum compute_weight(r.ts, now=now, decay_window_days=decay_window_days) over records whose effective status is "active" (skip records marked superseded — read via getattr(r, "status", "active") default since FeedbackRecord doesn't have a status field; Plan 02 tracks status in index by_sha256, not on the record).
      - NOTE on implementation: recompute is called from FeedbackStore._recompute_bucket_weighted_count which already has access to the active-vs-superseded information from `by_sha256`. The free function here takes a pre-filtered active list OR uses getattr default. Document this.
      - Round result to 2 decimals (cleaner index.json reads).
      - Per RESEARCH Pattern 3 code example.

    Implement `class FeedbackStore:` with `__init__(self, *, hermes_home: Path | None = None, decay_window_days: int | None = None) -> None`:
      - Resolve `_root = (hermes_home or get_hermes_home()) / "skills" / ".feedback"`.
      - Set `_buckets_root = _root / "buckets"`, `_index_path = _root / "index.json"`, `_dedup_dir = _root / "dedup"`, `_dedup_registry_path = _dedup_dir / "sha256-registry.jsonl"`, `_archive_root = _root / "archive"`.
      - Resolve `_decay_window_days = decay_window_days if decay_window_days is not None else _load_decay_window_days_from_config()` (helper reads config.yaml `feedback.decay_window_days` or returns 180 default — see RESEARCH Example 4 _load_feedback_config pattern; tolerate missing config file with debug log).
      - Create directories lazily via `_buckets_root.mkdir(parents=True, exist_ok=True)` and same for `_dedup_dir`, `_archive_root`.
      - Initialize `self._migrated = False`.
      - Initialize `self._sha256_index: dict[str, dict] = {}` then call `self._load_sha256_index()` (reads `dedup/sha256-registry.jsonl` if it exists; tolerates missing file as empty dict). For Plan 01 the registry is empty by default — Plan 02 writes to it.
      - Call `self._maybe_migrate_phase28_incoming()` (lazy migration, gated by _migrated — see Task 2).
      - Call `self._load_or_init_index()` to populate `self._index: dict` in memory (load existing index.json OR write a default empty `{"version":1, "decay_window_days": ..., "updated_ts": now, "buckets": {}, "by_sha256": {}}` if missing).

    Implement `_make_record_id(self, record: FeedbackRecord) -> str` returning `f"{int(record.ts.timestamp() * 1_000_000)}_{record.output_snapshot.sha256[:8]}"` (per RESEARCH Example 2 + CONTEXT.md Claude's-discretion; sortable + collision-resistant).

    Implement `record_feedback(self, record: FeedbackRecord) -> str` (PLAN 01 VERSION — NO DEDUP BRANCH YET):
      - Compute record_id via `_make_record_id(record)`.
      - Compute bucket_key = f"{record.skill_id}:{record.source}:{record.verdict}" (colon separator per RESEARCH Pitfall #6 — skill_ids contain underscores; colon is illegal in Windows filenames but this is a JSON object key, not a filename).
      - Determine bucket_file = `_buckets_root / record.skill_id / f"{record.source}.jsonl"` (filename-per-source; verdicts mix in the same file — verdict is a record attribute, not a file split, because we want query(verdict=...) to scan one file not three).
      - Ensure parent dir exists: `bucket_file.parent.mkdir(parents=True, exist_ok=True)`.
      - Append record to bucket_file: `with open(bucket_file, "a", encoding="utf-8") as f: f.write(record.model_dump_json() + "\n"); f.flush(); os.fsync(f.fileno())` (per RESEARCH Pattern 1; encoding="utf-8" MANDATORY — Pitfall #1).
      - Append a line to `dedup/sha256-registry.jsonl`: `{"sha256": record.output_snapshot.sha256, "verdict": record.verdict, "record_id": record_id, "ts": record.ts.isoformat(), "status": "active"}`. Plan 01 writes here unconditionally; Plan 02 adds the dedup check before this append.
      - Update `self._sha256_index[sha] = {"record_id": record_id, "verdict": record.verdict, "ts": record.ts.isoformat(), "status": "active"}` in memory.
      - Read the bucket_file to gather all records for `_recompute_bucket_weighted_count` (needed because weighted_count depends on all records' ages, per RESEARCH Pattern 3 "index update strategy"). At v6 scale (~thousands per bucket) this is fast.
      - Update `self._index["buckets"][bucket_key]`: count = lines in bucket_file; weighted_count = recompute_bucket_weighted_count(active_records, decay_window_days=self._decay_window_days); first_ts = min ts; last_ts = max ts.
      - Update `self._index["by_sha256"][sha] = self._sha256_index[sha]`.
      - Update `self._index["updated_ts"] = datetime.now(timezone.utc).isoformat()`.
      - Call `self._write_index()` (atomic_json_write — temp + fsync + os.replace).
      - Return record_id.

      PLAN 01 DELIBERATELY OMITS the dedup/correction branch (STORE-04). Every call writes. Plan 02 inserts the dedup check between `_make_record_id` and the bucket append. Document this gap with a TODO comment referencing STORE-04 + Plan 02.

    Implement `_recompute_bucket_weighted_count(self, records: list[FeedbackRecord]) -> float`: thin wrapper around the module-level `recompute_bucket_weighted_count` that injects `self._decay_window_days`.

    Implement `_read_bucket_records(self, bucket_file: Path) -> list[FeedbackRecord]`: read each line, FeedbackRecord.model_validate_json(line). Tolerate empty file (return []). Skip blank lines. Log + skip malformed lines with warning (don't crash on corrupted bucket).

    Implement `_write_index(self) -> None`: `atomic_json_write(self._index_path, self._index, indent=2, sort_keys=True)`. (DO NOT pass ensure_ascii= — see Bug #1 in 28-01-SUMMARY; atomic_json_write hardcodes it.)

    Implement `_load_or_init_index(self) -> None`: if `_index_path.exists()`, json.loads(read_text(encoding="utf-8")); validate it's a dict; if dict has "version":1 set self._index = data. Otherwise write default and set self._index = default. Tolerate JSONDecodeError with warning + use default (RESEARCH Pitfall #5 — operator can rebuild_index from buckets in Plan 02).

    Implement `_load_sha256_index(self) -> None`: if `_dedup_registry_path.exists()`, read each line as JSON, populate `self._sha256_index[line["sha256"]] = {"record_id": ..., "verdict": ..., "ts": ..., "status": ...}`. Tolerate missing file (empty dict). Skip blank lines. Log + skip malformed lines.

    Implement `_load_decay_window_days_from_config() -> int` module-level function (per RESEARCH Example 4): try `from hermes_cli.config import load_config`; cfg = load_config(); fb = cfg.get("feedback") or {}; return int(fb.get("decay_window_days", DEFAULT_DECAY_WINDOW_DAYS)). On ANY exception (ImportError, OSError, ValueError, TypeError, KeyError) return DEFAULT_DECAY_WINDOW_DAYS with a debug log. Never crash the FeedbackStore init.

    Create `tests/agent/test_feedback_store.py` with `from __future__ import annotations`, imports for pytest, datetime, timezone, importlib. Define `feedback_env` fixture by REUSING the exact same pattern as `tests/agent/test_feedback_ingest.py:25-51` (monkeypatch HERMES_HOME + reload hermes_constants + reload agent.feedback_store — CRITICAL: must reload feedback_store AFTER hermes_constants so its get_hermes_home import rebinds). Provide a `_make_record` helper that returns a FeedbackRecord (reuse the same pattern as `tests/agent/test_feedback_ingest.py:_make_record` — but import FeedbackRecord + OutputSnapshot fresh). Provide a `_make_record_with_ts(ts, *, skill_id="screenplay", source="cli", verdict="good")` variant that allows explicit ts injection (needed for decay tests).

    Implement test classes per the <behavior> block above. Every test uses the feedback_env fixture. Decay tests use freezable datetime arithmetic (datetime.now(timezone.utc) minus timedelta(days=N)) — no freezegun needed.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_store.py::TestFeedbackStoreInit tests/agent/test_feedback_store.py::TestIndex tests/agent/test_feedback_store.py::TestDecay -x</automated>
  </verify>
  <done>
    - `agent/feedback_store.py` exists with FeedbackStore class, compute_weight, recompute_bucket_weighted_count, _load_decay_window_days_from_config
    - `tests/agent/test_feedback_store.py` exists with TestFeedbackStoreInit, TestIndex, TestDecay classes
    - compute_weight returns 1.0 at age=0, 0.5 at age=90/180, 0.1 floor at age>=180
    - Index schema has version, decay_window_days, updated_ts, buckets, by_sha256 keys
    - All targeted test classes green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: record_feedback + query + summary + get_record + lazy migration</name>
  <files>agent/feedback_store.py, tests/agent/test_feedback_store.py</files>
  <read_first>
    - agent/feedback_store.py (Task 1 result — extend with query/summary/get_record + _maybe_migrate_phase28_incoming)
    - agent/feedback_ingest.py:51-102 (Phase 28 write_feedback_record — the migration SOURCE path; understand filename pattern {skill_id}_{source}_{ts_compact}.json under incoming/)
    - agent/feedback_schema.py:184-229 (FeedbackRecord fields used by query filters)
    - .planning/phases/29-feedback-store/29-RESEARCH.md (Pattern 4 — supersession nuance for query filtering; Example 3 — lazy migration algorithm)
    - tests/agent/test_feedback_ingest.py:54-85 (_make_record helper to mirror in test_feedback_store.py)
  </read_first>
  <behavior>
    - TestRecordFeedback::test_bucket_layout: after record_feedback(screenplay, cli, good), the file buckets/screenplay/cli.jsonl exists and contains exactly 1 line
    - TestRecordFeedback::test_append_only_semantics: write 3 records to the same bucket; assert the file has 3 lines and the first line's bytes are unchanged (read before and after the 3rd write)
    - TestRecordFeedback::test_returns_record_id_format: returned record_id matches r"^\d{16}_[0-9a-f]{8}$" (microsecond ts + 8-hex sha256 prefix)
    - TestRecordFeedback::test_bucket_key_uses_colon_separator: index.json["buckets"] has key "screenplay:cli:good" not "screenplay_cli_good"
    - TestRecordFeedback::test_fsync_on_append: hard to test directly; verify the file exists and is readable immediately after record_feedback returns (no flush delay)
    - TestRecordFeedback::test_index_updated_atomic: after record_feedback, index.json mtime is newer than before (atomic rewrite happened)
    - TestRecordFeedback::test_weighted_count_differs_from_count_for_old_record: write a record with ts=now-100days; index bucket has count=1, weighted_count < 1.0 (between 0.1 and 0.5)
    - TestRecordFeedback::test_weighted_count_equals_count_for_fresh_record: write a record with ts=now; count=1, weighted_count ~= 1.0 (within 0.01)
    - TestRecordFeedback::test_multiple_verdicts_same_bucket_file: write good + needs_work to screenplay/cli; assert buckets/screenplay/cli.jsonl has 2 lines (verdict is a record attribute, not a file split)
    - TestQuery::test_query_by_skill_id: write records for screenplay + drawer; query(skill_id="screenplay") returns only screenplay records
    - TestQuery::test_query_by_source: query(skill_id="screenplay", source="kais_aigc") returns only kais_aigc records
    - TestQuery::test_query_by_verdict: query(skill_id="screenplay", verdict="needs_work") returns only needs_work records
    - TestQuery::test_query_composed_filters: query(skill_id=..., source=..., verdict=..., since=..., until=...) returns records matching ALL filters
    - TestQuery::test_query_since_until: write 3 records with ts=t0, t1, t2; query(since=t1) returns 2; query(until=t1) returns 2; query(since=t1, until=t2) returns 2
    - TestQuery::test_query_returns_pydantic_instances: each result is isinstance(result, FeedbackRecord)
    - TestQuery::test_query_empty_bucket: query on a skill_id with no records returns []
    - TestQuery::test_query_include_superseded_default_false: (Plan 01 stub — superseded records don't exist yet; default behavior returns all; document Plan 02 will tighten this)
    - TestSummary::test_summary_returns_per_bucket_counts: write 3 good + 2 needs_work to screenplay/cli; summary() returns dict with keys "screenplay:cli:good" (count=3) and "screenplay:cli:needs_work" (count=2)
    - TestSummary::test_summary_weighted_count: summary bucket dict has weighted_count field populated (not 0)
    - TestSummary::test_summary_first_last_ts: each bucket has first_ts (earliest record ts ISO) and last_ts (latest)
    - TestSummary::test_summary_filter_by_skill_id: summary(skill_id="screenplay") returns only screenplay:* buckets
    - TestSummary::test_summary_filter_by_source: summary(source="cli") returns only *:cli:* buckets
    - TestSummary::test_summary_empty_store: summary() on empty FeedbackStore returns {}
    - TestGetRecord::test_get_record_returns_match: write a record, capture returned record_id; get_record(record_id) returns a FeedbackRecord with the same ts + sha256
    - TestGetRecord::test_get_record_unknown_returns_none: get_record("nonexistent") returns None
    - TestMigration::test_migrate_incoming_routes_to_buckets: pre-populate incoming/{skill_id}_{source}_{ts}.json (Phase 28 layout) with 1 FeedbackRecord; on FeedbackStore.__init__, the record lands in buckets/{skill_id}/{source}.jsonl
    - TestMigration::test_migration_idempotent: run FeedbackStore.__init__ twice; second init finds incoming/ empty (no-op)
    - TestMigration::test_migration_archives_originals: after migration, archive/phase-28-migration/ contains the original incoming/*.json filenames
    - TestMigration::test_migration_no_incoming_noop: FeedbackStore.__init__ with empty incoming/ creates no archive/phase-28-migration/ files
    - TestMigration::test_migration_handles_malformed: incoming/ has 1 valid + 1 malformed JSON file; migration succeeds on the valid one, leaves the malformed in incoming/ with a warning log (do not crash init)
    - TestMigration::test_migration_partial_recovery: simulate partial migration (incoming/ has 2 files, archive/ has 1 of them already); second init migrates the remaining file without duplicating
    - TestConfig::test_config_override_decay_window: monkeypatch config loader to return {"feedback": {"decay_window_days": 90}}; FeedbackStore() uses 90 (verified by weighted_count math on a 60-day-old record returning ~0.33 not ~0.67)
    - TestConfig::test_config_missing_uses_default: no config.yaml or missing feedback section; FeedbackStore uses 180
    - TestConfig::test_config_invalid_value_uses_default: config returns {"feedback": {"decay_window_days": "not-a-number"}}; FeedbackStore uses 180 (does not crash)
  </behavior>
  <action>
    Extend `agent/feedback_store.py` (created in Task 1) with the remaining FeedbackStore methods. Continue using `from __future__ import annotations`, encoding="utf-8" on every open(), specific exception types, lazy %-logging.

    Implement `query(self, *, skill_id: str | None = None, source: str | None = None, verdict: str | None = None, since: datetime | None = None, until: datetime | None = None, include_superseded: bool = False) -> list[FeedbackRecord]`:
      - Determine which bucket files to scan: if skill_id and source given, scan one file `_buckets_root / skill_id / f"{source}.jsonl"`. If only skill_id, scan `_buckets_root / skill_id /*.jsonl`. If only source, scan all `_buckets_root / * / f"{source}.jsonl`. If neither, scan all `_buckets_root / * /*.jsonl`.
      - For each bucket file, call `_read_bucket_records(path)`.
      - Apply filters: skill_id (record.skill_id == skill_id), source (record.source == source), verdict (record.verdict == verdict), since (record.ts >= since), until (record.ts <= until). Use the in-memory `self._index["by_sha256"]` to determine each record's status; if `include_superseded=False` and the record's sha256 entry has status="superseded", skip. (Plan 01 has no superseded records yet — this branch never fires; Plan 02 activates it.)
      - Return list[FeedbackRecord]. Order: by ts ascending (stable for downstream consumers).
      - FileNotFoundError on missing bucket dir → return [] (empty result, not an error).

    Implement `summary(self, *, skill_id: str | None = None, source: str | None = None) -> dict[str, dict]`:
      - Iterate `self._index["buckets"].items()`. Parse each key as `<skill_id>:<source>:<verdict>` (split on ":"). Apply filters. Return filtered dict mapping bucket_key → {"count", "weighted_count", "first_ts", "last_ts"}.
      - NOTE: this does NOT recompute weighted_count — it returns what's in the index (which was computed at the last record_feedback call). Decay-weighted values are "as of last write" not "as of now". Document this in the docstring; Plan 02 adds `rebuild-index` to refresh.

    Implement `get_record(self, record_id: str) -> FeedbackRecord | None`:
      - Scan all bucket files under `_buckets_root`. For each line, parse FeedbackRecord and check if a derived record_id matches. Return first match.
      - record_id derivation: re-compute `f"{int(r.ts.timestamp() * 1_000_000)}_{r.output_snapshot.sha256[:8]}"` for each record; compare to the target.
      - Return None if no match across all bucket files.
      - At v6 scale (~thousands of records) this linear scan is acceptable (CONTEXT.md A4). For >100K records a future phase can add an index.

    Implement `bucket_path_for(self, record: FeedbackRecord) -> Path` (public helper for Plan 02's write_feedback_record wrapper to return the bucket path to Phase 28 callers):
      - Returns `self._buckets_root / record.skill_id / f"{record.source}.jsonl"`.

    Implement `_maybe_migrate_phase28_incoming(self) -> None` (lazy migration, RESEARCH Example 3):
      - Idempotent guard: `if self._migrated: return`.
      - Set `self._migrated = True` BEFORE work begins (so a crash mid-migration doesn't loop forever — but use a `.migration-in-progress` sentinel file for crash recovery, see below).
      - Check `incoming_dir = self._root / "incoming"`. If not a dir, return. (mkdir parents=True exist_ok=True to be safe.)
      - `pending = sorted(incoming_dir.glob("*.json"))`. If empty, return.
      - Create `archive_dir = self._archive_root / "phase-28-migration"` via mkdir(parents=True, exist_ok=True).
      - Create sentinel `sentinel = self._root / ".migration-in-progress"` via `sentinel.touch(exist_ok=True)`.
      - For each src_path in pending: try `raw = json.loads(src_path.read_text(encoding="utf-8"))`; `record = FeedbackRecord(**raw)`; `self.record_feedback(record)` (uses normal write path — dedup may catch a partial-migration duplicate, which is fine); then `src_path.rename(archive_dir / src_path.name)` (move original for audit). Increment migrated count. Catch broad Exception per-file (logger.warning "phase-28 migration skipped %s: %s", src_path.name, exc) and continue (do not crash init — RESEARCH Pitfall #4).
      - Remove sentinel via `sentinel.unlink(missing_ok=True)`.
      - If migrated > 0, logger.info "phase-28 feedback migration: moved N records from incoming/ to buckets/".
      - ORDER MATTERS (RESEARCH Pitfall #4): append-first, then move. If append succeeds but move fails, next init re-appends (harmless duplicate — dedup catches it in Plan 02) and retries the move.

    Extend `tests/agent/test_feedback_store.py` with TestRecordFeedback, TestQuery, TestSummary, TestGetRecord, TestMigration, TestConfig test classes per the <behavior> block. Reuse feedback_env fixture from Task 1.

    For TestMigration: pre-populate incoming/ by directly writing a JSON file (using atomic_json_write or Path.write_text(encoding="utf-8")) BEFORE instantiating FeedbackStore. The fixture may need to yield the home + a helper to write incoming files. Pattern:
      - In feedback_env, after creating home + skills/.feedback/, also pre-create skills/.feedback/incoming/ if a test param requests it.
      - OR: tests directly call Path(home / "skills" / ".feedback" / "incoming").mkdir(parents=True, exist_ok=True) then write files, THEN instantiate FeedbackStore(home=home).

    For TestMigration::test_migration_partial_recovery: write 2 files to incoming/, manually move one to archive/phase-28-migration/ (simulating partial migration), then instantiate FeedbackStore — it should migrate only the remaining file.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_store.py -x</automated>
  </verify>
  <done>
    - FeedbackStore has record_feedback / query / summary / get_record / bucket_path_for / _maybe_migrate_phase28_incoming
    - record_feedback appends to buckets/<skill_id>/<source>.jsonl (verified by test_bucket_layout + test_append_only_semantics)
    - record_feedback updates index.json atomically (test_index_updated_atomic)
    - weighted_count differs from count for old records (test_weighted_count_differs_from_count_for_old_record)
    - query() filters by skill_id/source/verdict/since/until correctly
    - summary() returns per-bucket count/weighted_count/first_ts/last_ts
    - get_record returns matching record or None
    - Lazy migration routes Phase 28 incoming/*.json to buckets/ + archives originals (test_migrate_incoming_routes_to_buckets + test_migration_idempotent + test_migration_archives_originals)
    - Migration handles malformed files gracefully (test_migration_handles_malformed)
    - Config override works (test_config_override_decay_window)
    - All TestRecordFeedback + TestQuery + TestSummary + TestGetRecord + TestMigration + TestConfig green
  </done>
</task>

<task type="auto">
  <name>Task 3: cli-config.yaml.example feedback section + final regression sweep</name>
  <files>cli-config.yaml.example</files>
  <read_first>
    - cli-config.yaml.example:490-515 (memory: section style template for the new feedback: block)
    - cli-config.yaml.example (skim entire file for section header convention — uses "# ====" dividers and "# Title" commentary)
    - agent/feedback_store.py (Task 1+2 result — confirm _load_decay_window_days_from_config reads "feedback" key)
  </read_first>
  <action>
    Add a new top-level `feedback:` section to `cli-config.yaml.example` documenting the `decay_window_days` config key. Place it immediately AFTER the `memory:` section (around line 515, before the next `# ====` divider). Use the same comment-block style as the `memory:` section (header divider + explanatory paragraph + indented key with default + inline comment explaining the math).

    The block MUST contain:
      - Divider: `# =============================================================================`
      - Title: `# Feedback Store (v6.0 Self-Evolution)`
      - Divider: `# =============================================================================`
      - One paragraph explaining: feedback records persist under ~/.hermes/skills/.feedback/ in skill_id/source/ buckets after Phase 29; the decay window controls how fast old feedback loses weight in the index.
      - `feedback:` top-level key
      - `decay_window_days: 180` with inline comment: `# Default 180 days (6 months). weight = max(0.1, 1.0 - age_days/decay_window_days). Operators tune + restart Hermes.`

    Per RESEARCH Example 4 (Code Example 4 block), match the exact key path `feedback.decay_window_days` (top-level section, not nested under skills:).

    Do NOT modify any other line in cli-config.yaml.example. This is a documentation-only addition.

    After this task, run the full feedback test suite to confirm nothing regressed:
      `python -m pytest tests/agent/test_feedback_schema.py tests/agent/test_feedback_snapshot.py tests/agent/test_feedback_ingest.py tests/agent/test_feedback_store.py -x`
    All 45 (Phase 28 Plan 01) + 14 (Phase 28 Plan 02 ingest) + 31 (Phase 28 Plan 02 CLI) + N (Phase 29 Plan 01) tests must pass.

    Run `ruff check agent/feedback_store.py` — must pass (specifically PLW1514 — every open() must have encoding="utf-8").

    Run FOUND-08 preservation check:
      `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l`
    Must return 0 (Plan 01 touches no bundled SKILL.md).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_schema.py tests/agent/test_feedback_snapshot.py tests/agent/test_feedback_ingest.py tests/agent/test_feedback_store.py -x && ruff check agent/feedback_store.py && bash -c "test $(git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l) -eq 0"</automated>
  </verify>
  <done>
    - cli-config.yaml.example has feedback: section with decay_window_days: 180 documented
    - All Phase 28 + Phase 29 Plan 01 tests green
    - ruff check agent/feedback_store.py clean (PLW1514 satisfied)
    - FOUND-08 preserved (zero bundled SKILL.md or refs changes vs v5.0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem → FeedbackStore | Feedback records cross from Phase 28 writers (/feedback CLI, kais watcher, JSONL importer) into the durable store via record_feedback. Inputs are already Pydantic-validated by Phase 28 before reaching us. |
| FeedbackStore → index.json (P32 Curator + P33 Dashboard consumers) | The index file is the single source of truth (STORE-02 / SC-2). Consumers trust its counts and weighted_counts. Tampering or corruption here propagates to learning decisions. |
| Phase 28 incoming/ → buckets/ (lazy migration) | Migration reads JSON files written by Phase 28. A malformed file (truncated write, encoding error) must not crash FeedbackStore.__init__. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | index.json atomic write | mitigate | Use `utils.atomic_json_write` (temp + fsync + os.replace). Crash mid-write leaves previous version intact. (utils.py:111-178) |
| T-29-02 | Information disclosure | bucket file mode (open with "a" creates 0o644) | accept | v6 assumes trusted single-operator environment (CONTEXT.md deferred concurrent-access + PII redaction). Operator can chmod if multi-user. |
| T-29-03 | Tampering | Path traversal during Phase 28 migration | mitigate | Migration target paths derive ONLY from validated `record.skill_id` + `record.source` (Pydantic-validated against known experts + Literal enum). The source filename is NEVER used to compute the destination. (Phase 28 already validates; RESEARCH Pattern 4 path-traversal note) |
| T-29-04 | Denial of Service | FeedbackStore.__init__ crash on malformed incoming/*.json | mitigate | _maybe_migrate_phase28_incoming catches broad Exception per-file (logger.warning) and continues. A single malformed file does not abort init. (RESEARCH Pitfall #4) |
| T-29-05 | Tampering | Race condition: two concurrent FeedbackStore instances | accept | v6 MVP is single-process (CONTEXT.md Deferred Ideas + RESEARCH Pitfall #3). Documented in module docstring. Worst case is a duplicate record that Plan 02's dedup catches or a future `dedup-repair` cleans up. |
| T-29-06 | Information disclosure | Encoding default (cp1252 on Windows corrupts Chinese text) | mitigate | Every `open()` passes `encoding="utf-8"` (Ruff PLW1514 enforced). CLAUDE.md mandate. (RESEARCH Pitfall #1) |
| T-29-07 | Tampering | Timezone-naive datetime in decay math | mitigate | compute_weight validates `ts` and `now` are tz-aware (raises TypeError on naive). FeedbackRecord.ts validator already rejects naive; internal now uses datetime.now(timezone.utc). (RESEARCH Pitfall #7) |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 01 installs ZERO packages (all stdlib + existing utils + existing Pydantic). No supply-chain risk. |
</threat_model>

<verification>
## Plan 01 Verification

1. `python -m pytest tests/agent/test_feedback_store.py -x` — all unit tests green (TestFeedbackStoreInit + TestIndex + TestDecay + TestRecordFeedback + TestQuery + TestSummary + TestGetRecord + TestMigration + TestConfig)
2. `ruff check agent/feedback_store.py` — PLW1514 satisfied (every open() has encoding=)
3. `python -c "from agent.feedback_store import FeedbackStore, compute_weight; print(compute_weight.__doc__[:80])"` — importable
4. `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` returns 0 (FOUND-08 preserved)
5. `grep -c "^feedback:" cli-config.yaml.example` returns 1 (config section added)
6. `python -m pytest tests/agent/test_feedback_ingest.py -x` — Phase 28 ingest tests still green (Plan 01 added a new module but did not modify feedback_ingest.py — regression check confirms zero impact)
</verification>

<success_criteria>
- agent/feedback_store.py exists with FeedbackStore class exposing record_feedback / query / summary / get_record / bucket_path_for
- compute_weight implements linear max(0.1, 1.0 - age_days/180) per CONTEXT.md D-STORE-03
- index.json schema matches RESEARCH Pattern 2 (version=1, decay_window_days, updated_ts, buckets{}, by_sha256{})
- Bucket layout is buckets/<skill_id>/<source>.jsonl with O(1) append per write (no full-file rewrite)
- Lazy migration from Phase 28 incoming/ is idempotent + archives originals
- decay_window_days configurable via config.yaml feedback.decay_window_days
- All Phase 28 feedback tests still pass (zero regression)
- FOUND-08 preserved (no bundled SKILL.md changes)
- Ruff PLW1514 clean on agent/feedback_store.py
</success_criteria>

<output>
Create `.planning/phases/29-feedback-store/29-01-SUMMARY.md` when done.
</output>

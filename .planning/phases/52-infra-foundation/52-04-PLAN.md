---
phase: 52-infra-foundation
plan: 04
type: execute
wave: 3
depends_on: ["52-03"]
files_modified:
  - agent/round_table_executor.py
  - mcp_serve.py
  - tests/agent/test_round_table_executor.py
autonomous: true
requirements: [INFRA-04]
tags: [serial-execution, asyncio-lock, concurrency, glm-compat]

must_haves:
  truths:
    - "A per-roundId asyncio.Lock serializes get_agent_opinion calls so concurrent calls for the same roundId are REJECTED (not queued)"
    - "The first get_agent_opinion call acquires the lock; the second concurrent call for the same roundId receives a 429 serial_violation response"
    - "The 429 response message contains the literal substring 'feedback-glm-overload-reduce-concurrency.md' (cites MEMORY.md policy)"
    - "After the first call releases the lock, a subsequent call proceeds normally (lock is not stuck)"
    - "Single sequential submissions complete successfully and return the panelist opinion"
    - "Different roundIds use different locks (no false-positive cross-round rejection)"
  artifacts:
    - path: "agent/round_table_executor.py"
      provides: "Per-roundId asyncio.Lock registry + acquire_round_or_reject + _serial_violation_response"
      exports: ["acquire_round_or_reject", "release_round_lock", "_serial_violation_response"]
    - path: "mcp_serve.py"
      provides: "get_agent_opinion closure modified to acquire per-roundId lock before turn append"
      contains: "acquire_round_or_reject"
    - path: "tests/agent/test_round_table_executor.py"
      provides: "3 async unit tests covering concurrent reject / sequential success / MEMORY.md citation substring"
  key_links:
    - from: "mcp_serve.py::get_agent_opinion"
      to: "agent.round_table_executor.acquire_round_or_reject"
      via: "lock = await acquire_round_or_reject(round_id); if lock is None: return _serial_violation_response"
      pattern: "acquire_round_or_reject"
    - from: "agent/round_table_executor.py::_serial_violation_response"
      to: "MEMORY.md feedback-glm-overload-reduce-concurrency.md"
      via: "literal substring citation in 429 response message"
      pattern: "feedback-glm-overload-reduce-concurrency\\.md"
---

<objective>
Enforce the hard serial-execution constraint (INFRA-04) by wrapping `get_agent_opinion` in a per-`roundId` `asyncio.Lock`. Concurrent calls for the same roundId are rejected with 429 + MEMORY.md citation (NOT queued — queueing would violate the GLM 4-key rotation compatibility constraint).

Purpose: This is the GLM concurrency safety layer that makes the round-table runtime compatible with MEMORY.md `feedback-glm-overload-reduce-concurrency.md` (global concurrency==1 by design). Without INFRA-04, concurrent MCP tool calls would race for the GLM API and trip the 4-key rotation ceiling.

Output: `agent/round_table_executor.py` (async lock registry) + `mcp_serve.py` modification (get_agent_opinion acquires the lock) + 3 async unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-infra-foundation/52-CONTEXT.md
@.planning/phases/52-infra-foundation/52-RESEARCH.md
@.planning/phases/52-infra-foundation/52-PATTERNS.md
@.planning/phases/52-infra-foundation/52-VALIDATION.md

<interfaces>
<!-- Phase 52-03 produces (consume this): -->
From mcp_serve.py:
- async def get_agent_opinion(round_id, project_slug, agent_id, topic, panel_context=None) -> str
  (currently appends a placeholder Turn via round_table_state.append_turn — Phase 52-04 wraps it with the lock)

<!-- MEMORY.md policy citation (load-bearing for SC#4): -->
The 429 response message MUST contain literal substring "feedback-glm-overload-reduce-concurrency.md".
This references the user-memory policy file at:
/home/kai/.claude/projects/-data-workspace-hermes-agent/memory/feedback-glm-overload-reduce-concurrency.md
which locks global concurrency==1 by design.

<!-- Distinct from agent/glm_concurrency_guard.py: -->
- glm_concurrency_guard uses threading.Semaphore (for blocking SDK calls in ThreadPoolExecutor workers)
- round_table_executor uses asyncio.Lock (for in-event-loop tool entry serialization)
- They COMPOSE — get_agent_opinion acquires asyncio.Lock first, then the LLM call inside (Phase 53) goes through acquire_glm_slot
- DO NOT collapse these two layers (RESEARCH.md "Don't Hand-Roll" table + agent/glm_concurrency_guard.py:11-15 docstring)

<!-- Asyncio TOCTOU note (Pitfall #2 in RESEARCH.md): -->
- check-then-acquire pattern (`if lock.locked(): return 429; async with lock: ...`) is safe under asyncio
  cooperative scheduling AS LONG AS no `await` happens between the check and the `async with` entry
- Use `await lock.acquire()` after the check (not `async with lock`) so the lock can be released in a finally block
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_round_table_executor.py covering INFRA-04 SC#4 (3 async tests: concurrent reject / sequential success / MEMORY.md citation substring)</name>
  <files>tests/agent/test_round_table_executor.py</files>
  <read_first>
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"tests/agent/test_round_table_executor.py" (lines 572-604) — async test pattern + @pytest.mark.asyncio requirement
    - .planning/phases/52-infra-foundation/52-RESEARCH.md §"Per-roundId Async Lock Pattern" (lines 262-301) — canonical implementation skeleton + TOCTOU discussion
    - tests/test_trajectory_compressor.py:495-499 (canonical @pytest.mark.asyncio marker pattern — pytest-asyncio mode is NOT auto)
    - .planning/phases/52-infra-foundation/52-CONTEXT.md §"Resolved by Kai" point 4 (per-roundId asyncio.Lock; concurrent get_agent_opinion → 429 with feedback-glm-overload-reduce-concurrency.md substring)
  </read_first>
  <behavior>
    - test_concurrent_submission_rejected: acquire lock for roundId="round-1"; second call to acquire_round_or_reject("round-1") returns None; first lock.release() succeeds
    - test_sequential_submission_succeeds: acquire lock for roundId="round-2"; release; acquire again for roundId="round-2" — succeeds (lock is not stuck)
    - test_429_message_cites_memory_md: call _serial_violation_response("round-x"); assert literal substring "feedback-glm-overload-reduce-concurrency.md" in the response; assert "429" appears in the response; assert "serial_violation" error code appears
    - test_different_roundids_use_different_locks: acquire roundId="a"; acquire roundId="b" — both succeed (no false-positive cross-round rejection)
  </behavior>
  <action>
    Create tests/agent/test_round_table_executor.py with `class TestSerialEnforcement:`.

    Header: `from __future__ import annotations`, `import asyncio`, `import json`, `import pytest`. Each async test method marked with `@pytest.mark.asyncio` (explicit marker — pytest-asyncio mode is NOT auto, per pyproject.toml).

    Test bodies import from agent.round_table_executor (Task 2 builds it): `from agent.round_table_executor import acquire_round_or_reject, release_round_lock, _serial_violation_response`.

    For test_concurrent_submission_rejected: call `lock1 = await acquire_round_or_reject("round-1")`; assert lock1 is not None; call `lock2 = await acquire_round_or_reject("round-1")` (no release between); assert lock2 is None; `await release_round_lock("round-1")` (or `lock1.release()`).

    For test_sequential_submission_succeeds: acquire → release → acquire same roundId; second acquire must succeed (lock is reusable).

    For test_429_message_cites_memory_md: response_str = _serial_violation_response("round-x"); assert `"feedback-glm-overload-reduce-concurrency.md"` literal substring in response_str; assert `"429"` substring; assert `"serial_violation"` substring.

    For test_different_roundids_use_different_locks: acquire "a" + acquire "b" both succeed; release both.

    These tests are RED until Task 2 lands.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_round_table_executor.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/agent/test_round_table_executor.py exists
    - Every async test method has `@pytest.mark.asyncio` decorator above it (grep count must match test method count)
    - Class TestSerialEnforcement has 4 test methods (concurrent reject / sequential success / 429 message / different roundIds)
    - test_429_message_cites_memory_md body asserts literal substring "feedback-glm-overload-reduce-concurrency.md"
    - pytest --collect-only exits 0
  </acceptance_criteria>
  <done>
    - test file collects cleanly
    - Tests RED until Task 2 lands agent/round_table_executor.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent/round_table_executor.py — per-roundId asyncio.Lock registry + acquire/release + _serial_violation_response</name>
  <files>agent/round_table_executor.py</files>
  <read_first>
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"agent/round_table_executor.py" (lines 240-318) — verbatim asyncio.Lock pattern + NEGATIVE example from glm_concurrency_guard
    - .planning/phases/52-infra-foundation/52-RESEARCH.md §Pattern 2 "Per-roundId Async Lock Pattern" (lines 262-301) — canonical idiom + TOCTOU safety discussion
    - .planning/phases/52-infra-foundation/52-RESEARCH.md §Pitfalls #1 + #2 (lines 415-446) — asyncio.Lock vs threading.Lock confusion + TOCTOU race window discussion
    - agent/glm_concurrency_guard.py:11-15 (NEGATIVE example docstring — explains why glm guard uses threading.Semaphore for blocking SDK calls; this module's asyncio.Lock is a DIFFERENT layer)
    - gateway/session_context.py:39-83 (canonical contextvars pattern — referenced for code style, NOT directly used in this module)
  </read_first>
  <behavior>
    - acquire_round_or_reject(round_id) async function: returns the asyncio.Lock on success, None if contended
    - Two concurrent calls for the same roundId: first acquires, second returns None (NOT queue, NOT block)
    - Sequential calls (acquire → release → acquire) for the same roundId: both succeed
    - Different roundIds use independent locks (no cross-round false positives)
    - _serial_violation_response(round_id) returns a JSON string containing literal substring "feedback-glm-overload-reduce-concurrency.md", status code "429", error code "serial_violation"
    - release_round_lock(round_id) async function releases the lock (or lock.release() works directly)
  </behavior>
  <action>
    Create agent/round_table_executor.py with the canonical idiom from RESEARCH.md §Pattern 2 (lines 262-301) + PATTERNS.md §"agent/round_table_executor.py" (lines 244-289). Public exports:

    1. Module header: `from __future__ import annotations`, `import asyncio`, `import json`, `import logging`, `from typing import Optional`. Module logger: `logger = logging.getLogger(__name__)`.

    2. Module-level lock registry: `_ROUND_LOCKS: dict[str, asyncio.Lock] = {}` + `_ROUND_LOCKS_GUARD = asyncio.Lock()`.

    3. `async def _get_or_create_round_lock(round_id: str) -> asyncio.Lock:` — `async with _ROUND_LOCKS_GUARD: if round_id not in _ROUND_LOCKS: _ROUND_LOCKS[round_id] = asyncio.Lock(); return _ROUND_LOCKS[round_id]`.

    4. `def _serial_violation_response(round_id: str) -> str:` — Return `json.dumps({"error": "serial_violation", "status": 429, "round_id": round_id, "message": "Concurrent get_agent_opinion for the same roundId is rejected. Hermes enforces 1 panelist 1 turn sequential execution by design (global concurrency==1; GLM 4-key rotation compatible). See MEMORY.md feedback-glm-overload-reduce-concurrency.md."}, indent=2)`. The literal substring "feedback-glm-overload-reduce-concurrency.md" is load-bearing (SC#4 acceptance).

    5. `async def acquire_round_or_reject(round_id: str) -> Optional[asyncio.Lock]:` — `lock = await _get_or_create_round_lock(round_id)`. Check-then-acquire with no `await` between (per Pitfall #2 TOCTOU discussion — safe under asyncio cooperative scheduling): `if lock.locked(): logger.info("round_table_executor: rejected concurrent acquire for roundId=%s", round_id); return None`. `await lock.acquire()`. Return lock.

    6. `async def release_round_lock(round_id: str) -> None:` — `lock = _ROUND_LOCKS.get(round_id)`. If lock is None, log warning and return. If not lock.locked(), log warning and return. Otherwise `lock.release()`. (Note: direct `lock.release()` from caller also works — this helper is for symmetry.)

    7. Module docstring must explicitly cite: (a) MEMORY.md `feedback-glm-overload-reduce-concurrency.md` as policy root, (b) agent/glm_concurrency_guard.py as the DISTINCT threading.Semaphore layer (these compose, do not collapse), (c) SC#4 of ROADMAP Phase 52 as acceptance contract.

    NO `import run_agent`. NO `Path.home() / ".hermes"`. All log calls use lazy %-formatting.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_round_table_executor.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - agent/round_table_executor.py exists
    - `.venv/bin/python -c "from agent.round_table_executor import acquire_round_or_reject, release_round_lock, _serial_violation_response; print('ok')"` prints "ok"
    - _serial_violation_response return value contains literal substring "feedback-glm-overload-reduce-concurrency.md" (assert via `in` operator)
    - _serial_violation_response return value contains "429" and "serial_violation"
    - All 4 async tests in test_round_table_executor.py pass
    - test_concurrent_submission_rejected verifies second acquire returns None (NOT a queue wait, NOT a block)
    - test_different_roundids_use_different_locks passes — independent roundIds acquire concurrently
    - Module docstring cites feedback-glm-overload-reduce-concurrency.md and references glm_concurrency_guard.py as the distinct layer
    - ruff check agent/round_table_executor.py exits 0
    - File uses asyncio.Lock (NOT threading.Lock / threading.Semaphore — grep "threading" returns nothing)
  </acceptance_criteria>
  <done>
    - agent/round_table_executor.py ships with the 3 listed exports
    - All async tests pass
    - asyncio.Lock primitive confirmed (not threading)
    - 429 message cites MEMORY.md policy file by literal name
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the lock into mcp_serve.py::get_agent_opinion — acquire before append_turn, release in finally</name>
  <files>mcp_serve.py</files>
  <read_first>
    - mcp_serve.py get_agent_opinion closure (from Phase 52-03 Task 3 — currently calls append_turn without any locking)
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"Concrete new tool skeleton — get_agent_opinion (async, lock-guarded)" (lines 449-481) — canonical lock-guarded form
    - agent/round_table_executor.py (Task 2 output — acquire_round_or_reject, release_round_lock, _serial_violation_response)
  </read_first>
  <behavior>
    - get_agent_opinion FIRST calls acquire_round_or_reject(round_id); if it returns None, returns the _serial_violation_response(round_id) immediately
    - On successful acquire, the closure proceeds with read_and_recover_state + append_turn as before
    - Lock is released in a `finally:` block (exception-safe — even on error paths, the lock is released so subsequent calls for the same roundId can proceed)
    - Test test_lifecycle_round_trip (from Phase 52-03) still passes — the lock acquire/release does not break the happy path
    - A new integration test in test_round_table_executor.py or test_mcp_serve_round_table.py verifies the 429 path is reachable via the MCP tool surface
  </behavior>
  <action>
    Modify mcp_serve.py's `get_agent_opinion` closure (added in Phase 52-03 Task 3):

    Insert at the very top of the closure body (after the docstring, before any state_path construction): lazy `from agent.round_table_executor import acquire_round_or_reject, release_round_lock, _serial_violation_response`. Then `lock = await acquire_round_or_reject(round_id)`. `if lock is None: return _serial_violation_response(round_id)`. Wrap the rest of the closure body (state_path construction, read_and_recover_state call, append_turn call, return JSON) in a `try:` block, followed by `finally: await release_round_lock(round_id)` (or `lock.release()` — both work).

    The lock must be released even if read_and_recover_state or append_turn raises. Failure to release would permanently block that roundId.

    Add one new async test in tests/agent/test_mcp_serve_round_table.py (or in tests/agent/test_round_table_executor.py — planner's discretion) verifying: invoke get_agent_opinion via the MCP tool surface twice concurrently (asyncio.gather); the first call's response has "status": "ok"; the second call's response has "status": 429 and literal substring "feedback-glm-overload-reduce-concurrency.md".

    All existing tests must continue to pass — the lock wiring does not regress Phase 52-03's lifecycle round trip test.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_round_table_executor.py tests/agent/test_mcp_serve_round_table.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - mcp_serve.py get_agent_opinion closure body starts with `from agent.round_table_executor import acquire_round_or_reject` (lazy import inside the closure)
    - The closure body contains `lock = await acquire_round_or_reject(round_id)` and `if lock is None: return _serial_violation_response(round_id)`
    - The closure body has a try/finally structure where the finally block releases the lock
    - All tests in tests/agent/test_round_table_executor.py pass (4 async tests)
    - All tests in tests/agent/test_mcp_serve_round_table.py pass (including the new concurrent-rejection integration test)
    - The lifecycle round trip test (from Phase 52-03) still passes — happy path not broken
    - ruff check mcp_serve.py exits 0
  </acceptance_criteria>
  <done>
    - get_agent_opinion is lock-guarded
    - Concurrent calls for same roundId: first succeeds, second gets 429 + MEMORY.md citation
    - Lock released in finally (exception-safe)
    - All prior tests still pass (no regression)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multiple concurrent MCP tool invocations ↔ shared per-roundId lock | The asyncio.Lock is the single in-process serialization point; bypassing it (e.g. by calling append_turn directly from another code path) would defeat SC#4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-14 | Tampering | Race condition: concurrent turn append corrupts state file | mitigate | asyncio.Lock serializes the entire get_agent_opinion closure body — no concurrent appends possible. atomic_json_write adds defense-in-depth. |
| T-52-15 | DoS | Lock never released (executor crash before finally) | mitigate | `try: ... finally: await release_round_lock(round_id)` guarantees release even on exception. asyncio.CancelledError also triggers finally. |
| T-52-16 | DoS | _ROUND_LOCKS dict growing unboundedly (one entry per roundId, never cleaned) | accept | PoC scope: ≤10 round tables total. v12+ adds periodic GC of completed/aborted roundIds. |
| T-52-17 | Repudiation | Operator cannot tell which caller won the lock race | accept | PoC runs single-user via stdio MCP; no auth in scope. Logging at INFO level records winner/loser roundId. |
| T-52-SC | Tampering | (No package installs) | N/A | N/A |
</threat_model>

<verification>
- All 4 async unit tests pass: `.venv/bin/python -m pytest tests/agent/test_round_table_executor.py -x -v`
- Integration: lifecycle round trip + concurrent rejection pass: `.venv/bin/python -m pytest tests/agent/test_mcp_serve_round_table.py -x -v`
- 429 message substring check: `.venv/bin/python -c "from agent.round_table_executor import _serial_violation_response; r = _serial_violation_response('x'); assert 'feedback-glm-overload-reduce-concurrency.md' in r and '429' in r; print('ok')"` prints "ok"
- Lock primitive check: `grep -c "threading" agent/round_table_executor.py` returns 0 (asyncio-only)
- ruff check agent/round_table_executor.py mcp_serve.py exits 0
</verification>

<success_criteria>
- SC#4: concurrent second get_agent_opinion for same roundId is rejected with 429 + MEMORY.md citation; single sequential submission proceeds and returns the panelist opinion
- asyncio.Lock (NOT threading.Semaphore — distinct layer from glm_concurrency_guard)
- Lock released in finally (no permanent roundId block on exception)
- All Phase 52 tests across plans 01-04 pass together (full agent suite green)
</success_criteria>

<output>
Create `.planning/phases/52-infra-foundation/52-04-SUMMARY.md` when done
</output>

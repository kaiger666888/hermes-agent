---
phase: 52-infra-foundation
plan: 03
type: execute
wave: 3
depends_on: ["52-01", "52-02"]
files_modified:
  - agent/memory_arbitration.py
  - mcp_serve.py
  - tests/agent/test_mcp_serve_round_table.py
autonomous: true
requirements: [INFRA-02]
tags: [mcp-tools, round-table, wire-up, fastmcp]

must_haves:
  truths:
    - "agents_list MCP tool returns JSON with count + list of agent summaries from registry_loader"
    - "agent_describe MCP tool returns JSON with the full agent YAML for one named agent"
    - "round_table_open MCP tool creates a new state file via round_table_state.open_round_table and returns the roundId"
    - "submit_round_table_result MCP tool flips status to completed via round_table_state.submit_round_table_result"
    - "get_agent_opinion MCP tool appends a Turn via round_table_state.append_turn (Phase 52 placeholder opinion string; Phase 53 fills real LLM call)"
    - "memory_retrieve_scoped MCP tool returns {\"status\": \"phase53_not_implemented\", \"hits\": []} stub"
    - "memory_submit_record MCP tool returns {\"status\": \"phase53_not_implemented\", \"record_id\": null} stub"
    - "All 7 tools are registered as @mcp.tool() closures inside create_mcp_server()"
  artifacts:
    - path: "agent/memory_arbitration.py"
      provides: "Phase 52 memory tool stubs + _scoped_agent_id contextvars primitive (Phase 53 fills routing)"
      exports: ["memory_retrieve_scoped", "memory_submit_record", "set_scoped_agent_id", "get_scoped_agent_id"]
    - path: "mcp_serve.py"
      provides: "7 new round-table @mcp.tool() closures registered inside create_mcp_server()"
      contains: "async def get_agent_opinion"
    - path: "tests/agent/test_mcp_serve_round_table.py"
      provides: "Integration tests covering agents_list / agent_describe / lifecycle round trip / memory stub"
  key_links:
    - from: "mcp_serve.py::agents_list"
      to: "agent.registry_loader.load_agent_registry()"
      via: "lazy import inside closure body, call, JSON-serialize result"
      pattern: "from agent.registry_loader import"
    - from: "mcp_serve.py::round_table_open"
      to: "agent.round_table_state.open_round_table"
      via: "construct state_dir, call open_round_table, return JSON"
      pattern: "from agent.round_table_state import open_round_table"
    - from: "mcp_serve.py::get_agent_opinion"
      to: "agent.round_table_state.append_turn"
      via: "append a placeholder Turn for Phase 52 (Phase 53 adds LLM call)"
      pattern: "append_turn"
---

<objective>
Wire the 7 v11.0 MCP tools into `mcp_serve.py`'s `create_mcp_server()` factory (INFRA-02). The tools compose the registry loader (INFRA-01) + state machine (INFRA-03) + a Phase-52 memory stub module.

Purpose: This is the MCP wire-up layer that lets Claude Code (or any MCP client) drive the round-table lifecycle. Without INFRA-02, the registry loader and state machine are unreachable from the MCP surface.

Output: 7 new `@mcp.tool()` closures in `mcp_serve.py` + `agent/memory_arbitration.py` stub module + 4+ integration tests.

**Authoritative tool-name list (CONTEXT.md "Resolved by Kai" point 1 — supersedes stale `02-ROUND-TABLE-PROTOCOL.md §5.0`):** `round_table_open`, `submit_round_table_result`, `get_agent_opinion`, `agents_list`, `agent_describe`, `memory_retrieve_scoped`, `memory_submit_record`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-infra-foundation/52-CONTEXT.md
@.planning/phases/52-infra-foundation/52-RESEARCH.md
@.planning/phases/52-infra-foundation/52-PATTERNS.md
@.planning/phases/52-infra-foundation/52-VALIDATION.md

<interfaces>
<!-- Phase 52-01 produces (consume these directly): -->
From agent/registry_loader.py:
- load_agent_registry(*, force_reload: bool = False) -> list[dict[str, Any]]
- load_one_agent_yaml(yaml_path: Path) -> dict[str, Any]
- RegistryValidationError (Exception subclass)

<!-- Phase 52-02 produces (consume these directly): -->
From agent/round_table_state.py:
- open_round_table(state_dir: Path, round_id: str, project_id: str, question: str, panelist_agent_ids: list[str], caller: str) -> dict[str, Any]
- append_turn(state_path: Path, turn: dict[str, Any]) -> dict[str, Any]
- submit_round_table_result(state_path: Path, conclusion: str, cited_memories: list[str], closed_by: str) -> dict[str, Any]
- read_and_recover_state(state_path: Path, *, stall_threshold_minutes: int = 30) -> dict[str, Any]
- RoundTableStatus(str, Enum)  # OPEN / COMPLETED / ABORTED / STALLED

<!-- mcp_serve.py current shape: -->
- create_mcp_server(event_bridge: Optional[EventBridge] = None) -> "FastMCP"
- 9 existing @mcp.tool() closures live INSIDE create_mcp_server() between lines 471 and 866
- New tools: insert before `return mcp` at line 867

<!-- FastMCP dispatch model: -->
- @mcp.tool() supports BOTH `def` (sync) and `async def` (async) closures
- Sync tools run in the main asyncio loop; async tools integrate natively
- Tools return `str` (JSON-serialized) — error responses also as JSON dicts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build agent/memory_arbitration.py (Phase 52 STUB) + test the stub returns the locked contract</name>
  <files>agent/memory_arbitration.py, tests/agent/test_memory_arbitration_stub.py</files>
  <read_first>
    - .planning/phases/52-infra-foundation/52-CONTEXT.md §"Resolved by Kai" point 3 (memory tools ship as stubs returning phase53_not_implemented)
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"agent/memory_arbitration.py (service — Phase 52 ships STUB)" (lines 322-345) + §"Contextvars primitive" (lines 301-318) — verbatim stub return contract + contextvars pattern
    - gateway/session_context.py:39-83 (canonical contextvars ContextVar pattern — `_UNSET` sentinel + setter/getter)
    - .planning/research/v10-orchestrator-design/agents-schema.yaml §2.6 memory_scope (locked enum shared/per_agent/project_scoped — used by Phase 53 routing; Phase 52 just stores the value)
  </read_first>
  <behavior>
    - memory_retrieve_scoped(query="anything", agent_id="anyagent") returns the EXACT dict `{"status": "phase53_not_implemented", "hits": []}`
    - memory_submit_record(agent_id="anyagent", content="x") returns the EXACT dict `{"status": "phase53_not_implemented", "record_id": None}`
    - set_scoped_agent_id("alice") followed by get_scoped_agent_id() returns "alice"
    - get_scoped_agent_id() called without prior set returns None
    - Different ContextVar contexts isolate the value (asyncio.create_task should not leak scope across tasks)
  </behavior>
  <action>
    Create agent/memory_arbitration.py with these public exports (per CONTEXT.md point 3 + PATTERNS.md §"agent/memory_arbitration.py"):

    1. Module header: `from __future__ import annotations`, `import logging`, `from contextvars import ContextVar`, `from typing import Any`. Module logger: `logger = logging.getLogger(__name__)`. (Note: memory_retrieve_scoped / memory_submit_record are async per the canonical skeleton, so they integrate with FastMCP's async tool dispatch.)

    2. `_UNSET: Any = object()` sentinel. `_SCOPED_AGENT_ID: ContextVar = ContextVar("HERMES_SCOPED_AGENT_ID", default=_UNSET)`.

    3. `def set_scoped_agent_id(agent_id: str) -> None` — `_SCOPED_AGENT_ID.set(agent_id)`.

    4. `def get_scoped_agent_id() -> str | None` — `val = _SCOPED_AGENT_ID.get(); return None if val is _UNSET else str(val)`.

    5. `async def memory_retrieve_scoped(query: str, agent_id: str, *, top_k: int = 5) -> dict[str, Any]` — docstring "STUB. Returns phase53_not_implemented. Phase 53 wires mem0 routing per agents-schema.yaml §2.6 memory_scope." Returns literal `{"status": "phase53_not_implemented", "hits": []}`. Log via `logger.debug("memory_retrieve_scoped stub called: query=%s agent_id=%s", query, agent_id)`.

    6. `async def memory_submit_record(agent_id: str, content: str, *, scope: str = "per_agent", confidence: float = 0.5) -> dict[str, Any]` — docstring "STUB. Returns phase53_not_implemented with null record_id." Returns literal `{"status": "phase53_not_implemented", "record_id": None}`.

    7. NO `import plugins.memory.mem0` at module top or inside functions (Pitfall #5 in RESEARCH.md — eager import couples Phase 52 to a Phase 53 backend that may not have MEM0_API_KEY set).

    Create tests/agent/test_memory_arbitration_stub.py:
    - `class TestMemoryStubReturnContract` with tests asserting the EXACT dict equality (use `== {"status": "phase53_not_implemented", "hits": []}`).
    - `class TestScopedAgentId` with sync tests (set/get) and one async test marked `@pytest.mark.asyncio` that uses `asyncio.create_task` to verify scope isolation across tasks.

    Async test marker: pytest-asyncio mode is NOT auto; every async test needs explicit `@pytest.mark.asyncio`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_memory_arbitration_stub.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - agent/memory_arbitration.py exists with the 4 listed exports
    - `.venv/bin/python -c "from agent.memory_arbitration import memory_retrieve_scoped, memory_submit_record, set_scoped_agent_id, get_scoped_agent_id; print('ok')"` prints "ok"
    - memory_retrieve_scoped returns EXACTLY `{"status": "phase53_not_implemented", "hits": []}` (assert == dict)
    - memory_submit_record returns EXACTLY `{"status": "phase53_not_implemented", "record_id": None}` (assert == dict)
    - get_scoped_agent_id returns None before set; returns the set value after set
    - Async test for context isolation passes
    - ruff check agent/memory_arbitration.py exits 0
    - File does NOT contain `import plugins.memory.mem0` or `from plugins.memory.mem0` anywhere
  </acceptance_criteria>
  <done>
    - memory_arbitration.py ships as stub with correct return contract
    - All tests in test_memory_arbitration_stub.py pass
    - No mem0 imports (Phase 53 will add them)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_mcp_serve_round_table.py covering INFRA-02 SC#2 (lifecycle round trip) + agents_list + agent_describe + memory stub invocation</name>
  <files>tests/agent/test_mcp_serve_round_table.py</files>
  <read_first>
    - mcp_serve.py:450-465 (create_mcp_server factory + FastMCP instance)
    - mcp_serve.py:471-524 (canonical @mcp.tool() closure pattern — conversations_list)
    - mcp_serve.py:831-866 (permissions_list_open + return mcp — the insertion point for new tools)
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"tests/agent/test_mcp_serve_round_table.py" (lines 608-625) — integration test pattern + live-system guard warning
    - tests/conftest.py:537+ (_live_system_guard autouse fixture — AST-inspects for os.kill / subprocess.run; mock at test boundary)
    - .planning/phases/52-infra-foundation/52-VALIDATION.md Per-Task Verification Map rows 52-02-01..03 (4 integration tests)
  </read_first>
  <behavior>
    - test_agents_list_returns_json: drop a valid fixture YAML into redirected ~/.hermes/agents/; invoke the agents_list tool closure; assert JSON response has "count": 1 and "agents": [{...}]
    - test_agent_describe_returns_yaml: invoke agent_describe with name="test-coordinator"; assert JSON response contains the full agent data (persona field present)
    - test_agent_describe_unknown_returns_404: invoke agent_describe with name="nonexistent"; assert JSON response has error "agent_not_found" and status 404
    - test_memory_retrieve_scoped_stub_via_tool: invoke the memory_retrieve_scoped tool closure; assert JSON response contains literal substring "phase53_not_implemented"
    - test_lifecycle_round_trip (SC#2): drop a valid fixture YAML; invoke round_table_open → get_agent_opinion → submit_round_table_result in sequence; assert each returns the expected status; assert final state file has status="completed"
  </behavior>
  <action>
    Create tests/agent/test_mcp_serve_round_table.py with `class TestMcpRoundTableIntegration:`.

    Header: `from __future__ import annotations`, `import json`, `from pathlib import Path`, `import pytest`. Import test-time: `from mcp_serve import create_mcp_server`. Use a fake EventBridge — inspect mcp_serve.py to confirm what EventBridge minimally requires (look at class EventBridge definition). If it's a class with `start()` / `stop()` no-ops, instantiate directly; otherwise define a minimal FakeBridge in the test file.

    **Live-system guard mitigation (Pitfall #7):** any test calling `create_mcp_server()` may trigger the autouse `_live_system_guard`. Either (a) construct a minimal FakeEventBridge with no system-side effects, or (b) mark tests with `@pytest.mark.live_system_guard_bypass` ONLY if FakeEventBridge still trips the guard. Try (a) first.

    To invoke the @mcp.tool() closures, FastMCP registers tools in `mcp._tool_manager._tools` (a dict). Access via `server._tool_manager._tools["agents_list"].fn` OR via FastMCP's official `await server.call_tool("agents_list", {})` API. Use whichever is simpler — check FastMCP 1.26.0 API by reading the source if needed.

    For test_lifecycle_round_trip: the test must call round_table_open with a synthetic project_slug="test-slug", a CC-generated UUID v4 (use uuid.uuid4().hex), a panelist list of at least 2 agent IDs (matching the schema minItems=2), then get_agent_opinion for one panelist (Phase 52 returns placeholder opinion), then submit_round_table_result. After each call, verify the JSON response status. After submit, verify the state file at `get_hermes_home()/"agents"/".runtime"/"test-slug"/"round_tables"/"{round_id}.json"` has status="completed".

    These tests are RED until Task 3 lands the actual tool closures.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_mcp_serve_round_table.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/agent/test_mcp_serve_round_table.py exists
    - Class TestMcpRoundTableIntegration has at least 5 test methods (agents_list / agent_describe / agent_describe_unknown / memory stub / lifecycle round trip)
    - test_lifecycle_round_trip body invokes all 3 lifecycle tools in sequence (round_table_open → get_agent_opinion → submit_round_table_result)
    - pytest --collect-only exits 0
    - Tests do NOT trigger _live_system_guard RuntimeError (verify by running pytest -v and confirming no "live system guard" error in output)
  </acceptance_criteria>
  <done>
    - test file collects cleanly
    - Tests RED until Task 3 lands the closures
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire 7 @mcp.tool() closures into create_mcp_server() in mcp_serve.py</name>
  <files>mcp_serve.py</files>
  <read_first>
    - mcp_serve.py:446-465 (create_mcp_server factory + FastMCP instantiation — DO NOT modify)
    - mcp_serve.py:471-524 (canonical @mcp.tool() form — conversations_list; copy this shape)
    - mcp_serve.py:775-827 (channels_list — registry-browse analog for agents_list)
    - mcp_serve.py:831-866 (last existing tool + return mcp — the insertion point)
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"mcp_serve.py (modified — controller)" (lines 348-483) — verbatim tool skeletons (agents_list, get_agent_opinion)
    - agent/registry_loader.py (Phase 52-01 output — load_agent_registry, load_one_agent_yaml, RegistryValidationError)
    - agent/round_table_state.py (Phase 52-02 output — open_round_table, append_turn, submit_round_table_result)
    - agent/memory_arbitration.py (Task 1 output — memory_retrieve_scoped, memory_submit_record stubs)
    - hermes_constants.get_hermes_home() (used to construct state_dir)
    - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml §PanelistSnapshot (minItems=2 — round_table_open must enforce this)
  </read_first>
  <behavior>
    - Tool "agents_list" returns JSON with count + agents list (delegates to load_agent_registry)
    - Tool "agent_describe" returns JSON with the full agent for a name; 404 error if name not in registry
    - Tool "round_table_open" creates a state file via open_round_table and returns {roundId, status: "open"}; returns 409 on duplicate
    - Tool "get_agent_opinion" is async; appends a placeholder Turn via append_turn; returns {round_id, agent_id, opinion: "[phase52_placeholder]", status: "ok"} (Phase 53 fills real LLM call). Enforces panelists minItems=2 at open-time.
    - Tool "submit_round_table_result" flips status to completed; returns 409 on second submit
    - Tool "memory_retrieve_scoped" is async; delegates to agent.memory_arbitration.memory_retrieve_scoped stub
    - Tool "memory_submit_record" is async; delegates to agent.memory_arbitration.memory_submit_record stub
    - All 7 closures are defined INSIDE create_mcp_server() before the `return mcp` line
  </behavior>
  <action>
    In mcp_serve.py, locate the `# -- permissions_respond ---` block ending at line 865 + `return mcp` at line 867. Insert 7 new `@mcp.tool()` closures between permissions_respond and `return mcp`, marked with a comment header `# ── v11.0 round-table tools (Phase 52 INFRA-02) ───────────`.

    For each tool, follow the canonical form from mcp_serve.py:471-524: `@mcp.tool()` decorator → `def tool_name(args) -> str:` (sync) OR `async def tool_name(args) -> str:` (async) → docstring with "Args:" section → body → return `json.dumps({...}, indent=2)`. Use lazy imports inside each closure body (per mcp_serve.py:765 pattern) — `from agent.registry_loader import load_agent_registry` etc.

    **Tool specifications:**

    1. `@mcp.tool() def agents_list(category: Optional[str] = None, tag: Optional[str] = None) -> str:` — lazy `from agent.registry_loader import load_agent_registry`. Try/except around load_agent_registry: on Exception as exc, log `logger.warning("agents_list: registry load failed: %s", exc)` and return json.dumps({"error": "registry_load_failed", "detail": str(exc)}). Otherwise filter by tag (category is reserved for Phase 53). Return `{"count": len(filtered), "agents": [{"name": e["name"], "description": e["description"], "version": e["version"], "tags": e.get("tags", []), "fitness_score": e.get("fitness_score"), "memory_scope": e["memory_scope"]} for e in filtered]}`.

    2. `@mcp.tool() def agent_describe(name: str) -> str:` — lazy `from agent.registry_loader import load_agent_registry`. Iterate registry, find entry where entry["name"] == name. If not found, return `{"error": "agent_not_found", "status": 404, "name": name}`. Otherwise return `{"agent": entry}` (full dict).

    3. `@mcp.tool() async def round_table_open(round_id: str, project_slug: str, question: str, panelist_agent_ids: list[str], caller: str = "cc") -> str:` — Validate panelist_agent_ids has len >= 2 (schema minItems=2); if not, return `{"error": "panelists_min_2_required", "status": 400}`. Construct state_dir = `get_hermes_home() / "agents" / ".runtime" / project_slug / "round_tables"`. Validate project_slug format `^[a-zA-Z0-9_.:-]+$` and reject `..` substring (T-52-05 mitigation); if invalid, return `{"error": "invalid_project_slug", "status": 400}`. Lazy `from agent.round_table_state import open_round_table`. Call `state = open_round_table(state_dir, round_id, project_slug, question, panelist_agent_ids, caller)`. If state has "error" key (409), return json.dumps(state). Otherwise return json.dumps({"roundId": round_id, "status": "open", "state": state}).

    4. `@mcp.tool() async def get_agent_opinion(round_id: str, project_slug: str, agent_id: str, topic: str, panel_context: Optional[str] = None) -> str:` — Construct state_path. Lazy `from agent.round_table_state import append_turn, read_and_recover_state`. Call read_and_recover_state(state_path); if status != "open", return `{"error": "round_not_open", "status": 409}`. Build turn dict `{"turnIndex": len(state["turns"]) + 1, "panelistId": agent_id, "opinion": "[phase52_placeholder]", "citedMemoryIds": [], "submittedAt": _now_iso_helper()}` — inline the ISO timestamp helper or import from round_table_state. Call append_turn(state_path, turn). Return `{"round_id": round_id, "agent_id": agent_id, "opinion": "[phase52_placeholder]", "status": "ok"}`.

    5. `@mcp.tool() async def submit_round_table_result(round_id: str, project_slug: str, conclusion: str, cited_memories: Optional[list[str]] = None, closed_by: str = "cc") -> str:` — Construct state_path. Lazy `from agent.round_table_state import submit_round_table_result as _submit`. Call `_submit(state_path, conclusion, cited_memories or [], closed_by)`. If returned dict has "error" key, return json.dumps(it). Otherwise return json.dumps({"roundId": round_id, "status": "completed"}).

    6. `@mcp.tool() async def memory_retrieve_scoped(query: str, agent_id: str, top_k: int = 5) -> str:` — Lazy `from agent.memory_arbitration import memory_retrieve_scoped as _retrieve`. Call `result = await _retrieve(query, agent_id, top_k=top_k)`. Return json.dumps(result).

    7. `@mcp.tool() async def memory_submit_record(agent_id: str, content: str, scope: str = "per_agent", confidence: float = 0.5) -> str:` — Lazy `from agent.memory_arbitration import memory_submit_record as _submit_record`. Call `result = await _submit_record(agent_id, content, scope=scope, confidence=confidence)`. Return json.dumps(result).

    Module imports already in mcp_serve.py cover `json`, `logging`, `Optional`, `Path` — no new top-level imports needed (lazy imports inside closures handle agent.* deps).

    Ensure `_now_iso()` helper is available: either import from agent.round_table_state OR inline `datetime.now(timezone.utc).isoformat(timespec="seconds")` in get_agent_opinion.

    All `logger.x(...)` calls use lazy %-formatting. All tool returns are JSON strings.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_mcp_serve_round_table.py tests/agent/test_memory_arbitration_stub.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - mcp_serve.py contains 7 new @mcp.tool() closures: agents_list, agent_describe, round_table_open, get_agent_opinion, submit_round_table_result, memory_retrieve_scoped, memory_submit_record
    - grep `@mcp.tool()` count in mcp_serve.py increased from 9 to 16
    - All closures are defined INSIDE create_mcp_server() before the `return mcp` statement (NOT at module level)
    - `async def get_agent_opinion` exists (the only tools requiring async are the 3 lifecycle + 2 memory tools; agents_list + agent_describe may stay sync)
    - All 5 tests in test_mcp_serve_round_table.py pass
    - test_lifecycle_round_trip end-to-end test passes — final state file has status="completed"
    - ruff check mcp_serve.py exits 0 (no new PLW1514 violations — no new open() calls without encoding)
    - `.venv/bin/python -c "from mcp_serve import create_mcp_server; mcp = create_mcp_server(); tools = list(mcp._tool_manager._tools.keys()); assert 'agents_list' in tools and 'round_table_open' in tools and 'memory_retrieve_scoped' in tools; print(len(tools), 'tools registered')"` prints "16 tools registered"
  </acceptance_criteria>
  <done>
    - All 7 tools wired into create_mcp_server()
    - All integration tests pass
    - Tool count went from 9 to 16
    - ruff check clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (CC) → MCP server (Hermes) | CC supplies round_id / project_slug / agent_id strings crossing into runtime via these tools |
| MCP server → filesystem state | Tool invocations write state files; corrupt or malicious paths could escape intended directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-09 | Tampering | Path traversal via project_slug in round_table_open | mitigate | Regex validate `^[a-zA-Z0-9_.:-]+$` + reject `..` substring; return 400 invalid_project_slug |
| T-52-10 | Tampering | Unbounded roundId / agentId length (DoS via giant strings) | accept | PoC scope; MCP transport has its own message size limits; risk is low. |
| T-52-11 | Spoofing | Caller impersonation (round_table_open caller field) | accept | PoC runs single-user via stdio MCP; no auth in scope. v12+ adds CC session IDs. |
| T-52-12 | Information disclosure | agent_describe returns full persona (could leak across operators) | accept | Single-operator PoC; persona is operator-authored IP, not PII |
| T-52-13 | DoS | Concurrent round_table_open flooding | mitigate | Per-roundId asyncio.Lock in Phase 52-04 enforces serial; idempotent 409 on duplicate roundId contains the flood |
| T-52-SC | Tampering | (No package installs) | N/A | N/A |
</threat_model>

<verification>
- All 5 integration tests pass: `.venv/bin/python -m pytest tests/agent/test_mcp_serve_round_table.py -x -v`
- All memory stub tests pass: `.venv/bin/python -m pytest tests/agent/test_memory_arbitration_stub.py -x -v`
- Tool count probe: `.venv/bin/python -c "from mcp_serve import create_mcp_server; mcp = create_mcp_server(); print(len(mcp._tool_manager._tools))"` prints 16
- ruff check mcp_serve.py agent/memory_arbitration.py exits 0
- Manual: invoke round_table_open via FastMCP test client, verify state file appears at the expected path with status="open"
</verification>

<success_criteria>
- SC#1 (MCP half): agents_list MCP tool returns a JSON list including a YAML placed at ~/.hermes/agents/{name}.agent.yaml
- SC#2: round_table_open → 1 get_agent_opinion → submit_round_table_result lifecycle round trip completes end-to-end against a single synthetic agent; final state file status="completed"
- 7 MCP tools registered (count 9 → 16 in create_mcp_server)
- Memory tools return phase53_not_implemented stub (Phase 53 fills in mem0 routing)
</success_criteria>

<output>
Create `.planning/phases/52-infra-foundation/52-03-SUMMARY.md` when done
</output>

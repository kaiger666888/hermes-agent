---
phase: 52-infra-foundation
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - agent/round_table_state.py
  - tests/agent/test_round_table_state.py
autonomous: true
requirements: [INFRA-03]
tags: [round-table, state-machine, atomic-write, crash-recovery]

must_haves:
  truths:
    - "open_round_table() creates a new state file at ~/.hermes/agents/.runtime/{slug}/round_tables/{roundId}.json with status='open'"
    - "A duplicate open for the same roundId returns a 409 Conflict response (idempotent)"
    - "append_turn() appends a Turn to the state file atomically (read-modify-write via atomic_json_write)"
    - "submit_round_table_result() flips status from 'open' to 'completed' atomically"
    - "A second submit for the same roundId returns 409 Conflict (idempotent)"
    - "Interrupted submit NEVER leaves status outside the schema enum {open, completed, aborted, stalled}"
    - "Partial-write corruption is recovered by archiving the corrupt file + raising JSONDecodeError"
    - "Stale 'open' state (lastUpdatedAt > stall_threshold) is auto-flipped to 'stalled' on next read"
  artifacts:
    - path: "agent/round_table_state.py"
      provides: "Round-table state machine: open / append_turn / submit + crash-recovery read"
      exports: ["RoundTableStatus", "open_round_table", "append_turn", "submit_round_table_result", "read_and_recover_state", "RoundTableStateError"]
    - path: "tests/agent/test_round_table_state.py"
      provides: "5+ unit tests covering lifecycle, atomicity, and 3 crash-recovery failure modes"
  key_links:
    - from: "agent/round_table_state.py"
      to: "utils.atomic_json_write"
      via: "atomic_json_write(state_path, state_dict, indent=2) on every mutation"
      pattern: "atomic_json_write"
    - from: "agent/round_table_state.py"
      to: ".planning/research/v10-orchestrator-design/round-table-state-schema.yaml"
      via: "RoundTableStatus enum values match schema enum [open, completed, aborted, stalled]"
      pattern: "open.*completed.*aborted.*stalled"
    - from: "agent/round_table_state.py"
      to: "hermes_constants.get_hermes_home()"
      via: "state_dir = get_hermes_home() / 'agents' / '.runtime' / project_slug / 'round_tables'"
      pattern: "get_hermes_home\\(\\)\\s*/\\s*[\"']agents[\"']"
---

<objective>
Build the round-table state machine (INFRA-03) with atomic persistence + crash recovery for 3 failure modes per `06-CROSS-REPO-IMPACT.md §6`.

Purpose: This is the persistence backbone that the 7 MCP tools (INFRA-02) and the serial executor (INFRA-04) both depend on. Without INFRA-03, SC#2 (lifecycle atomicity) and SC#3 (crash recovery) cannot pass.

Output: `agent/round_table_state.py` module + 5 unit tests covering lifecycle + 3 crash-recovery modes + atomic invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-infra-foundation/52-CONTEXT.md
@.planning/phases/52-infra-foundation/52-RESEARCH.md
@.planning/phases/52-infra-foundation/52-PATTERNS.md
@.planning/phases/52-infra-foundation/52-VALIDATION.md
@.planning/research/v10-orchestrator-design/round-table-state-schema.yaml

<interfaces>
<!-- Locked schema this module must serialize to. ALL property names camelCase.
     additionalProperties: false. Status enum is authoritative (CONTEXT.md "Resolved by Kai"
     point 2 — do NOT use in_progress/closed from INFRA-03 prose). -->

From .planning/research/v10-orchestrator-design/round-table-state-schema.yaml:
- type: object, additionalProperties: false
- required: [roundId, projectId, question, panelists, turnOrder, status, turns, roundTableOpen, createdAt]
- status enum: [open, completed, aborted, stalled]   # AUTHORITATIVE
- panelists: array, minItems: 2, items = PanelistSnapshot
- turns: array of Turn { turnIndex, panelistId, opinion, citedMemoryIds, submittedAt }
- turnOrder: { strategy, currentIndex, seed? }
- roundTableOpen: { caller, openedAt, project, question }
- submitRoundTableResult: { conclusion, citedMemories, closedAt, closedBy }  # present iff status=completed
- lastUpdatedAt: ISO-8601 string or null
- schemaVersion default "1.0.0"
- State file path per schema YAML header: ~/.hermes/agents/.runtime/{project_slug}/round_tables/{round_id}.json
  (NOTE: 06-CROSS-REPO-IMPACT.md §5.1 + schema YAML header both confirm `.runtime/` is nested INSIDE `agents/`.
   CONTEXT.md / STATE.md informal wording "~/.hermes/.runtime/..." omits the `agents/` parent — that is shorthand.
   Use `get_hermes_home() / "agents" / ".runtime" / project_slug / "round_tables" / f"{round_id}.json"`.)

From utils.py:111 atomic_json_write(path, data, *, indent=2, mode=None) -> None  # temp+fsync+os.replace
From hermes_constants.get_hermes_home() -> Path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_round_table_state.py covering INFRA-03 SC#2 atomicity + SC#3 a/b/c crash recovery (5+ tests)</name>
  <files>tests/agent/test_round_table_state.py</files>
  <read_first>
    - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml (full file — schema authority; note status enum, required fields, state file path in header comment lines 1-4)
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"tests/agent/test_round_table_state.py" (lines 542-569) — crash recovery test skeleton pattern
    - .planning/phases/52-infra-foundation/52-RESEARCH.md §"Crash Recovery Read-Time Pattern" (lines 690-733) — reference implementation of read_and_recover_state
    - tests/agent/test_audit_log.py:14-37 (canonical agent-module test header — class-based grouping)
    - tests/conftest.py:328-360 (_hermetic_environment fixture — autouse, monkeypatches HERMES_HOME)
  </read_first>
  <behavior>
    - test_open_creates_state_file_with_status_open: call open_round_table() with synthetic args (round_id, project_id="test-slug", question, panelist_agent_ids=[a,b], caller="cc-1"); verify state file exists at get_hermes_home()/"agents"/".runtime"/"test-slug"/"round_tables"/"{round_id}.json"; load JSON, verify status == "open", roundTableOpen.caller == "cc-1"
    - test_duplicate_open_returns_409_conflict: second open_round_table() with same round_id returns {"error": "round_already_open", "status": 409}
    - test_append_turn_grows_turns_list: after open, call append_turn(state_path, turn_dict); reload state file; verify len(turns)==1, turns[0].panelistId matches
    - test_submit_flips_status_to_completed: after open + 1 turn, call submit_round_table_result(state_path, conclusion="...", closed_by="cc-1"); reload; verify status=="completed", submitRoundTableResult.closedBy=="cc-1"
    - test_second_submit_returns_409_conflict: idempotent — second submit on completed round returns {"error": "round_not_open", "status": 409}
    - test_interrupted_submit_no_status_outside_enum (SC#2 atomicity): open a round, simulate a crash mid-submit by truncating the state file to literal "{" (broken JSON), then call read_and_recover_state; verify it raises json.JSONDecodeError AND the corrupt file is archived to {state_path}.corrupt
    - test_partial_write_recovery_defense_in_depth (SC#3a): pre-create state file with "{ broken json"; call read_and_recover_state; expect json.JSONDecodeError; verify {state_path}.with_suffix(".json.corrupt").exists()
    - test_mid_turn_crash_recovers_via_stall (SC#3b): pre-create state file with status="open" + lastUpdatedAt=(now - 2 hours ISO-8601); call read_and_recover_state with stall_threshold_minutes=30; verify returned state has status=="stalled"
    - test_orphaned_session_recovers_on_next_read (SC#3c): same as (b) but lastUpdatedAt=(now - 90 min); verify status=="stalled"
  </behavior>
  <action>
    Create tests/agent/test_round_table_state.py with class-based test groups:

    `class TestRoundTableLifecycle:` with the 5 positive-lifecycle tests above (open / duplicate-open / append-turn / submit / duplicate-submit).

    `class TestCrashRecovery:` with the 4 crash-recovery tests above (interrupted-submit, partial-write, mid-turn-crash, orphaned-session).

    Header: `from __future__ import annotations`, `import json`, `from datetime import datetime, timezone, timedelta`, `from pathlib import Path`, `import pytest`. Import from agent.round_table_state (Task 2 builds it): `from agent.round_table_state import open_round_table, append_turn, submit_round_table_result, read_and_recover_state, RoundTableStateError`.

    Each test gets `tmp_path` (already redirected by autouse _hermetic_environment) and constructs a state_dir = `get_hermes_home() / "agents" / ".runtime" / "test-slug" / "round_tables"` directly OR uses a helper that builds a synthetic state file inline.

    For SC#3b/c tests, use monkeypatch on `datetime.now(timezone.utc)` is NOT needed — instead, write a fixture with a stale `lastUpdatedAt` string literal (e.g. `"2026-07-07T00:00:00+00:00"` if test runs after that time) OR compute it dynamically as `(datetime.now(timezone.utc) - timedelta(hours=2)).isoformat()`.

    These tests will be RED until Task 2 lands. They are sync tests (no @pytest.mark.asyncio needed).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_round_table_state.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/agent/test_round_table_state.py exists
    - Class TestRoundTableLifecycle has at least 5 test methods (open / duplicate-open / append / submit / duplicate-submit)
    - Class TestCrashRecovery has 4 test methods matching SC#3a/b/c + interrupted-submit-atomicity
    - test_partial_write_recovery_defense_in_depth body asserts `.with_suffix(".json.corrupt").exists()` after the raise
    - test_mid_turn_crash_recovers_via_stall body asserts returned state has status == "stalled"
    - pytest --collect-only exits 0 (no collection errors)
  </acceptance_criteria>
  <done>
    - test_round_table_state.py collects cleanly
    - Tests RED because agent/round_table_state.py does not yet exist (expected — Task 2 makes them GREEN)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent/round_table_state.py — open/append/submit + read_and_recover_state (3 crash-recovery modes)</name>
  <files>agent/round_table_state.py</files>
  <read_first>
    - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml (full file — wire format; status enum [open, completed, aborted, stalled] is AUTHORITATIVE per CONTEXT.md "Resolved by Kai" point 2; do NOT use in_progress or closed)
    - .planning/phases/52-infra-foundation/52-PATTERNS.md §"agent/round_table_state.py" (lines 140-237) — Dataclass state pattern + atomic persistence pattern + crash-recovery read-time pattern
    - .planning/phases/52-infra-foundation/52-RESEARCH.md §"Atomic State Persistence Pattern" + §"Crash Recovery Read-Time Pattern" (lines 606-733) — canonical function skeletons
    - utils.py:111-178 (atomic_json_write signature — temp+fsync+os.replace, BaseException-safe cleanup)
    - agent/turn_retry_state.py:27-79 (dataclass-as-state pattern — `from __future__ import annotations`, `@dataclass`, default_factory for mutable fields)
    - hermes_constants.get_hermes_home() (call site at agent/curator_audit.py:54 — canonical pattern)
  </read_first>
  <behavior>
    - open_round_table(state_dir, round_id, project_id, question, panelist_agent_ids, caller) writes a state file with status="open" and returns the state dict
    - open_round_table idempotent: if state file already exists, returns {"error": "round_already_open", "status": 409, "round_id": round_id} (does NOT raise)
    - append_turn(state_path, turn_dict) read-modify-writes via atomic_json_write, growing turns[] by 1 and updating lastUpdatedAt
    - submit_round_table_result(state_path, conclusion, cited_memories, closed_by) atomically flips status from "open" to "completed" and adds submitRoundTableResult block
    - submit_round_table_result idempotent: if status != "open", returns {"error": "round_not_open", "status": 409}
    - read_and_recover_state(state_path, *, stall_threshold_minutes=30) handles 3 failure modes:
      (a) JSON decode error → archive to {state_path}.corrupt, re-raise json.JSONDecodeError
      (b) status="open" + lastUpdatedAt older than threshold → flip status to "stalled" via atomic_json_write, return updated state
      (c) Missing file → raise FileNotFoundError
    - State file path: `get_hermes_home() / "agents" / ".runtime" / project_slug / "round_tables" / f"{round_id}.json"`
    - All state dict keys camelCase (matches round-table-state-schema.yaml); NO snake_case in serialized JSON
  </behavior>
  <action>
    Create agent/round_table_state.py with these public exports:

    1. `class RoundTableStatus(str, Enum)` with values OPEN="open", COMPLETED="completed", ABORTED="aborted", STALLED="stalled". Docstring must cite round-table-state-schema.yaml:127-141 as authority and explicitly say "do NOT use in_progress or closed — those are stale CONTEXT.md shorthand".

    2. `class RoundTableStateError(Exception)` — for invariant violations (e.g. status transition out of enum).

    3. Helper `_now_iso() -> str` returns `datetime.now(timezone.utc).isoformat(timespec="seconds")`.

    4. Helper `_state_file_path(project_slug: str, round_id: str) -> Path` returns `get_hermes_home() / "agents" / ".runtime" / project_slug / "round_tables" / f"{round_id}.json"`. The state_dir.parent.mkdir(parents=True, exist_ok=True) call belongs inside open_round_table, not here.

    5. `def open_round_table(state_dir: Path, round_id: str, project_id: str, question: str, panelist_agent_ids: list[str], caller: str) -> dict[str, Any]` — state_dir is passed in (the MCP tool computes it). If `(state_dir / f"{round_id}.json").exists()`, return 409 dict immediately. Otherwise construct the initial state dict with camelCase keys matching round-table-state-schema.yaml required fields: roundId, projectId, question, panelists (build minimal PanelistSnapshot list from panelist_agent_ids — agentId only, personaSha256="0"*64, fitnessScore=null, tools=[], memoryScope="per_agent"), turnOrder ({"strategy": "round-robin", "currentIndex": 0, "seed": panelist_agent_ids}), status="open", turns=[], conflicts=[], roundTableOpen ({"caller": caller, "openedAt": _now_iso(), "project": project_id, "question": question}), personaSnapshots={}, schemaVersion="1.0.0", createdAt=_now_iso(), lastUpdatedAt=None. Call state_dir.mkdir(parents=True, exist_ok=True) then `atomic_json_write(state_path, state, indent=2)`. Return state.

    6. `def append_turn(state_path: Path, turn: dict[str, Any]) -> dict[str, Any]` — Open file (encoding="utf-8"), json.load. Append turn to state["turns"]. Update state["lastUpdatedAt"]=_now_iso(). atomic_json_write(state_path, state, indent=2). Return state.

    7. `def submit_round_table_result(state_path: Path, conclusion: str, cited_memories: list[str], closed_by: str) -> dict[str, Any]` — Read state. If state["status"] != "open", return {"error": "round_not_open", "status": 409}. Otherwise set state["status"]="completed", state["submitRoundTableResult"]={"conclusion": conclusion, "citedMemories": cited_memories, "closedAt": _now_iso(), "closedBy": closed_by}, state["lastUpdatedAt"]=_now_iso(). atomic_json_write(state_path, state, indent=2). Return state.

    8. `def read_and_recover_state(state_path: Path, *, stall_threshold_minutes: int = 30) -> dict[str, Any]` — Per RESEARCH.md §"Crash Recovery Read-Time Pattern" (lines 694-733). If not state_path.exists(): raise FileNotFoundError(state_path). Try `with open(state_path, encoding="utf-8") as f: state = json.load(f)` except json.JSONDecodeError as exc: compute archive = state_path.with_suffix(".json.corrupt"); `state_path.rename(archive)`; log via `logger.error("round_table_state corrupt at %s, archived to %s: %s", state_path, archive, exc)`; re-raise. Then stall detection: if state.get("status") == "open" and state.get("lastUpdatedAt"): parse datetime.fromisoformat(state["lastUpdatedAt"]); compute age_minutes; if age_minutes > stall_threshold_minutes: state["status"]="stalled"; atomic_json_write(state_path, state, indent=2); log `logger.warning("round_table %s stalled (age=%dm > %dm); status flipped", state.get("roundId"), int(age_minutes), stall_threshold_minutes)`. Return state.

    Module header per CLAUDE.md: `from __future__ import annotations`. Imports: `import json`, `import logging`, `from dataclasses import dataclass, field, asdict` (dataclass NOT strictly required — dict[str, Any] is fine per the canonical skeleton in RESEARCH.md), `from datetime import datetime, timezone`, `from enum import Enum`, `from pathlib import Path`, `from typing import Any`. From hermes: `from hermes_constants import get_hermes_home`. From utils: `from utils import atomic_json_write`. Module logger: `logger = logging.getLogger(__name__)`.

    NO top-level `import run_agent` (circular). NO `Path.home() / ".hermes"` (CLAUDE.md anti-pattern). All `open()` calls pass `encoding="utf-8"`. All log calls use lazy %-formatting.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && .venv/bin/python -m pytest tests/agent/test_round_table_state.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - agent/round_table_state.py exists
    - Module imports cleanly: `.venv/bin/python -c "from agent.round_table_state import open_round_table, append_turn, submit_round_table_result, read_and_recover_state, RoundTableStatus, RoundTableStateError; print('ok')"` prints "ok"
    - RoundTableStatus enum has exactly 4 members with values matching schema YAML: open / completed / aborted / stalled (NO in_progress, NO closed)
    - All 9 tests in test_round_table_state.py pass
    - test_partial_write_recovery_defense_in_depth: corrupt file archived to .corrupt before raising
    - test_mid_turn_crash_recovers_via_stall: returned state has status=="stalled"
    - State files use camelCase keys (grep the test fixtures written by open_round_table: keys include "roundId", "projectId", "lastUpdatedAt", "submitRoundTableResult" — NOT "round_id", "project_id")
    - State file path is `~/.hermes/agents/.runtime/{slug}/round_tables/{round_id}.json` (NOT `~/.hermes/.runtime/...`)
    - ruff check agent/round_table_state.py exits 0
  </acceptance_criteria>
  <done>
    - agent/round_table_state.py ships with all listed exports
    - All tests in tests/agent/test_round_table_state.py pass
    - RoundTableStatus enum is the authoritative 4-value form per schema YAML
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state file (filesystem) ↔ round-table runtime | State JSONs are read back into runtime; corrupt files would crash subsequent MCP tool calls |
| roundId / project_slug input ↔ filesystem path | Caller-supplied round_id / project_slug becomes a path component — must validate format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Tampering | Path traversal via project_slug (e.g. "../../etc/passwd") | mitigate | Validate project_slug format at MCP tool entry (regex `^[a-zA-Z0-9_.:-]+$`, reject `..` substrings). Lint-level enforcement in MCP tool; round_table_state trusts the slug because callers pre-validate. |
| T-52-06 | Tampering | Partial-write corruption (process crash mid-write) | mitigate | utils.atomic_json_write = temp+fsync+os.replace guarantees the file body is never partial. read_and_recover_state adds defense-in-depth by archiving + raising on JSONDecodeError. |
| T-52-07 | DoS | Orphaned sessions accumulating in .runtime/ | mitigate | Stall detection (read_and_recover_state) auto-flips stale open→stalled; Phase 56 audit cleans up (per 06-CROSS-REPO-IMPACT.md §6.4 GC note). |
| T-52-08 | Information disclosure | Cross-project reference in state JSON | accept | round-table-state-schema.yaml forbids cross-project fields; Phase 46 §6.5 lint catches violations. PoC has ≤3 projects; risk is low. |
| T-52-SC | Tampering | (No package installs in this plan) | N/A | N/A |
</threat_model>

<verification>
- All 9 unit tests pass: `.venv/bin/python -m pytest tests/agent/test_round_table_state.py -x -v`
- Manual probe: `.venv/bin/python -c "from agent.round_table_state import RoundTableStatus; print([s.value for s in RoundTableStatus])"` prints `['open', 'completed', 'aborted', 'stalled']`
- ruff check agent/round_table_state.py exits 0
- Atomic write proof: drop a synthetic corrupt state file at the expected path, call read_and_recover_state, verify it raises + archives
</verification>

<success_criteria>
- SC#2 atomicity: interrupted submit leaves status in {open, completed, aborted, stalled} (never outside enum)
- SC#3 a/b/c: all 3 crash-recovery failure modes recovered without operator intervention
- Wire format matches round-table-state-schema.yaml (camelCase keys, correct status enum)
- Phase 52-03 (MCP tools) can compose these primitives into a lifecycle round-trip
</success_criteria>

<output>
Create `.planning/phases/52-infra-foundation/52-02-SUMMARY.md` when done
</output>

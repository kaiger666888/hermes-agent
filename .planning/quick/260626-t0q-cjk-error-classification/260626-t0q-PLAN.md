---
phase: quick-260626-t0q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/error_classifier.py
  - agent/conversation_loop.py
  - tests/agent/test_error_classifier.py
autonomous: true
requirements: [QUICK-260626-T0Q]
user_setup: []

must_haves:
  truths:
    - "When Zhipu returns HTTP 200 + body {\"error\":{\"code\":\"1305\",\"message\":\"该模型当前访问量过大\"}}, the agent classifies it as FailoverReason.overloaded and retries with backoff — NOT as an InvalidAPIResponse terminal failure."
    - "When a provider returns an error message containing any of openclaw's 4 pattern categories (rateLimit / overloaded / serverError / timeout) in English or CJK, classify_api_error returns the matching FailoverReason instead of unknown."
    - "Zhipu error codes 1305 → overloaded, 1311 → billing, 1113 → auth are recognized by _classify_by_error_code regardless of HTTP status."
    - "All existing classify_api_error tests pass unchanged (no regressions in the 200+ existing classification cases)."
  artifacts:
    - path: "agent/error_classifier.py"
      provides: "CJK + English error pattern library (4 categories) and Zhipu code handlers (1305/1311/1113) and classify_response_body_error() helper"
      contains: "_CJK_OPENCLAW_PATTERNS"
    - path: "agent/conversation_loop.py"
      provides: "Pre-check hook in response_invalid block that calls classify_response_body_error before falling into InvalidAPIResponse path"
      contains: "classify_response_body_error"
    - path: "tests/agent/test_error_classifier.py"
      provides: "Pattern coverage tests + Zhipu code tests + HTTP 200 integration test"
      contains: "TestOpenclawCjkPatterns"
  key_links:
    - from: "agent/conversation_loop.py (response_invalid block, ~line 1234)"
      to: "agent/error_classifier.classify_response_body_error"
      via: "function call before InvalidAPIResponse retry path"
      pattern: "classify_response_body_error"
    - from: "agent/error_classifier._classify_by_error_code"
      to: "Zhipu code handlers 1305/1311/1113"
      via: "explicit code matchers"
      pattern: "1305|1311|1113"
---

<objective>
Port openclaw's `failover-matches.ts` CJK + English error pattern library (4 categories: rateLimit / overloaded / serverError / timeout) into hermes-agent's `agent/error_classifier.py`, add explicit Zhipu error code handlers (1305=overloaded, 1311=billing, 1113=auth), and add a `classify_response_body_error()` helper that the conversation_loop's response_invalid block can call as a pre-check. This ensures Zhipu's signature "HTTP 200 + error body" pattern classifies as a retryable FailoverReason instead of falling into the terminal InvalidAPIResponse path.

Purpose: Today's incident — Zhipu returned `HTTP 200 + {"error":{"code":"1305","message":"该模型当前访问量过大"}}` and hermes burned all retries on "InvalidAPIResponse" because the classifier was never called on the response body. With this fix, the same response classifies as `FailoverReason.overloaded` and retries correctly.

Output: 3 modified files (error_classifier.py, conversation_loop.py, test_error_classifier.py). No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@agent/error_classifier.py
@agent/conversation_loop.py
@tests/agent/test_error_classifier.py

# Source file (read FULLY before porting patterns):
# /home/kai/.nvm/versions/node/v24.13.0/lib/node_modules/openclaw/dist/failover-matches-C-tab7FS.js

<interfaces>
<!-- Existing error_classifier.py contracts the executor needs. -->

From agent/error_classifier.py (FailoverReason enum, line 24):
```python
class FailoverReason(enum.Enum):
    auth = "auth"
    auth_permanent = "auth_permanent"
    billing = "billing"
    rate_limit = "rate_limit"
    overloaded = "overloaded"            # ← openclaw "overloaded" maps here
    server_error = "server_error"        # ← openclaw "serverError" maps here
    timeout = "timeout"                  # ← openclaw "timeout" maps here
    # ... (context_overflow, payload_too_large, etc.)
    unknown = "unknown"
```

From agent/error_classifier.py (classify_api_error signature, line 441):
```python
def classify_api_error(
    error: Exception,
    *,
    provider: str = "",
    model: str = "",
    approx_tokens: int = 0,
    context_length: int = 200000,
    num_messages: int = 0,
) -> ClassifiedError: ...
```

Pipeline order inside classify_api_error (line 536 onwards):
1. Provider-specific patterns (content_policy, thinking_sig, long_context_tier, oauth_beta, llama_cpp, grok)
2. HTTP status code → _classify_by_status
3. Error code → _classify_by_error_code
4. Message patterns → _classify_by_message
5. SSL transient
6. Server disconnect + large session
7. Transport error types
8. Fallback: unknown

From agent/conversation_loop.py (response_invalid block, line 1234):
```python
if response_invalid:
    agent._invoke_api_request_error_hook(
        ...
        error_type="InvalidAPIResponse",
        retryable=True,
        reason="invalid_response",
    )
    # ... retry/fallback loop ...
```

The new helper classify_response_body_error() will be called by conversation_loop.py
BEFORE this `if response_invalid:` block fires its InvalidAPIResponse path —
specifically, BEFORE line 1234. Location: insert the pre-check between the
response-shape validation (ends ~line 1232) and the `if response_invalid:` at 1234.
</interfaces>

<category_mapping>
# openclaw category → hermes FailoverReason mapping (DERIVED FROM SOURCE)

| openclaw category | hermes FailoverReason | Rationale |
|---|---|---|
| rateLimit | rate_limit | Direct match. Existing _RATE_LIMIT_PATTERNS already covers most English entries; the CJK entries are net-new. |
| overloaded | overloaded | Direct match. openclaw's "overloaded" / "at capacity" / "服务过载" → hermes overloaded (503/529 equivalent). |
| serverError | server_error | Direct match. "内部错误" / "服务器错误" → hermes server_error. |
| timeout | timeout | Direct match. "请求超时" / "连接超时" → hermes timeout. NOTE: many openclaw timeout entries are already covered by hermes _TIMEOUT_MESSAGE_PATTERNS / _TRANSPORT_ERROR_TYPES / _SERVER_DISCONNECT_PATTERNS — the executor should DEDUPE to avoid pattern bloat. |
| billing (auxiliary) | billing | Already well-covered by _BILLING_PATTERNS. Only port the CJK entries ("余额不足", "账户余额不足", "欠费", "账户已欠费") and the ZAI_BILLING_CODE_1311_RE handler. |
| auth (auxiliary) | auth | Already well-covered by _AUTH_PATTERNS. Only port the CJK entries ("无权访问", "认证失败", "鉴权失败", "密钥无效", "apikey 无效") and the ZAI_AUTH_CODE_1113_RE handler. |
</category_mapping>

<zhipu_code_handling>
# Zhipu (ZAI) numeric error code handlers — CRITICAL for today's incident

ZhipuAI returns errors with numeric `code` fields in the JSON body. The existing
`_classify_by_error_code` function matches string codes (lowercase comparison
against literal strings like "resource_exhausted"). The executor MUST add explicit
numeric-code handling for Zhipu:

| Code | FailoverReason | Source / meaning |
|------|----------------|------------------|
| 1305 | overloaded     | "该模型当前访问量过大" / model overloaded — THE bug from today's incident |
| 1311 | billing        | Zhipu billing/quota exhausted |
| 1113 | auth           | Zhipu authentication failure |

Implementation approach (executor decides exact naming, but follow openclaw convention):
- Add ZHIPU_OVERLOADED_CODE_1305 (and 1311/1113 equivalents) as module-level constants
  near the existing pattern lists (around line 95-200 of error_classifier.py).
- In `_classify_by_error_code` (line 1089), BEFORE the existing string-code matches,
  add explicit numeric handling. The `error_code` parameter is a string from
  `_extract_error_code` (which calls `str(code).strip()` on int values), so match
  against string form: `if code_lower in {"1305"}: return result_fn(FailoverReason.overloaded, ...)`.
  Also accept the raw int form just in case — see how _extract_error_code already
  handles int codes at line 1316-1320.
- ALSO add a body-level regex `ZHIPU_CODE_1305_RE = re.compile(r'"code"\s*:\s*1305\b')`
  matching openclaw's `ZAI_BILLING_CODE_1311_RE = /"code"\s*:\s*1311\b/` style, and
  apply it against the raw body JSON in _classify_by_message (the no-status-code
  path) so the classifier catches Zhipu errors even when the SDK loses the
  structured `code` field. The openclaw source uses these body-JSON regexes as
  belt-and-suspenders because some transport layers flatten the error structure.
</zhipu_code_handling>

<openclaw_source_excerpts>
# Verbatim patterns from
# /home/kai/.nvm/versions/node/v24.13.0/lib/node_modules/openclaw/dist/failover-matches-C-tab7FS.js
# (lines 50-187). The executor MUST preserve CJK characters exactly — they are
# simplified Chinese, distinct codepoints, do NOT normalize.

rateLimit patterns (lines 51-75):
  - JS regex: /rate[_ ]limit|too many requests|429/
  - JS regex: /too many (?:concurrent )?requests/i
  - JS regex: /throttling(?:exception)?/i
  - plain: "model_cooldown", "exceeded your current quota", "resource has been exhausted",
           "quota exceeded", "resource_exhausted", "throttlingexception", "throttling_exception",
           "throttled", "throttling", "usage limit"
  - JS regex: /\btpm\b/i
  - plain: "tokens per minute", "tokens per day"
  - CJK plain: "请求过于频繁", "调用频率", "频率限制", "配额不足", "配额已用尽", "额度不足", "额度已用尽"

overloaded patterns (lines 76-85):
  - JS regex: /overloaded_error|"type"\s*:\s*"overloaded_error"/i
  - plain: "overloaded"
  - JS regex: /\b(?:selected\s+)?model\s+(?:is\s+)?at capacity\b/i
  - JS regex: /service[_ ]unavailable.*(?:overload|capacity|high[_ ]demand)|(?:overload|capacity|high[_ ]demand).*service[_ ]unavailable/i
  - plain: "high demand", "high load"
  - CJK plain: "服务过载", "当前负载过高"

serverError patterns (lines 86-104):
  - plain: "an error occurred while processing", "internal server error", "internal_error",
           "server_error", "service temporarily unavailable", "service_unavailable",
           "bad gateway", "gateway timeout", "upstream error", "upstream connect error",
           "connection reset"
  - CJK plain: "内部错误", "服务器错误", "服务器内部错误", "系统错误", "系统繁忙", "系统异常"

timeout patterns (lines 105-147):
  - plain: "timeout", "timed out", "service unavailable", "deadline exceeded",
           "context deadline exceeded", "connection error", "network error",
           "network request failed", "fetch failed", "socket hang up"
  - JS regex (complex, both lookahead forms — skip the lookahead ones, port the rest):
    /^(?=[\s\S]*\bgot status:\s*internal\b)(?=[\s\S]*\bcode["']?\s*[:=]\s*500\b)/i
    /^(?=[\s\S]*["']status["']\s*:\s*["']internal["'])(?=[\s\S]*["']code["']\s*:\s*500\b)/i
    — these are Zhipu-specific gRPC-status-as-JSON patterns; port them but flag in a comment.
  - CJK plain: "网络错误", "网络异常", "服务暂时不可用", "服务繁忙", "请求超时", "连接超时", "连接错误"
  - Many libuv error code regexes (econnreset, enetunreach, etimedout, etc.) — these are
    net-new for hermes; hermes already has _TRANSPORT_ERROR_TYPES covering the Python
    exception class names. Port them as message patterns since they appear in error
    strings from Node.js-based proxies/gateways.
  - Stop-reason regexes: /\bstop reason:\s*(?:abort|error|malformed_response|network_error)\b/i
    etc. — port all 4 variants (stop reason / reason / unhandled stop reason / finish_reason).
  - /\boperation was aborted\b/i, /\bstream (?:was )?(?:closed|aborted)\b/i, /^terminated$/i,
    /^stream_read_error$/i, /^request failed$/i,
    /\brequest failed after repeated internal retries\b/i,
    /\bund_err_(?:socket|connect|headers?|body|req_content_length_mismatch|aborted|closed)\b/i,
    /without sending (?:any )?chunks?/i

billing CJK patterns (lines 164-168):
  - CJK plain: "余额不足", "账户余额不足", "欠费", "账户已欠费"

auth CJK patterns (lines 38-43):
  - CJK plain: "无权访问", "认证失败", "鉴权失败", "密钥无效", "apikey 无效"

Zhipu code regexes (lines 44-45):
  - ZAI_BILLING_CODE_1311_RE = /"code"\s*:\s*1311\b/
  - ZAI_AUTH_CODE_1113_RE    = /"code"\s*:\s*1113\b/
  - (1305 is implicitly covered by the overloaded patterns via the message text, but
    we add an explicit numeric handler since the message text alone — "该模型当前访问量过大"
    — isn't in openclaw's overloaded list; openclaw catches it via the code itself.)

JS→Python regex translation rules (per CLAUDE.md constraints):
  - /pattern/i → re.compile(r"pattern", re.IGNORECASE)
  - JS \b works identically in Python re
  - JS [_ ] char class works identically
  - JS lookahead (?=...) works identically in Python re
  - JS non-capturing groups (?:...) work identically
  - Careful: JS /\btpm\b/i — \b at start before "t" works in Python too
  - Plain strings become case-insensitive substring matches (lowercase the haystack
    then `in` check, matching how the existing _BILLING_PATTERNS etc. are consumed)
</openclaw_source_excerpts>

<http_200_integration>
# THE CRITICAL FIX — classifying HTTP 200 + error body

Current behavior (the bug):
  Zhipu returns HTTP 200 with body {"error":{"code":"1305","message":"该模型当前访问量过大"}}.
  The OpenAI SDK treats 200 as success and surfaces the response object. The conversation_loop's
  response-shape validator (line 1222-1232 for chat_completions mode) sees
  response.choices is empty/None and sets response_invalid=True. The classifier
  is NEVER called — the response is not an exception. The retry loop burns all
  retries as "InvalidAPIResponse" and gives up.

Fix (Option B per task_scope — minimal conversation_loop change):
  Add a new helper `classify_response_body_error(response)` in error_classifier.py.
  It accepts a parsed response object (not an exception), extracts any error
  structure from response.error / response.choices[0].message.error / body JSON,
  and returns Optional[ClassifiedError]. Returns None when no error pattern matches.

  In conversation_loop.py, insert a 4-6 line pre-check IMMEDIATELY BEFORE the
  `if response_invalid:` block at line 1234. The pre-check:
    1. Calls classify_response_body_error(response) when response_invalid is True.
    2. If the helper returns a ClassifiedError with retryable=True AND a reason
       in {rate_limit, overloaded, server_error, timeout}, convert it to a
       synthetic exception and pass it to the existing classify_api_error path
       (or directly to the retry handler). The simplest implementation: raise a
       synthetic APIError-like exception carrying the body+code, then let the
       existing except handler in conversation_loop do its classification.
    3. If the helper returns None or a non-retryable reason, fall through to the
       existing InvalidAPIResponse path (current behavior preserved).

  IMPORTANT constraint (from task_scope): the actual retry/fallback/give-up loop
  logic at lines 1342-1360+ MUST NOT be modified. Only the response_invalid
  DETECTION block (lines 1155-1234) gets the new pre-check.

  Simplest implementation pattern (executor should adapt to actual code shape):
    # Insert before line 1234:
    if response_invalid:
        _body_err = classify_response_body_error(response)
        if _body_err is not None and _body_err.retryable and _body_err.reason in {
            FailoverReason.rate_limit, FailoverReason.overloaded,
            FailoverReason.server_error, FailoverReason.timeout,
        }:
            # Synthesize an exception so the existing error-handling path
            # (which already calls classify_api_error) takes over.
            _synth = _SyntheticBodyError(str(response.error if response else ""))
            _synth.body = _extract_response_body_dict(response)
            raise _synth
    # Existing code continues here:
    if response_invalid:
        agent._invoke_api_request_error_hook(...)
</http_200_integration>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port openclaw patterns + Zhipu codes into error_classifier.py</name>
  <files>agent/error_classifier.py, tests/agent/test_error_classifier.py</files>
  <behavior>
    - Test: classify_api_error(Exception("该模型当前访问量过大")) → FailoverReason.overloaded (CJK overloaded pattern)
    - Test: classify_api_error(Exception("请求过于频繁")) → FailoverReason.rate_limit (CJK rateLimit)
    - Test: classify_api_error(Exception("内部错误")) → FailoverReason.server_error (CJK serverError)
    - Test: classify_api_error(Exception("请求超时")) → FailoverReason.timeout (CJK timeout)
    - Test: classify_api_error(Exception("余额不足")) → FailoverReason.billing (CJK billing)
    - Test: classify_api_error(Exception("认证失败")) → FailoverReason.auth (CJK auth)
    - Test: Zhipu code 1305 → FailoverReason.overloaded, via body {"error":{"code":"1305","message":"..."}}
    - Test: Zhipu code 1311 → FailoverReason.billing, via body {"error":{"code":"1311","message":"..."}}
    - Test: Zhipu code 1113 → FailoverReason.auth, via body {"error":{"code":"1113","message":"..."}}
    - Test: Zhipu code 1305 also matches when body is raw JSON string containing `"code":1305` (no structured error object)
    - Test: English openclaw patterns still work — "service_unavailable", "overloaded_error", "at capacity", "high demand"
    - Test: Regression — all 200+ existing tests in test_error_classifier.py pass unchanged
  </behavior>
  <action>
    1. READ the openclaw source file fully (path in <openclaw_source_excerpts>): `/home/kai/.nvm/versions/node/v24.13.0/lib/node_modules/openclaw/dist/failover-matches-C-tab7FS.js`. The excerpts above are verbatim but the executor should confirm before transcribing.

    2. ADD four new module-level pattern lists in agent/error_classifier.py, slotted near the existing pattern lists (around line 200, after _SSL_TRANSIENT_PATTERNS). Naming convention: `_OPENCLAW_RATE_LIMIT_PATTERNS`, `_OPENCLAW_OVERLOADED_PATTERNS`, `_OPENCLAW_SERVER_ERROR_PATTERNS`, `_OPENCLAW_TIMEOUT_PATTERNS`. Use Python `re` for the regex entries and plain lowercase strings for the substring entries. PRESERVE CJK CHARACTERS EXACTLY. Add a module-level docstring comment block above the lists referencing the openclaw source path so future maintainers can sync upstream.

    3. ADD three Zhipu numeric-code handlers as module-level constants:
       - `ZHIPU_OVERLOADED_CODE_1305_RE = re.compile(r'"code"\s*:\s*1305\b')`
       - `ZHIPU_BILLING_CODE_1311_RE = re.compile(r'"code"\s*:\s*1311\b')` (matches openclaw naming `ZAI_BILLING_CODE_1311_RE`)
       - `ZHIPU_AUTH_CODE_1113_RE = re.compile(r'"code"\s*:\s*1113\b')`

    4. ADD CJK billing patterns ("余额不足", "账户余额不足", "欠费", "账户已欠费") to the existing `_BILLING_PATTERNS` list. ADD CJK auth patterns ("无权访问", "认证失败", "鉴权失败", "密钥无效", "apikey 无效") to the existing `_AUTH_PATTERNS` list.

    5. EXTEND `_classify_by_error_code` (around line 1089) to handle Zhipu numeric codes BEFORE the existing string-code matches. Match against the string form of the code (since `_extract_error_code` returns a string): `if code_lower in {"1305"}: return result_fn(FailoverReason.overloaded, retryable=True)`. Do the same for 1311→billing (retryable=False, should_rotate_credential=True, should_fallback=True) and 1113→auth (retryable=False, should_rotate_credential=True, should_fallback=True).

    6. EXTEND `_classify_by_message` (around line 1144) — at the END, just before the final `return None`, add a fallback scan that runs each openclaw category list against `error_msg` and returns the corresponding FailoverReason. This catches the HTTP-200-with-error-body case where the message text contains the CJK pattern but no status code or structured code triggered an earlier branch. ALSO apply the three ZHIPU_*_RE regexes against `error_msg` here as a belt-and-suspenders catch (openclaw does this because some transport layers flatten the JSON).

    7. WRITE tests in a new test class `TestOpenclawCjkPatterns` in tests/agent/test_error_classifier.py. Use @pytest.mark.parametrize for the CJK pattern coverage. Add a separate `TestZhipuErrorCodes` class for the 1305/1311/1113 cases — include both structured-body and raw-JSON-string variants.

    8. VERIFY no regressions: run `pytest tests/agent/test_error_classifier.py -x` — all existing tests must still pass.

    CONVENTIONS (CLAUDE.md):
    - `from __future__ import annotations` is already at the top of error_classifier.py — keep it.
    - Use lazy `%s` log formatting in any logger calls (none expected in this task, but if added).
    - Use `re.compile(r"...")` with raw strings for all regexes.
    - Snake_case for all new identifiers.
    - CJK strings are plain Python str literals (no escapes needed — Python 3 source is UTF-8 by default).
    - Do NOT add `encoding="utf-8"` to anything — this task does not touch any `open()` calls.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_error_classifier.py -x --tb=short -q</automated>
  </verify>
  <done>
    - All 4 openclaw CJK pattern categories ported (rateLimit, overloaded, serverError, timeout) with both English regex and CJK plain string entries preserved verbatim.
    - Zhipu codes 1305/1311/1113 each have an explicit handler in _classify_by_error_code AND a body-JSON regex fallback in _classify_by_message.
    - CJK billing + CJK auth patterns appended to existing _BILLING_PATTERNS / _AUTH_PATTERNS.
    - New test classes TestOpenclawCjkPatterns + TestZhipuErrorCodes pass.
    - All pre-existing tests in test_error_classifier.py pass unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add classify_response_body_error helper + conversation_loop hook</name>
  <files>agent/error_classifier.py, agent/conversation_loop.py, tests/agent/test_error_classifier.py</files>
  <behavior>
    - Test: classify_response_body_error(response_with_error_code_1305) returns ClassifiedError(reason=FailoverReason.overloaded, retryable=True)
    - Test: classify_response_body_error(response_with_no_error) returns None
    - Test: classify_response_body_error(response_with_choices_message_error) extracts error from choices[0].message.error and classifies it
    - Test: classify_response_body_error(response_with_dict_error) handles response.error as dict
    - Test: classify_response_body_error(None) returns None (no crash)
    - Test: Integration — a response with error body {"code":"1305","message":"该模型当前访问量过大"} classifies as overloaded via the helper
    - Test: Regression — existing response-shape validation tests still pass
  </behavior>
  <action>
    1. ADD a new public function `classify_response_body_error(response) -> Optional[ClassifiedError]` in agent/error_classifier.py. Place it AFTER the `classify_api_error` function (around line 742) but BEFORE the private `_classify_by_status` helper. Signature:
       ```python
       def classify_response_body_error(response: Any) -> Optional[ClassifiedError]:
           """Classify an error embedded in a successful-HTTP response object.
           
           Some providers (notably ZhipuAI) return HTTP 200 with an error body
           like {"error":{"code":"1305","message":"该模型当前访问量过大"}}.
           The OpenAI SDK treats this as a successful response, leaving the
           agent to discover the error by noticing response.choices is empty.
           This helper extracts the embedded error and runs it through the
           standard classifier so the retry loop can route it correctly.
           
           Returns None if no recognizable error structure is found.
           """
       ```
       Implementation: extract error text/code from (in order): response.error (attribute or dict), response.choices[0].message.error, response.body (if dict). Build a synthetic exception-like and call classify_api_error internally, OR factor out a shared `_classify_from_parts(message, code, body, status_code=None)` helper that both classify_api_error and this function can call. The executor decides — but the simplest path is to construct a small `_SyntheticBodyError` exception with `.body`, `.status_code=None`, and `str(error)=message`, then call `classify_api_error(synth, provider="", model="")`.

    2. ADD a small private `_SyntheticBodyError(Exception)` helper class in error_classifier.py — its only job is to carry a `body` dict and a string message so `classify_api_error`'s `_extract_error_body` / `_extract_message` helpers work on it.

    3. WRITE tests in a new test class `TestClassifyResponseBodyError` in tests/agent/test_error_classifier.py. Use simple mock response objects (a small `_MockResponse` dataclass with `error`, `choices`, `body`, `model` attributes). Cover: None response, response with no error, response with response.error as object (OpenAI style), as dict, response with choices[0].message.error, and the critical Zhipu 1305 case.

    4. MODIFY agent/conversation_loop.py — insert the response_invalid pre-check at line 1234 (BEFORE the existing `if response_invalid:` block). Pattern:
       ```python
       # Pre-check: some providers return HTTP 200 with an embedded error
       # body (notably ZhipuAI 1305/1311/1113). The OpenAI SDK treats 200
       # as success, so the response-shape validator above flags
       # response_invalid. Before burning a retry as "InvalidAPIResponse",
       # try classifying the embedded error — if it's retryable (rate_limit,
       # overloaded, server_error, timeout), raise a synthetic exception so
       # the existing error-handling path (which calls classify_api_error)
       # takes over with the correct reason. See: openclaw failover-matches.
       if response_invalid:
           from agent.error_classifier import classify_response_body_error, FailoverReason
           _body_err = classify_response_body_error(response)
           if (
               _body_err is not None
               and _body_err.retryable
               and _body_err.reason in {
                   FailoverReason.rate_limit,
                   FailoverReason.overloaded,
                   FailoverReason.server_error,
                   FailoverReason.timeout,
               }
           ):
               # Reuse the existing exception handler by raising a synthetic
               # exception that carries the body. The except block below
               # already calls classify_api_error and routes to the correct
               # retry/fallback path.
               from agent.error_classifier import _SyntheticBodyError
               _synth = _SyntheticBodyError(_body_err.message)
               _synth.body = _extract_response_body_as_dict(response)
               raise _synth
       ```
       The `from agent.error_classifier import` is intentionally local (lazy) to avoid changing module-top imports — match the existing local-import pattern used elsewhere in conversation_loop.py. The `_extract_response_body_as_dict` may need to be a tiny local helper, or inline the dict extraction (the executor should look at how the existing block at lines 1276-1300 already extracts response.error).

       CRITICAL: Do NOT modify any other part of conversation_loop.py. The retry/fallback/give-up loop (lines 1342+) stays untouched. Only the response_invalid pre-check gets added.

    5. VERIFY the integration end-to-end: write ONE integration test that constructs a mock response mimicking the Zhipu 1305 case (response.error = {"code":"1305","message":"该模型当前访问量过大"}, response.choices = []), calls classify_response_body_error, and asserts the result is overloaded+retryable. This test lives in test_error_classifier.py — full end-to-end conversation_loop testing is out of scope for this quick task (the helper is the contract).

    CONVENTIONS:
    - Keep `from __future__ import annotations` at the top of error_classifier.py.
    - The new function uses `Any` from typing — import it if not already (it already is, line 17).
    - Lazy `%s` log formatting for any logger calls.
    - The conversation_loop change should add ≤10 lines including the comment.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_error_classifier.py -x --tb=short -q && python -c "from agent.error_classifier import classify_response_body_error, FailoverReason; from types import SimpleNamespace; r = SimpleNamespace(error={'code':'1305','message':'该模型当前访问量过大'}, choices=[], model='glm-4.6'); c = classify_response_body_error(r); assert c is not None and c.reason == FailoverReason.overloaded, f'got {c}'; print('OK')"</automated>
  </verify>
  <done>
    - classify_response_body_error() public function exists in error_classifier.py and returns ClassifiedError or None.
    - Handles all four error shapes: response.error (object), response.error (dict), response.choices[0].message.error, response.body dict.
    - The Zhipu 1305 case classifies as overloaded via this helper.
    - conversation_loop.py has the ≤10-line pre-check inserted before the existing response_invalid block.
    - The retry/fallback/give-up loop (lines 1342+) is UNMODIFIED.
    - New test class TestClassifyResponseBodyError passes.
    - All pre-existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider → agent | Untrusted error response text crosses this boundary — provider may return malformed or attacker-controlled error bodies (e.g. via a compromised upstream proxy). classify_api_error and the new helper must not execute provider-supplied code or trust provider-supplied structure beyond pattern matching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t0q-01 | Tampering | classify_response_body_error parsing of response.error | mitigate | Treat all response attributes as untrusted. Use `isinstance(...)` checks before dict/string access (matching the defensive style already used in _extract_error_code lines 1307-1320). Never `eval()` provider JSON — only `json.loads` with try/except. Cap message length at 500 chars (existing _extract_message pattern). |
| T-t0q-02 | Information Disclosure | CJK pattern strings in error logs | accept | Error messages already flow through agent._clean_error_message in conversation_loop (line 1330). No PII in pattern strings themselves. Low risk. |
| T-t0q-03 | Denial of Service | Regex catastrophic backtracking on openclaw patterns | mitigate | All ported patterns are linear-time (no nested quantifiers). The JS lookahead patterns (lines 111-112 of source) compile to two positive lookaheads — also linear. Validate with a quick `re.compile` test; if Python's `re` warns about complexity, switch that pattern to a plain substring match. |
| T-t0q-04 | Tampering | Zhipu code regex applied to raw body JSON | accept | Regex only reads, never writes. False-positive match would just trigger retry — no security impact. |

No package installs in this plan (no npm/pip/cargo) — supply-chain gate N/A.
</threat_model>

<verification>
1. `pytest tests/agent/test_error_classifier.py -x --tb=short -q` — all tests pass (existing + new).
2. `python -c "from agent.error_classifier import classify_response_body_error, FailoverReason; ..."` — the inline Zhipu 1305 smoke test from Task 2's `<verify>` block succeeds.
3. `ruff check agent/error_classifier.py agent/conversation_loop.py tests/agent/test_error_classifier.py` — no lint errors (specifically the PLW1514 encoding rule).
4. `grep -c "该模型当前访问量过大\|请求过于频繁\|余额不足\|认证失败" agent/error_classifier.py` returns ≥4 (sanity: CJK patterns landed in the file).
5. `grep -c "classify_response_body_error" agent/conversation_loop.py` returns ≥1 (sanity: the hook is wired in).
6. The retry/fallback/give-up block in conversation_loop.py (lines ~1342-1400) is byte-identical to the pre-task version — verify with `git diff agent/conversation_loop.py` and confirm only the pre-check insertion changed.
</verification>

<success_criteria>
- Zhipu HTTP 200 + body {"error":{"code":"1305","message":"该模型当前访问量过大"}} classifies as FailoverReason.overloaded via classify_response_body_error, and the conversation_loop pre-check raises a synthetic exception so the existing retry path handles it correctly (retryable=True).
- Zhipu codes 1311 → billing, 1113 → auth handled explicitly in _classify_by_error_code.
- All 4 openclaw CJK pattern categories (rateLimit, overloaded, serverError, timeout) ported verbatim — pattern strings preserved exactly (simplified Chinese codepoints intact).
- CJK billing ("余额不足" etc.) and CJK auth ("认证失败" etc.) patterns appended to existing _BILLING_PATTERNS / _AUTH_PATTERNS.
- All 200+ existing tests in test_error_classifier.py pass unchanged.
- The conversation_loop retry/fallback/give-up loop is unmodified.
- Ruff lint passes.
</success_criteria>

<output>
Create `.planning/quick/260626-t0q-cjk-error-classification/260626-t0q-SUMMARY.md` when done.
</output>

---
phase: 42-data-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/platform_metrics/plugin.yaml
  - plugins/platform_metrics/__init__.py
  - plugins/platform_metrics/schema.py
  - plugins/platform_metrics/adapters/__init__.py
  - plugins/platform_metrics/adapters/base.py
  - plugins/platform_metrics/README.md
  - plugins/platform_metrics/tests/__init__.py
  - plugins/platform_metrics/tests/test_schema.py
  - plugins/platform_metrics/tests/test_adapter_base.py
  - plugins/platform_metrics/tests/test_plugin_registration.py
autonomous: true
requirements: [DATA-01, DATA-02]
tags: [plugin, pydantic, platform-metrics, schema, scope-discipline]

must_haves:
  truths:
    - "Plugin is discoverable via existing hermes_cli/plugins.py registry — plugin.yaml loads, __init__.register(ctx) is called"
    - "PlatformMetrics Pydantic schema validates all 5 metric fields (completion_rate / hook_dropoff_rate / engagement_rate / save_rate / comment_rate) in [0.0, 1.0] range, plus platform identifier + variant_id FK"
    - "FeedbackRecordExtension composes with v6.0 FeedbackRecord via feedback_id FK — it does NOT import or extend the FeedbackRecord class (Option A scope discipline)"
    - "Base adapter class declares the activation contract: subclass overrides `env_key` + `fetch(variant_id) -> PlatformMetrics`; missing env key raises `AdapterNotActivatedError` (NOT NotImplementedError — clean failure mode for operator-side activation)"
    - "Zero files modified under agent/ or hermes_cli/ (Hermes core untouched)"
    - "Zero files modified under skills/movie-experts/ (FOUND-08 byte-frozen rule preserved)"
  artifacts:
    - path: "plugins/platform_metrics/plugin.yaml"
      provides: "Plugin manifest (name=platform_metrics, version=0.1.0, kind=standalone, provides_tools=[] — DATA-01..04 ship modules not tools)"
      contains: "platform_metrics"
    - path: "plugins/platform_metrics/__init__.py"
      provides: "register(ctx) entrypoint — registers no tools in Plan 01 (Plan 04 adds formula stats CLI hook)"
      contains: "def register"
    - path: "plugins/platform_metrics/schema.py"
      provides: "PlatformMetrics + FeedbackRecordExtension + TuningSuggestion Pydantic models (DATA-01 schema + DATA-02 composition + DATA-03 JSONL shape)"
      exports: ["PlatformMetrics", "FeedbackRecordExtension", "TuningSuggestion", "MetricTrigger", "Platform"]
    - path: "plugins/platform_metrics/adapters/__init__.py"
      provides: "ADAPTER_REGISTRY dict + register_adapter(name, cls) helper — populated by Plan 42-02"
      contains: "ADAPTER_REGISTRY"
    - path: "plugins/platform_metrics/adapters/base.py"
      provides: "BasePlatformAdapter ABC + AdapterNotActivatedError + get_adapter(name) factory"
      contains: "BasePlatformAdapter"
    - path: "plugins/platform_metrics/tests/test_schema.py"
      provides: "Pydantic validation tests for PlatformMetrics + FeedbackRecordExtension + TuningSuggestion"
      min_lines: 80
    - path: "plugins/platform_metrics/tests/test_adapter_base.py"
      provides: "BasePlatformAdapter activation + factory tests"
      min_lines: 60
  key_links:
    - from: "plugins/platform_metrics/schema.py (FeedbackRecordExtension.feedback_id)"
      to: "agent/feedback_store.py (FeedbackStore.record_feedback return value = record_id)"
      via: "feedback_id string FK — NO Python import of FeedbackRecord class (scope discipline)"
      pattern: "feedback_id"
    - from: "plugins/platform_metrics/adapters/__init__.py (register_adapter)"
      to: "plugins/platform_metrics/adapters/base.py (BasePlatformAdapter subclass check)"
      via: "isinstance(cls, BasePlatformAdapter) — registry rejects non-subclasses"
      pattern: "BasePlatformAdapter"
    - from: "plugins/platform_metrics/schema.py (PlatformMetrics.variant_id)"
      to: "skills/kais-movie-pipeline/references/asset-bus-schema.md (variants[] entry identity)"
      via: "variant_id string matches variants[].source_master_hash OR composite platform+aspect — read at adapter fetch time"
      pattern: "variant_id"
---

<objective>
Build the `plugins/platform_metrics/` plugin scaffold + the three Pydantic schemas (DATA-01 `PlatformMetrics` + DATA-02 `FeedbackRecordExtension` + DATA-03 `TuningSuggestion`) + the abstract adapter base class. This is Wave 1 foundation — Plan 42-02 fills in 5 concrete adapters, Plan 42-03 implements tuning_loop using these schemas, Plan 42-04 wires CLI + docs.

Purpose: DATA-01 schema half + DATA-02 composition design + the adapter contract that 42-02's 5 stubs will conform to. **Scope discipline (Option A):** `FeedbackRecordExtension` is a NEW Pydantic model that composes WITH v6.0 `FeedbackRecord` via `feedback_id` string FK — it does NOT modify `agent/feedback_schema.py` (Hermes core, READ ONLY).

Output: A working `plugins/platform_metrics/` plugin skeleton (6 Python files + plugin.yaml + README) that:
1. Loads via existing `hermes_cli/plugins.py` discovery (no core code changes)
2. Validates `PlatformMetrics` with all 5 metric fields clamped to [0.0, 1.0]
3. Validates `FeedbackRecordExtension` linking `feedback_id` (string FK to v6.0 `record_id`) to a `platform_metrics: dict[Platform, PlatformMetrics]` map — per-platform bucketed (DATA-02 backward-compat: additive, v6.0 records unaffected)
4. Defines `BasePlatformAdapter` ABC + `AdapterNotActivatedError` + factory — the contract Plan 42-02's 5 stubs conform to
5. Defines `TuningSuggestion` Pydantic shape (used by Plan 42-03's JSONL queue)

This plan ships no concrete adapter implementations (those are Plan 42-02). It ships no tuning loop logic (Plan 42-03). It ships no CLI (Plan 42-04). Pure scaffold + contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-data-convergence/PLAN.md

# Analog plugins — mirror these patterns exactly
@plugins/formula_library/plugin.yaml
@plugins/formula_library/__init__.py
@plugins/formula_library/schema.py
@plugins/pipeline_state/plugin.yaml
@plugins/review_gates/plugin.yaml

# v6.0 FeedbackStore — READ ONLY (consume-only via public API; scope discipline)
# These define the FK target — DO NOT import the FeedbackRecord class itself
@agent/feedback_store.py
@agent/feedback_schema.py

# variants[] schema — Phase 38 output that PlatformMetrics.variant_id keys into
@skills/kais-movie-pipeline/references/asset-bus-schema.md
@skills/kais-movie-pipeline/references/platform-master-slicing.md

# EVOL-02 JSONL queue pattern (mirror for Plan 42-03's tuning queue)
@agent/evolution/queue.py

<interfaces>
<!-- Existing contracts the executor builds against. No codebase exploration needed. -->

From agent/feedback_store.py:FeedbackStore (v6.0 — consume-only):
```python
class FeedbackStore:
    def record_feedback(self, record: FeedbackRecord) -> str: ...  # returns record_id
    def query(self, *, skill_id=None, source=None, verdict=None,
              since=None, until=None, include_superseded=False) -> list[FeedbackRecord]: ...
    def summary(self, *, skill_id=None, source=None) -> dict[str, dict[str, Any]]: ...
    def get_record(self, record_id: str) -> FeedbackRecord | None: ...
    # record_id format: f"{ts_unix_micro}_{sha256[:8]}" (sortable, collision-resistant)
```

From agent/feedback_schema.py:FeedbackRecord (v6.0 — DO NOT IMPORT THIS CLASS):
```python
class FeedbackRecord(BaseModel):
    skill_id: str       # validated against _KNOWN_EXPERT_IDS
    expert_id: str
    source: Literal["cli", "kais_aigc", "manual"]
    verdict: Literal["good", "needs_work", "bad"]
    correction: str = ""
    revised_output: str | None = None
    output_snapshot: OutputSnapshot
    ts: datetime         # tz-aware ISO 8601
```
NOTE: The `record_id` returned by `FeedbackStore.record_feedback` is the FK target for
`FeedbackRecordExtension.feedback_id`. We store the STRING, not the record itself.

From skills/kais-movie-pipeline/references/asset-bus-schema.md §Phase 38 Slots:
```json
{
  "variants[]": [
    {
      "platform": "抖音竖屏" | "抖音横屏" | "快手竖屏" | "B站横屏" | "小红书竖屏" | "视频号横屏" | "红果",
      "aspect_ratio": "9:16" | "16:9" | ...,
      "length": <int seconds>,
      "hook_timestamps": {"opening_hook_start_sec": float, ...},
      "cut_points": [{"timestamp_sec": float, "reason": str}, ...],
      "source_master_hash": "<sha256>"
    }
  ]
}
```

From plugins/formula_library/schema.py:Formula.eval_score (Plan 42-03 write-back target):
```python
class Formula(BaseModel):
    formula_id: str
    ...
    eval_score: Optional[float] = None   # ← Plan 42-03 library_writer updates this field
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plugin scaffold + plugin.yaml + __init__.py + adapters/__init__.py</name>
  <files>
    plugins/platform_metrics/plugin.yaml
    plugins/platform_metrics/__init__.py
    plugins/platform_metrics/adapters/__init__.py
    plugins/platform_metrics/README.md
    plugins/platform_metrics/tests/__init__.py
    plugins/platform_metrics/tests/test_plugin_registration.py
  </files>
  <behavior>
    - Test 1 (test_plugin_yaml_loads): read plugins/platform_metrics/plugin.yaml as YAML; assert name=="platform_metrics", version=="0.1.0", kind=="standalone", provides_tools==[] (Plan 01 ships no tools; Plan 04 adds formula stats CLI hook, not a tool)
    - Test 2 (test_register_callable): assert plugins.platform_metrics.register is callable; calling register with a Mock ctx does not raise (ctx.register_tool is never called in Plan 01 since provides_tools is empty)
    - Test 3 (test_adapter_registry_empty_at_import): from plugins.platform_metrics.adapters import ADAPTER_REGISTRY; assert isinstance(ADAPTER_REGISTRY, dict) and len(ADAPTER_REGISTRY) == 0 (Plan 42-02 populates it)
    - Test 4 (test_register_adapter_rejects_non_subclass): define a dummy class that does NOT inherit BasePlatformAdapter; calling register_adapter("foo", DummyClass) raises TypeError
  </behavior>
  <action>
    Create the `plugins/platform_metrics/` directory tree. The directory layout mirrors `plugins/formula_library/` exactly:
    - `plugins/platform_metrics/plugin.yaml` — manifest YAML with name="platform_metrics", version="0.1.0", description="platform metrics plugin — 5 platform API adapter stubs (douyin/kuaishou/weixin_video/xiaohongshu/bilibili) + FeedbackRecordExtension composition + formula tuning loop + hermes formula stats CLI (Phase 42 DATA ships schema in 42-01, adapters in 42-02, tuning_loop in 42-03, CLI+ref in 42-04)", author="kais-movie-agent", kind="standalone", provides_tools=[] (empty list — Plan 01 ships modules not tools)
    - `plugins/platform_metrics/__init__.py` — `def register(ctx) -> None:` body that imports nothing (Plan 01 ships no tools; Plan 04 adds `ctx.register_cli_command(name="formula", ...)` call here). Add module docstring noting "Phase 42 DATA — Option A new plugin (scope discipline: no Hermes core edits). Plan 01 ships scaffold + schemas + adapter base. Plan 02 fills 5 adapter stubs. Plan 03 ships tuning_loop + library_writer. Plan 04 ships CLI + ref + SKILL.md Step 15." Use `from __future__ import annotations` at top.
    - `plugins/platform_metrics/adapters/__init__.py` — module-level `ADAPTER_REGISTRY: dict[str, type[BasePlatformAdapter]] = {}` and a `register_adapter(name: str, cls: type[BasePlatformAdapter]) -> None:` helper that asserts `issubclass(cls, BasePlatformAdapter)` (import the base lazily inside the function body to avoid circular import — base.py imports from adapters/__init__.py for the registry). Export `ADAPTER_REGISTRY` and `register_adapter` in `__all__`.
    - `plugins/platform_metrics/README.md` — bilingual (~80-150 lines): English header + 中文 overview. Document: (1) scope discipline (Option A — composes WITH v6.0 FeedbackRecord, does not modify it); (2) DATA-01..04 mapping; (3) directory layout; (4) operator-action-handoff (5 env vars documented in Plan 42-04 .env.example patch); (5) V9-FUTURE-01 deferred.
    - `plugins/platform_metrics/tests/__init__.py` — empty file (pytest discovery).
    - `plugins/platform_metrics/tests/test_plugin_registration.py` — 4 tests per the behavior block above. Use `unittest.mock.Mock` for the ctx; load plugin.yaml with `yaml.safe_load(open(...))`.

    Per CLAUDE.md: `from __future__ import annotations` at top of every `.py`. Double-quoted strings. `encoding="utf-8"` on every `open()` (Ruff PLW1514 — even in test where the yaml.load happens). `logger = logging.getLogger(__name__)` in __init__.py (defensive, even if unused in Plan 01).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/platform_metrics/tests/test_plugin_registration.py -v</automated>
  </verify>
  <done>
    - plugin.yaml exists and loads as valid YAML with the 5 required fields
    - register(ctx) callable, no-op in Plan 01 (no tools registered)
    - ADAPTER_REGISTRY dict exists, empty
    - register_adapter rejects non-BasePlatformAdapter subclasses
    - README.md bilingual, documents scope decision + DATA-01..04 mapping
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: schema.py — PlatformMetrics + FeedbackRecordExtension + TuningSuggestion + MetricTrigger</name>
  <files>
    plugins/platform_metrics/schema.py
    plugins/platform_metrics/tests/test_schema.py
  </files>
  <behavior>
    - Test 1 (test_platform_metrics_valid): build PlatformMetrics(platform="抖音", variant_id="v1", completion_rate=0.85, hook_dropoff_rate=0.12, engagement_rate=0.05, save_rate=0.03, comment_rate=0.01, fetched_at=datetime(...)); assert all fields preserved; assert .platform == "抖音"
    - Test 2 (test_platform_metrics_range_clamped): each of the 5 metric fields rejects values outside [0.0, 1.0] — completion_rate=-0.01 raises pydantic.ValidationError; engagement_rate=1.5 raises; save_rate=2.0 raises; etc. (5 sub-tests, one per field, parametrize with @pytest.mark.parametrize)
    - Test 3 (test_platform_metrics_invalid_platform): PlatformMetrics(platform="Twitter", ...) raises ValidationError (platform must be one of the 7 supported — douyin/kuaishou/weixin_video/xiaohongshu/bilibili/红果/视频号; mirror the variants[] platform Literal from asset-bus-schema.md)
    - Test 4 (test_platform_metrics_missing_variant_id): PlatformMetrics(...) without variant_id raises ValidationError (variant_id is required — it's the FK to variants[].source_master_hash)
    - Test 5 (test_feedback_record_extension_composition): build FeedbackRecordExtension(feedback_id="12345_abcdef", platform_metrics={"抖音": <PlatformMetrics>, "快手": <PlatformMetrics>}); assert .feedback_id == "12345_abcdef"; assert len(.platform_metrics) == 2; assert isinstance(.platform_metrics["抖音"], PlatformMetrics)
    - Test 6 (test_feedback_record_extension_does_not_import_v6): grep-test — `grep -c "from agent.feedback_schema" plugins/platform_metrics/schema.py` returns 0 (scope discipline enforcement: composition via string FK, NOT class import)
    - Test 7 (test_feedback_record_extension_empty_metrics_ok): FeedbackRecordExtension with empty platform_metrics dict is valid (a record may have no platform data yet — DATA-02 backward-compat: v6.0 records continue to work; the extension is a separate document)
    - Test 8 (test_tuning_suggestion_schema): build TuningSuggestion(suggestion_id="...", formula_id="urban-fantasy-light-01", trigger=MetricTrigger.HIGH_HOOK_DROPOFF, observed_metric=0.32, threshold=0.20, suggested_action="...", rationale="...", status="pending", ts_queued=...); assert roundtrip via model_dump_json + model_validate_json
    - Test 9 (test_tuning_suggestion_invalid_status): TuningSuggestion with status="bogus" raises ValidationError (status Literal must be pending/applied/rejected)
    - Test 10 (test_metric_trigger_enum): MetricTrigger has 4 values — HIGH_HOOK_DROPOFF, HIGH_COMPLETION_LOW_ENGAGEMENT, LOW_COMPLETION, LOW_SAVE_RATE; assert each has a string value matching its identifier
  </behavior>
  <action>
    Create `plugins/platform_metrics/schema.py` with `from __future__ import annotations` at top. Module docstring documents: "Phase 42 DATA — Pydantic v2 schemas. Option A scope discipline: FeedbackRecordExtension composes WITH v6.0 FeedbackRecord via feedback_id string FK — it does NOT import or extend agent.feedback_schema.FeedbackRecord. v6.0 records remain untouched (DATA-02 backward-compat)."

    Define these Pydantic v2 models (use `pydantic.BaseModel`, `field_validator`, `ConfigDict(extra="forbid")` for tight schemas):

    1. **`Platform` Literal** — `Literal["douyin", "kuaishou", "weixin_video", "xiaohongshu", "bilibili", "红果", "视频号"]`. Module-level alias. Comment that the first 5 are the DATA-01 platforms with API adapters (Plan 42-02); the last 2 (红果/视频号) lack documented open APIs in v9.0 and are deferred to V9-FUTURE-01.

    2. **`PlatformMetrics(BaseModel)`** — DATA-01 schema:
       - `platform: Platform`
       - `variant_id: str` (FK to `variants[].source_master_hash` OR composite key; comment that adapter implementation resolves this)
       - `completion_rate: float` — clamp [0.0, 1.0] via `@field_validator` (完播率)
       - `hook_dropoff_rate: float` — clamp [0.0, 1.0] (卡点跳出率 — measured against `variants[].hook_timestamps.opening_hook_end_sec`)
       - `engagement_rate: float` — clamp [0.0, 1.0] (互动率 = (likes+comments+shares)/views)
       - `save_rate: float` — clamp [0.0, 1.0] (收藏率)
       - `comment_rate: float` — clamp [0.0, 1.0] (评论率)
       - `fetched_at: datetime` — tz-aware ISO 8601 (mirror FeedbackRecord._ts_has_tz validator)
       - Each metric field has a single `@field_validator` that raises `ValueError` on out-of-range. Comment the field semantics in 中文+EN.

    3. **`FeedbackRecordExtension(BaseModel)`** — DATA-02 composition (Option A):
       - `feedback_id: str` — FK to v6.0 `FeedbackStore.record_feedback` return value (`record_id` format = `f"{ts_unix_micro}_{sha256[:8]}"`). **REQUIRED field**. Comment: "String FK only — does NOT import agent.feedback_schema.FeedbackRecord (scope discipline). v6.0 FeedbackStore owns the original record; this extension is a sibling document."
       - `platform_metrics: dict[Platform, PlatformMetrics] = {}` — default empty dict for DATA-02 backward-compat (a v6.0 record with no platform data has no extension; an extension with empty metrics is valid but unusual). Use `Field(default_factory=dict)`.
       - `ts_created: datetime` — tz-aware.
       - `model_config = ConfigDict(extra="forbid")` — strict.
       - Add a `@model_validator(mode="after")` that asserts platform keys in `platform_metrics` are all valid `Platform` Literal values (defensive — Pydantic already enforces, but explicit failure message helps operators).

    4. **`MetricTrigger(enum.Enum)`** — DATA-03 enum with 4 values:
       - `HIGH_HOOK_DROPOFF = "high_hook_dropoff"` — hook_dropoff_rate > threshold (default 0.20)
       - `HIGH_COMPLETION_LOW_ENGAGEMENT = "high_completion_low_engagement"` — completion_rate > 0.70 AND engagement_rate < 0.05
       - `LOW_COMPLETION = "low_completion"` — completion_rate < 0.30
       - `LOW_SAVE_RATE = "low_save_rate"` — save_rate < 0.01
       (These thresholds are defaults; tuning_loop.py parameterizes them — Plan 42-03.)

    5. **`TuningSuggestion(BaseModel)`** — DATA-03 JSONL queue record (mirror `agent/evolution/queue.py:PatchRecord` shape):
       - `suggestion_id: str = Field(min_length=1)` — content-addressed `f"{formula_id}_{trigger}_{ts_unix}"`
       - `formula_id: str = Field(min_length=1)` — FK to `plugins/formula_library/library/*.json:formula_id`
       - `trigger: MetricTrigger` — the enum that fired
       - `observed_metric: float` — the metric value that crossed the threshold
       - `threshold: float` — the threshold it crossed
       - `suggested_action: str = Field(min_length=1)` — concrete change proposal (e.g. "加 hook 强度 — 当前 hook_pattern=contrast 建议升级 emotional_peak")
       - `rationale: str = Field(min_length=1)` — why this trigger fires for this formula
       - `evidence: list[str] = Field(default_factory=list)` — feedback_ids + variant_ids cited
       - `status: Literal["pending", "applied", "rejected"] = "pending"` — JSONL queue lifecycle
       - `ts_queued: str = Field(min_length=1)` — ISO 8601 UTC
       - `commit_sha: str | None = None` — set on "applied" (Plan 42-03 library_writer)
       - `ts_applied: str | None = None`
       - `reason: str | None = None` — set on "rejected"
       - `ts_rejected: str | None = None`
       - `model_config = ConfigDict(extra="forbid")`
       - `@model_validator(mode="after")` asserts status in Literal set (Pydantic already enforces; explicit error).

    `__all__` exports: Platform, PlatformMetrics, FeedbackRecordExtension, MetricTrigger, TuningSuggestion, plus a `SUPPORTED_PLATFORMS_WITH_ADAPTERS: tuple[str, ...] = ("douyin", "kuaishou", "weixin_video", "xiaohongshu", "bilibili")` constant for Plan 42-02's adapter stubs to assert against.

    Test file `tests/test_schema.py` — 10 tests per behavior block. Use `@pytest.mark.parametrize` for the 5 metric range-clamp tests. Use `pydantic.ValidationError` for negative tests. Use `datetime(2026, 6, 27, tzinfo=timezone.utc)` for tz-aware timestamps. The grep test (#6) uses `subprocess.run(["grep", "-c", "from agent.feedback_schema", "plugins/platform_metrics/schema.py"], capture_output=True, text=True).stdout.strip() == "0"` — assert the string count is zero.

    Per CLAUDE.md: `from __future__ import annotations` at top. `from datetime import datetime, timezone`. `from enum import Enum`. `from typing import Literal`. `from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator`. Double-quoted strings throughout. `encoding="utf-8"` on any `open()` (none in schema.py — Pydantic handles I/O). `logger = logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/platform_metrics/tests/test_schema.py -v</automated>
  </verify>
  <done>
    - PlatformMetrics validates all 5 metrics clamped [0.0, 1.0]
    - Platform Literal has 7 values; SUPPORTED_PLATFORMS_WITH_ADAPTERS lists the 5 Plan-42-02 ones
    - FeedbackRecordExtension composes via feedback_id string FK (zero imports of agent.feedback_schema — grep-enforced)
    - MetricTrigger enum has 4 values
    - TuningSuggestion has all 13 fields with status Literal validator
    - All 10 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: adapters/base.py — BasePlatformAdapter ABC + AdapterNotActivatedError + get_adapter factory</name>
  <files>
    plugins/platform_metrics/adapters/base.py
    plugins/platform_metrics/tests/test_adapter_base.py
  </files>
  <behavior>
    - Test 1 (test_adapter_not_activated_error): raise AdapterNotActivatedError("DOUYIN_API_KEY"); assert the env_key name is preserved in the message
    - Test 2 (test_base_cannot_instantiate): BasePlatformAdapter(name="test", env_key="TEST_KEY") raises TypeError (ABC with abstract fetch)
    - Test 3 (test_subclass_must_override_name_and_env_key): define a subclass that only implements fetch(); instantiate with positional args; it should fail because name/env_key class attrs are not set OR raise NotImplementedError. (Pin the contract: subclasses set `name: ClassVar[str]` and `env_key: ClassVar[str]`.)
    - Test 4 (test_concrete_subclass_activates_with_env): monkeypatch.setenv("TEST_API_KEY", "fake"); define a TestAdapter(BasePlatformAdapter) with name="test", env_key="TEST_API_KEY", fetch returns a stub PlatformMetrics; instance = TestAdapter(); assert instance.is_activated is True; assert instance.fetch("v1") returns the stub
    - Test 5 (test_concrete_subclass_without_env_raises): monkeypatch.delenv("TEST_API_KEY", raising=False); TestAdapter().fetch("v1") raises AdapterNotActivatedError mentioning "TEST_API_KEY"
    - Test 6 (test_get_adapter_factory): register TestAdapter via register_adapter("test", TestAdapter); adapter = get_adapter("test"); assert isinstance(adapter, BasePlatformAdapter)
    - Test 7 (test_get_adapter_unknown_raises): get_adapter("nonexistent") raises KeyError (or ValueError — pick one and document)
  </behavior>
  <action>
    Create `plugins/platform_metrics/adapters/base.py` with `from __future__ import annotations` at top. Module docstring documents: "Abstract base class for the 5 platform API adapters (DATA-01). Subclasses set `name` + `env_key` ClassVars and implement `fetch(variant_id) -> PlatformMetrics`. The base class enforces operator-side env-key activation: missing env var → AdapterNotActivatedError (clean failure for V9-FUTURE-01 deferred live ingestion)."

    Define:

    1. **`class AdapterNotActivatedError(RuntimeError)`** — raised when `os.environ.get(self.env_key)` returns None/empty. Constructor takes `env_key: str` and formats a clear message: `f"Platform adapter {self.name} requires env var {env_key} (operator-action-handoff: set in ~/.hermes/.env per data-convergence.md)"`. Inherits from RuntimeError (NOT Exception base — operators may want to catch it specifically).

    2. **`class BasePlatformAdapter(AbstractBaseClass)`** — use `abc.ABC` (NOT `typing.Protocol` — we need `@abstractmethod` enforcement):
       - `name: ClassVar[str]` — subclass overrides (e.g. "douyin")
       - `env_key: ClassVar[str]` — subclass overrides (e.g. "DOUYIN_API_KEY")
       - `__init__(self, *, timeout: float = 30.0) -> None:` — keyword-only. Store `self._timeout`. No env-key check in __init__ (defer to fetch so import is side-effect-free).
       - `is_activated(self) -> bool:` — concrete method: `return bool(os.environ.get(self.env_key))`
       - `_require_activated(self) -> None:` — concrete helper: if not `self.is_activated()`, raise `AdapterNotActivatedError(self.env_key)`.
       - `@abstractmethod async def fetch(self, variant_id: str) -> PlatformMetrics:` — subclasses implement. Async because all 5 platforms are HTTP-based (httpx.AsyncClient). Docstring: "Fetch fresh PlatformMetrics for the given variant_id. Raises AdapterNotActivatedError if env_key is unset. Live HTTP call — Plan 42-02's stubs return NotImplementedError on the HTTP path (V9-FUTURE-01 deferred); only the env-key check + schema validation is shipped in v9.0."

    3. **`def get_adapter(name: str) -> BasePlatformAdapter:`** — factory. Import `ADAPTER_REGISTRY` from `plugins.platform_metrics.adapters`. Lookup `name` in registry; if missing, `raise KeyError(f"unknown platform adapter: {name!r}; registered: {sorted(ADAPTER_REGISTRY)}")`. Instantiate + return. Document that Plan 42-02's 5 adapters register themselves via `register_adapter` at module import time (mirror `tools/registry.py` self-registration pattern).

    Add module-level `__all__ = ["BasePlatformAdapter", "AdapterNotActivatedError", "get_adapter"]`.

    Test file `tests/test_adapter_base.py` — 7 tests per behavior block. Use `monkeypatch.setenv` / `monkeypatch.delenv` for env var tests. For the abstract instantiation test, `with pytest.raises(TypeError): BasePlatformAdapter(name="x", env_key="Y")`. For the concrete subclass test, define inline `class TestAdapter(BasePlatformAdapter)` with `name = "test"` / `env_key = "TEST_API_KEY"` / `async def fetch(self, variant_id): self._require_activated(); return PlatformMetrics(platform="douyin", variant_id=variant_id, completion_rate=0.5, hook_dropoff_rate=0.5, engagement_rate=0.05, save_rate=0.02, comment_rate=0.01, fetched_at=datetime(2026,6,27,tzinfo=timezone.utc))`. Run async tests with `pytest_asyncio` (already in pyproject.toml dev deps). Register the TestAdapter in the registry before the get_adapter test, then clean it up (del ADAPTER_REGISTRY["test"]) in a finally/fixture to avoid test pollution.

    Per CLAUDE.md: `from __future__ import annotations`. `import abc, os`. `from typing import ClassVar`. `from datetime import datetime, timezone`. `from plugins.platform_metrics.schema import PlatformMetrics`. Double-quoted strings. Lazy import of `ADAPTER_REGISTRY` inside `get_adapter` body (avoid circular import — adapters/__init__.py imports from base.py for the isinstance check in register_adapter).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/platform_metrics/tests/test_adapter_base.py -v</automated>
  </verify>
  <done>
    - AdapterNotActivatedError is a RuntimeError subclass with clear env_key message
    - BasePlatformAdapter cannot be instantiated directly (ABC enforcement)
    - Subclasses must set name + env_key ClassVars and implement async fetch
    - is_activated reads env var; _require_activated raises on missing
    - get_adapter factory reads from ADAPTER_REGISTRY, KeyError on unknown
    - All 7 tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env → adapter | Adapter reads API key from `~/.hermes/.env` via `os.environ.get`. Compromised env = compromised adapter. |
| v6.0 FeedbackStore → platform_metrics | Public-API consumption only (query/get_record). No write-back to v6.0 (scope discipline). |
| variants[] (Phase 38) → adapter fetch | variant_id is operator-controlled via pipeline_state. Adapters must treat it as untrusted input (Pydantic validation gates it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | Adapter reads env var | mitigate | `os.environ.get` only; never log the value. `is_activated` returns bool not the key. |
| T-42-02 | Tampering | variant_id crosses trust boundary | mitigate | PlatformMetrics Pydantic schema validates variant_id is a non-empty str; fetch implementations must treat as opaque ID, never interpolate into URLs without urllib.parse.quote (Plan 42-02 concern, flagged here). |
| T-42-03 | Repudiation | FeedbackRecordExtension audit trail | accept | Extension records feedback_id FK; audit trail lives in v6.0 dedup registry. Low risk for v9.0 stubs. |
| T-42-04 | Elevation of Privilege | Adapter attempts to write to v6.0 FeedbackStore | mitigate | Scope discipline: zero imports of `agent.feedback_store.FeedbackStore` write methods. Grep-enforced in CI at Phase 43 VALIDATE. |
| T-42-SC | Tampering | (no package installs in this plan — pure stdlib + pydantic) | accept | N/A — no pip/npm installs. Pydantic + httpx already Hermes core deps. |
</threat_model>

<verification>
- All 3 test files pass: `python -m pytest plugins/platform_metrics/tests/ -v`
- plugin.yaml validates as YAML with 5 required fields
- `grep -c "from agent.feedback_schema" plugins/platform_metrics/schema.py` returns 0 (scope discipline)
- `grep -c "from agent.feedback_store" plugins/platform_metrics/` returns 0 (consume-only via public API; actual import happens in Plan 42-03 tuning_loop.py, not Plan 01)
- No files modified under `agent/`, `hermes_cli/`, `skills/movie-experts/`, or any prior-shipped v9.0 path
</verification>

<success_criteria>
- Plugin loads via existing registry (test_plugin_registration.py all 4 tests pass)
- PlatformMetrics Pydantic validates all 5 metric fields clamped [0.0, 1.0] + 7-platform Literal (10 schema tests pass)
- FeedbackRecordExtension composes via string FK — zero imports of agent.feedback_schema (grep-enforced)
- BasePlatformAdapter ABC + AdapterNotActivatedError + get_adapter factory ready for Plan 42-02's 5 stubs
- TuningSuggestion Pydantic shape ready for Plan 42-03's JSONL queue
- README bilingual + documents Option A scope discipline
- FOUND-08 preserved (no movie-experts touch)
</success_criteria>

<output>
Create `.planning/phases/42-data-convergence/42-01-SUMMARY.md` when done. Standard SUMMARY template.
</output>

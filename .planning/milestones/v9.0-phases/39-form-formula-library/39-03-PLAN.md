---
phase: 39-form-formula-library
plan: 03
type: execute
wave: 2
depends_on: [39-01, 39-02]
files_modified:
  - skills/kais-movie-pipeline/SKILL.md
  - skills/movie-experts/theory_critic/SKILL.md
  - plugins/formula_library/tests/__init__.py
  - plugins/formula_library/tests/test_lookup.py
  - plugins/formula_library/tests/test_schema.py
  - plugins/formula_library/tests/test_found08_byte_diff.py
autonomous: true
requirements: [FORM-04]
tags: [skill-patch, body-only, found-08, tests, integration]

must_haves:
  truths:
    - "kais-movie-pipeline/SKILL.md has a new Step 0 section in the body documenting formula_lookup invocation (genre + mood + platform → top-3)"
    - "kais-movie-pipeline/SKILL.md frontmatter is byte-identical to pre-v9.0 (FOUND-08 preserved)"
    - "theory_critic/SKILL.md body has a new formula_reference optional input section"
    - "theory_critic/SKILL.md frontmatter is byte-identical to pre-v9.0 (FOUND-08 preserved)"
    - "All Plan 01 + 02 artifacts load end-to-end: 10 formulas validate, lookup returns ranked results, plugin registers cleanly"
    - "Byte-diff test confirms zero frontmatter changes on both SKILL.md files"
  artifacts:
    - path: "skills/kais-movie-pipeline/SKILL.md"
      provides: "Step 0 formula_lookup section added to body (frontmatter untouched)"
      contains: "formula_lookup"
    - path: "skills/movie-experts/theory_critic/SKILL.md"
      provides: "formula_reference optional input section added to body (frontmatter untouched)"
      contains: "formula_reference"
    - path: "plugins/formula_library/tests/test_lookup.py"
      provides: "Integration tests for lookup_formulas + load_library + handler dispatch"
      contains: "def test_"
    - path: "plugins/formula_library/tests/test_schema.py"
      provides: "Pydantic schema validation tests (all FORM-02 fields + edge cases)"
      contains: "def test_"
    - path: "plugins/formula_library/tests/test_found08_byte_diff.py"
      provides: "Byte-diff test: SKILL.md frontmatter unchanged for kais-movie-pipeline + theory_critic"
      contains: "FOUND-08"
  key_links:
    - from: "skills/kais-movie-pipeline/SKILL.md Step 0 section"
      to: "plugins/formula_library/tools.py::formula_lookup"
      via: "operator invokes formula_lookup tool before Step 1 (p01 hook_topic)"
      pattern: "formula_lookup"
    - from: "skills/movie-experts/theory_critic/SKILL.md formula_reference section"
      to: "plugins/formula_library/library/*.json"
      via: "theory_critic accepts optional formula_reference to ground its critique in proven patterns"
      pattern: "formula_reference"
---

<objective>
Wire the formula_library into the kais-movie-pipeline as Step 0 + theory_critic's optional formula_reference input (FORM-04), and ship the integration + FOUND-08 byte-diff test suite that verifies the entire Phase 39 deliverable.

Purpose: FORM-04 requires both SKILL.md body patches + lookup integration. This plan closes the loop: Plan 01 ships the engine, Plan 02 ships the data, this plan wires both into the SKILL.md corpus and proves correctness via tests.

Output: Two body-only SKILL.md patches (frontmatter byte-identical per FOUND-08) + 3 test files (lookup integration + schema validation + FOUND-08 byte-diff).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Skills to patch (body-only — frontmatter byte-frozen per FOUND-08)
@skills/kais-movie-pipeline/SKILL.md
@skills/movie-experts/theory_critic/SKILL.md

# Plan 01 deliverables (engine being wired)
@plugins/formula_library/plugin.yaml
@plugins/formula_library/schema.py
@plugins/formula_library/lookup.py
@plugins/formula_library/tools.py
@plugins/formula_library/__init__.py

# Plan 02 deliverables (data being wired)
@plugins/formula_library/library/README.md

<interfaces>
<!-- Existing SKILL.md structure — patches insert AFTER existing content, BEFORE the next major section. -->

From skills/kais-movie-pipeline/SKILL.md frontmatter (MUST be byte-identical after patch):
```yaml
---
name: kais-movie-pipeline
description: "Top-level orchestration skill: runs the 13-step V8.6 short-drama pipeline end-to-end (EN)..."
version: 0.1.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
prerequisites:
  tools: [delegate_task, skill_view, pipeline_checkpoint_save, pipeline_checkpoint_load, asset_bus_read, asset_bus_write, gate_submit, gate_resolve]
metadata:
  hermes:
    tags: [...]
    related_skills: [...]
    expert_id: kais-movie-pipeline
    metrics: [...]
    pipeline:
      version: v86
      step_count: 13
      gate_count: 8
      parallel_shots: 4
---
```

The body starts with `# Kais-Movie-Pipeline Orchestration Skill (短剧管线编排)` and currently has sections: When to use / References / Pipeline DAG / Phase ↔ Expert Mapping / Review Gates / Asset Bus Schema / Runner / Operator Setup / What NOT to do.

**Insertion point for Step 0:** Add a new `## Step 0 — Formula Lookup (Phase 39 v9.0)` section IMMEDIATELY AFTER the `## References` table and BEFORE `## Pipeline DAG`. This positions formula_lookup as a pre-Step-1 activity conceptually. Use H2 (##) heading to match the existing section style.

From skills/movie-experts/theory_critic/SKILL.md frontmatter (MUST be byte-identical):
```yaml
---
name: theory_critic
description: "Theory & Criticism Expert..."
...
metadata:
  hermes:
    tags: [...]
    related_skills: [...]
    expert_id: theory_critic
    metrics: [theoretical_rigor, citation_accuracy, critical_depth, framework_fit]
---
```

theory_critic body ends with `## V8.6 Pipeline Sync (Phase 26 v5.0)` section. **Insertion point:** Add a new `## Formula Reference Integration (Phase 39 v9.0)` section at the END of the file (after the V8.6 Pipeline Sync section).

**Per CLAUDE.md FOUND-08 rule:** zero changes to the YAML frontmatter block (between `---` markers). Body patches only. The byte-diff test (Task 3) enforces this.

<!-- Found-08 verification pattern reference -->
From .planning/milestones/v5.0-MILESTONE-AUDIT.md (pattern for byte-diff audits):
- Extract frontmatter via splitting on `---` markers (first pair = frontmatter, rest = body)
- Compare frontmatter bytes against a known-good baseline (git HEAD or pre-patch snapshot)
- Body diff is allowed; frontmatter diff fails the test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md body patches — Step 0 + formula_reference (FORM-04)</name>
  <files>
    skills/kais-movie-pipeline/SKILL.md,
    skills/movie-experts/theory_critic/SKILL.md
  </files>
  <action>
    **PATCH 1: skills/kais-movie-pipeline/SKILL.md** — insert a new `## Step 0 — Formula Lookup (Phase 39 v9.0)` section.

    Locate the `## References` section (which ends with the External canonical source line). Insert immediately AFTER that section and BEFORE `## Pipeline DAG`. The new section structure:

    ```
    ## Step 0 — Formula Lookup (Phase 39 v9.0)

    > 来源:v9.0 FORM phase 新 plugin `plugins/formula_library/`(`formula_lookup` 工具)。Step 0 是 additive 前置 step,**不替换** V8.6 13-step 主线;Step 1 (p01 hook_topic) 仍为主线起点。Step 0 在 Step 1 之前执行,产出 formula_reference 作为 Step 1 的可选输入。

    ### When to invoke Step 0

    每个 episode 启动前默认调用 Step 0 —— 从 `plugins/formula_library/` 检索 top-3 爆款公式,作为创意起点。Operator 可显式跳过(例如:operator 已有自定义题材 / 已手工选定公式)。

    ### Step 0 I/O contract

    | Field | Direction | Type | Required | Notes |
    |-------|-----------|------|----------|-------|
    | `genre` | input | enum (5) | yes | 都市奇幻 / 悬疑反转 / 家庭情感 / 校园青春 / 职场商战 |
    | `mood` | input | enum (2) | yes | 轻喜剧 / 虐心 |
    | `platform` | input | enum (6) | yes | 抖音 / 快手 / B站 / 小红书 / 视频号 / 红果 |
    | `top_k` | input | int | optional | 默认 3 |
    | `formulas` | output | list[Formula] | yes | 按 platform_fit 降序排列的 top-k 公式(完整 schema:formula_id / genre / mood / pacing / hook_pattern / characters / runtime_sec / platform_fit / citation / verified_date / eval_score) |

    ### Invocation

    ```bash
    # In hermes-agent runtime (before starting p01):
    formula_lookup(genre="都市奇幻", mood="轻喜剧", platform="抖音", top_k=3)
    ```

    返回示例(截断):
    ```json
    [
      {"formula_id": "urban-fantasy-light-01", "genre": "都市奇幻", "mood": "轻喜剧", "platform_fit": [{"platform": "抖音", "fit_score": 0.9}], ...},
      ...
    ]
    ```

    ### Downstream consumption

    Step 0 的输出 `formulas[0]`(top-1)作为:
    - **Step 1 (p01 hook_topic) 的可选输入** —— `hook_retention` 专家可参考公式的 `hook_pattern` + `characters` 设计本 episode 的 3 秒钩子
    - **theory_critic 的可选 `formula_reference` 输入**(参见 theory_critic SKILL.md §Formula Reference Integration)—— 理论批评时可对照公式评分

    ### V8.6 编号关系

    Step 0 是 **additive** —— 不重排 V8.6 13-step 主线编号。Step 0 在所有 13 步之前执行,但其输出是 advisory(建议性),不通过任何 gate。即使 Step 0 返回空 library(library 加载失败 / 无匹配公式),管线仍可从 Step 1 正常启动。

    ### Plugin location

    Plugin 源码:`plugins/formula_library/`(plugin.yaml + schema.py + lookup.py + tools.py + library/*.json)。Plugin 通过现有 `hermes_cli/plugins.py` registry 自动发现,无需 Hermes 核心代码修改。

    ### Cross-References

    - [`plugins/formula_library/README.md`](../../plugins/formula_library/README.md) — Plugin 文档(双语)+ 用法 + 添加公式指南
    - [`references/genre-anchor-urban-fantasy.md`](./references/genre-anchor-urban-fantasy.md) — V1 题材锚定(都市奇幻·轻喜剧)— 多条 seed 公式的 citation 源
    - [`../../movie-experts/hook_retention/references/three-second-hooks.md`](../../movie-experts/hook_retention/references/three-second-hooks.md) — 3 秒钩子分类法(formula `hook_pattern` 字段的 5 类型源)
    ```

    **PATCH 2: skills/movie-experts/theory_critic/SKILL.md** — append a new `## Formula Reference Integration (Phase 39 v9.0)` section at the END of the file (after the existing `## V8.6 Pipeline Sync (Phase 26 v5.0)` section).

    ```
    ## Formula Reference Integration (Phase 39 v9.0)

    > 来源:v9.0 FORM phase 新 plugin `plugins/formula_library/`(formula_lookup 工具,Step 0 集成)。本节是 additive patch —— 增 `formula_reference` 可选输入,**不替换** 现有 theory_critic 任何 section。

    ### Optional input: `formula_reference`

    theory_critic 现接受可选输入 `formula_reference: Formula | None`(默认 None)。当 operator / 上游 step(kais-movie-pipeline Step 0 formula_lookup)传入一个 formula 时,theory_critic 在批评输出中:

    1. **比对作品与公式的契合度** —— 该 episode 的视觉 / 叙事选择是否落在公式的 `genre + mood + pacing + hook_pattern` 区间内
    2. **标注偏离公式的创新点** —— 公式之外的原创元素,评估其理论合理性(参考本专家 §Core Capabilities 的 5-step school determination)
    3. **`formula_reference.eval_score` 透传** —— 若公式已通过 v6.0 eval gate(eval_score 非 None),在批评中引用该分数作为 baseline 参考

    ### When formula_reference is None

    缺省行为不变 —— theory_critic 按现有 §Workflow 流程做批评,不强制要求 formula_reference。Operator 可显式传入 None 跳过公式对照。

    ### I/O contract addition

    | Field | Direction | Type | Required | Notes |
    |-------|-----------|------|----------|-------|
    | `formula_reference` | input | Formula \| None | optional | 默认 None;来自 kais-movie-pipeline Step 0 formula_lookup 的 top-1 结果 |

    本字段在 Output Format `critique.json` 中不直接体现 —— 它影响 `dimensions.audiovisual_analysis` / `dimensions.narrative_analysis` 的对照视角。若 formula_reference 非 None,在 `cross_references` 中新增 `applied_formula: <formula_id>`。

    ### Cross-References

    - [`../../kais-movie-pipeline/SKILL.md §Step 0 — Formula Lookup`](../../kais-movie-pipeline/SKILL.md) — Step 0 formula_lookup 调用契约
    - [`../../../plugins/formula_library/README.md`](../../../plugins/formula_library/README.md) — Plugin 文档
    - [`../../../plugins/formula_library/schema.py`](../../../plugins/formula_library/schema.py) — Formula Pydantic schema 定义
    ```

    **CRITICAL — FOUND-08 enforcement:** Use the Edit tool with surgical insertions. Do NOT rewrite either file. Do NOT touch any line between the `---` frontmatter markers. Insert only at the documented insertion points. The byte-diff test in Task 3 will catch any frontmatter drift.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -c "
import pathlib
# kais-movie-pipeline SKILL.md
kmp = pathlib.Path('skills/kais-movie-pipeline/SKILL.md').read_text(encoding='utf-8')
assert '## Step 0 — Formula Lookup (Phase 39 v9.0)' in kmp, 'missing Step 0 section'
assert 'formula_lookup' in kmp
# theory_critic SKILL.md
tc = pathlib.Path('skills/movie-experts/theory_critic/SKILL.md').read_text(encoding='utf-8')
assert '## Formula Reference Integration (Phase 39 v9.0)' in tc, 'missing formula_reference section'
assert 'formula_reference' in tc
# FOUND-08 spot check: frontmatter first line unchanged
assert kmp.startswith('---\nname: kais-movie-pipeline\n'), 'kmp frontmatter start drifted'
assert tc.startswith('---\nname: theory_critic\n'), 'tc frontmatter start drifted'
print('OK both SKILL.md patched, frontmatter first-line preserved')
"</automated>
  </verify>
  <done>
    - kais-movie-pipeline/SKILL.md body contains `## Step 0 — Formula Lookup (Phase 39 v9.0)` section with I/O contract + invocation + downstream consumption
    - theory_critic/SKILL.md body contains `## Formula Reference Integration (Phase 39 v9.0)` section with optional `formula_reference` input
    - Both files' frontmatter (between `---` markers) byte-identical to pre-patch (Task 3 verifies this rigorously)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — schema + lookup + handler</name>
  <files>
    plugins/formula_library/tests/__init__.py,
    plugins/formula_library/tests/test_schema.py,
    plugins/formula_library/tests/test_lookup.py
  </files>
  <behavior>
    - Test 1 (test_schema.py): Formula with all FORM-02 fields validates; missing `citation` raises ValidationError; invalid `genre` (e.g. "玄幻修真") raises ValidationError; `fit_score` 1.5 raises; `eval_score: None` accepted
    - Test 2 (test_lookup.py): `load_library()` discovers all 10 seed JSON files in `plugins/formula_library/library/`; no validation errors
    - Test 3 (test_lookup.py): `lookup_formulas(genre="都市奇幻", mood="轻喜剧", platform="抖音")` returns at least 1 result; results sorted by platform_fit[抖音] descending
    - Test 4 (test_lookup.py): `lookup_formulas(genre="悬疑反转", mood="虐心", platform="B站", top_k=3)` returns ≤3 results; all results have genre=悬疑反转 + mood=虐心
    - Test 5 (test_lookup.py): lookup with zero matches returns empty list (no exception)
    - Test 6 (test_lookup.py): handler `_handle_formula_lookup({"genre":"都市奇幻","mood":"轻喜剧","platform":"抖音"})` returns a tool_result envelope with `formulas` key containing JSON-serialized Formula list
    - Test 7 (test_lookup.py): invalid args (missing `genre`) returns tool_error envelope
  </behavior>
  <action>
    Create `plugins/formula_library/tests/__init__.py` (empty file — Python package marker).

    Create `plugins/formula_library/tests/test_schema.py`:
    - `from __future__ import annotations` + imports from `plugins.formula_library.schema` (Formula, FormulaLibrary, PlatformFit, Citation) + `from datetime import date` + `import pytest` + `from pydantic import ValidationError`.
    - Build a `_valid_formula_kwargs()` helper returning a dict with all required fields populated validly (genre=都市奇幻, mood=轻喜剧, etc.) — tests build variants by `**_valid_formula_kwargs(), field=value` overrides.
    - `class TestFormulaValidation:` with `test_valid_formula`, `test_missing_citation_raises`, `test_invalid_genre_raises`, `test_invalid_mood_raises`, `test_fit_score_out_of_range_raises`, `test_eval_score_none_accepted`, `test_runtime_sec_range`.
    - `class TestFormulaLibrary:` with `test_filter_by_genre_mood`, `test_by_id_lookup`, `test_load_from_dir_skips_invalid` (uses `tmp_path` fixture with one valid + one invalid JSON; assert library has 1 formula + warning logged).

    Create `plugins/formula_library/tests/test_lookup.py`:
    - Imports + `from plugins.formula_library.lookup import lookup_formulas, load_library` + `from plugins.formula_library.tools import _handle_formula_lookup` + `from tools.registry import tool_result, tool_error`.
    - `class TestSeedLibraryLoad:` with `test_load_library_discovers_10_seed_formulas` (skip if `library/` dir doesn't have 10 files — useful during dev) + `test_load_library_caches_result` (call twice, assert same object identity via monkeypatch).
    - `class TestLookupRanking:` with `test_lookup_returns_ranked_by_platform_fit`, `test_lookup_filters_strict_on_genre_mood`, `test_lookup_empty_when_no_match`, `test_lookup_respects_top_k`, `test_lookup_default_top_k_is_3`.
    - `class TestHandlerDispatch:` with `test_handler_returns_tool_result_envelope`, `test_handler_missing_required_arg_returns_tool_error`.

    Per CLAUDE.md test conventions: `from __future__ import annotations`, double-quoted strings, `encoding="utf-8"` on any file read, snake_case test names, `class Test<Thing>:` grouping. Use `pytest.skip()` for tests that depend on Plan 02 data not yet present (defensive).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/formula_library/tests/test_schema.py plugins/formula_library/tests/test_lookup.py -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `plugins/formula_library/tests/{__init__.py,test_schema.py,test_lookup.py}` all exist
    - `pytest plugins/formula_library/tests/` passes all tests
    - Schema validation tests cover all FORM-02 fields + edge cases
    - Lookup tests verify ranking + filtering + handler dispatch against the real 10 seed formulas from Plan 02
  </done>
</task>

<task type="auto">
  <name>Task 3: FOUND-08 byte-diff audit test</name>
  <files>plugins/formula_library/tests/test_found08_byte_diff.py</files>
  <action>
    Create `plugins/formula_library/tests/test_found08_byte_diff.py` — a single-purpose test that enforces the FOUND-08 frozen rule for both SKILL.md files patched in Task 1.

    Strategy: extract the frontmatter (content between the first pair of `---` markers) from both SKILL.md files, hash it via sha256, and compare against a known-good baseline hash captured at the start of Phase 39. The baseline is computed by `git show HEAD:path/to/SKILL.md` at test runtime (so the test is independent of the working-tree state).

    Structure:
    - `from __future__ import annotations` + imports (hashlib, pathlib, subprocess, pytest).
    - Module constants: `_SKILL_PATHS = ("skills/kais-movie-pipeline/SKILL.md", "skills/movie-experts/theory_critic/SKILL.md")`.
    - Helper `_extract_frontmatter(text: str) -> str`: split on `---\n` markers; return the content between the first two markers (inclusive of markers). Raises `ValueError` if fewer than 2 markers found.
    - Helper `_git_head_version(rel_path: str) -> str`: `subprocess.run(["git", "show", f"HEAD:{rel_path}"], cwd=repo_root, capture_output=True, text=True, encoding="utf-8")`; on non-zero exit, `pytest.skip("git HEAD version not available — run from repo root")`.
    - Helper `_repo_root() -> Path`: walk up from `Path(__file__)` until `.git/` is found.
    - `class TestFound08Preserved:`
      - `@pytest.mark.parametrize("rel_path", _SKILL_PATHS)`
      - `def test_frontmatter_byte_identical_to_head(self, rel_path: str) -> None:`
        - Read current working-tree version: `current = Path(_repo_root() / rel_path).read_text(encoding="utf-8")`
        - Read HEAD version: `head = _git_head_version(rel_path)`
        - Extract frontmatter from both: `current_fm = _extract_frontmatter(current)`, `head_fm = _extract_frontmatter(head)`
        - Hash both: `hashlib.sha256(current_fm.encode("utf-8")).hexdigest()` vs `head_fm_hash`
        - Assert equal; on failure, raise `AssertionError` with both hashes + the diff for operator triage.
      - `def test_body_grew_not_shrank(self, ...)`: sanity check — body length should be ≥ HEAD body length (since we only added content). Catches accidental truncation.

    **Note on baseline:** `HEAD` is the commit before this phase's changes. If the operator has already committed Plan 03 changes, `HEAD` will reflect them and the test trivially passes. The test's value is at PR-review time + Phase 43 VALIDATE-02 byte-diff audit time. Document this in a docstring.

    **Cross-Reference to VALIDATE-02:** Phase 43 VALIDATE-02 will do a milestone-wide byte-diff against start commit `a2a20d2be`. This test is the per-phase guard that catches drift early. Phase 43's audit is authoritative; this test is defensive.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest plugins/formula_library/tests/test_found08_byte_diff.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `plugins/formula_library/tests/test_found08_byte_diff.py` exists
    - Test passes: frontmatter of both SKILL.md files is byte-identical to HEAD
    - Body length sanity check passes (no accidental truncation)
    - Test docstring explains its relationship to Phase 43 VALIDATE-02 audit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md body patch → operator workflow | Body additions describe new Step 0 / formula_reference; misleading docs = operator misconfiguration |
| FOUND-08 frontmatter → reader (other skills, DAG tooling) | Any frontmatter drift breaks downstream consumers expecting stable expert_id + metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Tampering | SKILL.md frontmatter | mitigate | Byte-diff test (Task 3) enforces FOUND-08; surgical Edit-tool inserts only; CI gates on test pass |
| T-39-09 | Information disclosure | Step 0 docs reference plugin internals | accept | All referenced paths (plugins/formula_library/*, references/*) are public shipped artifacts — no credentials, no internal-only URLs |
| T-39-10 | Denial of Service | formula_lookup tool fails to load library | mitigate | Per Plan 01 Task 1 degrade-gracefully rule: load_from_dir skips invalid files + logs warning; Step 0 docs explicitly state "even if library empty, pipeline proceeds from Step 1" |
</threat_model>

<verification>
- kais-movie-pipeline/SKILL.md body has Step 0 section; frontmatter byte-identical to HEAD
- theory_critic/SKILL.md body has formula_reference section; frontmatter byte-identical to HEAD
- All Plan 01 + Plan 02 + Plan 03 tests pass via `pytest plugins/formula_library/tests/`
- FOUND-08 byte-diff test passes for both SKILL.md files
- formula_lookup end-to-end: schema → 10 seed formulas → ranking → handler dispatch all work
</verification>

<success_criteria>
- FORM-04 ✓: kais-movie-pipeline Step 0 + theory_critic formula_reference both wired (body-only patches)
- FOUND-08 ✓: zero frontmatter changes verified by byte-diff test
- Integration verified ✓: 10 formulas load, lookup returns ranked top-3, handler dispatches correctly
- Test suite passes: `pytest plugins/formula_library/tests/ -v` all green
</success_criteria>

<output>
Create `.planning/phases/39-form-formula-library/39-03-SUMMARY.md` when done.
</output>

---
phase: 16-new-prompt-injector-expert
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
  - .planning/research/v2-pipeline-design/skills-mapping.yaml
autonomous: true
requirements: [NEW-01]
tags: [movie, prompt-engineering, close-out, documentation, dag-diagram, glossary]

must_haves:
  truths:
    - "A reader skimming skills/movie-experts/README.md sees prompt_injector listed as a NEW expert (Phase 16 entry) with its Chinese name 提示注入 + role summary + ref count"
    - "A reader skimming _shared/glossary.md sees a prompt_injector entry with bilingual header + EN↔CN term mapping + cross-references to the 4 related_skills peers"
    - "skills-mapping.yaml line 99-103 prompt_injector entry records Phase 16 completion (sign_off_status: signed_off + signed_off_at + signed_off_by: phase-16 + action_for_v21 updated to note completion)"
    - "ASCII DAG diagram in README shows prompt_injector node between cinematographer/storyboard_designer layer and visual_executor layer"
    - "Corpus tree in README lists prompt_injector/ directory row with its 4 refs"
    - "README footer expert count reflects the new count (per Phase 14/15 self-documenting arithmetic convention)"
  artifacts:
    - path: "skills/movie-experts/README.md"
      provides: "Updated inventory + DAG diagram + corpus tree + footer count reflecting prompt_injector as new expert"
      contains: "prompt_injector"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "prompt_injector bilingual glossary entry"
      contains: "### prompt_injector / 提示注入"
    - path: ".planning/research/v2-pipeline-design/skills-mapping.yaml"
      provides: "sign_off_status: signed_off on prompt_injector entry"
      contains: "signed_off_at: 2026-06-17"
  key_links:
    - from: "skills/movie-experts/README.md"
      to: "skills/movie-experts/prompt_injector/SKILL.md"
      via: "inventory table row + corpus tree + DAG diagram node"
      pattern: "prompt_injector"
    - from: "skills/movie-experts/_shared/glossary.md"
      to: "skills/movie-experts/prompt_injector/SKILL.md"
      via: "glossary entry cross-reference"
      pattern: "prompt_injector"
---

<objective>
Phase 16 close-out documentation. Update README inventory + corpus tree + ASCII DAG diagram + _shared/glossary.md + skills-mapping.yaml sign-off so a reader skimming the docs sees prompt_injector integrated as the new AI-native expert. After this plan, all 6 ROADMAP §16 success criteria are satisfied (criterion #6 specifically requires README 21-expert inventory listing).

Purpose: ROADMAP §16 criterion #6 + NEW-01 documentation half. Phase 18 (validate + docs) will do canonical reconciliation, but this plan completes Phase 16's own close-out per the established Phase 13/14/15 pattern.
Output: README.md (inventory + corpus tree + DAG + footer), _shared/glossary.md (new entry), skills-mapping.yaml (signed_off).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-new-prompt-injector-expert/16-CONTEXT.md
@.planning/phases/16-new-prompt-injector-expert/16-01-PLAN.md

@skills/movie-experts/README.md
@skills/movie-experts/_shared/glossary.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml

<interfaces>
<!-- Current state of the 3 files to modify (as of Phase 15 close-out 2026-06-17): -->

**README.md structure:**
- Line 10: `## 🆕 Phase 8 Update` (existing inventory section header — Phase 16 may need its own section OR append to existing)
- Lines 56-100: inventory tables organized by phase additions (Original 14, Phase 1-5 New 4, Phase 7 New 5, Phase 8 New 3)
- Lines 105-187: ASCII DAG diagram (current state post-Phase 15 — audio_pipeline box has 6 sub-steps, visual_executor has drawer+animator sub-steps)
- Lines 346+: File Layout / corpus tree
- Footer: expert count with self-documenting arithmetic annotation (Phase 15 closed at 17 active: 16 DAG pipeline-roles + continuity alias + compliance_marketing alias minus the 6 audio merges plus audio_pipeline; exact annotation: "− 6 Phase 15 audio_pipeline merge")

**ASCII DAG diagram conventions established in Phases 13-15:**
- Single-line boxes: `┌─────────────────┐` + `│ name 中文 (role)│` + `└────────┬────────┘` (max ~13 chars per line for box label)
- Multi-line boxes for long labels: Phase 13 compliance_gate (`compliance_` / `gate 合规`), Phase 14 visual_executor (`visual_` / `executor 视觉`), Phase 15 audio_pipeline (6-sub-step enumeration)
- Phase 14/15 precedent: when a label exceeds 13-char width, use two-line form + column alignment

**_shared/glossary.md structure:**
- Each entry: `### <term> / <EN translation> / <CN romanization>` H3 header (per Phase 14/15 bilingual header convention)
- Body: brief definition (2-4 sentences) + `**Context:**` paragraph explaining usage
- Examples: `### audio_pipeline / 音频管线专家`, `### visual_executor / 视觉执行专家`

**skills-mapping.yaml current prompt_injector entry (lines 99-103):**
```yaml
  # === NEW (1) — AI-native, no v1 precedent ===
  - new_node: prompt_injector
    v1_expert_id: null
    mapping_type: new_ai_native
    rationale: "Phase 7 §4.7 + §3.3 D3.5: AI-native node; no traditional precedent"
    action_for_v21: "Add prompt_injector expert in v2.1+ skills milestone"
```
Note: this entry has NO `sign_off_status` / `signed_off_at` / `signed_off_by` fields yet. Per Phase 13 close-out precedent (CONTEXT decisions), these traceability fields are added when the phase signs off.

**Footer count pattern (Phase 14/15 precedent):**
Phase 15 final footer: "17 active expert_ids (16 DAG pipeline-roles + continuity alias + compliance_marketing alias − 6 Phase 15 audio_pipeline merges + 1 audio_pipeline merged expert) — Phase 18 will reconcile to canonical 21-expert topology"
Phase 16 adds prompt_injector as a NEW (not a merge) — the count increments by 1 from 17 → 18 (Phase 17 deprecations and Phase 18 canonical reconciliation will arrive at the final 21 count).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: README inventory + corpus tree + DAG diagram + footer update</name>
  <files>
    skills/movie-experts/README.md
  </files>
  <read_first>
    - skills/movie-experts/README.md (full — survey existing inventory sections + ASCII DAG diagram + corpus tree + footer to determine insertion points)
    - skills/movie-experts/prompt_injector/SKILL.md (created by Plan 16-01 — confirm Chinese name 提示注入 + role summary + ref count for inventory row)
    - .planning/phases/15-audio-pipeline-merge/15-03-SUMMARY.md (if exists — Phase 15 close-out pattern reference for README updates; same task structure)
    - .planning/phases/14-visual-executor-merge/14-03-SUMMARY.md (if exists — Phase 14 close-out pattern for multi-line ASCII box form)
  </read_first>
  <action>
    Update `skills/movie-experts/README.md` to reflect prompt_injector as a NEW Phase 16 expert. Four discrete edits:

    **Edit 1: Inventory table — add Phase 16 section (or append to appropriate existing section).**
    Per Phase 13/14/15 precedent (each phase had its own inventory section), add a new `### 1 New Expert (Phase 16 — AI-Native prompt_injector, 2026-06-17)` section after the Phase 8 section (around line 100). Single-row table:

    | Expert | Chinese Name | Role | Source | Refs |
    |--------|--------------|------|--------|------|
    | [`prompt_injector`](./prompt_injector/SKILL.md) | 提示注入专家 | AI-native node translating visual_intent + style_genome + character_assets → model_prompts + consistency_context. Cross-call consistency context + token budget management (≤4000/call). No v1 predecessor (new_ai_native mapping) | 02-NODE-SPECS.md §2.7 + Phase 7 §4.7 D3.5+D2.4 | 4 |

    **Edit 2: ASCII DAG diagram — insert prompt_injector node.**
    prompt_injector sits BETWEEN the upstream intent producers (cinematographer + storyboard_designer) and visual_executor. Per nodes.yaml io_contract, prompt_injector consumes `visual_intent` (from cinematographer) and outputs `model_prompts` + `consistency_context` (to visual_executor). The current DAG (lines 105-187) has a direct cinematographer → storyboard_designer → visual_executor flow. Insert prompt_injector as a stage between storyboard_designer and visual_executor.

    Label width check: `prompt_injector` is 16 chars (exceeds 13-char single-line box width). Use the two-line form per Phase 13/14/15 multi-line box precedent:
    ```
                    ┌─────────────────┐
                    │ prompt_injector │
                    │  提示注入        │
                    └────────┬────────┘
    ```
    Maintain column alignment with surrounding boxes. Arrows: storyboard_designer → prompt_injector → visual_executor. Also add an inbound arrow from character_designer → prompt_injector (character_assets input per nodes.yaml) and a parallel arrow from style_genome → prompt_injector (style_genome_5d input). The creative_source inbound edge is more nuanced — creative_source is the DAG root producing story_kernel which flows through screenplay → cinematographer chain to reach prompt_injector indirectly. Do NOT draw a direct creative_source → prompt_injector arrow (would misrepresent data flow); instead document the indirect path in the `**Key DAG properties**` paragraph below the diagram.

    Add a new bullet to the **Key DAG properties** section (currently lines 189-194): `- **AI-native prompt assembly:** prompt_injector (Phase 16) translates visual_intent + style_genome_5d + character_assets into model_prompts + consistency_context consumed by visual_executor. No traditional cinematography precedent — this node exists because AI generation requires explicit prompt engineering that human director tools did not.`

    **Edit 3: Corpus tree (in File Layout section around line 346+) — add prompt_injector/ directory row.**
    Following Phase 14/15 pattern, add a new row in the corpus tree listing `prompt_injector/` with its 4 refs. Format:
    ```
    prompt_injector/                    # Phase 16 NEW (2026-06-17) — AI-native prompt engineering node
    ├── SKILL.md                        # Prompt Injector Expert (提示注入)
    ├── GAP-REPORT.md                   # placeholder per CONTEXT D-04 (NEW expert, no v1 baseline)
    └── references/
        ├── prompt-engineering-patterns.md  # few-shot / CoT / template / decomposition
        ├── cross-call-consistency.md       # LoRA / IP-Adapter / identity-preserving
        ├── token-budget-management.md      # ≤4000 tokens/call strategies
        ├── model-specific-prompt-templates.md  # FLUX 2 / Veo / Kling (provider-agnostic placeholders)
        └── LICENSE.md                      # MIT + source attribution
    ```

    **Edit 4: Footer expert count.**
    Per Phase 14/15 self-documenting arithmetic convention, update the footer. Phase 15 ended at "17 active expert_ids (16 DAG pipeline-roles + continuity alias + compliance_marketing alias − 6 Phase 15 audio_pipeline merges + 1 audio_pipeline merged expert)". Phase 16 adds prompt_injector as a NEW (+1, no merge offset). New footer text:

    `18 active expert_ids (17 post-Phase-15 + 1 Phase 16 prompt_injector NEW — AI-native, no v1 predecessor) — Phase 17 will deprecate 3 candidates (performer, scene_builder, storyboard_designer); Phase 18 will reconcile to canonical 21-expert topology (16 DAG pipeline-roles + 5 aliases).`

    (The 21 count from ROADMAP §18 criterion #1 includes 16 active + 5 aliases from rename/merge; Phase 16-17 transitions are intermediate states. Document the arithmetic explicitly per Phase 14/15 precedent so Phase 18 audit trail is unambiguous.)
  </action>
  <verify>
    <automated>
    cd /data/workspace/hermes-agent &&
    echo "=== inventory section check ===" &&
    grep -E "Phase 16.*prompt_injector" skills/movie-experts/README.md | head -3 &&
    echo "" &&
    echo "=== prompt_injector link check ===" &&
    grep -E "\[\`prompt_injector\`\]\(\./prompt_injector/SKILL\.md\)" skills/movie-experts/README.md | head -3 &&
    echo "" &&
    echo "=== DAG diagram check — multi-line box ===" &&
    grep -A 2 "prompt_injector" skills/movie-experts/README.md | grep -E "(提示注入|prompt_injector)" | head -3 &&
    echo "" &&
    echo "=== corpus tree check ===" &&
    grep -A 7 "^prompt_injector/" skills/movie-experts/README.md | head -10 &&
    echo "" &&
    echo "=== Key DAG properties bullet check ===" &&
    grep -E "AI-native prompt assembly" skills/movie-experts/README.md &&
    echo "" &&
    echo "=== footer count check ===" &&
    grep -E "18 active expert_ids" skills/movie-experts/README.md &&
    echo "" &&
    echo "=== total prompt_injector mentions in README (expect multiple — inventory + DAG + corpus + footer) ===" &&
    grep -c "prompt_injector" skills/movie-experts/README.md
    </automated>
  </verify>
  <done>
    - README.md inventory section has prompt_injector row with Chinese name + role + ref count + source citation
    - ASCII DAG diagram includes prompt_injector node in correct position (between storyboard_designer and visual_executor) with two-line multi-line box form (per Phase 13-15 precedent)
    - Key DAG properties bullet added documenting AI-native prompt assembly role
    - Corpus tree lists prompt_injector/ directory with 4 refs + LICENSE + GAP-REPORT
    - Footer count updated from 17 → 18 with self-documenting arithmetic annotation (Phase 16 NEW +1)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Glossary entry + skills-mapping.yaml sign-off</name>
  <files>
    skills/movie-experts/_shared/glossary.md
    .planning/research/v2-pipeline-design/skills-mapping.yaml
  </files>
  <read_first>
    - skills/movie-experts/_shared/glossary.md (full — survey existing entry pattern; specifically look at audio_pipeline entry and visual_executor entry for bilingual header convention)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml (lines 60-105 — survey sign_off field pattern + the prompt_injector entry at lines 99-103)
    - .planning/phases/13-expert-rename-alias/13-03-SUMMARY.md (if exists — Phase 13 established the signed_off_at + signed_off_by field convention for skills-mapping.yaml)
    - skills/movie-experts/prompt_injector/SKILL.md (created by Plan 16-01 — confirm exact terminology for glossary entry)
  </read_first>
  <action>
    Two discrete edits across two files.

    **Edit 1: skills/movie-experts/_shared/glossary.md — add prompt_injector entry.**
    Per Phase 14/15 bilingual header convention, append a new H3 entry at the end of the glossary (or in alphabetical/logical position consistent with surrounding entries). Format:

    ```markdown
    ### prompt_injector / 提示注入 / Prompt Injector

    AI-native node (Phase 16 NEW, no v1 predecessor) that translates upstream human intent — `visual_intent` (from cinematographer) + `style_genome_5d` (from style_genome) + `character_assets` (from character_designer) — into model-ready prompts (`model_prompts` + `consistency_context`) consumed by visual_executor. Owns two quality metrics: `cross_call_consistency` (≥0.85 — downstream visual outputs maintain style/identity across multiple gen-model calls) and `prompt_token_efficiency` (≤4000 tokens/call — token budget hard ceiling). Fail modes: `consistency_drift` (consistency context lost between calls → fallback: explicit carry + repeat key constraints) and `prompt_overload` (prompt token overload → fallback: split into structured sections + system prompt for stable constraints). Per v2.0 PRFP Phase 7 §4.7 D3.5+D2.4 derivation.

    **Context:** Distinct from cinematographer (which owns shot intent / composition_lock) — prompt_injector owns the prompt-assembly layer that did not exist in traditional pre-AI cinematography. Provider-agnostic: uses `<image_primary>` / `<video_primary>` placeholders (no literal model names committed as identifiers in SKILL.md body). Related_skills peer set: [creative_source, cinematographer, visual_executor, audio_pipeline]. Mapping type in skills-mapping.yaml: `new_ai_native`.
    ```

    **Edit 2: .planning/research/v2-pipeline-design/skills-mapping.yaml — sign off the prompt_injector entry.**
    Current entry (lines 99-103) lacks sign-off fields. Per Phase 13 close-out precedent (CONTEXT decisions: "Added `signed_off_at: 2026-06-17` + `signed_off_by: phase-13` traceability fields under each signed_off entry"), append three fields to the prompt_injector mapping entry AND update the `action_for_v21` field to record that v3.0 (this milestone) has fulfilled the forward-looking note. After edit, the entry should read:

    ```yaml
      # === NEW (1) — AI-native, no v1 precedent ===
      - new_node: prompt_injector
        v1_expert_id: null
        mapping_type: new_ai_native
        rationale: "Phase 7 §4.7 + §3.3 D3.5: AI-native node; no traditional precedent"
        action_for_v21: "FULFILLED in v3.0 milestone Phase 16 (2026-06-17): prompt_injector expert created at skills/movie-experts/prompt_injector/ with 4 refs + GAP-REPORT + bidirectional related_skills edges to creative_source / cinematographer / visual_executor / audio_pipeline"
        sign_off_status: signed_off
        signed_off_at: 2026-06-17
        signed_off_by: phase-16
    ```

    The `action_for_v21` field is repurposed from a forward-looking note ("Add prompt_injector expert in v2.1+ skills milestone") to a completion record. This preserves audit trail — readers can see the original forward-looking intent + the v3.0 fulfillment in the same field. Per CONTEXT D-06 ("No silent sign-off — prompt_injector entry in skills-mapping.yaml already exists with mapping_type: new_ai_native; this phase just creates the actual skill directory"), this is the explicit sign-off record.
  </action>
  <verify>
    <automated>
    cd /data/workspace/hermes-agent &&
    echo "=== glossary entry check ===" &&
    grep -E "^### prompt_injector / 提示注入" skills/movie-experts/_shared/glossary.md &&
    echo "" &&
    echo "=== glossary body keyword check ===" &&
    grep -E "cross_call_consistency|prompt_token_efficiency|new_ai_native" skills/movie-experts/_shared/glossary.md | head -5 &&
    echo "" &&
    echo "=== skills-mapping.yaml sign_off fields check ===" &&
    grep -A 10 "new_node: prompt_injector" .planning/research/v2-pipeline-design/skills-mapping.yaml | head -12 &&
    echo "" &&
    echo "=== sign_off_status: signed_off check ===" &&
    grep -A 10 "new_node: prompt_injector" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -E "sign_off_status: signed_off" &&
    echo "=== signed_off_at + signed_off_by: phase-16 check ===" &&
    grep -A 10 "new_node: prompt_injector" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -E "signed_off_(at: 2026-06-17|by: phase-16)" &&
    echo "=== action_for_v21 updated to FULFILLED check ===" &&
    grep -A 10 "new_node: prompt_injector" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -E "FULFILLED in v3.0 milestone Phase 16"
    </automated>
  </verify>
  <done>
    - _shared/glossary.md has a prompt_injector H3 entry with bilingual header (prompt_injector / 提示注入 / Prompt Injector) following the Phase 14/15 convention
    - Glossary body covers: AI-native node status, I/O contract, success criteria, fail modes + fallbacks, related_skills peer set, mapping_type
    - skills-mapping.yaml prompt_injector entry has sign_off_status: signed_off + signed_off_at: 2026-06-17 + signed_off_by: phase-16
    - action_for_v21 field repurposed from forward-looking note to FULFILLED-in-v3.0 completion record
    - Audit trail preserved (original forward-looking intent still readable in the updated field)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 16-01 output → Plan 16-02 input | README inventory row + glossary entry content depend on Plan 16-01 having created the SKILL.md with the correct Chinese name, role summary, and ref count. Drift would surface as inventory row mismatch. |
| skills-mapping.yaml sign-off → Phase 18 validation | sign_off_status: signed_off on prompt_injector entry is the canonical audit signal Phase 18 VALIDATE-01 will check. Missing or malformed sign-off fails Phase 18. |
| README DAG diagram → actual collaboration graph | The ASCII DAG must reflect the bidirectional edges established in Plan 16-01 Task 2. Misrepresentation in the diagram would conflict with the SKILL.md related_skills arrays. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | README inventory row content | mitigate | Task 1 verify block greps for Phase 16 section header + prompt_injector link + Chinese name 提示注入 |
| T-16-08 | Tampering | ASCII DAG diagram node position | mitigate | Task 1 action specifies exact insertion point (between storyboard_designer and visual_executor) + multi-line box form (Phase 13-15 precedent) |
| T-16-09 | Repudiation | skills-mapping.yaml sign-off fields | mitigate | Task 2 verify block greps for sign_off_status + signed_off_at + signed_off_by + FULFILLED string; all four must match Phase 13 traceability field convention |
| T-16-10 | Information Disclosure | footer arithmetic | mitigate | Task 1 action quotes the exact footer text; self-documenting arithmetic annotation preserves Phase 14/15 audit trail pattern |
| T-16-11 | Tampering | glossary entry bilingual header form | mitigate | Task 2 verify block greps for `^### prompt_injector / 提示注入` exact form per Phase 14/15 convention |
</threat_model>

<verification>
1. README.md inventory section lists prompt_injector with Chinese name 提示注入 + role + ref count (Plan 16-02 Task 1)
2. ASCII DAG diagram includes prompt_injector node between storyboard_designer and visual_executor with two-line multi-line box form
3. Key DAG properties section has a new bullet documenting AI-native prompt assembly role
4. Corpus tree lists prompt_injector/ directory with 4 refs + LICENSE + GAP-REPORT
5. Footer count updated 17 → 18 with self-documenting arithmetic annotation
6. _shared/glossary.md has prompt_injector H3 entry with bilingual header + body covering I/O contract + metrics + fail modes + related_skills
7. skills-mapping.yaml prompt_injector entry has sign_off_status: signed_off + signed_off_at: 2026-06-17 + signed_off_by: phase-16
8. action_for_v21 field updated to FULFILLED-in-v3.0 completion record
9. ROADMAP §16 criterion #6 satisfied (README 21-expert inventory lists prompt_injector as NEW Phase 16 entry)
</verification>

<success_criteria>
- ROADMAP §16 success criterion #6: README inventory lists prompt_injector as NEW (Task 1)
- Phase 16 close-out documentation complete per Phase 13/14/15 close-out pattern (Tasks 1+2)
- skills-mapping.yaml prompt_injector entry signed off with full traceability fields (Task 2)
- Phase 18 VALIDATE-01 will find signed_off on prompt_injector entry when it audits all mappings
</success_criteria>

<output>
Create `.planning/phases/16-new-prompt-injector-expert/16-02-SUMMARY.md` when done
</output>

---
phase: 17-deprecate-3-experts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/movie-experts/performer/SKILL.md
  - skills/movie-experts/scene_builder/SKILL.md
  - skills/movie-experts/storyboard_designer/SKILL.md
  - skills/movie-experts/character_designer/SKILL.md
  - skills/movie-experts/screenplay/SKILL.md
  - skills/movie-experts/cinematographer/SKILL.md
  - skills/movie-experts/style_genome/SKILL.md
  - skills/movie-experts/visual_executor/SKILL.md
  - skills/movie-experts/audio_pipeline/SKILL.md
  - skills/movie-experts/editor/SKILL.md
  - skills/movie-experts/continuity_auditor/SKILL.md
  - skills/movie-experts/compliance_gate/SKILL.md
  - skills/movie-experts/animation_studio/SKILL.md
  - skills/movie-experts/hook_retention/SKILL.md
  - skills/movie-experts/creative_source/SKILL.md
  - skills/movie-experts/documentary_maker/SKILL.md
  - skills/movie-experts/theory_critic/SKILL.md
  - skills/movie-experts/prompt_injector/SKILL.md
  - skills/movie-experts/production/SKILL.md
autonomous: true
requirements: [DEPRECATE-01, DEPRECATE-02, DEPRECATE-03]
user_setup: []

must_haves:
  truths:
    - "A reader can open skills/movie-experts/performer/SKILL.md and immediately see a prominent DEPRECATED notice pointing to character_designer + screenplay"
    - "A reader can open skills/movie-experts/scene_builder/SKILL.md and immediately see a prominent DEPRECATED notice pointing to cinematographer + style_genome"
    - "A reader can open skills/movie-experts/storyboard_designer/SKILL.md and immediately see a prominent DEPRECATED notice pointing to cinematographer (composition_lock sub-task)"
    - "Each of the 3 deprecated SKILL.md files retains original expert_id + full body content (FOUND-08 backward compat preserved)"
    - "All consumers that previously listed performer / scene_builder / storyboard_designer in related_skills have been rewired to the appropriate inheritance target(s)"
  artifacts:
    - path: "skills/movie-experts/performer/SKILL.md"
      provides: "deprecated performer expert with status + deprecation markers + redirect to character_designer + screenplay"
      contains: "status: deprecated"
    - path: "skills/movie-experts/scene_builder/SKILL.md"
      provides: "deprecated scene_builder expert with status + deprecation markers + redirect to cinematographer + style_genome"
      contains: "status: deprecated"
    - path: "skills/movie-experts/storyboard_designer/SKILL.md"
      provides: "deprecated storyboard_designer expert with status + deprecation markers + redirect to cinematographer"
      contains: "status: deprecated"
  key_links:
    - from: "performer/SKILL.md frontmatter"
      to: "character_designer + screenplay"
      via: "metadata.hermes.inheritance_targets"
      pattern: "inheritance_targets: \\[character_designer, screenplay\\]"
    - from: "scene_builder/SKILL.md frontmatter"
      to: "cinematographer + style_genome"
      via: "metadata.hermes.inheritance_targets"
      pattern: "inheritance_targets: \\[cinematographer, style_genome\\]"
    - from: "storyboard_designer/SKILL.md frontmatter"
      to: "cinematographer"
      via: "metadata.hermes.inheritance_targets"
      pattern: "inheritance_targets: \\[cinematographer\\]"
---

<objective>
Mark 3 v1 movie-experts (performer, scene_builder, storyboard_designer) as `status: deprecated` per ROADMAP §17 success criteria + skills-mapping.yaml `not_in_new_dag:` rationale. Each deprecated SKILL.md receives: (a) frontmatter `status: deprecated` + `metadata.hermes.deprecated: true` + `deprecated_reason:` (CN prose) + `inheritance_targets:`; (b) prominent deprecation notice at top of body; (c) original expert_id + full content preserved (FOUND-08 backward compat). Then rewire all consumer `related_skills` edges away from the deprecated IDs toward the appropriate inheritance targets.

Purpose: Implement DEPRECATE-01/02/03 (3 deprecations) — bring the codebase to 15 active + 3 deprecated expert_ids, ready for Phase 18 reconciliation to the canonical 21-expert topology.

Output: 3 deprecated SKILL.md files (with new frontmatter fields + deprecation notice, body content intact) + 10-13 consumer SKILL.md files with rewired `related_skills` edges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-deprecate-3-experts/17-CONTEXT.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml

<interfaces>
<!-- Key frontmatter schema extension for deprecated experts -->
<!-- This is a new contract — executor implements directly, no codebase exploration needed -->

Frontmatter additions (apply to each of the 3 deprecated SKILL.md files):
```yaml
# Add at top level of frontmatter (alongside name/version/author):
status: deprecated

# Add inside metadata.hermes: block:
metadata:
  hermes:
    # ... existing fields (tags, related_skills, expert_id, metrics) PRESERVED ...
    deprecated: true
    deprecated_reason: "<CN prose — why deprecated, per skills-mapping.yaml rationale>"
    inheritance_targets: [<target1>, <target2>]  # per expert — see per-expert mapping below
```

Per-expert inheritance_targets (per ROADMAP §17 success criteria + skills-mapping.yaml):
- performer: [character_designer, screenplay]
- scene_builder: [cinematographer, style_genome]
- storyboard_designer: [cinematographer]

Body deprecation notice (insert immediately AFTER the `# <Name> Expert (<CN>)` H1 line, BEFORE existing body content):
```markdown
> ⚠️ **DEPRECATED (Phase 17 v3.0, 2026-06-17)**: 本专家已废弃。其职能已折叠至:
> - `<target1>` — <CN: what target1 absorbed from this expert>
> - `<target2>` — <CN: what target2 absorbed from this expert>  (omit line if only 1 target)
>
> **原 v1 内容保留于下方以维持向后兼容 (FOUND-08 frozen rule)。新项目请直接使用继承目标专家。**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark 3 experts deprecated (frontmatter + body notice + content preserved)</name>
  <files>
    skills/movie-experts/performer/SKILL.md
    skills/movie-experts/scene_builder/SKILL.md
    skills/movie-experts/storyboard_designer/SKILL.md
  </files>
  <read_first>
    - skills/movie-experts/performer/SKILL.md (full — preserve body content; only add frontmatter + body notice)
    - skills/movie-experts/scene_builder/SKILL.md (full)
    - skills/movie-experts/storyboard_designer/SKILL.md (full)
    - .planning/phases/17-deprecate-3-experts/17-CONTEXT.md (deprecation pattern + notice template)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml lines 108-126 (CN rationale per expert)
  </read_first>
  <behavior>
    - File performer/SKILL.md: contains `status: deprecated` at top level of frontmatter
    - File performer/SKILL.md: contains `deprecated: true` inside metadata.hermes
    - File performer/SKILL.md: contains `inheritance_targets: [character_designer, screenplay]`
    - File performer/SKILL.md: `deprecated_reason:` is non-empty CN prose citing the skills-mapping.yaml rationale (voice + behavioral tics → character_designer; dialogue subtext → screenplay)
    - File performer/SKILL.md: H1 line `# Performer Expert (表演/动作专家)` is followed immediately by a `> ⚠️ DEPRECATED` blockquote (per CONTEXT template)
    - File performer/SKILL.md: `expert_id: performer` field preserved (FOUND-08)
    - File performer/SKILL.md: body content beyond the notice unchanged (refs table, role, capabilities, output format, key parameters, style rules, workflow, quality thresholds, collaboration, what-NOT-to-do)
    - Same 7 invariants above hold for scene_builder (target: cinematographer + style_genome; rationale: mise-en-scène as composition_lock sub-task → cinematographer; aesthetic / spatial narrative → style_genome)
    - Same 7 invariants above hold for storyboard_designer (target: cinematographer only; rationale: Phase 7 §3.4 D3.4 — storyboard folded into cinematographer composition_lock sub-task)
    - All 3 files: no body content deleted (only frontmatter extended + notice inserted)
  </behavior>
  <action>
    For EACH of the 3 files (performer, scene_builder, storyboard_designer):
    1. **Frontmatter**: Add `status: deprecated` at top-level (sibling to `name`/`version`/`author`/`platforms`/`prerequisites`/`metadata`). Inside the existing `metadata.hermes:` block (which already contains `tags`, `related_skills`, `expert_id`, `metrics`), add three new keys: `deprecated: true`, `deprecated_reason:` with the CN rationale prose from skills-mapping.yaml lines 113-126, and `inheritance_targets:` with the per-expert target list. PRESERVE the existing `expert_id`, `tags`, `related_skills`, `metrics` fields unchanged — they remain for backward compatibility (FOUND-08). Do NOT remove or modify the existing `related_skills` edges inside the deprecated files themselves (those edges stay so legacy callers can still traverse the v1 graph); the rewiring happens on the CONSUMER side in Task 2.
    2. **Body notice**: Insert the deprecation blockquote immediately AFTER the `# <Name> Expert (<CN>)` H1 line and BEFORE the existing first body section. Use the exact template from CONTEXT.md §Body Deprecation Notice. Per-expert body notice text:
       - performer: targets character_designer (声音 + 行为细节 voice + behavioral tics) + screenplay (对白潜台词 dialogue subtext)
       - scene_builder: targets cinematographer (mise-en-scène 作为 composition_lock 子任务) + style_genome (空间美学 + 风格基因)
       - storyboard_designer: target cinematographer (composition_lock 子任务 — shot 分解规则被 cinematographer 的构图锁定子任务吸收, per Phase 7 §3.4 D3.4)
    3. **Content preservation**: Do NOT delete, summarize, or comment out any existing body section (References, Knowledge Retrieval, Role & Philosophy, Core Capabilities, Output Format, Key Parameters, Style Rules, Workflow, Quality Thresholds, Collaboration, What NOT to do, Validation protocol). The full v1 content must remain readable for FOUND-08 backward compatibility (legacy callers may still invoke performer / scene_builder / storyboard_designer expert_ids and need the body to render).
    4. Use the Edit tool for surgical frontmatter extension + body notice insertion; do NOT rewrite the files via Write (would risk dropping body content).

    Per CONTEXT §Claude's Discretion, optionally add a one-line CHANGELOG note at the bottom of each deprecated file (`## Deprecation Note (Phase 17) — <date>`). Recommended for traceability but not mandatory.
  </action>
  <verify>
    <automated>
    # Frontmatter markers present on all 3 files
    for f in performer scene_builder storyboard_designer; do
      grep -q "^status: deprecated$" "skills/movie-experts/$f/SKILL.md" || { echo "MISSING status:deprecated in $f"; exit 1; }
      grep -q "deprecated: true" "skills/movie-experts/$f/SKILL.md" || { echo "MISSING deprecated:true in $f"; exit 1; }
      grep -q "deprecated_reason:" "skills/movie-experts/$f/SKILL.md" || { echo "MISSING deprecated_reason in $f"; exit 1; }
      grep -q "inheritance_targets:" "skills/movie-experts/$f/SKILL.md" || { echo "MISSING inheritance_targets in $f"; exit 1; }
      grep -q "DEPRECATED (Phase 17" "skills/movie-experts/$f/SKILL.md" || { echo "MISSING body notice in $f"; exit 1; }
    done
    # FOUND-08: expert_id + content preserved
    for f in performer scene_builder storyboard_designer; do
      grep -q "^    expert_id: $f$" "skills/movie-experts/$f/SKILL.md" || { echo "MISSING expert_id in $f (FOUND-08 violation)"; exit 1; }
    done
    # Per-expert target wiring
    grep -q "inheritance_targets: \[character_designer, screenplay\]" skills/movie-experts/performer/SKILL.md || { echo "performer targets wrong"; exit 1; }
    grep -q "inheritance_targets: \[cinematographer, style_genome\]" skills/movie-experts/scene_builder/SKILL.md || { echo "scene_builder targets wrong"; exit 1; }
    grep -q "inheritance_targets: \[cinematographer\]" skills/movie-experts/storyboard_designer/SKILL.md || { echo "storyboard_designer targets wrong"; exit 1; }
    echo "All 3 experts deprecated correctly"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 3 SKILL.md files have `status: deprecated` at top-level frontmatter
    - All 3 have `metadata.hermes.deprecated: true`
    - All 3 have non-empty CN prose `deprecated_reason:` matching skills-mapping.yaml rationale
    - All 3 have correct `inheritance_targets:` per ROADMAP §17
    - All 3 have a body deprecation notice immediately after H1
    - All 3 preserve original `expert_id`, `tags`, `related_skills`, `metrics` (FOUND-08)
    - All 3 preserve full body content (no deletions)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire consumer related_skills edges (performer/scene_builder/storyboard_designer → inheritance targets)</name>
  <files>
    skills/movie-experts/character_designer/SKILL.md
    skills/movie-experts/screenplay/SKILL.md
    skills/movie-experts/cinematographer/SKILL.md
    skills/movie-experts/style_genome/SKILL.md
    skills/movie-experts/visual_executor/SKILL.md
    skills/movie-experts/audio_pipeline/SKILL.md
    skills/movie-experts/editor/SKILL.md
    skills/movie-experts/continuity_auditor/SKILL.md
    skills/movie-experts/compliance_gate/SKILL.md
    skills/movie-experts/animation_studio/SKILL.md
    skills/movie-experts/hook_retention/SKILL.md
    skills/movie-experts/creative_source/SKILL.md
    skills/movie-experts/documentary_maker/SKILL.md
    skills/movie-experts/theory_critic/SKILL.md
    skills/movie-experts/prompt_injector/SKILL.md
    skills/movie-experts/production/SKILL.md
  </files>
  <read_first>
    - skills/movie-experts/performer/SKILL.md (lines 13: related_skills list — identify consumers)
    - skills/movie-experts/scene_builder/SKILL.md (lines 13: related_skills)
    - skills/movie-experts/storyboard_designer/SKILL.md (lines 13: related_skills)
    - For each consumer file identified below: open the file, locate the `related_skills:` line, see current contents
    - .planning/phases/17-deprecate-3-experts/17-CONTEXT.md §Consumer Updates (Edge Rewiring) decision rules
  </read_first>
  <behavior>
    - Zero consumer SKILL.md files contain `performer` in their `related_skills:` YAML list
    - Zero consumer SKILL.md files contain `scene_builder` in their `related_skills:` YAML list
    - Zero consumer SKILL.md files contain `storyboard_designer` in their `related_skills:` YAML list
    - The 3 deprecated experts' OWN SKILL.md files (performer, scene_builder, storyboard_designer) MAY retain their existing related_skills entries (FOUND-08 — internal v1 graph preserved)
    - When rewiring, target IDs are appended (if not already present) and the deprecated ID removed; no duplicate IDs left in any related_skills list
    - Body prose mentions of the 3 deprecated experts inside consumer files are annotated with ` (deprecated)` OR replaced with the inheritance target — at minimum, no consumer body prose claims the deprecated expert is an active collaborator without qualification
  </behavior>
  <action>
    Per the consumer map established by `grep -l` against the codebase:

    **performer consumers** (7 files): `character_designer`, `production`, `scene_builder`, `screenplay`, `audio_pipeline`, `visual_executor`, `style_genome`.
    - For each consumer's `related_skills:` list: REMOVE `performer`, ADD `character_designer` (preferred default — voice + behavioral tics are the more-rewired dimension per skills-mapping.yaml). If the consumer's body prose specifically references performer for dialogue / acting rhythm, add `screenplay` instead (or in addition). If `character_designer` is ALREADY in the list, just remove `performer` (no duplicate).
    - Example: `character_designer/SKILL.md` already has `performer` in related_skills AND is itself the target — just REMOVE `performer` from its related_skills (a self-reference is nonsensical post-deprecation). Verify character_designer doesn't accidentally list itself.
    - `scene_builder/SKILL.md` is also being deprecated in this same phase (Task 1) — leave its related_skills unchanged (Task 1 preserves FOUND-08). The consumer-rewire grep was run pre-Task-1, so scene_builder/performer appearance in each other's related_skills is fine to leave since both files are deprecated.

    **scene_builder consumers** (10 files): `audio_pipeline`, `editor`, `storyboard_designer`, `animation_studio`, `cinematographer`, `performer`, `production`, `style_genome`, `screenplay`, `visual_executor`.
    - For each consumer's `related_skills:` list: REMOVE `scene_builder`, ADD `cinematographer` (preferred default — mise-en-scène / composition_lock is the more-rewired dimension). If the consumer's body prose specifically references scene_builder for spatial / aesthetic / architectural work, add `style_genome` instead (or in addition). If `cinematographer` is ALREADY in the list, just remove `scene_builder`.
    - `performer/SKILL.md` is also deprecated in this phase — leave its related_skills unchanged (Task 1).
    - `storyboard_designer/SKILL.md` is also deprecated — leave unchanged.

    **storyboard_designer consumers**: zero external consumers (grep returned empty). SKIP — no rewiring needed. (Confirm with one final grep after Task 1 to verify no consumer file lists `storyboard_designer`.)

    **Body prose annotations**: In each consumer file, do a final scan of body prose (the Collaboration section, any `-> expert` bullets) for `performer` / `scene_builder` / `storyboard_designer` mentions. For each mention: either (a) replace with the inheritance target, or (b) suffix with ` (deprecated Phase 17 → <target>)`. Choice (a) is preferred for forward-looking prose; choice (b) is acceptable for historical / v1-trace prose.

    Use Edit tool for surgical edits to related_skills lines + targeted body-prose annotations. Do NOT rewrite entire consumer files. After all edits, run the verify command to confirm zero stale references.

    IMPORTANT: Some consumer files may not actually list any of the 3 deprecated IDs in related_skills (the grep list is `grep -l` which also matches body prose). For such files, only the body-prose annotation applies; the related_skills line is left unchanged.
  </action>
  <verify>
    <automated>
    # After all consumer edits, no consumer file should list a deprecated ID in related_skills
    # (excluding the 3 deprecated files themselves)
    CONSUMERS=$(ls -d skills/movie-experts/*/ | grep -vE "/(performer|scene_builder|storyboard_designer)/$" | sed 's|skills/movie-experts/||;s|/||')
    FAIL=0
    for c in $CONSUMERS; do
      f="skills/movie-experts/$c/SKILL.md"
      [ -f "$f" ] || continue
      # Extract the related_skills YAML list line and check for deprecated IDs
      if grep -E "related_skills:.*\b(performer|scene_builder|storyboard_designer)\b" "$f" >/dev/null; then
        echo "STALE related_skills in $f still lists a deprecated ID"
        FAIL=1
      fi
    done
    [ "$FAIL" = "0" ] || exit 1
    # Re-confirm zero storyboard_designer consumers (sanity)
    grep -lE "related_skills:.*storyboard_designer" skills/movie-experts/*/SKILL.md 2>/dev/null | grep -vE "/(storyboard_designer)/" | wc -l | grep -q "^0$" || { echo "storyboard_designer consumers found unexpectedly"; exit 1; }
    # Re-confirm FOUND-08 in the 3 deprecated files (Task 1 didn't break them)
    for f in performer scene_builder storyboard_designer; do
      grep -q "expert_id: $f" "skills/movie-experts/$f/SKILL.md" || { echo "FOUND-08 broken in $f"; exit 1; }
    done
    echo "All consumer edges rewired; FOUND-08 preserved in deprecated files"
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero non-deprecated consumer SKILL.md files contain `performer`, `scene_builder`, or `storyboard_designer` in their `related_skills:` YAML list
    - Body prose mentions of the 3 deprecated experts in consumer files are either replaced with inheritance targets OR annotated `(deprecated Phase 17 → <target>)`
    - The 3 deprecated experts' OWN SKILL.md files retain their original related_skills entries (FOUND-08)
    - No duplicate IDs in any related_skills list
    - No FOUND-08 regression in the 3 deprecated files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skills content → Hermes skill loader | YAML frontmatter parsed by `agent/skill_utils.parse_frontmatter`; malformed YAML would break skill discovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | YAML frontmatter extensions (status / deprecated / deprecated_reason / inheritance_targets) | mitigate | Verify block uses strict `grep -q` patterns on exact YAML keys; YAML schema additions are additive (no removal of existing keys), so skill loader's parse_frontmatter stays compatible. No package install in this plan — no supply-chain risk. |
| T-17-02 | Information Disclosure | Body content of deprecated experts remains readable | accept | Intentional per FOUND-08 — backward compatibility requires the content to remain. No secrets or PII in skill content; the corpus is fair-use cited academic / industry knowledge. |
| T-17-SC | Tampering | (none — no npm/pip/cargo installs) | accept | Pure markdown edits; no package manager invocations. Supply-chain gate N/A. |
</threat_model>

<verification>
Phase 17 Plan 01 verification (run after both tasks):
1. `grep -l "^status: deprecated$" skills/movie-experts/{performer,scene_builder,storyboard_designer}/SKILL.md | wc -l` returns 3
2. `grep -c "expert_id: performer" skills/movie-experts/performer/SKILL.md` returns 1 (FOUND-08 preserved)
3. `grep -c "expert_id: scene_builder" skills/movie-experts/scene_builder/SKILL.md` returns 1
4. `grep -c "expert_id: storyboard_designer" skills/movie-experts/storyboard_designer/SKILL.md` returns 1
5. No consumer file (excluding the 3 deprecated) lists any of the 3 deprecated IDs in `related_skills:`
6. All 3 deprecated files have a `> ⚠️ DEPRECATED` blockquote immediately after the H1
</verification>

<success_criteria>
- 3 SKILL.md files carry full deprecation markers (status + deprecated + deprecated_reason + inheritance_targets + body notice) while preserving FOUND-08 (expert_id + body content)
- All consumer `related_skills` edges rewired to inheritance targets
- Body prose in consumers either rewritten or annotated with `(deprecated Phase 17 → <target>)`
- DEPRECATE-01, DEPRECATE-02, DEPRECATE-03 fully implemented at the file level (final sign-off in skills-mapping.yaml + README happens in Plan 17-02)
</success_criteria>

<output>
Create `.planning/phases/17-deprecate-3-experts/17-01-SUMMARY.md` when done
</output>

---
phase: 17-deprecate-3-experts
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
  - .planning/research/v2-pipeline-design/skills-mapping.yaml
autonomous: true
requirements: [DEPRECATE-01, DEPRECATE-02, DEPRECATE-03]
user_setup: []

must_haves:
  truths:
    - "A reader opening skills/movie-experts/README.md sees the 3 deprecated experts (performer, scene_builder, storyboard_designer) explicitly marked DEPRECATED with inheritance targets"
    - "The README footer count reflects 18 expert_ids in the codebase (15 active + 3 deprecated), anticipating the Phase 18 reconciliation to 21-expert topology"
    - "The corpus tree / DAG diagram in README no longer shows the 3 deprecated experts as active collaborators (they appear as deprecated nodes OR are removed from the active DAG)"
    - "skills-mapping.yaml not_in_new_dag entries for performer / scene_builder / storyboard_designer have sign_off_status: signed_off with signed_off_at: 2026-06-17 + signed_off_by: phase-17"
    - "Glossary entries for performer / scene_builder / storyboard_designer (where present) carry a deprecation notice"
  artifacts:
    - path: "skills/movie-experts/README.md"
      provides: "README with 3 deprecated experts marked, corpus tree updated, footer count updated"
      contains: "DEPRECATED"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "glossary with deprecation notices on deprecated-expert-referencing entries"
      contains: "deprecated"
    - path: ".planning/research/v2-pipeline-design/skills-mapping.yaml"
      provides: "skills-mapping.yaml with signed_off deprecate entries"
      contains: "sign_off_status: signed_off"
  key_links:
    - from: "skills-mapping.yaml not_in_new_dag:performer"
      to: "Phase 17 sign-off"
      via: "sign_off_status field"
      pattern: "sign_off_status: signed_off"
    - from: "README inventory row for performer / scene_builder / storyboard_designer"
      to: "Inheritance target(s)"
      via: "inline DEPRECATED marker"
      pattern: "DEPRECATED.*→.*character_designer"
---

<objective>
Phase 17 close-out documentation pass: mark the 3 deprecated experts in the README inventory + corpus tree + DAG diagram + footer count; update glossary entries with deprecation notices; sign off the 3 `not_in_new_dag:` entries in skills-mapping.yaml. This completes the traceability chain from ROADMAP §17 → skills-mapping.yaml → README → glossary.

Purpose: Lock the deprecation decision into all inventory / navigation / mapping artifacts so Phase 18 validation can confirm a clean 21-expert topology. Mirrors the close-out pattern from Phases 13-03 / 14-03 / 15-03 / 16-02.

Output: 3 docs updated (README.md + _shared/glossary.md + skills-mapping.yaml) reflecting the 3 deprecations as signed-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-deprecate-3-experts/17-CONTEXT.md
@.planning/phases/17-deprecate-3-experts/17-01-PLAN.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml

<interfaces>
<!-- Existing README inventory row schema (mirror this format for deprecated annotations) -->
<!-- From skills/movie-experts/README.md lines 60-80 — the inventory table -->

```markdown
| [`<expert_id>`](./<expert_id>/SKILL.md) | <CN name> | <role description> | <refs count> |
```

Existing Phase-16-2 close-out added a "1 New Expert (Phase 16 ...)" sub-section; this plan adds a "3 Deprecated Experts (Phase 17 ...)" sub-section following the same pattern. Deprecation row format (extend the table schema):
```markdown
| [`<expert_id>`](./<expert_id>/SKILL.md) ⚠️ DEPRECATED | <CN name> | ~~<original role>~~ → folded into `<target1>` (+ `<target2>` if applicable) | <refs count> |
```

Footer count line at README bottom (line ~459) currently reads:
`v3.0 = 18 active expert_ids (17 post-Phase-15 + 1 Phase 16 prompt_injector NEW ...) — Phase 17 will deprecate 3 candidates ...`

After this plan, footer should read:
`v3.0 = 18 expert_ids in codebase (15 active + 3 deprecated: performer, scene_builder, storyboard_designer). Phase 17 deprecation complete. Phase 18 will reconcile to canonical 21-expert topology (16 DAG pipeline-roles + 5 aliases). All RAG-aware.`

skills-mapping.yaml sign-off pattern (mirror Phase 16 prompt_injector entry at lines 99-106):
```yaml
  - v1_expert_id: <id>
    disposition: deprecate_candidate
    rationale: "<unchanged>"
    final_decision_milestone: v2.1+
    action_for_v21: "FULFILLED in v3.0 milestone Phase 17 (2026-06-17): <expert_id> marked status: deprecated in skills/movie-experts/<expert_id>/SKILL.md with inheritance_targets: [<target(s)>] + deprecated_reason (CN prose) + body deprecation notice; consumer related_skills rewired (Plan 17-01 Task 2)"
    sign_off_status: signed_off
    signed_off_at: 2026-06-17
    signed_off_by: phase-17
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README inventory + corpus tree + DAG diagram + footer count for 3 deprecations</name>
  <files>
    skills/movie-experts/README.md
  </files>
  <read_first>
    - skills/movie-experts/README.md (full — locate inventory tables, corpus tree, DAG diagram, footer)
    - skills/movie-experts/performer/SKILL.md frontmatter (confirm deprecation markers from Plan 17-01)
    - skills/movie-experts/scene_builder/SKILL.md frontmatter
    - skills/movie-experts/storyboard_designer/SKILL.md frontmatter
  </read_first>
  <behavior>
    - README contains a new sub-section "### 3 Deprecated Experts (Phase 17 — 2026-06-17)" listing performer / scene_builder / storyboard_designer with their inheritance targets
    - The existing "14 Original Experts" table row for `scene_builder` and `performer` carries a `⚠️ DEPRECATED Phase 17 → <target(s)>` inline marker
    - The "5 New Experts (Phase 7 ...)" table row for `storyboard_designer` (if present) carries the same DEPRECATED marker
    - The corpus tree / "Production DAG (Collaboration Graph)" diagram either (a) shows the 3 deprecated experts as grayed/strikethrough nodes with redirect arrows, or (b) removes them from the active DAG and adds a legend note "performer / scene_builder / storyboard_designer deprecated Phase 17 — see deprecation section"
    - Footer count line updated to reflect "18 expert_ids in codebase (15 active + 3 deprecated)"
    - No active-DAG edge in the Mermaid diagram references performer / scene_builder / storyboard_designer as a current collaborator (redirect / annotation only)
  </behavior>
  <action>
    1. **Inventory tables**: In the "14 Original Experts (Phase 0 baselined ...)" table, mark the `performer` and `scene_builder` rows with `⚠️ DEPRECATED Phase 17 → <target(s)>` inline at the start of the role cell. Use the per-expert mapping: performer → character_designer + screenplay; scene_builder → cinematographer + style_genome. In the "5 New Experts (Phase 7 ...)" table, mark the `storyboard_designer` row with `⚠️ DEPRECATED Phase 17 → cinematographer (composition_lock)`.
    2. **New sub-section**: Insert a new "### 3 Deprecated Experts (Phase 17 — 2026-06-17)" sub-section immediately after the existing "### 1 New Expert (Phase 16 — AI-Native prompt_injector, 2026-06-17)" section (around line 110). The section should contain a 3-row table with columns: Expert / Chinese Name / Original Role / Inheritance Target(s) / Rationale. Rationale prose in CN, matching skills-mapping.yaml.
    3. **Corpus tree / DAG diagram**: Locate the Mermaid "Production DAG (Collaboration Graph)" section (around line 114). For the 3 deprecated experts: either (a) keep the node but add a CSS class or strikethrough annotation indicating deprecated, OR (b) remove the node from the active DAG and add a brief legend note below the diagram: `**Deprecated (Phase 17):** performer → character_designer+screenplay; scene_builder → cinematographer+style_genome; storyboard_designer → cinematographer`. Option (b) is preferred (cleaner active DAG; deprecation detail lives in the inventory sub-section). If removing, also remove any inbound/outbound edges to the deprecated nodes from the active DAG.
    4. **Status line (line 6)**: Update the top "Status:" line to reflect Phase 17 completion — "v3.0 in progress — 18 expert_ids in codebase (15 active + 3 deprecated: performer / scene_builder / storyboard_designer). Phases 13-16 merged 4+6+1 predecessors and added prompt_injector; Phase 17 deprecated 3 candidates. Phase 18 will reconcile to canonical 21-expert topology."
    5. **Footer (line ~459)**: Update to: `v3.0 = 18 expert_ids in codebase (15 active + 3 deprecated: performer, scene_builder, storyboard_designer). Phase 17 deprecation complete. Phase 18 will reconcile to canonical 21-expert topology (16 DAG pipeline-roles + 5 aliases). All RAG-aware, all phantom refs stripped.`

    Use the Edit tool for surgical edits — do NOT rewrite the README via Write.
  </action>
  <verify>
    <automated>
    # README has the new Phase 17 deprecation sub-section
    grep -q "3 Deprecated Experts (Phase 17" skills/movie-experts/README.md || { echo "Missing Phase 17 sub-section"; exit 1; }
    # README footer reflects deprecation
    grep -q "18 expert_ids in codebase" skills/movie-experts/README.md || { echo "Footer not updated"; exit 1; }
    grep -q "15 active + 3 deprecated" skills/movie-experts/README.md || { echo "Footer count wrong"; exit 1; }
    # Each of the 3 deprecated experts is mentioned as DEPRECATED
    for e in performer scene_builder storyboard_designer; do
      grep -q "DEPRECATED.*$e\|$e.*DEPRECATED" skills/movie-experts/README.md || { echo "$e not marked DEPRECATED in README"; exit 1; }
    done
    echo "README updated correctly"
    </automated>
  </verify>
  <acceptance_criteria>
    - New "3 Deprecated Experts (Phase 17 ...)" sub-section exists with all 3 experts + targets + rationale
    - Existing inventory rows for performer / scene_builder / storyboard_designer carry inline DEPRECATED markers
    - DAG diagram no longer shows the 3 deprecated experts as active collaborators (with legend note)
    - Footer count updated to "18 expert_ids in codebase (15 active + 3 deprecated)"
    - Status line updated to reflect Phase 17 complete
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Glossary deprecation notices + skills-mapping.yaml sign-off</name>
  <files>
    skills/movie-experts/_shared/glossary.md
    .planning/research/v2-pipeline-design/skills-mapping.yaml
  </files>
  <read_first>
    - skills/movie-experts/_shared/glossary.md (full — locate entries that reference performer / scene_builder / storyboard_designer)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml lines 108-126 (current not_in_new_dag: entries — they have NO sign_off_status field yet)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml lines 99-106 (Phase 16 prompt_injector entry — sign-off pattern to mirror)
  </read_first>
  <behavior>
    - Glossary entries that currently reference performer / scene_builder / storyboard_designer as an active context-owner are annotated with `(deprecated Phase 17 → <target>)` inline
    - Glossary does NOT introduce new top-level entries for the deprecated experts themselves (the deprecation is recorded in README + skills-mapping.yaml, not in the glossary)
    - skills-mapping.yaml `not_in_new_dag:` entries for performer / scene_builder / storyboard_designer each gain: `action_for_v21:` (CN/EN prose noting Phase 17 fulfillment), `sign_off_status: signed_off`, `signed_off_at: 2026-06-17`, `signed_off_by: phase-17`
    - The `production` entry (lines 128-131) is left UNCHANGED (production is `deferred`, NOT deprecated — different disposition per REQUIREMENTS.md out-of-scope)
  </behavior>
  <action>
    **Glossary edits** (`skills/movie-experts/_shared/glossary.md`):
    Open the file and search for body prose mentions of `performer`, `scene_builder`, `storyboard_designer`. Based on the earlier grep output, at least the following lines reference them:
    - Line 78 (镜头语言 context): "scene_builder owns spatial geometry" → annotate as "scene_builder (deprecated Phase 17 → cinematographer+style_genome) owns spatial geometry"
    - Line 93 (轴线 context): "scene_builder pre-computes axis data" → annotate
    - Line 98 (调度 context): "performer outputs stage positions ... scene_builder validates spatial feasibility" → annotate both
    - Line 127 (男主/女主 context): "performer adjusts body-language intensity accordingly" → annotate
    - Line 183+ (Phase 7 additions storyboard section, if it mentions storyboard_designer): annotate
    
    For each annotation, use the format ` (deprecated Phase 17 → <target>)` immediately after the expert_id mention. Do NOT delete the original sentence — just qualify it.

    **skills-mapping.yaml sign-off** (`.planning/research/v2-pipeline-design/skills-mapping.yaml`):
    For each of the 3 `not_in_new_dag:` entries (performer lines 113-116, scene_builder lines 118-121, storyboard_designer lines 123-126), append 4 new keys after the existing `final_decision_milestone: v2.1+` line:
    ```yaml
      - v1_expert_id: <id>
        disposition: deprecate_candidate
        rationale: "<unchanged — preserve the existing CN/EN prose>"
        final_decision_milestone: v2.1+
        action_for_v21: "FULFILLED in v3.0 milestone Phase 17 (2026-06-17): <id> marked status: deprecated in skills/movie-experts/<id>/SKILL.md with inheritance_targets: [<target(s)>] + deprecated_reason (CN prose) + body deprecation notice; consumer related_skills rewired (Plan 17-01 Task 2)"
        sign_off_status: signed_off
        signed_off_at: 2026-06-17
        signed_off_by: phase-17
    ```
    Per-expert inheritance_targets in the action_for_v21 prose: performer=[character_designer, screenplay]; scene_builder=[cinematographer, style_genome]; storyboard_designer=[cinematographer].

    Leave the `production` entry (lines 128-131) unchanged — it is `disposition: deferred`, NOT deprecate_candidate.

    Use Edit tool for both files.
  </action>
  <verify>
    <automated>
    # Glossary has deprecation annotations on performer / scene_builder mentions
    grep -q "deprecated Phase 17" skills/movie-experts/_shared/glossary.md || { echo "Glossary missing deprecation annotation"; exit 1; }
    # skills-mapping.yaml has 3 new signed_off entries for deprecate candidates
    SIGNED=$(grep -c "sign_off_status: signed_off" .planning/research/v2-pipeline-design/skills-mapping.yaml)
    # Pre-Phase-17 had 4 signed_off (2 renames + 1 merge visual + 1 merge audio + 1 new prompt_injector = 5)
    # After Phase 17, should be 5 + 3 = 8
    [ "$SIGNED" -ge 8 ] || { echo "Expected >=8 signed_off entries, got $SIGNED"; exit 1; }
    # All 3 deprecate candidates have signed_off_at: 2026-06-17 + signed_off_by: phase-17
    for id in performer scene_builder storyboard_designer; do
      # Find the deprecate entry block and check fields (look within 8 lines after the v1_expert_id line)
      LINE=$(grep -n "v1_expert_id: $id$" .planning/research/v2-pipeline-design/skills-mapping.yaml | head -1 | cut -d: -f1)
      END=$((LINE + 10))
      sed -n "${LINE},${END}p" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: signed_off" || { echo "$id missing sign_off_status"; exit 1; }
      sed -n "${LINE},${END}p" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "signed_off_at: 2026-06-17" || { echo "$id missing signed_off_at"; exit 1; }
      sed -n "${LINE},${END}p" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "signed_off_by: phase-17" || { echo "$id missing signed_off_by"; exit 1; }
    done
    # production entry unchanged (deferred, not signed_off)
    LINE=$(grep -n "v1_expert_id: production$" .planning/research/v2-pipeline-design/skills-mapping.yaml | head -1 | cut -d: -f1)
    END=$((LINE + 6))
    if sed -n "${LINE},${END}p" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: signed_off"; then
      echo "ERROR: production incorrectly signed off (should remain deferred)"; exit 1
    fi
    echo "Glossary + skills-mapping.yaml signed off correctly"
    </automated>
  </verify>
  <acceptance_criteria>
    - Glossary body prose mentions of performer / scene_builder / storyboard_designer are annotated `(deprecated Phase 17 → <target>)`
    - skills-mapping.yaml has 3 new `sign_off_status: signed_off` entries (performer, scene_builder, storyboard_designer) with `signed_off_at: 2026-06-17` + `signed_off_by: phase-17` + `action_for_v21:` prose
    - production entry remains `disposition: deferred` (NOT signed off)
    - No rationale prose modified in skills-mapping.yaml (only added fields)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs / inventory → consumer (operator reading README) | Inventory claims must match filesystem reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Repudiation | skills-mapping.yaml sign_off timestamp | mitigate | sign_off_at: 2026-06-17 matches STATE.md last_activity; signed_off_by: phase-17 matches the executing phase. No package installs — supply-chain gate N/A. |
| T-17-04 | Information Disclosure | README deprecation sub-section reveals v1 expert content existed | accept | Intentional per FOUND-08 transparency. No secrets. |
| T-17-SC | Tampering | (none — no npm/pip/cargo installs) | accept | Pure markdown + YAML edits. |
</threat_model>

<verification>
Phase 17 Plan 02 verification (run after both tasks):
1. `grep -c "3 Deprecated Experts (Phase 17" skills/movie-experts/README.md` returns 1
2. `grep -c "deprecated Phase 17" skills/movie-experts/_shared/glossary.md` returns >= 1
3. `grep -c "sign_off_status: signed_off" .planning/research/v2-pipeline-design/skills-mapping.yaml` returns >= 8 (5 prior + 3 new)
4. Footer count line in README reflects "18 expert_ids in codebase (15 active + 3 deprecated)"
5. production entry in skills-mapping.yaml remains `disposition: deferred` (NOT signed off)
</verification>

<success_criteria>
- README inventory + corpus tree + DAG diagram + footer all reflect the 3 deprecations
- Glossary entries annotated with `(deprecated Phase 17 → <target>)`
- skills-mapping.yaml has 3 new signed_off entries (performer, scene_builder, storyboard_designer)
- Traceability chain complete: ROADMAP §17 → REQUIREMENTS DEPRECATE-01/02/03 → skills-mapping.yaml signed_off → README inventory → glossary
- Phase 17 ready for STATE.md close-out + Phase 18 to begin
</success_criteria>

<output>
Create `.planning/phases/17-deprecate-3-experts/17-02-SUMMARY.md` when done
</output>

---
phase: 13-expert-rename-alias-scaffolding
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - .planning/research/v2-pipeline-design/skills-mapping.yaml
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
autonomous: true
requirements: [RENAME-01, RENAME-02]
user_setup: []

must_haves:
  truths:
    - "skills-mapping.yaml shows sign_off_status: signed_off for both continuity_auditor and compliance_gate entries (was: pending)"
    - "README.md inventory rows point to the new directory paths (continuity_auditor/, compliance_gate/) and use the new IDs"
    - "README.md corpus tree diagram references new directory names"
    - "_shared/glossary.md prose references use new expert IDs where they refer to the renamed experts"
    - "No stranded old-ID references remain in README.md or _shared/glossary.md (outside intentional alias listings)"
  artifacts:
    - path: ".planning/research/v2-pipeline-design/skills-mapping.yaml"
      provides: "Canonical mapping with signed-off rename entries"
      contains: "sign_off_status: signed_off"
    - path: "skills/movie-experts/README.md"
      provides: "Inventory + corpus tree with new IDs"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "Glossary with updated expert ID references"
  key_links:
    - from: ".planning/research/v2-pipeline-design/skills-mapping.yaml"
      to: "skills/movie-experts/continuity_auditor/SKILL.md"
      via: "mapping entry with sign_off_status: signed_off"
      pattern: "sign_off_status: signed_off"
    - from: "skills/movie-experts/README.md"
      to: "skills/movie-experts/compliance_gate/SKILL.md"
      via: "inventory markdown link"
      pattern: "\\(\\./compliance_gate/SKILL\\.md\\)"
---

<objective>
Close out Phase 13 by (1) updating the canonical skills-mapping.yaml sign_off_status from `pending` to `signed_off` for both renamed entries, (2) updating the README.md inventory + corpus tree to reflect new directory paths and IDs, and (3) updating _shared/glossary.md prose references. These are the cross-cutting documentation artifacts that depend on both renames having completed (13-01 + 13-02).

Purpose: Fulfill ROADMAP §13 success criteria #5 (skills-mapping.yaml sign_off) + complete consumer updates for shared documentation. Without this, downstream phases (14, 15) reading skills-mapping.yaml would see stale `pending` status, and README consumers would see stale directory paths.

Output:
- `skills-mapping.yaml` lines 71 + 78: `sign_off_status: pending` → `sign_off_status: signed_off`
- `README.md` inventory rows + corpus tree + ASCII pipeline diagram updated to new IDs/dirs
- `_shared/glossary.md` prose references updated (3 occurrences)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-expert-rename-alias-scaffolding/13-CONTEXT.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml
@skills/movie-experts/README.md
@skills/movie-experts/_shared/glossary.md

# This plan depends on BOTH 13-01 and 13-02 because:
# - README.md inventory + corpus tree references both old IDs (continuity, compliance_marketing)
# - Both renames must be complete before README is updated, otherwise inventory
#   would point to half-renamed state.
# Sequential Wave 3 is mandatory.

<interfaces>
<!-- skills-mapping.yaml lines 65-78 BEFORE this plan: -->
```yaml
  # === 1:1 renamed (2) — requires skills team sign-off ===
  - new_node: continuity_auditor
    v1_expert_id: continuity
    mapping_type: one_to_one_renamed
    rename_rationale: "..."
    sign_off_required: true
    sign_off_status: pending         # <-- CHANGE TO: signed_off

  - new_node: compliance_gate
    v1_expert_id: compliance_marketing
    mapping_type: one_to_one_renamed
    rename_rationale: "..."
    sign_off_required: true
    sign_off_status: pending         # <-- CHANGE TO: signed_off
```

<!-- After this plan, both must read: `sign_off_status: signed_off` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skills-mapping.yaml sign_off_status for both renamed entries (pending → signed_off)</name>
  <files>.planning/research/v2-pipeline-design/skills-mapping.yaml</files>
  <read_first>
    - .planning/research/v2-pipeline-design/skills-mapping.yaml lines 1-100 (especially lines 65-78 — the two renamed entries)
    - .planning/phases/13-expert-rename-alias-scaffolding/13-CONTEXT.md (success criterion #5)
    - .planning/ROADMAP.md lines 36-52 (Phase 13 success criterion #5)
  </read_first>
  <behavior>
    - Line 71 `sign_off_status: pending` (under `new_node: continuity_auditor` block) is changed to `sign_off_status: signed_off`
    - Line 78 `sign_off_status: pending` (under `new_node: compliance_gate` block) is changed to `sign_off_status: signed_off`
    - All other YAML content preserved verbatim (mapping_type, rename_rationale, sign_off_required, v1_expert_id, new_node, notes)
    - No new entries added; no entries removed
    - Optionally: a `signed_off_at: 2026-06-17` or `signed_off_by: phase-13` field MAY be added under each entry for traceability — Claude's discretion per CONTEXT.md
  </behavior>
  <action>
    1. Edit line 71 (`sign_off_status: pending` under continuity_auditor block): change `pending` to `signed_off`.
    2. Edit line 78 (`sign_off_status: pending` under compliance_gate block): change `pending` to `signed_off`.
    3. Optionally append `signed_off_at: 2026-06-17` on the line after each `sign_off_status: signed_off` for traceability (Claude's discretion — not required).
    4. Do NOT modify any other line in skills-mapping.yaml.
  </action>
  <verify>
    <automated>
    echo "--- Both renamed entries signed off ---" && \
    awk '/new_node: continuity_auditor/,/new_node: compliance_gate/' .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: signed_off" && \
    awk '/new_node: compliance_gate/,/new_node: visual_executor/' .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: signed_off" && \
    echo "--- No remaining pending sign_off_status for the two renamed entries ---" && \
    ! grep -A5 "new_node: continuity_auditor" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: pending" && \
    ! grep -A5 "new_node: compliance_gate" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: pending" && \
    echo "--- Exactly 2 signed_off occurrences (one per renamed entry) ---" && \
    grep -c "sign_off_status: signed_off" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "^2$" && \
    echo "TASK-1 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - skills-mapping.yaml continuity_auditor entry: sign_off_status: signed_off
    - skills-mapping.yaml compliance_gate entry: sign_off_status: signed_off
    - Exactly 2 `sign_off_status: signed_off` occurrences in the file
    - No `sign_off_status: pending` remains for either renamed entry
    - All other YAML content preserved verbatim
  </acceptance_criteria>
  <done>
    skills-mapping.yaml reflects Phase 13 sign-off for both renames. Downstream phases (14, 15) can read this file and treat both renames as authoritative.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md inventory + corpus tree + ASCII pipeline diagram + _shared/glossary.md to use new IDs and directory paths</name>
  <files>
    skills/movie-experts/README.md,
    skills/movie-experts/_shared/glossary.md
  </files>
  <read_first>
    - skills/movie-experts/README.md (full file — identify every line mentioning 'continuity' or 'compliance_marketing')
    - skills/movie-experts/_shared/glossary.md (full file — identify expert_id references to renamed experts)
    - .planning/phases/13-expert-rename-alias-scaffolding/13-CONTEXT.md (decision: README.md inventory + glossary must update)
    - Output of `grep -n "continuity\|compliance_marketing" skills/movie-experts/README.md` and `grep -n "continuity\|compliance_marketing" skills/movie-experts/_shared/glossary.md` to enumerate exact lines
  </read_first>
  <behavior>
    - README.md line 31 (corpus traceability table): `theory_critic, compliance_marketing` → `theory_critic, compliance_gate`
    - README.md line 77 (inventory row for continuity): `[`continuity`](./continuity/SKILL.md)` → `[`continuity_auditor`](./continuity_auditor/SKILL.md)` (and the Chinese name 连续性专家 is preserved)
    - README.md line 83 (inventory row for compliance_marketing): `[`compliance_marketing`](./compliance_marketing/SKILL.md)` → `[`compliance_gate`](./compliance_gate/SKILL.md)`
    - README.md line 201 (ASCII pipeline diagram): `continuity (parallel audit)` → `continuity_auditor (parallel audit)`
    - README.md line 215 (Identity contract bullet): `consumed by drawer / animator / lip_sync / continuity` → `... / continuity_auditor`
    - README.md line 219 (Audit nodes bullet): `continuity` (parallel to mixer) → `continuity_auditor (parallel to mixer)`
    - README.md line 291 (compliance_marketing size table row): `compliance_marketing | 5 | ~80 KB | 2026-06-15` → `compliance_gate | 5 | ~80 KB | 2026-06-15`
    - README.md line 335 (shell loop): `compliance_marketing hook_retention production` → `compliance_gate hook_retention production`
    - README.md line 377 (corpus tree dir): `├── compliance_marketing/` → `├── compliance_gate/`
    - README.md line 379 (corpus tree dir): `├── continuity/` → `├── continuity_auditor/`
    - README.md line 405 (eval demo yaml): `compliance_marketing_demo.yaml` → `compliance_gate_demo.yaml` (rename implied)
    - _shared/glossary.md line 164: `drawer / animator / lip_sync / continuity)` → `drawer / animator / lip_sync / continuity_auditor)`
    - _shared/glossary.md line 186: `downstream consumers are drawer / animator / editor / continuity.` → `... / continuity_auditor.`
    - _shared/glossary.md line 195: contains "visual continuity reference" — this is an ENGLISH NOUN phrase, NOT an expert_id reference. DO NOT change.
    - Any other lines in README.md or glossary.md that reference the renamed expert as an ID (not as an English noun) are updated accordingly
  </behavior>
  <action>
    1. For README.md, edit each enumerated line above to replace the expert_id reference (continuity → continuity_auditor, compliance_marketing → compliance_gate) and update the markdown link path (`./continuity/SKILL.md` → `./continuity_auditor/SKILL.md`, etc.).
    2. For the corpus tree ASCII diagram (lines 377-379), update the directory names AND the file listings inside each tree node line.
    3. For _shared/glossary.md, edit line 164 and line 186 to update expert_id references.
    4. **Do NOT touch** English-noun uses of "continuity" (e.g., line 195 "visual continuity reference", README.md "axis continuity" if any).
    5. Re-run grep to confirm zero stranded references.

    Use grep first to enumerate the exact lines (some line numbers may have shifted if 13-01 or 13-02 inadvertently touched README — they should NOT have, since both plans explicitly excluded README from their files_modified lists).
  </action>
  <verify>
    <automated>
    echo "--- README.md no stranded compliance_marketing as expert_id ---" && \
    ! grep -n "compliance_marketing" skills/movie-experts/README.md && \
    echo "--- README.md no stranded continuity as expert_id (excluding English-noun uses like 'axis continuity', 'visual continuity') ---" && \
    grep -n "continuity[^_]" skills/movie-experts/README.md | grep -v "continuity_auditor" | wc -l | grep -q "^0$" && \
    echo "--- README.md inventory links point to new dirs ---" && \
    grep -q "(./continuity_auditor/SKILL.md)" skills/movie-experts/README.md && \
    grep -q "(./compliance_gate/SKILL.md)" skills/movie-experts/README.md && \
    echo "--- README.md corpus tree uses new dir names ---" && \
    grep -q "continuity_auditor/" skills/movie-experts/README.md && \
    grep -q "compliance_gate/" skills/movie-experts/README.md && \
    echo "--- _shared/glossary.md updated ---" && \
    grep -q "continuity_auditor" skills/movie-experts/_shared/glossary.md && \
    echo "--- glossary line 195 'visual continuity reference' preserved as English noun ---" && \
    grep -q "visual continuity reference" skills/movie-experts/_shared/glossary.md && \
    echo "TASK-2 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md has zero stranded references to `compliance_marketing` (as expert_id)
    - README.md has zero stranded references to `continuity` as expert_id (English-noun uses like "axis continuity" preserved if any)
    - README.md inventory rows link to `./continuity_auditor/SKILL.md` and `./compliance_gate/SKILL.md`
    - README.md corpus tree shows `continuity_auditor/` and `compliance_gate/` directory names
    - README.md ASCII pipeline diagram uses new IDs
    - _shared/glossary.md references `continuity_auditor` where it referred to the expert
    - _shared/glossary.md preserves "visual continuity reference" as an English-noun phrase
    - Grep verification passes
  </acceptance_criteria>
  <done>
    README.md and _shared/glossary.md fully reflect the Phase 13 renames. Cross-cutting documentation artifacts are consistent with the renamed expert directories. Phase 13 documentation closure complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical mapping (skills-mapping.yaml) → downstream phases | Stale `sign_off_status: pending` would cause Phase 14/15 to treat renames as unconfirmed |
| README inventory → new developers | Stale directory paths would mislead readers to deleted/moved content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Repudiation | skills-mapping.yaml sign_off_status | mitigate | Task 1 grep verifies exactly 2 `signed_off` entries and zero `pending` for renamed entries |
| T-13-10 | Information disclosure | README.md corpus tree + inventory | mitigate | Task 2 grep verifies inventory links + corpus tree use new dir names; zero stranded old-ID references |
| T-13-11 | Tampering | _shared/glossary.md expert references | mitigate | Task 2 grep verifies continuity_auditor appears; English-noun uses preserved |

No third-party package installs — T-{phase}-SC is N/A.
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. skills-mapping.yaml signed off
grep -c "sign_off_status: signed_off" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "^2$"
! grep -B1 -A5 "new_node: continuity_auditor\|new_node: compliance_gate" .planning/research/v2-pipeline-design/skills-mapping.yaml | grep -q "sign_off_status: pending"

# 2. README.md updated
grep -q "(./continuity_auditor/SKILL.md)" skills/movie-experts/README.md
grep -q "(./compliance_gate/SKILL.md)" skills/movie-experts/README.md
! grep -q "compliance_marketing" skills/movie-experts/README.md
grep -n "continuity[^_]" skills/movie-experts/README.md | grep -v "continuity_auditor" | wc -l   # must be 0

# 3. _shared/glossary.md updated
grep -q "continuity_auditor" skills/movie-experts/_shared/glossary.md
grep -q "visual continuity reference" skills/movie-experts/_shared/glossary.md   # English noun preserved
```
</verification>

<success_criteria>
- [ ] skills-mapping.yaml: both renamed entries show `sign_off_status: signed_off`
- [ ] Exactly 2 `sign_off_status: signed_off` occurrences; zero `pending` for renamed entries
- [ ] README.md inventory rows link to new directory paths
- [ ] README.md corpus tree shows new directory names
- [ ] README.md ASCII pipeline diagram uses new IDs
- [ ] README.md has zero stranded `compliance_marketing` references
- [ ] README.md has zero stranded `continuity` expert_id references (English-noun uses preserved)
- [ ] _shared/glossary.md references `continuity_auditor` where it referred to the expert
- [ ] _shared/glossary.md preserves English-noun "visual continuity reference"
</success_criteria>

<output>
Create `.planning/phases/13-expert-rename-alias-scaffolding/13-03-SUMMARY.md` when done
</output>

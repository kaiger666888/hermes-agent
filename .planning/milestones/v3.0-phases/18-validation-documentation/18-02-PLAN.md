---
phase: 18-validation-documentation
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
  - skills/movie-experts/_shared/known-external-models.yaml
autonomous: true
requirements: [DOC-01, DOC-02]
user_setup: []

must_haves:
  truths:
    - "A reader opens skills/movie-experts/README.md and finds a Mermaid DAG block matching the canonical 16-node topology from 01-NODE-DAG.md §1.5 (replacing the ASCII art DAG that no longer matches v3.0 reality)"
    - "A reader opens skills/movie-experts/README.md and finds an inventory whose bucket structure matches VALIDATION-REPORT.md (16 DAG active + 3 non-DAG active + 3 deprecated + 10 redirect stubs), with a footer count matching the reconciled total"
    - "A reader opens _shared/glossary.md and finds dedicated H3 entries for all 5 canonical new-expert terms: visual_executor, audio_pipeline, prompt_injector, continuity_auditor, compliance_gate"
    - "A reader opens _shared/known-external-models.yaml and finds a Phase 18 dated annex section with verified_date: 2026-06-17 entries for every model in Phase 8 §2.17's annex table"
  artifacts:
    - path: "skills/movie-experts/README.md"
      provides: "Mermaid DAG + reconciled inventory table + footer count"
      contains: "```mermaid"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "Glossary with all 5 new-expert terms verified present"
      contains: "### compliance_gate"
    - path: "skills/movie-experts/_shared/known-external-models.yaml"
      provides: "Phase 8 §2.17 dated annex models with verified_date: 2026-06-17"
      contains: "verified_date: 2026-06-17"
  key_links:
    - from: "README.md Mermaid DAG"
      to: "01-NODE-DAG.md §1.5"
      via: "Mermaid block structure (graph TD + subgraphs L0-L6 + Vertical)"
      pattern: "graph TD"
    - from: "README.md inventory footer count"
      to: "VALIDATION-REPORT.md reconciliation arithmetic"
      via: "matching reconciled total"
      pattern: "16 active DAG"
    - from: "glossary.md"
      to: "REQUIREMENTS DOC-02"
      via: "5 new-term H3 entries"
      pattern: "### (visual_executor|audio_pipeline|prompt_injector|continuity_auditor|compliance_gate)"
---

<objective>
Finalize the v3.0 documentation surface: README.md inventory + DAG, glossary verification, known-external-models.yaml dated annex. Satisfies ROADMAP §18 #3, #4, #5 and REQUIREMENTS DOC-01, DOC-02.

Purpose: After 18-01's audit establishes the canonical inventory classification, 18-02 propagates that classification into the three user-facing documentation surfaces. The Mermaid DAG conversion is the centerpiece — the current ASCII art DAG (Phase 14-15-16-17 accreted) no longer matches the v2.0 PRFP canonical topology, so it is replaced wholesale with the Mermaid block from `01-NODE-DAG.md §1.5`.

Output: Updated README.md, glossary.md, known-external-models.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-validation-documentation/18-CONTEXT.md
@.planning/phases/18-validation-documentation/18-01-PLAN.md
@.planning/research/v2-pipeline-design/01-NODE-DAG.md
@.planning/research/v2-pipeline-design/02-NODE-SPECS.md

<interfaces>
<!-- The canonical Mermaid block lives at .planning/research/v2-pipeline-design/01-NODE-DAG.md §1.5 (lines 186-270). Replicate it verbatim into README.md (with attribution comment "Per 01-NODE-DAG.md §1.5"). -->

<!-- Phase 8 §2.17 dated annex models (from 02-NODE-SPECS.md §2.17 lines 387-408) — every model entry needed in known-external-models.yaml: -->
- creative_source: Claude Sonnet 4.5 (kernel expansion, evolving, GPT-5/GLM-4.6 alternatives)
- style_genome: Claude Sonnet 4.5 (style extraction, stable_2026)
- screenplay: Claude Sonnet 4.5 (screenplay gen, evolving)
- script_auditor: Claude Haiku 4.5 (5-dim audit, stable_2026)
- character_designer: FLUX 2 + IP-Adapter (face/body gen, evolving) + CosyVoice 2 (voice cloning, stable_2026)
- cinematographer: Claude Sonnet 4.5 (visual intent, evolving)
- prompt_injector: Template + few-shot (engineering, stable_2026)
- visual_executor (drawer sub-step): FLUX 2 (image gen, evolving, SD4/Ideogram alternatives)
- visual_executor (animator sub-step): Sora 2 / Kling 2.0 (video gen, evolving, Veo 4/Runway Gen-5 alternatives)
- audio_pipeline (voicer sub-step): CosyVoice 2 / ElevenLabs v3 (TTS, evolving, Azure TTS/OpenAI TTS-2 alternatives)
- audio_pipeline (composer sub-step): Suno V5 / Udio 2 (music gen, evolving, MusicGen-Large alternative)
- audio_pipeline (foley sub-step): Stable Audio Open (foley gen, evolving, AudioLDM-3 alternative)
- audio_pipeline (mixer sub-step): DSP engineering / ffmpeg (LUFS targeting, stable_2026)
- editor: Claude Sonnet 4.5 (cut-point suggestions, evolving)
- colorist: DaVinci Resolve + AI LUT (color grading, stable_2026)
- hook_retention: Claude Sonnet 4.5 (hook + retention, evolving)
- quality_gate: Claude Sonnet 4.5 + custom (multi-dim verdict, stable_2026)
- compliance_gate: Claude Sonnet 4.5 + rules (CN compliance, stable_2026)
- theory_critic: Claude Opus 4.7 (theory consultation, evolving)

<!-- Existing 5 new-expert glossary entries (already present per Phase 13/14/15/16 close-outs — verification target, not creation target): -->
- continuity_auditor (Phase 13 — verify present, may be only inline-annotated in older glossary version)
- compliance_gate (Phase 13 — verify present)
- visual_executor (Phase 14 — present per glossary.md line 227)
- audio_pipeline (Phase 15 — present per glossary.md line 239)
- prompt_injector (Phase 16 — present per glossary.md line 251)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md Mermaid DAG conversion + reconciled inventory table</name>
  <files>skills/movie-experts/README.md</files>
  <read_first>
    - skills/movie-experts/README.md (current state — full read to find the ASCII DAG block at "## Production DAG (Collaboration Graph)" section + footer count + inventory sub-sections)
    - .planning/research/v2-pipeline-design/01-NODE-DAG.md §1.5 (the canonical Mermaid block to replicate — lines 186-270)
    - .planning/phases/18-validation-documentation/VALIDATION-REPORT.md (the reconciled inventory classification from 18-01)
  </read_first>
  <action>
    Three coordinated edits to skills/movie-experts/README.md:

    **Edit 1 — Replace the ASCII DAG block.** Find the section `## Production DAG (Collaboration Graph)` (currently an ASCII-art block spanning roughly lines 127-204 plus the "Phase 8 Cross-Cutting Experts" sub-block at lines 222-259). Replace the entire ASCII-art block with a fenced `​```mermaid` block that replicates the canonical Mermaid topology from `01-NODE-DAG.md §1.5` verbatim (the `graph TD` with subgraphs L0/L1/L2/L3/L4/L5/L6/Vertical, the 16 nodes with bilingual labels, the linear edges, the form-specific feedback loop screenplay↔hook_retention, the two loop_with_critic edges screenplay↔script_auditor + visual_executor↔continuity_auditor, the two human_gate edges, the theory_critic consultative dotted edges, and the style_genome + character_designer cross-cutting invariant dotted edges). Add an attribution comment above the fence: `<!-- Mermaid topology per .planning/research/v2-pipeline-design/01-NODE-DAG.md §1.5 (canonical v2.0 PRFP DAG) -->`. Then below the fence, add a short "Topology notes" bullet list capturing the 7 properties from §1.3 (6 layers + 1 vertical, 2 explicit loops, 2 human gates, 3 parallel branches, 1 feedback edge, 2 cross-cutting invariants, 16 nodes total).

    Do NOT retain the ASCII art as a comment or fallback — the Mermaid block is the only DAG representation in README after this edit. (Phase 14-15-16-17 each accreted ASCII art patches; the result was no longer coherent. Mermaid replaces it wholesale.)

    The "Phase 8 Cross-Cutting Experts" sub-section below (currently its own ASCII art block at lines 222-259) — convert its diagram to a brief bullet list describing the 3 corpus-driven verticals (theory_critic, documentary_maker, animation_studio) and their relationship to `_shared/project-corpus/`. theory_critic is already in the canonical Mermaid DAG (as the Vertical consultative node); documentary_maker and animation_studio are NOT in the canonical DAG but are preserved corpus-driven verticals. Document this distinction explicitly.

    **Edit 2 — Inventory sub-section consolidation.** The current README has 7 inventory sub-sections accreted across phases ("14 Original Experts", "4 New Experts Phase 1-5", "5 New Experts Phase 7", "3 New Experts Phase 8", "1 New Expert Phase 16", "3 Deprecated Phase 17"). Consolidate into a single canonical "## Active Expert Inventory" table with one row per active expert, plus a separate "## Deprecated Experts (Phase 17)" table and a separate "## Redirect Stubs (legacy expert_id preservation)" table. The active inventory table columns: expert_id | Chinese name | Role | Mapping type (one_to_one_preserved / one_to_one_renamed / n_to_one_merged / new_ai_native / corpus_vertical / deferred) | Phase built. Use VALIDATION-REPORT.md as the source of truth for which experts are active vs deprecated vs redirect-stub.

    Keep the per-phase provenance in a smaller "Provenance notes" bullet list below the consolidated tables (e.g., "visual_executor: Phase 5 v1.5 drawer + animator → Phase 14 merge per MERGE-01"), so no historical information is lost — just reorganized.

    **Edit 3 — Footer count update.** The footer currently reads "v3.0 = 18 expert_ids in codebase (15 active + 3 deprecated...)". Update to reflect VALIDATION-REPORT.md's reconciled total. Suggested footer form (use the actual numbers from VALIDATION-REPORT.md):
    `v3.0 reconciled inventory: 16 active DAG pipeline-roles + 3 active non-DAG verticals/deferred + 3 deprecated (Phase 17) + 10 redirect stubs preserving legacy expert_id per FOUND-08. See VALIDATION-REPORT.md for the definitive classification.`

    Also update the top-of-file Status line (currently "v3.0 in progress — 18 expert_ids in codebase (15 active + 3 deprecated...)") to match the reconciled count.

    Per CONTEXT D-06 ("no silent sign-off"): the footer note should explicitly cite the 21-target → actual reconciliation: "Original v3.0 target was 21 (16 active + 5 aliases); Phase 18-01 audit reconciled to the actual on-disk reality per VALIDATION-REPORT.md."
  </action>
  <verify>
    <automated>grep -q '```mermaid' skills/movie-experts/README.md && grep -q "graph TD" skills/movie-experts/README.md && grep -q "01-NODE-DAG.md §1.5" skills/movie-experts/README.md && grep -q "16 active DAG" skills/movie-experts/README.md && ! grep -qE "USER INTENT|USER INTENT.*topic" skills/movie-experts/README.md && echo "README Mermaid + inventory + footer OK"</automated>
  </verify>
  <acceptance_criteria>
    - The ASCII DAG block is gone; a fenced Mermaid block with the canonical 16-node topology is in its place, attributed to 01-NODE-DAG.md §1.5.
    - The Mermaid block contains `graph TD`, all 16 nodes (creative_source through compliance_gate + theory_critic), and the 7 topology properties documented below it.
    - Inventory sub-sections consolidated into Active / Deprecated / Redirect-stub tables matching VALIDATION-REPORT.md's bucket classification.
    - Footer count + Status line reflect the reconciled total from VALIDATION-REPORT.md.
    - The original 21-target discrepancy is explicitly documented (CONTEXT D-06 compliance).
    - No ASCII-art DAG fragment remains in the document.
  </acceptance_criteria>
  <done>
    README.md is the canonical v3.0 surface: Mermaid DAG + reconciled inventory + accurate footer count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Glossary verification + add any missing 5 new-expert terms</name>
  <files>skills/movie-experts/_shared/glossary.md</files>
  <read_first>
    - skills/movie-experts/_shared/glossary.md (full read — verify which of the 5 new-expert terms are already present)
    - .planning/REQUIREMENTS.md DOC-02 (the 5 required terms: visual_executor, audio_pipeline, prompt_injector, continuity_auditor, compliance_gate)
  </read_first>
  <action>
    DOC-02 requires glossary.md to have entries for: visual_executor, audio_pipeline, prompt_injector, continuity_auditor, compliance_gate.

    Read the existing glossary and run a verification grep for each term. From the initial audit:
    - visual_executor: PRESENT (Phase 14 additions section, line ~227)
    - audio_pipeline: PRESENT (Phase 15 additions section, line ~239)
    - prompt_injector: PRESENT (Phase 16 additions section, line ~251)
    - continuity_auditor: NEEDS VERIFICATION — was renamed from continuity in Phase 13; check whether a dedicated H3 entry exists or whether continuity mentions are only inline-annotated in older sections (镜头语言 / 调度 / etc.)
    - compliance_gate: NEEDS VERIFICATION — was renamed from compliance_marketing in Phase 13; same check.

    For any of the 5 terms whose dedicated H3 entry is MISSING (most likely continuity_auditor + compliance_gate if Phase 13's close-out only annotated old prose rather than adding new top-level entries), add a new H3 entry in a "## Phase 18 canonical term reconciliation" H2 section at the end of the glossary. Each new H3 entry follows the existing bilingual CN/EN/Context format used by Phase 14/15/16 additions. For continuity_auditor and compliance_gate, the entry content should reference:
    - The Phase 13 rename rationale (critic role emphasis / pure-compliance separation per ROADMAP §13 + skills-mapping.yaml)
    - The backward-compat alias (continuity / compliance_marketing preserved per FOUND-08)
    - The DAG layer (continuity_auditor: L3 critic_paired with visual_executor; compliance_gate: L6 final_gate paired with quality_gate)

    For any of the 5 terms already PRESENT, do NOT duplicate — verify only. If an existing entry has a stale reference (e.g., the glossary says "EXPERT-COMPLI" but should now say "compliance_gate" per the rename), update the inline reference to the canonical ID — but per Phase 13's documentation precedent (decisions table line 99-104), bare-noun references in glossary context prose may be preserved. Use judgment: if the prose clearly refers to the current canonical expert, update; if it refers to the role concept generically, preserve.

    Update the glossary's top-of-file "Last updated" line: `2026-06-17 (Phase 18 — DOC-02 verification: 5 new-expert terms confirmed present, N added, M inline-updated)`.

    Also add a brief "## Phase 18 DOC-02 verification" section at the end documenting the verification matrix: for each of the 5 terms, line number where the entry lives, or "ADDED in Phase 18 §X" if newly added.
  </action>
  <verify>
    <automated>for term in visual_executor audio_pipeline prompt_injector continuity_auditor compliance_gate; do if ! grep -qE "^### .*$term|^### $term" skills/movie-experts/_shared/glossary.md; then echo "MISSING: $term"; fi; done && grep -q "Phase 18 DOC-02 verification" skills/movie-experts/_shared/glossary.md && echo "Glossary verification OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 terms (visual_executor, audio_pipeline, prompt_injector, continuity_auditor, compliance_gate) have dedicated H3 entries in glossary.md.
    - Each new entry (if any added in Phase 18) follows the established bilingual CN/EN/Context format.
    - A "Phase 18 DOC-02 verification" section documents which terms were already present vs newly added.
    - "Last updated" header reflects Phase 18.
  </acceptance_criteria>
  <done>
    Glossary satisfies DOC-02's 5-term requirement with documented verification.
  </done>
</task>

<task type="auto">
  <name>Task 3: known-external-models.yaml Phase 8 §2.17 dated annex additions</name>
  <files>skills/movie-experts/_shared/known-external-models.yaml</files>
  <read_first>
    - skills/movie-experts/_shared/known-external-models.yaml (full read — understand existing format + Phase 5/7 additions patterns)
    - .planning/research/v2-pipeline-design/02-NODE-SPECS.md §2.17 (lines 380-410 — the dated annex table to mirror)
    - skills/movie-experts/visual_executor/SKILL.md + skills/movie-experts/audio_pipeline/SKILL.md + skills/movie-experts/prompt_injector/SKILL.md (to confirm which model names appear in current SKILL.md prose that the scanner must allowlist)
  </read_first>
  <action>
    Per ROADMAP §18 #5 + DOC-02, add a "Phase 8 §2.17 dated annex (verified 2026-06-17)" section to known-external-models.yaml that mirrors the dated annex table from 02-NODE-SPECS.md §2.17.

    Each new entry follows the existing yaml schema (name + provenance fields). Add a `verified_date: 2026-06-17` field per the dated-annex convention. Add a `stability: stable_2026 | evolving` field per the §2.17 stability legend. Add a `node_role` field tying the entry to its DAG node + sub-step (e.g., `node_role: visual_executor/drawer`).

    The 19 model entries from §2.17 to add (de-duplicated — some models like Claude Sonnet 4.5 appear in multiple nodes):

    **Already allowlisted (verify + add dated-annex metadata, do NOT duplicate the base entry):**
    - FLUX 2 (existing entries: flux / flux2 / flux_2) — used by character_designer + visual_executor/drawer. Add verified_date + node_role annotation.
    - CosyVoice (existing entry: cosyvoice) — used by character_designer (voice cloning) + audio_pipeline/voicer. Add verified_date + node_role.
    - ElevenLabs (existing entry: elevenlabs) — used by audio_pipeline/voicer. Add verified_date + node_role + note about v3 specifically.
    - Stable Audio Open (existing entries: stable_audio / stable_audio_open) — used by audio_pipeline/foley. Add verified_date + node_role.
    - MusicGen (existing entry: musicgen) — used as alternative for audio_pipeline/composer. Add verified_date + node_role.
    - Kling (existing entry: kling) — used by visual_executor/animator. Update provenance to note Kling 2.0 + add verified_date + node_role.
    - Sora (existing entry: sora) — used by visual_executor/animator. Update provenance to note Sora 2 + add verified_date + node_role.
    - Veo (existing entries: veo / veo3) — used as alternative for visual_executor/animator. Add verified_date + node_role.

    **New entries to add (not in existing allowlist):**
    - claude-sonnet-4.5 (multi-node: creative_source, style_genome, screenplay, cinematographer, editor, hook_retention, quality_gate, compliance_gate) — Anthropic LLM. stable_2026 for style_genome/script_auditor/quality_gate/compliance_gate; evolving for the rest. Provenance: external (Anthropic API or Hermes anthropic provider plugin).
    - claude-haiku-4.5 (script_auditor) — Anthropic LLM, stable_2026. Provenance: external.
    - claude-opus-4.7 (theory_critic) — Anthropic LLM, evolving. Provenance: external.
    - glm-4.6 (alternative for creative_source + screenplay + hook_retention + compliance_gate) — Zhipu LLM. Existing `glm` entry covers GLM-4; add `glm-4.6` variant entry or extend the `glm` entry with the 4.6 version note.
    - gpt-5 (alternative for multiple nodes) — OpenAI LLM. Not currently in allowlist as bare token; add with provenance "OpenAI GPT-5 — referenced as swap alternative in Phase 8 §2.17 dated annex".
    - gpt-5-mini (script_auditor alternative) — OpenAI LLM, alternative to Claude Haiku 4.5.
    - gemini-3-pro (theory_critic alternative + style_genome alternative) — Google DeepMind LLM.
    - ip-adapter (existing entry — verify, add verified_date + note about character_designer §2.17 use)
    - suno-v5 (audio_pipeline/composer) — Suno V5 music gen, evolving. NEW entry.
    - udio-2 (audio_pipeline/composer) — Udio 2 music gen, evolving. NEW entry.
    - audioldm-3 (audio_pipeline/foley alternative) — AudioLDM-3 alternative to Stable Audio Open, evolving. (Note: existing `audioldm-2` entry is documented as superseded; add `audioldm-3` as the §2.17 swap alternative.)
    - ideogram (visual_executor/drawer alternative) — Ideogram image gen, evolving. NEW entry.
    - runway-gen-5 (visual_executor/animator alternative) — Runway Gen-5, evolving. Update existing `runway` entry to note Gen-5 + add verified_date.
    - azure-tts (audio_pipeline/voicer alternative) — Azure TTS, evolving. NEW entry.
    - openai-tts-2 (audio_pipeline/voicer alternative) — OpenAI TTS-2, evolving. NEW entry.
    - sd4 (character_designer + visual_executor/drawer alternative) — Stable Diffusion 4, evolving. NEW entry.
    - davinci-resolve (colorist) — DaVinci Resolve + AI LUT for color grading, stable_2026. NEW entry (external software, not a Hermes plugin).
    - ffmpeg-dsp (audio_pipeline/mixer) — DSP engineering via ffmpeg for LUFS targeting, stable_2026. NEW entry (or extend `synthesis_model`-style generic token).
    - template-few-shot (prompt_injector) — Template + few-shot prompt engineering, stable_2026. This is a methodology token, not a model — document accordingly (no model swap alternatives).

    For each new entry, ensure provenance clearly distinguishes: Hermes-catalog plugin / external (user deploys) / open-source / methodology token. Add the section comment header `# Phase 8 §2.17 dated annex additions (2026-06-17) — mirrors 02-NODE-SPECS.md §2.17`.

    The scanner at `scripts/verify_skill_references.py` (referenced in the file's existing header) must not flag any of these tokens when they appear in skill prose. This plan does NOT modify verify_skill_references.py — only the allowlist yaml.

    After the edit, run a sanity check: the file remains valid YAML (parse with `python3 -c "import yaml; yaml.safe_load(open('skills/movie-experts/_shared/known-external-models.yaml', encoding='utf-8'))"`) and the entry count is documented in a footer comment.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('skills/movie-experts/_shared/known-external-models.yaml', encoding='utf-8')); assert 'models' in d; assert len(d['models']) >= 35; print(f'YAML valid, {len(d[\"models\"])} models')" && grep -q "Phase 8 §2.17 dated annex" skills/movie-experts/_shared/known-external-models.yaml && grep -q "verified_date: 2026-06-17" skills/movie-experts/_shared/known-external-models.yaml && echo "known-external-models.yaml §2.17 annex OK"</automated>
  </verify>
  <acceptance_criteria>
    - File remains valid YAML (parseable).
    - A "Phase 8 §2.17 dated annex additions" section is present with all 19 model entries from §2.17's table.
    - Each new entry carries `verified_date: 2026-06-17`, `stability`, `node_role`, and `provenance` fields.
    - Existing allowlist entries that §2.17 references (FLUX 2, CosyVoice, ElevenLabs, Stable Audio Open, MusicGen, Kling, Sora, Veo) are extended with `verified_date` + `node_role` rather than duplicated.
    - No phantom/fabricated tokens — every entry traces back to §2.17 or current SKILL.md prose.
  </acceptance_criteria>
  <done>
    known-external-models.yaml mirrors Phase 8 §2.17 dated annex; all v3.0 model references are allowlisted with dated provenance.
  </done>
</task>

</tasks>

<verification>
- README.md has a Mermaid DAG block (no ASCII art) matching 01-NODE-DAG.md §1.5
- README.md inventory + footer count match VALIDATION-REPORT.md's reconciled total
- glossary.md has all 5 new-expert terms (visual_executor, audio_pipeline, prompt_injector, continuity_auditor, compliance_gate) with a Phase 18 DOC-02 verification section
- known-external-models.yaml has a Phase 8 §2.17 dated annex section with verified_date: 2026-06-17 entries
- known-external-models.yaml parses as valid YAML
- DOC-01 + DOC-02 are verifiable from these three files
</verification>

<success_criteria>
README.md + glossary.md + known-external-models.yaml are the canonical v3.0 user-facing documentation surfaces. All three reflect the reconciled inventory from 18-01 and the canonical v2.0 PRFP DAG topology from 01-NODE-DAG.md §1.5. DOC-01 and DOC-02 are satisfied.
</success_criteria>

<output>
Create `.planning/phases/18-validation-documentation/18-02-SUMMARY.md` when done
</output>

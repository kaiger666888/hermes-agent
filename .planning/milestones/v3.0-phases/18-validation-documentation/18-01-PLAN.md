---
phase: 18-validation-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-validation-documentation/VALIDATION-REPORT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [VALIDATE-01, VALIDATE-02]
user_setup: []

must_haves:
  truths:
    - "A reader can open VALIDATION-REPORT.md and find a definitive classification of every expert_id declaration as ACTIVE / ALIAS (redirect stub) / DEPRECATED, with arithmetic reconciling 31 SKILL.md files to the canonical 16 DAG pipeline-roles + 5 aliases target"
    - "A reader can verify FOUND-08 compliance from VALIDATION-REPORT.md: every alias is explicit (no silent renames), every redirect stub carries status + redirect target, every deprecated expert carries status + inheritance_targets"
    - "A reader can verify backward compatibility from VALIDATION-REPORT.md: every old expert_id (continuity / compliance_marketing / drawer / animator / voicer / lip_sync / composer / foley / mixer / spatial_audio / performer / scene_builder / storyboard_designer) still resolves to a current SKILL.md via alias or redirect"
    - "ROADMAP.md §18 criterion #1 reflects the actual reconciled count (replacing the 21 placeholder with the audited total), with the audit math documented"
  artifacts:
    - path: ".planning/phases/18-validation-documentation/VALIDATION-REPORT.md"
      provides: "Definitive inventory classification + FOUND-08 audit + backward-compat verification for v3.0 milestone"
      contains: "## Inventory Classification"
      min_lines: 120
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 18 criterion #1 reflecting actual reconciled count"
      contains: "Phase 18"
  key_links:
    - from: "VALIDATION-REPORT.md arithmetic"
      to: "ROADMAP §18 #1 target"
      via: "explicit reconciliation table"
      pattern: "16 active DAG \\+ 2 vertical \\+ 1 deferred \\+ 3 deprecated \\+ .* redirect stubs"
    - from: "VALIDATION-REPORT.md FOUND-08 audit"
      to: "REQUIREMENTS.md VALIDATE-01"
      via: "FOUND-08 compliance section"
      pattern: "zero silent renames"
---

<objective>
Validate the v3.0 milestone end-state against ROADMAP §18 success criteria #1, #6, #7 and REQUIREMENTS VALIDATE-01, VALIDATE-02.

Purpose: Phase 18 is the integration gate. Before documentation finalization (18-02) and sign-off (18-03), Phase 18-01 must definitively establish (a) the real expert_id inventory classification, (b) FOUND-08 frozen-rule compliance, (c) backward-compat alias resolution, and (d) reconcile any discrepancy between ROADMAP §18 #1's stated target (21 = 16 active + 5 aliases) and the actual post-Phase-17 state.

Output: `VALIDATION-REPORT.md` (the canonical audit artifact referenced by VALIDATE-01 + VALIDATE-02), updated ROADMAP §18 #1 with reconciled count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-validation-documentation/18-CONTEXT.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml
@.planning/research/v2-pipeline-design/01-NODE-DAG.md

<interfaces>
<!-- Canonical 16 DAG pipeline-roles from 01-NODE-DAG.md §1.1 + §1.5 Mermaid -->
ACTIVE DAG NODES (16):
  1. creative_source (L0 root, preserved)
  2. style_genome (L1, preserved)
  3. screenplay (L1, preserved)
  4. script_auditor (L1, preserved)
  5. character_designer (L1, preserved)
  6. cinematographer (L2, preserved)
  7. prompt_injector (L2, NEW Phase 16)
  8. visual_executor (L3, NEW Phase 14 merge of drawer+animator)
  9. audio_pipeline (L4, NEW Phase 15 merge of voicer+lip_sync+composer+foley+mixer+spatial_audio)
  10. continuity_auditor (L3, renamed from continuity Phase 13)
  11. editor (L4, preserved)
  12. colorist (L4, preserved)
  13. hook_retention (L5, preserved)
  14. quality_gate (L6, preserved)
  15. compliance_gate (L6, renamed from compliance_marketing Phase 13)
  16. theory_critic (Vertical consultative, preserved)

NON-DAG ACTIVE EXPERTS (3 — corpus-driven verticals + 1 deferred, per Phase 8 + skills-mapping.yaml not_in_new_dag):
  - documentary_maker (Phase 8 corpus-driven vertical, NOT in canonical DAG, ACTIVE)
  - animation_studio (Phase 8 corpus-driven vertical, NOT in canonical DAG, ACTIVE)
  - production (skills-mapping.yaml: disposition=deferred, v3.0 did NOT deprecate; ACTIVE pending FUTURE-09)

DEPRECATED (3 — Phase 17):
  - performer → character_designer + screenplay
  - scene_builder → cinematographer + style_genome
  - storyboard_designer → cinematographer

REDIRECT STUBS (10 — preserve old expert_id per FOUND-08):
  Renames (2): continuity, compliance_marketing
  Merges into visual_executor (2): drawer, animator
  Merges into audio_pipeline (6): voicer, lip_sync, composer, foley, mixer, spatial_audio

ARITHMETIC:
  16 DAG active + 3 non-DAG active + 3 deprecated + 10 redirect stubs = 32 expected
  Actual find: 31 SKILL.md files (non _eval, non _shared)
  DISCREPANCY: Verify which expert_id is missing from the 32 expectation vs the 31 reality. (Spoiler: lip_sync is in the find list — the 31 count is correct; the 32 above overcounts because one of the redirect stubs may have a different filename pattern. Audit must confirm.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate inventory + classify every expert_id + reconcile count</name>
  <files>.planning/phases/18-validation-documentation/VALIDATION-REPORT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VALIDATE-01, VALIDATE-02 definitions)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml (canonical mapping)
    - .planning/research/v2-pipeline-design/01-NODE-DAG.md §1.1 + §1.5 (16-node canonical set)
    - skills/movie-experts/README.md (current inventory claims + footer count)
  </read_first>
  <action>
    Run the canonical enumeration script (do NOT hand-count):
    ```
    find skills/movie-experts -maxdepth 2 -name 'SKILL.md' | grep -v '_eval\|_shared' | sort
    ```
    Then for each of the resulting SKILL.md files, extract (via grep -m1) the frontmatter `expert_id:` value, `status:` value (if present), `aliases:` value (if present), `merged_into:` / `folded_into:` / `renamed_to:` / `deprecated_reason:` / `inheritance_targets:` fields.

    Classify each into exactly one of four buckets:

    **ACTIVE (DAG pipeline-role):** expert_id is in the canonical 16-node set from 01-NODE-DAG.md §1.1. Status field absent (active). Directory contains substantive content (not a stub).

    **ACTIVE (non-DAG vertical / deferred):** documentary_maker, animation_studio, production. These are valid experts that v3.0 did not migrate into the canonical DAG. Documentary_maker + animation_studio are Phase 8 corpus-driven verticals. Production is skills-mapping.yaml:140-143 `disposition: deferred` (FUTURE-09). Status field absent (active).

    **DEPRECATED (Phase 17):** performer, scene_builder, storyboard_designer. Status field = `status: deprecated`. Per REQUIREMENTS DEPRECATE-01/02/03.

    **REDIRECT STUB (preserves old expert_id per FOUND-08):** status field = `status: renamed` OR `status: merged_into` OR `status: folded_into`. Body content is a redirect notice pointing at the current SKILL.md. These are: continuity (renamed → continuity_auditor), compliance_marketing (renamed → compliance_gate), drawer + animator (merged → visual_executor), voicer + lip_sync + composer + foley + mixer + spatial_audio (merged/folded → audio_pipeline).

    Write the classification table to VALIDATION-REPORT.md with 4 sections (one per bucket) + a reconciliation arithmetic block showing:
    ```
    ACTIVE DAG (16) + ACTIVE non-DAG (3) + DEPRECATED (3) + REDIRECT STUBS (10)
    = 32 total SKILL.md files (expected from spec)
    vs. N actual find result (run the find command to get the real number)
    ```

    IMPORTANT — ROADMAP §18 #1 states "21 (16 active + 5 aliases)". This was an early estimate that does NOT match reality. The actual redirect stub count is 10 (not 5), and there are 3 additional ACTIVE non-DAG experts (documentary_maker, animation_studio, production) that the 21-figure did not account for. The VALIDATION-REPORT.md reconciliation section MUST:
    (1) Document the actual count clearly.
    (2) Explain why 21 ≠ actual (the 5-alias figure undercounted redirect stubs; the 16-active figure did not include non-DAG verticals + deferred).
    (3) Recommend a corrected ROADMAP §18 #1 criterion that reflects the audited classification (per CONTEXT D-06 "no silent sign-off" — surface the discrepancy, do not hide it).
    (4) Note that the "21" figure is a target-derived abstraction (16 DAG pipeline-roles + ~5 rename/merge aliases); the on-disk reality is richer because FOUND-08 preservation requires every old expert_id to keep a redirect stub, which is more than 5.

    The report must also document any directory whose `expert_id:` field is absent or whose status field is malformed — flagged as INVENTORY DEFECTS requiring a fix (out of scope for 18-01; surface for 18-03 to patch or document).
  </action>
  <verify>
    <automated>test -f .planning/phases/18-validation-documentation/VALIDATION-REPORT.md && grep -q "## Inventory Classification" .planning/phases/18-validation-documentation/VALIDATION-REPORT.md && grep -q "## FOUND-08 Compliance Audit" .planning/phases/18-validation-documentation/VALIDATION-REPORT.md && grep -q "## Backward Compatibility Verification" .planning/phases/18-validation-documentation/VALIDATION-REPORT.md && grep -q "## Reconciliation Arithmetic" .planning/phases/18-validation-documentation/VALIDATION-REPORT.md && echo "VALIDATION-REPORT.md structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION-REPORT.md exists with 4 mandatory H2 sections (Inventory Classification, FOUND-08 Compliance Audit, Backward Compatibility Verification, Reconciliation Arithmetic).
    - Every non-_eval, non-_shared SKILL.md file appears in exactly one classification bucket.
    - The reconciliation arithmetic block explicitly computes 16 + 3 + 3 + 10 vs the actual find count.
    - The report explicitly notes the 21-target discrepancy and recommends a corrected ROADMAP §18 #1 criterion.
    - No SKILL.md content is modified in this task — only the audit report is produced.
  </acceptance_criteria>
  <done>
    VALIDATION-REPORT.md published with definitive classification of all 31 SKILL.md files; reconciliation arithmetic documented; ROADMAP §18 #1 discrepancy surfaced with recommended correction.
  </done>
</task>

<task type="auto">
  <name>Task 2: FOUND-08 audit + backward-compat verification (populate VALIDATION-REPORT.md H2 sections 2-3)</name>
  <files>.planning/phases/18-validation-documentation/VALIDATION-REPORT.md</files>
  <read_first>
    - .planning/phases/18-validation-documentation/VALIDATION-REPORT.md (Task 1 output — fill in the empty FOUND-08 + Backward-Compat sections)
    - skills/movie-experts/_shared/SKILL-LAYOUT.md (the canonical FOUND-08 frozen-rule spec for expert_id + alias declaration)
    - skills/movie-experts/continuity_auditor/SKILL.md (canonical alias declaration pattern — frontmatter `aliases: [continuity]`)
    - skills/movie-experts/visual_executor/SKILL.md (canonical multi-alias pattern — frontmatter `aliases: [drawer, animator]`)
    - skills/movie-experts/audio_pipeline/SKILL.md (canonical 6-alias pattern — frontmatter `aliases: [voicer, lip_sync, composer, foley, mixer, spatial_audio]`)
  </read_first>
  <action>
    Populate the `## FOUND-08 Compliance Audit` and `## Backward Compatibility Verification` H2 sections in VALIDATION-REPORT.md (which Task 1 created as empty section headers).

    **FOUND-08 Compliance Audit** — verify zero silent renames / merges / deprecations. For each migration in Phase 13-17, produce one row in a compliance table with columns:
    - Migration (e.g., "Phase 13 RENAME: continuity → continuity_auditor")
    - Old expert_id (e.g., continuity)
    - New expert_id (e.g., continuity_auditor)
    - Redirect stub present? (yes/no — must be yes per FOUND-08)
    - Redirect stub `status:` field value (must be one of: renamed, merged_into, folded_into, deprecated)
    - New expert `metadata.hermes.aliases:` field value (must list the old expert_id)
    - Verdict: PASS / FAIL (FAIL = any of: no stub, no status, no aliases entry, status mismatched to migration type)

    Expected migrations to audit (13 total):
    - Phase 13: continuity → continuity_auditor; compliance_marketing → compliance_gate
    - Phase 14: drawer → visual_executor; animator → visual_executor
    - Phase 15: voicer → audio_pipeline; lip_sync → audio_pipeline; composer → audio_pipeline; foley → audio_pipeline; mixer → audio_pipeline; spatial_audio → audio_pipeline
    - Phase 17 (deprecations — NOT redirects, but still need FOUND-08 preservation): performer, scene_builder, storyboard_designer (verify status: deprecated + inheritance_targets present + body content intact)

    For Phase 17 deprecations the audit row columns differ slightly:
    - Migration (e.g., "Phase 17 DEPRECATE: performer → character_designer + screenplay")
    - Deprecated expert_id (performer)
    - Inheritance targets (character_designer + screenplay)
    - status: deprecated present? (yes/no)
    - metadata.hermes.deprecated_reason present? (yes/no)
    - metadata.hermes.inheritance_targets present? (yes/no)
    - Original body content preserved? (yes/no — must be yes per FOUND-08; deprecation is additive frontmatter + a notice block, NOT a content wipe)
    - Verdict: PASS / FAIL

    Any FAIL verdict is an INVENTORY DEFECT — do NOT patch in this task; surface for 18-03. The report explicitly states "v3.0 milestone cannot close until all FAIL verdicts are resolved".

    **Backward Compatibility Verification** — per VALIDATE-02, verify that every old expert_id reference still resolves. Run this verification matrix:
    - For each redirect stub (continuity, compliance_marketing, drawer, animator, voicer, lip_sync, composer, foley, mixer, spatial_audio): grep the entire `skills/movie-experts/` tree for `expert_id: <old_id>` and `aliases:.*<old_id>` to confirm the old ID is still discoverable from any consumer that references it.
    - For each deprecated (performer, scene_builder, storyboard_designer): grep for the deprecated ID in any consumer's `related_skills` list — these should have been rewired to inheritance targets in Phase 17-01, so the grep should return zero NEW related_skills entries (only the deprecated expert's own frontmatter + _eval/baseline/ snapshots + historical references in skills-mapping.yaml). Any new stranded reference is an INVENTORY DEFECT.
    - Document the verification matrix as a table in the report.

    The backward-compat section must end with an explicit verdict: "All <N> legacy expert_id references resolve via alias or redirect (VALIDATE-02 PASS)" or "N defects found (VALIDATE-02 FAIL — defects must be resolved before milestone close)".
  </action>
  <verify>
    <automated>grep -q "PASS" .planning/phases/18-validation-documentation/VALIDATION-REPORT.md && grep -cE "^\| (Phase 1[3-7]|Migration)" .planning/phases/18-validation-documentation/VALIDATION-REPORT.md | head -1 && echo "FOUND-08 + backward-compat sections populated"</automated>
  </verify>
  <acceptance_criteria>
    - `## FOUND-08 Compliance Audit` section contains a 13-row table (10 redirects + 3 deprecations) with PASS/FAIL verdict per row.
    - `## Backward Compatibility Verification` section contains a verification matrix + explicit VALIDATE-02 verdict (PASS or FAIL with defect count).
    - Any FAIL verdict or defect is explicitly flagged for 18-03 resolution; no silent failures.
    - The report does not patch any defect — only reports. Phase 18-03 owns patches.
  </acceptance_criteria>
  <done>
    VALIDATION-REPORT.md FOUND-08 + backward-compat sections complete with per-migration verdicts and an explicit VALIDATE-02 pass/fail call.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP §18 #1 + commit VALIDATION-REPORT.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 18 section, success criteria block)
    - .planning/phases/18-validation-documentation/VALIDATION-REPORT.md (Task 1 + 2 output — reconciliation arithmetic informs the ROADMAP edit)
  </read_first>
  <action>
    Update ROADMAP.md Phase 18 success criterion #1 to reflect the reconciled count. The current text reads:
    > `grep -r 'expert_id:' skills/movie-experts/ | wc -l` returns 21 (16 active + 5 aliases from rename/merge); no orphan IDs

    Replace with a reconciled criterion that:
    (1) Keeps the grep command as the verification mechanism but updates the target count to match the audited reality (use the number from VALIDATION-REPORT.md Reconciliation Arithmetic — likely 31 or the 4-bucket total).
    (2) Rewrites the bucket description from "21 (16 active + 5 aliases)" to "31 SKILL.md files = 16 active DAG pipeline-roles + 3 active non-DAG (documentary_maker, animation_studio, production) + 3 deprecated (Phase 17) + 10 redirect stubs preserving legacy expert_id per FOUND-08".
    (3) Adds a parenthetical note: "Original v3.0 estimate was 21 (16 + 5 aliases); reconciled to actual on-disk inventory per Phase 18-01 VALIDATION-REPORT.md. The 21 figure undercounted redirect stubs (10, not 5) and omitted 3 active non-DAG verticals + 3 deprecated."

    Also update criterion #6 and #7 to reference VALIDATION-REPORT.md as the canonical audit artifact:
    - #6: "FOUND-08 frozen rule compliance verified: zero silent renames; all aliases explicit. Per VALIDATION-REPORT.md §FOUND-08 Compliance Audit."
    - #7: "Backward compat verified: old expert_id references still resolve via aliases. Per VALIDATION-REPORT.md §Backward Compatibility Verification."

    Per CONTEXT D-06 ("No silent sign-off"), the discrepancy between the original 21-target and the actual reconciled count MUST be surfaced visibly in ROADMAP §18 — do not silently rewrite criterion #1 as if the original 21 had never been written.

    Commit message: `docs(18): VALIDATION-REPORT.md + ROADMAP §18 #1 reconciliation (16+3+3+10 actual vs 21 estimate)`
  </action>
  <verify>
    <automated>grep -q "16 active DAG pipeline-roles" .planning/ROADMAP.md && grep -q "VALIDATION-REPORT.md" .planning/ROADMAP.md && echo "ROADMAP §18 reconciled"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP §18 criterion #1 reflects the actual audited count with the original 21-target discrepancy explicitly documented.
    - ROADMAP §18 criteria #6 and #7 reference VALIDATION-REPORT.md as the canonical audit artifact.
    - The 21-target is not silently erased — the discrepancy is visible to readers (per CONTEXT D-06).
  </acceptance_criteria>
  <done>
    ROADMAP §18 success criteria updated to reflect audited reality; VALIDATION-REPORT.md is the canonical reference for criteria #1, #6, #7.
  </done>
</task>

</tasks>

<verification>
- VALIDATION-REPORT.md exists at `.planning/phases/18-validation-documentation/VALIDATION-REPORT.md`
- Report contains all 4 mandatory H2 sections: Inventory Classification, FOUND-08 Compliance Audit, Backward Compatibility Verification, Reconciliation Arithmetic
- ROADMAP §18 criterion #1 reflects the audited count (not the original 21 placeholder)
- ROADMAP §18 criteria #6, #7 reference VALIDATION-REPORT.md
- No SKILL.md content was modified by this plan (audit-only — patches belong to 18-03)
- Any defect found in the audit is explicitly surfaced (no silent failures)
</verification>

<success_criteria>
VALIDATE-01 + VALIDATE-02 are verifiable from VALIDATION-REPORT.md. ROADMAP §18 #1/#6/#7 reflect audited reality. The v3.0 milestone has a single canonical audit artifact that downstream plans (18-02, 18-03) and the eventual milestone close can reference.
</success_criteria>

<output>
Create `.planning/phases/18-validation-documentation/18-01-SUMMARY.md` when done
</output>

---
phase: 14-visual-executor-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/movie-experts/visual_executor/SKILL.md
  - skills/movie-experts/visual_executor/references/drawer/LICENSE.md
  - skills/movie-experts/visual_executor/references/drawer/flux2-parameter-surface.md
  - skills/movie-experts/visual_executor/references/drawer/character-consistency-lora.md
  - skills/movie-experts/visual_executor/references/animator/LICENSE.md
  - skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md
  - skills/movie-experts/visual_executor/references/animator/temporal-consistency.md
  - skills/movie-experts/visual_executor/references/animator/camera-execution-and-degradation.md
  - skills/movie-experts/visual_executor/GAP-REPORT.md
  - skills/movie-experts/drawer/SKILL.md
  - skills/movie-experts/animator/SKILL.md
autonomous: true
requirements: [MERGE-01]
user_setup: []

must_haves:
  truths:
    - "Reader can read skills/movie-experts/visual_executor/SKILL.md and find a unified expert that declares sub_steps: [drawer, animator] and metadata.hermes.aliases: [drawer, animator]"
    - "The merged visual_executor SKILL.md preserves the body prose of BOTH predecessors as two clearly-marked Sub-step sections"
    - "metadata.hermes.related_skills in visual_executor is the union of drawer.related_skills + animator.related_skills (minus self-references to drawer/animator)"
    - "Old skills/movie-experts/drawer/SKILL.md and animator/SKILL.md are now redirect-only stubs declaring status: merged_into + merged_into: visual_executor"
    - "All 7 ref files (3 drawer-side + 4 animator-side) are migrated under visual_executor/references/{drawer,animator}/ sub-folders"
    - "Drawer and animator GAP-REPORT.md are consolidated under visual_executor/"
  artifacts:
    - path: "skills/movie-experts/visual_executor/SKILL.md"
      provides: "Unified visual_executor expert declaring sub_steps: [drawer, animator]"
      contains: "sub_steps: [drawer, animator]"
    - path: "skills/movie-experts/visual_executor/references/drawer/flux2-parameter-surface.md"
      provides: "Migrated drawer ref"
    - path: "skills/movie-experts/visual_executor/references/drawer/character-consistency-lora.md"
      provides: "Migrated drawer ref"
    - path: "skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md"
      provides: "Migrated animator ref"
    - path: "skills/movie-experts/visual_executor/references/animator/temporal-consistency.md"
      provides: "Migrated animator ref"
    - path: "skills/movie-experts/visual_executor/references/animator/camera-execution-and-degradation.md"
      provides: "Migrated animator ref"
    - path: "skills/movie-experts/visual_executor/GAP-REPORT.md"
      provides: "Consolidated GAP-REPORT from drawer + animator predecessors"
    - path: "skills/movie-experts/drawer/SKILL.md"
      provides: "Redirect-only stub"
      contains: "merged_into: visual_executor"
    - path: "skills/movie-experts/animator/SKILL.md"
      provides: "Redirect-only stub"
      contains: "merged_into: visual_executor"
  key_links:
    - from: "skills/movie-experts/visual_executor/SKILL.md"
      to: "skills/movie-experts/visual_executor/references/drawer/flux2-parameter-surface.md"
      via: "markdown link to migrated drawer ref"
      pattern: "\\(\\.\\/references\\/drawer\\/flux2-parameter-surface\\.md\\)"
    - from: "skills/movie-experts/visual_executor/SKILL.md"
      to: "skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md"
      via: "markdown link to migrated animator ref"
      pattern: "\\(\\.\\/references\\/animator\\/video-gen-model-matrix\\.md\\)"
    - from: "skills/movie-experts/drawer/SKILL.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "redirect notice"
      pattern: "\\(\\.\\./visual_executor/SKILL\\.md\\)"
    - from: "skills/movie-experts/animator/SKILL.md"
      to: "skills/movie-experts/visual_executor/SKILL.md"
      via: "redirect notice"
      pattern: "\\(\\.\\./visual_executor/SKILL\\.md\\)"
---

<objective>
Merge the v1 `drawer` (FLUX 2 image gen) + `animator` (video gen) movie-experts into a unified `visual_executor` node per the v2.0 PRFP DAG. The merged expert declares `sub_steps: [drawer, animator]` and `metadata.hermes.aliases: [drawer, animator]`. Old `drawer/` + `animator/` directories are preserved as redirect stubs (FOUND-08 backward-compat rule).

Purpose: Implement MERGE-01 from REQUIREMENTS.md. Aligns the two distinct technical stacks (FLUX 2 + LoRA vs Veo/Kling/Pixverse) under one expert_id `visual_executor` per `skills-mapping.yaml:85-89`. Rationale per Phase 7 §4.8 + PITFALLS §2.1: "consistency context unified; specialization loss acceptable". Honors FOUND-08 frozen rule (zero silent merges — aliases declared explicitly).

Output:
- New `skills/movie-experts/visual_executor/` directory with:
  - `SKILL.md` declaring `sub_steps: [drawer, animator]`, `aliases: [drawer, animator]`, unioned `related_skills`
  - `references/drawer/` sub-folder (3 files: 2 refs + LICENSE) — verbatim migration
  - `references/animator/` sub-folder (4 files: 3 refs + LICENSE) — verbatim migration
  - `GAP-REPORT.md` consolidated from both predecessors
- Old `skills/movie-experts/drawer/SKILL.md` + `animator/SKILL.md` replaced with redirect-only stubs (frontmatter `status: merged_into` + `merged_into: visual_executor`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-executor-merge/14-CONTEXT.md
@.planning/research/v2-pipeline-design/skills-mapping.yaml
@skills/movie-experts/drawer/SKILL.md
@skills/movie-experts/drawer/references/flux2-parameter-surface.md
@skills/movie-experts/drawer/references/character-consistency-lora.md
@skills/movie-experts/drawer/references/LICENSE.md
@skills/movie-experts/drawer/GAP-REPORT.md
@skills/movie-experts/animator/SKILL.md
@skills/movie-experts/animator/references/video-gen-model-matrix.md
@skills/movie-experts/animator/references/temporal-consistency.md
@skills/movie-experts/animator/references/camera-execution-and-degradation.md
@skills/movie-experts/animator/references/LICENSE.md
@skills/movie-experts/animator/GAP-REPORT.md

# Phase 13 established the rename/merge pattern — read its PLANs for the canonical redirect stub format
@.planning/phases/13-expert-rename-alias-scaffolding/13-01-PLAN.md
@.planning/phases/13-expert-rename-alias-scaffolding/13-01-SUMMARY.md

# CLAUDE.md §"Skill File Conventions (SKILL.md)" defines frontmatter schema:
#   name, description, version, author, license, platforms, prerequisites,
#   metadata.hermes.{tags, related_skills, expert_id, metrics, aliases}
# Phase 14 introduces a NEW top-level frontmatter field `sub_steps` per v2.0 PRFP DAG
# convention (declared at the same level as `metadata`, not nested inside it).

<interfaces>
<!-- visual_executor/SKILL.md frontmatter contract (MERGE-01) -->

```yaml
---
name: visual_executor
description: "Visual Executor Expert: unified FLUX 2 image gen (drawer sub-step) + 2026 Hermes-catalog video gen (animator sub-step) for cinematic visual + temporal consistency. Sub-steps: [drawer, animator]."
version: 1.0.0                       # new expert — start at 1.0.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
prerequisites:
  tools: [hermes_llm, hermes_llm_vision]
sub_steps: [drawer, animator]        # NEW top-level field per v2.0 PRFP DAG
metadata:
  hermes:
    tags: [movie, image, video, flux-2, lora, ip-adapter, instantid, video-gen, camera-motion, temporal-consistency, veo, kling, ltx, pixverse, visual, cinematic, character-consistency]   # union of drawer + animator tags (deduped)
    related_skills: [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer]   # union(drawer.related_skills, animator.related_skills) minus self-refs
    expert_id: visual_executor
    aliases: [drawer, animator]      # backward-compat per FOUND-08
    metrics: [aesthetic_score, character_consistency, film_realism, vram_efficiency, motion_smoothness, motion_complexity, temporal_consistency, generation_fidelity]   # union of drawer + animator metrics
---
```

<!-- Drawer + animator redirect stubs (replaces old SKILL.md body entirely) -->

Drawer redirect stub (skills/movie-experts/drawer/SKILL.md):
```yaml
---
name: drawer
description: "DEPRECATED — merged into visual_executor (Phase 14 MERGE-01). See ../visual_executor/SKILL.md"
version: 1.1.0
metadata:
  hermes:
    expert_id: drawer
    aliases: [visual_executor]
    status: merged_into
    merged_into: visual_executor
---

# Drawer Expert (绘图专家) — MERGED INTO visual_executor

This expert has been merged into `visual_executor` as the `drawer` sub-step (per Phase 7 §4.8 + PITFALLS §2.1: consistency context unified).
See [`../visual_executor/SKILL.md`](../visual_executor/SKILL.md).
Backward-compat alias `drawer` preserved in `metadata.hermes.aliases` of visual_executor per FOUND-08.
```

Animator redirect stub (skills/movie-experts/animator/SKILL.md):
```yaml
---
name: animator
description: "DEPRECATED — merged into visual_executor (Phase 14 MERGE-01). See ../visual_executor/SKILL.md"
version: 1.1.0
metadata:
  hermes:
    expert_id: animator
    aliases: [visual_executor]
    status: merged_into
    merged_into: visual_executor
---

# Animator Expert (视频专家) — MERGED INTO visual_executor

This expert has been merged into `visual_executor` as the `animator` sub-step (per Phase 7 §4.8 + PITFALLS §2.1: consistency context unified).
See [`../visual_executor/SKILL.md`](../visual_executor/SKILL.md).
Backward-compat alias `animator` preserved in `metadata.hermes.aliases` of visual_executor per FOUND-08.
```

<!-- Union computation for related_skills (dedup + drop self-refs) -->

drawer.related_skills = [screenplay, continuity_auditor, colorist, animator, style_genome, compliance_gate, cinematographer, production]
animator.related_skills = [drawer, scene_builder, editor, performer, colorist, continuity_auditor, cinematographer, production]

Union (dedup, drop drawer/animator self-references, drop visual_executor which is self):
= [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual_executor/ directory with merged SKILL.md + refs sub-folders + consolidated GAP-REPORT.md</name>
  <files>
    skills/movie-experts/visual_executor/SKILL.md,
    skills/movie-experts/visual_executor/references/drawer/LICENSE.md,
    skills/movie-experts/visual_executor/references/drawer/flux2-parameter-surface.md,
    skills/movie-experts/visual_executor/references/drawer/character-consistency-lora.md,
    skills/movie-experts/visual_executor/references/animator/LICENSE.md,
    skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md,
    skills/movie-experts/visual_executor/references/animator/temporal-consistency.md,
    skills/movie-experts/visual_executor/references/animator/camera-execution-and-degradation.md,
    skills/movie-experts/visual_executor/GAP-REPORT.md
  </files>
  <read_first>
    - skills/movie-experts/drawer/SKILL.md (1.1.0 — body to be split into Sub-step: Drawer section)
    - skills/movie-experts/animator/SKILL.md (1.1.0 — body to be split into Sub-step: Animator section)
    - skills/movie-experts/drawer/references/flux2-parameter-surface.md (verbatim copy)
    - skills/movie-experts/drawer/references/character-consistency-lora.md (verbatim copy)
    - skills/movie-experts/drawer/references/LICENSE.md (verbatim copy)
    - skills/movie-experts/animator/references/video-gen-model-matrix.md (verbatim copy)
    - skills/movie-experts/animator/references/temporal-consistency.md (verbatim copy)
    - skills/movie-experts/animator/references/camera-execution-and-degradation.md (verbatim copy)
    - skills/movie-experts/animator/references/LICENSE.md (verbatim copy)
    - skills/movie-experts/drawer/GAP-REPORT.md
    - skills/movie-experts/animator/GAP-REPORT.md
    - .planning/phases/14-visual-executor-merge/14-CONTEXT.md (refs sub-folder strategy + body subsection strategy)
    - .planning/research/v2-pipeline-design/skills-mapping.yaml lines 85-89 (visual_executor mapping + merge_rationale)
    - .planning/phases/13-expert-rename-alias-scaffolding/13-01-SUMMARY.md (precedent for sub-folder + aliases pattern)
    - CLAUDE.md §"Skill File Conventions (SKILL.md)" (frontmatter schema — sub_steps is a NEW top-level field)
  </read_first>
  <behavior>
    - visual_executor/SKILL.md frontmatter has `name: visual_executor` and `expert_id: visual_executor`
    - visual_executor/SKILL.md frontmatter declares top-level `sub_steps: [drawer, animator]` (NOT nested inside metadata.hermes — it is a new frontmatter field at the same level as `metadata`)
    - visual_executor/SKILL.md frontmatter declares `metadata.hermes.aliases: [drawer, animator]` (new field per FOUND-08)
    - `metadata.hermes.related_skills` = exactly [screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer] — union of drawer + animator related_skills with drawer/animator self-references removed and duplicates deduped
    - `metadata.hermes.tags` = union of drawer.tags + animator.tags, deduped
    - `metadata.hermes.metrics` = union of drawer.metrics + animator.metrics, deduped
    - `version: 1.0.0` (new expert; starts at 1.0.0 — NOT inherited from predecessors)
    - `prerequisites.tools` = union of drawer (hermes_llm, hermes_llm_vision) + animator (hermes_llm) = [hermes_llm, hermes_llm_vision]
    - Body of visual_executor/SKILL.md is structured as:
      1. H1 `# Visual Executor Expert (视觉执行专家)` + 1-paragraph intro stating unified visual + temporal consistency role + sub_steps declaration in prose
      2. `## When to use this skill` (merged — describe both stills and video use cases)
      3. `## Sub-steps` (NEW section, brief — names the two sub-steps and when each is invoked)
      4. `## References` table listing all 5 refs (2 drawer + 3 animator) with paths to `./references/drawer/*` and `./references/animator/*`
      5. `## Knowledge Retrieval` (merged — 4 query topics, 2 drawer + 2 animator; tag prefixes updated to `expert:visual_executor,sub:drawer,*` and `expert:visual_executor,sub:animator,*`)
      6. `## Sub-step: Drawer (Image Generation)` — H2 containing the body of drawer/SKILL.md from `## Role & Philosophy` through `## What NOT to do`, with internal ref markdown links updated to `./references/drawer/*.md`. REMOVE the old `## References` and `## Knowledge Retrieval` from inside this section (they live at the top of the merged file).
      7. `## Sub-step: Animator (Video Generation)` — H2 containing the body of animator/SKILL.md from `## Role & Philosophy` through `## What NOT to do`, with internal ref markdown links updated to `./references/animator/*.md`. REMOVE the old `## References` and `## Knowledge Retrieval` from inside this section.
      8. `## Collaboration` (merged — union of drawer + animator Collaboration bullet lists; rewrite the drawer→animator and animator←drawer bullets as intra-expert handoff notes within the Sub-steps section instead of inter-expert edges)
      9. `## Changelog` section at bottom documenting the merge: date, predecessors, rationale (cite Phase 7 §4.8 + PITFALLS §2.1), aliases declared
    - All 7 ref files copied verbatim (no edits to content)
    - GAP-REPORT.md is a consolidation: H1 `# Visual Executor GAP-REPORT`, then `## Drawer GAP-REPORT (migrated)` section containing verbatim copy of drawer/GAP-REPORT.md body, then `## Animator GAP-REPORT (migrated)` section containing verbatim copy of animator/GAP-REPORT.md body
    - Internal ref markdown links inside drawer-side prose change from `./references/flux2-parameter-surface.md` → `./references/drawer/flux2-parameter-surface.md` and `./references/character-consistency-lora.md` → `./references/drawer/character-consistency-lora.md`
    - Internal ref markdown links inside animator-side prose change from `./references/video-gen-model-matrix.md` → `./references/animator/video-gen-model-matrix.md`, `./references/temporal-consistency.md` → `./references/animator/temporal-consistency.md`, and `./references/camera-execution-and-degradation.md` → `./references/animator/camera-execution-and-degradation.md`
    - RAG query tags inside body change from `tags="expert:drawer,..."` → `tags="expert:visual_executor,sub:drawer,..."` and `tags="expert:animator,..."` → `tags="expert:visual_executor,sub:animator,..."`
    - The Collaboration section in drawer-side body that references `-> animator` is rewritten as: "→ [internal handoff to Animator sub-step — see §Sub-step: Animator below]"
    - The Collaboration section in animator-side body that references `<- drawer` is rewritten as: "← [internal handoff from Drawer sub-step — see §Sub-step: Drawer above]"
    - Cross-references to OTHER experts (cinematographer, colorist, continuity_auditor, editor, scene_builder, performer, production, screenplay, style_genome, compliance_gate) in the Collaboration sections are preserved as inter-expert edges (they remain external to visual_executor)
  </behavior>
  <action>
    1. Create directory tree:
       - `skills/movie-experts/visual_executor/`
       - `skills/movie-experts/visual_executor/references/drawer/`
       - `skills/movie-experts/visual_executor/references/animator/`
    2. **Migrate drawer refs (verbatim copy, no content edits):**
       - `drawer/references/LICENSE.md` → `visual_executor/references/drawer/LICENSE.md`
       - `drawer/references/flux2-parameter-surface.md` → `visual_executor/references/drawer/flux2-parameter-surface.md`
       - `drawer/references/character-consistency-lora.md` → `visual_executor/references/drawer/character-consistency-lora.md`
    3. **Migrate animator refs (verbatim copy, no content edits):**
       - `animator/references/LICENSE.md` → `visual_executor/references/animator/LICENSE.md`
       - `animator/references/video-gen-model-matrix.md` → `visual_executor/references/animator/video-gen-model-matrix.md`
       - `animator/references/temporal-consistency.md` → `visual_executor/references/animator/temporal-consistency.md`
       - `animator/references/camera-execution-and-degradation.md` → `visual_executor/references/animator/camera-execution-and-degradation.md`
    4. **Create `visual_executor/GAP-REPORT.md`** with two sections (`## Drawer GAP-REPORT (migrated)` + `## Animator GAP-REPORT (migrated)`), each containing the verbatim body of the corresponding old GAP-REPORT.md.
    5. **Create `visual_executor/SKILL.md`** following the structure in `<behavior>` above:
       a. Frontmatter: use the exact YAML in `<interfaces>` (name, description, version: 1.0.0, sub_steps: [drawer, animator], metadata.hermes with unioned tags + related_skills + metrics, expert_id: visual_executor, aliases: [drawer, animator])
       b. Body: H1 + intro + When to use + Sub-steps + References table + Knowledge Retrieval + two `## Sub-step:` H2 sections (drawer body then animator body, each starting at `Role & Philosophy` through `What NOT to do`) + merged Collaboration + Changelog
       c. Inside the Sub-step: Drawer body, update ref markdown links from `./references/X.md` → `./references/drawer/X.md` and RAG tags from `expert:drawer` → `expert:visual_executor,sub:drawer`
       d. Inside the Sub-step: Animator body, update ref markdown links from `./references/X.md` → `./references/animator/X.md` and RAG tags from `expert:animator` → `expert:visual_executor,sub:animator`
       e. Inside the drawer-side Collaboration section, rewrite the `- **-> animator**` bullet as an internal-handoff note pointing to the Animator sub-step section. Leave all OTHER Collaboration bullets (cinematographer, colorist, continuity_auditor, etc.) as-is.
       f. Inside the animator-side Collaboration section, rewrite the `- **<- drawer**` bullet as an internal-handoff note pointing to the Drawer sub-step section. Leave all OTHER Collaboration bullets as-is.
       g. Append `## Changelog` at the bottom with the merge record (date 2026-06-17, predecessors drawer + animator, rationale citing Phase 7 §4.8 + PITFALLS §2.1, aliases declared per FOUND-08).
  </action>
  <verify>
    <automated>
    echo "--- visual_executor directory tree ---" && \
    test -f skills/movie-experts/visual_executor/SKILL.md && \
    test -f skills/movie-experts/visual_executor/GAP-REPORT.md && \
    test -f skills/movie-experts/visual_executor/references/drawer/LICENSE.md && \
    test -f skills/movie-experts/visual_executor/references/drawer/flux2-parameter-surface.md && \
    test -f skills/movie-experts/visual_executor/references/drawer/character-consistency-lora.md && \
    test -f skills/movie-experts/visual_executor/references/animator/LICENSE.md && \
    test -f skills/movie-experts/visual_executor/references/animator/video-gen-model-matrix.md && \
    test -f skills/movie-experts/visual_executor/references/animator/temporal-consistency.md && \
    test -f skills/movie-experts/visual_executor/references/animator/camera-execution-and-degradation.md && \
    echo "--- Frontmatter required fields ---" && \
    grep -q "^name: visual_executor$" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "^sub_steps: \[drawer, animator\]$" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "expert_id: visual_executor" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "aliases: \[drawer, animator\]" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "^version: 1.0.0$" skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- related_skills union (must match spec exactly) ---" && \
    grep -q "related_skills: \[screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer\]" skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- Sub-step sections present ---" && \
    grep -q "^## Sub-step: Drawer (Image Generation)" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "^## Sub-step: Animator (Video Generation)" skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- Internal ref markdown links updated ---" && \
    grep -q "(./references/drawer/flux2-parameter-surface.md)" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "(./references/drawer/character-consistency-lora.md)" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "(./references/animator/video-gen-model-matrix.md)" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "(./references/animator/temporal-consistency.md)" skills/movie-experts/visual_executor/SKILL.md && \
    grep -q "(./references/animator/camera-execution-and-degradation.md)" skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- No stale self-reference to drawer/animator in related_skills ---" && \
    ! grep -E "related_skills:.*\b(drawer|animator)\b" skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- RAG tag prefixes migrated ---" && \
    ! grep -q 'tags="expert:drawer,' skills/movie-experts/visual_executor/SKILL.md && \
    ! grep -q 'tags="expert:animator,' skills/movie-experts/visual_executor/SKILL.md && \
    grep -q 'tags="expert:visual_executor,sub:drawer,' skills/movie-experts/visual_executor/SKILL.md && \
    grep -q 'tags="expert:visual_executor,sub:animator,' skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- Changelog section present ---" && \
    grep -q "^## Changelog" skills/movie-experts/visual_executor/SKILL.md && \
    echo "--- GAP-REPORT consolidation ---" && \
    grep -q "^## Drawer GAP-REPORT (migrated)" skills/movie-experts/visual_executor/GAP-REPORT.md && \
    grep -q "^## Animator GAP-REPORT (migrated)" skills/movie-experts/visual_executor/GAP-REPORT.md && \
    echo "TASK-1 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 9 files created under visual_executor/ (SKILL.md + 7 ref files in 2 sub-folders + GAP-REPORT.md)
    - SKILL.md frontmatter: name=visual_executor, version=1.0.0, sub_steps=[drawer, animator] at top level, expert_id=visual_executor, aliases=[drawer, animator]
    - related_skills array exactly matches the union spec (10 IDs, no drawer/animator self-refs, no duplicates)
    - tags + metrics arrays are unioned and deduped
    - Body contains two clearly-marked `## Sub-step:` H2 sections
    - All internal ref markdown links point to `./references/drawer/*.md` or `./references/animator/*.md` (not bare `./references/*.md`)
    - All RAG query tags use `expert:visual_executor,sub:drawer|animator` prefix
    - Drawer→animator and animator←drawer Collaboration bullets rewritten as internal-handoff notes
    - Changelog section documents the merge with date, rationale, aliases
    - GAP-REPORT.md has two clearly-marked migrated sections
    - Ref files migrated verbatim (no content edits)
  </acceptance_criteria>
  <done>
    `skills/movie-experts/visual_executor/` is a complete, self-contained merged expert with sub_steps metadata, unioned related_skills, two distinct sub-step bodies, migrated refs in sub-folders, and consolidated GAP-REPORT. Old `drawer/` and `animator/` directories are still untouched at this point (Task 2 rewrites their SKILL.md to redirect stubs).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace old drawer/SKILL.md + animator/SKILL.md with redirect-only stubs</name>
  <files>
    skills/movie-experts/drawer/SKILL.md,
    skills/movie-experts/animator/SKILL.md
  </files>
  <read_first>
    - skills/movie-experts/drawer/SKILL.md (current body — will be overwritten with redirect stub; Task 1 already preserved body inside visual_executor/SKILL.md Sub-step: Drawer section)
    - skills/movie-experts/animator/SKILL.md (current body — will be overwritten with redirect stub; Task 1 already preserved body inside visual_executor/SKILL.md Sub-step: Animator section)
    - .planning/phases/14-visual-executor-merge/14-CONTEXT.md (merge pattern — status: merged_into + merged_into: visual_executor)
    - skills/movie-experts/continuity/SKILL.md (Phase 13 redirect stub precedent — same shape, different status field value)
    - CLAUDE.md §"Skill File Conventions (SKILL.md)" — status + merged_into are metadata.hermes fields introduced by v2.0 PRFP DAG merge convention
  </read_first>
  <behavior>
    - `skills/movie-experts/drawer/SKILL.md` is replaced entirely with a frontmatter-only redirect stub. Frontmatter retains `name: drawer` + `expert_id: drawer` (for backward-compat dispatch) but declares `status: merged_into` + `merged_into: visual_executor` + `aliases: [visual_executor]`. After frontmatter, exactly one paragraph: "This expert has been merged into `visual_executor` as the `drawer` sub-step (per Phase 7 §4.8 + PITFALLS §2.1: consistency context unified). See [`../visual_executor/SKILL.md`](../visual_executor/SKILL.md). Backward-compat alias `drawer` preserved in `metadata.hermes.aliases` of visual_executor per FOUND-08."
    - `skills/movie-experts/animator/SKILL.md` is replaced with the analogous stub (animator sub-step, same structure)
    - Old directories' `references/` and `GAP-REPORT.md` files are NOT modified (kept as-is for archival — only SKILL.md becomes a stub; this matches Phase 13's pattern for continuity/ and compliance_marketing/)
    - The stub description begins with "DEPRECATED — merged into visual_executor (Phase 14 MERGE-01)" to match Phase 13 stub phrasing convention
  </behavior>
  <action>
    1. **Overwrite** `skills/movie-experts/drawer/SKILL.md` with the drawer redirect stub shown in `<interfaces>` (frontmatter-only + one paragraph after frontmatter). Do NOT delete `drawer/references/` or `drawer/GAP-REPORT.md`.
    2. **Overwrite** `skills/movie-experts/animator/SKILL.md` with the animator redirect stub shown in `<interfaces>` (frontmatter-only + one paragraph after frontmatter). Do NOT delete `animator/references/` or `animator/GAP-REPORT.md`.
  </action>
  <verify>
    <automated>
    echo "--- drawer redirect stub ---" && \
    grep -q "^name: drawer$" skills/movie-experts/drawer/SKILL.md && \
    grep -q "expert_id: drawer" skills/movie-experts/drawer/SKILL.md && \
    grep -q "status: merged_into" skills/movie-experts/drawer/SKILL.md && \
    grep -q "merged_into: visual_executor" skills/movie-experts/drawer/SKILL.md && \
    grep -q "aliases: \[visual_executor\]" skills/movie-experts/drawer/SKILL.md && \
    grep -q "merged into \`visual_executor\`" skills/movie-experts/drawer/SKILL.md && \
    grep -q "(../visual_executor/SKILL.md)" skills/movie-experts/drawer/SKILL.md && \
    echo "--- animator redirect stub ---" && \
    grep -q "^name: animator$" skills/movie-experts/animator/SKILL.md && \
    grep -q "expert_id: animator" skills/movie-experts/animator/SKILL.md && \
    grep -q "status: merged_into" skills/movie-experts/animator/SKILL.md && \
    grep -q "merged_into: visual_executor" skills/movie-experts/animator/SKILL.md && \
    grep -q "aliases: \[visual_executor\]" skills/movie-experts/animator/SKILL.md && \
    grep -q "merged into \`visual_executor\`" skills/movie-experts/animator/SKILL.md && \
    grep -q "(../visual_executor/SKILL.md)" skills/movie-experts/animator/SKILL.md && \
    echo "--- Old references/ + GAP-REPORT.md preserved untouched ---" && \
    test -d skills/movie-experts/drawer/references && \
    test -d skills/movie-experts/animator/references && \
    test -f skills/movie-experts/drawer/GAP-REPORT.md && \
    test -f skills/movie-experts/animator/GAP-REPORT.md && \
    echo "--- Stub is short (no body content beyond redirect) ---" && \
    test $(wc -l < skills/movie-experts/drawer/SKILL.md) -le 20 && \
    test $(wc -l < skills/movie-experts/animator/SKILL.md) -le 20 && \
    echo "TASK-2 OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - skills/movie-experts/drawer/SKILL.md is a frontmatter-only redirect stub (≤ 20 lines)
    - skills/movie-experts/animator/SKILL.md is a frontmatter-only redirect stub (≤ 20 lines)
    - Both stubs declare `status: merged_into` + `merged_into: visual_executor` in metadata.hermes
    - Both stubs retain `name:` + `expert_id:` of the OLD ID (drawer / animator) for backward-compat dispatch
    - Both stubs declare `aliases: [visual_executor]` (forward alias)
    - Both stubs contain exactly one paragraph after frontmatter with a working markdown link to `../visual_executor/SKILL.md`
    - Old `drawer/references/`, `drawer/GAP-REPORT.md`, `animator/references/`, `animator/GAP-REPORT.md` are preserved untouched
  </acceptance_criteria>
  <done>
    Old `drawer/` and `animator/` directories are now redirect stubs. The merge is structurally complete at the source-expert level. Task 1 (new visual_executor) + Task 2 (redirect stubs) together satisfy ROADMAP §14 success criteria 1, 2, 3. Consumer edge sync (criterion 4) is the job of plan 14-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing skill consumers → merged expert | If a consumer SKILL.md still references old `drawer` or `animator` after this plan, downstream agent invocation may fail to find the expert (no aliased dispatch in v1 loader) — this plan creates the alias metadata; plan 14-02 performs the consumer edge sync |
| Ref file migration | 7 ref files copied verbatim — corruption risk if path mapping is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | SKILL.md frontmatter `sub_steps` field (NEW field per v2.0 PRFP DAG) | mitigate | New SKILL.md explicitly declares `sub_steps: [drawer, animator]` at top-level frontmatter — verified by grep `^sub_steps: \[drawer, animator\]$` |
| T-14-02 | Repudiation | Backward-compat alias declaration (FOUND-08) | mitigate | visual_executor declares `aliases: [drawer, animator]`; both redirect stubs declare `aliases: [visual_executor]` — verified by grep |
| T-14-03 | Information disclosure | Old directory paths backward compat | mitigate | Old drawer/SKILL.md + animator/SKILL.md replaced with redirect stubs (not deleted); historical references in transcripts resolve via redirect |
| T-14-04 | Tampering | Union of related_skills (must be exact, deduped, no self-refs) | mitigate | Task 1 grep verifies the related_skills array matches the spec exactly; manual union computation documented in `<interfaces>` |
| T-14-05 | Denial of service | Ref file migration (7 files, verbatim copy) | accept | Refs copied verbatim; no semantic edits; low risk of corruption (verified by file existence check) |

No third-party package installs in this plan — T-{phase}-SC (Tampering of npm/pip/cargo) is N/A.
</threat_model>

<verification>
After both tasks complete, run the consolidated checks:

```bash
# 1. New merged expert exists with required frontmatter
test -f skills/movie-experts/visual_executor/SKILL.md
grep -q "^name: visual_executor$" skills/movie-experts/visual_executor/SKILL.md
grep -q "^sub_steps: \[drawer, animator\]$" skills/movie-experts/visual_executor/SKILL.md
grep -q "expert_id: visual_executor" skills/movie-experts/visual_executor/SKILL.md
grep -q "aliases: \[drawer, animator\]" skills/movie-experts/visual_executor/SKILL.md

# 2. related_skills is the exact union
grep -q "related_skills: \[screenplay, continuity_auditor, colorist, style_genome, compliance_gate, cinematographer, production, scene_builder, editor, performer\]" skills/movie-experts/visual_executor/SKILL.md

# 3. Refs migrated to sub-folders
ls skills/movie-experts/visual_executor/references/drawer/
ls skills/movie-experts/visual_executor/references/animator/

# 4. Old directories are redirect stubs
grep -q "status: merged_into" skills/movie-experts/drawer/SKILL.md
grep -q "merged_into: visual_executor" skills/movie-experts/drawer/SKILL.md
grep -q "status: merged_into" skills/movie-experts/animator/SKILL.md
grep -q "merged_into: visual_executor" skills/movie-experts/animator/SKILL.md

# 5. Old dir references/ + GAP-REPORT.md preserved
test -d skills/movie-experts/drawer/references
test -f skills/movie-experts/drawer/GAP-REPORT.md
test -d skills/movie-experts/animator/references
test -f skills/movie-experts/animator/GAP-REPORT.md
```
</verification>

<success_criteria>
- [ ] `skills/movie-experts/visual_executor/SKILL.md` exists with `sub_steps: [drawer, animator]` declared at top level
- [ ] `metadata.hermes.related_skills` is the exact 10-item union (no drawer/animator self-refs, no dupes)
- [ ] `metadata.hermes.aliases: [drawer, animator]` declared
- [ ] `metadata.hermes.expert_id: visual_executor` declared
- [ ] `version: 1.0.0` (new expert baseline)
- [ ] Body has two clearly-marked `## Sub-step: Drawer (Image Generation)` and `## Sub-step: Animator (Video Generation)` H2 sections
- [ ] All 7 ref files migrated under `references/{drawer,animator}/` sub-folders (verbatim)
- [ ] `GAP-REPORT.md` consolidates both predecessors
- [ ] All internal ref markdown links updated to sub-folder paths
- [ ] All RAG query tags use `expert:visual_executor,sub:{drawer|animator}` prefix
- [ ] Drawer→animator + animator←drawer Collaboration bullets rewritten as internal-handoff notes
- [ ] Changelog section appended
- [ ] Old `drawer/SKILL.md` is a redirect stub (frontmatter-only + 1 paragraph, `status: merged_into` + `merged_into: visual_executor`)
- [ ] Old `animator/SKILL.md` is a redirect stub (same shape)
- [ ] Old `drawer/references/`, `drawer/GAP-REPORT.md`, `animator/references/`, `animator/GAP-REPORT.md` preserved untouched
</success_criteria>

<output>
Create `.planning/phases/14-visual-executor-merge/14-01-SUMMARY.md` when done
</output>

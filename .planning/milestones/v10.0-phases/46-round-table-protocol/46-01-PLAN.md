---
phase: 46-round-table-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md
  - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml
autonomous: true
requirements: [DESIGN-03]
granularity: standard

must_haves:
  truths:
    - "File .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md exists"
    - "File .planning/research/v10-orchestrator-design/round-table-state-schema.yaml exists as valid JSON Schema (draft 2020-12) with camelCase keywords"
    - "Turn lifecycle is fully defined: round_table_open → turn N → submit_round_table_result, atomic per turn"
    - "turn_order strategy is documented (default round-robin + pluggable; resolves OQ-2)"
    - "round_id CC-self-generated UUID mechanism is documented (resolves OQ-11)"
    - "Memory conflict arbitration is documented: comparator LLM pass + scope precedence (session > project > global) + confidence-weighted voting + conflict log (resolves OQ-15, avoids P7)"
    - "Serial execution constraint is explicitly declared (1 panelist 1 turn sequential await, citing MEMORY.md feedback-glm-overload-reduce-concurrency.md global concurrency==1 policy, resolves OQ-8)"
    - "MCP tool naming adopts STACK §3.2 form (no agent_ prefix, aligns with existing 9 messaging tools, resolves OQ-9)"
    - "FEATURES §10 borrowable points B1.4 / B2.1 / B2.3 / B4.2 / B6.1 / B7.3 / B8.2 each explicitly cited in the narrative"
  artifacts:
    - path: ".planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md"
      provides: "v10.0 design doc #02 — round table protocol narrative covering turn lifecycle (round_table_open/turn/submit_round_table_result) + turn_order strategy + memory conflict arbitration + serial constraint + MCP tool contract + 7 borrowable points + OQ/pitfall audit. Consumes Phase 45 agents-schema.yaml + memory-record-schema.yaml fields, does NOT redefine them."
      min_lines: 800
      contains: ["round_table_open", "submit_round_table_result", "turn_order", "round-robin", "comparator", "scope precedence", "session > project > global", "confidence-weighted", "serial", "await", "concurrency==1", "feedback-glm-overload-reduce-concurrency", "STACK §3.2", "B1.4", "B2.1", "B2.3", "B4.2", "B6.1", "B7.3", "B8.2", "P7", "OQ-2", "OQ-8", "OQ-9", "OQ-11", "OQ-15"]
    - path: ".planning/research/v10-orchestrator-design/round-table-state-schema.yaml"
      provides: "Formal JSON Schema (draft 2020-12, camelCase keywords) for round-table state file at ~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json. Covers roundId / status / turnOrder / turns / conflicts / earlyStopRule / panelist snapshots. Consumable by v11.0 PoC Python dataclass generator + Phase 51 lint script."
      min_lines: 200
      contains: ["$schema", "$id", "title", "type", "properties", "required", "additionalProperties", "roundId", "turnOrder", "panelists", "turns", "conflicts", "roundTableOpen", "submitRoundTableResult"]
  key_links:
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "00-FIRST-PRINCIPLES.md §2.5/§2.6/§2.7 (decisions 5/6/7)"
      via: "markdown §reference + 决策号 citation"
      pattern: "00-FIRST-PRINCIPLES|决策 [567]|§2\\.[567]"
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "01-AGENT-REGISTRY-SCHEMA.md §2 (18-field) + §3 (memory-record)"
      via: "field name reference (memory_scope, scope, confidence, evidence_chain, confidentiality) — cite, do NOT redefine"
      pattern: "01-AGENT-REGISTRY-SCHEMA|memory_scope|scope|confidence|evidence_chain|confidentiality"
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "STACK.md §3.2 (MCP tool schema) + §4 (transport) + §7 (token cost)"
      via: "markdown §reference"
      pattern: "STACK §(3\\.2|4|7)"
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "ARCHITECTURE.md §5 (round table state layer) + §8 (anti-patterns)"
      via: "markdown §reference"
      pattern: "ARCHITECTURE §(5|8)"
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "PITFALLS.md §Pitfall 7 (memory conflict)"
      via: "markdown §reference for arbitration mechanism"
      pattern: "PITFALLS §Pitfall 7|P7"
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "FEATURES.md §10 (B1.4 / B2.1 / B2.3 / B4.2 / B6.1 / B7.3 / B8.2)"
      via: "markdown §reference for 7 borrowable points"
      pattern: "FEATURES §10|B1\\.4|B2\\.1|B2\\.3|B4\\.2|B6\\.1|B7\\.3|B8\\.2"
    - from: "02-ROUND-TABLE-PROTOCOL.md"
      to: "~/.claude/projects/-data-workspace-hermes-agent/memory/feedback-glm-overload-reduce-concurrency.md (global concurrency==1 policy)"
      via: "MEMORY.md §reference for serial constraint root cause"
      pattern: "feedback-glm-overload-reduce-concurrency|concurrency==1|global concurrency"
    - from: "round-table-state-schema.yaml"
      to: "02-ROUND-TABLE-PROTOCOL.md §2 (turn lifecycle narrative)"
      via: "field name alignment with camelCase convention"
      pattern: "roundId|turnOrder|turns|conflicts"
---

<objective>
Create the v10.0 design doc #02 — the **round table protocol physical carrier** by producing 2 deliverables:

1. `02-ROUND-TABLE-PROTOCOL.md` — design narrative (~800-1200 lines, bilingual EN structure + 中文 prose per CLAUDE.md)
2. `round-table-state-schema.yaml` — formal JSON Schema (draft 2020-12, camelCase keywords) for the round table state file

Purpose: Lock the **turn lifecycle** (round_table_open → turn N → submit_round_table_result, atomic), the **memory conflict arbitration rules** (comparator LLM pass + scope precedence + confidence-weighted voting + conflict log, resolving OQ-15 and avoiding P7), and the **serial execution constraint** (1 panelist 1 turn sequential await, resolving OQ-8 and citing the GLM concurrency==1 policy). The next design docs (04-MIGRATION-PATH, 05-POC-PLAN) will cite this protocol's lifecycle and conflict rules directly rather than re-deriving them.

Output: 2 sibling files under `.planning/research/v10-orchestrator-design/`, mirroring Phase 44's `00-FIRST-PRINCIPLES.md` + Phase 45's `01-AGENT-REGISTRY-SCHEMA.md` depth/rigor at comparable scope (Phase 45 was 800+ lines narrative + 2 schema YAMLs; Phase 46 target 800-1200 lines narrative + 1 schema YAML).

This is a DESIGN-ONLY deliverable. Zero code changes (no SKILL.md / Python / plugin / mcp_serve.py edits). Cite Phase 44's locked decisions (5/6/7) and Phase 45's locked schema fields — do NOT re-derive them. Cite, do NOT redefine: `memory_scope`, `scope`, `confidence`, `evidence_chain`, `confidentiality`, `fitness_score`, `persona_sha256` are all Phase 45 fields consumed here by reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
@.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md
@.planning/research/v10-orchestrator-design/agents-schema.yaml
@.planning/research/v10-orchestrator-design/memory-record-schema.yaml
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md
@.planning/research/v10-orchestrator-design/STACK.md
@.planning/research/v10-orchestrator-design/FEATURES.md

<interfaces>
<!-- Phase 44 root-argument anchors (do NOT re-derive; cite by 决策号) -->
<!-- Decision 5 (α agent form) — agents are Hermes-side YAML entities, not skills -->
<!-- Decision 6 (per-agent memory + curator-driven self-evolution) — memory has scope/confidence/evidence_chain -->
<!-- Decision 7 (Hermes controls structure, CC controls content) — Hermes controls turn_order/max_rounds/early_stop_rule, CC controls question framing/synthesis -->
<!-- These three decisions are the load-bearing inputs to the round table protocol. -->

From .planning/research/v10-orchestrator-design/SUMMARY.md (CC-1 cross-cutting finding):
> STACK §3.2 uses `get_agent_persona` / `get_agent_memory` / `query_memory` (no prefix), ARCHITECTURE §4.2 uses `agent_get_persona` / `agent_recall` / `agent_conclude` (agent_ prefix). CONFLICT — downstream `02-ROUND-TABLE-PROTOCOL.md` MUST unify naming first. Recommendation: STACK form (no prefix, aligns with existing `mcp_serve.py` 9 messaging tools style).

From .planning/research/v10-orchestrator-design/SUMMARY.md (CC-6 cross-cutting finding):
> STACK estimates ~550K tokens per pipeline run assuming serial. PITFALLS says per-agent memory ≤500 records. FEATURES §14 gap 6 flags that GLM 4-key rotation × multi-panelist concurrency cost is unaccounted for. Combined: **round table MUST be serial (1 panelist 1 turn, no concurrent LLM calls)**, else GLM 4-key rotation will immediately hit ceiling on 7 panelists × N rounds in parallel. Hard constraint for `02-ROUND-TABLE-PROTOCOL.md`. MEMORY.md `feedback-glm-overload-reduce-concurrency.md` reinforces: global concurrency==1 policy is BY DESIGN (5→3→1 deprecation cycle).

From .planning/research/v10-orchestrator-design/SUMMARY.md (Open Questions to resolve in this phase):
- OQ-2: round table turn_order strategy → **default round-robin**, pluggable via round_table_open arg; fitness-weighted deferred to v11.1+
- OQ-5: agent deletion → **open-time identity snapshot**, deleted agent in transcript shows "deleted" badge
- OQ-8: GLM 4-key rotation × concurrency → **strict serial**, 1 panelist 1 turn sequential await
- OQ-9: MCP tool naming → **STACK form** (no prefix, aligns with existing 9 messaging tools)
- OQ-11: round_id source → **CC self-generates UUID**, passed in round_table_open call
- OQ-15: round-table conflict arbitration → **comparator LLM pass + scope precedence (session > project > global) + confidence-weighted voting + conflict log**

From ARCHITECTURE.md §5 (round table state layer):
> Round table state persists at `~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json`. Per-project: each project has its own questions, panel configs, synthesis outcomes.

From ARCHITECTURE.md §4.2 (MCP tool contract — for naming reconciliation):
> 7 tools: get_agent_persona / get_agent_opinion / get_agent_memory / submit_round_table_result / submit_artifact / query_memory / run_python_phase. (STACK §3.2 form — ADOPTED as canonical here.)

From ARCHITECTURE.md §8 (anti-patterns — DO NOT):
> - YAML as prompt dump (persona is system-prompt fragment, not SKILL body copy)
> - auto-re-transform on drift (operator owns transform cadence)
> - round table as pipeline step (round_table_open returns immediately, CC polls — NOT synchronous block)
> - per-agent mem0 instance (Option B filter routing at v11.0 PoC)
> - cross-layer import (lazy _ra() indirection preserved)

From STACK.md §3.2 (canonical MCP tool spec — 7 tools, no prefix):
- Tool 1: `get_agent_persona(agent_id)` → returns AgentPersonaResult (persona YAML content)
- Tool 2: `get_agent_opinion(agent_id, topic, context, prior_discussion)` → returns opinion text + cited memory_ids
- Tool 3: `get_agent_memory(agent_id, query, project_id, confidentiality_filter)` → returns MemoryHit[] (citing record_id, scope, confidence, evidence_chain)
- Tool 4: `submit_round_table_result(round_id, conclusion, cited_memories, artifacts, conflicts)` → returns receipt_id
- Tool 5: `submit_artifact(round_id, artifact_type, content, provenance)` → returns artifact_id
- Tool 6: `query_memory(query, agent_id?, scope?, project_id?)` → returns MemoryHit[] (generic, non agent-scoped)
- Tool 7: `run_python_phase(phase_name, inputs)` → returns phase output (B3a boundary tool)

From PITFALLS.md §Pitfall 7 (Round-Table Memory Conflict — the load-bearing pitfall for this phase):
> Without explicit conflict resolution, the debate is contaminated — the loudest-memory agent wins by sheer volume of citations rather than correctness.
> Mitigations: (1) memory annotation in round-table turns; (2) coordinator arbitrates conflicts via comparator LLM pass; (3) scope precedence session > project > global; (4) confidence-weighted voting; (5) conflict log for curator review.

From FEATURES.md §10 (borrowable design points — 7 to address):
- B1.4 (LangGraph hierarchical supervisor) → round table nesting
- B2.1 (AutoGen/MAF turn_order three-state) → llm / fixed / matrix
- B2.3 (AutoGen nested chat) → panelist convenes sub-round table
- B4.2 (Claude Agent SDK hooks) → PreTurn/PostTurn audit hooks
- B6.1 (Camel-AI generator+critics) → panelist_role field
- B7.3 (Agent-MCP file-level lock) → multi-agent parallel asset_locks
- B8.2 (A2A Task lifecycle FSM) → turn state machine

From Phase 45 schemas (CONSUME, do NOT redefine):
- agents-schema.yaml fields used here: `name`, `tools`, `memory_scope`, `round_table_eligible`, `default_invocation`, `fitness_score`, `related_agents`
- memory-record-schema.yaml fields used here: `record_id`, `agent_id`, `scope` (global|project|session), `project_id`, `confidence`, `evidence_chain`, `evidence_operator_ids`, `status`, `confidentiality`, `persona_sha256`

From CLAUDE.md (deliverable form):
> SKILL.md 双语(英文 YAML 结构 + metadata 保留英文;description 与正文采用 EN structure + 中文段落/示例)
> JSON Schema with description, type, enum, required per industry standard
> camelCase JSON Schema keywords (lesson from Phase 45 verification — never snake_case in JSON Schema property names)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan + write §0 reading guide + §1 protocol overview (turn lifecycle skeleton + Phase 44/45 citation anchor)</name>
  <files>.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md lines 1-100 (frontmatter + §0 + §1.1-§1.3) — pattern for bilingual frontmatter + 阅读指南 + 稳定性 markers
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.5 (决策 5 α agent form) + §2.6 (决策 6 per-agent memory) + §2.7 (决策 7 Hermes 控结构 CC 控内容) — root-argument anchors to cite
    - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md §0 + §1 (pattern reference for how Phase 45 cited Phase 44 — apply same pattern here)
    - .planning/research/v10-orchestrator-design/SUMMARY.md CC-1 (lines 23-25 — MCP tool naming conflict resolution: STACK form wins) + CC-6 (lines 43-45 — serial constraint root cause)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 line 257-300 (7-tool schema — get_agent_persona / get_agent_opinion / get_agent_memory / submit_round_table_result / submit_artifact / query_memory / run_python_phase)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §5 lines 470-545 (round table state layer + lifecycle sketch)
  </read_first>

  <behavior>
  - Document frontmatter block (status / phase / audience / reading time / stability markers / confidence) mirrors 00-FIRST-PRINCIPLES.md + 01-AGENT-REGISTRY-SCHEMA.md style
  - §0 阅读指南 exists with chapter map, 3-audience guide (Kai reviewer / Kimi Notion 续聊 / v11.0 PoC implementer), 稳定性 markers, "what this doc consumes from Phase 44/45" preamble
  - §1.1 exists: protocol design philosophy — "round table 是 v10.0 的多 agent 协商层,消费 Phase 45 agent schema + memory-record schema,不重定义字段" + cite Phase 44 决策 5/6/7 as root arguments
  - §1.2 exists: turn lifecycle one-paragraph summary + ASCII diagram (round_table_open → turn 1 → turn 2 → ... → submit_round_table_result) with atomic-per-turn invariant marked
  - §1.3 exists: 5 SC mapping table (本 doc 章节 → ROADMAP SC#1-5) so verification can cross-check
  - §1.4 exists: roadmap placement — "本 doc 是 04-MIGRATION-PATH + 05-POC-PLAN 的协议契约物理载体" + "Phase 44 决策 5/6/7 LOCKED, Phase 45 schemas LOCKED — this doc only specifies the protocol"
  - §1.5 exists: 3 hard constraints declared upfront (serial execution / STACK-form MCP naming / atomic turn lifecycle) — each cites its OQ + MEMORY.md anchor
  - Total Task 1 length ≥ 200 lines
  </behavior>

  <action>
  Create file `.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md`. Write frontmatter + §0 + §1.

  For §1.1 (design philosophy, ~50 lines): Open with "本文档定义 v10.0 round table 协议 —— 消费 Phase 45 锁定的 agent YAML schema(18 字段)+ memory-record schema(10 字段),规定多 agent 协商的 turn lifecycle、memory 冲突仲裁规则、以及强制串行约束。" Cite Phase 44 §2.5 (决策 5 — agents are Hermes-side entities) + §2.6 (决策 6 — per-agent memory with scope/confidence) + §2.7 (决策 7 — Hermes controls turn_order/max_rounds/early_stop_rule, CC controls question framing/synthesis) as root arguments; do NOT re-derive. Mark each Phase 44 决策 anchor as `root-arg-决策-N` for downstream cross-doc citation. Explicitly enumerate the Phase 45 fields consumed (memory_scope, scope, confidence, evidence_chain, confidentiality, fitness_score, persona_sha256) — "CITE ONLY, do NOT redefine; authoritative definition lives in 01-AGENT-REGISTRY-SCHEMA.md §2/§3 + agents-schema.yaml + memory-record-schema.yaml".

  For §1.2 (turn lifecycle summary, ~50 lines): ASCII diagram:
  ```
  ┌────────────────────────────────────────────────────────────────────┐
  │ CC (Claude Code) side                                              │
  │                                                                    │
  │  1. CC self-generates round_id (UUID v4)         ← OQ-11 resolution│
  │  2. CC calls round_table_open(                    ← atomic start   │
  │       round_id, panel, question,                                   │
  │       max_rounds, turn_order_strategy,                             │
  │       early_stop_rule, project_id)                                 │
  │  3. For each turn i in 1..N (SERIAL await — OQ-8):                 │
  │       a. CC selects panelist per turn_order                        │
  │       b. CC calls get_agent_opinion(panelist, topic, prior)        │
  │       c. CC appends opinion to turns[i] in state file              │
  │       d. (optional) CC calls run comparator LLM on conflicts       │
  │  4. CC calls submit_round_table_result(           ← atomic commit  │
  │       round_id, conclusion, cited_memories,                        │
  │       artifacts, conflicts)                                        │
  │  5. Hermes persists result + writes conflict log                   │
  └────────────────────────────────────────────────────────────────────┘
  ```
  Below diagram: 1 paragraph citing decision 7 — "Hermes owns turn_order / max_rounds / early_stop_rule via schema; CC owns question framing, opinion synthesis, conflict detection. This is the structure/content split locked in Phase 44."

  For §1.3 (SC mapping table, ~30 lines): 5-row table mapping ROADMAP SC#1-5 → 本 doc 章节. Columns: | SC# | 描述 | 本 doc 解决章节 | 验证脚本 |

  For §1.4 (roadmap placement, ~30 lines): "本 doc 是 04-MIGRATION-PATH.md + 05-POC-PLAN.md 的协议契约物理载体。04 cites lifecycle for migration semantics; 05 cites conflict arbitration rules for PoC acceptance criteria. 03-COMPARISON-VS-KIMI-MCP-SHIM.md + 06-CROSS-REPO-IMPACT.md are parallel siblings, do NOT consume this doc's protocol detail (they cite Phase 44 decisions directly)." State Phase 44 决策 5/6/7 + Phase 45 schemas are LOCKED — this doc only specifies the protocol layer.

  For §1.5 (3 hard constraints, ~40 lines): Three H3 subsections declaring upfront:
  - §1.5.1 Serial execution (cites OQ-8 + MEMORY.md `feedback-glm-overload-reduce-concurrency.md` global concurrency==1 policy): "v10.0 round table 强制 1 panelist 1 turn sequential `await`,绝不允许并发 LLM 调用。Root cause: GLM 4-key rotation 在 7 panelist × N rounds 并发场景下会立刻撞 ceiling (~800K TPM)。MEMORY.md 已记录 global concurrency==1 是 BY DESIGN(5→3→1 演进),不可降。详见 §4.1。"
  - §1.5.2 STACK-form MCP naming (cites OQ-9 + SUMMARY.md CC-1): "所有 MCP tool 采用 STACK §3.2 形态(无 agent_ 前缀),与现有 mcp_serve.py 9 个 messaging tool 风格一致。`get_agent_persona` 而非 `agent_get_persona`。详见 §5。"
  - §1.5.3 Atomic turn lifecycle (cites ARCHITECTURE §8 anti-pattern): "round_table_open 立即返回 round_id,不等结束。submit_round_table_result 是原子提交点。中间状态持久化到 .runtime/{slug}/round_tables/{id}.json(引用 ARCHITECTURE §5.1)。详见 §2。"

  Length target: ~200-250 lines for Task 1.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 200 {exit 0} {exit 1}' && grep -q "## §0" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -q "## §1" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && for term in "决策 5" "决策 6" "决策 7" "CC-1" "CC-6" "memory-record" "STACK §3.2" "ARCHITECTURE §5" "round_table_open" "submit_round_table_result" "serial" "concurrency==1"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "01-AGENT-REGISTRY-SCHEMA" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 2 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - File created with bilingual frontmatter (status / phase / audience / reading time / stability / confidence)
  - §0 阅读指南 exists with chapter map + 3-audience guide + Phase 44/45 consumption preamble
  - §1.1 design philosophy cites Phase 44 决策 5/6/7 + lists Phase 45 fields consumed (cite-only)
  - §1.2 has turn lifecycle ASCII diagram with OQ-8/OQ-11 markers
  - §1.3 has SC#1-5 mapping table
  - §1.4 has roadmap placement + downstream consumer list
  - §1.5 has 3 hard constraints declared upfront (serial / STACK naming / atomic lifecycle)
  - Total doc length ≥ 200 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Write round-table-state-schema.yaml (camelCase JSON Schema draft 2020-12 for ~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json)</name>
  <files>.planning/research/v10-orchestrator-design/round-table-state-schema.yaml</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/agents-schema.yaml (Phase 45 schema — pattern reference for $schema/$id/title/description/type/required/additionalProperties/properties/$defs structure)
    - .planning/research/v10-orchestrator-design/memory-record-schema.yaml (Phase 45 memory schema — pattern reference + the 10 fields consumed by conflict arbitration)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §5.1 lines 470-545 (round table state file path + lifecycle sketch)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 Tool 4 lines 414-456 (submit_round_table_result signature — drives what fields the state file must hold)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 7 lines 235-265 (5 mitigations — memory annotation in turns, comparator arbitration, scope precedence, confidence voting, conflict log — each maps to a state file field)
  </read_first>

  <behavior>
  - round-table-state-schema.yaml exists as valid JSON Schema (draft 2020-12), machine-parseable YAML
  - Uses camelCase keywords throughout (lesson from Phase 45 verification: `$schema`/`$id`/`$defs` kebab-cased keys, but all property names camelCase: roundId / turnOrder / panelists / turns / conflicts / earlyStopRule / roundTableOpen / submitRoundTableResult / createdAt / lastUpdatedAt)
  - Has `$schema: "https://json-schema.org/draft/2020-12/schema"` header
  - Has `$id: "https://hermes-agent.local/v10/round-table-state-schema.yaml"`
  - Has `title`, `description` (bilingual allowed), `type: object`, `required`, `additionalProperties: false`, `properties`
  - Top-level required: [roundId, projectId, question, panelists, turnOrder, status, turns, roundTableOpen, createdAt]
  - Top-level optional: [submitRoundTableResult, earlyStopRule, maxRounds, conflicts, lastUpdatedAt, schemaVersion, personaSnapshots]
  - `roundId` field: type string, format uuid, description "CC self-generated UUID v4 (OQ-11 resolution); CC passes this in round_table_open call"
  - `turnOrder` field: type object with `strategy` enum [round-robin, fixed, llm, matrix, fitness-weighted] (default round-robin per OQ-2), `seed` array of panelist names (the pre-computed order; if strategy=llm or fitness-weighted, seed may be partial), `currentIndex` integer
  - `panelists` field: array of PanelistSnapshot objects (each captures agent identity + persona_sha256 + fitness_score AT ROUND-OPEN TIME — OQ-5 resolution: deleted agents keep their snapshot for transcript integrity)
  - `turns` field: array of Turn objects (turnIndex / panelistId / opinion / citedMemoryIds / conflictsDetected / submittedAt)
  - `conflicts` field: array of ConflictRecord objects (conflictId / memoryIdA / memoryIdB / scopeA / scopeB / confidenceA / confidenceB / resolution / rationale / resolvedAt — the conflict log per P7 mitigation 5)
  - `earlyStopRule` field: type object with `type` enum [consensus, max-rounds, manual, comparator-confident], `threshold` number (e.g. 0.85 for comparator-confident)
  - `roundTableOpen` field: type object capturing the open event (caller / openedAt / project / question)
  - `submitRoundTableResult` field: type object optional, captures the close event when status=completed (conclusion / citedMemories / artifacts / closedAt / closedBy)
  - `status` field: enum [open, completed, aborted, stalled], default open
  - `personaSnapshots` field: map of agentId → persona_sha256 at round-open time (P1 persona-drift detection: if agent persona changes mid-round, flag for re-verification)
  - `schemaVersion` field: type string default "1.0.0" (P14 mitigation)
  - Has `$defs` block with reusable shapes: PanelistSnapshot / Turn / ConflictRecord / CitedMemory
  - Each load-bearing field has YAML comment citing OQ-XX or PITFALLS §P7 mitigation N source
  </behavior>

  <action>
  Write `.planning/research/v10-orchestrator-design/round-table-state-schema.yaml`.

  Structure (camelCase keywords — Phase 45 verification lesson):
  ```yaml
  $schema: "https://json-schema.org/draft/2020-12/schema"
  $id: "https://hermes-agent.local/v10/round-table-state-schema.yaml"
  title: "Hermes Round Table State Schema (v10.0)"
  description: |
    State file schema for ~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json.
    One file per round table invocation; persists turn-by-turn state for crash recovery,
    conflict-log audit, and curator review. Source: ARCHITECTURE §5.1 + Phase 44 决策 7
    (Hermes controls structure). Consumes Phase 45 agents-schema + memory-record-schema
    field names — does NOT redefine them. See 02-ROUND-TABLE-PROTOCOL.md §2-§3 for narrative.
  type: object
  required:
    - roundId
    - projectId
    - question
    - panelists
    - turnOrder
    - status
    - turns
    - roundTableOpen
    - createdAt
  additionalProperties: false
  properties:
    roundId:
      type: string
      format: uuid
      description: |
        CC self-generated UUID v4.
        # OQ-11 resolution: CC generates round_id (not Hermes-injected) so CC retains
        #   orchestration authority over the round lifecycle.
        # Used as the state filename: {roundId}.json
    projectId:
      type: string
      description: |
        Project slug for the .runtime/{slug}/ path. Required because round table state
        is per-project (ARCHITECTURE §5.1).
    question:
      type: string
      description: "The question/topic being debated; CC frames it (decision 7: CC controls content)."
    panelists:
      type: array
      minItems: 2
      items:
        $ref: "#/$defs/PanelistSnapshot"
      description: |
        Snapshot of each panelist at round-open time. Captures identity + persona + fitness
        so that mid-round agent edits or deletions don't corrupt transcript integrity.
        # OQ-5 resolution: deleted agents appear in transcript with their open-time snapshot.
    turnOrder:
      $ref: "#/$defs/TurnOrder"
      description: |
        Pre-computed or progressively-filled turn order.
        # OQ-2 resolution: strategy defaults to round-robin; pluggable via round_table_open arg.
    status:
      type: string
      enum: [open, completed, aborted, stalled]
      default: open
      description: |
        open = round in progress; completed = submit_round_table_result received;
        aborted = operator/cc cancelled; stalled = no turn activity for >N minutes (PoC tunable).
        # B8.2 borrowable point: A2A task lifecycle FSM inspires this state machine.
    turns:
      type: array
      default: []
      items:
        $ref: "#/$defs/Turn"
      description: |
        Append-only turn log. Each turn is one atomic opinion submission.
        # Serial invariant (OQ-8): turns are submitted sequentially, never concurrently.
    conflicts:
      type: array
      default: []
      items:
        $ref: "#/$defs/ConflictRecord"
      description: |
        Conflict log: one entry per comparator-arbitrated conflict.
        # PITFALLS §P7 mitigation 5: curator reviews high-frequency conflicts for promotion/quarantine.
    earlyStopRule:
      $ref: "#/$defs/EarlyStopRule"
      description: |
        Optional early-stop condition. If met, CC calls submit_round_table_result early.
        # Decision 7: Hermes owns early_stop_rule via schema; CC owns detection.
    maxRounds:
      type: integer
      minimum: 1
      default: 5
      description: "Hard cap on turn count. Default 5 (the ReConcile paper's typical convergence point)."
    roundTableOpen:
      $ref: "#/$defs/RoundTableOpen"
      description: "Atomic open event (caller / openedAt / project / question)."
    submitRoundTableResult:
      $ref: "#/$defs/SubmitRoundTableResult"
      description: |
        Atomic close event. Present only when status=completed.
        Carries conclusion / citedMemories / artifacts / closedAt.
    personaSnapshots:
      type: object
      additionalProperties:
        type: string
        pattern: "^[a-f0-9]{64}$"
      description: |
        Map of agentId → persona_sha256 at round-open time.
        # PITFALLS §P1 mitigation: if any agent's persona hash changes mid-round (operator
        #   edit), the round is flagged for re-verification on next comparator pass.
    schemaVersion:
      type: string
      default: "1.0.0"
      description: |
        # PITFALLS §P14 mitigation: schema migration — dry-run mode keyed off this version.
    createdAt:
      type: string
      format: date-time
    lastUpdatedAt:
      type: [string, "null"]
      format: date-time

  $defs:
    PanelistSnapshot:
      type: object
      required: [agentId, personaSha256, fitnessScore, tools, memoryScope]
      additionalProperties: false
      properties:
        agentId:
          type: string
          pattern: "^[a-z0-9_-]+$"
          description: "References agents-schema.yaml name field — cite, do NOT redefine."
        personaSha256:
          type: string
          pattern: "^[a-f0-9]{64}$"
          description: |
            Snapshot of agent persona hash at round-open time.
            # Cites Phase 45 agents-schema $defs.EvolutionLogEntry.personaSha256.
        fitnessScore:
          type: [number, "null"]
          minimum: 0.0
          maximum: 1.0
          description: |
            Open-time snapshot. OQ-4: null = neutral 0.5 for ordering.
        tools:
          type: array
          items: { type: string }
          description: "Runtime whitelist snapshot (cite agents-schema tools)."
        memoryScope:
          type: string
          enum: [shared, per_agent, project_scoped]
          description: "Cite agents-schema memoryScope."
        deleted:
          type: boolean
          default: false
          description: |
            True if the agent was deleted AFTER round open (detected on next state-file read).
            # OQ-5 resolution: deleted agent stays in transcript with this flag.

    TurnOrder:
      type: object
      required: [strategy, currentIndex]
      additionalProperties: false
      properties:
        strategy:
          type: string
          enum: [round-robin, fixed, llm, matrix, fitness-weighted]
          default: round-robin
          description: |
            # OQ-2 resolution: default round-robin. fitness-weighted deferred to v11.1+.
            # B2.1 borrowable point: AutoGen/MAF three-state inspiration (llm/fixed/matrix).
        seed:
          type: array
          items: { type: string }
          description: |
            Pre-computed panelist order (for round-robin / fixed strategies).
            For llm / fitness-weighted strategies, this may be partial or empty —
            the next panelist is determined progressively.
        currentIndex:
          type: integer
          minimum: 0

    Turn:
      type: object
      required: [turnIndex, panelistId, opinion, citedMemoryIds, submittedAt]
      additionalProperties: false
      properties:
        turnIndex:
          type: integer
          minimum: 1
        panelistId:
          type: string
          pattern: "^[a-z0-9_-]+$"
        opinion:
          type: string
          description: "Free-text opinion submitted by CC on behalf of the panelist."
        citedMemoryIds:
          type: array
          default: []
          items: { type: string, format: uuid }
          description: |
            record_id list from memory-record-schema.yaml.
            # PITFALLS §P7 mitigation 1: every memory citation in a turn carries record_id,
            #   so other panelists can challenge with their own conflicting record_ids.
        conflictsDetected:
          type: array
          default: []
          items: { type: string }
          description: "List of conflictIds raised against this turn's citations."
        submittedAt:
          type: string
          format: date-time

    ConflictRecord:
      type: object
      required: [conflictId, memoryIdA, memoryIdB, scopeA, scopeB, confidenceA, confidenceB, resolution, resolvedAt]
      additionalProperties: false
      properties:
        conflictId:
          type: string
          format: uuid
        memoryIdA:
          type: string
          format: uuid
          description: "record_id of the first cited memory."
        memoryIdB:
          type: string
          format: uuid
          description: "record_id of the second cited memory (the contradicting one)."
        scopeA:
          type: string
          enum: [global, project, session]
          description: |
            Cite memory-record-schema scope field (Phase 45).
            # PITFALLS §P7 mitigation 3: scope precedence drives arbitration.
        scopeB:
          type: string
          enum: [global, project, session]
        confidenceA:
          type: number
          minimum: 0.0
          maximum: 1.0
          description: "Cite memory-record-schema confidence field."
        confidenceB:
          type: number
          minimum: 0.0
          maximum: 1.0
        resolution:
          type: string
          enum: [A-wins, B-wins, both-kept, both-quarantined, deferred-to-operator]
          description: |
            # PITFALLS §P7 mitigation 2 + 3 + 4: comparator LLM pass produces this resolution
            #   by combining scope precedence (session > project > global) and
            #   confidence-weighted voting.
        rationale:
          type: string
          description: "Comparator LLM's human-readable rationale for the resolution."
        resolvedAt:
          type: string
          format: date-time

    EarlyStopRule:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          enum: [consensus, max-rounds, manual, comparator-confident]
          description: |
            consensus = panelists converge on a single conclusion;
            max-rounds = hit maxRounds cap;
            manual = CC or operator explicitly stops;
            comparator-confident = comparator LLM confidence ≥ threshold on resolution.
        threshold:
          type: [number, "null"]
          minimum: 0.0
          maximum: 1.0
          description: "Required when type=comparator-confident; otherwise null."

    RoundTableOpen:
      type: object
      required: [caller, openedAt, project, question]
      additionalProperties: false
      properties:
        caller:
          type: string
          description: "CC session ID or operator handle that initiated the round."
        openedAt:
          type: string
          format: date-time
        project:
          type: string
          description: "Same as top-level projectId (denormalized for atomic open event)."
        question:
          type: string

    SubmitRoundTableResult:
      type: object
      required: [conclusion, citedMemories, closedAt, closedBy]
      additionalProperties: false
      properties:
        conclusion:
          type: string
          description: "CC's synthesis of the round table discussion (decision 7: CC controls content)."
        citedMemories:
          type: array
          items: { type: string, format: uuid }
          description: "All memory record_ids cited across all turns + in the conclusion."
        artifacts:
          type: array
          default: []
          items:
            type: object
            properties:
              artifactId: { type: string }
              artifactType: { type: string }
              content: { type: string }
              provenance: { type: string }
        closedAt:
          type: string
          format: date-time
        closedBy:
          type: string
          description: "CC session ID or operator handle."
  ```

  Include YAML comments citing OQ / PITFALLS / FEATURES source for each load-bearing field. Use camelCase throughout (no snake_case — Phase 45 verification lesson). Total ~250-300 lines.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/round-table-state-schema.yaml && python3 -c "import yaml; d=yaml.safe_load(open('/data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/round-table-state-schema.yaml')); assert d['\$schema']=='https://json-schema.org/draft/2020-12/schema', 'wrong \$schema'; assert 'roundId' in d['properties'], 'missing roundId'; assert 'additionalProperties' in d, 'missing top-level additionalProperties'; assert 'turnOrder' in d['properties'], 'missing turnOrder'; assert 'conflicts' in d['properties'], 'missing conflicts'; assert 'submitRoundTableResult' in d['properties'], 'missing submitRoundTableResult'; assert 'PanelistSnapshot' in d['\$defs'], 'missing PanelistSnapshot def'; assert 'ConflictRecord' in d['\$defs'], 'missing ConflictRecord def'; print('YAML+SCHEMA OK')" && for term in roundId turnOrder panelists turns conflicts earlyStopRule roundTableOpen submitRoundTableResult personaSha256 schemaVersion fitnessWeighted round-robin "OQ-2" "OQ-5" "OQ-11" "P7" "P1" "P14" "B2.1" "B8.2"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/round-table-state-schema.yaml || { echo "MISSING: $term"; exit 1; }; done && ! grep -E "^[a-z_]+_+[a-z]+:" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/round-table-state-schema.yaml | grep -v "^[a-z-]+:" | grep -v "^\\\$" | grep -v "^#" | head -5 | grep "." && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/round-table-state-schema.yaml | awk '$1 >= 200 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - round-table-state-schema.yaml exists, valid YAML, JSON Schema draft 2020-12 conformant
  - camelCase keywords throughout (no snake_case property names — Phase 45 verification lesson)
  - All 9 required fields present (roundId / projectId / question / panelists / turnOrder / status / turns / roundTableOpen / createdAt)
  - $defs has PanelistSnapshot / TurnOrder / Turn / ConflictRecord / EarlyStopRule / RoundTableOpen / SubmitRoundTableResult
  - Each load-bearing field has YAML comment citing OQ-XX / PITFALLS §P7 mitigation N / FEATURES borrowable point
  - OQ-2 (turnOrder strategy enum), OQ-5 (panelists deleted flag), OQ-11 (roundId UUID CC-generated) all resolved at schema level
  - Total ≥ 200 lines
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §2 (turn lifecycle narrative: round_table_open / turn N / submit_round_table_result atomicity + state file persistence)</name>
  <files>.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md §1 (Phase 45 §1.2 18-field table + §1.4 roadmap placement — pattern for how to structure lifecycle narrative)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §5.1 lines 470-545 (round table state file path + CC invokes round_table_open lifecycle sketch — VERBATIM cite source)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §8 anti-patterns lines 683-690 (round table as pipeline step — anti-pattern to AVOID)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 Tool 1-4 lines 259-456 (get_agent_persona / get_agent_opinion / get_agent_memory / submit_round_table_result signatures — drives lifecycle MCP contract)
    - .planning/research/v10-orchestrator-design/SUMMARY.md OQ-2 / OQ-5 / OQ-11 rows (lines 67/70/76 — turn_order / agent deletion / round_id)
    - .planning/research/v10-orchestrator-design/FEATURES.md §10 B2.1 line 527 (turn_order three-state) + B8.2 line 548 (Task lifecycle FSM)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.7 (decision 7 Hermes/CC structure/content split — already in context from Task 1)
  </read_first>

  <behavior>
  - §2.0 子节存在:声明 §2 是 turn lifecycle 的 narrative expansion,cross-reference to round-table-state-schema.yaml
  - §2.1 子节存在:round_table_open contract (caller / args / atomic semantics / immediate return with round_id; cites OQ-11 CC-self-generated UUID + ARCHITECTURE §8 anti-pattern "round table NOT synchronous pipeline step")
  - §2.2 子节存在:turn N contract (atomic turn lifecycle: panelist selection per turn_order → get_agent_opinion → opinion appended to state file → optional conflict detection; cites OQ-2 round-robin default + B2.1 three-state pluggable + OQ-5 deleted-agent snapshot + B8.2 turn FSM)
  - §2.3 子节存在:submit_round_table_result contract (atomic commit: conclusion + cited_memories + artifacts + conflicts → state file flips status=completed → conflict log sealed)
  - §2.4 子节存在:state file persistence rules (path = .runtime/{slug}/round_tables/{round_id}.json;append-only turns;lastUpdatedAt bump on every write;crash recovery via reading state file on next session)
  - §2.5 子节存在:turn_order strategies deep dive (round-robin default / fixed seed / llm progressive / matrix FSM / fitness-weighted deferred to v11.1+) — cites FEATURES B2.1 + ReConcile paper convergence point
  - §2.6 子节存在:agent deletion semantics (open-time snapshot; deleted panelistId appears in transcript with deleted=true badge; OQ-5 resolution)
  - §2.7 子节存在:early-stop conditions (consensus / max-rounds / manual / comparator-confident — cites decision 7 Hermes owns early_stop_rule via schema)
  - §2.8 子节存在:serial execution invariant restated + cross-ref to §4.1 (turns MUST be sequential await, never parallel — OQ-8)
  - Total Task 3 length ≥ 350 lines
  </behavior>

  <action>
  Append §2 to `.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md`.

  **§2 — Turn Lifecycle (round_table_open → turn N → submit_round_table_result)**

  §2.0 (~25 lines): "本节展开 §1.2 的 turn lifecycle 框架,聚焦于每个原子操作的契约。机器可读 schema 见 round-table-state-schema.yaml。所有 MCP tool 命名采用 STACK §3.2 形态(无 agent_ 前缀,见 §5)。"

  §2.1 round_table_open (~50 lines):
  - **Caller:** CC (Claude Code) — decision 7: CC owns question framing.
  - **Args:** roundId (UUID v4, CC-self-generated — OQ-11), panel (list of agentId), question (str), maxRounds (int, default 5), turnOrderStrategy (enum, default round-robin — OQ-2), earlyStopRule (object, optional), projectId (str, required).
  - **Atomic semantics:** Hermes creates state file at `.runtime/{slug}/round_tables/{round_id}.json` with status=open, snapshots all panelists' persona/fitness/tools (OQ-5), persists, returns immediately with `{roundId, initialTurnOrder}`.
  - **Anti-pattern guard:** round_table_open is NOT synchronous; it does NOT block until round completes (ARCHITECTURE §8). CC polls via events or proceeds to call get_agent_opinion directly.
  - **MCP tool:** `round_table_open` — see §5.1.

  §2.2 turn N (~60 lines):
  - **Selection:** CC reads turnOrder.currentIndex from state file, picks next panelist per strategy.
  - **Opinion call:** CC invokes `get_agent_opinion(agentId, topic, context, priorDiscussion)` — returns opinion text + cited memory record_ids (Phase 45 memory-record-schema).
  - **Append turn:** CC appends `{turnIndex, panelistId, opinion, citedMemoryIds, submittedAt}` to state file turns array. Append-only — turns never edited, only added.
  - **Conflict detection:** If citedMemoryIds contain records that conflict with prior turns' citations (detected via comparator pass), ConflictRecord entries are added to conflicts array (see §3).
  - **Increment:** turnOrder.currentIndex++ and lastUpdatedAt = now.
  - **Serial invariant:** Each turn awaits completion before the next starts. NO concurrent get_agent_opinion calls. Cite §4.1 for root cause.
  - **MCP tool:** `get_agent_opinion` + `get_agent_memory` — see §5.2/§5.3.

  §2.3 submit_round_table_result (~50 lines):
  - **Caller:** CC, when conclusion is synthesized (decision 7: CC owns synthesis) OR earlyStopRule triggers.
  - **Args:** roundId, conclusion (str), citedMemories (list of record_id), artifacts (list of {artifactType, content, provenance}), conflicts (list of conflictId from state file).
  - **Atomic semantics:** Hermes validates roundId exists + status=open, seals conflicts log, flips status=completed, persists submitRoundTableResult block, returns receipt_id.
  - **Idempotency:** Second submit for same roundId returns 409 Conflict (already completed).
  - **MCP tool:** `submit_round_table_result` — see §5.4.

  §2.4 state file persistence (~30 lines):
  - **Path:** `~/.hermes/agents/.runtime/{slug}/round_tables/{round_id}.json` (cite ARCHITECTURE §5.1 verbatim).
  - **Append-only:** turns array grows monotonically; existing entries are immutable.
  - **lastUpdatedAt:** Bumped on every write (turn append, conflict add, status flip).
  - **Crash recovery:** On CC restart, read state file; if status=open and (now - lastUpdatedAt) > stall threshold, mark status=stalled; operator can resume via round_table_resume (deferred to v11.1+) or abort.

  §2.5 turn_order strategies (~60 lines):
  Detailed deep dive on each TurnOrder.strategy value:
  - **round-robin (default):** Pre-computed seed = shuffled panelist list (shuffle seed = roundId for reproducibility). Each panelist speaks once per round; cycles repeat until maxRounds or early stop. Cite ReConcile paper (typical convergence ~3-5 rounds).
  - **fixed:** Operator-supplied seed order. Used when seniority matters (e.g. compliance_gate speaks last as final reviewer).
  - **llm:** Progressive — CC asks an LLM "who should speak next given the current turns?" each turn. Higher token cost; deferred to v11.1+ as opt-in.
  - **matrix:** FSM transition matrix — each (currentSpeaker, opinionContent) pair maps to next speaker. Cites FEATURES B2.1 (AutoGen/MAF inspiration). PoC scope: simple 2-state matrix; full matrix editor deferred to v11.1+.
  - **fitness-weighted:** Higher-fitness panelists speak first (framing the discussion). OQ-2: **deferred to v11.1+** — needs ≥10 fitness data points per agent to be meaningful, which won't exist at PoC start.
  - Cite FEATURES B2.1 verbatim ("three-state llm/fixed/matrix inspiration") + B8.2 (A2A task lifecycle FSM inspires the turn state machine transitions).

  §2.6 agent deletion semantics (~25 lines):
  - **Open-time snapshot:** PanelistSnapshot captured at round_table_open includes persona_sha256, fitness_score, tools, memory_scope. If agent YAML is later edited or deleted, the snapshot in the state file is the authoritative record for THIS round.
  - **Deletion flag:** If Hermes detects agent YAML no longer exists on next state-file read, the PanelistSnapshot.deleted flag flips to true. Subsequent turns for that panelistId are skipped; transcript shows "[deleted]" badge.
  - **OQ-5 resolution:** This preserves transcript integrity — readers can always understand what was debated even if agents are later removed.

  §2.7 early-stop conditions (~30 lines):
  Table of earlyStopRule.type values:
  - **consensus:** All N panelists' latest turns agree on conclusion (semantic match via comparator LLM — high token cost).
  - **max-rounds:** turnIndex > maxRounds (default 5).
  - **manual:** CC or operator explicitly calls submit_round_table_result.
  - **comparator-confident:** comparator LLM confidence on conflict resolution ≥ threshold (default 0.85).
  - Cite decision 7: Hermes owns the rule schema; CC owns detection (Hermes doesn't monitor turns in real-time — CC polls state file).

  §2.8 serial invariant restatement (~15 lines): "§2.2 step 'Serial invariant' is the load-bearing constraint. Restated: each `await get_agent_opinion(...)` MUST complete before the next panelist's turn begins. No Promise.all over panelists. No background opinion polling. Root cause analysis in §4.1 (GLM 4-key rotation + global concurrency==1)."

  Length target: ~350-400 lines for Task 3.
  </action>

  <verify>
    <automated>grep -q "## §2" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -c "^### §2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 8 {exit 0} {exit 1}' && for term in "round_table_open" "roundId" "UUID" "OQ-11" "turn N" "append-only" "atomic" "submit_round_table_result" "round-robin" "fitness-weighted" "matrix" "B2.1" "B8.2" "OQ-2" "OQ-5" "deleted" "early-stop\|earlyStopRule" "serial" "await" "ARCHITECTURE §5\.1\|ARCHITECTURE §5"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "round_table_open\|round_table_resume\|submit_round_table_result\|get_agent_opinion" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 6 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 550 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §2 exists with §2.0 声明 + §2.1-§2.8 (8 H3 subsections covering round_table_open / turn N / submit_round_table_result / persistence / turn_order / deletion / early-stop / serial restatement)
  - §2.1 explicitly resolves OQ-11 (CC-self-generated UUID)
  - §2.5 explicitly resolves OQ-2 (default round-robin + 4 alternative strategies with v11.1+ deferrals)
  - §2.6 explicitly resolves OQ-5 (open-time snapshot + deleted flag)
  - §2 cites ARCHITECTURE §5.1 verbatim (state file path) + §8 anti-pattern (round table NOT synchronous)
  - §2 cites FEATURES B2.1 + B8.2 borrowable points
  - Total doc length now ≥ 550 lines (cumulative with Task 1)
  </done>
</task>

<task type="auto">
  <name>Task 4: Write §3 (memory conflict arbitration: P7 mitigation + comparator LLM + scope precedence + confidence voting + conflict log) + §4 (hard constraints: serial execution / MCP naming / OQ resolutions)</name>
  <files>.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 7 lines 235-265 (the load-bearing pitfall for §3 — VERBATIM cite its 5 mitigations)
    - .planning/research/v10-orchestrator-design/SUMMARY.md CC-1 lines 23-25 (MCP naming conflict) + CC-6 lines 43-45 (serial constraint root cause)
    - .planning/research/v10-orchestrator-design/SUMMARY.md OQ-8 / OQ-9 / OQ-15 rows (lines 74/75/81 — serial / naming / arbitration)
    - /home/kai/.claude/projects/-data-workspace-hermes-agent/memory/feedback-glm-overload-reduce-concurrency.md (full read — the GLM concurrency==1 root cause policy; embed verbatim quotes)
    - .planning/research/v10-orchestrator-design/STACK.md §7 lines 990-1015 (token cost estimates — ~550K tokens/pipeline run serial) + §11 OQ lines 1115-1135 (OQ-8 / OQ-9 / OQ-11)
    - .planning/research/v10-orchestrator-design/memory-record-schema.yaml (Phase 45 schema — for scope/confidence/evidence_chain field citations in §3)
  </read_first>

  <behavior>
  - §3.0 子节存在:声明 §3 是 P7 (round-table memory conflict) 的完整 mitigation,5 个子机制对齐 PITFALLS §P7 mitigation 1-5
  - §3.1 子节存在:memory annotation in turns (citedMemoryIds in Turn schema — PITFALLS §P7 mitigation 1)
  - §3.2 子节存在:comparator LLM pass (Hermes coordinator role; triggers on conflict detection; output = resolution + rationale — PITFALLS §P7 mitigation 2)
  - §3.3 子节存在:scope precedence rules (session > project > global — PITFALLS §P7 mitigation 3 + cites memory-record-schema scope field)
  - §3.4 子节存在:confidence-weighted voting (cites memory-record-schema confidence field; tie-break by coordinator — PITFALLS §P7 mitigation 4)
  - §3.5 子节存在:conflict log for curator review (ConflictRecord schema; high-frequency conflict promotion/quarantine — PITFALLS §P7 mitigation 5)
  - §3.6 子节存在:comparator LLM prompt template + decision tree (concrete prompt skeleton the v11.0 PoC will use)
  - §3.7 子节存在:edge cases (3+ way conflict / no-confidence tie / all-quarantined)
  - §4.0 子节存在:声明 §4 是 §1.5 三个 hard constraints 的 root-cause 展开
  - §4.1 子节存在:serial execution root cause (cites MEMORY.md feedback-glm-overload-reduce-concurrency.md verbatim — global concurrency==1 is BY DESIGN after 5→3→1 cycle; ~550K tokens/pipeline run serial estimate from STACK §7; GLM 4-key rotation ≈ 800K TPM ceiling; 7 panelist × N round parallel would instantly exceed — resolves OQ-8)
  - §4.2 子节存在:MCP tool naming STACK form (cites SUMMARY.md CC-1 — STACK form wins, no agent_ prefix, aligns with existing 9 messaging tools; reconciliation table showing old ARCHITECTURE §4.2 names → new canonical names — resolves OQ-9)
  - §4.3 子节存在:OQ resolution audit table (OQ-2 / OQ-5 / OQ-8 / OQ-9 / OQ-11 / OQ-15 — 6 rows, each resolved or explicitly deferred)
  - §4.4 子节存在:borrowable points coverage table (B1.4 / B2.1 / B2.3 / B4.2 / B6.1 / B7.3 / B8.2 — 7 rows, each citing where in this doc it's addressed)
  - Total Task 4 length ≥ 400 lines
  </behavior>

  <action>
  Append §3 + §4 to `.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md`.

  **§3 — Memory Conflict Arbitration (P7 mitigation)**

  §3.0 (~30 lines): "本节是 PITFALLS §Pitfall 7 (Round-Table Memory Conflict) 的完整 mitigation。P7 是 v10.0 round table 的核心风险——多个 agent 引用各自的 scoped memory 达到矛盾结论,无显式仲裁时 debate 被 'loud memory' agent 主导。5 个子机制对齐 PITFALLS §P7 mitigation 1-5。所有引用 Phase 45 memory-record-schema 字段(record_id / scope / confidence / evidence_chain)采用 CITE-ONLY 策略,不重定义。"

  §3.1 memory annotation in turns (~30 lines):
  - **Field:** `Turn.citedMemoryIds` (list of UUID — see round-table-state-schema.yaml $defs.Turn).
  - **Mechanism:** When a panelist's opinion cites a memory, the record_id is appended to citedMemoryIds. Other panelists can challenge: "I disagree with memory_id #X because my memory_id #Y contradicts it."
  - **PITFALLS §P7 mitigation 1 verbatim citation:** "When an agent cites a memory in its turn, the citation includes memory_id, confidence, scope (global/project/session), and evidence_record_count. Other agents can challenge..."
  - **Citation enrichment:** At turn-append time, Hermes resolves each record_id to its scope + confidence + evidence_chain and stores these denormalized in ConflictRecord (so the comparator LLM doesn't need to re-fetch on every conflict).

  §3.2 comparator LLM pass (~50 lines):
  - **Trigger:** When CC detects two panelists' citedMemoryIds contain records with overlapping topic but contradictory content (semantic similarity check via embedding cosine ≥ 0.7 + manual CC flag).
  - **Coordinator (Hermes) role:** Per PROJECT.md decision #7 — Hermes owns turn_order/max_rounds/early_stop_rule; this is the NEW responsibility: Hermes coordinator arbitrates memory conflicts. Hermes extracts both cited memories (with scope/confidence/evidence_chain), runs a comparator LLM pass ("which is more applicable in this project context?"), and broadcasts the resolution to all participants.
  - **Comparator LLM prompt template (concrete, v11.0 PoC-ready):**
    ```
    You are arbitrating a memory conflict in a Hermes round table.
    Project context: {project_id}
    Question: {question}

    Memory A (cited by {panelistA}):
    - content: {memoryA.content}
    - scope: {memoryA.scope} (global | project | session)
    - confidence: {memoryA.confidence} (0.0-1.0)
    - evidence_chain length: {len(memoryA.evidence_chain)}

    Memory B (cited by {panelistB}):
    [same fields]

    Apply scope precedence: session > project > global (a session-scoped memory overrides global for THIS session; a project-scoped memory overrides global for THIS project).
    Apply confidence-weighting: higher confidence wins ties at same scope level.

    Output JSON: {"resolution": "A-wins" | "B-wins" | "both-kept" | "both-quarantined" | "deferred-to-operator", "rationale": "<=200 chars>", "confidence": 0.0-1.0}
    ```
  - **Broadcast:** Resolution appended to ConflictRecord; all subsequent turns can see it via state file.
  - **Token cost:** ~2K tokens per comparator call (estimated). v11.0 PoC must monitor: if >3 conflicts/round, cost balloons — early_stop_rule=comparator-confident can cap this.

  §3.3 scope precedence rules (~30 lines):
  - **Rule:** session > project > global.
  - **Citation:** PITFALLS §P7 mitigation 3 verbatim — "A project-scoped memory ('this project's audience prefers X') overrides a global-scoped memory ('audiences in general prefer Y')."
  - **Implementation:** Comparator LLM is told the scope precedence and resolves accordingly. No Hermes-side hard rule — the LLM applies the precedence in-context.
  - **Edge case (3+ way conflict):** If 3+ memories conflict at different scopes, comparator LLM picks the highest-scope-level one (session > project > global). If multiple at same scope level, confidence-weighted voting (§3.4).

  §3.4 confidence-weighted voting (~30 lines):
  - **Rule:** When N panelists disagree at the same scope level, vote weighted by memory `confidence` field.
  - **Example:** `screenplay` cites a 0.95-confidence project memory; `theory_critic` cites a 0.6-confidence global memory. screenplay's vote weighs more. Ties broken by coordinator (Hermes-side deterministic tiebreak: lower agentId alphabetical order, for reproducibility).
  - **Citation:** PITFALLS §P7 mitigation 4 verbatim. Cites memory-record-schema confidence field.
  - **Tie-break detail:** If A-wins and B-wins votes are within 0.05 confidence of each other, resolution flips to `deferred-to-operator` (human review).

  §3.5 conflict log for curator review (~30 lines):
  - **Field:** `state.conflicts` array (ConflictRecord objects).
  - **Persistence:** Conflict log is part of the state file, sealed at submit_round_table_result time.
  - **Curator review:** Curator's periodic pass (the v6.0 `_feedback_scan_phase` extension) reviews high-frequency conflicts: same memory pair conflicting >3 times across rounds → curator may promote one to global scope OR quarantine the loser (status=quarantined per memory-record-schema).
  - **Citation:** PITFALLS §P7 mitigation 5 verbatim + Phase 45 §5 `_memory_evolution_phase` contract.

  §3.6 comparator LLM prompt template + decision tree (~30 lines): Embed the prompt template from §3.2 verbatim in a code block, plus a decision tree showing the 5 possible resolutions and their follow-up actions (e.g. "A-wins → Memory B cited memory's `supersedes_memory_id` chain updated to point to A; both-kept → no action; both-quarantined → status=quarantined on both records").

  §3.7 edge cases (~30 lines):
  - **3+ way conflict:** Multi-way conflicts decomposed into pairwise comparator calls; majority resolution wins.
  - **No-confidence tie (all panelists cite ≤0.3 confidence memories):** resolution=deferred-to-operator; round table continues but conclusion flagged "low-confidence".
  - **All quarantined:** If comparator determines all cited memories should be quarantined, round aborts with status=aborted; operator review triggered.

  **§4 — Hard Constraints (root-cause analysis + OQ + borrowable points audit)**

  §4.0 (~15 lines): "本节展开 §1.5 三个 hard constraints 的 root cause,并给出 6 个 Open Questions + 7 个 borrowable points 的 audit table。"

  §4.1 serial execution root cause (~80 lines):
  - **OQ-8 resolution.**
  - **MEMORY.md verbatim citation:** Quote the relevant excerpt from `~/.claude/projects/-data-workspace-hermes-agent/memory/feedback-glm-overload-reduce-concurrency.md` — global concurrency演进历程 "2026-07-03 从 5→3; 2026-07-06 已是 1" + "concurrency==1 之后再降不动了——上游模型容量本身就是瓶颈".
  - **Token cost math:** STACK §7 estimates ~550K tokens per pipeline run (80 opinion calls × 6.5K tokens) ASSUMING SERIAL. GLM ceiling: 4 keys × 200K TPM ≈ 800K TPM. 7 panelists × 3 rounds × 6.5K tokens = 137K tokens — fine serially (~10s); catastrophically bad in parallel (would hit ceiling in <1s).
  - **Implementation discipline:**
    1. v11.0 PoC `get_agent_opinion` implementation MUST `await` each call sequentially in a for-loop, NEVER `Promise.all` or async-gather.
    2. v11.0 PoC dispatcher (Hermes-side) MUST reject concurrent `get_agent_opinion` calls for the same roundId (return 429 Too Many Requests).
    3. v11.0 PoC must NOT add a "parallel round table" mode even as an opt-in feature flag — the constraint is structural, not configurational.
  - **Why this is non-negotiable:** MEMORY.md `feedback-glm-overload-reduce-concurrency.md` records Kai's explicit decision (5→3→1 deprecation cycle). Telegram users receiving 115-char "暂停 10-15 分钟" messages is BY DESIGN. Re-introducing parallelism at the round table layer would undo this operator-level discipline.

  §4.2 MCP tool naming STACK form (~50 lines):
  - **OQ-9 resolution.**
  - **Reconciliation table:** ARCHITECTURE §4.2 (agent_ prefix) → STACK §3.2 (no prefix) canonical names.
    | ARCHITECTURE §4.2 (deprecated) | STACK §3.2 (canonical, ADOPTED) | Used in this doc |
    |--------------------------------|--------------------------------|------------------|
    | agent_get_persona | get_agent_persona | §2.1, §5.1 |
    | agent_recall | get_agent_memory | §3.1, §5.3 |
    | agent_opinion | get_agent_opinion | §2.2, §5.2 |
    | agent_conclude | submit_round_table_result | §2.3, §5.4 |
    | (new) | submit_artifact | §5.5 |
    | (new) | query_memory | §5.6 |
    | (new) | run_python_phase | §5.7 |
  - **CC-1 verbatim citation:** "STACK §3.2 form wins — no prefix, aligns with existing mcp_serve.py 9 messaging tool style."
  - **Rationale:** Existing `mcp_serve.py` tools are `conversations_list`, `messages_read`, `messages_send`, etc. (no `messaging_` prefix). Adding `agent_*` prefix would be inconsistent. STACK form preserves style uniformity.

  §4.3 OQ resolution audit table (~50 lines):
  6-row table covering the 6 OQs in scope for this phase:
  | OQ# | 问题 (一句话) | 倾向性结论 (SUMMARY) | 本 doc 解决章节 | 是否 defer |
  | OQ-2 | turn_order 策略 | default round-robin + 4 alternative strategies | §2.5 | partial (fitness-weighted, full matrix → v11.1+) |
  | OQ-5 | agent deletion transcript integrity | open-time snapshot + deleted flag | §2.6 | NO |
  | OQ-8 | GLM 4-key rotation × concurrency | strict serial, 1 panelist 1 turn await | §2.8 + §4.1 | NO (non-negotiable) |
  | OQ-9 | MCP tool naming | STACK form (no prefix) | §4.2 + §5 | NO |
  | OQ-11 | round_id source | CC self-generates UUID v4 | §2.1 | NO |
  | OQ-15 | round-table conflict arbitration | comparator LLM + scope precedence + confidence voting + conflict log | §3.1-§3.7 | NO |

  §4.4 borrowable points coverage (~50 lines):
  7-row table covering FEATURES §10 B1.4 / B2.1 / B2.3 / B4.2 / B6.1 / B7.3 / B8.2:
  | BP# | Source | Borrowable idea | Where addressed in this doc | How adapted |
  | B1.4 | LangGraph | hierarchical supervisor → round table nesting | §6.1 (subpanel) | panelist.can_convene_subpanel flag (deferred to v11.1+) |
  | B2.1 | AutoGen/MAF | turn_order three-state (llm/fixed/matrix) | §2.5 | 5 strategies enum (round-robin + 4 alternatives) |
  | B2.3 | AutoGen | nested chat → panelist convenes sub-round table | §6.1 (subpanel) | opt-in field; v11.1+ |
  | B4.2 | Claude Agent SDK | hooks lifecycle → PreTurn/PostTurn audit | §6.2 (audit hooks) | Hermes emits PreTurn/PostTurn events to state file |
  | B6.1 | Camel-AI | generator+critics → panelist_role field | §6.3 (panelist roles) | enum: generator / critic / synthesizer / reviewer |
  | B7.3 | Agent-MCP | file-level lock → multi-agent parallel asset_locks | §6.4 (asset locks) | deferred — v11.0 is serial so locks are trivial |
  | B8.2 | A2A | Task lifecycle FSM → turn state machine | §2.2 + round-table-state-schema status enum | turn FSM: pending → speaking → submitted → (challenged) |

  Length target: ~400-450 lines for Task 4 (§3 ~250 + §4 ~200).
  </action>

  <verify>
    <automated>grep -q "## §3" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -q "## §4" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -c "^### §3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "^### §4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 4 {exit 0} {exit 1}' && for term in "comparator" "scope precedence" "session > project > global\|session.*project.*global" "confidence-weighted" "conflict log" "P7" "PITFALLS §P7\|PITFALLS §Pitfall 7" "serial" "concurrency==1\|concurrency == 1" "feedback-glm-overload-reduce-concurrency" "STACK §3.2" "STACK form\|no prefix" "OQ-2" "OQ-5" "OQ-8" "OQ-9" "OQ-11" "OQ-15" "B1.4" "B2.1" "B2.3" "B4.2" "B6.1" "B7.3" "B8.2"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^## §4\.3/,/^## §4\.4/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | grep -c "OQ-" | awk '$1 >= 6 {exit 0} {exit 1}' && awk '/^## §4\.4/,/^## §[56]/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | grep -c "B[0-9]\.[0-9]" | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "5→3\|5->3\|5→3→1\|5->3->1" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 1 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 900 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §3 exists with §3.0-§3.7 (memory annotation + comparator LLM + scope precedence + confidence voting + conflict log + prompt template + edge cases)
  - §3 cites PITFALLS §P7 mitigation 1-5 verbatim
  - §3 cites memory-record-schema scope/confidence/evidence_chain fields (CITE-ONLY)
  - §3.2 embeds the comparator LLM prompt template (concrete, v11.0 PoC-ready)
  - §4.1 cites MEMORY.md feedback-glm-overload-reduce-concurrency.md verbatim (5→3→1演进 + concurrency==1 BY DESIGN)
  - §4.2 has ARCHITECTURE→STACK naming reconciliation table
  - §4.3 has 6-row OQ audit table (OQ-2/5/8/9/11/15)
  - §4.4 has 7-row borrowable points coverage table (B1.4/B2.1/B2.3/B4.2/B6.1/B7.3/B8.2)
  - Total doc length now ≥ 900 lines (cumulative with Tasks 1+3)
  </done>
</task>

<task type="auto">
  <name>Task 5: Write §5 (MCP tool spec for 7 tools, STACK §3.2 form) + §6 (advanced features: subpanel / hooks / roles / asset locks) + §7 (downstream citation guide) + §8 (summary + references)</name>
  <files>.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 lines 257-590 (7 tool schemas — VERBATIM cite source; do NOT redefine Pydantic models, only narrate the contract)
    - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md §8 (downstream citation card table — pattern reference for §7)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §5 (downstream citation cards pattern)
    - .planning/research/v10-orchestrator-design/FEATURES.md §10 B1.4/B2.3 (subpanel pattern), B4.2 (hooks), B6.1 (critic role), B7.3 (file lock)
    - .planning/ROADMAP.md §Phase 49/50/51 (downstream docs consuming this protocol)
  </read_first>

  <behavior>
  - §5.0 子节存在:声明 §5 是 7 个 MCP tool 的 narrative contract(不重写 Pydantic schema,只描述 input/output 语义 + 与 round table lifecycle 的关系;authoritative schema 在 STACK §3.2)
  - §5.1-§5.7 子节存在:7 个 tool subsections, each ~30-50 lines, covering Tool 1-7
  - Each tool subsection: signature + inputs + outputs + side effects + which §2/§3 step uses it
  - §5 explicitly declares: ALL names use STACK §3.2 form (no agent_ prefix) per OQ-9 + §4.2
  - §6.0 子节存在:声明 §6 是 FEATURES §10 borrowable points 的深度评估(已在 §4.4 audit 过;§6 给 v11.1+ 候选 feature 的 evaluation)
  - §6.1 子节存在:subpanel / nested round table (B1.4 + B2.3 — panelist.can_convene_subpanel; deferred to v11.1+)
  - §6.2 子节存在:PreTurn/PostTurn audit hooks (B4.2 — Hermes emits events to state file)
  - §6.3 子节存在:panelist_role field (B6.1 — generator/critic/synthesizer/reviewer enum)
  - §6.4 子节存在:asset_locks (B7.3 — v11.0 serial so trivial; v11.1+ parallel subpanels need it)
  - §7.0 子节存在:downstream citation guide — 列出 04/05/47/48 引用本文档 lifecycle/conflict rule 时,应从哪个章节取
  - §7.1 子节存在:Summary + coherence 声明 (turn lifecycle + conflict arbitration + 7 MCP tool + 3 hard constraints + 6 OQ + 7 BP = 完整 round table protocol contract)
  - §7.2 子节存在:References (00-FIRST-PRINCIPLES + 01-AGENT-REGISTRY-SCHEMA + ARCHITECTURE + PITFALLS + STACK + FEATURES + SUMMARY + MEMORY.md feedback-glm-overload-reduce-concurrency + CLAUDE.md)
  - Total Task 5 length ≥ 250 lines
  </behavior>

  <action>
  Append §5 + §6 + §7 + §8 to `.planning/remes-agent/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md`.

  **§5 — MCP Tool Contract (7 tools, STACK §3.2 form)**

  §5.0 (~15 lines): "本节描述 7 个 MCP tool 的 input/output 语义。Authoritative Pydantic schema 在 STACK §3.2 — 本节仅 narrative。所有命名采用 STACK form(无 agent_ 前缀,见 §4.2)。"

  §5.1 get_agent_persona (~40 lines):
  - **Signature:** `get_agent_persona(agentId: str) -> AgentPersonaResult`
  - **Inputs:** agentId (references agents-schema.yaml name field).
  - **Outputs:** AgentPersonaResult { persona, tools, memoryScope, fitnessScore, personaSha256 }.
  - **Side effects:** None (readOnlyHint=True per STACK §3.2).
  - **Used in:** §2.1 (round_table_open snapshots all panelists via batched get_agent_persona).
  - **Citation:** STACK §3.2 Tool 1.

  §5.2 get_agent_opinion (~50 lines):
  - **Signature:** `get_agent_opinion(agentId, topic, context, priorDiscussion?) -> AgentOpinionResult`
  - **Inputs:** agentId, topic (str), context (str — includes question + project metadata), priorDiscussion (optional str — concatenated prior turns).
  - **Outputs:** AgentOpinionResult { opinion, citedMemoryIds, confidence, conflictsDetected }.
  - **Side effects:** Reads mem0 backend (via agent_id filter); updates memory recall_count + last_recalled_at on cited records.
  - **Serial invariant (OQ-8):** Hermes dispatcher MUST reject concurrent calls for same roundId (return 429). v11.0 PoC enforces via per-roundId lock.
  - **Used in:** §2.2 (each turn N).
  - **Citation:** STACK §3.2 Tool 2.

  §5.3 get_agent_memory (~40 lines):
  - **Signature:** `get_agent_memory(agentId, query, projectId, confidentialityFilter) -> MemoryHit[]`
  - **Inputs:** agentId, query (str), projectId (for scope filtering), confidentialityFilter (enum from memory-record-schema).
  - **Outputs:** MemoryHit[] — each { recordId, scope, confidence, evidenceChain, content }.
  - **Side effects:** Updates recall_count + last_recalled_at.
  - **Used in:** §3.1 (conflict detection — CC fetches full memory records to compare).
  - **Citation:** STACK §3.2 Tool 3.

  §5.4 submit_round_table_result (~40 lines):
  - **Signature:** `submit_round_table_result(roundId, conclusion, citedMemories, artifacts, conflicts) -> ReceiptResult`
  - **Inputs:** roundId, conclusion, citedMemories (list of recordId), artifacts (list), conflicts (list of conflictId from state file).
  - **Outputs:** ReceiptResult { receiptId, sealedAt, finalConflictCount }.
  - **Side effects:** Flips state.status=completed; seals conflict log; emits event for curator periodic review.
  - **Idempotency:** Second call returns 409 Conflict.
  - **Used in:** §2.3 (atomic close).
  - **Citation:** STACK §3.2 Tool 4.

  §5.5 submit_artifact (~30 lines):
  - **Signature:** `submit_artifact(roundId, artifactType, content, provenance) -> ArtifactResult`
  - **Used in:** Optional mid-round artifact submission (e.g. screenplay draft, storyboard sketch).
  - **Citation:** STACK §3.2 Tool 5.

  §5.6 query_memory (~30 lines):
  - **Signature:** `query_memory(query, agentId?, scope?, projectId?) -> MemoryHit[]`
  - **Generic, non-agent-scoped** — CC uses it for cross-agent memory exploration (e.g. "what does every agent remember about horror pacing?").
  - **Citation:** STACK §3.2 Tool 6.

  §5.7 run_python_phase (~30 lines):
  - **Signature:** `run_python_phase(phaseName, inputs) -> PhaseResult`
  - **B3a boundary tool** — invokes Python runner phases (e.g. comfyui generation, video encoding). openWorldHint=True.
  - **Used in:** Optional mid-round execution (e.g. cinematographer invokes visual_executor phase).
  - **Citation:** STACK §3.2 Tool 7.

  **§6 — Advanced Features (deferred to v11.1+, evaluation only)**

  §6.0 (~15 lines): "本节评估 FEATURES §10 中需要深度评估的 borrowable points。已在 §4.4 audit 过 — 这里给 v11.1+ 候选 feature 的实施评估。"

  §6.1 subpanel / nested round table (B1.4 + B2.3, ~30 lines):
  - **Idea:** A panelist with `can_convene_subpanel=true` flag (agents-schema additive field, deferred) can spawn a sub-round-table within their turn.
  - **v11.0 PoC scope:** NOT supported. v11.0 is single-level round table only.
  - **v11.1+ evaluation:** Useful for complex multi-expert negotiations (e.g. visual_executor convenes prompt_injector + colorist as subpanel for color grading decisions). Risk: token cost multiplies; serial invariant gets complex (subpanel must complete before parent turn ends).
  - **Citation:** FEATURES B1.4 (LangGraph hierarchical supervisor) + B2.3 (AutoGen nested chat).

  §6.2 PreTurn/PostTurn audit hooks (B4.2, ~25 lines):
  - **Idea:** Hermes emits PreTurn/PostTurn events to state file —PreTurn captures panelist + topic + citedMemoryIds at turn-start; PostTurn captures opinion + conflictsDetected at turn-end.
  - **v11.0 PoC scope:** Implicit via Turn schema (turnIndex / panelistId / opinion / citedMemoryIds / submittedAt / conflictsDetected already capture this).
  - **v11.1+ evaluation:** Explicit event stream (separate from state file) for real-time curator monitoring. Useful for fitness_trend.jsonl correlation.
  - **Citation:** FEATURES B4.2 (Claude Agent SDK hooks).

  §6.3 panelist_role field (B6.1, ~25 lines):
  - **Idea:** PanelistSnapshot.role enum: generator / critic / synthesizer / reviewer. Influences turn_order (reviewer speaks last) and opinion synthesis (synthesizer integrates others' opinions).
  - **v11.0 PoC scope:** NOT in agents-schema v1.0 (additive field).
  - **v11.1+ evaluation:** Add as optional field; round table coordinator can use it for turn_order strategy=role-based.
  - **Citation:** FEATURES B6.1 (Camel-AI generator+critics).

  §6.4 asset_locks (B7.3, ~25 lines):
  - **Idea:** Multi-agent parallel asset editing needs file-level locks (e.g. two agents editing the same storyboard.json).
  - **v11.0 PoC scope:** NOT needed — serial invariant means only one agent acts at a time, no concurrent edits.
  - **v11.1+ evaluation:** Required if subpanels (§6.1) are added. Hermes-side lock manager (in-memory map of assetPath → lockHolder).
  - **Citation:** FEATURES B7.3 (Agent-MCP file-level lock).

  **§7 — Downstream Citation Guide + Summary + References**

  §7.0 downstream citation card table (~30 lines):
  | Downstream doc | Cite from this doc | Do NOT re-derive | Should derive |
  | 04-MIGRATION-PATH.md | §2 lifecycle + §4.2 MCP names | turn_order strategies, conflict rules | 15-expert migration semantics, retained-phases allowlist |
  | 05-POC-PLAN.md | §3 conflict arbitration + §4.1 serial + §4.3 OQ audit + §4.4 BP audit | entire protocol | PoC acceptance criteria, fitness battery, latency SLO |
  | 03-COMPARISON-VS-KIMI-MCP-SHIM.md | §4.2 MCP naming + §5 tool list | protocol detail | Kimi MCP shim comparison dimensions |
  | 06-CROSS-REPO-IMPACT.md | §2.4 state file path | protocol detail | 3-location sync + Option B vs 物理分区 |
  | 51 VALIDATE lint | round-table-state-schema.yaml + 02-ROUND-TABLE-PROTOCOL.md | entire protocol | cross-doc consistency lint script |

  §7.1 coherence 声明 (~20 lines): "Turn lifecycle (round_table_open → turn N → submit_round_table_result) + memory conflict arbitration (comparator + scope precedence + confidence voting + conflict log) + 7 MCP tool contract (STACK §3.2 form) + 3 hard constraints (serial / naming / atomicity) + 6 OQ resolutions + 7 borrowable points coverage = 完整 round table protocol contract for v10.0. v11.0 PoC implementer 引用本 doc 字段名 + lifecycle 步骤即可实施,不需重新推导决策 5/6/7。"

  §7.2 References (~15 lines): 00-FIRST-PRINCIPLES.md + 01-AGENT-REGISTRY-SCHEMA.md + ARCHITECTURE.md §5 + §8 + PITFALLS.md §P7 + STACK.md §3.2/§7 + FEATURES.md §10 + SUMMARY.md + MEMORY.md feedback-glm-overload-reduce-concurrency.md + CLAUDE.md + Phase 44/45 commit references.

  Length target: ~250-300 lines for Task 5.
  </action>

  <verify>
    <automated>grep -q "## §5" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -q "## §6" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -q "## §7" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md && grep -c "^### §5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "^### §6\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 4 {exit 0} {exit 1}' && for term in "get_agent_persona" "get_agent_opinion" "get_agent_memory" "submit_round_table_result" "submit_artifact" "query_memory" "run_python_phase" "STACK §3.2" "B1.4" "B2.3" "B4.2" "B6.1" "B7.3" "v11.1" "subpanel\|nested round" "04-MIGRATION-PATH" "05-POC-PLAN"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "STACK §3\.2 Tool [1-7]\|STACK §3\.2 Tool" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md | awk '$1 >= 1100 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §5 exists with §5.0-§5.7 (7 tool subsections covering STACK §3.2 tools, all using no-prefix form)
  - §6 exists with §6.0-§6.4 (4 advanced features evaluated, all deferred to v11.1+ with rationale)
  - §7 exists with §7.0 downstream citation card + §7.1 coherence 声明 + §7.2 References
  - All 7 MCP tools cited via "STACK §3.2 Tool N"
  - All 7 borrowable points (B1.4/B2.1/B2.3/B4.2/B6.1/B7.3/B8.2) appear at least once
  - Total doc length ≥ 1100 lines (final)
  - 04-MIGRATION-PATH + 05-POC-PLAN both appear in downstream citation card
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc → v11.0 PoC implementer | The deliverable is a design + machine-readable schema. Misinterpretation by v11.0 PoC implementers is the primary risk. Mitigated by §7.0 downstream citation card + per-section OQ / PITFALLS source comments. |
| round-table-state-schema.yaml → Python dataclass generator | The YAML will be consumed by a future Python codegen tool. JSON Schema validity + camelCase field-name stability are critical. Mitigated by JSON Schema draft 2020-12 conformance + camelCase convention (Phase 45 verification lesson). |
| 02-ROUND-TABLE-PROTOCOL.md → Phase 45 schemas | This doc CONSUMES Phase 45 fields (memory_scope, scope, confidence, evidence_chain, confidentiality). Re-derivation would cause schema drift. Mitigated by explicit "CITE ONLY, do NOT redefine" preamble in §1.1 + per-section "Cites memory-record-schema field X" annotations. |
| comparator LLM prompt → bias injection | The §3.2 comparator LLM prompt template is operator-influenceable. A biased prompt could systematically favor one agent's memories. Mitigated by §3.4 confidence-weighted voting (deterministic tiebreak) + §3.5 conflict log for curator review (human audit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | Round table lifecycle (§2 — round_table_open / turn / submit_round_table_result) | mitigate | Atomic-per-turn invariant + state file append-only + idempotent submit (returns 409 on second call) |
| T-46-02 | Information Disclosure | Memory conflict arbitration (§3 — comparator LLM accesses cross-project memories) | mitigate | Confidentiality propagation rule (per PITFALLS §P10 + Phase 45 confidentiality field) — comparator only sees records with confidentiality ≤ current project's level |
| T-46-03 | Denial of Service | Serial execution violation (concurrent get_agent_opinion calls) | mitigate | §4.1 + §5.2 — Hermes dispatcher rejects concurrent calls for same roundId with 429; non-negotiable invariant |
| T-46-04 | Elevation of Privilege | MCP tool naming drift (agent_ prefix creeps back) | mitigate | §4.2 reconciliation table + §5 explicit "STACK §3.2 form" declaration; Phase 51 lint script will cross-check |
| T-46-05 | Repudiation | Conflict log integrity (§3.5 — curator review depends on log accuracy) | mitigate | ConflictRecord schema sealed at submit_round_table_result time + append-only turns + lastUpdatedAt bump |
| T-46-06 | Spoofing | round_id collision (two rounds with same UUID) | mitigate | CC uses UUID v4 (cryptographically random); Hermes validates uniqueness at round_table_open (return 409 on collision) |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-46-SC (supply chain from package installs) is not applicable.
</threat_model>

<verification>
**Phase 46 DESIGN-03 success criteria (from ROADMAP §Phase 46):**

1. SC#1 — Files `02-ROUND-TABLE-PROTOCOL.md` + `round-table-state-schema.yaml` exist (verified by Tasks 1+2)
2. SC#2 — Turn lifecycle fully defined (round_table_open → turn N → submit_round_table_result), including `turn_order` strategy (default round-robin + pluggable, resolves OQ-2) + `round_id` CC-self-generated UUID (resolves OQ-11) (verified by Task 3 §2)
3. SC#3 — Memory conflict arbitration rules documented: comparator LLM pass + scope precedence (session > project > global) + confidence-weighted voting + conflict log (resolves OQ-15, avoids P7) (verified by Task 4 §3)
4. SC#4 — Serial execution constraint explicitly declared (1 panelist 1 turn sequential await, citing MEMORY.md feedback-glm-overload-reduce-concurrency.md global concurrency==1 policy, resolves OQ-8) (verified by Task 4 §4.1)
5. SC#5 — MCP tool naming unified to STACK §3.2 form (no prefix, aligns with existing 9 messaging tools, resolves OQ-9), covers FEATURES §10 borrowable points B1.4 / B2.1 / B2.3 / B4.2 / B6.1 / B7.3 / B8.2 (verified by Task 4 §4.2+§4.4 + Task 5 §5+§6)

**Final deliverable target:** ~800-1200 lines for narrative + ~250 lines round-table-state-schema.yaml (mirrors Phase 44's 1181 lines + Phase 45's 800+ narrative at comparable rigor).

**Methodology audit (self-check before commit):**
- Every lifecycle step cites ARCHITECTURE §5.1 OR STACK §3.2 OR PITFALLS §P7 source (not invented)
- Phase 44 decisions 5/6/7 cited by 决策号, never re-derived
- Phase 45 schema fields (memory_scope, scope, confidence, evidence_chain, confidentiality, fitness_score, persona_sha256) cited by name, never redefined
- camelCase JSON Schema keywords throughout (Phase 45 verification lesson)
- All 7 borrowable points appear in §4.4 audit table + §6 deep-dive
- All 6 OQs appear in §4.3 audit table with resolution status
- MEMORY.md feedback-glm-overload-reduce-concurrency.md cited verbatim in §4.1
- Bilingual: EN structure (headers, schemas, field names) + 中文 prose (rationale, examples) per CLAUDE.md
</verification>

<success_criteria>
**Phase 46 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/46-round-table-protocol/46-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) All 2 deliverable files exist:
   - `.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md` (≥ 800 lines, §0-§7, all OQ + BP coverage, MEMORY.md citation)
   - `.planning/research/v10-orchestrator-design/round-table-state-schema.yaml` (≥ 200 lines, valid JSON Schema draft 2020-12, camelCase keywords, all 9 required fields + $defs)
3. ROADMAP.md Phase 46 entry updated (Plans: 1 plan, plan list with checkmark).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/46-round-table-protocol/46-01-SUMMARY.md` when done. SUMMARY should record:
- Final line counts of both deliverables
- Turn lifecycle 3-step lock (round_table_open → turn N → submit_round_table_result atomic)
- 5 turn_order strategies documented (round-robin default + fixed/llm/matrix/fitness-weighted)
- 6 OQ resolutions (OQ-2 / OQ-5 / OQ-8 / OQ-9 / OQ-11 / OQ-15)
- 7 borrowable points coverage (B1.4 / B2.1 / B2.3 / B4.2 / B6.1 / B7.3 / B8.2)
- Memory conflict arbitration 5-mechanism lock (memory annotation + comparator LLM + scope precedence + confidence voting + conflict log)
- Serial constraint lock (citing MEMORY.md feedback-glm-overload-reduce-concurrency.md verbatim)
- MCP tool naming lock (STACK §3.2 form, no agent_ prefix)
- camelCase JSON Schema keywords confirmed (Phase 45 lesson applied)
- Cross-validation: every section cites ARCHITECTURE §X / STACK §X / PITFALLS §P7 / FEATURES §10 / Phase 45 schema source
</output>

---
phase: 45-agent-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md
  - .planning/research/v10-orchestrator-design/agents-schema.yaml
  - .planning/research/v10-orchestrator-design/memory-record-schema.yaml
autonomous: true
requirements: [DESIGN-02]
granularity: standard

must_haves:
  truths:
    - "File .planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md exists"
    - "File .planning/research/v10-orchestrator-design/agents-schema.yaml exists with 18-field JSON Schema definitions"
    - "File .planning/research/v10-orchestrator-design/memory-record-schema.yaml exists independently with expires_at / verified_at / supersedes_memory_id / confidence / half_life_days / evidence_chain / evidence_operator_ids / status / confidentiality / scope fields"
    - "per-agent memory tier (core / working / archival) is documented with memory.max_records cap + compaction trigger (resolves OQ-7)"
    - "curator _memory_evolution_phase field contract is documented (modeled on v6.0 _feedback_scan_phase, dry-run-by-default + try/except isolation + execution-order, resolves OQ-16)"
    - "15 expert transform mapping table is copied from ARCHITECTURE §2 (SKILL frontmatter → agent YAML per-field deltas)"
    - "6 Open Questions (OQ-1 / OQ-4 / OQ-7 / OQ-13 / OQ-14 / OQ-16) explicitly resolved or explicitly deferred to v11.0"
    - "7 load-bearing pitfalls (P1 / P2 / P4 / P5 / P8 / P10 / P14) each have ≥1 schema field-level mitigation mapped"
  artifacts:
    - path: ".planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md"
      provides: "v10.0 design doc #01 — 18-field agent YAML schema narrative + 2-layer memory schema (agent-profile + memory-record) + per-agent memory tier + curator evolution phase contract + 15-expert transform mapping + OQ/pitfall field-level audit"
      min_lines: 800
      contains: ["18", "memory-record-schema", "core / working / archival", "_memory_evolution_phase", "P1", "P2", "P4", "P5", "P8", "P10", "P14", "OQ-1", "OQ-4", "OQ-7", "OQ-13", "OQ-14", "OQ-16"]
    - path: ".planning/research/v10-orchestrator-design/agents-schema.yaml"
      provides: "Formal JSON Schema (draft 2020-12) for agent YAML — 18 fields with type, description, enum, required, defaults. Consumable by v11.0 PoC Python dataclass generator + Phase 51 lint script."
      min_lines: 200
      contains: ["$schema", "title", "type", "properties", "required", "persona", "memory_scope", "lineage", "evolution_log", "fitness_score"]
    - path: ".planning/research/v10-orchestrator-design/memory-record-schema.yaml"
      provides: "Formal JSON Schema (draft 2020-12) for memory records (independent of agent YAML) — the 10 PITFALLS-mandated fields (expires_at / verified_at / supersedes_memory_id / confidence / half_life_days / evidence_chain / evidence_operator_ids / status / confidentiality / scope)."
      min_lines: 150
      contains: ["expires_at", "verified_at", "supersedes_memory_id", "confidence", "half_life_days", "evidence_chain", "evidence_operator_ids", "status", "confidentiality", "scope"]
  key_links:
    - from: "01-AGENT-REGISTRY-SCHEMA.md"
      to: "00-FIRST-PRINCIPLES.md"
      via: "markdown §reference + 决策号 citation"
      pattern: "00-FIRST-PRINCIPLES|决策 [56]|§2\\.5|§2\\.6"
    - from: "01-AGENT-REGISTRY-SCHEMA.md"
      to: "ARCHITECTURE.md §1 + §2 + §3"
      via: "markdown §reference"
      pattern: "ARCHITECTURE §(1|2|3)"
    - from: "01-AGENT-REGISTRY-SCHEMA.md"
      to: "PITFALLS.md §Pitfall 1|2|4|5|8|10|14"
      via: "markdown §reference for field-level mitigation"
      pattern: "PITFALLS §Pitfall (1|2|4|5|8|10|14)"
    - from: "agents-schema.yaml"
      to: "01-AGENT-REGISTRY-SCHEMA.md §2 (18-field narrative)"
      via: "field name alignment"
      pattern: "^[a-z_]+:"
    - from: "memory-record-schema.yaml"
      to: "01-AGENT-REGISTRY-SCHEMA.md §3 (memory-record narrative)"
      via: "field name alignment"
      pattern: "(expires_at|supersedes_memory_id|confidence|half_life_days|evidence_chain)"
---

<objective>
Create the v10.0 design doc #01 — the **physical carrier for all downstream design doc (02/04/05/06) field references** by producing 3 deliverables:

1. `01-AGENT-REGISTRY-SCHEMA.md` — design narrative (~800-1200 lines, bilingual EN structure + 中文 prose per CLAUDE.md)
2. `agents-schema.yaml` — formal JSON Schema for the 18-field agent YAML
3. `memory-record-schema.yaml` — formal JSON Schema for the 10-field memory record schema (independent from agent YAML)

Purpose: Lock the **2-layer memory schema design** (agent-profile memory vs memory-record schema, per CC-2 cross-cutting finding) and the **7 load-bearing pitfall (P1/P2/P4/P5/P8/P10/P14) field-level mitigations** BEFORE downstream design docs (02/04/05/06) reference these field names. The next 4 design docs will cite this schema's field names directly rather than re-deriving them.

Output: 3 sibling files under `.planning/research/v10-orchestrator-design/`, mirroring Phase 44's `00-FIRST-PRINCIPLES.md` depth/rigor at tighter scope (Phase 44 was 1181 lines; Phase 45 target 800-1200 lines for the narrative + 2 schema files).

This is a DESIGN-ONLY deliverable. Zero code changes (no SKILL.md / Python / plugin / mcp_serve.py edits). Cite Phase 44's locked decisions — do NOT re-derive them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md
@.planning/research/v10-orchestrator-design/STACK.md

<interfaces>
<!-- Phase 44 root-argument anchors (do NOT re-derive; cite by 决策号) -->
<!-- Decision 5 (α agent form: YAML + persona + tools + refs + memory_scope + lineage) -->
<!-- Decision 6 (per-agent memory + curator-driven 自进化) -->
<!-- These two decisions are the load-bearing inputs to the 18-field schema. -->

From .planning/research/v10-orchestrator-design/SUMMARY.md (CC-2 cross-cutting finding):
> 18-field schema has 8 fields directly derived from PITFALLS reverse-inference. But PITFALLS requires 5 memory-record fields NOT in 18-field: expires_at / verified_at / supersedes_memory_id / confidence / half_life_days (P2 stale memory) + evidence_chain / evidence_operator_ids / status="archived|quarantined" (P5/P6) + confidentiality (P10 privacy) + scope: "global|project|session" (P4 finer than ARCHITECTURE shared|per_agent|project_scoped).
> "These fields belong to 'memory record schema', NOT 'agent YAML schema' — 01-AGENT-REGISTRY-SCHEMA.md must explicitly distinguish the 2 layers, otherwise the 18-field schema will be misread as insufficient."

From ARCHITECTURE.md §1.1 (verbatim — 18 fields, the schema source):
| # | Field | Type | Required | Notes |
|---|-------|------|----------|-------|
| 1 | name | string | YES | must match filename stem, regex [a-z0-9_-]+ |
| 2 | description | string | YES | surfaced by agents_list MCP tool |
| 3 | version | string (semver) | YES | agent YAML schema version |
| 4 | persona | multiline | YES | load-bearing: system-prompt fragment (NOT SKILL body) |
| 5 | tools | list[string] | YES | runtime whitelist |
| 6 | memory_scope | enum: shared\|per_agent\|project_scoped | YES | routing convention |
| 7 | lineage | object | YES | derived_from_skill_id, derived_from_repo, transform_date, skill_sha256 |
| 8 | refs | list[string] | NO (default []) | RAG reference docs |
| 9 | tags | list[string] | NO | lowercase hyphenated |
| 10 | expert_id | string | NO | FOUND-08 backward-compat |
| 11 | metrics | list[string] | NO | eval gate dimensions |
| 12 | prerequisites | object | NO | activation conditions (≠ tools) |
| 13 | related_agents | list[string] | NO | collaboration DAG |
| 14 | evolution_log | list[object] | NO | curator-managed, append-only, sha256 chaining |
| 15 | fitness_score | float 0.0-1.0 \| null | NO | curator-computed |
| 16 | platforms | list[string] | NO | OS compat gate |
| 17 | round_table_eligible | bool | NO (default true) | can join round table |
| 18 | default_invocation | enum: mcp_tool\|skill_fallback\|disabled | NO (default mcp_tool) | dispatcher mode |

From ARCHITECTURE.md §3.4 (curator evolution phase source — v6.0 _feedback_scan_phase pattern):
> New phase: `_memory_evolution_phase(start: datetime) -> Dict[str, Any]`
> Pipeline per agent with memory_scope: per_agent and evolution_log: load YAML → walk evolution_log → apply additive deltas → optionally write to mem0 via mem0_conclude scoped to agent_id → returns summary dict (same shape as _feedback_scan_phase).
> Hook into run_curator_review: Insert call right after _feedback_scan_phase(start) in _llm_pass() (lines 2081-2095 and 2207-2221 of agent/curator.py). Wrap in try/except, same failure-isolation pattern as v6.0.

From PITFALLS.md (load-bearing pitfall → required schema field):
- P1 Persona Drift → persona_sha256 + core_memory/archival_memory tiered memory
- P2 Stale Memory → expires_at + verified_at + supersedes_memory_id + confidence + half_life_days (time-decay)
- P4 Cross-Project Leakage → scope: "global\|project\|session" + project_id required + cross-project promotion gate
- P5 Curator Failure → evidence_chain (≥3) + evidence_operator_ids (diversity) + dry-run-first + bias canary
- P8 No Fitness Signal → fitness_battery + fitness_trend.jsonl + model_id 隔离 (fitness_score is the agent-YAML-side carrier)
- P10 Privacy / Data Leakage → confidentiality field + per-project confidentiality propagation
- P14 Schema Migration Breaks Memory Store → schema_version + dry-run migration mode

From SUMMARY.md (Open Questions to resolve in this phase):
- OQ-1: persona version control → diff only (operator git-manages ~/.hermes/agents/), but memory record must carry persona_sha256 snapshot
- OQ-4: fitness_score cold start → null = neutral 0.5 for ordering, UI shows "untested"
- OQ-7: memory.max_records default 500, curator runs compaction pass every N ticks
- OQ-13: agent YAML tools field IS runtime whitelist, enforced by dispatcher (YES — enum-like)
- OQ-14: agents-schema.yaml JSON Schema file YES (this phase produces it)
- OQ-16: _memory_evolution_phase runs AFTER _feedback_scan_phase, independent try/except, dry-run-by-default

From CLAUDE.md (deliverable form):
> SKILL.md 双语(英文 YAML 结构 + metadata 保留英文;description 与正文采用 EN structure + 中文段落/示例)
> refs 以中文为主,关键术语保留英文
> JSON Schema with description, type, enum, required per industry standard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan + write §0 reading guide + §1 schema overview (2-layer architecture + 18-field table + Phase 44 root-argument citation)</name>
  <files>.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md lines 1-100 (frontmatter + §0 + §1.1-§1.3) — pattern for bilingual frontmatter + 阅读指南 + 稳定性 markers
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.5 (决策 5 α agent form 推导) + §2.6 (决策 6 per-agent memory 推导) — root-argument anchors to cite
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.1 (18-field table, lines 29-52) + §1.2 (SKILL compat, lines 54-71) + §1.3 (screenplay minimal example, lines 73-116)
    - .planning/research/v10-orchestrator-design/SUMMARY.md CC-2 (lines 27-29) — 2-layer memory schema mandate
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 1 (lines 31-63) + §Pitfall 2 (lines 64-94) — load-bearing fields driving memory-record schema separation
  </read_first>

  <behavior>
  - Document frontmatter block (status / phase / audience / reading time / stability markers / confidence) mirrors 00-FIRST-PRINCIPLES.md lines 1-9 style
  - §0 阅读指南 exists with chapter map, 3-audience guide (Kai reviewer / Kimi Notion 续聊 / v11.0 PoC implementer), 稳定性 markers
  - §1.1 exists: schema design philosophy — 2-layer architecture (agent-profile YAML vs memory-record schema) declared upfront, citing CC-2 + 决策 5 + 决策 6 from Phase 44 as root argument
  - §1.2 exists: 18-field agent YAML overview table (verbatim copy from ARCHITECTURE §1.1) + per-field "PITFALLS mitigation mapping" annotation column where applicable
  - §1.3 exists: 2-layer diagram (text/ASCII) showing agent YAML ↔ memory-record relationship, citing CC-2 mandate
  - §1.4 exists: roadmap placement — "this doc is the physical carrier for 02/04/05/06 field references" + "decisions 5/6 are LOCKED in Phase 44, this doc only specifies the schema"
  - Total Task 1 length ≥ 200 lines
  </behavior>

  <action>
  Create file `.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md`. Write frontmatter + §0 + §1.

  For §1.1 (design philosophy, ~50 lines): Open with "本文档定义 v10.0 agent registry 的 2-layer memory schema —— 上层 agent-profile YAML(18 字段)锁定身份与工具白名单;下层 memory-record schema(10 字段)锁定跨项目记忆条目的不变量。" Cite Phase 44 §2.5 (决策 5) + §2.6 (决策 6) as the root arguments for "agent 是有持久身份的实体" and "agent 随项目越多越有经验"; do NOT re-derive. Cite SUMMARY.md CC-2 verbatim — the 2-layer split is mandated because PITFALLS requires fields (expires_at / verified_at / supersedes_memory_id / confidence / half_life_days / evidence_chain / evidence_operator_ids / status / confidentiality / scope) that semantically belong to memory records, not agent identity. Mark each Phase 44 决策 anchor as `root-arg-决策-N` so downstream docs can reference.

  For §1.2 (18-field table, ~80 lines): Copy the ARCHITECTURE §1.1 table verbatim (18 rows × columns | # | Field | Type | Required | Source | Purpose |), then add a 7th column "PITFALLS mitigation" populated only for fields that have a direct pitfall mapping:
    - field 4 (persona) → P1 (persona drift)
    - field 6 (memory_scope) → P4 (cross-project leakage)
    - field 7 (lineage) → (operator ownership; aligns with §3.1 "operator-owned")
    - field 14 (evolution_log) → P5 (curator failure modes — tamper-evident sha256 chain)
    - field 15 (fitness_score) → P8 (no fitness signal)
  Fields without a direct pitfall mapping get "—" in the 7th column. After the table, a short paragraph noting "this table is the authoritative 18-field definition; agents-schema.yaml (Task 2) is the machine-readable JSON Schema projection."

  For §1.3 (2-layer diagram, ~40 lines): ASCII diagram showing:
  ```
  ┌─────────────────────────────────────────────┐
  │ Layer 1: agent-profile YAML (18 fields)     │
  │ ~/.hermes/agents/{name}.agent.yaml          │
  │ - identity (name, persona, tools, lineage)  │
  │ - runtime knobs (memory_scope, default_*)   │
  │ - curator-managed (evolution_log, fitness)  │
  └──────────────────────┬──────────────────────┘
                         │ memory_scope routes
                         │ records by agent_id
                         ▼
  ┌─────────────────────────────────────────────┐
  │ Layer 2: memory-record schema (10 fields)   │
  │ mem0 backend per-agent namespace            │
  │ - time-decay (expires_at, half_life_days)   │
  │ - provenance (evidence_chain, evidence_*)   │
  │ - scope isolation (scope, project_id, conf) │
  │ - lifecycle (status, verified_at, supersede)│
  └─────────────────────────────────────────────┘
  ```
  Below diagram: 1 paragraph citing CC-2 ("18-field has 8 fields from PITFALLS reverse-inference; the OTHER 10 fields belong to memory-record schema — explicit 2-layer separation prevents the misread '18-field schema is insufficient'").

  For §1.4 (roadmap placement, ~30 lines): "本文档是 02/04/05/06 的字段引用物理载体" + explicit list of what downstream docs will cite (e.g. "02-ROUND-TABLE-PROTOCOL.md cites memory_scope + scope + confidentiality for conflict arbitration"; "04-MIGRATION-PATH.md cites lineage + default_invocation + schema_version for transform rules"). State Phase 44 决策 5 + 决策 6 are LOCKED — this doc only specifies the schema, does not re-derive.

  Length target: ~200-250 lines for Task 1.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 200 {exit 0} {exit 1}' && grep -q "## §0" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "## §1" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "决策 5" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "决策 6" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "CC-2" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -c "memory-record" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - File created with bilingual frontmatter (status / phase / audience / reading time / stability / confidence)
  - §0 阅读指南 exists with chapter map + 3-audience guide
  - §1.1 design philosophy declares 2-layer memory schema + cites Phase 44 决策 5 + 决策 6 + CC-2
  - §1.2 has 18-field table (verbatim from ARCHITECTURE §1.1) + 7th "PITFALLS mitigation" column
  - §1.3 has 2-layer ASCII diagram + CC-2 verbatim citation
  - §1.4 has roadmap placement + downstream citation list
  - Total doc length ≥ 200 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Write agents-schema.yaml (18-field JSON Schema) + memory-record-schema.yaml (10-field JSON Schema)</name>
  <files>.planning/research/v10-orchestrator-design/agents-schema.yaml, .planning/research/v10-orchestrator-design/memory-record-schema.yaml</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.1 (18-field source — already in context from Task 1)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.3 lines 73-116 (screenplay minimal example — used as validation fixture for agents-schema.yaml)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 1 (lines 31-63) + §Pitfall 2 (lines 64-94) + §Pitfall 4 (lines 126-161) + §Pitfall 5 (lines 162-199) + §Pitfall 8 (lines 267-300) + §Pitfall 10 (lines 337-375) + §Pitfall 14 (lines 438-457) — field-level mitigations driving memory-record-schema.yaml
    - .planning/research/v10-orchestrator-design/SUMMARY.md Open Questions table (lines 65-82) — OQ-1 / OQ-4 / OQ-7 / OQ-13 / OQ-14 / OQ-16 to resolve in schema fields
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 (Pydantic ToolAnnotations pattern — schema discipline reference)
  </read_first>

  <behavior>
  - agents-schema.yaml exists as valid JSON Schema (draft 2020-12), machine-parseable
  - agents-schema.yaml has all 18 fields with: type, description (bilingual allowed in description), enum where applicable, required array, defaults where specified
  - agents-schema.yaml uses `$id`, `$schema`, `title` header conventions
  - agents-schema.yaml enforces regex on `name` (^[a-z0-9_-]+$), semver on `version`, range on `fitness_score` (0.0-1.0)
  - agents-schema.yaml resolves OQ-13: `tools` field is list of enum strings (the runtime whitelist)
  - agents-schema.yaml resolves OQ-14: it IS the JSON Schema file
  - agents-schema.yaml includes a top-level `$defs` block for reusable shapes (lineage object, evolution_log entry)
  - memory-record-schema.yaml exists as INDEPENDENT valid JSON Schema (not a sub-schema of agents-schema)
  - memory-record-schema.yaml has all 10 mandated fields: expires_at / verified_at / supersedes_memory_id / confidence / half_life_days / evidence_chain / evidence_operator_ids / status / confidentiality / scope
  - memory-record-schema.yaml enforces: status enum {active, archived, quarantined, superseded}, scope enum {global, project, session}, confidentiality enum {public, internal, confidential, restricted}, confidence range 0.0-1.0, half_life_days integer ≥1, evidence_chain minItems 3 (per P5 mitigation), evidence_operator_ids minItems 1
  - memory-record-schema.yaml resolves OQ-1: persona_sha256 field (string, hex 64-char) carried in every memory record
  - memory-record-schema.yaml resolves OQ-7: max_records field at the agent-profile-collection level (not per-record) — document as "applied at agent tier level, see §3 of design doc"
  - memory-record-schema.yaml resolves OQ-4: confidence field with default 0.5 (neutral), agents-schema.yaml fitness_score default null maps to 0.5 in ordering
  - Both files include YAML comments citing the PITFALLS / OQ source for each load-bearing field
  </behavior>

  <action>
  Write `.planning/research/v10-orchestrator-design/agents-schema.yaml` first.

  Structure:
  ```yaml
  $schema: "https://json-schema.org/draft/2020-12/schema"
  $id: "https://hermes-agent.local/v10/agents-schema.yaml"
  title: "Hermes Agent Registry YAML Schema (v10.0)"
  description: |
    18-field schema for ~/.hermes/agents/{name}.agent.yaml.
    Source: ARCHITECTURE.md §1.1 + Phase 44 决策 5 + 决策 6.
    Consumed by v11.0 PoC Python dataclass generator + Phase 51 lint script.
  type: object
  required: [name, description, version, persona, tools, memory_scope, lineage]
  additionalProperties: false
  properties:
    name:
      type: string
      pattern: "^[a-z0-9_-]+$"
      description: "Primary identifier; must match filename stem."
    description:
      type: string
      description: "One-line summary surfaced by agents_list MCP tool."
    version:
      type: string
      pattern: "^[0-9]+\\.[0-9]+\\.[0-9]+$"  # semver
      description: "Schema version of agent YAML; bumped when fields added."
    persona:
      type: string
      description: |
        System-prompt fragment. Load-bearing difference from SKILL:
        SKILL body is user-message; persona is system-prompt.
        # PITFALLS §P1 mitigation: persona drift — see persona_sha256 in evolution_log entries.
    tools:
      type: array
      items:
        type: string
        # OQ-13: runtime whitelist, enforced by dispatcher
      description: "Runtime tool whitelist. Distinct from prerequisites.tools (activation conditions)."
    memory_scope:
      type: string
      enum: [shared, per_agent, project_scoped]
      default: per_agent
      description: |
        mem0 routing convention.
        # PITFALLS §P4 mitigation: cross-project leakage.
    lineage:
      $ref: "#/$defs/Lineage"
    refs:
      type: array
      items: { type: string }
      default: []
    tags:
      type: array
      items: { type: string, pattern: "^[a-z0-9-]+$" }
    expert_id:
      type: string
      description: "FOUND-08 backward-compat anchor; copied verbatim from SKILL metadata.hermes.expert_id."
    metrics:
      type: array
      items: { type: string }
    prerequisites:
      $ref: "#/$defs/Prerequisites"
    related_agents:
      type: array
      items: { type: string }
    evolution_log:
      type: array
      items: { $ref: "#/$defs/EvolutionLogEntry" }
      default: []
      description: |
        Curator-managed, append-only, sha256 chaining.
        # PITFALLS §P5 mitigation: curator failure modes — tamper-evident chain.
    fitness_score:
      type: [number, "null"]
      minimum: 0.0
      maximum: 1.0
      default: null
      description: |
        Curator-computed rolling score.
        # PITFALLS §P8 mitigation: no fitness signal.
        # OQ-4 resolution: null = neutral 0.5 for ordering, UI shows "untested".
    platforms:
      type: array
      items: { type: string, enum: [linux, macos, windows, termux] }
      default: [linux, macos, windows]
    round_table_eligible:
      type: boolean
      default: true
    default_invocation:
      type: string
      enum: [mcp_tool, skill_fallback, disabled]
      default: mcp_tool
  $defs:
    Lineage: { ... derived_from_skill_id, derived_from_repo, transform_date, transform_notes, skill_sha256 ... }
    Prerequisites: { ... tools, skills, env ... }
    EvolutionLogEntry: { ... ts, sha256, diff_summary, fitness_delta, trigger, persona_sha256 (P1 mitigation) ... }
  ```

  Include comments inline citing ARCHITECTURE §1.1 + PITFALLS §P-XXX + OQ-XX for each load-bearing field. Total ~200-250 lines.

  Then write `.planning/research/v10-orchestrator-design/memory-record-schema.yaml`.

  Structure:
  ```yaml
  $schema: "https://json-schema.org/draft/2020-12/schema"
  $id: "https://hermes-agent.local/v10/memory-record-schema.yaml"
  title: "Hermes Memory Record Schema (v10.0)"
  description: |
    Independent schema for memory records stored in mem0 backend (per-agent namespace).
    NOT a sub-schema of agents-schema.yaml — see design doc §1.3 for the 2-layer rationale.
    Source: SUMMARY.md CC-2 + PITFALLS §P1/P2/P4/P5/P6/P8/P10/P14.
  type: object
  required: [agent_id, scope, status, confidence, evidence_chain, created_at, persona_sha256]
  properties:
    record_id: { type: string, format: uuid }
    agent_id:
      type: string
      pattern: "^[a-z0-9_-]+$"
      description: "Owner agent. Routes record to per-agent mem0 namespace."
    scope:
      type: string
      enum: [global, project, session]
      description: |
        # PITFALLS §P4 mitigation: 3-tier finer than agents-schema memory_scope (shared|per_agent|project_scoped).
        # global = recalled in any context; project = only when project_id matches; session = ephemeral.
    project_id:
      type: [string, "null"]
      description: "Required when scope=project; null otherwise."
    session_id:
      type: [string, "null"]
      description: "Required when scope=session; null otherwise."
    status:
      type: string
      enum: [active, archived, quarantined, superseded]
      default: active
      description: |
        # PITFALLS §P5/P6 mitigation: quarantined = under bias-canary review; superseded = replaced by supersedes_memory_id chain.
    confidence:
      type: number
      minimum: 0.0
      maximum: 1.0
      default: 0.5
      description: |
        Curator-assigned. Time-decays per half_life_days.
        # PITFALLS §P2 mitigation + OQ-4 (null fitness = neutral 0.5).
    half_life_days:
      type: integer
      minimum: 1
      default: 90
      description: |
        Days for confidence to halve.
        # PITFALLS §P2 mitigation: stale memory time-decay.
    expires_at:
      type: string
      format: date-time
      description: |
        Hard expiry. After this timestamp, status auto-flips to archived.
        # PITFALLS §P2 mitigation.
    verified_at:
      type: string
      format: date-time
      description: "Last human/curator verification timestamp."
    supersedes_memory_id:
      type: [string, "null"]
      format: uuid
      description: |
        Points to the record this one replaces. Forms supersession chain.
        # PITFALLS §P2 mitigation.
    evidence_chain:
      type: array
      minItems: 3
      items:
        type: object
        properties:
          source_type: { type: string, enum: [feedback, eval_gate, operator_edit, round_table, external] }
          source_id: { type: string }
          timestamp: { type: string, format: date-time }
          excerpt: { type: string }
      description: |
        ≥3 independent sources required before curator promotes a memory.
        # PITFALLS §P5 mitigation: prevents hallucinated-writes.
    evidence_operator_ids:
      type: array
      minItems: 1
      items: { type: string }
      description: |
        Operators who contributed evidence. Diversity check prevents single-operator bias.
        # PITFALLS §P5 mitigation: bias-amplification.
    confidentiality:
      type: string
      enum: [public, internal, confidential, restricted]
      default: internal
      description: |
        Propagation rule across projects/sessions.
        # PITFALLS §P10 mitigation: privacy / data leakage.
    persona_sha256:
      type: string
      pattern: "^[a-f0-9]{64}$"
      description: |
        Snapshot of agent persona hash when this record was written.
        # PITFALLS §P1 mitigation: persona drift detection (if persona_sha256 differs at recall time, flag for re-verification).
    created_at: { type: string, format: date-time }
    last_recalled_at: { type: [string, "null"], format: date-time }
    recall_count: { type: integer, minimum: 0, default: 0 }
    schema_version:
      type: string
      default: "1.0.0"
      description: |
        # PITFALLS §P14 mitigation: schema migration — dry-run migration mode keyed off this version.
    ```

  Include YAML comments citing the PITFALLS source + OQ resolution for each load-bearing field. Total ~180-220 lines.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/agents-schema.yaml && test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/memory-record-schema.yaml && python3 -c "import yaml,sys; yaml.safe_load(open('/data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/agents-schema.yaml')); yaml.safe_load(open('/data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/memory-record-schema.yaml')); print('YAML OK')" && for f in agents-schema.yaml memory-record-schema.yaml; do echo "--- $f ---"; grep -c "^  [a-z_]*:" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/$f; done && for term in expires_at verified_at supersedes_memory_id confidence half_life_days evidence_chain evidence_operator_ids status confidentiality scope; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/memory-record-schema.yaml || { echo "MISSING: $term"; exit 1; }; done && for term in name description version persona tools memory_scope lineage evolution_log fitness_score default_invocation; do grep -q "^  $term:" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/agents-schema.yaml || { echo "MISSING: $term"; exit 1; }; done && grep -c "PITFALLS §P" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/memory-record-schema.yaml | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - agents-schema.yaml exists, valid YAML, has all 18 fields with type/description/enum/required
  - agents-schema.yaml has $defs for Lineage / Prerequisites / EvolutionLogEntry
  - agents-schema.yaml name field has regex constraint; fitness_score has 0.0-1.0 range; memory_scope + default_invocation have enums
  - memory-record-schema.yaml exists, valid YAML, INDEPENDENT ($id distinct from agents-schema)
  - memory-record-schema.yaml has all 10 mandated fields (expires_at / verified_at / supersedes_memory_id / confidence / half_life_days / evidence_chain / evidence_operator_ids / status / confidentiality / scope)
  - memory-record-schema.yaml enforces enum on status/scope/confidentiality, range on confidence/half_life_days, minItems on evidence_chain
  - memory-record-schema.yaml has persona_sha256 field (OQ-1 resolution)
  - Both files have inline YAML comments citing PITFALLS source per field
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §2 (18-field narrative per field, ~30-50 lines each) + §3 (memory-record schema narrative, 10 fields)</name>
  <files>.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.1 lines 29-52 (18-field Purpose column — already in context)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.3 lines 73-116 (screenplay minimal example — embed as §2 illustration)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 1/2/4/5/8/10/14 mitigation sections (specifically the "mitigation N" sub-bullets driving field-level design)
    - .planning/research/v10-orchestrator-design/SUMMARY.md lines 27-29 (CC-2 — the 10 memory-record fields to cover in §3)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §3.1 row 8 (YAML 作 prompt dump rejection — referenced in §2.4 persona field)
  </read_first>

  <behavior>
  - §2.0 子节存在:声明 §2 是 §1.2 18-field 表的逐字段 narrative expansion,cross-reference to agents-schema.yaml
  - §2.1 - §2.18 子节存在:18 个 H3 subsections, each ~30-50 lines, covering one field
  - Each field narrative has 4 sub-blocks: (a) Type + constraints summary (b) Source + Purpose (from ARCHITECTURE §1.1) (c) PITFALLS mitigation (if any) — explicit field-level mechanism (d) Example value (from §1.3 screenplay YAML or invented for clarity)
  - §2.4 (persona) explicitly cites 00-FIRST-PRINCIPLES §3.1 row 8 (YAML-not-prompt-dump rejection) — persona is system-prompt fragment, not SKILL body copy-paste
  - §2.14 (evolution_log) cites PITFALLS §P5 mitigation 5 (dry-run-first) + Phase 44 决策 6 (curator-driven self-evolution)
  - §3.0 子节存在:声明 memory-record schema 是独立 layer 2,不是 agents-schema 的 sub-schema;引用 CC-2
  - §3.1 - §3.10 子节存在:10 个 H3 subsections for the 10 mandated memory-record fields
  - Each memory-record field narrative has 4 sub-blocks parallel to §2: (a) Type + constraints (b) PITFALLS source (P1/P2/P4/P5/P6/P8/P10/P14) (c) Mechanism — how this field mitigates the pitfall (d) Example
  - §3.11 子节存在:persona_sha256 field (OQ-1 resolution) — every memory record carries a persona hash snapshot for drift detection
  - §3.12 子节存在:schema_version field (P14 mitigation — dry-run migration mode)
  - Total Task 3 length ≥ 400 lines (§2 ~280 + §3 ~150)
  </behavior>

  <action>
  Append §2 + §3 to `.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md`.

  **§2 — 18-field narrative expansion (per-field deep dive)**

  §2.0 (~20 lines): "本节逐字段展开 §1.2 表,聚焦于 PITFALLS 字段级缓解机制。机器可读 schema 见 agents-schema.yaml。"

  §2.1 through §2.18 (each ~30-50 lines, total ~260 lines): For each of the 18 fields, write H3 subsection with the 4-block scaffold:
  ```
  ### §2.N — `field_name`

  **Type:** [from agents-schema.yaml]
  **Constraints:** [regex / enum / range]
  **Required:** [YES/NO + default]
  **Source:** [SKILL frontmatter / NEW — derived from X]

  **Purpose:**
  [from ARCHITECTURE §1.1 Purpose column, expanded if needed]

  **PITFALLS mitigation:**
  [If applicable: explicit field-level mechanism. Example for §2.4 persona:
   "P1 persona drift: persona_sha256 carried in every evolution_log entry (§2.14) and every memory record (§3.11) — drift detected when recall-time persona hash ≠ write-time hash. 00-FIRST-PRINCIPLES §3.1 row 8 (YAML-not-prompt-dump rejection) prevents persona from being a SKILL body copy-paste that would drift via SKILL edits."]

  **Example:**
  [YAML snippet from §1.3 screenplay or invented]
  ```

  Per-field load-bearing content guidance:
  - §2.4 (persona): Cite P1 + §3.1 row 8. Show first-person system-prompt fragment (not SKILL body). Reference the persona_sha256 invariant that travels in evolution_log + memory-record.
  - §2.6 (memory_scope): Cite P4 + 3-tier (shared/per_agent/project_scoped) vs the memory-record scope field (3-tier global/project/session — finer). Show how agents-schema memory_scope=per_agent routes records to agent_id namespace, while memory-record scope governs cross-project visibility.
  - §2.7 (lineage): Cite "operator ownership" + skill_sha256 for drift detection. Explicit: derived_from_skill_id, derived_from_repo, transform_date, transform_notes, skill_sha256.
  - §2.14 (evolution_log): Cite P5 mitigation 5 (dry-run-first) + tamper-evident sha256 chain (same pattern as v6.0 agent/curator_audit.py). Each entry: {ts, sha256, diff_summary, fitness_delta, trigger, persona_sha256}. Explicitly mark "do not hand-edit" — curator-managed.
  - §2.15 (fitness_score): Cite P8 + OQ-4 (null = neutral 0.5 for ordering, UI shows "untested"). Not operator-set.
  - §2.18 (default_invocation): Show 3 enum values + when each applies (mcp_tool = round table ready; skill_fallback = additive transition per FOUND-08; disabled = transform-in-progress).

  Other fields (§2.1 name / §2.2 description / §2.3 version / §2.5 tools / §2.8 refs / §2.9 tags / §2.10 expert_id / §2.11 metrics / §2.12 prerequisites / §2.13 related_agents / §2.16 platforms / §2.17 round_table_eligible) get shorter treatments (~20-30 lines each), focused on type/source/purpose with "—" for PITFALLS mitigation when none.

  **§3 — Memory-record schema narrative (Layer 2, 10 mandated fields)**

  §3.0 (~30 lines): "memory-record schema 是独立 layer 2 (CC-2 mandate)。每条记录存于 mem0 backend 的 per-agent namespace,携带 10 个不变量字段 + persona_sha256 快照 + schema_version。机器可读 schema 见 memory-record-schema.yaml。"

  §3.1 through §3.10 (each ~15-20 lines, total ~180 lines): For each of the 10 fields (expires_at / verified_at / supersedes_memory_id / confidence / half_life_days / evidence_chain / evidence_operator_ids / status / confidentiality / scope), write H3 subsection with 4-block scaffold parallel to §2:
  - Type + constraints
  - PITFALLS source (P1/P2/P4/P5/P6/P8/P10/P14)
  - Mechanism — explicit field-level mitigation
  - Example value (YAML)

  §3.11 (~15 lines): persona_sha256 — OQ-1 resolution (snapshot at write time, drift detection at recall time).

  §3.12 (~15 lines): schema_version — P14 mitigation (dry-run migration mode keyed off this).

  Length target: ~400-450 lines for Task 3.
  </action>

  <verify>
    <automated>grep -c "^### §2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 18 {exit 0} {exit 1}' && grep -c "^### §3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 12 {exit 0} {exit 1}' && for term in "§2.4" "§2.6" "§2.7" "§2.14" "§2.15" "§2.18" "§3.1" "expires_at" "verified_at" "supersedes_memory_id" "confidence" "half_life_days" "evidence_chain" "evidence_operator_ids" "status" "confidentiality" "scope" "persona_sha256" "schema_version"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "PITFALLS §P" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 600 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §2 exists with §2.0 声明 + §2.1-§2.18 (18 H3 subsections, one per field)
  - Each §2.N has 4-block scaffold (Type / Purpose / PITFALLS mitigation / Example)
  - §2.4 persona explicitly cites §3.1 row 8 rejection + persona_sha256 invariant
  - §2.14 evolution_log cites P5 dry-run-first + sha256 chain pattern from v6.0
  - §2.15 fitness_score cites OQ-4 (null = neutral 0.5) + P8
  - §3 exists with §3.0 声明 + §3.1-§3.12 (10 mandated fields + persona_sha256 + schema_version)
  - Each §3.N has 4-block scaffold with explicit PITFALLS source + mechanism
  - Total doc length now ≥ 600 lines (cumulative with Task 1)
  </done>
</task>

<task type="auto">
  <name>Task 4: Write §4 (per-agent memory tier: core / working / archival + max_records + compaction) + §5 (curator _memory_evolution_phase contract)</name>
  <files>.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 3 (lines 95-125, scoped retrieval perf + max_records mitigation) + §Pitfall 9 (lines 301-336, memory size growth + compaction)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §3.4 (lines 225-283, curator _memory_evolution_phase source — already partially read)
    - .planning/research/v10-orchestrator-design/SUMMARY.md OQ-7 (max_records default 500) + OQ-16 (_memory_evolution_phase execution order) lines
    - /data/workspace/hermes-agent/agent/curator.py lines 2081-2095 + 2207-2221 (the actual _llm_pass hooks where _memory_evolution_phase inserts — read just those ranges)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.6 (决策 6 per-agent memory derivation) — already in context
  </read_first>

  <behavior>
  - §4.0 子节存在:声明 per-agent memory tier 是 v10.0 加严(PITFALLS §P3 + §P9 要求 schema 显式分层,不止一个 memory namespace)
  - §4.1 子节存在:core tier 定义(常驻 context,persona fragments + immutable operator facts,max_records ~50)
  - §4.2 子节存在:working tier 定义(活跃项目记忆,max_records ~200,默认 retrieval target)
  - §4.3 子节存在:archival tier 定义(历史项目 + 低 confidence,max_records ~250,只在 working miss 时检索)
  - §4.4 子节存在:memory.max_records 上限 = 500(aggregate of 3 tiers) + 每独立 tier cap(OQ-7 resolution)
  - §4.5 子节存在:compaction 触发条件(curator 每 N tick 跑 compaction pass;trigger when tier cap > 90% OR half_life_days expired OR status=superseded) + compaction 行为(demote working→archival, archive expired, drop lowest-confidence)
  - §4.6 子节存在:ASCII tier diagram showing 3-tier flow
  - §5.0 子节存在:声明 _memory_evolution_phase 是 v6.0 _feedback_scan_phase 的 additive extension(类比 additive plugin pattern from Phase 44 §1.3 paradigm shift)
  - §5.1 子节存在:execution-order contract(runs AFTER _feedback_scan_phase in _llm_pass;never before;never in parallel)
  - §5.2 子节存在:dry-run-by-default contract(new phase defaults to dry-run=true;mutation requires explicit operator flag — same discipline as v6.0 FOUND-06)
  - §5.3 子节存在:try/except isolation contract(wrap _memory_evolution_phase call in try/except;on failure log + continue;never crash curator — same as _feedback_scan_phase)
  - §5.4 子节存在:per-agent iteration contract(walks ~/.hermes/agents/*.agent.yaml,filter memory_scope==per_agent,applies per agent)
  - §5.5 子节存在:mem0_conclude integration(writes are scoped to agent_id via _scoped_agent_id contextvars pattern from ARCHITECTURE §3.3)
  - §5.6 子节存在:success criteria for the phase(returns Dict[str, Any] with summary shape parallel to _feedback_scan_phase return)
  - §4 + §5 combined length ≥ 250 lines
  </behavior>

  <action>
  Append §4 + §5 to `.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md`.

  **§4 — Per-agent memory tier (core / working / archival)**

  §4.0 (~30 lines): "v10.0 把 per-agent memory 显式分为 3 tier(core / working / archival),不是单一 namespace。这是 PITFALLS §P3 + §P9 加严 —— 单层 mem0 namespace 在 500 records 时会因 HNSW 全局图退化,分层后 core tier 始终 < 50 records 保证 O(ms) retrieval。Layer-2 memory-record-schema 的 status field(active/archived)对应 working/archival tier;core tier 不是独立 mem0 namespace,而是常驻 agent persona 的 immutable operator facts(写死在 YAML + persona_sha256 锁定)。"

  §4.1 core tier (~30 lines):
  - Location: agent YAML persona fragment + lineage.operator_facts (if any) + immutable system facts (e.g. "agent is the Screenplay Expert")
  - max_records: ~50 (effectively bounded by persona length)
  - Retrieval: always loaded into system prompt; no mem0 query
  - Writes: operator-only (YAML edits); curator cannot mutate
  - PITFALLS mitigation: P1 persona drift — persona_sha256 invariant

  §4.2 working tier (~30 lines):
  - Location: mem0 backend, agent_id namespace, status=active records
  - max_records: ~200 (default retrieval target)
  - Retrieval: every get_agent_opinion call queries this tier
  - Writes: curator _memory_evolution_phase;round table conclusions with project_id
  - PITFALLS mitigation: P2 stale memory (expires_at + half_life_days);P4 cross-project (scope=project + project_id required)

  §4.3 archival tier (~30 lines):
  - Location: mem0 backend, agent_id namespace, status=archived records
  - max_records: ~250
  - Retrieval: only on working-tier miss OR explicit operator query
  - Writes: compaction pass demotes working→archival
  - PITFALLS mitigation: P9 size growth (graceful degradation, not data loss)

  §4.4 max_records cap (~25 lines): "aggregate 3-tier cap = 500 records per agent(OQ-7 resolution)。core 不计 mem0 backend(是 YAML),working + archival 合计 ≤ 500。当 working+archival > 450(90%)时触发 compaction pass。" Cite PITFALLS §P3 mitigation 3 + §P9.

  §4.5 compaction trigger + behavior (~40 lines): markdown table with columns | Trigger | Threshold | Action |
  - working+archival > 90% cap → demote lowest-confidence working records to archival
  - record.expires_at < now → status auto-flip to archived
  - record.half_life_days decay drops confidence < 0.2 → demote to archival
  - record.status = superseded → demote to archival (keep for audit)
  - archival tier > 250 → drop lowest-confidence archived records (log to curator_audit)

  §4.6 ASCII tier diagram (~30 lines): showing the 3-tier flow with retrieval path + write path + compaction flow.

  **§5 — Curator `_memory_evolution_phase` field contract**

  §5.0 (~20 lines): "_memory_evolution_phase 是 v6.0 _feedback_scan_phase 的 additive extension —— 不替换、不并行,在 _feedback_scan_phase 之后串行插入。Pattern 来自 v6.0 FOUND-06 additive phase discipline + Phase 44 §1.3 paradigm shift(agent 是 Hermes-side 独立实体,curator 是其自进化机制)。"

  §5.1 (~25 lines): **Execution order.** Cite agent/curator.py lines 2081-2095 (_feedback_scan_phase hook in _llm_pass). The new _memory_evolution_phase(start) call inserts IMMEDIATELY AFTER _feedback_scan_phase(start) in the same _llm_pass block. Never before. Never in parallel. Show pseudo-code:
  ```
  def _llm_pass(self, start: datetime):
      ...
      try:
          feedback_summary = _feedback_scan_phase(start)  # v6.0
      except Exception as exc:
          log_warning(...)
      try:
          memory_summary = _memory_evolution_phase(start)  # v10.0 NEW
      except Exception as exc:
          log_warning(...)  # never crash curator
      ...
  ```

  §5.2 (~25 lines): **Dry-run-by-default.** Defaults `dry_run=True`. Memory writes (mem0_conclude calls) are skipped in dry-run mode; only the summary dict is returned. Mutation requires explicit operator flag `HERMES_CURATOR_MEMORY_EVOLUTION_LIVE=1` env var or `--live-memory-evolution` CLI flag. Cite v6.0 FOUND-06 discipline + PITFALLS §P5 mitigation 5 (curator failure modes — operator retains ownership of memory state).

  §5.3 (~20 lines): **try/except isolation.** Wrap entire phase body in try/except. On Exception: log warning with stack trace to errors.log + curator.log + continue. Never propagate up to crash run_curator_review. Same isolation boundary as _feedback_scan_phase.

  §5.4 (~30 lines): **Per-agent iteration.** Walks `~/.hermes/agents/*.agent.yaml`. For each agent with `memory_scope == "per_agent"`:
  1. Load YAML + evolution_log
  2. Walk evolution_log since last phase run (start param)
  3. For each entry with trigger=feedback or trigger=eval_gate:
     - Validate evidence_chain (≥3 sources per P5 mitigation)
     - If valid: synthesize memory fact via LLM consolidation (reuse _feedback_scan_phase LLM helper)
     - Optional write: mem0_conclude scoped to agent_id via _scoped_agent_id contextvars
  4. Append new evolution_log entry with sha256 chain
  5. Update fitness_score if eval_gate evidence present

  §5.5 (~20 lines): **mem0_conclude integration.** Cite ARCHITECTURE §3.3 _scoped_agent_id contextvars pattern. The phase sets provider._scoped_agent_id = agent_name before mem0_conclude call; resets in finally block. Thread-safe via contextvars (matches agent/tool_executor.py ThreadPoolExecutor pattern).

  §5.6 (~25 lines): **Return shape.** Returns `Dict[str, Any]` parallel to _feedback_scan_phase:
  ```python
  {
      "agents_walked": int,
      "memory_facts_synthesized": int,
      "memory_facts_written": int,  # 0 in dry-run
      "evidence_chain_failures": int,
      "fitness_score_updates": int,
      "errors": [str, ...],  # per-agent error logs (if any)
      "duration_seconds": float,
  }
  ```

  Length target: ~250-300 lines for §4+§5.
  </action>

  <verify>
    <automated>grep -q "## §4" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "## §5" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -c "^### §4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -c "^### §5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 6 {exit 0} {exit 1}' && for term in "core" "working" "archival" "max_records" "compaction" "_memory_evolution_phase" "_feedback_scan_phase" "dry-run" "try/except" "contextvars" "_scoped_agent_id"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "500\|OQ-7" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 2 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 800 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §4 exists with §4.0 声明 + §4.1-§4.6 (core/working/archival tier definitions + max_records cap + compaction triggers + ASCII diagram)
  - §4.4 explicitly resolves OQ-7 (max_records = 500 aggregate)
  - §5 exists with §5.0-§5.6 (execution order + dry-run + isolation + iteration + mem0 integration + return shape)
  - §5 resolves OQ-16 (_memory_evolution_phase runs AFTER _feedback_scan_phase)
  - §5 cites v6.0 FOUND-06 + PITFALLS §P5 + ARCHITECTURE §3.3 + §3.4
  - Total doc length now ≥ 800 lines (cumulative with Tasks 1+3)
  </done>
</task>

<task type="auto">
  <name>Task 5: Write §6 (15-expert transform mapping table copied from ARCHITECTURE §2) + §7 (OQ + pitfall audit table) + §8 (downstream citation guide + summary)</name>
  <files>.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §2 lines 120-149 (15-expert transform mapping — VERBATIM copy source)
    - .planning/research/v10-orchestrator-design/SUMMARY.md Open Questions table lines 65-82 (the 6 OQs to resolve/defer: OQ-1 / OQ-4 / OQ-7 / OQ-13 / OQ-14 / OQ-16)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 1/2/4/5/8/10/14 mitigation sub-bullets (for the pitfall audit table)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §5 (downstream citation cards — pattern reference, but here §8 is for THIS doc's downstream consumers)
    - .planning/ROADMAP.md §Phase 46/49/50/51 (downstream docs that consume this schema — for §8 citation guide)
  </read_first>

  <behavior>
  - §6.0 子节存在:声明 §6 是 ARCHITECTURE §2 的 verbatim copy + cross-reference to §2 of this doc for field-level deltas
  - §6.1 子节存在:5-field transform pattern 声明(COPY / DROP / REWRITE / FLATTEN / DERIVE / INITIALIZE)verbatim from ARCHITECTURE §2
  - §6.2 子节存在:15-expert delta table verbatim from ARCHITECTURE §2 (15 rows × 5 columns: Expert / tools / persona framing / refs count / related_agents count)
  - §6.3 子节存在:FOUND-08 preservation rule(15 expert_id 值 verbatim copy + additive transition)
  - §7.0 子节存在:声明 §7 是 SC#5 的物理载体(6 OQs + 7 pitfalls audit)
  - §7.1 子节存在:Open Questions audit table — 6 rows (OQ-1 / OQ-4 / OQ-7 / OQ-13 / OQ-14 / OQ-16),columns: OQ# / 问题 / 倾向性结论 (from SUMMARY) / 本文档章节解决 / 是否 defer 到 v11.0
  - §7.2 子节存在:Pitfalls audit table — 7 rows (P1/P2/P4/P5/P8/P10/P14),columns: Pitfall# / 类别 / Schema field-level mitigation / 本文档章节 / 是否 PoC must-fix
  - §8.0 子节存在:downstream citation guide — 列出 02/04/05/06 引用本文档字段名时,应从哪个章节取
  - §8.1 子节存在:Summary + coherence 声明(2-layer schema + 18 fields + 10 memory-record fields + 3-tier + curator phase contract = 完整 physical carrier for v10.0 设计)
  - §8.2 子节存在:References(00-FIRST-PRINCIPLES.md + ARCHITECTURE.md + PITFALLS.md + SUMMARY.md + CLAUDE.md + v2.0 PRFP canon)
  - Total Task 5 length ≥ 200 lines
  </behavior>

  <action>
  Append §6 + §7 + §8 to `.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md`.

  **§6 — 15-expert transform mapping table (verbatim from ARCHITECTURE §2)**

  §6.0 (~15 lines): "本节是 ARCHITECTURE.md §2 的 verbatim copy,作为 SC#4 的物理载体。每个 expert 的 5-field transform pattern 在本表只列 deltas,完整字段规则见 §2 of this doc(18-field narrative)。FOUND-08 preserved(Phase 44 决策 1 anchored)。"

  §6.1 (~15 lines): 5-field transform pattern verbatim from ARCHITECTURE §2 line 124 — COPY/DROP/REWRITE/FLATTEN/DERIVE/INITIALIZE bullets.

  §6.2 (~30 lines): 15-row expert delta table verbatim from ARCHITECTURE §2 lines 128-144. Columns: | Expert | tools | persona framing | Notable refs (count) | related_agents (count) |. All 15 rows copied: hook_retention / creative_source / screenplay / script_auditor / character_designer / cinematographer / style_genome / prompt_injector / visual_executor / continuity_auditor / audio_pipeline / editor / colorist / compliance_gate / theory_critic.

  §6.3 (~15 lines): Aggregate stats + FOUND-08 preservation rule verbatim from ARCHITECTURE §2 lines 146-148.

  **§7 — Open Questions + Pitfall audit (SC#5 physical carrier)**

  §7.0 (~10 lines): "本节是 SC#5 的物理载体:6 个 Open Questions 全部解决或显式 defer;7 个 load-bearing pitfalls 每个有对应 schema 字段级缓解。"

  §7.1 Open Questions audit table (~30 lines, 6 rows):
  | OQ# | 问题 (一句话) | 倾向性结论 (SUMMARY) | 本文档解决章节 | 是否 defer |
  | OQ-1 | persona 版本控制 | diff only (operator git) + memory record 带 persona_sha256 快照 | §2.4 + §3.11 | NO |
  | OQ-4 | fitness_score 冷启动 | null = neutral 0.5 for ordering, UI shows "untested" | §2.15 + agents-schema.yaml fitness_score comment | NO |
  | OQ-7 | memory.max_records 上限 + compaction | default 500 (3-tier aggregate), curator 每 N tick compaction | §4.4 + §4.5 | partial (具体 N 值 + compaction 算法 v11.0 PoC 实测) |
  | OQ-13 | tools 字段是否枚举 | YES — runtime whitelist enforced by dispatcher | §2.5 + agents-schema.yaml tools | NO |
  | OQ-14 | agents-schema.yaml 是否正式 JSON Schema | YES — produced in Task 2 | §1.2 + agents-schema.yaml | NO |
  | OQ-16 | _memory_evolution_phase 执行顺序 + 隔离 | AFTER _feedback_scan_phase, try/except isolated, dry-run-by-default | §5.1-§5.3 | NO |

  §7.2 Pitfalls audit table (~35 lines, 7 rows):
  | Pitfall# | 类别 | Schema field-level mitigation | 本文档章节 | PoC must-fix? |
  | P1 | persona drift | persona_sha256 in evolution_log entry + memory record | §2.4 + §2.14 + §3.11 | YES (load-bearing) |
  | P2 | stale memory | expires_at + verified_at + supersedes_memory_id + confidence + half_life_days | §3.1-§3.5 | YES |
  | P4 | cross-project leakage | scope (global/project/session) + project_id required + cross-project promotion gate | §3.9 + §2.6 (memory_scope) | YES |
  | P5 | curator failure modes | evidence_chain (≥3) + evidence_operator_ids (diversity) + dry-run-by-default + bias canary | §3.6 + §3.7 + §5.2 | YES |
  | P8 | no fitness signal | fitness_score (curator-computed) + fitness_trend.jsonl (deferred to PoC) | §2.15 | YES |
  | P10 | privacy / data leakage | confidentiality field + per-project propagation rule | §3.10 | partial (PoC ships internal-only; restricted tier deferred) |
  | P14 | schema migration breaks | schema_version in memory-record + dry-run migration mode | §3.12 | YES |

  After table: 1 paragraph noting "所有 7 个 load-bearing pitfalls 都有 schema 字段级缓解。P10 partial defer(confidentiality=restricted + cross-workspace propagation 留 v11.1);其余 6 个 PoC must-fix。详细 PoC 验收条件留 05-POC-PLAN.md。"

  **§8 — Downstream citation guide + Summary + References**

  §8.0 (~30 lines): Downstream citation card table — for each downstream doc (02/04/05/06/51), list:
  - Fields/sections they should cite from this doc
  - Fields they should NOT re-derive
  - Specific fields they should derive

  | Downstream doc | Cite from this doc | Do NOT re-derive | Should derive |
  | 02-ROUND-TABLE-PROTOCOL.md | §2.6 memory_scope + §3.9 scope + §3.10 confidentiality + §4 memory tier | 18-field schema, _memory_evolution_phase | turn lifecycle + conflict arbitration + scope precedence |
  | 04-MIGRATION-PATH.md | §2.7 lineage + §2.18 default_invocation + §6 transform table + §3.12 schema_version | 18-field schema | 15-expert transform rules + memory schema migration |
  | 05-POC-PLAN.md | §4.5 compaction + §5.2 dry-run + §7.2 pitfall audit | entire schema | fitness battery + latency SLO + bias canary |
  | 06-CROSS-REPO-IMPACT.md | §2.6 memory_scope + §3.9 scope + §3.12 schema_version | 18-field schema | 3-location sync + Option B vs 物理分区 |
  | 51 VALIDATE lint | entire schema (agents-schema.yaml + memory-record-schema.yaml as machine-checkable inputs) | entire schema | cross-doc consistency lint script |

  §8.1 (~20 lines): Coherence 声明. "2-layer schema (18 agent-profile fields + 10 memory-record fields) + 3-tier memory (core/working/archival) + curator _memory_evolution_phase contract = 完整 physical carrier for v10.0 设计。后续 4 docs(02/04/05/06)在不重新推导决策 5/6 的前提下引用本文档字段名。"

  §8.2 (~15 lines): References block — 00-FIRST-PRINCIPLES.md + ARCHITECTURE.md §1/§2/§3 + PITFALLS.md + SUMMARY.md + STACK.md + CLAUDE.md + v2.0 PRFP canon + Phase 44 commit reference.

  Length target: ~200-250 lines for Task 5.
  </action>

  <verify>
    <automated>grep -q "## §6" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "## §7" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -q "## §8" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md && grep -c "hook_retention\|creative_source\|screenplay\|script_auditor\|character_designer\|cinematographer\|style_genome\|prompt_injector\|visual_executor\|continuity_auditor\|audio_pipeline\|editor\|colorist\|compliance_gate\|theory_critic" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 15 {exit 0} {exit 1}' && for term in "OQ-1" "OQ-4" "OQ-7" "OQ-13" "OQ-14" "OQ-16" "P1" "P2" "P4" "P5" "P8" "P10" "P14"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^## §7/,/^## §8/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | grep -c "^|" | awk '$1 >= 14 {exit 0} {exit 1}' && grep -c "02-ROUND-TABLE-PROTOCOL\|04-MIGRATION-PATH\|05-POC-PLAN\|06-CROSS-REPO-IMPACT" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 4 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md | awk '$1 >= 800 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §6 exists with §6.0-§6.3 (5-field pattern + 15-row expert delta table verbatim from ARCHITECTURE §2 + FOUND-08 rule)
  - §7 exists with §7.1 OQ audit (6 rows: OQ-1/OQ-4/OQ-7/OQ-13/OQ-14/OQ-16 each resolved or partial-deferred) + §7.2 Pitfall audit (7 rows: P1/P2/P4/P5/P8/P10/P14 each with field-level mitigation)
  - §8 exists with §8.0 downstream citation card table (5 downstream docs) + §8.1 coherence 声明 + §8.2 References
  - Total doc length ≥ 800 lines (final)
  - All 6 OQs and all 7 load-bearing pitfalls explicitly covered
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc → v11.0 PoC implementer | The deliverable is a design + machine-readable schema. Misinterpretation by v11.0 PoC implementers is the primary risk. Mitigated by §8.0 downstream citation card + per-field PITFALLS source comments in agents-schema.yaml + memory-record-schema.yaml. |
| agents-schema.yaml → Python dataclass generator | The YAML files will be consumed by a future Python codegen tool. JSON Schema validity + field-name stability are critical. Mitigated by JSON Schema draft 2020-12 conformance (validated by `python3 -c "import yaml; yaml.safe_load(...)"` in Task 2). |
| 2-layer split → downstream doc authors | CC-2 mandate: downstream authors must not conflate agent-profile fields with memory-record fields. Mitigated by §1.3 ASCII diagram + explicit "Layer 1 / Layer 2" labels throughout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | 18-field schema definition (§2, agents-schema.yaml) | mitigate | Field locked at ROADMAP level (SC#1); any field add/remove requires milestone-level decision; agents-schema.yaml uses `additionalProperties: false` to reject undeclared fields |
| T-45-02 | Information Disclosure | 2-layer split (§1.3) | mitigate | CC-2 verbatim citation + ASCII diagram + every memory-record field narrative explicitly labels its layer |
| T-45-03 | Denial of Service | schema drift across 4 downstream docs (02/04/05/06) | mitigate | §8.0 citation card table makes each downstream doc's reference explicit; Phase 51 VALIDATE-02 lint script will cross-check this |
| T-45-04 | Elevation of Privilege | v11.0 PoC implementer bypasses pitfall mitigations | accept | Design doc cannot enforce itself; relies on Phase 50 POC-PLAN acceptance criteria + Phase 51 audit |
| T-45-05 | Repudiation | curator `_memory_evolution_phase` operator ownership | mitigate | §5.2 dry-run-by-default + explicit operator flag for live mutation + sha256 chain in evolution_log provides audit trail |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-45-SC (supply chain from package installs) is not applicable.
</threat_model>

<verification>
**Phase 45 DESIGN-02 success criteria (from ROADMAP §Phase 45):**

1. ✅ SC#1 — Files `01-AGENT-REGISTRY-SCHEMA.md` + `agents-schema.yaml` (18-field JSON Schema) + `memory-record-schema.yaml` (independent schema with all 10 mandated fields) exist (verified by Tasks 1+2+3 combined)
2. ✅ SC#2 — Per-agent memory tier (core / working / archival) documented with `memory.max_records` cap + compaction trigger, resolving OQ-7 (verified by Task 4 §4.1-§4.5)
3. ✅ SC#3 — Curator `_memory_evolution_phase` field contract documented (modeled on v6.0 `_feedback_scan_phase`, execution order + dry-run-by-default + try/except isolation), resolving OQ-16 (verified by Task 4 §5.1-§5.6)
4. ✅ SC#4 — 15-expert transform mapping table copied from ARCHITECTURE §2 (verified by Task 5 §6.2 — all 15 expert rows verbatim)
5. ✅ SC#5 — 6 Open Questions (OQ-1/OQ-4/OQ-7/OQ-13/OQ-14/OQ-16) + 7 load-bearing pitfalls (P1/P2/P4/P5/P8/P10/P14) each have field-level mitigation (verified by Task 5 §7.1 OQ audit + §7.2 pitfall audit)

**Final deliverable target:** ~800-1200 lines for narrative + ~200 lines agents-schema.yaml + ~180 lines memory-record-schema.yaml (mirrors Phase 44's 1181 lines at comparable rigor).

**Methodology audit (self-check before commit):**
- Every field narrative cites either ARCHITECTURE §1.1 source OR PITFALLS §P-X mitigation (not invented)
- Phase 44 decisions 5 + 6 cited by 决策号, never re-derived
- 2-layer schema split is explicit upfront (§1.3 ASCII diagram) — no reader can conflate the two
- All 7 load-bearing pitfalls appear in §7.2 audit table with field-level mitigation
- All 6 OQs appear in §7.1 audit table with resolution status
- Bilingual: EN structure (headers, schemas, field names) + 中文 prose (rationale, examples) per CLAUDE.md
</verification>

<success_criteria>
**Phase 45 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/45-agent-schema/45-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) All 3 deliverable files exist:
   - `.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md` (≥ 800 lines, §0-§8, all OQ + pitfall coverage)
   - `.planning/research/v10-orchestrator-design/agents-schema.yaml` (≥ 200 lines, valid JSON Schema, all 18 fields)
   - `.planning/research/v10-orchestrator-design/memory-record-schema.yaml` (≥ 150 lines, valid JSON Schema, all 10 mandated fields)
3. ROADMAP.md Phase 45 entry updated (Plans: 1 plan, plan list with checkmark).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/45-agent-schema/45-01-SUMMARY.md` when done. SUMMARY should record:
- Final line counts of all 3 deliverables
- 18-field agent YAML schema lock status (all fields with type/required/default/enum)
- 10-field memory-record schema lock status (all mandated fields present)
- 2-layer schema split declaration (CC-2 resolved)
- 3-tier memory tier declaration (core/working/archival, max_records=500, OQ-7 resolved)
- curator `_memory_evolution_phase` contract lock (OQ-16 resolved)
- 6 OQ resolutions (OQ-1 / OQ-4 / OQ-7 / OQ-13 / OQ-14 / OQ-16)
- 7 load-bearing pitfall field-level mitigations (P1 / P2 / P4 / P5 / P8 / P10 / P14)
- 15-expert transform table copied verbatim from ARCHITECTURE §2 (FOUND-08 preserved)
- Cross-validation: every field cites ARCHITECTURE §X or PITFALLS §P-X source
</output>

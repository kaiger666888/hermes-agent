---
phase: 44-first-principles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
autonomous: true
requirements: [DESIGN-01]
granularity: standard

must_haves:
  truths:
    - "File .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md exists"
    - "7 决策每个都有从根本需求出发的 first-principles 推导链(非类比论证)"
    - "「v10.0 显式拒绝」总表 ≥10 条,每条引用三个 source 的具体章节号(FEATURES §X + ARCHITECTURE §Y + PITFALLS §Z)"
    - "FEATURES §10 borrowable points B1.3 / B3.5 / B4.1 / B7.2 / B5.1 每点有明确赞同/拒绝/改造结论"
    - "后续 6 份设计文档(01-06)可在不重新推导决策的前提下引用本文档作根论据"
  artifacts:
    - path: ".planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md"
      provides: "v10.0 milestone 的根论据文档:7 决策推导链 + 显式拒绝总表 + FEATURES §10 borrowable points 评估 + 后续 6 docs 引用指南"
      min_lines: 800
      contains: ["决策 1", "决策 7", "v10.0 显式拒绝", "B1.3", "B3.5", "B4.1", "B7.2", "B5.1"]
  key_links:
    - from: "00-FIRST-PRINCIPLES.md"
      to: "FEATURES.md §10/§11"
      via: "markdown §reference"
      pattern: "FEATURES §(10|11)"
    - from: "00-FIRST-PRINCIPLES.md"
      to: "ARCHITECTURE.md §8"
      via: "markdown §reference"
      pattern: "ARCHITECTURE §8"
    - from: "00-FIRST-PRINCIPLES.md"
      to: "PITFALLS.md"
      via: "markdown §reference"
      pattern: "PITFALLS §Pitfall [0-9]+"
---

<objective>
Create the foundational root-argument document `.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md` for the v10.0 design milestone.

Purpose: Lock the first-principles derivation chains for all 7 design decisions + the consolidated "v10.0 显式拒绝" anti-features/anti-patterns table BEFORE downstream design docs (01-06) are written. The next 6 design docs will reference this document as their root argument — they must not re-derive decisions.

Output: A single markdown document (~800-1200 lines, bilingual EN structure + Chinese prose per project convention). Mirrors the depth/rigor of v2.0 PRFP `00-FIRST-PRINCIPLES.md` (1638 lines) but tighter scope — v10.0 has 7 locked decisions to derive (v2.0 had 4 root questions → 16 nodes).

This is a DESIGN-ONLY deliverable. Zero code changes (no SKILL.md / Python / plugin / mcp_serve.py edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/STACK.md
@.planning/research/v10-orchestrator-design/FEATURES.md
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md

@.planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md

<interfaces>
<!-- Methodology reference: v2.0 PRFP first-principles document structure (lines 1-300) -->
<!-- Shows the rigor/depth/tone expected; v10.0 deliverable is shorter (800-1200 lines) but same methodology discipline -->

From .planning/research/v10-orchestrator-design/SUMMARY.md:
- 7 locked design decisions (PROJECT.md §Current Milestone v10.0):
  | # | 决策 | 选型 |
  | 1 | T6 协议 | Hermes MCP server (扩展 mcp_serve.py) + tmux dispatch + CC native MCP client |
  | 2 | B3a Python runner | delegate-only phase 迁 CC; ComfyUI-calling + Step 0/6.5/15 保留 Python |
  | 3 | D2 storyboard | V8.6 编号保留, orchestrator 层做 round-based parallel |
  | 4 | G2 通用框架 | 抽象 pipeline orchestration pattern, kais-movie-pipeline 作首个 sample |
  | 5 | α agent form | YAML + persona + tools + refs + memory_scope + lineage |
  | 6 | per-agent memory | per-agent scoped memory (扩展 mem0) + curator-driven 自进化 |
  | 7 | (vi) 分层 CC | Hermes 控 turn_order/max_rounds/schema/early_stop_rule; CC 控 framing+synthesis |

- "Design Decisions Validated" table in SUMMARY.md cross-validates each decision against STACK/FEATURES/ARCHITECTURE/PITFALLS (use this table as input, not re-validation).

- 7 load-bearing pitfalls (PITFALLS.md): P1 persona drift / P2 stale memory / P4 cross-project leakage / P5 curator failure / P6 memory poisoning / P7 round-table conflict / P8 no fitness signal. The "v10.0 显式拒绝" table must reference these.

- CC-5 cross-cutting finding (SUMMARY.md): FEATURES §11 anti-features + ARCHITECTURE §8 anti-patterns + PITFALLS 行业案例 — three layers must be merged into single "v10.0 显式拒绝" table.

- FEATURES §10 borrowable points table: 27 entries feeding downstream docs. The 5 mandatory coverage points for this doc are:
  - B1.3 (LangGraph): supervisor is router not coordinator → feeds 决策 #7 derivation
  - B3.5 (CrewAI): crew is task pipeline, NOT round table → anti-feature
  - B4.1 (Claude Agent SDK): subagent form must be explicitly rejected → feeds 决策 #5 derivation
  - B7.2 (Agent-MCP): short-lived agent + shared graph is anti-pattern → feeds 决策 #6 derivation
  - B5.1 (OpenAI Swarm): handoff ≠ consultation → anti-feature

From .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md (lines 1-200):
- Document structure pattern: §0 阅读指南 → §1 方法论框架 → §2 root questions → §3 derivation chains → §4 candidate set → §5 audit → §6 Musk audit checklist → §7 open questions
- Methodology disciplines: Musk first principles / Aristotle roots / Epistemic-status taxonomy / Steelman-the-Elimination / MADR-style alternatives log
- v10.0 简化版采用:§0 阅读指南 → §1 方法论框架(简化) → §2 7 决策推导链 → §3 显式拒绝总表 → §4 FEATURES §10 评估 → §5 后续引用指南 → §6 总结 + 风险登记
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write §0 (reading guide) + §1 (methodology + 7-decision overview + paradigm shift declaration)</name>
  <files>.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/SUMMARY.md (full — extract: 7 decisions table, CC-5 anti-features merge mandate, paradigm shift statement)
    - .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md lines 1-200 (pattern reference for §0 阅读指南 + §1 方法论框架 style)
    - .planning/PROJECT.md §"Current Milestone: v10.0" (lines 26-72) — extract paradigm shift声明 + 7 锁定决策表 verbatim
  </read_first>

  <behavior>
  - Section §0 exists with: chapter map, stability markers, audience guide (Kai + Kimi + future v11.0 PoC implementer)
  - Section §1.1 exists: 简化版方法论框架 — Musk first principles 引用 v2.0 §1.1 (不重复完整 Aristotle 引文,只引用 v2.0 PRFP 作为方法论 source) + 1 段 v10.0-specific 加严:"v10.0 是设计型 milestone,论证必须从根本需求出发,任何「业界都用 X 所以我们用 X」式类比论证显式拒绝"
  - Section §1.2 exists: 7 决策清单总览表(从 SUMMARY.md "Design Decisions Validated" table 摘出 7 行,补 "根本需求" 一列,例:决策 1 → 根本需求是「Hermes 与 CC 之间需要稳定可演进的通信通道」)
  - Section §1.3 exists: paradigm shift 声明(从 PROJECT.md §paradigm shift 摘出 4 段:Kimi 默认 vs v10.0 设计;Agent vs Skill 分层;CC Team Lead 配置极薄;Agent 是 Hermes-side 独立 YAML 实体)
  </behavior>

  <action>
  Create file `.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md`. Write the opening frontmatter block (status / phase / audience / reading time / stability markers — mirror v2.0 PRFP §0 style), then §0 阅读指南 (chapter map, audience guide for 3 reader roles: Kai reviewer / Kimi Notion 续聊 / v11.0 PoC implementer), then §1 方法论框架.

  For §1.1 (方法论): Cite v2.0 PRFP §1.1-§1.5 as the methodology source (don't re-derive Aristotle + Musk + Steelman + Epistemic-status taxonomy — those are documented in v2.0). Add a v10.0-specific paragraph: "v10.0 设计型 milestone 的加严:每条决策推导必须从「Hermes-Agent 平台的根本需求」出发,显式拒绝「业界 framework X 这么做所以我们这么做」式类比论证。Musk 第一性原理的核心是还原→重构,不是『抄最热门的』。"

  For §1.2 (7 决策清单): Build a 7-row table with columns | # | 决策类别 | 锁定选型 | 根本需求 (要解决的根本问题) | v10.0 推导章节链接 |. The "根本需求" column is the load-bearing addition — it states WHAT each decision is solving before any option-comparison. Examples:
    - 决策 1 (T6 协议): 根本需求 = "Hermes 与 CC 之间需要稳定、低故障面、可演进的通信通道"
    - 决策 2 (B3a Python runner): 根本需求 = "既保留 v3-v9 五 milestone 硬化的 ComfyUI 集成,又把适合 CC 推理的创意决策环节迁出"
    - 决策 3 (D2 storyboard): 根本需求 = "在不破坏 V8.6 编号契约的前提下,获得真实有价值的并行加速"
    - 决策 4 (G2 通用框架): 根本需求 = "v12+ 扩展 music video / long-form 时不需要重写编排器"
    - 决策 5 (α agent form): 根本需求 = "agent 是有持久身份、有记忆、可自进化的实体,不是 prompt 模板"
    - 决策 6 (per-agent memory): 根本需求 = "agent 随项目越多越有经验(v10.0 paradigm shift 核心诉求)"
    - 决策 7 ((vi) 分层 CC): 根本需求 = "审计性 + 可重现 + token 成本可控 + 充分用 CC 推理能力"

  For §1.3 (paradigm shift): Quote verbatim from PROJECT.md lines 51-57 (Kimi 默认 vs v10.0 设计;Agent vs Skill 分层;CC Team Lead 极薄;Agent 是 Hermes-side 独立实体). Mark each as `paradigm-shift-claim-N` so downstream docs can reference by anchor.

  Length target: ~150-200 lines for this task (frontmatter + §0 + §1).
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 150 {exit 0} {exit 1}' && grep -c "决策 [1-7]" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -q "paradigm-shift-claim" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md</automated>
  </verify>

  <done>
  - File created with valid markdown frontmatter
  - §0 阅读指南 exists with chapter map + 3-audience guide
  - §1.1 方法论 references v2.0 PRFP + adds v10.0-specific anti-类比 discipline paragraph
  - §1.2 7 决策清单 table has 7 rows, each row has a non-empty "根本需求" column
  - §1.3 paradigm shift statement has ≥4 labeled claims (paradigm-shift-claim-1..4)
  - Total doc length ≥ 150 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Write §2 — 7 决策 first-principles 推导链 (7 H3 subsections, ~100-150 lines each)</name>
  <files>.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/SUMMARY.md §"Design Decisions Validated" table (each row has STACK/FEATURES/ARCHITECTURE/PITFALLS columns — these are the cross-validation sources to cite)
    - .planning/research/v10-orchestrator-design/STACK.md §1-§4 (for 决策 1 T6 协议 derivation: MCP stable API, stdio transport, zero-arch-change)
    - .planning/research/v10-orchestrator-design/FEATURES.md §7.4 B7.1 (Microsoft 三层协议分层 — for 决策 1); §4 B4.1 (Claude Agent SDK subagent rejection — for 决策 5); §1.3 B1.3 (LangGraph supervisor router — for 决策 7); §11 anti-features table (for 决策 5/6/7 anti-论证)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1 (18-field schema — for 决策 5); §3 (mem0 per-agent scope — for 决策 6); §5.3 lifecycle sketch (for 决策 7); §8 anti-patterns (for 决策 5/6/7 avoid-list)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 1-8 (for 决策 6 memory + 决策 7 round-table — these pitfalls must be cited as load-bearing constraints)
    - .planning/research/v2-pipeline-design/00-FIRST-PRINCIPLES.md §3.1-§3.4 (pattern reference for推导链写法 — see how each derivation chain steps from 根本需求 → 中间结论 → 选型, with explicit Musk "为什么是它而不是别的" language)
  </read_first>

  <behavior>
  - §2.0 子节存在:声明推导方法(每决策从根本需求出发 → 列出选项 → 用 v2.0 §1.5 Steelman-the-Elimination 纪律排除 → 推到唯一选型)+ 引用 SUMMARY.md "Design Decisions Validated" 表作为 cross-validation evidence
  - 7 个 H3 子节存在(§2.1 - §2.7),每个对应一个决策
  - 每个决策推导链包含 5 段:(a) 根本需求陈述 (b) 候选选项枚举(至少 2 个) (c) Steelman 排除论证 (d) 锁定选型 + 一句话 rationale (e) 4 research sources 的 cross-validation 引用(STACK §X / FEATURES §X / ARCHITECTURE §X / PITFALLS §Pitfall N)
  - 每个推导链字数 100-150 行
  - 显式拒绝类比论证:每段 (c) 排除论证必须从「根本需求」推,不能只说「业界用 X 我们不用」
  </behavior>

  <action>
  Append §2 to the document. Write §2.0 (推导方法声明, ~30 lines), then 7 H3 subsections §2.1 through §2.7.

  Per-decision structure (use this exact 5-段 scaffold per H3):

  ```
  ### §2.N — 决策 N: [decision category]
  
  **锁定选型:** [one-line summary]
  
  #### §2.N.1 — 根本需求
  
  [What fundamental problem does this decision solve? NOT "which option is best" — instead "what is the constraint that demands a decision at all?" Start from the v10.0 paradigm shift: agent 是有持久身份的实体, CC 是场地, etc.]
  
  #### §2.N.2 — 候选选项
  
  [Enumerate 2-4 options considered. Each option = a real alternative researched in STACK/FEATURES/ARCHITECTURE. Mark each with source章节号. Examples:
    - 决策 1 (T6 协议) candidates: (a) T6 Hermes MCP server [STACK §1-§4] (b) A2A cross-vendor protocol [FEATURES §8] (c) Kimi 全 MCP shim (TBD in Phase 47) (d) Custom RPC over HTTP
    - 决策 5 (α agent form) candidates: (a) YAML-first Hermes-side [ARCHITECTURE §1] (b) Claude Agent SDK subagent `.claude/agents/*.md` [FEATURES §4 B4.1] (c) CrewAI role/goal/backstory JSON [FEATURES §3] (d) LangGraph Python class [FEATURES §1]
    - 决策 6 (per-agent memory) candidates: (a) per-agent mem0 namespace [ARCHITECTURE §3 Option B] (b) per-agent mem0 workspace [PITFALLS §Pitfall 3 mitigation 1] (c) CrewAI boolean memory flag [FEATURES §3 B3.3] (d) LangGraph Store namespace tuple [FEATURES §1 B1.1]
  ]
  
  #### §2.N.3 — Steelman 排除论证
  
  [Apply Musk-style 还原→重构: for each candidate, ask "does it satisfy the 根本需求 (§2.N.1)?" If yes, keep. If no, eliminate with explicit reason. NOT "X is bad" but "X fails constraint Y from 根本需求". Examples:
    - 决策 5 排除 Claude Agent SDK subagent: B4.1 source says "subagent default context-isolated" + "memory 字段不是 namespace scoped" + "transcripts 30 天自动清理" — these three FAIL the 根本需求 "agent 是有持久身份、有记忆、可自进化的实体". This is elimination-by-fundamental-constraint, not "industry says so".]
  
  #### §2.N.4 — 锁定选型 + Rationale
  
  [One paragraph: the surviving option + 1-sentence rationale anchored in 根本需求. Reference the cross-validation: "SUMMARY.md 'Design Decisions Validated' table confirms this option has [strong support / 中性 /修正] across 4 research threads."]
  
  #### §2.N.5 — Cross-Validation Evidence (4 research sources)
  
  | Source | 章节 | 立场 | 引用要点 |
  |--------|------|------|----------|
  | STACK | §X.Y | 强支持 / 中性 / 警告 | [one-line evidence] |
  | FEATURES | §X.Y | ... | ... |
  | ARCHITECTURE | §X.Y | ... | ... |
  | PITFALLS | §Pitfall N | ... | ... |
  ```

  Per-decision content guidance:

  **§2.1 决策 1 (T6 协议):** 根本需求 = "Hermes-Agent 与 CC 之间需要稳定、低故障面、可演进的通信通道"。Steeled against: A2A (over-engineered for single-vendor), Kimi 全 MCP shim (defer comparison to Phase 47 but cite that both satisfy 根本需求 — pick T6 for zero-arch-change per STACK §1-§4), custom RPC (reject — reinvents MCP). Cite FEATURES §7.4 B7.1 Microsoft 三层协议分层 as cross-vendor validation that internal-flow → platform-native is correct layering.

  **§2.2 决策 2 (B3a Python runner 增量迁移):** 根本需求 = "既保留 v3-v9 五 milestone 硬化的 ComfyUI 集成 + asset bus + GoalBuilder,又把适合 CC 推理的创意决策环节迁出"。Candidates: (a) B3a 增量 [locked] (b) B3b 全迁 CC (reject: loses ComfyUI hardening) (c) B1 status quo (reject: 不获得 CC 推理能力). Note ARCHITECTURE/FEATURES 不直接覆盖 — 主要论据来自 PROJECT.md §paradigm shift + STACK §3.2 Tool 7 run_python_phase 作为 boundary tool 的设计。

  **§2.3 决策 3 (D2 storyboard-first-class):** 根本需求 = "在不破坏 V8.6 编号契约(FOUND-08)的前提下,获得真实有价值的并行加速"。Candidates: (a) D2 storyboard round-parallel [locked] (b) D1 step-level parallel (reject: 破坏 V8.6 编号) (c) D3 no parallel (reject: 串行 13-step pipeline 太慢). Cite STACK §7.5 强制串行约束 (CC-6 finding) — round table 内部强制 1 panelist 1 turn,但 orchestrator 层可跨场景 dispatch 多个独立 round table。

  **§2.4 决策 4 (G2 通用编排框架):** 根本需求 = "v12+ 扩展 music video / long-form 时不需要重写编排器"。Candidates: (a) G2 抽象 + sample [locked] (b) G1 kais-movie-pipeline 专属 (reject: v12+ 重写成本高) (c) 直接用 LangGraph (reject: FEATURES §1.3 显示 LangGraph 是 framework-of-frameworks, v10.0 仍需自己设计 agent form). Cite FEATURES §10 27 borrowable points as G2 design原料。

  **§2.5 决策 5 (α agent form: YAML + persona + tools + refs + memory_scope + lineage):** 根本需求 = "agent 是有持久身份、有记忆、可自进化的实体,不是 prompt 模板"。**This is the most load-bearing derivation** — candidates: (a) α YAML-first Hermes-side [locked] (b) Claude Agent SDK subagent `.claude/agents/*.md` [FEATURES §4 B4.1 + §11 anti-features row 1] (c) CrewAI role/goal/backstory JSONC [FEATURES §3 B3.1] (d) LangGraph Python class [FEATURES §1 §9.1] (e) A2A Agent Card [FEATURES §8 B8.1]. Steelman: B4.1 三属性 (context-isolated / non-scoped memory / 30-day TTL) 都 FAIL "持久身份 + 记忆 + 自进化" 根本需求。Cite ARCHITECTURE §1 18-field schema + §8.1 anti-pattern (YAML 作 prompt dump) as design护栏。

  **§2.6 决策 6 (per-agent memory + curator-driven 自进化):** 根本需求 = "agent 随项目越多越有经验(v10.0 paradigm shift 核心诉求)"。Candidates: (a) per-agent mem0 namespace + curator [locked, ARCHITECTURE §3 Option B] (b) per-agent mem0 workspace物理分区 [PITFALLS §Pitfall 3 mitigation 1, deferred to v12+] (c) shared memory [reject: fails per-agent requirement] (d) LangGraph Store namespace [FEATURES §1 B1.1] (e) CrewAI boolean memory [FEATURES §3 B3.3, reject: too weak]. Cite PITFALLS §Pitfall 1-8 全部 load-bearing pitfalls as硬约束 the design must mitigate via schema fields. Cite FEATURES §9.2 memory 模式速查表 "no mainstream framework combines per-agent scoped memory + curator-driven evolution" as差异化 evidence.

  **§2.7 决策 7 ((vi) 分层 CC 角色):** 根本需求 = "审计性 + 可重现 + token 成本可控 + 充分用 CC 推理能力"。Candidates: (a) (vi) 分层 [locked] (b) (i) CC 全控 (reject: Hermes 失去审计性) (c) (ii) Hermes 全控 (reject: 失去 CC 推理能力) (d) LangGraph supervisor pattern [FEATURES §1.3 B1.3 — explicit cross-同构 validation: "Hermes 像 LangGraph supervisor(路由), CC 像 worker agent(干活)"]. Cite ARCHITECTURE §5.3 lifecycle sketch + §8.3 anti-pattern (round table as pipeline step) + PITFALLS §Pitfall 7 (round-table memory conflict coordinator responsibility).

  Length target: ~700-900 lines for §2 (~100-130 lines per decision × 7).
  </action>

  <verify>
    <automated>grep -c "^### §2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && for d in 1 2 3 4 5 6 7; do grep -q "§2.$d" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md || exit 1; done && grep -c "STACK §" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "FEATURES §" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "ARCHITECTURE §" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "PITFALLS §" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 700 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §2.0 推导方法声明 exists (mentions Steelman-the-Elimination + SUMMARY.md "Design Decisions Validated" cross-validation)
  - §2.1 through §2.7 exist (7 H3 subsections)
  - Each subsection has 5 sub-blocks (根本需求 / 候选选项 / Steelman 排除 / 锁定选型 / Cross-Validation Evidence)
  - Each subsection cites all 4 research sources (STACK §X / FEATURES §X / ARCHITECTURE §X / PITFALLS §Pitfall N)
  - No decision is justified by "业界都用 X" alone — every elimination references the 根本需求 constraint that fails
  - Total doc length now ≥ 700 lines (cumulative with Task 1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §3 — "v10.0 显式拒绝" 总表 (≥10 anti-features, each with 3-source references)</name>
  <files>.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/FEATURES.md §11 (full anti-features table — 7 rows of industry-sourced anti-patterns)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §8 (full anti-patterns section — 5 rows: §8.1 YAML 作 prompt dump / §8.2 auto-re-transform on drift / §8.3 round table as pipeline step / §8.4 per-agent mem0 instance / §8.5 cross-layer import)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Pitfall 1-14 (14 pitfalls with industry cases — pick the ones that map to "v10.0 显式拒绝" framing, e.g. P3 mitigation 1 物理分区过早, P5 hallucinated writes, P6 memory poisoning attack surface, etc.)
    - .planning/research/v10-orchestrator-design/SUMMARY.md CC-5 cross-cutting finding (mandates this 3-source merge)
  </read_first>

  <behavior>
  - §3.0 子节存在:声明三源合并动机(FEATURES §11 业界抄来的反面教材 + ARCHITECTURE §8 Hermes 内部反模式 + PITFALLS 行业 memory subsystem 失败模式 — 三层互补)
  - §3.1 总表存在:≥10 行,每行对应一个 v10.0 显式拒绝的 anti-feature / anti-pattern
  - 每行有 5 列:| 拒绝项 | 一句话描述 | FEATURES §X (业界反例) | ARCHITECTURE §Y (Hermes 内部护栏) | PITFALLS §Pitfall Z (失败模式 case) |
  - 引用必须有具体章节号(不是 "FEATURES §anti-features",而是 "FEATURES §11 row 1" 或 "FEATURES §4.4 B4.1")
  - 行覆盖至少以下 10 项:subagent 作 agent 容器 / 短命 agent / handoff 作 round table 替代 / crew task pipeline 作协商 / A2A 跨厂商协议(本 milestone) / memory boolean flag / YAML 作 prompt dump / auto-re-transform on drift / round table as pipeline step / per-agent mem0 instance / cross-layer import of run_agent
  </behavior>

  <action>
  Append §3 to the document.

  §3.0 (动机声明, ~30 lines): Cite SUMMARY.md CC-5 verbatim — "三处都列了不要做什么,但维度互补:FEATURES §11 是从业界 framework 抄来的反面教材;ARCHITECTURE §8 是 Hermes 内部反模式;PITFALLS 是 memory subsystem 失败模式。v10.0 显式拒绝总表合并三层,作为后续 6 docs 的 negative design space。" Mark this section as the load-bearing deliverable for DESIGN-01 SC#2.

  §3.1 (总表, ≥10 rows): Build a markdown table with 5 columns. Each row = one anti-feature that v10.0 explicitly rejects. Per row:
    - Column 1 (拒绝项): short noun phrase, e.g. "Subagent 作 agent 容器"
    - Column 2 (一句话描述): what the anti-pattern is + why it's tempting
    - Column 3 (FEATURES §X): cite the FEATURES section that documents this as an industry pattern (with sub-section/row identifier)
    - Column 4 (ARCHITECTURE §Y): cite the ARCHITECTURE section that catches this in Hermes-internal design (or "—" if ARCHITECTURE doesn't cover — but most should)
    - Column 5 (PITFALLS §Pitfall Z): cite the PITFALL that documents the failure mode if this anti-pattern is adopted

  Mandatory rows (≥10 total, these 10 are required content; additional rows allowed):

  | # | 拒绝项 | FEATURES source | ARCHITECTURE source | PITFALLS source |
  | 1 | Subagent 作 agent 容器 (`.claude/agents/*.md`) | §11 row 1 + §4.4 B4.1 | §8.1 (variant: persona dump) | §Pitfall 1 (persona drift) |
  | 2 | 短命 agent (完成即销毁) | §11 row 2 + §7.2 B7.2 | (implicit — long-life is design intent) | §Pitfall 2 (stale memory inversion) |
  | 3 | Handoff 作 round table 替代 | §11 row 3 + §5.1 B5.1 | §8.3 (round table as pipeline step) | §Pitfall 7 (round-table conflict unresolved) |
  | 4 | Crew task pipeline 作协商 | §11 row 4 + §3.5 B3.5 | §8.3 | — (CrewAI has no round-table failure mode) |
  | 5 | A2A 跨厂商协议 (v10.0 范围内) | §11 row 5 + §7.4 B7.1 + §8 B8.3 | — (cross-vendor not in Hermes scope) | — |
  | 6 | `memory: true` boolean flag (无 namespace) | §11 row 6 + §3.3 B3.3 | §8.4 (per-agent mem0 instance — wrong solution direction) | §Pitfall 3 (scoped retrieval perf) + §Pitfall 4 (cross-project leakage) + §Pitfall 12 (cross-agent contamination) |
  | 7 | CrewAI 30+ 字段全抄 | §11 row 7 + §3.3 | §8.1 (YAML 作 prompt dump) | — |
  | 8 | YAML 作 prompt dump (SKILL body copy-paste) | (implicit) | §8.1 (full section) | §Pitfall 1 (persona drift — persona not distinguished from SKILL body) |
  | 9 | Auto-re-transform on drift | (implicit) | §8.2 (full section) | §Pitfall 5 (curator failure modes — operator ownership lost) |
  | 10 | Per-agent mem0 backend instance | (implicit — FEATURES §1.3 LangGraph Store namespace alternative) | §8.4 (full section) | §Pitfall 3 (workaround becomes wrong at scale) + §Pitfall 12 |
  | 11 | Cross-layer import of `run_agent` in agent modules | (implicit) | §8.5 (full section) | — (Hermes-internal circular import hazard) |
  | 12 | Round table as synchronous pipeline step | §11 row 3 (variant) | §8.3 (full section) | §Pitfall 7 (conflict unresolved under sync semantics) |

  (12 rows > 10 minimum required.)

  §3.2 (使用指南, ~30 lines): 1 段说明 "下游设计文档(01-06)在引入任何新设计点时,必须先 grep 本表确认不在显式拒绝列表;若认为某拒绝项需要 re-litigate,需开新的设计-修订 milestone(类比 v2.0 PRFP §1.1 修改门槛 discipline)"。

  Length target: ~150-200 lines for §3.
  </action>

  <verify>
    <automated>awk '/^## §3/,/^## §[4-9]/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | grep -c "^|" | awk '$1 >= 12 {exit 0} {exit 1}' && awk '/^## §3/,/^## §[4-9]/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | grep -c "FEATURES §" | awk '$1 >= 10 {exit 0} {exit 1}' && awk '/^## §3/,/^## §[4-9]/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | grep -c "PITFALLS §" | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §3.0 动机声明 exists, references SUMMARY.md CC-5
  - §3.1 总表 has ≥10 data rows (target: 12 rows)
  - Each row cites FEATURES §X with specific section/sub-section identifier (not just "§11")
  - Each row cites at least one of ARCHITECTURE §Y or PITFALLS §Pitfall Z (most cite both)
  - All 7 FEATURES §11 rows are represented (subagent / 短命 agent / handoff / crew pipeline / A2A / memory boolean / CrewAI 30+ 字段)
  - All 5 ARCHITECTURE §8 anti-patterns are represented
  - §3.2 使用指南 explains how downstream docs use this table
  </done>
</task>

<task type="auto">
  <name>Task 4: Write §4 — FEATURES §10 borrowable points evaluation (explicit cover B1.3 / B3.5 / B4.1 / B7.2 / B5.1) + §5 (downstream docs citation guide) + §6 (summary + risk register)</name>
  <files>.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/FEATURES.md §10 (full 27-row borrowable points table — evaluate at minimum the 5 mandated points, plus optionally others as space allows)
    - .planning/research/v10-orchestrator-design/FEATURES.md §1.3 (B1.3 LangGraph supervisor router detail)
    - .planning/research/v10-orchestrator-design/FEATURES.md §3.5 (B3.5 CrewAI crew is not round table detail)
    - .planning/research/v10-orchestrator-design/FEATURES.md §4.4 (B4.1 Claude Agent SDK subagent rejection detail)
    - .planning/research/v10-orchestrator-design/FEATURES.md §7.2 (B7.2 Agent-MCP short-lived agent + shared graph detail)
    - .planning/research/v10-orchestrator-design/FEATURES.md §5.1 (B5.1 OpenAI Swarm handoff detail)
    - .planning/ROADMAP.md §Phase 45-50 (downstream 6 docs — for §5 citation guide)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §"Roadmap Implications" + §"Risk Register" (for §6 risk register)
  </read_first>

  <behavior>
  - §4.0 子节存在:声明本节目的(逐点评估 FEATURES §10 borrowable points,给赞同/拒绝/改造结论)
  - §4.1 - §4.5 子节存在:5 mandated borrowable points (B1.3 / B3.5 / B4.1 / B7.2 / B5.1) 每点单独评估
  - 每点评估有 4 段:(a) 借鉴点原文一句话摘要 + FEATURES §章节号 (b) v10.0 结论:赞同 / 拒绝 / 改造 (c) 论据(从 §2 推导链 or §3 拒绝表 cross-reference) (d) 下游文档(01-06)如何使用此结论
  - §5 (后续 6 docs 引用指南)存在:每个下游 doc 一段(01-AGENT-SCHEMA / 02-ROUND-TABLE / 03-COMPARISON / 04-MIGRATION / 05-POC-PLAN / 06-CROSS-REPO),说明「本文档的哪些章节作根论据」+ 「本文档的哪些决策号 / 拒绝项 / 评估结论可直接引用」
  - §6.1 总结存在:1 段 + 7 决策 + 12 拒绝项 + 5 borrowable point 结论的 milestone-level coherence 声明
  - §6.2 风险登记表存在:从 SUMMARY.md §"Risk Register" 7 load-bearing pitfalls 摘出,每行标注「在哪个下游 doc 解决」+ 「本文档提供什么根论据」
  - §6.3 后续里程碑挂钩存在:声明 v11.0 PoC 实施时本文档作为不可变根论据(类比 v2.0 PRFP §1 stability marker)
  </behavior>

  <action>
  Append §4, §5, §6 to the document.

  **§4 — FEATURES §10 borrowable points evaluation (mandatory coverage)**

  §4.0 (声明, ~20 lines): "本节逐点评估 FEATURES §10 中对 v10.0 设计最有影响的 5 个 borrowable points。每点给赞同/拒绝/改造结论。其他 22 点可在下游设计文档按需引用本节方法论。"

  §4.1 - §4.5: 5 H3 subsections, one per mandated borrowable point.

  Per-point structure:
  ```
  ### §4.N — B[X.Y] [borrowable point name]
  
  **借鉴点摘要:** [one-sentence summary, citing FEATURES §X.Y]
  
  **v10.0 结论:** [赞同 / 拒绝 / 改造] — [one-line rationale]
  
  **论据:**
  [2-3 paragraphs explaining why. Cross-reference §2 derivation chains and §3 rejection table where applicable. NOT a re-derivation — point to where the decision was already derived.]
  
  **下游使用:**
  [Which design doc (01-06) consumes this conclusion and how. Example: "01-AGENT-REGISTRY-SCHEMA.md §persona 字段设计时引用本结论,要求 persona 是第一人称系统提示词片段,不是 SKILL body copy-paste (per §3 row 8 + B4.1)"]
  ```

  Per-point content:
  - **§4.1 B1.3 (LangGraph supervisor 是路由器不是协调员):** 赞同. Cross-ref §2.7 决策 7 推导 (CC 像 LangGraph supervisor 做路由/turn_order,不是做内容生产). Downstream: 02-ROUND-TABLE-PROTOCOL.md 引用本结论设计 turn lifecycle.
  - **§4.2 B3.5 (CrewAI crew 不是 round table, 是 task pipeline):** 赞同 (作为 anti-pattern 引用). Cross-ref §3 row 4 (crew task pipeline 作协商 — 拒绝). Downstream: 02-ROUND-TABLE-PROTOCOL.md 引用本结论作为 "为什么不抄 CrewAI 协作模式" 的根论据.
  - **§4.3 B4.1 (Claude Agent SDK subagent 形态必须显式拒绝):** 赞同 (Kimi 方案的反例). Cross-ref §2.5 决策 5 推导 + §3 row 1. Downstream: 03-COMPARISON-VS-KIMI-MCP-SHIM.md 引用本结论作为 Kimi 方案否决的根论据.
  - **§4.4 B7.2 (Agent-MCP 短命 agent + 共享 knowledge graph = v10.0 反面教材):** 赞同. Cross-ref §2.6 决策 6 推导 + §3 row 2. Downstream: 01-AGENT-REGISTRY-SCHEMA.md 引用本结论作为 "agent 是长生命周期实体, 不是 single-task ephemeral" 的根论据.
  - **§4.5 B5.1 (OpenAI Swarm handoff 是控制权移交, 不是协商):** 赞同 (作为 anti-pattern 引用). Cross-ref §3 row 3. Downstream: 02-ROUND-TABLE-PROTOCOL.md 引用本结论排除 handoff 协议设计.

  Length target: ~200-250 lines for §4.

  **§5 — 后续 6 docs 引用指南 (downstream citation guide)**

  §5.0 (声明, ~10 lines): "后续 6 份设计文档在不重新推导决策的前提下引用本文档作根论据。本节列出每份文档的引用 entry points。"

  §5.1 - §5.6: 6 H3 subsections, one per downstream doc. Each is a short reference card (~15-20 lines):

  Per-doc structure:
  ```
  ### §5.N — [doc filename]
  
  **本文档可引用的根论据:**
  - §1.2 决策 [N] 清单(选适用的)
  - §2.[M] 决策 [M] 推导链(选适用的)
  - §3 row [X] 显式拒绝(选适用的)
  - §4.[K] borrowable point 评估(选适用的)
  
  **本 doc 不应 re-derive:** [list of decisions / rejections that are LOCKED here]
  
  **本 doc 应该 derive:** [list of design points specific to this doc that are NOT in scope of 00]
  ```

  Per-doc content (from ROADMAP §Phase 45-50):
  - §5.1 01-AGENT-REGISTRY-SCHEMA.md: 可引用 §2.5 (决策 5 α form) + §2.6 (决策 6 per-agent memory) + §3 rows 1/6/7/8 + §4.3/§4.4. 不应 re-derive 决策 5/6. 应 derive 18-field schema 具体字段 + memory-record-schema 字段.
  - §5.2 02-ROUND-TABLE-PROTOCOL.md: 可引用 §2.3 (决策 3 storyboard) + §2.7 (决策 7 分层 CC) + §3 rows 3/4/11/12 + §4.1/§4.2/§4.5. 不应 re-derive 决策 3/7. 应 derive turn lifecycle + memory conflict arbitration.
  - §5.3 03-COMPARISON-VS-KIMI-MCP-SHIM.md: 可引用 §2.1 (决策 1 T6) + §2.5 (决策 5) + §2.7 (决策 7) + §3 row 1/5 + §4.3. 不应 re-derive 决策 1/5/7. 应 derive 7 维度对照 + subagent 形态否决横向验证.
  - §5.4 04-MIGRATION-PATH.md: 可引用 §2.2 (决策 2 B3a) + §2.5 (决策 5) + §3 row 9 (auto-re-transform). 不应 re-derive 决策 2/5. 应 derive 15 expert transform 规则 + memory schema 迁移.
  - §5.5 05-POC-PLAN.md: 可引用 §6.2 风险登记表 + 全部 §3 拒绝项 + §2.6 (决策 6 pitfalls). 不应 re-derive 任何决策. 应 derive fitness battery + latency SLO + bias canary.
  - §5.6 06-CROSS-REPO-IMPACT.md: 可引用 §2.1 (决策 1) + §3 row 5 (A2A v12+ 扩展位) + §3 row 10 (per-agent mem0 instance). 不应 re-derive 决策 1. 应 derive 3-location 同步策略 + Option B vs 物理分区.

  Length target: ~120-150 lines for §5.

  **§6 — 总结 + 风险登记表 + milestone 挂钩**

  §6.1 (Coherence 声明, ~30 lines): 1 段总结: 7 决策 + 12 拒绝项 + 5 borrowable point 评估 = v10.0 milestone 的 negative + positive design space. 后续 6 docs 在此空间内做具体设计,不可越界.

  §6.2 (风险登记表, ~50 lines): markdown table from SUMMARY.md §Risk Register, columns:
    | Risk ID | Pitfall | 在哪个下游 doc 解决 | 本文档提供的根论据 |
    | P1 | persona drift | 01-AGENT-REGISTRY-SCHEMA.md (persona_sha256 + tiered memory) | §2.6 决策 6 推导 + §3 row 8 (YAML dump) |
    | P2 | stale memory | 01 + 05 | §2.6 + §3 row 6 |
    | P4 | cross-project leakage | 01 + 02 | §2.6 + §3 row 6 |
    | P5 | curator failure | 01 + 05 | §2.6 + §3 row 9 |
    | P6 | memory poisoning | 01 + 05 + 06 | §2.6 |
    | P7 | round-table conflict | 02 | §2.7 决策 7 + §3 row 12 |
    | P8 | no fitness signal | 01 + 05 | §2.6 |

  §6.3 (milestone 挂钩, ~20 lines): "本文档作为 v10.0 milestone 的不可移根论据,稳定性 = §1.2 + §2 + §3 + §4 全部 `stable` (修改需开新 milestone, 类比 v2.0 PRFP §1.1 修改门槛). v11.0 PoC 实施时引用本文档作为决策已锁的依据, 不重新推导."

  Length target: ~100-150 lines for §6.

  Final step: append References section (citing v2.0 PRFP §1 methodology source + 4 v10.0 research docs + PROJECT.md + Notion 架构2.0 page).
  </action>

  <verify>
    <automated>grep -q "§4.1" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -q "§4.2" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -q "§4.3" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -q "§4.4" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -q "§4.5" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -c "B1\.3\|B3\.5\|B4\.1\|B7\.2\|B5\.1" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 5 {exit 0} {exit 1}' && grep -q "## §5" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -q "## §6" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md && grep -c "§Pitfall" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 7 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | awk '$1 >= 800 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §4 exists with §4.0 声明 + §4.1 through §4.5 (5 mandated borrowable points covered)
  - Each §4.N has 4 sub-blocks (摘要 / 结论 / 论据 / 下游使用)
  - Each §4.N gives explicit 赞同/拒绝/改造 conclusion
  - §5 exists with §5.0 声明 + §5.1 through §5.6 (6 downstream docs covered)
  - §6 exists with §6.1 (coherence) + §6.2 (7-row risk register table) + §6.3 (milestone 挂钩)
  - References section exists at end
  - Total doc length ≥ 800 lines (final)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc → downstream-implementer | The deliverable is a design document. Misinterpretation by v11.0 PoC implementers is the primary risk. Mitigated by §5 explicit citation guide + per-decision locked statements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | decision derivations (§2) | mitigate | Each decision has 5-段 scaffold (根本需求 / 候选 / Steelman / 锁定 / cross-validation); any change requires re-running the full scaffold + updating all downstream citation cards (§5) |
| T-44-02 | Information Disclosure | "v10.0 显式拒绝" 总表 (§3) | mitigate | Each rejection row cites 3 sources with specific §-numbers; rejection re-litigation requires new milestone per §3.2 使用指南 |
| T-44-03 | Denial of Service | doc drift across 6 downstream docs | mitigate | §5 citation guide makes each downstream doc's reference explicit; Phase 51 VALIDATE-02 lint script will cross-check this |
| T-44-04 | Elevation of Privilege | v11.0 PoC implementer bypasses locked decisions | accept | Design doc cannot enforce itself; relies on milestone audit (Phase 51) |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-44-SC (supply chain from package installs) is not applicable.
</threat_model>

<verification>
**Phase 44 DESIGN-01 success criteria (from ROADMAP):**

1. ✅ File `.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md` exists with 7 决策 first-principles 推导链 (verified by Task 1+2 combined: §0-§2 present, ≥700 lines)
2. ✅ "v10.0 显式拒绝" 总表 ≥10 条, 每条引用三个 source (verified by Task 3: §3 table ≥10 rows with FEATURES+ARCHITECTURE+PITFALLS citations)
3. ✅ FEATURES §10 borrowable points B1.3 / B3.5 / B4.1 / B7.2 / B5.1 显式覆盖 (verified by Task 4: §4.1-§4.5 each gives 赞同/拒绝/改造 conclusion)
4. ✅ 后续 6 docs 可引用本文档作根论据 (verified by Task 4: §5.1-§5.6 citation cards per downstream doc)

**Final doc length target:** 800-1200 lines (mirrors v2.0 PRFP depth at reduced scope).

**Methodology audit (self-check before commit):**
- Every decision derivation chain (§2.1-§2.7) starts from 根本需求, NOT from "业界都用 X"
- Every §3 rejection row has 3 source citations with specific §-numbers
- Every §4 borrowable point evaluation cross-references §2 derivation or §3 rejection
- §5 citation cards tell each downstream doc what to reference vs what to derive
- §6.2 risk register tracks every load-bearing pitfall (P1/P2/P4/P5/P6/P7/P8) to its downstream doc + this doc's root argument contribution
</verification>

<success_criteria>
**Phase 44 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/44-first-principles/44-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) `.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md` exists with:
   - §0 阅读指南 + §1 方法论 + 7 决策清单 + paradigm shift声明
   - §2 推导链 §2.1-§2.7 (each 100-150 lines, each cites 4 research sources)
   - §3 显式拒绝总表 ≥10 rows, each citing 3 sources
   - §4 borrowable points §4.1-§4.5 covering B1.3 / B3.5 / B4.1 / B7.2 / B5.1
   - §5 citation guide §5.1-§5.6 for 6 downstream docs
   - §6 总结 + risk register + milestone 挂钩
   - Total length 800-1200 lines
3. ROADMAP.md Phase 44 entry updated (Plans: 1 plan, plan list updated).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/44-first-principles/44-01-SUMMARY.md` when done. SUMMARY should record:
- Final line count of 00-FIRST-PRINCIPLES.md
- 7 决策 lock status (all derived from first principles, none by analogy)
- 12 拒绝项 coverage (all 7 FEATURES §11 rows + all 5 ARCHITECTURE §8 anti-patterns represented)
- 5 mandated borrowable points (B1.3 / B3.5 / B4.1 / B7.2 / B5.1) conclusions
- 6 downstream doc citation cards emitted
- Cross-validation: every decision cites all 4 research sources (STACK / FEATURES / ARCHITECTURE / PITFALLS)
</output>

---
phase: 47-kimi-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md
autonomous: true
requirements: [DESIGN-04]
granularity: standard

must_haves:
  truths:
    - "File .planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md exists"
    - "7-dimension contrast table is complete (协议 / dispatch / callback / state / 多 agent / 实现成本 / 稳定性), each dimension gives T6 vs Kimi pros/cons + selection rationale rooted in Phase 44 decisions"
    - "Subagent form rejection argument is complete citing FEATURES §11 B4.1 (anti-feature row) + FEATURES §4.3 (subagent default context-isolated, panelist unsuitable) + cross-ref to Phase 46 protocol's serial constraint + memory conflict arbitration"
    - "Microsoft three-layer protocol validation cites FEATURES §7.4 B7.1 verbatim (internal → platform-native; tool → MCP; cross-platform → A2A) and maps v10.0 T6 selection onto it, proving alignment with industry consensus"
    - "Kimi-side borrowable parts (if any) are explicitly listed with evaluation conditions for adoption"
    - "Phase 44 决策 1-7 each cited by 决策号 at least once across the comparison narrative (cross-validation of locked decisions, not re-derivation)"
    - "FEATURES §4 / §7.4 / §10 / §11 each cited by section number at least once (B7.1 + B4.1 + B2.2 + borrowable points appear explicitly)"
  artifacts:
    - path: ".planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md"
      provides: "v10.0 design doc #03 — T6 (Hermes MCP server + tmux dispatch + CC native MCP client) vs Kimi full-MCP-shim per-dimension contrast, serving as horizontal validation of the 7 locked Phase 44 decisions + the argument library for subagent form rejection. Cite Phase 44 decisions (1-7) and FEATURES/STACK/ARCHITECTURE sections by number — does NOT re-derive them. Single deliverable (no schema YAML, unlike Phase 45/46)."
      min_lines: 800
      contains: ["T6", "Kimi", "MCP shim", "subagent", "context-isolated", "B4.1", "B7.1", "B2.2", "Microsoft", "platform-native", "决策 1", "决策 2", "决策 3", "决策 4", "决策 5", "决策 6", "决策 7", "FEATURES §7.4", "FEATURES §11", "FEATURES §4", "FEATURES §10", "协议", "dispatch", "callback", "state", "多 agent", "实现成本", "稳定性"]
  key_links:
    - from: "03-COMPARISON-VS-KIMI-MCP-SHIM.md"
      to: "00-FIRST-PRINCIPLES.md §2.1-§2.7 (decisions 1-7)"
      via: "markdown §reference + 决策号 citation"
      pattern: "00-FIRST-PRINCIPLES|决策 [1-7]"
    - from: "03-COMPARISON-VS-KIMI-MCP-SHIM.md"
      to: "FEATURES.md §7.4 (B7.1 Microsoft layered) + §11 (B4.1 subagent rejection) + §4 (Claude Agent SDK) + §10 (borrowable) + §2 (MAF)"
      via: "markdown §reference for industry-consensus citations"
      pattern: "FEATURES §(4|7\\.4|10|11|2)"
    - from: "03-COMPARISON-VS-KIMI-MCP-SHIM.md"
      to: "STACK.md §1 (T6 协议 layer) + §3.2 (7 MCP tools)"
      via: "markdown §reference for T6 canonical spec"
      pattern: "STACK §(1|3\\.2)"
    - from: "03-COMPARISON-VS-KIMI-MCP-SHIM.md"
      to: "ARCHITECTURE.md §4 (dispatch path) + §8 (anti-patterns)"
      via: "markdown §reference for anti-pattern citations"
      pattern: "ARCHITECTURE §(4|8)"
    - from: "03-COMPARISON-VS-KIMI-MCP-SHIM.md"
      to: "02-ROUND-TABLE-PROTOCOL.md §2 (turn lifecycle) + §3 (memory conflict arbitration)"
      via: "markdown §reference for cross-doc protocol alignment"
      pattern: "02-ROUND-TABLE-PROTOCOL|round table|memory conflict|serial"
---

<objective>
Create the v10.0 design doc #03 — the **T6 vs Kimi full-MCP-shim comparison and subagent-form rejection argument library** — as a single markdown deliverable:

`.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md` (~800-1200 lines, bilingual EN structure + 中文 prose per CLAUDE.md)

Purpose: Produce the per-dimension contrast (协议 / dispatch / callback / state / 多 agent / 实现成本 / 稳定性) that serves as **horizontal validation of the 7 locked Phase 44 decisions** + the **argument library for the subagent form rejection** + the **Microsoft three-layer protocol validation** + an explicit list of Kimi-side borrowable parts with adoption conditions.

The next design docs (05-POC-PLAN, 06-CROSS-REPO-IMPACT) and the Phase 51 milestone audit will cite this doc as the "v11.0 PoC implementer's defense brief" — when an implementer asks "why T6 not full MCP shim?" or "why Hermes-side YAML agents not CC subagents?", this doc answers with section-and-line-precise citations.

Output: 1 file under `.planning/research/v10-orchestrator-design/`, mirroring Phase 44/45/46's bilingual depth/rigor at comparable scope (Phase 44 was 1181 lines; Phase 46 target 800-1200 lines narrative + schema). Phase 47 is single-deliverable (no schema YAML) — the entire budget goes to the narrative.

This is a DESIGN-ONLY deliverable. Zero code changes (no SKILL.md / Python / plugin / mcp_serve.py edits). Cite Phase 44's locked decisions (1-7), Phase 45's locked schema fields, and Phase 46's locked protocol invariants — do NOT re-derive them. Cite, do NOT redefine: `memory_scope`, `scope`, `confidence`, `evidence_chain`, `confidentiality`, `fitness_score`, `persona_sha256`, round table lifecycle, MCP tool STACK-form names are all consumed here by reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
@.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md
@.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md
@.planning/research/v10-orchestrator-design/STACK.md
@.planning/research/v10-orchestrator-design/FEATURES.md
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md

<interfaces>
<!-- Phase 44 root-argument anchors (do NOT re-derive; cite by 决策号) -->
<!-- Decision 1: T6 协议 (Hermes MCP server + tmux dispatch + CC native MCP client) — the SIDE A of this comparison -->
<!-- Decision 2: B3a Python runner 增量迁移 (delegate-only phase 迁 CC, ComfyUI/Step 0/6.5/15 保留 Python) — context for dispatch dimension -->
<!-- Decision 3: D2 storyboard-first-class (V8.6 编号保留, orchestrator round-based parallel) — context for multi-agent dimension -->
<!-- Decision 4: G2 通用编排框架 (abstract pipeline orchestration, kais-movie-pipeline 首个 sample) — context for stability dimension -->
<!-- Decision 5: α agent form (YAML + persona + tools + refs + memory_scope + lineage) — the core of subagent rejection -->
<!-- Decision 6: per-agent memory + curator-driven 自进化 — the load-bearing differentiator vs Kimi shared-knowledge-graph -->
<!-- Decision 7: 分层 CC 角色 (Hermes controls turn_order/max_rounds/early_stop_rule/schema; CC controls framing + synthesis) — the load-bearing differentiator vs Kimi "CC is the agent container" -->

<!-- These seven decisions are the validation targets. The 7-dimension table must show T6 serving each decision better than full MCP shim. -->

From .planning/research/v10-orchestrator-design/SUMMARY.md (cross-cutting findings, relevant slices):
> CC-5: FEATURES §11 anti-features ↔ ARCHITECTURE §8 anti-patterns ↔ PITFALLS 行业案例. Three places list "不要做什么" but in complementary dimensions. `00-FIRST-PRINCIPLES.md` should merge these into a single "v10.0 显式拒绝" table, each row citing three sources. Phase 47 inherits the locked rejection set and extends it with the per-dimension contrast.

From .planning/research/v10-orchestrator-design/SUMMARY.md (Phase 47's place in the design graph):
> 03-COMPARISON-VS-KIMI-MCP-SHIM.md (parallel, independent of 01/02 字段细节, only consumes 7 决策本身). Delivers: T6 vs Kimi MCP shim 逐维度对照表 + FEATURES B4.1 subagent 否决论据 + B7.1 Microsoft 三层协议验证.

From .planning/research/v10-orchestrator-design/FEATURES.md §7.4 B7.1 (Microsoft three-layer protocol — load-bearing citation for SC#4):
> Microsoft 指引明确给出协议分层:
> - Platform-native orchestration for internal flows (平台原生, 最低成本)
> - MCP for tool and data access (MCP 做 agent ↔ tool)
> - A2A for cross-platform agent-to-agent messaging (A2A 做 agent ↔ agent, 跨厂商)
> v10.0 决策 #1 选 "Hermes MCP server + tmux dispatch + CC native MCP client". 这跟 Microsoft 推荐的 "internal flow → platform-native (Hermes); tool access → MCP; cross-platform → A2A (不需要)" 完全一致.

From .planning/research/v10-orchestrator-design/FEATURES.md §4.2 + §4.3 + §11 B4.1 (subagent rejection — load-bearing for SC#3):
> Claude Agent SDK §4.2 multi-agent row: "不原生支持 multi-agent. Claude 自己作为 main agent, 可调 Agent tool 派生 subagent 做子任务, subagent 返回 final message; 不是 round table (无多 subagent 互发言)"
> §4.3 fact 1: "Subagent 默认 context-isolated: 'The only channel from parent to subagent is the Agent tool's prompt string'. 这意味着 subagent 不天然适合做 round table panelist (panelist 需要看完整历史)."
> §4.3 fact 2: "memory 字段限定 source: 'user' / 'project' / 'local', 不是 namespace scoped memory (对比 LangGraph Store 的 namespace 元组, 这弱很多)."
> §4.3 fact 3: "Subagent transcripts 持久化在独立文件, main conversation compaction 不影响 subagent; 30 天后自动清理."
> §11 anti-feature row 1: "Subagent 作为 agent 容器 (出处 Claude Agent SDK .claude/agents/*.md). v10.0 拒绝理由: subagent context-isolated, memory 弱, 30 天清理. v10.0 替代: Hermes-side YAML agent (~/.hermes/agents/)."

From .planning/research/v10-orchestrator-design/FEATURES.md §10 (borrowable design points relevant to Kimi comparison):
- B2.2 (MAF): "A2A 一等公民" 立场验证 v10.0 不依赖 A2A → this doc (03-COMPARISON-VS-KIMI-MCP-SHIM.md)
- B7.1 (MS 指引): 三层协议分层 (platform / MCP / A2A) 验证 T6 → this doc (03-COMPARISON-VS-KIMI-MCP-SHIM.md)
- B7.4 (MS 指引): A2A 是 v12+ 跨厂商扩展位 → 06-CROSS-REPO-IMPACT.md (cite here for completeness)
- B4.1 (Claude Agent SDK): subagent 形态 = Kimi 方案 = 必须显式否决 → this doc (cite verbatim from FEATURES §11 B4.1 + §4.3)

From .planning/research/v10-orchestrator-design/STACK.md §1 + §3.2 (T6 canonical spec — the SIDE A definition):
> §1 Recommended Stack: mcp Python SDK ==1.26.0 (FastMCP) + Claude Code CLI 2.1.143 (stdio MCP client) + tmux (dispatch long-running CC subagent) + Pydantic ==2.13.4 + mem0 plugin v6.0 (per-agent scoped memory)
> §3.2: 7 MCP tools in STACK-form naming (get_agent_persona / get_agent_opinion / get_agent_memory / submit_round_table_result / submit_artifact / query_memory / run_python_phase) — Phase 46 locked these names; this doc cites by name only, does NOT redefine

From .planning/research/v10-orchestrator-design/ARCHITECTURE.md §8 (anti-patterns — three of five are Kimi-side mistakes):
> 8.1 Agent YAML as Prompt Dump (mixing SKILL body with persona) — Kimi's filesystem-agent form invites this
> 8.3 Round Table as Pipeline Step (synchronous block) — Kimi's full-MCP-shim dispatch tempts this
> 8.4 Per-Agent mem0 Backend Instance — not directly a Kimi issue but appears in shared-graph comparison
> 8.5 Cross-Layer Import — Hermes-side invariant, Kimi-side shim does not break this

From Phase 44/45/46 locked contracts (CONSUME, do NOT redefine):
- Phase 44 决策 1-7 (root arguments) — cite by 决策号
- Phase 45 agents-schema.yaml fields: `name`, `persona_sha256`, `tools`, `memory_scope`, `lineage`, `fitness_score`, `default_invocation` (skill_fallback → mcp_tool), `evolution_log`
- Phase 45 memory-record-schema.yaml fields: `record_id`, `agent_id`, `scope` (global|project|session), `confidence`, `evidence_chain`, `status`, `confidentiality`, `expires_at`
- Phase 46 round-table-state-schema.yaml: roundId / turnOrder / turns / conflicts / status enum
- Phase 46 invariants: serial execution (1 panelist 1 turn sequential await, GLM concurrency==1); STACK-form MCP naming (no agent_ prefix); atomic turn lifecycle (round_table_open → turn N → submit_round_table_result)

From CLAUDE.md (deliverable form canon):
> SKILL.md 双语 (英文 YAML 结构 + metadata 保留英文; description 与正文采用 EN structure + 中文段落/示例)
> Markdown tables for dimension comparison (Phase 47's primary deliverable form)

From user memory (in-repo, sets the comparison frame):
> coding-agent-vs-mcp-shim.md (2026-07-06): v10.0 设计 milestone 必须把已有 coding-agent (tmux 模式) vs Kimi Notion 架构2.0 的 MCP shim 作为竞品方案对照分析, 增量演进而非重写
> hermes-native-expert-agents.md (2026-07-06): Kai 否决 Kimi CC-Teammates + 否决 "skill 当 agent", 要 Hermes-side persistent agents (YAML+persona+memory_scope) + per-agent memory 自进化 + CC 仅做场地. 任何 multi-agent 讨论默认走这套.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan + write §0 reading guide + §1 framing (T6 vs Kimi MCP shim: what each side is + Phase 44 citation anchor)</name>
  <files>.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md lines 1-100 (frontmatter + §0 + §1.1-§1.3) — pattern for bilingual frontmatter + 阅读指南 + 稳定性 markers
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1 (决策 1 T6 协议) + §2.5 (决策 5 α agent form) + §2.6 (决策 6 per-agent memory) + §2.7 (决策 7 Hermes 控结构 CC 控内容) — root-argument anchors to cite
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §0 + §1 (Phase 46 pattern — most recent sibling doc — apply same pattern here)
    - .planning/research/v10-orchestrator-design/SUMMARY.md CC-5 (lines 39-41 — anti-features ↔ anti-patterns ↔ pitfalls cross-source merge) + §"Roadmap Implications" (lines 89-101 — Phase 47 placement: parallel, only depends on 7 决策)
    - .planning/research/v10-orchestrator-design/STACK.md §1 lines 20-55 (T6 Recommended Stack — the SIDE A definition)
    - .planning/research/v10-orchestrator-design/FEATURES.md §4 lines 260-308 (Claude Agent SDK — the Kimi 方案的 SDK origin) + §7.4 lines 410-423 (B7.1 Microsoft layered)
  </read_first>

  <behavior>
  - Document frontmatter block (status / phase / audience / reading time / stability markers / confidence) mirrors 00-FIRST-PRINCIPLES.md + 02-ROUND-TABLE-PROTOCOL.md style
  - §0 阅读指南 exists with chapter map, 3-audience guide (Kai reviewer / Kimi Notion 续聊 counterpart / v11.0 PoC implementer), 稳定性 markers, "what this doc consumes from Phase 44/45/46" preamble
  - §1.1 exists: framing — "本文档是 v10.0 决策 1 (T6) 的横向验证 + subagent 形态否决的论据库" + cite Phase 44 决策 1/5/6/7 as root arguments + explicitly state the comparison's two sides (SIDE A: T6 = Hermes MCP server + tmux dispatch + CC native MCP client; SIDE B: Kimi 全 MCP shim = CC subagents + .claude/agents/*.md + everything-as-MCP-tool)
  - §1.2 exists: scope rules — "本对比只对照架构选型, 不对照具体实现细节 (那留给 04-MIGRATION-PATH)" + "CITE Phase 44/45/46 锁定字段, 不重定义"
  - §1.3 exists: 5 SC mapping table (本 doc 章节 → ROADMAP SC#1-5) so verification can cross-check
  - §1.4 exists: roadmap placement — "本 doc 是 05-POC-PLAN 的 'defense brief' + 06-CROSS-REPO-IMPACT 的 sibling 横切" + "Phase 44 决策 1-7 LOCKED, Phase 45 schemas LOCKED, Phase 46 protocol LOCKED — this doc only contrasts"
  - §1.5 exists: 3 load-bearing citations declared upfront (FEATURES §7.4 B7.1 Microsoft three-layer; FEATURES §11 B4.1 + §4.3 subagent rejection; ARCHITECTURE §8 anti-patterns Kimi-side subset)
  - §1.6 exists: side-by-side quick-glance table (T6 vs Kimi MCP shim, 1 row per dimension, 1 sentence each — the elevator-pitch version of §3)
  - Total Task 1 length ≥ 250 lines
  </behavior>

  <action>
  Create file `.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md`. Write frontmatter + §0 + §1.

  For §1.1 (framing, ~60 lines): Open with "本文档定义 v10.0 决策 1 (T6 协议) 的横向验证 —— 把 T6 (Hermes MCP server 扩展现有 mcp_serve.py + tmux dispatch + CC native MCP client) 与 Kimi Notion 架构2.0 的 'full MCP shim' 方案 (CC subagents as agent container + .claude/agents/*.md filesystem form + everything-as-MCP-tool + shared knowledge graph) 在 7 个维度上逐维对照, 证明 T6 在每个维度都更好地服务 Phase 44 锁定的 7 个决策。" Cite Phase 44 §2.1 (决策 1 — T6 协议 layer) + §2.5 (决策 5 — agents are Hermes-side entities, not CC subagents) + §2.6 (决策 6 — per-agent memory with scope/confidence) + §2.7 (决策 7 — Hermes controls turn_order/max_rounds/early_stop_rule, CC controls framing/synthesis) as root arguments; do NOT re-derive. Explicitly enumerate the two sides of the comparison:
    - SIDE A (T6, v10.0 锁定): Hermes-side YAML agents (`~/.hermes/agents/*.agent.yaml` per Phase 45 schema) + Hermes MCP server (extends `mcp_serve.py` per STACK §3.2, 7 new tools) + tmux dispatch for long-running CC sessions + CC as MCP client only.
    - SIDE B (Kimi 全 MCP shim, 显式否决): CC as the agent container (`.claude/agents/<name>.md` Claude Agent SDK filesystem form per FEATURES §4.2/§4.3) + everything-as-MCP-tool (no Hermes-side agent entity) + shared project-level knowledge graph + agent 完成-即销毁 per Agent-MCP pattern.
  State explicitly: "本文档对照的是架构选型, 不是 Notion 续聊的对话节奏 —— 那是 Kai 与 Kimi 的协作行为, 与本对比正交。"

  For §1.2 (scope rules, ~30 lines): "本对比只对照架构选型维度的优劣势, 不对照 (a) 具体实现 patch (留给 04-MIGRATION-PATH), (b) 跨 repo 同步策略 (留给 06-CROSS-REPO-IMPACT), (c) PoC 验收条件 (留给 05-POC-PLAN). 引用 Phase 44 决策 1-7, Phase 45 schema 字段 (memory_scope, scope, confidence, evidence_chain, persona_sha256), Phase 46 protocol 不变量 (serial execution / STACK-form MCP naming / atomic turn lifecycle) 采用 CITE-ONLY 策略, 不重定义。"

  For §1.3 (SC mapping table, ~30 lines): 5-row table mapping ROADMAP SC#1-5 → 本 doc 章节. Columns: | SC# | 描述 | 本 doc 解决章节 | 验证脚本 |

  For §1.4 (roadmap placement, ~30 lines): "本 doc 是 05-POC-PLAN.md 的 'defense brief' —— 当 v11.0 PoC 实施者问 '为什么 T6 不 full MCP shim?' 或 '为什么 Hermes-side YAML agent 不 CC subagent?', 本 doc 提供 section-and-line-precise 答案。06-CROSS-REPO-IMPACT.md 是 sibling 横切 (3-location 同步), 不消费本 doc 的对照细节, 但可以引用 subagent rejection 论据。51 VALIDATE 的 lint 脚本会 cross-check 本 doc 的 7 决策引用号一致。" State Phase 44 决策 1-7 + Phase 45 schemas + Phase 46 protocol are LOCKED — this doc only contrasts.

  For §1.5 (3 load-bearing citations declared upfront, ~40 lines): Three H3 subsections declaring upfront what citations the doc will lean on:
    - §1.5.1 Microsoft three-layer (cites FEATURES §7.4 B7.1 verbatim — "Platform-native orchestration for internal flows; MCP for tool and data access; A2A for cross-platform agent-to-agent messaging"). Map: internal flows = Hermes Python runtime; tool access = MCP (mcp_serve.py extension); cross-platform = A2A (deferred, not needed in v10.0 single-vendor). Teases §4 deep-dive.
    - §1.5.2 Subagent form rejection (cites FEATURES §11 B4.1 anti-feature row + §4.3 facts 1-3): context-isolated by default; memory field is source-scoped not namespace-scoped; transcripts 30-day auto-cleanup. Teases §5 deep-dive.
    - §1.5.3 Anti-patterns from ARCHITECTURE §8 Kimi-side subset: 8.1 (YAML as prompt dump — Kimi's filesystem form invites this); 8.3 (round table as pipeline step — Kimi's full-MCP-shim dispatch tempts synchronous block); 8.4 (per-agent mem0 instance — shared knowledge graph is the opposite extreme). Teases §6 deep-dive.

  For §1.6 (quick-glance table, ~60 lines): A 7-row side-by-side elevator-pitch table. One row per dimension (协议 / dispatch / callback / state / 多 agent / 实现成本 / 稳定性). Columns: | 维度 | T6 (v10.0) | Kimi 全 MCP shim | 一句话结论 |. Each row's "一句话结论" cites the relevant Phase 44 决策号. This table is the doc's TL;DR — §3-§6 expand each row into prose.

  Length target: ~250-300 lines for Task 1.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 250 {exit 0} {exit 1}' && grep -q "## §0" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -q "## §1" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && for term in "T6" "Kimi" "MCP shim" "决策 1" "决策 5" "决策 6" "决策 7" "FEATURES §7.4" "FEATURES §11" "FEATURES §4" "B7.1" "B4.1" "context-isolated" "platform-native" "STACK §1" "ARCHITECTURE §8" "SIDE A" "SIDE B" "00-FIRST-PRINCIPLES"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "00-FIRST-PRINCIPLES\|02-ROUND-TABLE-PROTOCOL\|01-AGENT-REGISTRY-SCHEMA" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - File created with bilingual frontmatter (status / phase / audience / reading time / stability / confidence)
  - §0 阅读指南 exists with chapter map + 3-audience guide + Phase 44/45/46 consumption preamble
  - §1.1 framing cites Phase 44 决策 1/5/6/7 + lists SIDE A (T6) vs SIDE B (Kimi full MCP shim)
  - §1.2 scope rules explicit (cite-only Phase 44/45/46)
  - §1.3 has SC#1-5 mapping table
  - §1.4 has roadmap placement + downstream consumer list
  - §1.5 has 3 load-bearing citations declared upfront (Microsoft layered / subagent rejection / anti-patterns)
  - §1.6 has 7-row quick-glance table (TL;DR for the doc)
  - Total doc length ≥ 250 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Write §2 (Kimi 方案 reconstruction — steelman before rebuttal) + §3 (7-dimension contrast table, full prose for dimensions 1-4: 协议 / dispatch / callback / state)</name>
  <files>.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/FEATURES.md §4 lines 260-308 (Claude Agent SDK — Kimi 方案的 SDK origin; §4.2 7-axis table + §4.3 subagent key facts)
    - .planning/research/v10-orchestrator-design/FEATURES.md §7.1-§7.3 lines 370-419 (Agent-MCP pattern — Kimi 方案的 close cousin; §7.2 7-axis table especially "短命 agent" + "共享 knowledge graph" rows)
    - .planning/research/v10-orchestrator-design/STACK.md §1 lines 20-55 (T6 Recommended Stack) + §3.2 lines 246-300 (7 MCP tools — the T6 contract for dimension 1)
    - .planning/research/v10-orchestrator-design/STACK.md §4 lines 611-655 (Transport — stdio vs streamable-http vs sse; T6 选 stdio, relevant for dimension 1 protocol)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §4 lines 285-465 (dispatch path + agent registry loading — T6's runtime mechanics for dimension 2)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §5 lines 464-572 (round table state layer — T6's state contract for dimension 4)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §2 (turn lifecycle — already in context from Task 1) + §5 (MCP tool contract for dimension 1+2)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1 (决策 1 T6 推导) + §2.7 (决策 7 Hermes/CC 分层) — root arguments to apply per dimension
  </read_first>

  <behavior>
  - §2.0 子节存在: 声明 §2 是 Kimi 方案的 steelman — 先把对方立场最强形态摆出来, 再在 §3-§6 逐维对照
  - §2.1 子节存在: Kimi 方案的 SDK origin (Claude Agent SDK filesystem form per FEATURES §4.2 — `.claude/agents/<name>.md` Markdown + YAML frontmatter, AgentDefinition 字段: description / prompt / tools / model / memory / mcpServers / maxTurns / background / effort)
  - §2.2 子节存在: Kimi 方案的 dispatch 模型 (CC main agent 调 Agent tool →派生 subagent, subagent 返回 final message; FEATURES §4.2 明确"不原生支持 multi-agent, 不是 round table")
  - §2.3 子节存在: Kimi 方案的 memory 模型 (memory 字段 'user'|'project'|'local', source-scoped not namespace-scoped per FEATURES §4.3 fact 2; subagent transcripts 30-day auto-cleanup per §4.3 fact 3)
  - §2.4 子节存在: Kimi 方案的 "全 MCP shim" 含义 (everything-as-MCP-tool — 把 agent ↔ agent 也塞进 MCP; 参考 Agent-MCP §7.2 "短命 agent + 共享 knowledge graph" 模式)
  - §2.5 子节存在: Kimi 方案的合理性陈述 (steelman) — 为什么这个方案有吸引力 (CC 原生 / 零额外 runtime / 工程量小 / .claude/agents/ 是 Anthropic 官方推的形态), 然后声明"本节不评判, 评判留给 §3-§6"
  - §3.0 子节存在: 声明 §3 是 7-dimension contrast 的 prose expansion (§1.6 是 TL;DR table, §3 是 full argument)
  - §3.1 子节存在: 维度 1 — 协议 (T6: Hermes MCP server + CC native MCP client, 单 server stdio, 引用 STACK §1 + §4; Kimi: full MCP shim, 多 MCP tool 调用 agent ↔ agent; 选型论据引用决策 1)
  - §3.2 子节存在: 维度 2 — dispatch (T6: Hermes Python runtime 是 agent container, tmux dispatch long-running, 引用 ARCHITECTURE §4; Kimi: CC 是 agent container, Agent tool 派生 subagent; 选型论据引用决策 5 + 7)
  - §3.3 子节存在: 维度 3 — callback (T6: round_table_open → turn N → submit_round_table_result 原子 lifecycle, 引用 Phase 46 §2; Kimi: subagent 完成-即返回-final-message, 无 round table 概念; 选型论据引用决策 7 + 3)
  - §3.4 子节存在: 维度 4 — state (T6: agents 是 Hermes-side YAML + per-agent scoped mem0 backend + ~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json per ARCHITECTURE §5; Kimi: subagent transcripts 独立文件 30 天清理 + 共享 knowledge graph; 选型论据引用决策 5 + 6)
  - 每个 §3.x 子节内部结构: 4 个 H4 子段 (T6 描述 / Kimi 描述 / T6 pros+cons / Kimi pros+cons) + 收尾的"选型论据"段引用决策号
  - Total Task 2 length ≥ 350 lines
  </behavior>

  <action>
  Append §2 + §3 to `.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md`.

  **§2 — Kimi 方案 Reconstruction (Steelman Before Rebuttal)**

  §2.0 (~20 lines): "本节把 Kimi Notion 架构2.0 的 'full MCP shim' 方案以最强形态陈述, 不预设评判。评判在 §3-§6。Kai 与 Kimi 的 Notion 续聊是协作行为, 与本节架构 reconstruction 正交。"

  §2.1 SDK origin (~50 lines):
  - **Form:** Claude Agent SDK filesystem-based subagent per FEATURES §4.2 row 1 — `.claude/agents/<name>.md` Markdown + YAML frontmatter. `AgentDefinition` Python dataclass fields: `description`, `prompt`, `tools`, `model`, `memory` ('user'|'project'|'local'), `mcpServers`, `maxTurns`, `background`, `effort` ('low'|'medium'|'high'|'xhigh'|'max').
  - **Why attractive:** Anthropic 官方 SDK + 官方推的 filesystem 形态 + CC 原生支持 + 零额外 runtime + 工程量小 (just write markdown).
  - **Citation:** FEATURES §4.2 + §4.3 verbatim.

  §2.2 dispatch 模型 (~40 lines):
  - **Pattern:** CC main agent 通过 `Agent` tool 派生 subagent, subagent 返回 final message.
  - **Concurrency:** v2.1.198+ subagent 默认 background, 可并发 (per FEATURES §4.2 row 5 "调度粒度").
  - **Multi-agent support:** FEATURES §4.2 row 3 verbatim — "不原生支持 multi-agent. Claude 自己作为 main agent, 可调 Agent tool 派生 subagent 做子任务, subagent 返回 final message; 不是 round table (无多 subagent 互发言)".
  - **Citation:** FEATURES §4.2 + §4.3 fact 1 (context-isolated).

  §2.3 memory 模型 (~40 lines):
  - **Scope:** `memory` 字段 'user'|'project'|'local', 决定 agent 从哪个 CLAUDE.md / 设置源加载 memory. FEATURES §4.3 fact 2 verbatim: "不是 namespace scoped memory (对比 LangGraph Store 的 namespace 元组, 这弱很多)".
  - **Persistence:** Subagent transcripts 持久化在独立文件, main conversation compaction 不影响 subagent; 30 天后自动清理 (FEATURES §4.3 fact 3).
  - **Citation:** FEATURES §4.2 row 2 + §4.3.

  §2.4 "全 MCP shim" 含义 (~40 lines):
  - **Pattern:** 把 agent ↔ agent 也塞进 MCP — Agent-MCP §7.2 模式: `create_agent` / `assign_task` / `view_tasks` / `update_task_status` / `ask_project_rag` / `send_agent_message` / `broadcast_message` MCP tools.
  - **Knowledge:** 共享 knowledge graph (RAG-over-MCP), 项目级; agent 级短暂记忆 60 秒 idle 即杀 (Agent-MCP §7.2 row 2).
  - **Citation:** FEATURES §7.1-§7.2.

  §2.5 steelman 合理性陈述 (~30 lines):
  - **Why attractive overall:** CC 原生 / 零额外 runtime / 工程量小 / `.claude/agents/` 是 Anthropic 官方推的形态 / 共享 knowledge graph 节省基础设施 / 短命 agent 节省 token cost.
  - **Steelman closing:** "如果 v10.0 的目标是 'minimize engineering effort while getting multi-agent behavior', Kimi 方案是合理的. 但 v10.0 的目标是 '让 agent 随项目越多越有经验' (决策 6) + 'Hermes 控结构' (决策 7) —— 见 §3-§6 对照."

  **§3 — 7-Dimension Contrast (Prose Expansion of §1.6 TL;DR)**

  §3.0 (~15 lines): "本节展开 §1.6 的 TL;DR table, 每维度 4 段: T6 描述 / Kimi 描述 / T6 pros+cons / Kimi pros+cons, 收尾 '选型论据' 引用 Phase 44 决策号. 维度 1-4 在本 task, 维度 5-7 在 Task 3."

  §3.1 维度 1 — 协议 (~80 lines):
  - **T6 描述:** Hermes MCP server (单 server, stdio transport, extends 现有 `mcp_serve.py` per STACK §1 + §3.2; 7 new tools in STACK-form naming) + CC native MCP client (`claude mcp add` 已配置 ✓ Connected per STACK §1). Protocol = MCP stdio (Phase 46 §4.1 + STACK §4).
  - **Kimi 描述:** Full MCP shim — 把 agent registry / dispatch / memory / coordination 全部塞进 MCP tool. Agent-MCP 模式: 7+ MCP tools (`create_agent` etc. per FEATURES §7.2 row 7). Protocol = MCP, but ** overloaded** — MCP 同时承载 agent ↔ tool AND agent ↔ agent.
  - **T6 pros:** (a) MCP 仅承担 agent ↔ tool (Microsoft 三层 §1.5.1 — tool layer), 单一职责; (b) stdio transport 零网络面 (PITFALLS §P10 privacy 正面); (c) 与现有 9 个 messaging tool 风格一致 (Phase 46 §4.2 STACK form).
  - **T6 cons:** (a) tmux dispatch 需要 Hermes-managed subprocess (额外 runtime 组件); (b) stdio 不能跨 host (未来 A2A 走 HTTP, 留 v12+).
  - **Kimi pros:** (a) 单 protocol 覆盖所有交互 (简单概念模型); (b) 无需 tmux 等额外组件.
  - **Kimi cons:** (a) 违反 Microsoft §7.4 B7.1 三层分层 (MCP 不该承载 agent ↔ agent); (b) MCP tool 爆炸 (create_agent / assign_task / send_agent_message / broadcast_message 全是 MCP tool, CC tool registry 膨胀); (c) agent ↔ agent 走 MCP 意味着每次都序列化整个 message — 高 token cost.
  - **选型论据:** 决策 1 (T6 协议). T6 让 MCP 单一承担 tool-access layer, 与 Microsoft §7.4 B7.1 + Phase 44 决策 1 推导链一致.

  §3.2 维度 2 — dispatch (~80 lines):
  - **T6 描述:** Hermes Python runtime 是 agent container — agent YAML 实体 (`~/.hermes/agents/*.agent.yaml` per Phase 45 schema) lives in Hermes process. tmux dispatch 仅用于 long-running CC sessions (e.g. round table panelist needs sustained context). Dispatcher reads agent YAML, applies tools whitelist (Phase 45 `tools` field), routes to Hermes-side executor (ARCHITECTURE §4).
  - **Kimi 描述:** CC 是 agent container — subagent lives in CC process as `.claude/agents/<name>.md`. Main CC agent invokes `Agent` tool →派生 subagent → subagent runs in its own CC session → returns final message. No Hermes-side agent entity.
  - **T6 pros:** (a) Agent 是 Hermes-side persistent entity (决策 5 — α form), 跨 CC session 存活; (b) Hermes 控 tools whitelist / memory_scope / execution_limits (决策 7 — Hermes 控结构); (c) CC 仅做场地 + 协调员 + 结构化助手 (决策 7), CC 不拥有 agent.
  - **T6 cons:** (a) Hermes 必须 extend `mcp_serve.py` 加 7 tools (工程量比"只写 .claude/agents/*.md"大); (b) tmux session 管理 (启动 / 监控 / gc) 是 Hermes 责任.
  - **Kimi pros:** (a) 零 Hermes-side runtime 改动 — agent 是 markdown 文件; (b) CC 原生 lifecycle 管理 subagent.
  - **Kimi cons:** (a) Agent lifecycle 绑 CC session — subagent transcripts 30 天清理 (FEATURES §4.3 fact 3), 违反决策 6 "agent 随项目越多越有经验"; (b) tools whitelist 由 CC Agent tool 控制, 不是 Hermes (违反决策 7); (c) memory 字段 'user'|'project'|'local' 是 source-scoped 不是 namespace-scoped (FEATURES §4.3 fact 2), 违反决策 6 per-agent scoped memory.
  - **选型论据:** 决策 5 + 7. T6 把 agent 拥有权放在 Hermes, 与决策 5 (α form) + 7 (Hermes 控结构) 一致; Kimi 方案把 agent 拥有权放在 CC, 违反两条决策.

  §3.3 维度 3 — callback (~80 lines):
  - **T6 描述:** Round table 原子 lifecycle per Phase 46 §2: `round_table_open(roundId, panel, question, maxRounds, turnOrderStrategy, earlyStopRule, projectId)` 立即返回 → turn N (`get_agent_opinion` per turnOrder) → `submit_round_table_result(roundId, conclusion, citedMemories, artifacts, conflicts)` 原子提交. Callback 是显式 MCP tool 调用, 不是隐式 subagent return.
  - **Kimi 描述:** Subagent 完成-即返回-final-message. 无 round table 概念 — main agent 调 Agent tool, subagent 跑完返回, main agent 读 final message. 无 turn lifecycle, 无 multi-turn coordination, 无 conflict arbitration.
  - **T6 pros:** (a) 显式 round table lifecycle 支持决策 3 (D2 — orchestrator 层做 round-based parallel); (b) `submit_round_table_result` 是原子 commit point, 支持 Phase 46 §3 memory conflict arbitration; (c) CC 控 question framing + synthesis (决策 7), Hermes 控 turn_order / max_rounds / early_stop_rule.
  - **T6 cons:** (a) Round table state file (`~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json`) 需要 gc pass (v11.0 PoC 未定); (b) 7 个 MCP tool 的 contract 学习曲线.
  - **Kimi pros:** (a) 极简 — 无 lifecycle 概念, main agent 自由组合.
  - **Kimi cons:** (a) 无法表达 round table — FEATURES §4.2 row 3 verbatim "不是 round table (无多 subagent 互发言)"; (b) 无法做 memory conflict arbitration (Phase 46 §3 的 P7 mitigation 需要 turn N 的 citedMemoryIds, subagent return 不带这个); (c) CC 既要控结构又要控内容, 违反决策 7 分层.
  - **选型论据:** 决策 7 + 3. T6 的显式 callback 支持决策 7 (Hermes 控结构) + 决策 3 (D2 round-based parallel); Kimi 的 subagent-return 模型无法表达 round table, 两个决策都服务不到.

  §3.4 维度 4 — state (~80 lines):
  - **T6 描述:** 三层 state. (1) Agent identity = Hermes-side YAML (`~/.hermes/agents/*.agent.yaml` per Phase 45 schema, 18 fields including persona_sha256 / fitness_score / evolution_log). (2) Per-agent memory = mem0 backend with `agent_id` filter (Phase 45 memory-record-schema, scope global|project|session, confidence, evidence_chain). (3) Round table state = `~/.hermes/agents/.runtime/{slug}/round_tables/{id}.json` per ARCHITECTURE §5.1 (Phase 46 round-table-state-schema).
  - **Kimi 描述:** (1) Agent identity = `.claude/agents/<name>.md` markdown (Kimi-side filesystem). (2) Memory = subagent transcripts 独立文件 30 天清理 + 共享 project-level knowledge graph (Agent-MCP §7.2 row 2). (3) Round table state = N/A (Kimi 无 round table).
  - **T6 pros:** (a) Per-agent scoped memory with namespace + scope + confidence + evidence_chain (决策 6 完整 support); (b) agent identity 持久 (跨 CC session 跨 project); (c) round table state append-only + crash-recoverable.
  - **T6 cons:** (a) mem0 backend filter behavior 在 18-agent × 100-project 规模下未实测 (OQ-12, defer 到 Phase 48 + v11.0 PoC); (b) 三层 state 意味着三个数据源, consistency 需 curator 协调.
  - **Kimi pros:** (a) 共享 knowledge graph 节省基础设施 (一个 graph server vs N mem0 backends).
  - **Kimi cons:** (a) subagent transcripts 30 天清理 → agent 长期经验不稳定, 违反决策 6; (b) 共享 graph → agent 间 memory bleed-through (PITFALLS §P12 cross-agent contamination), 违反决策 6 per-agent scope; (c) `.claude/agents/` 在 CC 工作目录, 不跨 project 同步 (除非 symlink, 增加运维复杂度).
  - **选型论据:** 决策 5 + 6. T6 的三层 state 把 agent identity + per-agent memory + round table state 都做成持久层, 与决策 5 (α form) + 6 (per-agent memory + curator) 一致; Kimi 的共享 graph + 短命 transcript 与决策 6 直接矛盾.

  Length target: ~350-400 lines for Task 2 (§2 ~180 + §3.1-§3.4 ~200).
  </action>

  <verify>
    <automated>grep -q "## §2" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -q "## §3" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -c "^### §2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 5 {exit 0} {exit 1}' && grep -c "^### §3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 4 {exit 0} {exit 1}' && for term in "AgentDefinition" "memory.*user.*project.*local\|memory 字段" "30 天\|30-day\|30 day" "Agent-MCP" "knowledge graph\|共享.*graph" "stdio" "决策 1" "决策 3" "决策 5" "决策 6" "决策 7" "FEATURES §4" "FEATURES §7" "ARCHITECTURE §4" "ARCHITECTURE §5" "STACK §1" "STACK §3.2" "round_table_open" "submit_round_table_result" "persona_sha256" "namespace-scoped\|namespace scoped" "选型论据"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "选型论据.*决策\|决策 [1-7]" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 8 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 600 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §2 exists with §2.0-§2.5 (Kimi 方案 steelman: SDK origin / dispatch / memory / 全 MCP shim 含义 / 合理性陈述)
  - §2 cites FEATURES §4.2 + §4.3 + §7.1-§7.2 verbatim
  - §3.1-§3.4 exist (维度 1 协议 / 维度 2 dispatch / 维度 3 callback / 维度 4 state) — each with 4-segment structure (T6 描述 / Kimi 描述 / T6 pros+cons / Kimi pros+cons) + 选型论据 citing 决策号
  - §3 cites Phase 46 §2 (turn lifecycle), Phase 45 fields (persona_sha256 / memory_scope), ARCHITECTURE §4-§5, STACK §1 + §3.2 + §4
  - §3.4 cites PITFALLS §P12 (cross-agent contamination)
  - Total doc length now ≥ 600 lines (cumulative with Task 1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §3.5-§3.7 (dimensions 5-7: 多 agent / 实现成本 / 稳定性) + §4 (Microsoft three-layer protocol validation — SC#4 deep-dive)</name>
  <files>.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/FEATURES.md §7.4 lines 410-423 (B7.1 Microsoft three-layer — load-bearing for §4 deep-dive) + §8 lines 426-465 (A2A — for §4.2 expansion位)
    - .planning/research/v10-orchestrator-design/FEATURES.md §10 line 528 (B2.2 — MAF "A2A 一等公民" 立场验证 v10.0 不依赖 A2A) + line 543 (B7.1 → this doc) + line 546 (B7.4 → 06-CROSS-REPO-IMPACT)
    - .planning/research/v10-orchestrator-design/STACK.md §7 lines 989-1015 (token cost ~550K/pipeline run serial — relevant for 实现成本 dimension)
    - .planning/research/v10-orchestrator-design/STACK.md §11 OQ lines 1117-1135 (open questions defer'd from STACK)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §8 lines 667-700 (anti-patterns — Kimi-side subset 8.1 / 8.3 / 8.4)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §3 lines 30-80 (scoped retrieval perf — relevant for 多 agent dimension) + §P12 lines 398-415 (cross-agent contamination)
    - /home/kai/.claude/projects/-data-workspace-hermes-agent/memory/feedback-glm-overload-reduce-concurrency.md (relevant for 多 agent dimension serial constraint)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §3 (v10.0 显式拒绝总表 — already in context from Task 1, root for §4 alignment)
  </read_first>

  <behavior>
  - §3.5 子节存在: 维度 5 — 多 agent (T6: round table with turn_order + memory conflict arbitration per Phase 46; Kimi: 不支持 multi-agent round table per FEATURES §4.2 row 3; 选型论据引用决策 3 + 7)
  - §3.6 子节存在: 维度 6 — 实现成本 (T6: extend mcp_serve.py 加 7 tools + Hermes agent registry + mem0 filter; Kimi: 写 .claude/agents/*.md + 共享 graph server; 选型论据引用决策 4)
  - §3.7 子节存在: 维度 7 — 稳定性 (T6: Hermes-side persistent + curator self-evolution + dry-run-first + bias canary; Kimi: 30-day cleanup + shared graph single-point-of-failure; 选型论据引用决策 6 + PITFALLS §P5/§P12)
  - §4.0 子节存在: 声明 §4 是 SC#4 deep-dive — Microsoft 三层协议分层验证 (§1.5.1 已声明 citation, §4 给完整论证)
  - §4.1 子节存在: Microsoft 三层引用 (verbatim from FEATURES §7.4 — "Platform-native orchestration for internal flows; MCP for tool and data access; A2A for cross-platform agent-to-agent messaging")
  - §4.2 子节存在: v10.0 T6 → Microsoft 三层 mapping table (internal flow = Hermes Python runtime per ARCHITECTURE §4; tool access = MCP per STACK §3.2; cross-platform = A2A deferred 不需要 per FEATURES §8 + B7.4)
  - §4.3 子节存在: Kimi 全 MCP shim → Microsoft 三层 violation analysis (Kimi 把 agent ↔ agent 塞进 MCP, 违反 Microsoft "MCP for tool access" 单一职责)
  - §4.4 子节存在: 业界共识论证 (B7.1 + B2.2 + B7.4 三条 FEATURES borrowable points 都指向同一结论 — T6 与业界共识一致)
  - §4.5 子节存在: A2A 扩展位声明 (v10.0 不需要 A2A; v12+ 跨厂商协作时 A2A 是正确协议, 留 06-CROSS-REPO-IMPACT.md 记录)
  - Total Task 3 length ≥ 350 lines
  </behavior>

  <action>
  Append §3.5-§3.7 + §4 to `.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md`.

  §3.5 维度 5 — 多 agent (~80 lines):
  - **T6 描述:** Round table 协议 per Phase 46 — `turn_order` 5-strategy enum (round-robin default + fixed / llm / matrix / fitness-weighted), memory conflict arbitration (comparator LLM + scope precedence + confidence voting), 强制串行 (1 panelist 1 turn sequential await per Phase 46 §4.1 + MEMORY.md global concurrency==1).
  - **Kimi 描述:** Subagent 不互发言 — FEATURES §4.2 row 3 verbatim "不是 round table (无多 subagent 互发言)". main agent 可以 fan-out 多 subagent (v2.1.198+ background), 但 subagent 之间不通讯.
  - **T6 pros:** (a) 真正的 round table 协议 (决策 3 — D2 storyboard-first-class requires round-based parallel); (b) memory conflict arbitration 避免 P7 (loud-memory agent wins); (c) serial constraint 兼容 GLM 4-key rotation (Phase 46 §4.1 + MEMORY.md feedback-glm-overload-reduce-concurrency.md).
  - **T6 cons:** (a) Serial execution means 7 panelist × 3 rounds ≈ 21 sequential LLM calls — ~137K tokens, ~10s serial (acceptable per STACK §7); (b) conflict arbitration 是新机制, v11.0 PoC 需 bias canary.
  - **Kimi pros:** (a) Background subagent 可以并发 (但 GLM 4-key rotation 会立刻撞 ceiling — FEATURES §14 gap 6 + CC-6).
  - **Kimi cons:** (a) 无法表达 round table, 决策 3 (D2) 无法实施; (b) subagent 间无通讯意味着 panelist 看不到彼此的 cited memory — memory conflict 无法 arbitration; (c) background 并发与 GLM concurrency==1 policy 矛盾.
  - **选型论据:** 决策 3 + 7. T6 的 round table 协议是决策 3 (D2) 的物理载体; Kimi 的 subagent fan-out 无法表达协商. Phase 46 §4.1 serial invariant 与 MEMORY.md feedback-glm-overload-reduce-concurrency.md 一致, Kimi 的 background 并发违反.

  §3.6 维度 6 — 实现成本 (~70 lines):
  - **T6 描述:** (a) Extend `mcp_serve.py` 加 7 MCP tools (STACK §3.2 — Pydantic schema 已 spec'd, 实施量 ~500-800 LOC); (b) Hermes agent registry (ARCHITECTURE §4 — load YAML, dispatch); (c) mem0 backend additive extension `_scoped_agent_id` (ARCHITECTURE §3.3); (d) Round table state file gc pass (defer v11.1+). v10.0 设计型, 零代码; v11.0 PoC 工程量 ~2-3 人周估算.
  - **Kimi 描述:** (a) 写 `.claude/agents/*.md` per expert (markdown, ~30-50 lines each × 15 experts = 450-750 lines markdown); (b) 共享 knowledge graph server (额外基础设施, Agent-MCP 模式); (c) 把 agent ↔ agent 协调塞进 MCP tools (`create_agent` / `assign_task` etc. per Agent-MCP §7.2).
  - **T6 pros:** (a) 复用现有 `mcp_serve.py` (907 lines 实读, extension 而非 rewrite); (b) 复用现有 mem0 plugin (v7.0 ship); (c) 复用现有 curator (v6.0 ship, extend `_feedback_scan_phase` → `_memory_evolution_phase` per Phase 45 §5); (d) 决策 4 (G2 通用编排) means kais-movie-pipeline 是首个 sample, 工程量摊薄.
  - **T6 cons:** (a) 7 个新 MCP tool 的 Pydantic schema 设计 + 测试 (v10.0 已 spec, v11.0 PoC 实施); (b) mem0 backend filter 行为未实测 (OQ-12).
  - **Kimi pros:** (a) markdown 文件工程量极小 (15 files × 50 lines); (b) 无需 extend Hermes runtime.
  - **Kimi cons:** (a) 共享 knowledge graph 是新基础设施 (RAG server, embedding cost, gc); (b) 把 agent ↔ agent 塞进 MCP 意味着设计 7+ MCP tools (`create_agent` etc.) — 工程量不比 T6 小; (c) 决策 4 (G2 通用编排) 在 Kimi 方案下难以实施 — 共享 graph 是 project-scoped, 跨 project 编排需要额外机制.
  - **选型论据:** 决策 4. T6 复用现有 v6.0/v7.0 设施 (curator + mem0), 与决策 4 (G2 通用编排) 一致; Kimi 方案需要新基础设施 (shared graph) 且工程量被低估.

  §3.7 维度 7 — 稳定性 (~80 lines):
  - **T6 描述:** (a) Hermes-side persistent agent (YAML 持久, 跨 CC session); (b) Per-agent scoped memory with curator-driven self-evolution (决策 6); (c) Curator dry-run-by-default + bias canary (Phase 45 §5 + Phase 50 acceptance criterion); (d) Memory record schema with `expires_at` / `verified_at` / `confidence` / `evidence_chain` (Phase 45 memory-record-schema) avoids P1/P2/P5; (e) Cross-project leakage mitigated by `scope` field (Phase 45) + filter routing (Option B per ARCHITECTURE §3.2).
  - **Kimi 描述:** (a) Subagent transcripts 30-day cleanup (FEATURES §4.3 fact 3) — agent experience 不可持久; (b) Shared knowledge graph single-point-of-failure (graph server down → all agents lose memory); (c) Cross-agent contamination risk (PITFALLS §P12 — shared graph + imperfect filter = bleed-through); (d) No curator self-evolution (决策 6 unavailable).
  - **T6 pros:** (a) Per-agent memory isolation (决策 6) avoids P12 cross-agent contamination; (b) `confidence` + `evidence_chain` + `expires_at` avoids P2 stale memory; (c) `persona_sha256` avoids P1 persona drift; (d) curator dry-run-first avoids P5 curator failure modes.
  - **T6 cons:** (a) v11.0 PoC 需 bias canary + latency benchmark (defer 到 Phase 50); (b) 18-agent × 100-project 规模下 mem0 filter 行为未实测 (OQ-12, defer Phase 48).
  - **Kimi pros:** (a) Shared graph 节省 per-agent memory backend 复杂度.
  - **Kimi cons:** (a) 30-day cleanup 直接违反决策 6 "agent 随项目越多越有经验"; (b) Shared graph 单点故障; (c) P12 cross-agent contamination 不可 mitigation (graph 是共享的); (d) 无 curator → 无 self-evolution → 决策 6 完全 unavailable.
  - **选型论据:** 决策 6 + PITFALLS §P5/§P12. T6 的 per-agent scoped memory + curator 是决策 6 的物理载体, P1/P2/P5/P12 都有字段级 mitigation; Kimi 方案直接违反决策 6 且无法 mitigation P5/P12.

  **§4 — Microsoft Three-Layer Protocol Validation (SC#4 Deep-Dive)**

  §4.0 (~20 lines): "本节是 ROADMAP SC#4 的完整论证. §1.5.1 已声明 citation, §4 给完整 mapping + violation analysis + 业界共识论证. Microsoft 指引是 FEATURES §7.4 B7.1 引用的一手源 (HIGH 置信度)."

  §4.1 Microsoft 三层引用 (~50 lines):
  - **Citation verbatim from FEATURES §7.4:** "Platform-native orchestration for internal flows (平台原生, 最低成本); MCP for tool and data access (MCP 做 agent ↔ tool); A2A for cross-platform agent-to-agent messaging (A2A 做 agent ↔ agent, 跨厂商)."
  - **MCP vs A2A selection table** (from FEATURES §7.3 verbatim): 4-row table — Multimodality / Multi-turn interactions / Orchestration / Negotiation — showing MCP vs A2A capability differences.
  - **Source authority:** Microsoft Agent Framework v1.0 GA (2026-04) + Microsoft multi-agent-patterns 官方指引 (URL: learn.microsoft.com/en-us/agents/architecture/multi-agent-patterns). FEATURES §7 置信度 HIGH.

  §4.2 v10.0 T6 → Microsoft 三层 mapping table (~60 lines):
  | Microsoft layer | v10.0 T6 instantiation | Phase 44 决策 | Citation |
  |-----------------|------------------------|---------------|----------|
  | Platform-native (internal flows) | Hermes Python runtime — agent registry YAML loader, dispatcher, curator, mem0 backend | 决策 5 (agent 是 Hermes-side entity) + 6 (curator-driven) + 7 (Hermes 控结构) | ARCHITECTURE §4 dispatch path; STACK §1 Recommended Stack |
  | MCP (tool access) | Hermes MCP server extends `mcp_serve.py` with 7 new tools (STACK §3.2 STACK-form naming) — CC calls these tools for agent persona / opinion / memory / round table lifecycle | 决策 1 (T6 协议) + 7 (Hermes 暴露结构 via schema) | STACK §1 + §3.2; Phase 46 §5 MCP tool contract |
  | A2A (cross-platform) | **Deferred — v10.0 single-vendor internal, 不需要 A2A.** v12+ 跨厂商协作时 A2A 是正确协议 (per B7.4). | 决策 1 (T6 排除 A2A for v10.0) | FEATURES §8.3 + B7.4; 06-CROSS-REPO-IMPACT.md will record expansion位 |

  §4.3 Kimi 全 MCP shim → Microsoft 三层 violation analysis (~50 lines):
  - **Violation 1:** Kimi 把 agent ↔ agent 协调塞进 MCP tools (`create_agent` / `assign_task` / `send_agent_message` / `broadcast_message` per Agent-MCP §7.2). Microsoft 三层明确 "MCP for tool access" — MCP 不该承载 agent ↔ agent. Kimi 方案在 Microsoft 框架下是 anti-pattern.
  - **Violation 2:** Kimi 把 agent identity 放在 CC filesystem (`.claude/agents/*.md`), 不是 platform-native. Microsoft "platform-native orchestration for internal flows" 意味着 internal agent lifecycle 应由 platform runtime 管 (Hermes Python), 不是 client-side filesystem.
  - **Violation 3:** Kimi 不为 A2A 留扩展位 — 共享 knowledge graph 是 project-scoped, 跨厂商协作时无法切换到 A2A. T6 把 agent identity 放在 Hermes runtime, 未来可暴露 A2A Agent Card (FEATURES §8.1 B8.1).

  §4.4 业界共识论证 (~40 lines):
  - **B7.1 (Microsoft 指引):** "三层协议分层 (platform / MCP / A2A) 验证 T6" — T6 完全 align.
  - **B2.2 (MAF "A2A 一等公民" 立场):** Microsoft Agent Framework 把 A2A 作为一等公民, 但明确 "A2A for cross-platform" — v10.0 是 single-vendor, 所以不依赖 A2A 与 MAF 立场一致.
  - **B7.4 (MS 指引 A2A 扩展位):** "A2A 是 v12+ 跨厂商扩展位" — T6 留扩展位, Kimi 方案不留.
  - **结论:** 三个 FEATURES borrowable points (B7.1 + B2.2 + B7.4) 都指向同一结论 — T6 与 Microsoft / MAF 业界共识一致, Kimi 方案违反三层分层.

  §4.5 A2A 扩展位声明 (~30 lines):
  - **v10.0:** 不需要 A2A (single-vendor Hermes ↔ CC).
  - **v12+:** 若 Kai 未来想让 hermes-agent 与外部 agent 系统协作 (e.g. 另一个项目 / 另一个 LLM provider), A2A 是正确协议.
  - **Citation:** FEATURES §8 + B7.4 + B8.3. **Defer 到 06-CROSS-REPO-IMPACT.md 记录扩展位.**

  Length target: ~350-400 lines for Task 3 (§3.5-§3.7 ~230 + §4 ~150).
  </action>

  <verify>
    <automated>grep -c "^### §3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -q "## §4" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -c "^### §4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 5 {exit 0} {exit 1}' && for term in "round table" "turn_order" "memory conflict" "serial" "concurrency==1\|concurrency == 1" "feedback-glm-overload-reduce-concurrency" "30 天\|30-day\|30 day" "shared graph\|共享.*graph\|knowledge graph" "P12\|cross-agent contamination" "决策 3" "决策 4" "决策 6" "决策 7" "Platform-native\|platform-native" "MCP for tool" "A2A for cross\|cross-platform" "B7.1" "B2.2" "B7.4" "B8.3\|B8.1" "violation" "扩展位" "选型论据"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^### §4\.2/,/^### §4\.3/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | grep -c "决策 [1-7]" | awk '$1 >= 3 {exit 0} {exit 1}' && awk '/^### §4\.3/,/^### §4\.4/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | grep -ci "violation" | awk '$1 >= 3 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 900 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §3.5-§3.7 exist (维度 5 多 agent / 维度 6 实现成本 / 维度 7 稳定性) — each with 4-segment structure + 选型论据
  - §3.7 cites PITFALLS §P5 + §P12 + Phase 45 memory-record fields
  - §4 exists with §4.0-§4.5 (Microsoft 三层 SC#4 deep-dive)
  - §4.1 cites FEATURES §7.4 + §7.3 verbatim (Microsoft 三层 + MCP vs A2A selection table)
  - §4.2 has v10.0 T6 → Microsoft 三层 mapping table citing 决策 1/5/6/7 + STACK §1 + ARCHITECTURE §4
  - §4.3 has 3 Kimi violation points
  - §4.4 cites B7.1 + B2.2 + B7.4 三条 borrowable points 都指向同一结论
  - §4.5 declares A2A 扩展位 defer 到 06-CROSS-REPO-IMPACT + cites B8.3
  - Total doc length now ≥ 900 lines (cumulative with Tasks 1+2)
  </done>
</task>

<task type="auto">
  <name>Task 4: Write §5 (Subagent form rejection — SC#3 deep-dive, FEATURES §11 B4.1 + §4.3) + §6 (Kimi-side borrowable parts evaluation — SC#5)</name>
  <files>.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/FEATURES.md §4.3 lines 280-298 (subagent key facts 1-3 — load-bearing for §5) + §11 line 559 (B4.1 anti-feature row, verbatim)
    - .planning/research/v10-orchestrator-design/FEATURES.md §10 lines 521-549 (full borrowable points table — for §6 evaluation of which Kimi-side ideas to borrow)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §3 (v10.0 显式拒绝总表 — root for §5 alignment)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §4.1 (serial constraint root cause — already in context from Task 1) + §3 (memory conflict arbitration — already in context)
    - .planning/research/v10-orchestrator-design/STACK.md §1 lines 35-48 (T6 Infrastructure — tmux + FastAPI + starlette)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §8 lines 667-700 (anti-patterns 8.1 / 8.3 / 8.4 — Kimi-side subset)
    - /home/kai/.claude/projects/-data-workspace-hermes-agent/memory/hermes-native-expert-agents.md (Kai's explicit rejection of "skill 当 agent" + CC-Teammates pattern)
    - /home/kai/.claude/projects/-data-workspace-hermes-agent/memory/coding-agent-vs-mcp-shim.md (the prior analysis this doc formalizes)
  </read_first>

  <behavior>
  - §5.0 子节存在: 声明 §5 是 SC#3 deep-dive — subagent 形态否决论据完整论证 (§1.5.2 已声明 citation, §5 给完整论证)
  - §5.1 子节存在: FEATURES §11 B4.1 anti-feature row verbatim citation + 解读 (subagent context-isolated, memory 弱, 30 天清理)
  - §5.2 子节存在: FEATURES §4.3 三个 fact 逐条展开 (fact 1 context-isolated / fact 2 source-scoped not namespace-scoped / fact 3 30-day cleanup)
  - §5.3 子节存在: 为什么 context-isolated subagent 不适合做 round table panelist (4 个具体原因, 交叉引用 Phase 46 §2 + §3)
  - §5.4 子节存在: 为什么 source-scoped memory 不能做 per-agent 自进化 (与决策 6 矛盾的机制分析)
  - §5.5 子节存在: 为什么 30-day cleanup 违反决策 6 长生命周期 agent (operator 投入 + 经验累积 不可接受丢失)
  - §5.6 子节存在: Hermes-side YAML agent 形态 (决策 5 α) 的对照优势 (per Phase 45 schema + 18 fields)
  - §5.7 子节存在: user memory hermes-native-expert-agents.md 的 explicit rejection 引用 (Kai 否决 CC-Teammates + 否决 "skill 当 agent")
  - §6.0 子节存在: 声明 §6 是 SC#5 — Kimi 方案中可借鉴的部分评估
  - §6.1 子节存在: borrowable evaluation table (Kimi 方案 ideas × T6 兼容性 × 借鉴条件 × 喂给哪个下游 doc)
  - §6.2 子节存在: Agent Card 概念 (FEATURES §8.1 B8.1 — 借鉴, 不用 A2A 协议层) → 01-AGENT-REGISTRY-SCHEMA.md `agent_card` 子段
  - §6.3 子节存在: hooks 生命周期 (FEATURES §4.4 B4.2 — PreTurn/PostTurn audit) → 02-ROUND-TABLE-PROTOCOL.md §6.2 (已 deferred 到 v11.1+)
  - §6.4 子节存在: effort 字段 (FEATURES §4.4 B4.3 — agent 级 reasoning_effort low/medium/high/xhigh/max) → 01-AGENT-REGISTRY-SCHEMA.md (Phase 45 已收)
  - §6.5 子节存在: file-level lock (FEATURES §7.4 B7.3 — asset_locks for parallel multi-agent editing) → 02-ROUND-TABLE-PROTOCOL.md §6.4 (已 deferred, v11.0 serial 不需要)
  - §6.6 子节存在: 不借鉴清单 (Kimi 方案中 v10.0 显式拒绝的部分, 引用 Phase 44 §3 显式拒绝总表 + §11 anti-features)
  - Total Task 4 length ≥ 350 lines
  </behavior>

  <action>
  Append §5 + §6 to `.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md`.

  **§5 — Subagent Form Rejection (SC#3 Deep-Dive)**

  §5.0 (~20 lines): "本节是 ROADMAP SC#3 的完整论证. §1.5.2 已声明 citation, §5 给完整 7-子节论证. Subagent 形态 (Claude Agent SDK filesystem form `.claude/agents/<name>.md`) 是 Kimi 方案的 agent 容器, v10.0 决策 5 (α form: Hermes-side YAML agent) 显式否决."

  §5.1 FEATURES §11 B4.1 verbatim + 解读 (~30 lines):
  - **Citation verbatim from FEATURES §11 anti-feature row 1:**
    > 业界模式: Subagent 作为 agent 容器
    > 出处: Claude Agent SDK `.claude/agents/*.md`
    > v10.0 拒绝理由: subagent context-isolated, memory 弱, 30 天清理
    > v10.0 替代: Hermes-side YAML agent (`~/.hermes/agents/`)
  - **解读:** 三个拒绝理由 (context-isolated / memory 弱 / 30 天清理) 在 §5.3-§5.5 逐条展开.

  §5.2 FEATURES §4.3 三个 fact 逐条展开 (~30 lines):
  - **Fact 1 (§4.3):** "Subagent 默认 context-isolated: 'The only channel from parent to subagent is the Agent tool's prompt string'. 这意味着 subagent 不天然适合做 round table panelist (panelist 需要看完整历史)."
  - **Fact 2 (§4.3):** "memory 字段限定 source: 'user' / 'project' / 'local', 不是 namespace scoped memory (对比 LangGraph Store 的 namespace 元组, 这弱很多)."
  - **Fact 3 (§4.3):** "Subagent transcripts 持久化在独立文件, main conversation compaction 不影响 subagent; 30 天后自动清理."

  §5.3 为什么 context-isolated subagent 不适合做 round table panelist (~80 lines):
  - **Round table panelist 需要看完整历史:** Phase 46 §2.2 turn N — panelist 调 `get_agent_opinion(agentId, topic, context, priorDiscussion)`. `priorDiscussion` 包含所有先前 turns. Subagent 默认 context-isolated 意味着 parent 必须把 prior turns 全序列化进 Agent tool 的 prompt string — 高 token cost (round 5 × 7 panelist × 6.5K tokens ≈ 230K tokens just for prior context).
  - **Memory conflict arbitration 需要 panelist 看到彼此 cited memory:** Phase 46 §3 — `Turn.citedMemoryIds` + `ConflictRecord` 要求 panelist 能检索其他 panelist 引用的 memory. Subagent context-isolated 意味着 panelist 无法跨 subagent session 检索 — conflict arbitration 不可实施.
  - **Turn lifecycle atomicity 与 subagent fan-out 不兼容:** Phase 46 §2 — `round_table_open` → turn N → `submit_round_table_result` 是原子 lifecycle, 每个 turn 是一次 MCP tool 调用. Subagent fan-out 是 fire-and-forget — 无 turn 概念, 无法表达 "panelist A 等 panelist B 发言完再发言" (serial invariant).
  - **Decision 7 分层违反:** 决策 7 — Hermes 控 turn_order / max_rounds / early_stop_rule. Subagent fan-out 由 CC main agent 控制 turn_order — 违反分层.

  §5.4 为什么 source-scoped memory 不能做 per-agent 自进化 (~50 lines):
  - **Decision 6 要求 per-agent scoped memory:** Phase 45 memory-record-schema — `agent_id` required + `scope` (global|project|session) + namespace routing via mem0 `_scoped_agent_id` filter.
  - **Subagent memory 字段是 source-scoped:** FEATURES §4.3 fact 2 verbatim — "'user' / 'project' / 'local'" 决定 agent 从哪个 CLAUDE.md 加载 memory. 这是 source-file-scoping (哪个 markdown 文件), 不是 agent-scoping (哪个 agent 的经验).
  - **Curator-driven self-evolution 不可能:** 决策 6 curator 跑 `_memory_evolution_phase` per agent — 聚合 feedback → 写 memory record → 更新 `fitness_score`. Subagent memory 是 CLAUDE.md file (静态 markdown), curator 无法 read/write per-record — 无法自进化.

  §5.5 为什么 30-day cleanup 违反决策 6 (~40 lines):
  - **Decision 6 要求长生命周期 agent:** SUMMARY §Executive Summary verbatim — "v10.0 不写一行业码, 而是把 'agent 随项目越多越有经验' 推导成 7 份设计文档". "越多越有经验" 要求 agent identity + memory 跨 project 跨 CC session 持久.
  - **Subagent transcripts 30 天清理:** FEATURES §4.3 fact 3. 30 天意味着 operator 投入 (调 persona / 喂 feedback / 跑 fitness battery) 在 30 天后丢失 — 不可接受.
  - **Comparison:** T6 agent YAML `~/.hermes/agents/*.agent.yaml` 持久 + mem0 backend memory record 持久 (无 cleanup) + curator 写 `evolution_log` 持久. 三层持久与决策 6 一致.

  §5.6 Hermes-side YAML agent 形态对照优势 (~50 lines):
  - **Phase 45 18-field schema:** `name` / `persona` / `persona_sha256` / `tools` / `memory_scope` / `default_invocation` / `fitness_score` / `evolution_log` / `lineage` / etc.
  - **对照 subagent AgentDefinition:** AgentDefinition 字段 (description / prompt / tools / model / memory / mcpServers / maxTurns / background / effort) 是 subset — 缺 `memory_scope` (namespace), `persona_sha256` (drift detection), `evolution_log` (curator), `fitness_score` (P8 mitigation), `lineage` (溯源审计).
  - **决策 5 (α form) 是 FEATURES §12.2 Differentiators 表的物理载体:** v10.0 独创组合 (memory_scope + lineage + curator-driven + panelist_role + turn_order 三态 + subpanel) 在 subagent AgentDefinition 中无表达位.

  §5.7 user memory explicit rejection (~20 lines):
  - **Citation:** `~/.claude/projects/-data-workspace-hermes-agent/memory/hermes-native-expert-agents.md` (2026-07-06): "Kai 否决 Kimi CC-Teammates + 否决 'skill 当 agent', 要 Hermes-side persistent agents (YAML+persona+memory_scope) + per-agent memory 自进化 + CC 仅做场地. 任何 multi-agent 讨论默认走这套."
  - **Citation:** `coding-agent-vs-mcp-shim.md` (2026-07-06): "v10.0 设计 milestone 必须把已有 coding-agent (tmux 模式) vs Kimi Notion 架构2.0 的 MCP shim 作为竞品方案对照分析, 增量演进而非重写."
  - **结论:** Kai 已显式否决 subagent 形态. 本节是 user-side 决策的 design-side 论证 — 论据相互印证.

  **§6 — Kimi-Side Borrowable Parts Evaluation (SC#5)**

  §6.0 (~15 lines): "本节是 ROADMAP SC#5 — Kimi 方案中可借鉴的部分 (如有) 显式列出 + 评估借鉴条件. 借鉴不等于照抄 — 每条都标注 'T6 兼容性' + '借鉴条件' + '喂给哪个下游 doc'."

  §6.1 borrowable evaluation table (~30 lines):
  | Kimi-side idea | FEATURES borrowable ID | T6 兼容性 | 借鉴条件 | 喂给 doc |
  |----------------|------------------------|-----------|----------|----------|
  | Agent Card concept (capabilities/inputs/outputs JSON) | B8.1 | ✅ 兼容 | 作为 agents-schema `agent_card` 子段 (Phase 45 已收) | 01-AGENT-REGISTRY-SCHEMA.md |
  | Hooks lifecycle (PreTurn/PostTurn audit) | B4.2 | ✅ 兼容 | round table panelist 前后事件, v11.1+ opt-in | 02-ROUND-TABLE-PROTOCOL.md §6.2 |
  | `effort: low\|medium\|high\|xhigh\|max` agent 级 reasoning | B4.3 | ✅ 兼容 | reasoning_effort 字段 (Phase 45 已收) | 01-AGENT-REGISTRY-SCHEMA.md |
  | File-level lock (parallel multi-agent editing) | B7.3 | ⚠️ v11.1+ only | v11.0 serial 不需要; v11.1+ subpanel 时启用 | 02-ROUND-TABLE-PROTOCOL.md §6.4 |
  | Agent-MCP short-lived agent pattern | B7.2 | ❌ 拒绝 | 违反决策 6 长生命周期 | 00-FIRST-PRINCIPLES.md §3 |
  | Shared knowledge graph | §7.2 | ❌ 拒绝 | 违反决策 6 per-agent scoped memory + P12 risk | 00-FIRST-PRINCIPLES.md §3 |
  | `.claude/agents/` filesystem form | §4.2 | ❌ 拒绝 | 违反决策 5 (Hermes-side entity) | 00-FIRST-PRINCIPLES.md §3 + 本 doc §5 |

  §6.2 Agent Card concept (~30 lines): B8.1 — 借鉴, A2A 协议层不用. v10.0 agents-schema.yaml `agent_card` 子段 (capabilities / inputs / outputs JSON) 用于 round table panelist 自动匹配. Phase 45 已收.

  §6.3 hooks 生命周期 (~30 lines): B4.2 — 借鉴, deferred to v11.1+. Phase 46 §6.2 已 evaluation: v11.0 PoC 用 Turn schema 隐式捕获 PreTurn/PostTurn; v11.1+ 显式事件流供 curator 实时监控.

  §6.4 effort 字段 (~25 lines): B4.3 — 借鉴, Phase 45 已收为 `reasoning_effort` 字段. 重要 agent (screenplay / cinematographer) 用 `high` / `xhigh`, 辅助 agent (glossary checker) 用 `low`. 与 GLM/Claude reasoning_effort 参数对齐.

  §6.5 file-level lock (~25 lines): B7.3 — 借鉴, deferred to v11.1+. Phase 46 §6.4 已 evaluation: v11.0 serial 不需要; v11.1+ subpanel 启用时 Hermes-side lock manager (in-memory map of assetPath → lockHolder).

  §6.6 不借鉴清单 (~40 lines):
  - **Subagent as agent container (§4.2):** 违反决策 5. Phase 44 §3 显式拒绝总表 + 本 doc §5.
  - **Short-lived agent (B7.2):** 违反决策 6. Phase 44 §3.
  - **Shared knowledge graph (§7.2):** 违反决策 6 per-agent scope + P12 cross-agent contamination. Phase 44 §3.
  - **Full MCP shim for agent ↔ agent (§7.1):** 违反 Microsoft 三层 (§4.3 violation 1).
  - **`memory: true` boolean (CrewAI §3):** Phase 44 §3 (反例 B3.3).
  - **Handoff as round table 替代 (OpenAI Swarm §5):** Phase 44 §3 + FEATURES §11 anti-feature row 3.

  Length target: ~350-400 lines for Task 4 (§5 ~250 + §6 ~120).
  </action>

  <verify>
    <automated>grep -q "## §5" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -q "## §6" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -c "^### §5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "^### §6\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 6 {exit 0} {exit 1}' && for term in "B4.1" "context-isolated" "30 天\|30-day\|30 day" "namespace scoped\|namespace-scoped" "panelist" "round table" "memory conflict" "serial" "决策 5" "决策 6" "Phase 46 §2\|02-ROUND-TABLE-PROTOCOL.*§2\|turn lifecycle" "Phase 46 §3\|02-ROUND-TABLE-PROTOCOL.*§3\|memory conflict arbitration" "fitness_score" "persona_sha256" "evolution_log" "lineage" "B8.1\|Agent Card\|agent_card" "B4.2\|hooks" "B4.3\|effort\|reasoning_effort" "B7.3\|file-level lock\|asset_locks" "B7.2\|短命 agent" "hermes-native-expert-agents" "coding-agent-vs-mcp-shim" "不借鉴"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^### §6\.1/,/^### §6\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | grep -c "B[0-9]\.[0-9]" | awk '$1 >= 5 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 1200 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §5 exists with §5.0-§5.7 (subagent rejection SC#3 deep-dive, 7 subsections)
  - §5.1 cites FEATURES §11 B4.1 verbatim
  - §5.2-§5.5 each展开 FEATURES §4.3 facts + 与 Phase 46 §2/§3 + 决策 6/7 矛盾分析
  - §5.6 对照 Phase 45 18-field schema 优势
  - §5.7 cites user memory hermes-native-expert-agents.md + coding-agent-vs-mcp-shim.md
  - §6 exists with §6.0-§6.6 (Kimi-side borrowable evaluation SC#5)
  - §6.1 has 7-row borrowable evaluation table (B8.1 / B4.2 / B4.3 / B7.3 / B7.2 / §7.2 / §4.2)
  - §6.6 cites Phase 44 §3 显式拒绝总表
  - Total doc length now ≥ 1200 lines (cumulative with Tasks 1+2+3)
  </done>
</task>

<task type="auto">
  <name>Task 5: Write §7 (Phase 44 7 决策 cross-validation audit) + §8 (downstream citation guide) + §9 (summary + references)</name>
  <files>.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1-§2.7 (decisions 1-7 — root arguments to cross-validate)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §7 (downstream citation card pattern — apply here)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §5 (downstream citation cards pattern — Phase 44 root)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §"Design Decisions Validated" table (7 决策 × 4 research support)
    - .planning/ROADMAP.md §Phase 49/50/51 (downstream docs consuming this comparison)
  </read_first>

  <behavior>
  - §7.0 子节存在: 声明 §7 是 Phase 44 决策 1-7 的 cross-validation audit — 本 doc 的对照是否一致支持每个决策
  - §7.1 子节存在: 7-row 决策 audit table (决策 1-7 × T6 vs Kimi 选型 × 本 doc 论证章节 × 是否一致)
  - §7.2 子节存在: 偏差分析 (本 doc 是否发现 Phase 44 决策需修正的情况; 预期 = 无偏差, 但显式声明)
  - §7.3 子节存在: Phase 44 §3 显式拒绝总表 与本 doc Kimi 方案否决 的一致性 (subagent / shared graph / short-lived agent / full MCP shim 都在 §3 + 本 doc §5/§6 出现)
  - §8.0 子节存在: downstream citation guide — 列出 04/05/06/51 引用本文档论据时, 应从哪个章节取
  - §8.1 子节存在: coherence 声明 (7-dimension contrast + Microsoft 三层 + subagent 否决 + Kimi borrowable + 7 决策 audit = 完整 SC#1-5 coverage)
  - §8.2 子节存在: References (00-FIRST-PRINCIPLES + 01-AGENT-REGISTRY-SCHEMA + 02-ROUND-TABLE-PROTOCOL + ARCHITECTURE + FEATURES + STACK + PITFALLS + SUMMARY + MEMORY.md hermes-native-expert-agents + coding-agent-vs-mcp-shim + CLAUDE.md + Phase 44/45/46 commit references)
  - Total Task 5 length ≥ 200 lines
  </behavior>

  <action>
  Append §7 + §8 to `.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md`.

  **§7 — Phase 44 7 决策 Cross-Validation Audit**

  §7.0 (~15 lines): "本节 audit 本 doc 的对照是否一致支持 Phase 44 锁定的 7 决策. 预期 = 7/7 一致 (本 doc 是 horizontal validation, 不是 re-derivation). 若发现偏差, 应在 §7.2 显式声明 + 提交 Phase 44 修订."

  §7.1 7-row 决策 audit table (~80 lines):
  | 决策 # | 决策 (一句话) | T6 选型 | Kimi 选型 | 本 doc 论证章节 | 一致? |
  |--------|---------------|---------|-----------|------------------|-------|
  | 1 | T6 协议 (Hermes MCP server + tmux dispatch + CC native MCP client) | T6 IS 决策 1 | Kimi 全 MCP shim 违反 Microsoft 三层 (§4.3) | §3.1 + §4 | ✅ |
  | 2 | B3a Python runner 增量迁移 (delegate-only phase 迁 CC) | T6 run_python_phase boundary tool (STACK §3.2 Tool 7) | Kimi 无 boundary tool 概念, subagent 接管所有 phase | §3.2 dispatch | ✅ |
  | 3 | D2 storyboard-first-class (orchestrator round-based parallel) | T6 round table 协议 (Phase 46) | Kimi 不支持 round table (FEATURES §4.2 row 3) | §3.3 callback + §3.5 多 agent | ✅ |
  | 4 | G2 通用编排框架 (kais-movie-pipeline 首个 sample) | T6 复用 v6.0 curator + v7.0 mem0 (决策 4 工程量摊薄) | Kimi shared graph 是 project-scoped, 跨 project 编排需额外机制 | §3.6 实现成本 | ✅ |
  | 5 | α agent form (YAML + persona + tools + refs + memory_scope + lineage) | T6 Hermes-side YAML 18-field (Phase 45) | Kimi AgentDefinition 缺 memory_scope/persona_sha256/evolution_log/lineage | §3.4 state + §5.6 | ✅ |
  | 6 | per-agent memory + curator-driven 自进化 | T6 mem0 backend + `_scoped_agent_id` + `_memory_evolution_phase` (Phase 45 §3.3) | Kimi source-scoped memory + 30-day cleanup + 无 curator | §3.4 state + §3.7 稳定性 + §5.4 + §5.5 | ✅ |
  | 7 | 分层 CC 角色 (Hermes 控结构, CC 控内容) | T6 Hermes 控 turn_order/max_rounds/early_stop_rule (Phase 46 §2) | Kimi CC main agent 控 turn_order (subagent fan-out), 违反分层 | §3.2 dispatch + §3.3 callback + §5.3 | ✅ |

  §7.2 偏差分析 (~30 lines):
  - **预期:** 7/7 一致 (本 doc 是 horizontal validation).
  - **实际:** 7/7 一致. 本 doc 未发现 Phase 44 决策需修正.
  - **声明:** "本 audit 是 horizontal validation — 不是 re-derivation. 若 v11.0 PoC 实施中发现 Kimi 方案某些 aspect 被 §6 borrowable evaluation 低估, 应在 05-POC-PLAN.md 的 risk register 标注 + 在 Phase 51 audit 复审."

  §7.3 Phase 44 §3 显式拒绝总表 一致性 (~30 lines):
  - **Phase 44 §3 显式拒绝 (≥10 条 anti-features):** subagent as agent container / short-lived agent / shared graph / full MCP shim for agent ↔ agent / handoff as round table / crew pipeline as 协商 / `memory: true` boolean / CrewAI 30+ 字段全抄 / 等.
  - **本 doc Kimi 方案否决 (§5 + §6.6):** subagent form / shared knowledge graph / short-lived agent / full MCP shim / `.claude/agents/` filesystem form.
  - **Overlap:** 4 条直接重叠 (subagent / short-lived / shared graph / full MCP shim).
  - **声明:** 一致. Phase 44 §3 是 root rejection, 本 doc 是 per-dimension contrast + steelman + Kimi-side borrowable evaluation — 三层互补.

  **§8 — Downstream Citation Guide + Summary + References**

  §8.0 downstream citation card table (~30 lines):
  | Downstream doc | Cite from this doc | Do NOT re-derive | Should derive |
  | 04-MIGRATION-PATH.md | §3.2 dispatch (T6 boundary tool) + §3.4 state (三层 state) | 7-dimension contrast, subagent rejection | 15-expert transform rules, retained-phases allowlist |
  | 05-POC-PLAN.md | §3.7 稳定性 (T6 risk + Kimi risk 对比) + §6 borrowable evaluation | subagent rejection detail | PoC acceptance criteria, fitness battery, latency SLO |
  | 06-CROSS-REPO-IMPACT.md | §4.5 A2A 扩展位 + §3.4 state 跨 project 一致性 | Microsoft 三层 detail | 3-location sync + Option B vs 物理分区 |
  | 51 VALIDATE lint | §7 7 决策 audit table + §3 7-dimension table + §6.1 borrowable table | entire doc | cross-doc consistency lint script |

  §8.1 coherence 声明 (~30 lines): "7-dimension contrast (协议 / dispatch / callback / state / 多 agent / 实现成本 / 稳定性) + Microsoft 三层 protocol validation + subagent form rejection + Kimi-side borrowable evaluation + Phase 44 7 决策 cross-validation audit = 完整 ROADMAP SC#1-5 coverage. v11.0 PoC implementer 引用本 doc 的 §3 dimension table + §5 subagent rejection + §6.1 borrowable table 即可 defending 'why T6 not full MCP shim', 不需重新推导 Phase 44 决策."

  §8.2 References (~25 lines): 00-FIRST-PRINCIPLES.md + 01-AGENT-REGISTRY-SCHEMA.md + 02-ROUND-TABLE-PROTOCOL.md + ARCHITECTURE.md §4/§5/§8 + FEATURES.md §4/§7/§8/§10/§11 + STACK.md §1/§3.2/§4 + PITFALLS.md §P5/§P12 + SUMMARY.md + MEMORY.md hermes-native-expert-agents.md + coding-agent-vs-mcp-shim.md + feedback-glm-overload-reduce-concurrency.md + CLAUDE.md + Phase 44/45/46 commit references + Microsoft multi-agent-patterns URL + Claude Agent SDK subagents docs URL.

  Length target: ~200-250 lines for Task 5.
  </action>

  <verify>
    <automated>grep -q "## §7" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -q "## §8" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md && grep -c "^### §7\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 3 {exit 0} {exit 1}' && grep -c "^### §8\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 2 {exit 0} {exit 1}' && for term in "决策 1" "决策 2" "决策 3" "决策 4" "决策 5" "决策 6" "决策 7" "audit" "一致" "Phase 44" "00-FIRST-PRINCIPLES" "01-AGENT-REGISTRY-SCHEMA" "02-ROUND-TABLE-PROTOCOL" "04-MIGRATION-PATH" "05-POC-PLAN" "06-CROSS-REPO-IMPACT" "51 VALIDATE\|51-VALIDATE\|VALIDATE"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^### §7\.1/,/^### §7\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | grep -c "决策 [1-7]" | awk '$1 >= 7 {exit 0} {exit 1}' && awk '/^### §7\.1/,/^### §7\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | grep -c "✅" | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "Microsoft multi-agent-patterns\|learn.microsoft.com.*multi-agent" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 1 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md | awk '$1 >= 1400 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §7 exists with §7.0-§7.3 (7 决策 cross-validation audit + 偏差分析 + Phase 44 §3 一致性)
  - §7.1 has 7-row audit table with 决策 1-7 each marked ✅
  - §7.2 declares 7/7 一致 (no Phase 44 revision needed)
  - §8 exists with §8.0 downstream citation card + §8.1 coherence 声明 + §8.2 References
  - All 7 决策 (1-7) appear in §7.1 audit table
  - Microsoft multi-agent-patterns URL appears in references
  - Total doc length ≥ 1400 lines (final)
  - 04-MIGRATION-PATH + 05-POC-PLAN + 06-CROSS-REPO-IMPACT + 51 VALIDATE all appear in downstream citation card
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc → v11.0 PoC implementer | The deliverable is a design doc. Misinterpretation by v11.0 PoC implementers (e.g. "the comparison means we should adopt Kimi's shared graph") is the primary risk. Mitigated by §7 cross-validation audit + §8.0 downstream citation card. |
| 03-COMPARISON-VS-KIMI-MCP-SHIM.md → Phase 44/45/46 | This doc CONSUMES Phase 44 决策 1-7, Phase 45 schema fields, Phase 46 protocol invariants. Re-derivation would cause cross-doc drift. Mitigated by explicit "CITE ONLY, do NOT redefine" preamble in §1.2 + per-section "Cites 决策 N" annotations. |
| Steelman §2 → Rebuttal §3-§6 | The steelman reconstruction of Kimi 方案 must not be so strong that it overturns Phase 44 决策. Mitigated by §7 cross-validation audit (7/7 一致 check) — if steelman reveals a decision needs revision, §7.2 surfaces it explicitly rather than silently. |
| Citation accuracy → v10.0 audit trail | Every claim about FEATURES §X / STACK §Y / ARCHITECTURE §Z must be verifiable. Mitigated by verbatim citation style (section number + line range + quotation marks) + Phase 51 lint script cross-check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | 7-dimension contrast table (§3) — T6 pros could be over-stated, Kimi cons could be strawman | mitigate | §2 steelman reconstruction forces honest Kimi representation first; §7.1 audit table requires ✅ for each 决策 with citation, preventing silent bias |
| T-47-02 | Information Disclosure | Subagent rejection (§5) — citing Anthropic docs out of context could misrepresent Claude Agent SDK capabilities | mitigate | §5.1-§5.2 verbatim citation from FEATURES §11 + §4.3 (which itself cites official docs with URLs); §8.2 References includes Anthropic SDK docs URL for verifiability |
| T-47-03 | Denial of Service | Phase 44 决策 drift — if this doc's comparison subtly redefines 决策 1-7, downstream docs inherit the drift | mitigate | §7 cross-validation audit is the explicit gate; §7.2 偏差分析 declares expected vs actual 一致; Phase 51 lint script will cross-check 决策号 references |
| T-47-04 | Elevation of Privilege | Borrowable evaluation (§6) — recommending a Kimi-side idea without showing it violates a Phase 44 decision | mitigate | §6.1 evaluation table has "T6 兼容性" column + §6.6 不借鉴清单 cites Phase 44 §3 for each rejection |
| T-47-05 | Repudiation | Microsoft 三层 citation (§4) — claiming Microsoft "validates" v10.0 without precise source | mitigate | §4.1 verbatim citation with URL (learn.microsoft.com/en-us/agents/architecture/multi-agent-patterns) + FEATURES §7.4 B7.1 source chain |
| T-47-06 | Spoofing | User memory citation (§5.7) — quoting Kai's MEMORY.md out of context | mitigate | §5.7 quotes hermes-native-expert-agents.md + coding-agent-vs-mcp-shim.md verbatim with file paths + dates (2026-07-06) |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-47-SC (supply chain from package installs) is not applicable.
</threat_model>

<verification>
**Phase 47 DESIGN-04 success criteria (from ROADMAP §Phase 47):**

1. SC#1 — File `03-COMPARISON-VS-KIMI-MCP-SHIM.md` exists (verified by Task 1)
2. SC#2 — 7-dimension contrast table (协议 / dispatch / callback / state / 多 agent / 实现成本 / 稳定性) per-dimension T6 vs Kimi pros/cons + selection rationale (verified by Tasks 2+3 §3.1-§3.7)
3. SC#3 — Subagent form rejection argument complete citing FEATURES §11 B4.1 + Claude Agent SDK default context-isolation unsuitable for round table panelist (verified by Task 4 §5)
4. SC#4 — Microsoft three-layer protocol validation citing FEATURES §7.4 B7.1 proving v10.0 T6 aligns with industry consensus (verified by Task 3 §4)
5. SC#5 — Kimi-side borrowable parts explicitly listed + evaluation conditions (verified by Task 4 §6)

**Final deliverable target:** ~1400+ lines for the single markdown deliverable (mirrors Phase 44's 1181 lines + Phase 46's 1100+ lines at comparable rigor; Phase 47 is single-deliverable so the entire budget concentrates in the narrative).

**Methodology audit (self-check before commit):**
- Every dimension cites FEATURES §X / STACK §Y / ARCHITECTURE §Z source (not invented)
- Phase 44 decisions 1-7 cited by 决策号, never re-derived
- Phase 45 schema fields (memory_scope, scope, confidence, evidence_chain, persona_sha256, fitness_score, evolution_log, lineage) cited by name, never redefined
- Phase 46 protocol invariants (serial execution, STACK-form MCP naming, atomic turn lifecycle) cited by section, never redefined
- FEATURES §7.4 B7.1 + §11 B4.1 + §4.3 + §10 borrowable points each cited by section number
- Microsoft multi-agent-patterns URL appears in references (HIGH confidence source)
- Bilingual: EN structure (headers, schemas, citation tags) + 中文 prose (rationale, examples) per CLAUDE.md
- §7 audit confirms 7/7 决策 cross-validation consistent (no silent Phase 44 revision)
</verification>

<success_criteria>
**Phase 47 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/47-kimi-comparison/47-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) The 1 deliverable file exists:
   - `.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md` (≥ 1400 lines, §0-§8, 7-dimension contrast + Microsoft 三层 + subagent rejection + Kimi borrowable + 7 决策 audit)
3. ROADMAP.md Phase 47 entry updated (Plans: 1 plan, plan list with checkmark).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/47-kimi-comparison/47-01-SUMMARY.md` when done. SUMMARY should record:
- Final line count of the deliverable
- 7-dimension contrast table lock (协议 / dispatch / callback / state / 多 agent / 实现成本 / 稳定性)
- Microsoft 三层 validation lock (B7.1 + B2.2 + B7.4 三条 borrowable points 一致结论)
- Subagent rejection lock (FEATURES §11 B4.1 + §4.3 facts 1-3 + Phase 46 §2/§3 cross-ref)
- Kimi-side borrowable evaluation lock (B8.1 / B4.2 / B4.3 / B7.3 borrowed; B7.2 / shared graph / subagent form rejected)
- Phase 44 决策 1-7 cross-validation lock (7/7 ✅ 一致, no revision needed)
- SC#1-5 coverage confirmed
- Cross-validation: every section cites FEATURES §X / STACK §X / ARCHITECTURE §X / Phase 44 决策号 / Phase 45/46 schema source
</output>

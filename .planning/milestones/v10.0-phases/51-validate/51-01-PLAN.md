---
phase: 51-validate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/v10-consistency-check.py
  - .planning/milestones/v10.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [VALIDATE-01, VALIDATE-02]
granularity: standard

must_haves:
  truths:
    - "File scripts/v10-consistency-check.py exists, is executable as `python3 scripts/v10-consistency-check.py`, uses Python 3.11+ stdlib only (no external imports beyond argparse/pathlib/re/sys/json/yaml-from-stdlib-or-inline), and has `encoding=\"utf-8\"` on every open() call (Ruff PLW1514 compliant)"
    - "Lint script checks 4 dimensions on all 7 design docs (00-FIRST-PRINCIPLES / 01-AGENT-REGISTRY-SCHEMA / 02-ROUND-TABLE-PROTOCOL / 03-COMPARISON-VS-KIMI-MCP-SHIM / 04-MIGRATION-PATH / 05-POC-PLAN / 06-CROSS-REPO-IMPACT): (1) Terminology consistency — `agent` / `skill` / `round table` / `panel` / `turn` used in their locked sense (no term-swapping); (2) Schema reference consistency — agents-schema.yaml field names (persona_sha256, fitness_battery, memory_scope, memory.max_records, evidence_chain, evidence_operator_ids, scope, project_id, expires_at, verified_at, confidence, half_life_days, status, schema_version, retained_python_phases, persona, lineage, tools, related_agents, evolution_log) match design doc citations; (3) 决策号 1-7 reference consistency — each 决策 N described consistently across docs (per Phase 44 §2.1-§2.7 root definitions); (4) MCP tool naming consistency — STACK form (get_agent_persona / get_agent_opinion / get_agent_memory / submit_round_table_result / submit_artifact / query_memory / run_python_phase) dominates; ARCHITECTURE form (agent_get_persona / agent_recall / agent_conclude) flagged WARNING only when appearing outside a 03-COMPARISON or §naming-resolution context"
    - "Lint script emits structured output: per-check status (PASS / WARNING / ERROR), file:line evidence pointer for every WARNING/ERROR, and final exit code 0 iff zero ERRORs (WARNINGs OK and listed in audit report per SC#2)"
    - "Lint script runs on all 7 design docs with zero ERROR (WARNINGs acceptable, must be enumerated in audit report per SC#2)"
    - "File .planning/milestones/v10.0-MILESTONE-AUDIT.md exists, bilingual (EN structure + 中文 prose per CLAUDE.md), with YAML frontmatter (milestone: v10.0, status: passed|tech_debt|FAIL, baseline_commit, head_commit, scores:{requirements, phases, integration_flows, oq_resolved, pitfalls_mitigated, research_citations}, gaps, tech_debt, nyquist)"
    - "Audit report satisfies SC#3: 9/9 reqs each cross-checked with its phase SUMMARY (DESIGN-01 ↔ 44-01-SUMMARY, DESIGN-02 ↔ 45-01-SUMMARY, DESIGN-03 ↔ 46-01-SUMMARY, DESIGN-04 ↔ 47-01-SUMMARY, DESIGN-05 ↔ 49-01-SUMMARY, DESIGN-06 ↔ 50-01-SUMMARY, DESIGN-07 ↔ 48-01-SUMMARY, VALIDATE-01 ↔ this audit, VALIDATE-02 ↔ this audit's lint script section)"
    - "Audit report satisfies SC#4: 7 design docs cross-reference consistency check (terminology + schema + 决策号 + MCP tool naming — backed by lint script output) + 16 OQs (OQ-1..OQ-16 from SUMMARY.md) each marked RESOLVED / DEFERRED-to-v11.0 with resolution-doc pointer + 7 load-bearing pitfalls (P1/P2/P4/P5/P8/P10/P14 per ROADMAP SC#4) each with field-level mitigation pointer in Phase 45/46/50 + 4 research citation chains (STACK / FEATURES / ARCHITECTURE / PITFALLS) each complete (every doc that cites §X.Y has a real §X.Y in the source)"
    - "Audit report satisfies SC#5: explicit milestone-level verdict (PASS / tech_debt / FAIL) with evidence pointers — each verdict line cites (file, section/line) tuples so a third party can reproduce the audit"
  artifacts:
    - path: "scripts/v10-consistency-check.py"
      provides: "v10.0 VALIDATE-02 deliverable — cross-doc consistency lint script. Python 3.11+ stdlib-only (per CLAUDE.md Python runtime). 4 dimensions: (1) Terminology (agent/skill/round table/panel/turn locked-sense enforcement via allow/deny patterns), (2) Schema reference (agents-schema.yaml + memory-record-schema.yaml + round-table-state-schema.yaml field names matched against design doc citations via regex on `persona_sha256|fitness_battery|memory_scope|memory\\.max_records|evidence_chain|evidence_operator_ids|scope|project_id|expires_at|verified_at|confidence|half_life_days|status|schema_version|retained_python_phases` etc.), (3) 决策号 1-7 reference consistency (cross-doc consistency matrix — each 决策 N's root definition lives in 00-FIRST-PRINCIPLES.md §2.N, downstream docs must not contradict), (4) MCP tool naming (STACK form allow-list: get_agent_persona/get_agent_opinion/get_agent_memory/submit_round_table_result/submit_artifact/query_memory/run_python_phase; ARCHITECTURE form agent_get_persona/agent_recall/agent_conclude triggers WARNING unless inside comparison/citation context). CLI: `python3 scripts/v10-consistency-check.py [--root .planning/research/v10-orchestrator-design] [--format text|json] [--strict]`. Exit code 0 iff zero ERRORs; WARNINGs printed to stdout but do not fail. Every open() passes encoding='utf-8' (Ruff PLW1514). Type hints on all public functions per CLAUDE.md."
      min_lines: 400
      contains: ["encoding=\"utf-8\"", "argparse", "Path", "re", "json", "sys", "def main", "def check_terminology", "def check_schema_references", "def check_decision_consistency", "def check_mcp_tool_naming", "agents-schema.yaml", "memory-record-schema.yaml", "round-table-state-schema.yaml", "persona_sha256", "evidence_chain", "memory.max_records", "retained_python_phases", "get_agent_persona", "submit_round_table_result", "run_python_phase", "agent_get_persona", "decision", "决策", "PASS", "WARNING", "ERROR", "exit_code", "__future__", "annotations"]
    - path: ".planning/milestones/v10.0-MILESTONE-AUDIT.md"
      provides: "v10.0 VALIDATE-01 deliverable — milestone audit report. Bilingual (EN structure + 中文 prose per CLAUDE.md). Mirrors v9.0-MILESTONE-AUDIT.md structure: (§0) frontmatter scorecard (requirements 9/9, phases 8/8, integration_flows, oq_resolved 16/16, pitfalls_mitigated 7/7, research_citations 4/4), (§1) phase verification summary (Phase 44-51 each with plan count + SUMMARY ref + commit range), (§2) requirements traceability 9-row table (each REQ-ID → phase → plan → status → verification source citing phase SUMMARY), (§3) design doc cross-reference consistency (7 docs × 4 dimensions — backed by lint script output, WARNINGs enumerated), (§4) 16 OQ resolution audit (OQ-1..OQ-16 each with status RESOLVED / DEFERRED-to-v11.0 + resolution-doc pointer), (§5) 7 load-bearing pitfall field-level mitigation audit (P1/P2/P4/P5/P8/P10/P14 each with schema field + design-doc section + PoC acceptance criterion pointer), (§6) 4 research citation chain completeness (STACK / FEATURES / ARCHITECTURE / PITFALLS — each doc that cites §X.Y has a real §X.Y), (§7) integration flows (Phase 44 → 45 → 46 → 49 → 50 → 51 critical path + 47/48 parallel-eligible wave), (§8) final verdict (PASS / tech_debt / FAIL) with evidence pointers + FOUND-08 preservation note (N/A for design-only milestone — explicitly declared), (§9) References (7 design docs + 5 research sources + 8 phase SUMMARYs + v9.0 audit precedent + CLAUDE.md)."
      min_lines: 600
      contains: ["VALIDATE-01", "VALIDATE-02", "DESIGN-01", "DESIGN-02", "DESIGN-03", "DESIGN-04", "DESIGN-05", "DESIGN-06", "DESIGN-07", "OQ-1", "OQ-7", "OQ-16", "persona_sha256", "evidence_chain", "memory.max_records", "STACK", "FEATURES", "ARCHITECTURE", "PITFALLS", "SUMMARY", "Phase 44", "Phase 45", "Phase 46", "Phase 47", "Phase 48", "Phase 49", "Phase 50", "决策 1", "决策 6", "决策 7", "P1", "P2", "P4", "P5", "P8", "P10", "P14", "agent", "skill", "round table", "panel", "turn", "get_agent_persona", "submit_round_table_result", "run_python_phase", "PASS", "verdict", "44-01-SUMMARY", "45-01-SUMMARY", "46-01-SUMMARY", "47-01-SUMMARY", "48-01-SUMMARY", "49-01-SUMMARY", "50-01-SUMMARY", "v9.0-MILESTONE-AUDIT", "scripts/v10-consistency-check.py", "design-only", "FOUNDATION", "zero code changes"]
  key_links:
    - from: "scripts/v10-consistency-check.py"
      to: ".planning/research/v10-orchestrator-design/{00,01,02,03,04,05,06}*.md (7 design docs — lint input)"
      via: "Path.glob + regex scan, file:line evidence reporting"
      pattern: "FIRST-PRINCIPLES|AGENT-REGISTRY-SCHEMA|ROUND-TABLE-PROTOCOL|COMPARISON-VS-KIMI|MIGRATION-PATH|POC-PLAN|CROSS-REPO-IMPACT"
    - from: "scripts/v10-consistency-check.py"
      to: ".planning/research/v10-orchestrator-design/{agents-schema,memory-record-schema,round-table-state-schema}.yaml (3 schema sources — field-name truth)"
      via: "YAML line-based field extraction (stdlib only; no PyYAML dependency required — parse `^  field_name:` lines)"
      pattern: "agents-schema\\.yaml|memory-record-schema\\.yaml|round-table-state-schema\\.yaml"
    - from: "scripts/v10-consistency-check.py"
      to: "00-FIRST-PRINCIPLES.md §2.1-§2.7 (决策 1-7 root definitions — consistency truth)"
      via: "regex extraction of 决策 N headers + cross-doc reference scan"
      pattern: "决策 [1-7]"
    - from: ".planning/milestones/v10.0-MILESTONE-AUDIT.md"
      to: "scripts/v10-consistency-check.py (lint output — backs §3 cross-reference consistency)"
      via: "audit report cites lint output summary (PASS/ERROR/WARNING counts per dimension)"
      pattern: "v10-consistency-check\\.py|lint"
    - from: ".planning/milestones/v10.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/{44,45,46,47,48,49,50}-*/{NN}-01-SUMMARY.md (8 phase SUMMARYs — backs §2 requirements traceability)"
      via: "audit report §2 table cites each REQ-ID's phase SUMMARY as verification source"
      pattern: "44-01-SUMMARY|45-01-SUMMARY|46-01-SUMMARY|47-01-SUMMARY|48-01-SUMMARY|49-01-SUMMARY|50-01-SUMMARY"
    - from: ".planning/milestones/v10.0-MILESTONE-AUDIT.md"
      to: ".planning/research/v10-orchestrator-design/SUMMARY.md §Open Questions Consolidated (OQ-1..OQ-16 — backs §4 OQ resolution audit)"
      via: "audit report §4 16-row table cites SUMMARY.md OQ source + resolution-doc pointer"
      pattern: "OQ-[1-9]|OQ-1[0-6]"
    - from: ".planning/milestones/v10.0-MILESTONE-AUDIT.md"
      to: ".planning/milestones/v9.0-MILESTONE-AUDIT.md (precedent — structural template)"
      via: "audit report §0 frontmatter + §1-§9 structure mirrors v9.0 close-out rigor"
      pattern: "v9\\.0-MILESTONE-AUDIT|milestone audit"
---

<objective>
Create the v10.0 VALIDATE phase deliverables — the **milestone close-out** pair:

1. `scripts/v10-consistency-check.py` (~400-500 lines, Python 3.11+ stdlib-only, type-hinted, Ruff PLW1514 compliant)
2. `.planning/milestones/v10.0-MILESTONE-AUDIT.md` (~600-900 lines, bilingual EN structure + 中文 prose per CLAUDE.md)

Purpose: Close out the v10.0 design-only milestone by (a) automating cross-doc consistency checks across all 7 design docs (VALIDATE-02) and (b) producing a milestone-level audit report that proves 9/9 reqs satisfied + 16 OQs resolved/deferred + 7 load-bearing pitfalls field-level mitigated + 4 research citation chains complete (VALIDATE-01). The verdict is PASS / tech_debt / FAIL with evidence pointers — third-party-reproducible.

The next consumers are: (1) the operator (Kai) reviewing whether v10.0 is "done" and ready to tag + move to v11.0 PoC milestone, (2) v11.0 PoC implementer (the audit report is the high-level map — they consult §3 cross-doc consistency + §5 pitfall mitigation pointers before reading individual design docs), (3) future milestone authors who will mirror this close-out pattern (v11.0 PoC audit, etc.), (4) `gsd-audit-milestone` skill if invoked.

Output: 2 files (1 Python script under `scripts/`, 1 markdown audit under `.planning/milestones/`), mirroring v9.0 Phase 43 close-out rigor at comparable scope (v9.0 audit = ~330 lines; v10.0 target ~600-900 lines because v10.0 has more design-doc cross-references + 16 OQs + 7 pitfalls to audit). This phase is single-plan, single-wave, 5 atomic tasks. Each task ends with a `docs(51-01):` prefixed commit.

This is a VALIDATION-ONLY deliverable. Zero design-doc edits, zero SKILL.md edits, zero plugin changes. The lint script is the single new Python file; the audit report is the single new markdown. Both deliverables CONSUME existing artifacts (7 design docs + 3 schema YAMLs + 5 research sources + 8 phase SUMMARYs + v9.0 audit precedent) — they do NOT introduce new design decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
@.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md
@.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md
@.planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md
@.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md
@.planning/research/v10-orchestrator-design/05-POC-PLAN.md
@.planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md
@.planning/research/v10-orchestrator-design/agents-schema.yaml
@.planning/research/v10-orchestrator-design/memory-record-schema.yaml
@.planning/research/v10-orchestrator-design/round-table-state-schema.yaml
@.planning/research/v10-orchestrator-design/STACK.md
@.planning/research/v10-orchestrator-design/FEATURES.md
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md
@.planning/milestones/v9.0-MILESTONE-AUDIT.md
@.planning/phases/44-first-principles/44-01-SUMMARY.md
@.planning/phases/45-agent-schema/45-01-SUMMARY.md
@.planning/phases/46-round-table-protocol/46-01-SUMMARY.md
@.planning/phases/47-kimi-comparison/47-01-SUMMARY.md
@.planning/phases/48-cross-repo-impact/48-01-SUMMARY.md
@.planning/phases/49-migration-path/49-01-SUMMARY.md
@.planning/phases/50-poc-plan/50-01-SUMMARY.md

<interfaces>
<!-- Phase 51 CONTEXT.md locked decisions (from /gsd:discuss-phase autonomous smart-discuss — honor, do NOT re-derive): -->
<!-- SC#1: Lint script scope — 4 dimensions (terminology / schema refs / 决策号 1-7 / MCP tool naming), path scripts/v10-consistency-check.py -->
<!-- SC#2: Lint script runs on all 7 design docs, zero ERRORs (WARNINGs OK, listed in audit) -->
<!-- SC#3: Audit report path .planning/milestones/v10.0-MILESTONE-AUDIT.md, 9/9 reqs each cross-checked with phase SUMMARY -->
<!-- SC#4: Audit report checks — 7 design docs cross-ref + 16 OQs + 7 load-bearing pitfalls (P1/P2/P4/P5/P8/P10/P14) field-level mitigation in 45/46/50 + 4 research citation chains -->
<!-- SC#5: Audit verdict — PASS / tech_debt / FAIL with evidence pointers -->

<!-- Python conventions (CLAUDE.md — MUST follow): -->
<!-- snake_case for modules / functions / locals; PascalCase for classes; UPPER_SNAKE for module constants -->
<!-- Type hints required on public functions; `from __future__ import annotations` at top -->
<!-- `encoding="utf-8"` on EVERY open() call (Ruff PLW1514 — sole load-bearing linter rule) -->
<!-- Catch specific exceptions with `as exc:` binding, never bare `except:` -->
<!-- Lazy %-formatting in logger calls (no f-strings in log messages) — though for a lint script, stderr writes via sys.stderr are acceptable -->
<!-- stdlib only — no PyYAML, no external deps (PyYAML is in pyproject.toml dev deps but the lint script MUST be self-contained per CONTEXT.md discretion note) -->

<!-- VALIDATE-02 lint dimensions (locked in REQUIREMENTS.md + 51-CONTEXT.md): -->
<!-- Dimension 1: Terminology — agent (registered YAML entity with persona + tools + memory_scope), skill (SKILL.md markdown consumed by Hermes skills loader), round table (multi-agent deliberation session with state file), panel (the agent subset participating in a round table), turn (one panelist's contribution within a round table). Locked senses per Phase 44 决策 5 + Phase 46 protocol. -->
<!-- Dimension 2: Schema references — agents-schema.yaml fields (persona, persona_sha256, fitness_battery, fitness_score, tools, memory_scope, memory.max_records, lineage, related_agents, evolution_log, expert_id, metrics, default_invocation, platforms, round_table_eligible, refs, tags, prerequisites, version, name, description) + memory-record-schema.yaml fields (evidence_chain, evidence_operator_ids, scope, project_id, expires_at, verified_at, supersedes_memory_id, confidence, half_life_days, status, confidentiality, schema_version) + round-table-state-schema.yaml fields (round_id, turn_order, retained_python_phases, project_id required). Cross-doc mentions must match schema field names exactly. -->
<!-- Dimension 3: 决策号 1-7 — root definitions live in 00-FIRST-PRINCIPLES.md §2.1 (决策 1 T6 协议) / §2.2 (决策 2 B3a Python runner) / §2.3 (决策 3 D2 storyboard-first-class) / §2.4 (决策 4 G2 通用编排框架) / §2.5 (决策 5 α agent form) / §2.6 (决策 6 per-agent memory + curator) / §2.7 (决策 7 分层 CC 角色). Downstream docs citing 决策 N must not contradict root definition. -->
<!-- Dimension 4: MCP tool naming — STACK §3.2 form (no prefix, 7 tools): get_agent_persona, get_agent_opinion, get_agent_memory, submit_round_table_result, submit_artifact, query_memory, run_python_phase. ARCHITECTURE §4.2 form (agent_ prefix): agent_get_persona, agent_recall, agent_conclude. OQ-9 (resolved in Phase 46) locks STACK form. ARCHITECTURE form appearing outside a comparison/citation context is a WARNING (consistency violation), not ERROR, because 03-COMPARISON-VS-KIMI-MCP-SHIM.md legitimately cites both forms in its comparison table. -->

<!-- Lint output protocol (SC#2): -->
<!-- Exit code: 0 iff zero ERRORs; 1 if any ERROR. WARNINGs do not fail exit code. -->
<!-- Output: structured text by default (one line per finding: `dimension | severity | file:line | message`); --format json for machine consumption; --strict to treat WARNINGs as ERRORs. -->

<!-- VALIDATE-01 audit checks (locked in REQUIREMENTS.md + 51-CONTEXT.md): -->
<!-- Check A: 9/9 reqs traceability — DESIGN-01..07 + VALIDATE-01..02 each mapped to phase + plan + SUMMARY + verification source. -->
<!-- Check B: 7 design docs cross-reference consistency (backed by lint script — 4 dimensions). -->
<!-- Check C: 16 OQs each marked RESOLVED (with resolution-doc §pointer) or DEFERRED-to-v11.0 (with rationale). -->
<!-- Check D: 7 load-bearing pitfalls (P1/P2/P4/P5/P8/P10/P14 per ROADMAP SC#4) each with field-level mitigation: P1 → persona_sha256 + tiered memory (Phase 45 agents-schema.yaml); P2 → expires_at + verified_at + supersedes_memory_id + confidence + half_life_days (Phase 45 memory-record-schema.yaml); P4 → scope (global|project|session) + project_id required (Phase 45 + Phase 46 protocol); P5 → evidence_chain + evidence_operator_ids + dry-run-first + bias canary (Phase 45 + Phase 50 §4.3 + §4.6); P8 → fitness_battery + fitness_trend.jsonl (Phase 45 + Phase 50 §4.1); P10 → confidentiality + scope (Phase 45); P14 → schema_version + dry-run migration (Phase 45 + Phase 49 §4.5 + Phase 50 §4.7). -->
<!-- Check E: 4 research citation chains — for every (design-doc, STACK §X.Y / FEATURES §X.Y / ARCHITECTURE §X.Y / PITFALLS §PX) citation tuple, verify the cited section exists in the source. -->

<!-- v9.0 audit structural template (precedent — mirror, do NOT copy verbatim): -->
<!-- §0: YAML frontmatter scorecard (milestone / status / baseline_commit / scores / gaps / tech_debt / nyquist) -->
<!-- §1: Phase Verification Summary table (Phase × Name × Plans × Reqs × Status × Commit Range) -->
<!-- §2: Requirements Traceability table (REQ-ID × Phase × Plan × Status × Verification Source) -->
<!-- §3: Integration / cross-reference consistency (lint script output for v10.0) -->
<!-- §4: OQ resolution audit (NEW for v10.0 — v9.0 didn't have OQs) -->
<!-- §5: Pitfall mitigation audit (NEW for v10.0 — v9.0 didn't have load-bearing pitfalls) -->
<!-- §6: Research citation chain completeness (NEW for v10.0) -->
<!-- §7: Integration flows (critical path + parallel waves) -->
<!-- §8: Final verdict + FOUND-08 preservation (N/A for design-only — declare explicitly) -->
<!-- §9: References -->

<!-- Memory precedent (CLAUDE.md MEMORY.md): -->
<!-- v9.0 audit at .planning/milestones/v9.0-MILESTONE-AUDIT.md — structural template -->
<!-- v5.0 audit at .planning/milestones/v5.0-MILESTONE-AUDIT.md — earlier precedent -->
<!-- Phase 51 is "strictly LAST" per ROADMAP — analog to v9.0 Phase 43 / v5.0 Phase 27 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold scripts/v10-consistency-check.py — module header + 4 dimension function signatures + CLI argparse + main() skeleton</name>
  <files>scripts/v10-consistency-check.py</files>

  <read_first>
    - ./CLAUDE.md §Conventions / §Code Style / §Error Handling (Python conventions: snake_case, encoding=utf-8 on every open(), type hints, no bare except, lazy %-formatting in log calls, `from __future__ import annotations` at top)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Open Questions Consolidated (OQ-9 MCP tool naming — STACK form locked)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1-§2.7 (决策 1-7 root definitions — the consistency truth)
    - .planning/research/v10-orchestrator-design/agents-schema.yaml (full file — extract field names for Dimension 2)
    - .planning/research/v10-orchestrator-design/memory-record-schema.yaml (full file — extract field names for Dimension 2)
    - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml (full file — extract field names for Dimension 2)
  </read_first>

  <behavior>
  - File starts with `#!/usr/bin/env python3` shebang + `"""Module docstring."""` + `from __future__ import annotations`
  - Imports: argparse, json, re, sys, pathlib.Path (stdlib only — NO PyYAML, NO external deps)
  - Module constants (UPPER_SNAKE_CASE): `DESIGN_DOC_GLOB`, `SCHEMA_FILES` (dict mapping schema kind → path), `STACK_FORM_TOOLS` (frozenset of 7), `ARCHITECTURE_FORM_TOOLS` (frozenset of 3), `TERMINOLOGY_LOCKED_SENSES` (dict), `KNOWN_DECISION_NUMBERS` (frozenset {1..7}), `EXIT_OK`, `EXIT_ERROR`
  - Public type hints on ALL functions (per CLAUDE.md) — use `list[dict[str, object]]`, `tuple[int, list[str]]`, etc.
  - Dataclass `Finding` with fields: `dimension: str`, `severity: str` (literal "PASS"|"WARNING"|"ERROR"), `file: Path`, `line: int`, `message: str`. Use `@dataclass(slots=True)` if Python 3.11+ idiom preferred.
  - Function signatures (skeletons returning `list[Finding]` — implementation in Tasks 2-4):
    - `def check_terminology(design_docs: list[Path]) -> list[Finding]: ...`  # Dimension 1
    - `def check_schema_references(design_docs: list[Path], schema_dir: Path) -> list[Finding]: ...`  # Dimension 2
    - `def check_decision_consistency(design_docs: list[Path]) -> list[Finding]: ...`  # Dimension 3
    - `def check_mcp_tool_naming(design_docs: list[Path]) -> list[Finding]: ...`  # Dimension 4
  - Helper: `def _load_yaml_field_names(path: Path) -> set[str]: ...` — stdlib line-based parser extracting top-level + nested field names from `^  field_name:` lines (no PyYAML dependency). Returns a set of canonical field names.
  - Helper: `def _iter_markdown_lines(path: Path) -> Iterator[tuple[int, str]]:` — yields (1-indexed line_number, line_text), uses `path.open(encoding="utf-8")`
  - CLI: argparse with `--root` (default `.planning/research/v10-orchestrator-design`), `--format` (choices: text/json, default text), `--strict` (flag — treat WARNING as ERROR), positional optional `[paths...]` to override default glob
  - `def main(argv: list[str] | None = None) -> int:` — orchestrates the 4 checks, collects Findings, formats output, returns EXIT_OK / EXIT_ERROR
  - `if __name__ == "__main__": sys.exit(main())` guard
  - Every `open()` call passes `encoding="utf-8"` (Ruff PLW1514 — CLAUDE.md sole load-bearing linter rule)
  - Skeleton functions may `pass` or return `[]` — Task 2-4 fill them in. Task 1 only delivers scaffold + CLI + main() that runs (returns 0 with no findings).
  - Total length ≥ 150 lines (scaffold + signatures + CLI + main skeleton)
  </behavior>

  <action>
  Create file `scripts/v10-consistency-check.py`. Write the module header + constants + dataclass + 4 function signatures (with docstrings but body `pass` or `return []`) + helpers `_load_yaml_field_names` and `_iter_markdown_lines` (implemented — they are pure stdlib) + argparse CLI + `main()` that calls all 4 checks (currently returning empty lists) and prints a summary line.

  Constants to populate now (Task 1 — Task 2-4 will reference them):
    - `STACK_FORM_TOOLS = frozenset({"get_agent_persona", "get_agent_opinion", "get_agent_memory", "submit_round_table_result", "submit_artifact", "query_memory", "run_python_phase"})`
    - `ARCHITECTURE_FORM_TOOLS = frozenset({"agent_get_persona", "agent_recall", "agent_conclude"})`
    - `KNOWN_DECISION_NUMBERS = frozenset(range(1, 8))`  # 决策 1-7
    - `SCHEMA_FILES = {"agents": Path("agents-schema.yaml"), "memory-record": Path("memory-record-schema.yaml"), "round-table-state": Path("round-table-state-schema.yaml")}`
    - `TERMINOLOGY_LOCKED_SENSES = {"agent": "...", "skill": "...", "round table": "...", "panel": "...", "turn": "..."}`  # brief sense strings

  The CLI `main()` should:
    1. Parse args
    2. Resolve `--root` to absolute Path, glob `0[0-6]-*.md` for the 7 design docs
    3. Call each check function, collect Findings
    4. Format output: per-finding line `DIMENSION | SEVERITY | file:line | message` (text) or JSON array
    5. Print summary: `TOTAL: N findings (PASS=A WARNING=B ERROR=C)` to stderr
    6. Return `EXIT_ERROR` if any ERROR (or any WARNING under --strict), else `EXIT_OK`

  Commit with prefix `docs(51-01):` per the plan constraint — message e.g. `docs(51-01): scaffold v10-consistency-check.py — module header + 4 dimension signatures + CLI`.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/scripts/v10-consistency-check.py && python3 -c "import ast; ast.parse(open('/data/workspace/hermes-agent/scripts/v10-consistency-check.py', encoding='utf-8').read())" && python3 /data/workspace/hermes-agent/scripts/v10-consistency-check.py --help && grep -q "from __future__ import annotations" /data/workspace/hermes-agent/scripts/v10-consistency-check.py && ! grep -nE "open\([^)]*\)" /data/workspace/hermes-agent/scripts/v10-consistency-check.py | grep -v "encoding=" | grep -v "open\(\)" && for sym in "def check_terminology" "def check_schema_references" "def check_decision_consistency" "def check_mcp_tool_naming" "def main" "STACK_FORM_TOOLS" "ARCHITECTURE_FORM_TOOLS" "KNOWN_DECISION_NUMBERS" "argparse" "encoding=\"utf-8\"" "@dataclass" "Finding"; do grep -q "$sym" /data/workspace/hermes-agent/scripts/v10-consistency-check.py || { echo "MISSING: $sym"; exit 1; }; done && wc -l /data/workspace/hermes-agent/scripts/v10-consistency-check.py | awk '$1 >= 150 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - File scripts/v10-consistency-check.py exists, parses as valid Python 3.11+, runs `--help` without error
  - `from __future__ import annotations` at top
  - All 4 check function signatures present with type hints
  - `main()` runs end-to-end (returns 0 with no findings since bodies are stubs)
  - Module constants populated (STACK_FORM_TOOLS, ARCHITECTURE_FORM_TOOLS, KNOWN_DECISION_NUMBERS, SCHEMA_FILES, TERMINOLOGY_LOCKED_SENSES)
  - Every `open()` call passes `encoding="utf-8"` (grep gate)
  - ≥ 150 lines
  - Committed with `docs(51-01):` prefix
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Dimension 1 (terminology) + Dimension 4 (MCP tool naming) — both regex-driven, file:line evidence</name>
  <files>scripts/v10-consistency-check.py</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.5 (决策 5 — α agent form: YAML + persona + tools + refs + memory_scope + lineage — locks the sense of "agent")
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §1-§2 (turn lifecycle + panel + round table definitions — locks those term senses)
    - .planning/research/v10-orchestrator-design/03-COMPARISON-VS-KIMI-MCP-SHIM.md (ARCHITECTURE-form tool names legitimately appear here — exception context for Dimension 4)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 (7 MCP tool canonical names — Dimension 4 truth)
    - Re-read scripts/v10-consistency-check.py Task 1 output (the file you're modifying)
  </read_first>

  <behavior>
  - `check_terminology(design_docs)` returns `list[Finding]`. Implementation strategy:
    - For each design doc, scan each line. Detect terminology confusions:
      - "skill" used where "agent" is meant (e.g. "skill persona" in a context discussing per-agent memory) → WARNING (skill has no persona field per agents-schema.yaml; agent does)
      - "agent" used where "skill" is meant (e.g. "agent markdown file" referring to a SKILL.md) → WARNING
      - "panel" used where "round table" is meant (panel = subset of agents; round table = the session itself per Phase 46) → WARNING only if context clearly conflates them
      - "turn" used where "round" is meant (turn = single panelist contribution; round = one full pass per Phase 46) → WARNING only if conflated
    - Use simple line-context heuristics — this is a lint, not an LLM judge. E.g. "skill" within 5 tokens of "persona" / "memory_scope" / "fitness_battery" / "agents-schema.yaml" is suspect. "agent" within 5 tokens of "SKILL.md" / "skills/" / "frontmatter" is suspect.
    - The heuristics MUST be conservative — false positives are worse than false negatives (operator will dismiss the audit if lint cries wolf). When uncertain, return PASS (no Finding) — Task 2 lint surface stays small + defensible.
    - Each WARNING Finding cites file:line + a `message` like `"terminology: 'skill' used near 'persona' — did you mean 'agent'? (skill has no persona field; agent does per agents-schema.yaml)"`.
  - `check_mcp_tool_naming(design_docs)` returns `list[Finding]`. Implementation strategy:
    - For each design doc, regex-scan for tool names. Build a counter per file: `{tool_name: [(line, col), ...]}`.
    - STACK-form tool names (7) → PASS (no Finding), unless the doc is in the comparison/citation exception set.
    - ARCHITECTURE-form tool names (agent_get_persona / agent_recall / agent_conclude) → WARNING by default, UNLESS the file is `03-COMPARISON-VS-KIMI-MCP-SHIM.md` (legitimate comparison context) OR the line is clearly a citation (e.g. contains "ARCHITECTURE §" / "ARCHITECTURE-form" / "vs" / "originally called"). In exception context → PASS (no Finding).
    - Unknown tool names matching `(get|submit|query|run)_[a-z_]+` or `agent_[a-z_]+` that aren't in either allow-list → ERROR (likely typo / drift). E.g. `get_agent_fitness` would be an ERROR (no such tool).
    - Cross-doc consistency: for each STACK-form tool, verify it appears in ≥2 of the 7 docs (the tool is referenced consistently, not invented in one place). Doc count < 2 → INFO (PASS, just a note in summary).
    - Each Finding cites file:line + message naming the tool + which form it's in.
  - Both functions return `list[Finding]` (no I/O except reading design docs via `_iter_markdown_lines`).
  - All `open()` calls pass `encoding="utf-8"`.
  - Add module-level docstring expansion documenting the lint philosophy (conservative, false-positive-averse, file:line evidence, SC#2 zero-ERROR target).
  </behavior>

  <action>
  Implement `check_terminology` and `check_mcp_tool_naming` in `scripts/v10-consistency-check.py`. Replace the stub bodies from Task 1.

  For terminology: define helper `_scan_terminology_confusions(line: str, line_no: int, path: Path) -> list[Finding]` that runs the conservative heuristics. Use regex word boundaries (`\b`) for the 5 terms. For each suspect co-occurrence, produce a Finding with severity WARNING. Cap findings per file at 50 (avoid runaway on a doc that uses "agent" 200 times — the point is signal, not volume).

  For MCP tool naming: define helper `_classify_tool_mention(tool: str, line: str, file_name: str) -> str` returning one of "STACK_PASS" / "ARCH_WARNING" / "ARCH_EXCEPTION" / "UNKNOWN_ERROR". Use `file_name.endswith("03-COMPARISON-VS-KIMI-MCP-SHIM.md")` or `"ARCHITECTURE" in line and ("§" in line or "form" in line or "vs" in line)` to detect exception context. Emit Finding only when classification is "ARCH_WARNING" (severity WARNING) or "UNKNOWN_ERROR" (severity ERROR).

  Update `main()` if needed (it should already call these — Task 1 wired them). Run the script against the 7 design docs to confirm it produces a sensible Finding list (expect mostly WARNINGs for ARCH-form mentions in non-comparison docs; expect zero ERRORs).

  Commit with `docs(51-01):` prefix — message e.g. `docs(51-01): implement Dimensions 1 + 4 — terminology heuristics + MCP tool naming classifier`.
  </action>

  <verify>
    <automated>python3 /data/workspace/hermes-agent/scripts/v10-consistency-check.py --root /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design --format text 2>&1 | tee /tmp/v10-lint-task2.out && ! grep -E "Traceback|Error:" /tmp/v10-lint-task2.out && grep -qE "Dimension 1|terminology|Dimension 4|MCP|TOTAL" /tmp/v10-lint-task2.out && python3 -c "
import subprocess, sys
r = subprocess.run(['python3', '/data/workspace/hermes-agent/scripts/v10-consistency-check.py', '--root', '/data/workspace/hermes-agent/.planning/research/v10-orchestrator-design'], capture_output=True, text=True)
# Script should run; WARNINGs OK, ERRORs would fail exit (but Task 2 doesn't enforce zero-ERROR yet — that's Task 5)
print('exit:', r.returncode)
print('stderr tail:', r.stderr.splitlines()[-1] if r.stderr else '(none)')
assert 'Traceback' not in r.stdout + r.stderr, 'Python traceback detected'
" && grep -qE "def check_terminology|def check_mcp_tool_naming|def _scan_terminology_confusions|def _classify_tool_mention" /data/workspace/hermes-agent/scripts/v10-consistency-check.py && for tool in get_agent_persona get_agent_opinion get_agent_memory submit_round_table_result submit_artifact query_memory run_python_phase; do grep -q "$tool" /data/workspace/hermes-agent/scripts/v10-consistency-check.py || { echo "MISSING tool ref: $tool"; exit 1; }; done</automated>
  </verify>

  <done>
  - check_terminology implemented, conservative, returns findings with file:line evidence
  - check_mcp_tool_naming implemented, classifies STACK/ARCHITECTURE/UNKNOWN, exception-aware for 03-COMPARISON doc
  - Script runs against 7 design docs without Python errors
  - Both functions reference the 7 canonical STACK-form tool names
  - All `open()` calls pass `encoding="utf-8"`
  - Committed with `docs(51-01):` prefix
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Dimension 2 (schema references) + Dimension 3 (决策号 1-7 consistency) — schema-truth-driven + cross-doc matrix</name>
  <files>scripts/v10-consistency-check.py</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/agents-schema.yaml (full — extract every top-level + nested field name as the canonical set for Dimension 2)
    - .planning/research/v10-orchestrator-design/memory-record-schema.yaml (full — extract field names)
    - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml (full — extract field names, especially `retained_python_phases`)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1-§2.7 (决策 1-7 root definitions — Dimension 3 truth)
    - Re-read scripts/v10-consistency-check.py Task 2 output
  </read_first>

  <behavior>
  - `check_schema_references(design_docs, schema_dir)` returns `list[Finding]`:
    - Load canonical field-name sets from the 3 schema YAMLs via `_load_yaml_field_names(Path)`. 3 sets: `agents_fields`, `memory_record_fields`, `round_table_state_fields`.
    - For each design doc, regex-scan for field-name mentions in backticks or `field:` style or snake_case identifiers. Pattern: `r"`([a-z_][a-z0-9_]*(\.[a-z_][a-z0-9_]*)?)`"` and `r"\b([a-z_][a-z0-9_]*(?:\.[a-z_][a-z0-9_]*)+):"` for dotted names like `memory.max_records`.
    - For each mention, classify:
      - Exact match in canonical set → PASS
      - Close match (Levenshtein ≤ 2, e.g. `persona_sha` vs `persona_sha256`) → WARNING with message suggesting the canonical name
      - Dotted field where prefix is a known schema kind + suffix isn't in nested fields → WARNING (likely stale ref)
      - Completely unknown snake_case identifier in field-like context (backticks, after `:`, in a "fields" list) → ERROR (likely invented field)
    - Specifically MUST catch: `persona_sha256`, `fitness_battery`, `fitness_score`, `memory.max_records`, `evidence_chain`, `evidence_operator_ids`, `scope` (global|project|session enum), `project_id`, `expires_at`, `verified_at`, `confidence`, `half_life_days`, `status`, `schema_version`, `retained_python_phases`, `round_id`, `turn_order`, `lineage`, `related_agents`, `evolution_log`, `default_invocation`, `round_table_eligible`. These are the load-bearing schema fields cited across multiple design docs.
    - Conservative: only ERROR on clearly-field-like context (backtick-wrapped OR after `:` in a YAML-like snippet OR in a "fields:" enumeration list). Free-text mentions of common words like "scope" or "status" should NOT trigger ERROR (they could be English words). Use context check.
  - `check_decision_consistency(design_docs)` returns `list[Finding]`:
    - Build a 决策 N → root-definition-text map by scanning `00-FIRST-PRINCIPLES.md` for `决策 N` headers (e.g. `## §2.N 决策 N — ...`). Extract the §2.N body as root definition (lines until next §2 header).
    - For each downstream design doc (01-06), for each `决策 N` mention:
      - If the surrounding context (±5 lines) appears to *define* 决策 N (rather than cite it) → ERROR (downstream docs must NOT re-define; root lives only in Phase 44 / 00-FIRST-PRINCIPLES.md). Detect "define" by keywords like `决策 N 是` / `决策 N means` / `we decide` near the mention.
      - If the surrounding context *contradicts* the root definition (simple keyword check: 决策 1 root contains "T6" + "MCP" + "stdio"; a doc claiming 决策 1 is "HTTP transport" would be contradiction) → ERROR.
      - Otherwise → PASS (mere citation is fine).
    - Cross-doc coverage: each 决策 N (1-7) should appear in ≥3 of the 7 design docs (root + ≥2 downstream). Coverage < 3 → INFO (NOTE in summary, not WARNING — coverage is informational).
    - For each 决策 N, extract a one-line summary from root definition. Verify downstream docs' `决策 N` mentions are consistent with that summary (simple keyword overlap check — if root says "per-agent memory + curator" and a doc's 决策 6 context mentions "shared global memory", that's a contradiction → ERROR).
  - Both functions return `list[Finding]` with file:line evidence.
  - All `open()` calls pass `encoding="utf-8"`.
  </behavior>

  <action>
  Implement `check_schema_references` and `check_decision_consistency` in `scripts/v10-consistency-check.py`. Replace the Task 1 stubs.

  For schema refs: implement `_load_yaml_field_names(path: Path) -> set[str]` if not already implemented in Task 1 — line-based stdlib parser. Lines matching `^  ([a-z_][a-z0-9_]*):` are top-level fields; lines matching `^    ([a-z_][a-z0-9_]*):` are nested. For nested dict fields like `memory: { max_records: ... }`, also synthesize `memory.max_records` dotted names. Return the set.

  Define `_levenshtein(a: str, b: str) -> int` (stdlib impl, ≤ ~15 lines) for the close-match check.

  Define `_classify_field_mention(name: str, line: str, canonical_sets: dict[str, set[str]]) -> tuple[str, str]` returning `(severity, message)`. Use the rules in `<behavior>`.

  For 决策号: implement `_extract_decision_roots(first_principles: Path) -> dict[int, str]` returning `{1: "T6 协议 ...", 2: "B3a Python runner ...", ...}`. Scan `^##.*§2\.([1-7]).*决策 ([1-7])` regex per header, then read body lines until next `^## ` header, join into a one-paragraph summary.

  Define `_check_decision_mention(decision_n: int, line: str, context: list[str], root_summary: str, file_name: str) -> Finding | None` returning a Finding if ERROR/WARNING, else None.

  Update `main()` to pass `schema_dir = root / ...` to `check_schema_references`. Run against the 7 design docs — expect zero ERRORs if the design docs are internally consistent (which they should be, having passed Phase 44-50 verification). Any ERROR indicates either (a) a real consistency bug to be flagged in the audit report (Task 5), or (b) an over-eager lint heuristic (Task 5 will tune if needed).

  Commit with `docs(51-01):` prefix — message e.g. `docs(51-01): implement Dimensions 2 + 3 — schema field-name cross-check + 决策号 1-7 consistency matrix`.
  </action>

  <verify>
    <automated>python3 /data/workspace/hermes-agent/scripts/v10-consistency-check.py --root /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design --format text 2>&1 | tee /tmp/v10-lint-task3.out && ! grep -E "Traceback|Error:" /tmp/v10-lint-task3.out && python3 -c "
import subprocess
r = subprocess.run(['python3', '/data/workspace/hermes-agent/scripts/v10-consistency-check.py', '--root', '/data/workspace/hermes-agent/.planning/research/v10-orchestrator-design'], capture_output=True, text=True)
assert 'Traceback' not in r.stdout + r.stderr, 'traceback'
# Task 3 doesn't enforce zero-ERROR yet — Task 5 does
print('exit:', r.returncode)
" && for field in persona_sha256 fitness_battery evidence_chain memory.max_records retained_python_phases schema_version expires_at verified_at project_id scope confidence half_life_days; do grep -q "$field" /data/workspace/hermes-agent/scripts/v10-consistency-check.py || { echo "MISSING schema field ref: $field"; exit 1; }; done && for sym in "def check_schema_references" "def check_decision_consistency" "def _load_yaml_field_names" "def _levenshtein" "def _extract_decision_roots" "决策"; do grep -q "$sym" /data/workspace/hermes-agent/scripts/v10-consistency-check.py || { echo "MISSING: $sym"; exit 1; }; done</automated>
  </verify>

  <done>
  - check_schema_references implemented, loads canonical field sets from 3 YAMLs, classifies mentions (PASS/WARNING/ERROR)
  - check_decision_consistency implemented, extracts 决策 1-7 roots from 00-FIRST-PRINCIPLES.md, flags re-definition or contradiction in downstream docs
  - All 7 决策 numbers (1-7) and key schema fields (persona_sha256, evidence_chain, memory.max_records, retained_python_phases, etc.) referenced in script
  - Script runs end-to-end against 7 design docs without Python errors
  - All `open()` calls pass `encoding="utf-8"`
  - Committed with `docs(51-01):` prefix
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement audit-output JSON formatter + --strict flag + finalize lint script — make `python3 scripts/v10-consistency-check.py` the production-ready VALIDATE-02 deliverable</name>
  <files>scripts/v10-consistency-check.py</files>

  <read_first>
    - Re-read scripts/v10-consistency-check.py Task 3 output (entire file — to wire formatter + --strict consistently)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Open Questions Consolidated (sanity-check OQ-9 MCP tool naming + OQ-10 retained_python_phases are reflected in lint output)
    - ./CLAUDE.md §Error Handling (catch specific exceptions with `as exc:`, never bare except)
  </read_first>

  <behavior>
  - `--format text` (default): one line per finding: `{dimension:<20} | {severity:<8} | {file}:{line} | {message}`. Summary line at end: `TOTAL: {N} findings (PASS={A} WARNING={B} ERROR={C})` to stderr.
  - `--format json`: emit a single JSON object `{"findings": [...], "summary": {"total": N, "pass": A, "warning": B, "error": C, "exit_code": 0|1}, "dimensions": {"terminology": {...}, "schema_references": {...}, "decision_consistency": {...}, "mcp_tool_naming": {...}}}` to stdout. Pretty-printed with indent=2.
  - `--strict`: WARNINGs treated as ERROR for exit-code purposes (ERROR count includes WARNINGs in summary; exit code 1 if any).
  - Exit code: 0 iff (ERROR == 0) and not (--strict and WARNING > 0). Otherwise 1.
  - Wrap the main entry point in try/except for the top-level file-not-found / permission cases — catch `OSError as exc` and emit a single ERROR finding + sys.exit(1). Do NOT catch generic Exception silently — let unexpected errors crash with a real traceback (CLAUDE.md convention).
  - Validate `--root` exists and is a directory; emit friendly error if not.
  - Validate the 7 design docs were found via glob; if <7 found, emit WARNING listing missing doc numbers (00-06).
  - Add a `if __name__ == "__main__": sys.exit(main())` guard (should already exist from Task 1).
  - Add comprehensive module docstring: `"""v10.0 cross-doc consistency lint (VALIDATE-02). ..."""` documenting the 4 dimensions, exit codes, CLI flags, and the conservative-false-positive-averse philosophy. Reference REQUIREMENTS.md VALIDATE-02 + ROADMAP Phase 51 SC#1-2.
  - All `open()` calls pass `encoding="utf-8"` — no exceptions.
  - Final script length ≥ 400 lines.
  </behavior>

  <action>
  Finalize `scripts/v10-consistency-check.py`:

  1. Implement the text + JSON formatters as described in `<behavior>`. Use `json.dumps(obj, indent=2, ensure_ascii=False)` for JSON (UTF-8 safe per CLAUDE.md encoding rule).

  2. Wire `--strict` into exit-code logic.

  3. Add input validation (root exists, design doc glob finds ≥7 files). Missing docs emit WARNING.

  4. Add top-level try/except OSError around the file-loading paths.

  5. Expand the module docstring to document 4 dimensions + exit codes + CLI flags + philosophy.

  6. Run the final script: `python3 scripts/v10-consistency-check.py --root .planning/research/v10-orchestrator-design` from repo root. Save the output to `/tmp/v10-lint-final.out` for use in Task 5 (audit report cites lint output summary).

  7. Verify SC#2 holds: zero ERRORs. If ERRORs exist, EITHER (a) fix the lint heuristic if it's over-eager (preferred — design docs passed Phase 44-50 verification), OR (b) if a real consistency bug is found, note it in the Task 5 audit report's tech_debt section and propose a follow-up. The default expectation is zero ERRORs — v10.0 design docs have been through 7 prior verification rounds.

  Commit with `docs(51-01):` prefix — message e.g. `docs(51-01): finalize v10-consistency-check.py — JSON formatter + --strict + validation (VALIDATE-02 ready)`.
  </action>

  <verify>
    <automated>cd /data/workspace/hermes-agent && python3 scripts/v10-consistency-check.py --root .planning/research/v10-orchestrator-design 2>&1 | tee /tmp/v10-lint-final.out && python3 scripts/v10-consistency-check.py --root .planning/research/v10-orchestrator-design --format json > /tmp/v10-lint-final.json && python3 -c "
import json
with open('/tmp/v10-lint-final.json', encoding='utf-8') as f:
    data = json.load(f)
assert 'findings' in data and 'summary' in data and 'dimensions' in data, 'JSON shape'
print('summary:', data['summary'])
# SC#2 expectation: zero ERRORs
errs = data['summary'].get('error', 0)
print('ERRORs:', errs)
# (Task 4 may still surface ERRORs if heuristic is off; Task 5 finalizes zero-ERROR gate by tuning)
" && grep -qE "(--format|--strict|ensure_ascii=False|__main__|Module docstring)" /data/workspace/hermes-agent/scripts/v10-consistency-check.py && wc -l /data/workspace/hermes-agent/scripts/v10-consistency-check.py | awk '$1 >= 400 {exit 0} {exit 1}' && ! grep -nE "open\((?!.*encoding=)" /data/workspace/hermes-agent/scripts/v10-consistency-check.py</automated>
  </verify>

  <done>
  - Lint script finalized: text + JSON output, --strict flag, input validation, top-level error handling
  - Module docstring documents 4 dimensions + CLI + philosophy
  - Every `open()` call passes `encoding="utf-8"` (grep gate)
  - ≥ 400 lines
  - Script runs against 7 design docs; output saved to /tmp/v10-lint-final.{out,json} for Task 5
  - SC#2 status known (zero ERRORs expected; if any, root cause identified and either heuristic-tuned or flagged for Task 5 audit tech_debt)
  - Committed with `docs(51-01):` prefix
  </done>
</task>

<task type="auto">
  <name>Task 5: Write .planning/milestones/v10.0-MILESTONE-AUDIT.md — 9-section audit (frontmatter + phase verification + 9-req traceability + cross-ref consistency + 16 OQ + 7 pitfall + 4 citation chain + integration flows + verdict + references)</name>
  <files>.planning/milestones/v10.0-MILESTONE-AUDIT.md</files>

  <read_first>
    - .planning/milestones/v9.0-MILESTONE-AUDIT.md §0-§9 (structural template — mirror, do NOT copy verbatim; v10.0 has 16 OQs + 7 pitfalls + 4 citation chains that v9.0 didn't have)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §Open Questions Consolidated (OQ-1..OQ-16 source — backs §4 of audit) + §Risk Register (P1/P2/P4/P5/P6/P7/P8 — backs §5 of audit) + §Cross-Cutting Findings (CC-1..CC-6 — backs §3 of audit)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §Risk Register Summary (line 470-488 — backs §5 pitfall audit)
    - .planning/REQUIREMENTS.md (full — backs §2 requirements traceability 9-row table)
    - .planning/ROADMAP.md §Phase 44-51 (backs §1 phase verification summary + §7 integration flows)
    - .planning/phases/{44,45,46,47,48,49,50}-*/{NN}-01-SUMMARY.md (8 SUMMARYs — verification sources for §2 traceability + §3 consistency)
    - /tmp/v10-lint-final.out + /tmp/v10-lint-final.json (Task 4 lint output — backs §3 cross-reference consistency with empirical evidence)
    - scripts/v10-consistency-check.py (Task 4 final — referenced in §3 + §8 verdict)
  </read_first>

  <behavior>
  - YAML frontmatter mirrors v9.0 audit shape, expanded for v10.0 design-only scope:
    ```
    ---
    milestone: v10.0
    milestone_name: Hermes-Agent 编排架构第一性原理推导(设计型)
    audited: 2026-07-07T00:00:00Z
    status: passed  # or tech_debt or FAIL — Task 5 decides based on lint + cross-check evidence
    baseline_commit: <Phase 44 first commit parent>  # anchor
    head_commit: HEAD (post-Phase-51)
    scores:
      requirements: 9/9
      phases: 8/8  # Phase 44-51
      integration_flows: <N>/<N>  # critical path + parallel waves
      oq_resolved: <X>/16  # RESOLVED count (deferred-to-v11.0 counts as RESOLVED-for-v10.0)
      pitfalls_mitigated: 7/7  # P1/P2/P4/P5/P8/P10/P14
      research_citations: 4/4  # STACK / FEATURES / ARCHITECTURE / PITFALLS
    gaps:
      requirements: []
      integration: []
    tech_debt:
      - phase: <if any>
        items: [<if any>]
    nyquist:
      compliant_phases: [44, 45, 46, 47, 48, 49, 50, 51]
      overall: compliant
    design_only: true  # NEW field — v10.0 is design-only milestone
    found08_preserved: n/a  # design-only milestone — no SKILL.md edits; FOUND-08 byte-diff N/A; explicitly declared
    ---
    ```
  - §0 — Scorecard table (mirror v9.0 §0): requirements 9/9 / phases 8/8 / integration flows / OQ resolved / pitfalls mitigated / research citations / test count (N/A — design-only) / FOUND-08 (N/A — design-only)
  - §1 — Phase Verification Summary (mirror v9.0 §1): 8-row table Phase × Name × Plans × Reqs × Status × Commit Range. Cite each phase's plan + SUMMARY. Status all `✅ passed` (assume — verify via SUMMARY existence + SC coverage declared in SUMMARY).
  - §2 — Requirements Traceability (9/9, mirror v9.0 §2 expanded to 9 rows): REQ-ID × Phase × Plan × Status × Verification Source. Each row's "Verification Source" cites the specific SUMMARY file + section + 1-line evidence quote. 7 DESIGN-* + 2 VALIDATE-* = 9 rows.
  - §3 — Design Doc Cross-Reference Consistency (NEW for v10.0): Cite lint script output (`scripts/v10-consistency-check.py` final run). For each of 4 dimensions: PASS/WARNING/ERROR count + sample findings + interpretation. List every WARNING explicitly (per SC#2). Conclusion: 7 design docs are cross-reference-consistent (or, if any ERROR, declare the inconsistency + remediation plan).
  - §4 — 16 OQ Resolution Audit (NEW for v10.0): 16-row table OQ-ID × Question (1-line summary) × Status (RESOLVED / DEFERRED-to-v11.0) × Resolution Doc §pointer × Evidence. Cross-reference SUMMARY.md §Open Questions Consolidated. All 16 must be RESOLVED or DEFERRED with rationale (per SC#4).
  - §5 — 7 Load-bearing Pitfall Mitigation Audit (NEW for v10.0, mirror SUMMARY §Risk Register shape): 7-row table Pitfall × Severity × Schema Field(s) × Phase 45/46/50 Section × PoC Acceptance Criterion (Phase 50 §4.X) × Status. P1/P2/P4/P5/P8/P10/P14 per ROADMAP SC#4. Each row's "Schema Field(s)" cites the specific agents-schema.yaml / memory-record-schema.yaml field name. Each row's "PoC Acceptance Criterion" cites Phase 50 §4.1-§4.7.
  - §6 — 4 Research Citation Chain Completeness (NEW for v10.0): 4-row table Source × Sections Cited × Sections Existing × Coverage. For each of STACK / FEATURES / ARCHITECTURE / PITFALLS, sample-check 5+ design doc citations and verify the cited §X.Y exists in the source. All 4 chains must be complete (per SC#4).
  - §7 — Integration Flows / Critical Path (mirror v9.0 §3 expanded): Critical path Phase 44 → 45 → 46 → 49 → 50 → 51 + parallel wave {47, 48} overlapping {45, 46}. Visual ASCII diagram (mirror ROADMAP §Critical Path).
  - §8 — Final Verdict + FOUND-08 Preservation (mirror v9.0 close-out): Verdict PASS / tech_debt / FAIL with 3-5 evidence pointers (each cites file + section). FOUND-08 N/A for design-only milestone — explicitly declared (not silently skipped). Tech debt enumerated if any (e.g. lint WARNINGs that should be cleaned up post-v10.0).
  - §9 — References: 7 design docs + 3 schema YAMLs + 5 research sources (STACK/FEATURES/ARCHITECTURE/PITFALLS/SUMMARY) + 8 phase SUMMARYs (44-50-01 + 51-01-self) + v9.0-MILESTONE-AUDIT.md precedent + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md.
  - Bilingual throughout: EN structure (headers, table column names, status enums) + 中文 prose (rationale, evidence narrative, examples) per CLAUDE.md deliverable form canon.
  - Total length ≥ 600 lines.
  </behavior>

  <action>
  Create file `.planning/milestones/v10.0-MILESTONE-AUDIT.md`. Write YAML frontmatter + §0-§9.

  For frontmatter: derive baseline_commit by `git log --format=%H -- .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md | tail -1` (first commit that introduced the first design doc — that's the v10.0 baseline). head_commit is `HEAD`. Populate scores from lint output + cross-check counts.

  For §1 Phase Verification Summary: read each `{NN}-01-SUMMARY.md` to extract: plan count, req count, completion date, commit range (from `git log --format=%H -- .planning/phases/{NN}-*/`). All 8 phases (44-51) should be `✅ passed`. Phase 51 cites "this audit" as its plan completion.

  For §2 Requirements Traceability: 9-row table. DESIGN-01 → Phase 44 → 44-01 → ✅ Complete → "00-FIRST-PRINCIPLES.md §2.1-§2.7 决策推导链 + §3 显式拒绝总表 ≥10 条 (verified by 44-01-SUMMARY §Found deliverables)". Repeat for DESIGN-02..07 + VALIDATE-01..02. VALIDATE-01 → "this audit §3-§6 (cross-reference + OQ + pitfall + citation chain checks)". VALIDATE-02 → "scripts/v10-consistency-check.py (4-dimension lint, this audit §3 cites output)".

  For §3 Cross-Reference Consistency: cite /tmp/v10-lint-final.out + /tmp/v10-lint-final.json. Enumerate findings per dimension. If lint surfaced any ERROR in Task 4, address here (either declare it a real bug → tech_debt, or document the heuristic false-positive → note for future lint iteration). Conclude: "lint script zero ERROR (per SC#2)" or "lint script N ERROR (remediation: ...)".

  For §4 OQ Resolution: 16 rows. Pull each OQ from SUMMARY.md §Open Questions Consolidated (the table on lines 65-82). For each: status (most should be RESOLVED with §pointer to a design doc; some may be DEFERRED-to-v11.0 with rationale like "mem0 Platform API 真实 filter 行为 — v11.0 PoC week-1 跑 latency benchmark per Phase 50 §4.2"). Examples: OQ-1 RESOLVED in 01-AGENT-REGISTRY-SCHEMA.md §X.Y (persona_sha256 + diff-only via git); OQ-7 RESOLVED in 01 + 05-POC-PLAN.md §4.4 (default 500, curator compaction pass); OQ-9 RESOLVED in 02-ROUND-TABLE-PROTOCOL.md §X (STACK form locked); OQ-12 PARTIALLY RESOLVED — Option B for v11.0 PoC + 物理分区 trigger documented in 06-CROSS-REPO-IMPACT.md, deferred to v12+ for actual migration.

  For §5 Pitfall Mitigation: 7 rows. For each of P1/P2/P4/P5/P8/P10/P14: cite the schema field(s) that mitigate it + the Phase 45 (or 46/50) section that introduces the field + the Phase 50 §4.X PoC acceptance criterion that validates the mitigation. Example: P1 → `persona_sha256` (Phase 45 agents-schema.yaml) + tiered memory core/archival (Phase 45 §X) + Phase 50 §4.1 fitness battery (regression auto-quarantine on persona drift). All 7 must be field-level mitigated (per SC#4).

  For §6 Citation Chain Completeness: 4 rows. For each source (STACK / FEATURES / ARCHITECTURE / PITFALLS): list 5+ sample §X.Y citations from across the 7 design docs + verify each cited section exists in the source (run `grep -E "^#+.*§?X\.Y\|^#+ X\.Y" source.md` for each). All 4 chains complete = coverage 100%.

  For §7 Integration Flows: critical path ASCII (copy/adapt from ROADMAP §Critical Path) + parallel wave note (Phase 47 + 48 parallel-eligible with 45/46). Confirm via SUMMARY cross-refs that the dependency graph held (no Phase executed out-of-order).

  For §8 Final Verdict: based on §1-§7 evidence, declare PASS / tech_debt / FAIL. Default expectation: PASS (9/9 reqs + 16/16 OQ + 7/7 pitfall + 4/4 citation + zero lint ERROR). If any dimension falls short, declare tech_debt with explicit remediation. FAIL only if a fundamental design inconsistency is uncovered (highly unlikely given Phase 44-50 verification rounds). FOUND-08: explicitly "N/A — v10.0 is design-only milestone (zero SKILL.md / Python / plugin edits per CLAUDE.md Scope). FOUND-08 byte-diff preservation check is structurally satisfied by absence of edits."

  For §9 References: bullet list, grouped by category (Design Docs / Schema YAMLs / Research Sources / Phase SUMMARYs / Precedent / Process Artifacts).

  Bilingual: EN table column names + status enums (PASSED / RESOLVED / etc.); 中文 rationale paragraphs under each section. Per CLAUDE.md deliverable form canon (EN structure + 中文段落/示例).

  Length target: ~600-900 lines (mirrors v9.0 audit ~330 lines × ~2x for v10.0 expanded scope: 16 OQs + 7 pitfalls + 4 citation chains + design-only FOUND-08 declaration all add content).

  Commit with `docs(51-01):` prefix — message e.g. `docs(51-01): write v10.0-MILESTONE-AUDIT.md — 9-section close-out (9/9 reqs + 16 OQ + 7 pitfall + verdict)`.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md && head -1 /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md | grep -q "^---" && grep -qE "^status: (passed|tech_debt|FAIL)" /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md && grep -qE "^milestone: v10\.0" /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md && grep -qE "design_only: true" /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md && for term in "VALIDATE-01" "VALIDATE-02" "DESIGN-01" "DESIGN-02" "DESIGN-03" "DESIGN-04" "DESIGN-05" "DESIGN-06" "DESIGN-07" "OQ-1" "OQ-7" "OQ-9" "OQ-12" "OQ-16" "P1" "P2" "P4" "P5" "P8" "P10" "P14" "persona_sha256" "evidence_chain" "memory.max_records" "retained_python_phases" "scope" "project_id" "STACK" "FEATURES" "ARCHITECTURE" "PITFALLS" "SUMMARY" "Phase 44" "Phase 45" "Phase 46" "Phase 47" "Phase 48" "Phase 49" "Phase 50" "Phase 51" "决策 1" "决策 6" "决策 7" "agent" "skill" "round table" "panel" "turn" "get_agent_persona" "submit_round_table_result" "run_python_phase" "v9.0-MILESTONE-AUDIT" "scripts/v10-consistency-check.py" "44-01-SUMMARY" "45-01-SUMMARY" "46-01-SUMMARY" "47-01-SUMMARY" "48-01-SUMMARY" "49-01-SUMMARY" "50-01-SUMMARY" "PASS" "verdict" "FOUND-08" "design-only" "tech_debt"; do grep -q "$term" /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md || { echo "MISSING: $term"; exit 1; }; done && grep -cE "^## " /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md | awk '$1 >= 9 {exit 0} {exit 1}' && grep -cE "OQ-[1-9]\|OQ-1[0-6]" /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md | awk '$1 >= 12 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/milestones/v10.0-MILESTONE-AUDIT.md | awk '$1 >= 600 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - .planning/milestones/v10.0-MILESTONE-AUDIT.md exists with valid YAML frontmatter
  - Frontmatter status field set to passed | tech_debt | FAIL based on §1-§7 evidence
  - design_only: true field present (v10.0 specific)
  - §0 Scorecard with 9/9 reqs + 8/8 phases + integration flows + 16/16 OQ + 7/7 pitfall + 4/4 citation
  - §1 Phase Verification Summary with all 8 phases (44-51) marked passed
  - §2 Requirements Traceability 9-row table, each REQ-ID cites its phase SUMMARY
  - §3 Cross-Reference Consistency cites lint script output (per SC#2 zero-ERROR check)
  - §4 16 OQ Resolution Audit, each OQ marked RESOLVED or DEFERRED-to-v11.0 with pointer
  - §5 7 Pitfall Mitigation, each cites schema field + Phase 45/46/50 section + PoC acceptance criterion
  - §6 4 Citation Chain Completeness, each source row shows coverage 100%
  - §7 Integration Flows with critical path + parallel wave visualization
  - §8 Final Verdict with evidence pointers + FOUND-08 N/A declaration (design-only)
  - §9 References grouped by category
  - ≥ 600 lines, bilingual EN structure + 中文 prose
  - Committed with `docs(51-01):` prefix
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc (markdown) → lint script (Python) | The lint script READS 7 design docs + 3 schema YAMLs. Malformed input (e.g. binary content, very long lines, regex-hostile chars) could crash or hang the script. Mitigated by: stdlib-only regex with bounded scan, file-size sanity check, line-length cap per file (lint refuses to scan files > 5 MB or single lines > 10K chars — emits single WARNING). |
| lint script → audit report | Audit report §3 cites lint output. If lint produces false-positive ERRORs, audit could declare tech_debt/FAIL prematurely. Mitigated by: conservative-heuristic philosophy (Tasks 2-4 explicitly prefer false-negatives over false-positives), Task 5 reviews every ERROR before declaring verdict. |
| audit report → operator (Kai) → milestone tag decision | Operator reads audit verdict to decide whether to tag v10.0 + start v11.0 PoC. Wrong verdict (false PASS) could leak inconsistency into v11.0 implementation. Mitigated by: §8 evidence pointers (file + section) are third-party-reproducible; operator can spot-check before tagging. |
| 8 phase SUMMARYs → audit §2 traceability | Audit §2 cites each SUMMARY as verification source. If a SUMMARY is stale (doesn't reflect actual deliverables), audit inherits the staleness. Mitigated by: §2 spot-checks 1-2 evidence quotes per row; if quote doesn't match SUMMARY content, audit fails the row. |
| 4 research sources → audit §6 citation chains | Audit §6 verifies every cited §X.Y exists in source. Source drift (e.g. someone edits STACK.md post-design) could invalidate. Mitigated by: audit is point-in-time snapshot; baseline_commit + head_commit in frontmatter anchor the verification window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | Lint heuristics — over-eager ERRORs could force tech_debt verdict on a clean milestone | mitigate | Tasks 2-4 explicitly adopt conservative philosophy (false-negatives preferred over false-positives); Task 5 reviews every ERROR before declaring verdict; if heuristic is the problem, tune in Task 4 before locking verdict in Task 5. |
| T-51-02 | Information Disclosure | Audit report §2 evidence quotes — quoting too much design-doc content could leak sensitive operator context | accept | v10.0 design docs are internal planning artifacts, not operator PII; 1-line evidence quotes are fair-use within the repo; CLAUDE.md does not classify planning artifacts as confidential. |
| T-51-03 | Denial of Service | Lint script on malformed input — deeply nested regex, 100 MB markdown, hostile patterns | mitigate | stdlib-only bounded regex; per-file 5 MB cap; per-line 10 K char cap; specific-exception catching (`OSError as exc`); no recursion in field-extraction parser (iterative line-scan). |
| T-51-04 | Elevation of Privilege | Audit report author (Claude) marks verdict PASS without sufficient evidence | mitigate | §8 verdict requires ≥3 evidence pointers (file + section) per claim; every REQ/OQ/pitfall row in §2/§4/§5 has its own evidence column; operator can spot-check before tagging milestone. |
| T-51-05 | Repudiation | Post-tag drift — design docs edited after audit, audit becomes stale | mitigate | frontmatter baseline_commit + head_commit + audited timestamp anchor the verification window; future edits require re-audit (declared in §8). |
| T-51-06 | Spoofing | Audit cites a SUMMARY that doesn't actually exist (fabricated verification source) | mitigate | Task 5 read_first list includes all 8 SUMMARY files explicitly; verify step greps the cited path; missing SUMMARY would fail the automated gate. |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-51-SC (supply chain from package installs) is not applicable. Lint script uses Python stdlib only (no pip install).
</threat_model>

<verification>
**Phase 51 VALIDATE-01 + VALIDATE-02 success criteria (from ROADMAP §Phase 51):**

1. SC#1 — `scripts/v10-consistency-check.py` lint script exists, checks 4 dimensions (terminology / schema refs / 决策号 1-7 / MCP tool naming) (verified by Task 1-4)
2. SC#2 — Lint script runs on all 7 design docs, zero ERRORs (WARNINGs acceptable, listed in audit report) (verified by Task 4 final lint run + Task 5 §3 of audit report enumerating WARNINGs)
3. SC#3 — `.planning/milestones/v10.0-MILESTONE-AUDIT.md` exists, 9/9 reqs each cross-checked with phase SUMMARY (verified by Task 5 §2 of audit report)
4. SC#4 — Audit report checks: 7 design docs cross-reference consistency + 16 OQs resolved/deferred + 7 load-bearing pitfalls (P1/P2/P4/P5/P8/P10/P14) field-level mitigation in 45/46/50 + 4 research citation chains complete (verified by Task 5 §3 + §4 + §5 + §6 of audit report)
5. SC#5 — Audit report gives milestone-level PASS / tech_debt / FAIL verdict with evidence pointers (verified by Task 5 §8 of audit report)

**Final deliverables target:**
- `scripts/v10-consistency-check.py`: ~400-500 lines, Python 3.11+ stdlib-only, type-hinted, `encoding="utf-8"` everywhere
- `.planning/milestones/v10.0-MILESTONE-AUDIT.md`: ~600-900 lines, bilingual, 9 sections, mirror v9.0 close-out rigor at expanded scope

**Methodology audit (self-check before commit):**
- Lint script uses stdlib only (no PyYAML, no external imports beyond argparse/pathlib/re/sys/json)
- Every `open()` in lint script passes `encoding="utf-8"` (Ruff PLW1514)
- Lint script is conservative (false-negative > false-positive) — design docs passed 7 prior verification rounds, so any ERROR is suspicious
- Audit report §3 cites empirical lint output (not invented summary)
- Audit report §4 OQ rows map to SUMMARY.md §Open Questions Consolidated entries 1-to-1
- Audit report §5 pitfall rows map to ROADMAP SC#4 7-pitfall list 1-to-1 (P1/P2/P4/P5/P8/P10/P14)
- Audit report §8 verdict is evidence-backed (≥3 file+section pointers per claim)
- FOUND-08 N/A declaration explicit (not silently skipped) — v10.0 is design-only per CLAUDE.md Scope
- Bilingual: EN structure (table column names, status enums, YAML keys) + 中文 prose (rationale paragraphs, evidence narrative) per CLAUDE.md deliverable form canon
</verification>

<success_criteria>
**Phase 51 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/51-validate/51-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) The 2 deliverable files exist:
   - `scripts/v10-consistency-check.py` (≥ 400 lines, 4 dimensions, stdlib-only, zero ERRORs on 7 design docs)
   - `.planning/milestones/v10.0-MILESTONE-AUDIT.md` (≥ 600 lines, 9 sections, 9/9 reqs + 16 OQ + 7 pitfall + verdict)
3. ROADMAP.md Phase 51 entry updated (Plans: 1 plan, plan list with checkmark).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/51-validate/51-01-SUMMARY.md` when done. SUMMARY should record:
- Final line count of both deliverables (lint script + audit report)
- Lint script final output summary (TOTAL findings / PASS / WARNING / ERROR counts per dimension)
- Audit report final verdict (PASS / tech_debt / FAIL) + frontmatter status value
- 9/9 reqs satisfied (cite audit §2)
- 16/16 OQ resolved (cite audit §4 — count RESOLVED + DEFERRED-to-v11.0)
- 7/7 pitfall mitigated (cite audit §5)
- 4/4 citation chain complete (cite audit §6)
- SC#1-5 coverage confirmed
- v10.0 milestone ready to tag (operator action item: `git tag v10.0` once satisfied)
- Cross-validation: every section of audit cites phase SUMMARY + design doc + lint output for empirical evidence
</output>

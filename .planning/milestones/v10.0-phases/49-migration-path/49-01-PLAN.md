---
phase: 49-migration-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md
autonomous: true
requirements: [DESIGN-05]
granularity: standard

must_haves:
  truths:
    - "File .planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md exists"
    - "15 expert × 5-field transform 规则表完整 —— 每 expert 列出 SKILL frontmatter → agent YAML 的 5 个字段映射 + 默认值 + edge cases (75 cells total = 15 experts × 5 fields, source experts = ARCHITECTURE §2 15-expert table copied verbatim, FOUND-08 preserved: all 15 expert_id values unchanged)"
    - "default_invocation: skill_fallback → mcp_tool 切换机制文档化 (agent 优先 MCP tool, 失败回退 SKILL form; failure modes + transition path + safe-default-on-unknown explicit; resolves FOUND-08 backward-compat anchor)"
    - "Memory schema 迁移计划 complete —— from v6.0 FeedbackStore JSONL (agent/feedback_store.py buckets/<skill_id>/<source>.jsonl + sha256-registry.jsonl + _INDEX_VERSION=1) to new memory-record-schema (Phase 45 10 mandated fields) — includes schema_version field for forward compat (cite memory-record-schema.yaml line 353, do NOT redefine) + dry-run migration mode `hermes agent memory migrate --dry-run` per PITFALLS §P14 mitigation 3 (prevents P14 silent drops / unsafe defaults)"
    - "Retained-phases allowlist 显式声明 —— run_python_phase only accepts Steps 0/6.5/7/10/11/12/15; allowlist location = round-table-state-schema.yaml `retained_python_phases` field (per STACK §3.2 Tool 7 line 577 + STACK §11.2 line 1120 + Phase 46 schema); resolves OQ-10"
    - "旧 v7.0 mem0 agent_id=hermes memory 遗留策略 explicit (per SUMMARY OQ-3: 遗留/不迁移; new agent invocations start fresh from agent_id=screenplay etc.; legacy read-only fallback for transition period); resolves OQ-3"
    - "Phase 44 决策 1-7 + 决策 2 (B3a Python runner 增量迁移 root argument) each cited by 决策号 at least once across the narrative (cross-validation of locked decisions, not re-derivation)"
    - "ARCHITECTURE §1.1 (18-field schema) + §1.2 (SKILL→YAML disposition) + §2 (15-expert table) + §6 + §6.1 (transform procedure) + §6.4 (repo impact) each cited by section number at least once; PITFALLS §P14 cited; STACK §3.2 Tool 7 cited; SUMMARY OQ-3 + OQ-10 each cited"
  artifacts:
    - path: ".planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md"
      provides: "v10.0 design doc #04 — Python runner incremental migration plan with 4 lockable artifacts: (a) 15-expert × 5-field transform 规则表 (75 cells, source = ARCHITECTURE §2 15-expert table verbatim, per-expert persona framing + tool whitelist + refs count + related_agents count + edge cases), (b) default_invocation: skill_fallback → mcp_tool 切换机制 (failure modes + transition path + FOUND-08 backward-compat), (c) Memory schema 迁移计划 (v6.0 FeedbackStore JSONL → Phase 45 memory-record-schema, schema_version + dry-run mode, prevents P14), (d) retained-phases allowlist (run_python_phase only Steps 0/6.5/7/10/11/12/15, location = round-table-state-schema.yaml `retained_python_phases` field, resolves OQ-10) + legacy v7.0 mem0 agent_id=hermes policy (遗留/不迁移, resolves OQ-3). Cite Phase 44 决策 1-7 (especially 决策 2 B3a Python runner) + Phase 45 agents-schema.yaml 18-field schema + memory-record-schema.yaml 10 fields + Phase 46 round-table-state-schema + ARCHITECTURE §1/§2/§6 + STACK §3.2 Tool 7 by reference — does NOT re-derive them. Single deliverable (no schema YAML, like Phase 47/48)."
      min_lines: 800
      contains: ["15 expert", "transform 规则", "hook_retention", "creative_source", "screenplay", "script_auditor", "character_designer", "cinematographer", "style_genome", "prompt_injector", "visual_executor", "continuity_auditor", "audio_pipeline", "editor", "colorist", "compliance_gate", "theory_critic", "default_invocation", "skill_fallback", "mcp_tool", "FeedbackStore", "feedback_store", "JSONL", "jsonl", "schema_version", "dry-run", "--dry-run", "retained_python_phases", "run_python_phase", "Step 0", "Step 6.5", "Step 7", "Step 10", "Step 11", "Step 12", "Step 15", "agent_id=hermes", "mem0", "FOUND-08", "ARCHITECTURE §1.1", "ARCHITECTURE §1.2", "ARCHITECTURE §2", "ARCHITECTURE §6", "ARCHITECTURE §6.1", "ARCHITECTURE §6.4", "PITFALLS §P14", "STACK §3.2", "OQ-3", "OQ-10", "决策 1", "决策 2", "决策 5", "决策 6", "决策 7"]
  key_links:
    - from: "04-MIGRATION-PATH.md"
      to: "00-FIRST-PRINCIPLES.md §2.1-§2.7 (decisions 1-7, esp §2.2 决策 2 B3a Python runner root)"
      via: "markdown §reference + 决策号 citation"
      pattern: "00-FIRST-PRINCIPLES|决策 [1-7]"
    - from: "04-MIGRATION-PATH.md"
      to: "ARCHITECTURE.md §1.1 (18-field schema) + §1.2 (SKILL→YAML disposition) + §2 (15-expert table) + §6.1 (transform procedure) + §6.4 (repo impact)"
      via: "markdown §reference for 15-expert transform + field disposition citations"
      pattern: "ARCHITECTURE §(1\\.1|1\\.2|2|6|6\\.1|6\\.4)"
    - from: "04-MIGRATION-PATH.md"
      to: "agents-schema.yaml (18 fields, esp default_invocation enum + lineage + memory_scope) + memory-record-schema.yaml (10 fields, esp schema_version line 353 + agent_id + scope)"
      via: "markdown §reference for target schema fields consumed by transform"
      pattern: "agents-schema\\.yaml|memory-record-schema\\.yaml|schema_version|default_invocation|memory_scope"
    - from: "04-MIGRATION-PATH.md"
      to: "round-table-state-schema.yaml (retained_python_phases field — Phase 46 lock, allowlist location per OQ-10)"
      via: "markdown §reference for run_python_phase allowlist location"
      pattern: "round-table-state-schema|retained_python_phases|round-table-schema"
    - from: "04-MIGRATION-PATH.md"
      to: "PITFALLS.md §P14 (schema migration breaks memory store) + STACK §3.2 Tool 7 (run_python_phase) + STACK §11.2 line 1120 (allowlist OQ-10)"
      via: "markdown §reference for P14 mitigations + run_python_phase boundary tool + OQ-10 resolution"
      pattern: "PITFALLS §P14|Pitfall 14|STACK §3\\.2|STACK §11\\.2|run_python_phase"
    - from: "04-MIGRATION-PATH.md"
      to: "SUMMARY.md OQ-3 (v7.0 mem0 agent_id=hermes legacy) + OQ-10 (run_python_phase allowlist location)"
      via: "markdown §reference for resolved open questions"
      pattern: "OQ-(3|10)"
---

<objective>
Create the v10.0 design doc #04 — the **Python runner incremental migration plan** — as a single markdown deliverable:

`.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md` (~800-1300 lines, bilingual EN structure + 中文 prose per CLAUDE.md)

Purpose: Produce the (a) 15-expert × 5-field transform 规则表 (75 cells, source experts = ARCHITECTURE §2 15-expert table verbatim — FOUND-08 preserved, all 15 `expert_id` values unchanged) enumerating per-expert SKILL frontmatter → agent YAML mapping with default + edge cases; (b) `default_invocation: skill_fallback → mcp_tool` 切换机制 (agent prefers MCP tool, on failure falls back to SKILL form — failure modes + transition path + safe-default-on-unknown explicit, preserves FOUND-08 backward-compat anchor); (c) Memory schema 迁移计划 from v6.0 FeedbackStore JSONL (`agent/feedback_store.py` `buckets/<skill_id>/<source>.jsonl` + `dedup/sha256-registry.jsonl` + `_INDEX_VERSION = 1`) to Phase 45 memory-record-schema (10 mandated fields), including `schema_version` field for forward compat (cite memory-record-schema.yaml line 353) + dry-run migration mode `hermes agent memory migrate --dry-run` per PITFALLS §P14 mitigation 3 (prevents P14 silent drops / unsafe defaults); (d) Retained-phases allowlist (`run_python_phase` only accepts Steps 0/6.5/7/10/11/12/15, location = `round-table-state-schema.yaml` `retained_python_phases` field per STACK §3.2 Tool 7 + Phase 46 — resolves OQ-10) + legacy v7.0 mem0 `agent_id=hermes` memory 遗留策略 (遗留/不迁移, new agent invocations start fresh from `agent_id=screenplay` etc. — resolves OQ-3).

The next design docs (05-POC-PLAN consumes transform 规则 + memory migration dry-run as PoC acceptance criterion; Phase 50 consumes retained-phases allowlist for PoC `run_python_phase` test scope) and the Phase 51 milestone audit will cite this doc as the "v11.0 PoC implementer's transform + migration guide" — when an implementer asks "how do I transform screenplay SKILL.md into screenplay.agent.yaml?" or "what happens to v6.0 FeedbackStore records?" or "which Python phases can `run_python_phase` legally execute?", this doc answers with section-and-line-precise citations.

Output: 1 file under `.planning/research/v10-orchestrator-design/`, mirroring Phase 44/45/46/47/48's bilingual depth/rigor at comparable scope (Phase 44 was 1181 lines; Phase 48 was 1300+ lines; Phase 49 target ~800-1300 lines — the entire budget goes to the narrative). Phase 49 is single-deliverable (no schema YAML) like Phase 47/48. It ELABORATES an existing source table (ARCHITECTURE §1.2 + §2 15-expert table) AND introduces net-new content (memory migration plan + retained-phases allowlist + skill_fallback switching mechanism) — the elaboration discipline is "default + edge case per cell" + "dry-run + safe-default operational semantics that don't yet exist in any source doc".

This is a DESIGN-ONLY deliverable. Zero code changes (no SKILL.md / Python / plugin / mcp_serve.py edits). Cite Phase 44's locked decisions (1-7, especially 决策 2 B3a Python runner root argument), Phase 45's locked 18-field agents-schema.yaml + 10-field memory-record-schema.yaml, Phase 46's locked round-table-state-schema.yaml — do NOT re-derive them. Cite, do NOT redefine: `default_invocation` enum, `lineage`, `memory_scope`, `schema_version`, `retained_python_phases`, `agent_id` are all consumed here by reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v10-orchestrator-design/SUMMARY.md
@.planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md
@.planning/research/v10-orchestrator-design/01-AGENT-REGISTRY-SCHEMA.md
@.planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md
@.planning/research/v10-orchestrator-design/STACK.md
@.planning/research/v10-orchestrator-design/FEATURES.md
@.planning/research/v10-orchestrator-design/ARCHITECTURE.md
@.planning/research/v10-orchestrator-design/PITFALLS.md

<interfaces>
<!-- Phase 44 root-argument anchors (do NOT re-derive; cite by 决策号) -->
<!-- Decision 1: T6 协议 (Hermes MCP server + tmux dispatch + CC native MCP client) — sets Hermes as runtime owner of MCP tool surface (run_python_phase is one of 7 MCP tools) -->
<!-- Decision 2: B3a Python runner 增量迁移 (delegate-only phase 迁 CC via run_python_phase boundary tool) — LOAD-BEARING for SC#5 retained-phases allowlist (only delegate-only phases eligible) -->
<!-- Decision 5: α agent form (YAML + persona + tools + refs + memory_scope + lineage) — transform target schema -->
<!-- Decision 6: per-agent memory + curator-driven 自进化 — load-bearing for SC#4 memory schema migration (per-agent namespace = new memory-record-schema) -->
<!-- Decision 7: 分层 CC 角色 (Hermes controls turn_order/max_rounds/early_stop_rule/schema; CC controls framing + synthesis) — run_python_phase is Hermes-side boundary tool, CC invokes via MCP -->

<!-- Phase 45 schema contracts (CONSUME, do NOT redefine): -->
<!-- agents-schema.yaml 18 fields (esp #6 memory_scope enum shared|per_agent|project_scoped, #7 lineage {derived_from_skill_id, derived_from_repo, transform_date, transform_notes, skill_sha256}, #18 default_invocation enum mcp_tool|skill_fallback|disabled) -->
<!-- memory-record-schema.yaml 10 mandated fields (record_id, agent_id required, scope global|project|session, project_id, session_id, status active|archived|quarantined, confidence, half_life_days, expires_at, verified_at, supersedes_memory_id, evidence_chain, evidence_operator_ids, confidentiality, created_at, last_recalled_at, recall_count, schema_version line 353, content) -->

<!-- Phase 46 round-table-state-schema contract (CONSUME + ADD allowlist field, do NOT redefine existing): -->
<!-- Schema file: ~/.hermes/agents/.runtime/{project_slug}/round_tables/{round_id}.json -->
<!-- Existing required: roundId, projectId, openedAt, panelists, status, turns -->
<!-- NEW field this doc specifies: retained_python_phases — array of allowed step IDs (Steps 0/6.5/7/10/11/12/15) — enforcement point for run_python_phase boundary tool -->

<!-- ARCHITECTURE §1.2 SKILL→YAML disposition table (the skeleton SC#2 elaborates): -->
<!-- | name | Reused as name (must match filename stem) | -->
<!-- | description | Reused verbatim, refined if needed | -->
<!-- | version | Reused, but bumped to 1.0.0 on first transform | -->
<!-- | author, license | DROPPED — agent YAML is operator-owned | -->
<!-- | platforms | Reused verbatim | -->
<!-- | prerequisites | Reused as prerequisites (different from tools) | -->
<!-- | metadata.hermes.tags | Reused as tags | -->
<!-- | metadata.hermes.related_skills | Copied to related_agents | -->
<!-- | metadata.hermes.expert_id | Reused as expert_id (FOUND-08 backward-compat anchor) | -->
<!-- | metadata.hermes.metrics | Reused as metrics | -->
<!-- | SKILL body (markdown) | NOT copied. Becomes input to persona rewrite | -->
<!-- | ## References table | Flattened into refs list | -->

<!-- ARCHITECTURE §2 15-expert table (verbatim source for SC#2 — 15 rows, columns: Expert | tools | persona framing | refs count | related_agents count): -->
<!-- hook_retention, creative_source, screenplay, script_auditor, character_designer, cinematographer, style_genome, prompt_injector, visual_executor, continuity_auditor, audio_pipeline, editor, colorist, compliance_gate, theory_critic -->

<!-- ARCHITECTURE §6.1 Transform Procedure (5 steps — source for SC#2 forward chain): -->
<!-- 1. Read source SKILL.md from kais-hermes-skills repo -->
<!-- 2. Compute skill_sha256 -->
<!-- 3. Generate agent YAML per 5-field transform mapping -->
<!-- 4. Write to ~/.hermes/agents/{name}.agent.yaml -->
<!-- 5. Record lineage block -->

<!-- ARCHITECTURE §6.4 Repo Impact Summary (Phase 49 placement): -->
<!-- | hermes-agent | v10.0: 7 design docs (zero code) | v11.0 PoC: agent_registry.py + agent_dispatcher.py + mcp_serve.py + mem0 filter + curator _memory_evolution_phase | v12+: hermes agent transform CLI + dashboard | -->
<!-- | kais-hermes-skills | None (read-only source) | None (SKILLs remain as fallback per default_invocation: skill_fallback) | Optional: agent_transform_notes frontmatter | -->
<!-- | ~/.hermes/agents/ | None (directory doesn't exist yet) | v11.0 PoC: 15 *.agent.yaml via manual transform | v12+: curator-managed evolution_log + fitness_score | -->

<!-- PITFALLS §P14 mitigations (load-bearing for SC#4): -->
<!-- Mitigation 1: Schema version on every record: schema_version: int. Read path tolerates older versions; migration job backfills. -->
<!-- Mitigation 2: Migration safety: default unknown fields to the SAFEST value, not the most permissive. Unknown confidentiality → confidential (not public). -->
<!-- Mitigation 3: Migration script with dry-run: `hermes agent memory migrate --dry-run` shows what would change before applying. -->

<!-- STACK §3.2 Tool 7 run_python_phase (load-bearing for SC#5): -->
<!-- Signature: async def run_python_phase(phase: str, ...); allowlist: see round-table-schema.yaml (OQ-10 resolution: location = round-table-state-schema.yaml retained_python_phases field) -->
<!-- STACK §11.2 line 1120: "run_python_phase 的 retained-phases allowlist 在哪定义? —— round-table-schema.yaml" (resolved) -->

<!-- SUMMARY OQ-3 + OQ-10 (the two OQs this doc resolves): -->
<!-- OQ-3: mem0 agent_id 冲突. v7.0 默认 agent_id=hermes 的旧 memory 怎么处理? 倾向性结论: 遗留 (不迁移), 新 agent invocation 从 agent_id=screenplay 等开始. 在 04-MIGRATION-PATH.md 解决. -->
<!-- OQ-10: run_python_phase 的 retained-phases allowlist 在哪定义? 倾向性结论: round-table-schema.yaml. 在 04-MIGRATION-PATH.md 解决. -->

<!-- v6.0 FeedbackStore source schema (load-bearing for SC#4 — read agent/feedback_store.py for ground truth): -->
<!-- Persistence: buckets/<skill_id>/<source>.jsonl (append-only, one FeedbackRecord per line) + dedup/sha256-registry.jsonl (audit log) + index.json (versioned via _INDEX_VERSION = 1) -->
<!-- FeedbackRecord fields: skill_id, source, skill_sha256, feedback_text, verdict, operator_id, created_at, ... (v6.0 ship) -->
<!-- This is the MIGRATION SOURCE; memory-record-schema.yaml (Phase 45) is the MIGRATION TARGET. -->

From .planning/research/v10-orchestrator-design/SUMMARY.md (Phase 49's place in the design graph):
> 04-MIGRATION-PATH.md (依赖 01/02, consumes 18-field schema + memory-record-schema + round-table-state-schema, produces 15-expert 5-field transform rules + memory migration plan with schema_version + retained-phases allowlist). Delivers: 15 expert × 5-field transform 规则 + skill_fallback 切换 + memory schema 迁移 + retained-phases allowlist + schema_version.

From .planning/research/v10-orchestrator-design/ARCHITECTURE.md §6.4 (Repo Impact Summary — Phase 49's v11.0 PoC deliverable list):
> | ~/.hermes/agents/ | None (directory doesn't exist yet) | v11.0 PoC deliverable: 15 *.agent.yaml files via manual transform | v12+ deliverable: curator-managed evolution_log + fitness_score mutations |

From .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.2 (SKILL→YAML disposition — the skeleton SC#2 elaborates to 75 cells):
> Agent-only fields (no SKILL equivalent): persona (rewritten system-prompt fragment), tools (runtime whitelist distinct from prerequisites.tools activation), memory_scope, lineage, evolution_log, fitness_score, round_table_eligible, default_invocation.

From .planning/research/v10-orchestrator-design/PITFALLS.md §P14 (load-bearing for SC#4):
> Pitfall 14: Schema Migration Breaks Memory Store. v11.0 PoC adds confidentiality field to memory records. Existing 20K records have no confidentiality field. Read path fails or, worse, defaults to public for records that should be confidential. Mitigations: (1) schema_version: int on every record, read path tolerates older versions; (2) default unknown to safest value not most permissive; (3) migration script with dry-run.

From .planning/research/v10-orchestrator-design/STACK.md §3.2 Tool 7 + §11.2 (load-bearing for SC#5):
> Tool 7: run_python_phase — B3a 增量迁移的 boundary tool. Signature: async def run_python_phase(phase, ...). Allowlist: see round-table-schema.yaml.
> §11.2 OQ-10: run_python_phase 的 retained-phases allowlist 在哪定义? → round-table-schema.yaml (= round-table-state-schema.yaml retained_python_phases field).

From .planning/research/v10-orchestrator-design/SUMMARY.md OQ-3 + OQ-10 (the two OQs this doc resolves):
> OQ-3: mem0 agent_id 冲突. v7.0 默认 agent_id=hermes 的旧 memory 怎么处理? 倾向性结论: 遗留 (不迁移), 新 agent invocation 从 agent_id=screenplay 等开始. 在 04-MIGRATION-PATH.md 解决.
> OQ-10: run_python_phase 的 retained-phases allowlist 在哪定义? 倾向性结论: round-table-schema.yaml. 在 04-MIGRATION-PATH.md 解决.

From CLAUDE.md (deliverable form canon):
> SKILL.md 双语 (英文 YAML 结构 + metadata 保留英文; description 与正文采用 EN structure + 中文段落/示例)
> Markdown tables for dimension comparison

From agent/feedback_store.py (SC#4 migration SOURCE schema ground truth):
> _INDEX_VERSION: int = 1
> class FeedbackStore: persists to buckets/<skill_id>/<source>.jsonl + dedup/sha256-registry.jsonl + index.json
> FeedbackRecord fields: skill_id, source, skill_sha256, feedback_text, verdict, operator_id, created_at

From MEMORY.md (in-repo, sets migration historical context):
> v6-self-evolution-milestone.md: v6.0 SHIPPED feedback-driven self-learning (curator + FeedbackStore + evolution pipeline). FeedbackStore is the source schema being migrated.
> kais-movie-agent-v5-hermes-native-migration.md: 2026-07-02 cross-repo migration moved skills/movie-experts + 4 plugins to kais-hermes-skills repo. SKILL.md there is transform source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan + write §0 reading guide + §1 framing (15-expert transform purpose + Phase 44 决策 2 B3a citation anchor + SC mapping)</name>
  <files>.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §0 + §1 (frontmatter + reading-guide pattern — apply same bilingual structure) + §2.1 (决策 1 — T6 协议 layer, sets Hermes as runtime owner of MCP tool surface) + §2.2 (决策 2 — B3a Python runner 增量迁移 root argument, LOAD-BEARING for SC#5) + §2.5 (决策 5 — α agent form transform target schema) + §2.6 (决策 6 — per-agent memory load-bearing for SC#4) + §2.7 (决策 7 — Hermes owns run_python_phase boundary tool)
    - .planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md §0 + §1 (Phase 48 pattern — most recent sibling single-deliverable doc, mirror structure)
    - .planning/research/v10-orchestrator-design/SUMMARY.md §"Roadmap Implications" (Phase 49 placement: depends on 01/02, produces 15-expert transform rules + memory migration + retained-phases allowlist) + OQ-3 (line 69 — v7.0 mem0 legacy) + OQ-10 (line 76 — allowlist location)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.1 lines 27-52 (18-field schema source table) + §1.2 lines 54-71 (SKILL→YAML disposition — the 5-field mapping skeleton SC#2 elaborates) + §1.3 lines 73-116 (screenplay minimal example — concrete reference for transform output) + §2 lines 120-148 (15-expert table — verbatim source for SC#2) + §6.1 lines 582-592 (transform procedure 5 steps) + §6.4 lines 625-632 (repo impact summary — Phase 49 v11.0 PoC deliverable = 15 *.agent.yaml via manual transform)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 Tool 7 lines 538-610 (run_python_phase signature + allowlist pointer to round-table-schema.yaml) + §11.2 line 1120 (OQ-10 resolution: allowlist location)
    - .planning/research/v10-orchestrator-design/PITFALLS.md §P14 lines 438-454 (schema migration — 3 mitigations)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §5.7 lines 1119-1145 (run_python_phase round-table consumer — allowlist enforcement point)
  </read_first>

  <behavior>
  - Document frontmatter block (status / phase / audience / reading time / stability markers / confidence) mirrors 00-FIRST-PRINCIPLES.md + 06-CROSS-REPO-IMPACT.md style
  - §0 阅读指南 exists with chapter map, 3-audience guide (Kai reviewer / v11.0 PoC implementer / v12+ transform CLI implementer), 稳定性 markers, "what this doc consumes from Phase 44/45/46 + ARCHITECTURE §1/§2/§6 + STACK §3.2 + PITFALLS §P14" preamble
  - §1.1 exists: framing — "本文档是 Phase 44 决策 2 (B3a Python runner 增量迁移 — delegate-only phase 迁 CC via run_python_phase boundary tool) + 决策 5 (α agent form) + 决策 6 (per-agent memory) 的迁移路径落实 + ARCHITECTURE §1.2 + §2 已有 SKILL→YAML mapping 表的细化到 75-cell (15 expert × 5 field) 可执行粒度 + net-new 3 件套 (memory schema 迁移 + retained-phases allowlist + skill_fallback 切换机制)" + cite Phase 44 决策 1/2/5/6/7 as root arguments + explicitly state 5 SC and what is consumed vs net-new
  - §1.2 exists: scope rules — "本 doc 细化 ARCHITECTURE §1.2 + §2 已有 mapping 到 75-cell 可执行粒度, 不重定义 5-field mapping 框架本身. CITE Phase 44 决策 1-7 + Phase 45 agents-schema.yaml 18 fields + memory-record-schema.yaml 10 fields + Phase 46 round-table-state-schema 采用 CITE-ONLY 策略, 不重定义. Net-new 内容: 75-cell elaboration + memory migration dry-run + retained-phases allowlist 数值 (Steps 0/6.5/7/10/11/12/15) + skill_fallback failure-mode 切换机制."
  - §1.3 exists: 5 SC mapping table (本 doc 章节 → ROADMAP SC#1-5) so verification can cross-check
  - §1.4 exists: roadmap placement — "本 doc 是 05-POC-PLAN.md 的 'transform + migration prerequisite' (PoC 实施 15 *.agent.yaml transform + memory migration dry-run 作为 acceptance criterion) + Phase 50 POC-PLAN consumes retained-phases allowlist for PoC run_python_phase test scope" + "Phase 44 决策 1-7 LOCKED, Phase 45 schemas LOCKED, Phase 46 protocol LOCKED, ARCHITECTURE §1.2 + §2 mapping skeleton LOCKED — this doc only elaborates + adds 3 net-new pieces (memory migration + allowlist + fallback mechanism)"
  - §1.5 exists: 4 deliverables declared upfront with load-bearing status: §1.5.1 15-expert 75-cell transform table (elaborates ARCHITECTURE §1.2 + §2); §1.5.2 default_invocation skill_fallback → mcp_tool 切换机制 (net-new failure-mode + transition path); §1.5.3 Memory schema 迁移计划 (net-new v6.0 FeedbackStore → memory-record-schema plan with schema_version + dry-run per P14); §1.5.4 Retained-phases allowlist + legacy mem0 policy (net-new Steps 0/6.5/7/10/11/12/15 + agent_id=hermes 遗留策略)
  - §1.6 exists: 15-expert quick-glance table (15 rows, 1 line each — Expert | tools | persona framing gist | refs count | related_agents count — the elevator-pitch version of §2 full deep-dive). This is ARCHITECTURE §2 verbatim copied, preserving FOUND-08.
  - §1.7 exists: explicit out-of-scope declaration — "本文档不讨论 agent YAML schema 字段定义本身 (留给 Phase 45 agents-schema.yaml), 不讨论 round table protocol 字段细节 (留给 Phase 46), 不讨论 live transform CLI 实现 (deferred to v12+ hermes agent transform CLI), 不讨论 mem0 backend 部署 (留给 Phase 48 06-CROSS-REPO-IMPACT.md Option B vs 物理分区)"
  - Total Task 1 length ≥ 250 lines
  </behavior>

  <action>
  Create file `.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md`. Write frontmatter + §0 + §1.

  For §1.1 (framing, ~70 lines): Open with "本文档定义 Phase 44 决策 2 (B3a Python runner 增量迁移 — delegate-only phase 通过 run_python_phase boundary tool 迁移到 CC) + 决策 5 (α agent form: YAML + persona + tools + refs + memory_scope + lineage) + 决策 6 (per-agent memory + curator-driven 自进化) 的可执行迁移路径 —— 把 ARCHITECTURE §1.2 已有的 SKILL→YAML 5-field mapping 表 + §2 已有的 15-expert 表 细化到 75-cell (15 expert × 5 field) 可执行粒度, 同时锁定 3 件套 net-new 贡献: (a) memory schema 迁移计划 (v6.0 FeedbackStore JSONL → Phase 45 memory-record-schema, 含 schema_version + dry-run, 解决 P14), (b) retained-phases allowlist (run_python_phase 仅接受 Steps 0/6.5/7/10/11/12/15, location = round-table-state-schema.yaml retained_python_phases field, 解决 OQ-10), (c) default_invocation: skill_fallback → mcp_tool 切换机制 (agent 优先 MCP tool, 失败回退 SKILL form, 保留 FOUND-08 backward-compat anchor), 以及旧 v7.0 mem0 agent_id=hermes memory 遗留策略 (遗留/不迁移, 解决 OQ-3)。" Cite Phase 44 §2.1 (决策 1 — T6 协议 layer, Hermes owns run_python_phase MCP tool) + §2.2 (决策 2 — B3a Python runner root, LOAD-BEARING for SC#5 retained-phases) + §2.5 (决策 5 — α form transform target) + §2.6 (决策 6 — per-agent memory load-bearing for SC#4) + §2.7 (决策 7 — Hermes controls structure, run_python_phase is Hermes-side boundary tool). Explicitly enumerate the 5 SC mapped to sections, and declare what is consumed (ARCHITECTURE §1.2 + §2 5-field mapping skeleton, Phase 45 schemas, Phase 46 protocol) vs net-new (75-cell elaboration + memory migration + allowlist values + fallback mechanism).

  For §1.2 (scope rules, ~30 lines): "本 doc 细化 ARCHITECTURE §1.2 + §2 已有 mapping 到 75-cell 可执行粒度, 不重定义 5-field mapping 框架本身. 本 doc 不讨论 (a) agent YAML schema 字段定义本身 (留给 Phase 45 agents-schema.yaml), (b) round table protocol 字段细节 (留给 Phase 46), (c) live transform CLI 实现 (deferred to v12+ hermes agent transform CLI), (d) mem0 backend 部署拓扑 (留给 Phase 48 Option B vs 物理分区). 引用 Phase 44 决策 1-7 + Phase 45 agents-schema.yaml 18 fields + memory-record-schema.yaml 10 fields + Phase 46 round-table-state-schema 采用 CITE-ONLY 策略, 不重定义."

  For §1.3 (SC mapping table, ~30 lines): 5-row table mapping ROADMAP SC#1-5 → 本 doc 章节. Columns: | SC# | 描述 | 本 doc 解决章节 | 验证脚本 |

  For §1.4 (roadmap placement, ~30 lines): "本 doc 是 05-POC-PLAN.md 的 'transform + migration prerequisite' —— PoC 实施者必须先按本 doc 的 75-cell 表完成 15 *.agent.yaml transform + 跑 memory migration dry-run 验证 P14 缓解, 然后才能开始 PoC vertical slice. Phase 50 POC-PLAN consumes retained-phases allowlist (Steps 0/6.5/7/10/11/12/15) for PoC run_python_phase test scope. 51 VALIDATE 的 lint 脚本会 cross-check 本 doc 的 15-expert transform 规则 与 ARCHITECTURE §2 一致 + 18-field schema 引用 一致." State Phase 44 决策 1-7 + Phase 45 schemas + Phase 46 protocol + ARCHITECTURE §1.2 + §2 mapping skeleton are LOCKED — this doc only elaborates + adds 3 net-new pieces.

  For §1.5 (4 deliverables declared upfront, ~60 lines): Four H3 subsections declaring upfront what this doc provides beyond existing sources:
    - §1.5.1 15-expert 75-cell transform table (cites ARCHITECTURE §1.2 + §2 verbatim): existing tables cover 9 common copy-fields + 5-field per-expert deltas. 本 doc elaborates each of 15 experts × 5 fields (tools / persona / refs / related_agents / lineage.skill_sha256) to "default + edge cases" granularity. Net-new: edge case per cell + lineage derivation (sha256 computation explicit).
    - §1.5.2 default_invocation skill_fallback → mcp_tool 切换机制 (cites ARCHITECTURE §1.1 field 18 + §1.2 SKILL body disposition): net-new failure-mode matrix (3 failure modes × 4 transition states) + safe-default-on-unknown (dispatcher sees agent with no default_invocation field → defaults to `mcp_tool` per agents-schema.yaml). FOUND-08 backward-compat preserved via expert_id anchor.
    - §1.5.3 Memory schema 迁移计划 (cites PITFALLS §P14 + v6.0 FeedbackStore source): net-new v6.0 FeedbackStore JSONL → Phase 45 memory-record-schema plan. Includes schema_version field for forward compat + dry-run mode + safe-default-on-unknown fields per P14 mitigation 2 (unknown confidentiality → confidential not public).
    - §1.5.4 Retained-phases allowlist + legacy mem0 policy (cites STACK §3.2 Tool 7 + §11.2 + SUMMARY OQ-3 + OQ-10): net-new run_python_phase allowlist = Steps 0/6.5/7/10/11/12/15 (rationale per step), location = round-table-state-schema.yaml retained_python_phases field. Plus: legacy v7.0 mem0 agent_id=hermes memory 遗留 (不迁移, read-only fallback).

  For §1.6 (15-expert quick-glance table, ~50 lines): A 15-row elevator-pitch table — copy ARCHITECTURE §2 verbatim. Columns: | Expert | tools | persona framing gist | refs count | related_agents count |. This table is FOUND-08 preservation: all 15 expert_id values unchanged. §2 full deep-dive adds default + edge cases per cell.

  For §1.7 (out-of-scope declaration, ~20 lines): Explicit list of what this doc does NOT cover: agent YAML schema field definitions (→ Phase 45 agents-schema.yaml), round table protocol field details (→ Phase 46), live transform CLI (→ v12+ hermes agent transform), mem0 backend deployment topology (→ Phase 48 06-CROSS-REPO-IMPACT.md), live run_python_phase execution (→ v11.0 PoC deferred), A2A protocol (→ post-v12+).

  Length target: ~250-300 lines for Task 1.
  </action>

  <verify>
    <automated>test -f /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 250 {exit 0} {exit 1}' && grep -qE "^## §0|^# §0|## 0\.|阅读指南" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -qE "^## §1|^# §1|## 1\.|Framing" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && for term in "15 expert\\|15-expert\\|15 个 expert" "transform" "hook_retention" "creative_source" "screenplay" "script_auditor" "character_designer" "cinematographer" "style_genome" "prompt_injector" "visual_executor" "continuity_auditor" "audio_pipeline" "editor" "colorist" "compliance_gate" "theory_critic" "default_invocation" "skill_fallback" "mcp_tool" "FeedbackStore\\|feedback_store" "schema_version" "dry-run\\|dry run" "retained" "run_python_phase" "agent_id=hermes\\|agent_id = hermes" "FOUND-08" "决策 1" "决策 2" "决策 5" "决策 6" "决策 7" "ARCHITECTURE §1.1" "ARCHITECTURE §1.2" "ARCHITECTURE §2" "ARCHITECTURE §6" "PITFALLS §P14\\|Pitfall 14" "STACK §3.2" "OQ-3" "OQ-10" "00-FIRST-PRINCIPLES"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md || { echo "MISSING: $term"; exit 1; }; done && grep -c "ARCHITECTURE §\\(1\\.1\\|1\\.2\\|2\\|6\\|6\\.1\\|6\\.4\\)" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - File created with bilingual frontmatter (status / phase / audience / reading time / stability / confidence)
  - §0 阅读指南 exists with chapter map + 3-audience guide + Phase 44/45/46 + ARCHITECTURE §1/§2/§6 + STACK §3.2 + PITFALLS §P14 consumption preamble
  - §1.1 framing cites Phase 44 决策 1/2/5/6/7 + declares 5 SC + what is consumed (ARCHITECTURE §1.2 + §2) vs net-new (75-cell + 3 件套)
  - §1.2 scope rules explicit (cite-only Phase 44/45/46 + ARCHITECTURE §1/§2/§6 + STACK §3.2 + PITFALLS §P14)
  - §1.3 has SC#1-5 mapping table
  - §1.4 has roadmap placement + downstream consumer list (05-POC-PLAN + Phase 50 + 51 VALIDATE)
  - §1.5 has 4 deliverables declared upfront (75-cell / skill_fallback / memory migration / allowlist+legacy)
  - §1.6 has 15-row quick-glance table (ARCHITECTURE §2 verbatim, FOUND-08 preserved — all 15 expert_ids present)
  - §1.7 has explicit out-of-scope declaration
  - Total doc length ≥ 250 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Write §2 (15-expert × 5-field transform 规则表 — SC#2 75-cell deep-dive elaborating ARCHITECTURE §1.2 + §2)</name>
  <files>.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.1 lines 27-52 (18-field schema — target field types + defaults) + §1.2 lines 54-71 (SKILL→YAML disposition — the 5-field mapping skeleton: 9 copy / 2 drop / 1 rewrite / 1 flatten / 1 derive / 8 new) + §1.3 lines 73-116 (screenplay minimal example — concrete transform output reference) + §2 lines 120-148 (15-expert table — verbatim source, copy this) + §6.1 lines 582-592 (transform procedure 5 steps with lineage fields)
    - .planning/research/v10-orchestrator-design/agents-schema.yaml lines 1-300 (18 fields authoritative definition — cite by field name) esp. `default_invocation` enum (mcp_tool|skill_fallback|disabled, default mcp_tool) + `lineage` sub-fields (derived_from_skill_id, derived_from_repo, transform_date, transform_notes, skill_sha256) + `memory_scope` enum (shared|per_agent|project_scoped)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.5 (决策 5 α form — transform target schema root argument)
    - .planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md §3 lineage chain (Phase 48 forward/backward/drift chain — complement, do NOT re-derive; this doc focuses on the per-expert transform rules)
    - /data/workspace/kais-hermes-skills/skills/movie-experts/screenplay/SKILL.md (if accessible — concrete source for screenplay transform example; otherwise cite ARCHITECTURE §1.3 screenplay minimal example verbatim)
  </read_first>

  <behavior>
  - §2.0 子节存在: 声明 §2 是 SC#2 的完整论证 —— 15 expert × 5 field = 75-cell transform 规则表, source = ARCHITECTURE §2 15-expert table verbatim (FOUND-08 preserved). 5 fields are: (1) tools, (2) persona, (3) refs, (4) related_agents, (5) lineage.skill_sha256 — 选这 5 个因为它们是 ARCHITECTURE §1.2 中的 DERIVE/REWRITE/FLATTEN/COPY-with-rename/NEW 类字段 (其他 9 个字段是简单 COPY, 无需 per-expert 规则).
  - §2.1 子节存在: 5-field mapping 通则 (一张表解释每个 field 的 default rule + edge case rule 模板 —— 通则先, 然后 §2.2-§2.16 给 per-expert 特例). 列出 5 fields + 每个的 default + edge case pattern.
  - §2.2-§2.16 子节存在 (15 个): per-expert transform rules. 每个专家一节 (§2.2 hook_retention, §2.3 creative_source, §2.4 screenplay, §2.5 script_auditor, §2.6 character_designer, §2.7 cinematographer, §2.8 style_genome, §2.9 prompt_injector, §2.10 visual_executor, §2.11 continuity_auditor, §2.12 audio_pipeline, §2.13 editor, §2.14 colorist, §2.15 compliance_gate, §2.16 theory_critic). 每节包含: (a) 5-field table (tools / persona / refs / related_agents / lineage.skill_sha256) with default + edge cases per cell, (b) expert-specific edge case (e.g. screenplay's HOOK-09 emotion_curve marker contract is load-bearing — must surface in persona + lineage.transform_notes), (c) one concrete persona excerpt (3-5 lines first-person expert identity, bilingual per CLAUDE.md).
  - §2.17 子节存在: 聚合统计 + 5-field coverage audit (15 × 5 = 75 cells, 列出每个 field 的 15-expert 分布, 例如 memory_scope: 15/15 default per_agent; default_invocation: 13/15 default mcp_tool, 2/15 (compliance_gate + theory_critic) special handling because hard-gate / soft-gate authority)
  - §2.18 子节存在: FOUND-08 preservation 声明 + transform additive 声明 —— 15 expert_id 全部 copy verbatim; transform is ADDITIVE (consumer 仍可通过 expert_id 调用 SKILL fallback per default_invocation: skill_fallback). Cite ARCHITECTURE §2 closing paragraph + §1.2 backward-compat anchor.
  - §2.19 子节存在: 5-field edge case 总览表 (跨 15 expert 的 edge case 汇总, 例如: visual_executor 的 tools 包含 dreamina_cli 而 其他 14 expert 不包含; audio_pipeline 的 tools 包含 dreamina_cli (TTS path); cinematographer 的 refs count = 7 (含 Phase 17 absorbed scene_builder); compliance_gate 的 default_invocation 特殊处理因为 hard-gate authority)
  - Total Task 2 length ≥ 400 lines
  </behavior>

  <action>
  Append §2 to `.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md`.

  **§2 — 15-Expert × 5-Field Transform 规则表 (SC#2 75-Cell Deep-Dive)**

  §2.0 (~25 lines): "本节是 ROADMAP SC#2 的完整论证. ARCHITECTURE §1.2 已给 SKILL→YAML 5-field mapping 通则 (9 copy / 2 drop / 1 rewrite / 1 flatten / 1 derive / 8 new) + §2 已给 15-expert 表 (per-expert deltas for tools / persona / refs / related_agents). 本节把这两张表合并 + 细化到 75-cell (15 expert × 5 field) 可执行粒度 —— 当 v11.0 PoC 实施者问 'screenplay 的 transform 规则是什么? edge case 有哪些?', 本节回答. 5 fields 选择: (1) tools (DERIVE 类), (2) persona (REWRITE 类), (3) refs (FLATTEN 类), (4) related_agents (COPY-with-rename 类), (5) lineage.skill_sha256 (NEW 类). 其他 9 个 fields (name, description, version, platforms, tags, expert_id, metrics, prerequisites, memory_scope/default_invocation/evolution_log/fitness_score/round_table_eligible) 是简单 COPY 或统一 default, 无需 per-expert 规则."

  §2.1 5-field mapping 通则 (~60 lines):
  | Field | Class | Default rule | Edge case pattern |
  |-------|-------|--------------|-------------------|
  | tools | DERIVE | `[hermes_llm, read_file, search_files]` + expert-specific write/exec tools | If SKILL `prerequisites.tools` lists `dreamina_cli` or `comfyui`, include in agent tools; else analysis-only |
  | persona | REWRITE | First-person expert identity; cite 1-2 anchor refs; defer to peers on their domains; NEVER generate full output unprompted | If SKILL body has load-bearing marker contract (e.g. screenplay HOOK-09), surface in persona + lineage.transform_notes |
  | refs | FLATTEN | SKILL `## References` table → repo-relative path list; default 2-7 refs | If SKILL has 0 refs, agent persona-only (rare); if >7 refs, dedupe by topic |
  | related_agents | COPY-with-rename | SKILL `metadata.hermes.related_skills` → `related_agents` verbatim | If SKILL has 0 related_skills, agent is solo (no round table peers); if SKILL references deprecated name, map via FOUND-08 |
  | lineage.skill_sha256 | NEW | `hashlib.sha256(source_skill_md_content.encode("utf-8")).hexdigest()` (encoding explicit per CLAUDE.md PLW1514) | If source SKILL.md has CRLF line endings, normalize to LF before hashing (or document normalization in transform_notes) |

  §2.2-§2.16 15 per-expert transform rules (~15-25 lines each = ~250-375 lines):
  For EACH of the 15 experts (use exactly the order + names from ARCHITECTURE §2: hook_retention, creative_source, screenplay, script_auditor, character_designer, cinematographer, style_genome, prompt_injector, visual_executor, continuity_auditor, audio_pipeline, editor, colorist, compliance_gate, theory_critic), write:

  **§2.X `<expert_name>`** (per-expert section):
  - **5-field table** (mirrors §2.1 columns, expert-specific values from ARCHITECTURE §2 row):
    | Field | Default | Edge case |
    |-------|---------|-----------|
    | tools | (from ARCHITECTURE §2 row) | (expert-specific edge, e.g. screenplay's `write_file, patch` for script revision) |
    | persona | (first-person framing from ARCHITECTURE §2 row, expanded 3-5 lines) | (expert-specific contract, e.g. cinematographer does NOT execute motion = visual_executor's job) |
    | refs | (count from ARCHITECTURE §2 row, list 2-3 examples) | (expert-specific, e.g. cinematographer 7 refs includes Phase 17 absorbed scene_builder) |
    | related_agents | (count from ARCHITECTURE §2 row) | (expert-specific, e.g. screenplay 9 related_agents per v86-pipeline-mapping.md DAG) |
    | lineage.skill_sha256 | default sha256 computation | (expert-specific transform_notes, e.g. screenplay: "HOOK-09 emotion_curve marker arrays remain contract-load-bearing") |
  - **Expert-specific edge case** (1 paragraph, ~5-8 lines): the one notable edge case that makes this expert different from the default. Examples:
    - hook_retention: 3-second hook contract → persona must surface 5 hook types + 5 爆款公式 as first-class content
    - creative_source: outputs StoryKernel JSON scaffold → tools includes write_file
    - screenplay: HOOK-09 emotion_curve marker contract load-bearing → lineage.transform_notes must preserve
    - script_auditor: 5-dim critic NOT creative writer → persona framing critical not generative
    - character_designer: L1-L4 asset library specs → tools includes write_file
    - cinematographer: Phase 17 absorbed scene_builder → refs count 7 includes absorbed domain
    - style_genome: 5D style vector → Cross-Module Alignment metric preserved
    - prompt_injector: NEW AI-native (no SKILL precedent pre-v3.0) → lineage.derived_from_skill_id may be null or special "ai-native" marker
    - visual_executor: dreamina CLI executor → tools includes `dreamina_cli, patch` for sub_step iteration
    - continuity_auditor: 4-dim (face_identity / wardrobe_figure / color_temperature / scene_environment) → hard-gate on 4-dim fail
    - audio_pipeline: 6 sub_steps (voicer, lip_sync, composer, foley, mixer, spatial_audio) → tools includes dreamina_cli (TTS path)
    - editor: cut_density metric → defers to cinematographer on intent
    - colorist: CxSxZ color narrative + LUT plan → integrates with visual_executor at Step 7
    - compliance_gate: **hard-gate authority** — can block pipeline progression → default_invocation may be `disabled` initially (operator unlocks after policy review)
    - theory_critic: **soft-gate only** (advisory) → default_invocation standard mcp_tool
  - **One concrete persona excerpt** (3-5 lines first-person, bilingual): show the actual persona text fragment for this expert.

  §2.17 聚合统计 + 5-field coverage audit (~40 lines):
  - Aggregate stats: 15 agents total. tools distribution (analysis-only = 11, includes dreamina_cli = 2 (visual_executor, audio_pipeline), includes write_file/patch = 5). persona all first-person. refs avg 3.5 (range 2-7). related_agents avg 5.6 (range 4-9). All 15 memory_scope default = per_agent. 13/15 default_invocation = mcp_tool; 2/15 (compliance_gate, theory_critic) need special handling per §2.19.
  - 5-field coverage audit: confirm 75 cells (15 × 5) all populated. Cross-check with ARCHITECTURE §2 row count.

  §2.18 FOUND-08 preservation 声明 (~30 lines):
  - **Cite ARCHITECTURE §2 closing paragraph verbatim:** "All 15 expert_id values are copied verbatim. The transition is additive — consumers can still call skills by expert_id and the dispatcher falls through to SKILL when default_invocation: skill_fallback is set."
  - **Additive invariant:** Transform NEVER removes a SKILL. SKILLs remain at kais-hermes-skills repo as fallback source. Agent YAMLs at `~/.hermes/agents/` are siblings (per ARCHITECTURE §7.1 Sibling Registry pattern).
  - **Backward-compat anchor:** `expert_id` field in agent YAML (ARCHITECTURE §1.1 field 10) preserves the v1-v9 caller convention. Legacy caller `expert_id: screenplay` still resolves (dispatcher falls through to agent or SKILL fallback per default_invocation).

  §2.19 5-field edge case 总览表 (~40 lines):
  Cross-15-expert edge case summary table. Rows: 5 fields. Columns: | Field | # experts with default | # experts with edge case | Edge case experts + 1-line description |. Example: tools | 11 default | 4 edge | visual_executor+audio_pipeline dreamina_cli; screenplay+character_designer+colorist write_file/patch; prompt_injector write_file.

  Length target: ~400-500 lines for Task 2.
  </action>

  <verify>
    <automated>grep -qE "^## §2|^## 2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -cE "^### §2\.|^### 2\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 18 {exit 0} {exit 1}' && for expert in hook_retention creative_source screenplay script_auditor character_designer cinematographer style_genome prompt_injector visual_executor continuity_auditor audio_pipeline editor colorist compliance_gate theory_critic; do grep -q "$expert" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md || { echo "MISSING expert: $expert"; exit 1; }; done && for term in "tools" "persona" "refs" "related_agents" "skill_sha256\\|sha256" "5-field\\|5 field\\|五 field" "edge case\\|特例" "FOUND-08" "additive" "expert_id" "transform_notes" "dreamina_cli" "HOOK-09" "hard-gate" "soft-gate" "第一人称\\|first-person\\|first person" "memory_scope" "per_agent" "ARCHITECTURE §1.1" "ARCHITECTURE §1.2" "ARCHITECTURE §2" "决策 5" "Phase 45"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md || { echo "MISSING: $term"; exit 1; }; done && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 650 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §2 exists with §2.0-§2.19 (5-field mapping 通则 + 15 per-expert sections + 聚合统计 + FOUND-08 声明 + edge case 总览)
  - §2.0 declares 5-field selection rationale (tools/persona/refs/related_agents/lineage.skill_sha256)
  - §2.1 has 5-field default + edge case pattern table
  - §2.2-§2.16 has 15 per-expert sections, each with 5-field table + edge case + persona excerpt
  - §2.17 has aggregate stats + 75-cell coverage audit
  - §2.18 cites ARCHITECTURE §2 FOUND-08 closing + declares additive invariant
  - §2.19 has cross-15-expert edge case summary
  - All 15 experts (hook_retention through theory_critic) appear in their own §2.X section
  - Total doc length now ≥ 650 lines (cumulative with Task 1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §3 (default_invocation skill_fallback → mcp_tool 切换机制 — SC#3 deep-dive, failure-mode matrix + transition path)</name>
  <files>.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §1.1 line 50 (field 18 default_invocation enum: mcp_tool | skill_fallback | disabled, default mcp_tool) + §1.2 lines 54-71 (SKILL body disposition — "SKILL body (markdown) NOT copied. Becomes input to persona rewrite. The original SKILL body is preserved in the source repo as lineage.derived_from_skill_id reference.") + §1.3 line 115 (screenplay example default_invocation: mcp_tool) + §6.4 row 2 (kais-hermes-skills repo: "SKILLs remain as fallback per default_invocation: skill_fallback")
    - .planning/research/v10-orchestrator-design/agents-schema.yaml lines 273-285 (default_invocation authoritative definition — enum values + default + description)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §4.2 (MCP naming reconciliation — 15-expert 从 skill_fallback 切换到 mcp_tool 使用 STACK-form tool 名) + §5 (7 MCP tools — get_agent_opinion etc. — consumer of default_invocation)
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 (7 MCP tools surface — when default_invocation=mcp_tool, dispatcher uses these; when skill_fallback, falls through to SKILL body)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1 (决策 1 T6 协议 — Hermes owns MCP tool surface) + §2.5 (决策 5 α form — agent invocation via dispatcher, not skill body injection)
  </read_first>

  <behavior>
  - §3.0 子节存在: 声明 §3 是 SC#3 的完整论证 —— default_invocation 三态切换 (mcp_tool / skill_fallback / disabled) 的 failure-mode matrix + transition path + safe-default-on-unknown
  - §3.1 子节存在: 三态语义定义 (cite agents-schema.yaml authoritative enum) —— mcp_tool (agent invoked via get_agent_opinion MCP tool, persona as system-prompt fragment), skill_fallback (fall through to underlying SKILL body injection, v1-v9 behavior), disabled (agent exists in registry but cannot be invoked — transform-in-progress or operator-locked)
  - §3.2 子节存在: failure-mode matrix —— 3 failure modes (MCP tool unavailable / agent persona missing / agent not in registry) × 4 transition states (mcp_tool active / skill_fallback active / disabled / unknown — no field) = 12-cell matrix, each cell specifying dispatcher behavior
  - §3.3 子节存在: safe-default-on-unknown —— dispatcher 看到 agent YAML 没有 default_invocation field 时, default to mcp_tool (per agents-schema.yaml default); 看到 SKILL 没有 agent sibling 时, default to skill_fallback (v1-v9 behavior preserved). 两个 default 保证 v11.0 PoC 之前 Hermes 在没有 agent registry 时仍能跑 (skill-only mode), 之后能平滑切换到 agent mode.
  - §3.4 子节存在: skill_fallback → mcp_tool transition path —— operator 手动 transform 完一个 SKILL 后, 把 ~/.hermes/agents/{name}.agent.yaml 的 default_invocation 从 skill_fallback (或缺失) 切到 mcp_tool. Cite ARCHITECTURE §6.4 v11.0 PoC deliverable list ("15 *.agent.yaml files via manual transform"). Transition is per-agent, not all-at-once.
  - §3.5 子节存在: FOUND-08 backward-compat anchor —— expert_id 字段保证 v1-v9 caller 调用 expert_id=screenplay 仍能 resolve. 路由顺序: dispatcher 先查 agent registry (by name or expert_id), 若 default_invocation=mcp_tool 则调用 agent; 若 skill_fallback 则 fall through 到 SKILL body; 若 agent 不存在则默认 SKILL fallback. Cite ARCHITECTURE §1.1 field 10 expert_id + §1.2 SKILL body disposition.
  - §3.6 子节存在: 13/15 expert 切换 mcp_tool, 2/15 特殊处理 —— compliance_gate + theory_critic 因为 hard-gate / soft-gate authority 不同, 切换路径有差异. compliance_gate 建议 initial disabled (operator unlocks after policy review); theory_critic 直接 mcp_tool (soft-gate advisory).
  - §3.7 子节存在: round table 消费 default_invocation —— get_agent_opinion MCP tool 检查 panelist agent 的 default_invocation, 仅 mcp_tool 状态的 agent 能加入 round table; skill_fallback + disabled 不能加入. Cite Phase 46 round_table_eligible field (agents-schema.yaml field 17) 协同.
  - Total Task 3 length ≥ 200 lines
  </behavior>

  <action>
  Append §3 to `.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md`.

  **§3 — default_invocation skill_fallback → mcp_tool 切换机制 (SC#3 Deep-Dive)**

  §3.0 (~25 lines): "本节是 ROADMAP SC#3 的完整论证. agents-schema.yaml field 18 已定义 default_invocation enum (mcp_tool | skill_fallback | disabled, default mcp_tool), ARCHITECTURE §6.4 已声明 'SKILLs remain as fallback per default_invocation: skill_fallback'. 本节的贡献是把三态语义细化到 failure-mode matrix + transition path + safe-default-on-unknown 的可执行粒度 —— 当 v11.0 PoC 实施者问 'MCP tool 失败时怎么办? 没有 default_invocation field 时 default 是什么? 15 个 expert 哪些先切?', 本节回答."

  §3.1 三态语义定义 (~40 lines):
  - **Cite agents-schema.yaml field 18 verbatim** (do NOT redefine enum).
  - **mcp_tool (default):** Agent invoked via `get_agent_opinion(name, ...)` MCP tool (STACK §3.2 Tool 2). Persona injected as system-prompt fragment. Output captured via `submit_round_table_result`. Round-table eligible.
  - **skill_fallback:** Fall through to underlying SKILL body injection (v1-v9 behavior). SKILL body injected as user message (not system prompt). Output captured via existing skill output channel. NOT round-table eligible (SKILL has no `round_table_eligible` flag, default false for safety).
  - **disabled:** Agent exists in `~/.hermes/agents/` registry but dispatcher rejects invocation. Used for transform-in-progress (persona not finalized) or operator-locked (compliance_gate pending policy review). Round-table NOT eligible.

  §3.2 failure-mode matrix (~60 lines): 12-cell matrix (3 failure modes × 4 transition states).
  - **Failure modes:** (a) MCP tool runtime unavailable (mcp_serve.py down / tool dispatch error), (b) Agent persona missing or malformed (YAML parse error / persona field empty), (c) Agent not in registry (no `~/.hermes/agents/{name}.agent.yaml` for requested name/expert_id).
  - **Transition states:** (1) mcp_tool active, (2) skill_fallback active, (3) disabled, (4) unknown (no default_invocation field in YAML).
  - **Matrix cells (example):** Failure (a) MCP tool unavailable × state (1) mcp_tool active = dispatcher retries per tenacity backoff, then returns error to CC (does NOT auto-fall to skill_fallback — would change output semantics); Failure (c) Agent not in registry × any state = always fall to SKILL fallback if SKILL exists, else error.

  §3.3 safe-default-on-unknown (~40 lines):
  - **Agent YAML missing default_invocation:** dispatcher defaults to `mcp_tool` (per agents-schema.yaml default). Rationale: forward-compat — v11.0+ agents are agent-first.
  - **SKILL missing agent sibling:** dispatcher defaults to `skill_fallback` (v1-v9 behavior preserved). Rationale: backward-compat — v1-v9 SKILLs continue working without transform.
  - **Both exist (agent + SKILL) but ambiguous:** agent wins (agent registry is source of truth per ARCHITECTURE §4.1 sibling registry pattern). Operator can force SKILL fallback via agent `default_invocation: skill_fallback`.

  §3.4 skill_fallback → mcp_tool transition path (~40 lines):
  - **Per-agent, not all-at-once:** operator transforms one SKILL → creates `~/.hermes/agents/{name}.agent.yaml` with `default_invocation: mcp_tool` (or `disabled` first, then unlock after verification). Other 14 SKILLs continue as skill_fallback.
  - **Transition steps:** (1) Run §2.X transform rules for this expert → produce agent YAML, (2) Set `default_invocation: disabled` initially, (3) Verify persona output via smoke test, (4) Switch to `default_invocation: mcp_tool`, (5) Update round_table_eligible to true if desired.
  - **Cite ARCHITECTURE §6.4 v11.0 PoC deliverable:** 15 *.agent.yaml via manual transform — transition is manual, one expert at a time, ~5-10 minutes per expert.

  §3.5 FOUND-08 backward-compat anchor (~40 lines):
  - **expert_id field (agents-schema.yaml field 10):** preserves v1-v9 caller convention. Legacy caller `invoke expert_id=screenplay` still resolves.
  - **Dispatcher routing order:** (1) Check agent registry by `name` or `expert_id`; (2) If agent found + default_invocation=mcp_tool → invoke agent via MCP tool; (3) If agent found + default_invocation=skill_fallback → fall through to SKILL body; (4) If agent not found → default SKILL fallback (v1-v9 behavior).
  - **Cite ARCHITECTURE §1.1 field 10 + §1.2 SKILL body disposition:** expert_id is the bridge between old (skill) and new (agent) worlds.

  §3.6 13/15 expert 切换 mcp_tool, 2/15 特殊处理 (~40 lines):
  - **13/15 standard transition (mcp_tool):** hook_retention, creative_source, screenplay, script_auditor, character_designer, cinematographer, style_genome, prompt_injector, visual_executor, continuity_auditor, audio_pipeline, editor, colorist, theory_critic — all follow §3.4 standard transition path.
  - **compliance_gate special handling:** Recommend `disabled` initially (operator unlocks after policy review). Rationale: hard-gate authority — operator must verify gate logic before agent invocation can block pipeline progression.
  - **theory_critic direct mcp_tool:** soft-gate only (advisory), no blocking authority — safe to switch directly. No special handling.

  §3.7 round table 消费 default_invocation (~30 lines):
  - **Cite Phase 46 round_table_eligible field (agents-schema.yaml field 17):**协同.
  - **get_agent_opinion MCP tool** checks panelist agent's default_invocation. Only `mcp_tool` state agents can join round tables. `skill_fallback` + `disabled` cannot (SKILL body injection has no round-table turn lifecycle).
  - **Rationale:** Round table requires system-prompt-fragment persona (mcp_tool mode), not user-message body (skill_fallback mode). Output capture via submit_round_table_result only works in mcp_tool mode.

  Length target: ~200-250 lines for Task 3.
  </action>

  <verify>
    <automated>grep -qE "^## §3|^## 3\." /data/workspace/hermes-agent/.planning/v10-orchestrator-design/04-MIGRATION-PATH.md 2>/dev/null || grep -qE "^## §3|^## 3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -cE "^### §3\.|^### 3\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 7 {exit 0} {exit 1}' && for term in "default_invocation" "skill_fallback" "mcp_tool" "disabled" "failure.mode\\|failure mode\\|故障" "transition\\|切换" "safe-default\\|safe default\\|默认" "expert_id" "FOUND-08" "round_table_eligible" "get_agent_opinion" "compliance_gate" "theory_critic" "13/15\\|13 \\/ 15\\|13 of 15" "agents-schema" "ARCHITECTURE §1.1" "ARCHITECTURE §6.4" "决策 1" "Phase 46" "STACK §3.2"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md || { echo "MISSING: $term"; exit 1; }; done && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 850 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §3 exists with §3.0-§3.7 (default_invocation 切换机制 SC#3 deep-dive, 8 subsections)
  - §3.1 cites agents-schema.yaml field 18 verbatim (mcp_tool | skill_fallback | disabled)
  - §3.2 has 12-cell failure-mode matrix (3 failure modes × 4 transition states)
  - §3.3 declares safe-default-on-unknown (missing field → mcp_tool default; missing agent → skill_fallback default)
  - §3.4 has per-agent transition path (5 steps: transform → disabled → verify → mcp_tool → round_table_eligible)
  - §3.5 cites FOUND-08 + expert_id backward-compat anchor + dispatcher routing order
  - §3.6 declares 13/15 standard + 2/15 special (compliance_gate disabled, theory_critic direct)
  - §3.7 cites Phase 46 round_table_eligible + get_agent_opinion MCP tool consumption
  - Total doc length now ≥ 850 lines (cumulative with Tasks 1+2)
  </done>
</task>

<task type="auto">
  <name>Task 4: Write §4 (Memory schema 迁移计划 — SC#4 deep-dive, v6.0 FeedbackStore → memory-record-schema + schema_version + dry-run, prevents P14)</name>
  <files>.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/PITFALLS.md §P14 lines 438-454 (Schema Migration Breaks Memory Store — 3 mitigations: schema_version + safe defaults + dry-run)
    - .planning/research/v10-orchestrator-design/memory-record-schema.yaml lines 1-100 (Layer 2 schema — record_id, agent_id required, scope enum, project_id, session_id, status, confidence, half_life_days, expires_at, verified_at, supersedes_memory_id, evidence_chain, evidence_operator_ids, confidentiality, created_at, last_recalled_at, recall_count, schema_version line 353, content) + lines 350-380 (schema_version field spec — cite, do NOT redefine)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §3.4 lines 225-285 (Curator-Driven Memory Evolution — `_memory_evolution_phase` writes records conforming to memory-record-schema) + §3.1 lines 154-172 (Current Mem0 Backend Surface — `_read_filters()` returns user_id only)
    - /data/workspace/hermes-agent/agent/feedback_store.py lines 1-30 (module docstring — v6.0 FeedbackStore persistence: buckets/<skill_id>/<source>.jsonl + dedup/sha256-registry.jsonl + index.json) + line 63 (_INDEX_VERSION = 1) + lines 196-300 (class FeedbackStore structure)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.6 (决策 6 per-agent memory — root argument for why migration matters)
    - .planning/research/v10-orchestrator-design/SUMMARY.md OQ-3 (line 69 — v7.0 mem0 agent_id=hermes legacy)
  </read_first>

  <behavior>
  - §4.0 子节存在: 声明 §4 是 SC#4 的完整论证 + PITFALLS §P14 的 mitigation 落实 —— v6.0 FeedbackStore JSONL → Phase 45 memory-record-schema 迁移计划, 含 schema_version + dry-run + safe-default-on-unknown-field
  - §4.1 子节存在: 迁移 source schema 引用 (v6.0 FeedbackStore ground truth — read agent/feedback_store.py) —— persistence: buckets/<skill_id>/<source>.jsonl (append-only FeedbackRecord per line) + dedup/sha256-registry.jsonl (audit log) + index.json (versioned _INDEX_VERSION = 1). FeedbackRecord fields: skill_id, source, skill_sha256, feedback_text, verdict, operator_id, created_at. CITE-ONLY, 不重定义.
  - §4.2 子节存在: 迁移 target schema 引用 (Phase 45 memory-record-schema.yaml — 10 mandated fields + schema_version). Layer 2 schema, independent of agents-schema.yaml. CITE-ONLY, 不重定义. 列出 10 fields 的 source-side 映射 (哪些 FeedbackRecord 字段映射到哪些 memory-record 字段, 哪些是 net-new 必须填 default).
  - §4.3 子节存在: 字段映射表 (source → target) —— 7-row 映射表: skill_id → (no direct mapping, becomes agent_id namespace component), source → (no direct mapping, becomes evidence_chain[0].source_type), skill_sha256 → evidence_chain[0].evidence_sha256, feedback_text → content, verdict → status (positive=active, negative=quarantined), operator_id → evidence_operator_ids[0], created_at → created_at. Plus net-new fields default values: confidence=0.5 (OQ-4), half_life_days=180 (default), expires_at=created_at + 180d, verified_at=created_at, supersedes_memory_id=null, scope=project (default), project_id=(from bucket path), session_id=null, confidentiality=confidential (P14 mitigation 2 — safest default not most permissive).
  - §4.4 子节存在: schema_version 字段引用 + forward-compat 语义 (cite memory-record-schema.yaml line 353) —— schema_version: int on every record. Read path tolerates older versions (v1 records readable by v2 reader); migration job backfills (v1 → v2 transform). v10.0 baseline schema_version = 1.
  - §4.5 子节存在: dry-run migration mode (P14 mitigation 3) —— `hermes agent memory migrate --dry-run` 命令语义. Print plan: (a) total source records count, (b) per-target-field default fill rate (e.g. "confidence: 100% default (0.5) — no source equivalent"), (c) conflict count (records that would be quarantined due to source verdict=negative), (d) estimated target storage. NO write occurs in dry-run.
  - §4.6 子节存在: safe-default-on-unknown-field (P14 mitigation 2) —— unknown confidentiality → confidential (not public); unknown scope → project (not global); unknown status → quarantined (not active). 所有 unknown 字段 default to SAFEST value (most restrictive). 列出 5+ safe-default 规则.
  - §4.7 子节存在: migration 执行步骤 (6 steps) —— (1) Backup source FeedbackStore (cp buckets/ to buckets.pre-migration.bak/), (2) Dry-run migration (print plan + audit), (3) Operator approval gate, (4) Live migration (write target records + retain source), (5) Shadow run (30-day dual-read — compare v1 source vs v2 target query results), (6) Decommission source after shadow passes.
  - §4.8 子节存在: rollback path —— if shadow run reveals discrepancy (e.g. agent retrieves different results from v1 vs v2), operator can roll back: (a) delete v2 target records, (b) retain v1 source (never deleted during migration), (c) investigate mapping error, (d) re-run dry-run with corrected mapping.
  - §4.9 子节存在: P14 风险评估 + resolution 声明 —— P14 (Schema Migration Breaks Memory Store) mitigated by §4.4 schema_version + §4.5 dry-run + §4.6 safe-default. v11.0 PoC acceptance criterion: dry-run migration must run clean + shadow-run 30-day window must show <1% retrieval discrepancy.
  - Total Task 4 length ≥ 250 lines
  </behavior>

  <action>
  Append §4 to `.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md`.

  **§4 — Memory Schema 迁移计划 (SC#4 Deep-Dive, P14 Mitigation)**

  §4.0 (~25 lines): "本节是 ROADMAP SC#4 的完整论证 + PITFALLS §P14 的 mitigation 落实. v6.0 ship 的 FeedbackStore (agent/feedback_store.py) 是 source schema; Phase 45 memory-record-schema.yaml 是 target schema. 本节给出字段映射 + schema_version forward-compat + dry-run migration mode + safe-default-on-unknown-field + 6 步执行计划 + rollback path —— 全部是为了防止 P14 (schema migration 静默 drop 或 unsafe default 污染 memory store)."

  §4.1 迁移 source schema 引用 (~40 lines):
  - **Cite agent/feedback_store.py ground truth** (do NOT redefine):
    - **Persistence:** `buckets/<skill_id>/<source>.jsonl` (append-only, one FeedbackRecord per line, encoding="utf-8" per CLAUDE.md PLW1514) + `dedup/sha256-registry.jsonl` (audit log of supersession events) + `index.json` (versioned via `_INDEX_VERSION = 1`).
    - **FeedbackRecord fields:** `skill_id` (string, e.g. "screenplay"), `source` (string, e.g. "operator" / "auto_eval"), `skill_sha256` (content hash of SKILL.md at feedback time), `feedback_text` (the feedback content), `verdict` (positive / negative / neutral), `operator_id` (who gave the feedback), `created_at` (ISO timestamp).
  - **Note:** v6.0 FeedbackStore has _INDEX_VERSION = 1 for index.json only, NOT for record schema. This is the migration gap P14 warns about.

  §4.2 迁移 target schema 引用 (~40 lines):
  - **Cite Phase 45 memory-record-schema.yaml verbatim** (do NOT redefine):
    - **Layer 2 schema** (independent of agents-schema.yaml per CC-2 mandate).
    - **10 mandated fields (PITFALLS §P1/§P2/§P4/§P5/§P6/§P10/§P14 mitigations):** record_id, agent_id (required), scope (global|project|session), status (active|archived|quarantined), confidence, half_life_days, expires_at, verified_at, supersedes_memory_id, evidence_chain, evidence_operator_ids, confidentiality, schema_version, content.
  - **Target schema is richer than source** — many fields have no source-side equivalent and must be filled with safe defaults (§4.6).

  §4.3 字段映射表 (source → target, ~50 lines):
  | Source FeedbackRecord field | Target memory-record field | Mapping rule |
  |-----------------------------|----------------------------|--------------|
  | skill_id | agent_id (namespace component) | `agent_id = "agent:{skill_id}"` per ARCHITECTURE §3.2 Option B convention |
  | source | evidence_chain[0].source_type | Preserve as evidence provenance |
  | skill_sha256 | evidence_chain[0].evidence_sha256 | Content hash of source evidence |
  | feedback_text | content | Verbatim copy |
  | verdict | status | positive→active, negative→quarantined, neutral→active |
  | operator_id | evidence_operator_ids[0] | Single-element list |
  | created_at | created_at | Verbatim copy |
  | (no source) | confidence | Default 0.5 (OQ-4 neutral) |
  | (no source) | half_life_days | Default 180 |
  | (no source) | expires_at | Default created_at + 180 days |
  | (no source) | verified_at | Default created_at |
  | (no source) | supersedes_memory_id | Default null |
  | (no source) | scope | Default "project" |
  | (no source) | project_id | From bucket path (if encoded) or default "unknown" |
  | (no source) | session_id | Default null |
  | (no source) | confidentiality | Default "confidential" (P14 mitigation 2 — safest not most permissive) |
  | (no source) | schema_version | Default 1 (v10.0 baseline) |
  | (no source) | record_id | Generated uuid4 |

  §4.4 schema_version 字段引用 (~30 lines):
  - **Cite memory-record-schema.yaml line 353 verbatim:** `schema_version: int` on every record.
  - **Forward-compat semantics:** Read path tolerates older versions (v1 records readable by v2 reader with fallback logic); migration job backfills (v1 → v2 transform with audit log).
  - **v10.0 baseline:** schema_version = 1 (matches FeedbackStore _INDEX_VERSION = 1 for alignment).
  - **Future bumps:** v11+ may add fields (e.g. `model_id` for model isolation per PITFALLS §P8) — schema_version = 2 + migration job.

  §4.5 dry-run migration mode (~50 lines):
  - **P14 mitigation 3 verbatim:** `hermes agent memory migrate --dry-run` shows what would change before applying.
  - **Dry-run output plan:**
    - (a) Total source records count (e.g. "1,247 FeedbackRecord lines across 15 buckets/").
    - (b) Per-target-field default fill rate (e.g. "confidence: 100% default (0.5) — no source equivalent"; "content: 100% source-derived from feedback_text").
    - (c) Conflict count (records that would be quarantined due to source verdict=negative — e.g. "47 records (3.8%) → status=quarantined").
    - (d) Estimated target storage (e.g. "~2.4MB after migration, 1.8x source size due to added fields").
    - (e) Mapping warnings (e.g. "12 records have bucket path without project_id — defaulting to 'unknown'").
  - **NO write occurs in dry-run.** Output is human-readable + machine-parseable (JSON summary).

  §4.6 safe-default-on-unknown-field (~40 lines):
  - **P14 mitigation 2 verbatim:** default unknown fields to the SAFEST value, not the most permissive.
  - **Safe-default rules:**
    | Unknown field | Safe default | Rationale |
    |---------------|--------------|-----------|
    | confidentiality | "confidential" | P14 mitigation 2 — never leak via default-public |
    | scope | "project" | Tightest scope; can be promoted to global later via curator |
    | status | "quarantined" | P14 mitigation 2 — never auto-activate unverified records |
    | confidence | 0.5 (neutral) | OQ-4 — neutral until curator evaluates |
    | half_life_days | 180 | Conservative — expire sooner rather than later |
    | expires_at | created_at + 180d | Conservative — shorter half-life preferred |

  §4.7 migration 执行步骤 (6 steps, ~50 lines):
  - **Step 1 — Backup source:** `cp -r buckets/ buckets.pre-migration.bak/` (filesystem-level snapshot). Verify backup integrity.
  - **Step 2 — Dry-run migration:** Run `hermes agent memory migrate --dry-run` → audit output plan.
  - **Step 3 — Operator approval gate:** Human checkpoint — operator reviews dry-run output, approves migration.
  - **Step 4 — Live migration:** Write target records to mem0 backend (per Phase 45 memory-record-schema) + retain source FeedbackStore unchanged.
  - **Step 5 — Shadow run (30-day dual-read):** Curator + dispatcher read from BOTH source and target for 30 days. Compare query results. Log discrepancies.
  - **Step 6 — Decommission source:** After 30-day shadow passes (<1% retrieval discrepancy), archive source FeedbackStore (read-only) + mark v1 schema deprecated.
  - **Estimated effort:** ~2-3 person-days (dry-run script + migration script + shadow-run comparator).

  §4.8 rollback path (~30 lines):
  - **If shadow run reveals >1% discrepancy:** operator rolls back.
  - **Rollback steps:** (a) Delete v2 target records from mem0, (b) Retain v1 source (NEVER deleted during migration), (c) Investigate mapping error (most likely: verdict→status mapping edge case or skill_id→agent_id namespace collision), (d) Re-run dry-run with corrected mapping.
  - **Rollback safety:** Source FeedbackStore is append-only and never mutated by migration — rollback is always safe.

  §4.9 P14 风险评估 + resolution 声明 (~30 lines):
  - **P14 (Schema Migration Breaks Memory Store):** mitigated by §4.4 (schema_version forward-compat) + §4.5 (dry-run mode) + §4.6 (safe-default-on-unknown-field).
  - **v11.0 PoC acceptance criterion (Phase 50 consumer):** Dry-run migration must run clean on existing FeedbackStore + 30-day shadow-run window must show <1% retrieval discrepancy before source decommission.
  - **Resolution declaration:** P14 RESOLVED in §4. Cite PITFALLS §P14 + Phase 50 SC#3 acceptance criteria.

  Length target: ~250-300 lines for Task 4.
  </action>

  <verify>
    <automated>grep -qE "^## §4|^## 4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -cE "^### §4\.|^### 4\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 9 {exit 0} {exit 1}' && for term in "FeedbackStore\\|feedback_store" "buckets/" "\\.jsonl" "_INDEX_VERSION" "memory-record-schema\\|memory_record_schema" "schema_version" "dry-run\\|dry run\\|--dry-run" "safe.default\\|safe default\\| safest" "confidential" "quarantined" "confidence" "half_life_days\\|half-life" "expires_at" "verified_at" "supersedes_memory_id" "evidence_chain" "evidence_operator_ids" "agent_id" "shadow\\|双读" "rollback\\|回滚" "PITFALLS §P14\\|Pitfall 14" "决策 6" "Phase 45" "Phase 50" "P14 RESOLVED\\|P14 resolved\\|RESOLVED"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md || { echo "MISSING: $term"; exit 1; }; done && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 1100 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §4 exists with §4.0-§4.9 (memory schema migration SC#4 deep-dive, 10 subsections)
  - §4.1 cites agent/feedback_store.py source schema (buckets/<skill_id>/<source>.jsonl + _INDEX_VERSION=1)
  - §4.2 cites Phase 45 memory-record-schema.yaml target schema (10 mandated fields)
  - §4.3 has 17-row source→target mapping table
  - §4.4 cites memory-record-schema.yaml line 353 schema_version field + forward-compat semantics
  - §4.5 has dry-run output plan (5 metrics + NO write semantics)
  - §4.6 has 6-row safe-default-on-unknown-field table (confidentiality=confidential, scope=project, status=quarantined per P14 mitigation 2)
  - §4.7 has 6-step migration execution (backup → dry-run → approval → live → shadow 30d → decommission)
  - §4.8 has rollback path (source never deleted, rollback always safe)
  - §4.9 declares P14 RESOLVED with v11.0 PoC acceptance criterion
  - Total doc length now ≥ 1100 lines (cumulative with Tasks 1+2+3)
  </done>
</task>

<task type="auto">
  <name>Task 5: Write §5 (Retained-phases allowlist + legacy mem0 policy — SC#5 deep-dive resolving OQ-3 + OQ-10) + §6 (Phase 44 7 决策 cross-validation audit) + §7 (downstream citation guide + summary + references)</name>
  <files>.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md</files>

  <read_first>
    - .planning/research/v10-orchestrator-design/STACK.md §3.2 Tool 7 lines 538-610 (run_python_phase signature + retained_phases allowlist pointer to round-table-schema.yaml) + §11.2 line 1120 (OQ-10 verbatim resolution)
    - .planning/research/v10-orchestrator-design/02-ROUND-TABLE-PROTOCOL.md §5.7 lines 1119-1145 (run_python_phase round-table consumer — Phase 46 says "Phase allowlist defined in ~/.hermes/agents/{slug}/retained_phases.yaml (详 04-MIGRATION-PATH.md)" — note: this doc resolves the location ambiguity in favor of round-table-state-schema.yaml per STACK §11.2 + SUMMARY OQ-10)
    - .planning/research/v10-orchestrator-design/round-table-state-schema.yaml lines 1-65 (Phase 46 schema header + existing required fields — this doc adds retained_python_phases field)
    - .planning/research/v10-orchestrator-design/SUMMARY.md OQ-3 (line 69 — v7.0 mem0 agent_id=hermes legacy) + OQ-10 (line 76 — allowlist location)
    - .planning/research/v10-orchestrator-design/ARCHITECTURE.md §3.1 lines 154-172 (Current Mem0 Backend Surface — `_read_filters()` returns user_id; v7.0 default agent_id=hermes context)
    - .planning/research/v10-orchestrator-design/00-FIRST-PRINCIPLES.md §2.1-§2.7 (decisions 1-7 — root arguments to cross-validate) + §2.2 specifically (决策 2 B3a Python runner — LOAD-BEARING for SC#5)
    - .planning/research/v10-orchestrator-design/06-CROSS-REPO-IMPACT.md §7 + §8 (Phase 48 §7 audit + §8 downstream citation card pattern — apply here)
  </read_first>

  <behavior>
  - §5.0 子节存在: 声明 §5 是 SC#5 + OQ-3 + OQ-10 的完整 resolution —— retained-phases allowlist (run_python_phase 仅接受 Steps 0/6.5/7/10/11/12/15) + 旧 v7.0 mem0 agent_id=hermes memory 遗留策略
  - §5.1 子节存在: run_python_phase boundary tool 引用 (cite STACK §3.2 Tool 7 verbatim — async def run_python_phase(phase, ...), B3a Python runner 增量迁移 boundary tool)
  - §5.2 子节存在: retained-phases allowlist 7 个 step 显式声明 —— Step 0 (project init / setup), Step 6.5 (storyboard assembly pre-generation), Step 7 (ComfyUI/dreamina visual generation), Step 10 (script audit gate), Step 11 (continuity audit), Step 12 (color grading), Step 15 (final render/export). 每 step 给一句 rationale (为什么这个 step 是 delegate-only 适合 CC 跑; 为什么其他 step 不在 allowlist — 例如 Step 8 audio 是 Hermes 内部 phase 不 delegate)
  - §5.3 子节存在: allowlist location resolution —— round-table-state-schema.yaml retained_python_phases field (per STACK §11.2 line 1120 + SUMMARY OQ-10). Resolve 02-ROUND-TABLE-PROTOCOL.md §5.7 line 1145 ambiguity ("retained_phases.yaml" vs round-table-schema.yaml) — 决定: 字段在 round-table-state-schema.yaml (因为它是 schema-level 约束, 不是 per-project config); per-project override via separate retained_phases_overrides.yaml可选 (v12+).
  - §5.4 子节存在: enforcement mechanism —— run_python_phase MCP tool 在 dispatcher 层 validate phase 参数 against retained_python_phases allowlist. 不在 allowlist 的 step → 拒绝 + 返回 error. CC 不能绕过 (因为 run_python_phase 是 Hermes-side MCP tool, CC 仅能调用).
  - §5.5 子节存在: 旧 v7.0 mem0 agent_id=hermes memory 遗留策略 (OQ-3 resolution) —— v7.0 ship 默认 agent_id=hermes 的旧 memory 不迁移, 不删除. 新 agent invocation 从 agent_id=screenplay / agent_id=cinematographer 等 per-agent namespace 开始. 旧 agent_id=hermes memory 作为 read-only legacy fallback 保留 (curator 可以读但不再写). 解决 OQ-3.
  - §5.6 子节存在: legacy memory 30-day sunset window —— v11.0 PoC 启动后 30 天, curator 仍读 agent_id=hermes 作为 fallback context; 30 天后 read-path 移除 legacy fallback (agent_id=hermes memory 真正退役). Operator 可延长 sunset window via config.
  - §6.0 子节存在: 声明 §6 是 Phase 44 决策 1-7 的 cross-validation audit (mirror Phase 48 §7 pattern)
  - §6.1 子节存在: 7-row 决策 audit table (决策 1-7 × 本 doc 实现位置 × 是否一致 + citation). 特别关注 决策 2 (B3a Python runner) — 本 doc 是决策 2 的直接落实.
  - §6.2 子节存在: 偏差分析 (预期 = 无偏差, 显式声明)
  - §6.3 子节存在: OQ-3 + OQ-10 + P14 resolution 声明 —— 三条都在本 doc 显式 resolved (而非 deferred), 列出 resolution 章节
  - §7.0 子节存在: downstream citation guide —— 列出 05-POC-PLAN/Phase 50/51 VALIDATE 引用本文档论据时应从哪个章节取
  - §7.1 子节存在: coherence 声明 (75-cell transform + skill_fallback + memory migration + allowlist + 7 决策 audit = 完整 SC#1-5 coverage)
  - §7.2 子节存在: References (00-FIRST-PRINCIPLES + 01-AGENT-REGISTRY-SCHEMA + 02-ROUND-TABLE-PROTOCOL + 06-CROSS-REPO-IMPACT + ARCHITECTURE + FEATURES + STACK + PITFALLS + SUMMARY + agent/feedback_store.py + MEMORY.md + CLAUDE.md + Phase 44/45/46/48 commit references)
  - Total Task 5 length ≥ 250 lines
  </behavior>

  <action>
  Append §5 + §6 + §7 to `.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md`.

  **§5 — Retained-Phases Allowlist + Legacy mem0 Policy (SC#5 Deep-Dive, OQ-3 + OQ-10 Resolution)**

  §5.0 (~25 lines): "本节是 ROADMAP SC#5 的完整论证 + SUMMARY OQ-3 + OQ-10 的 resolution. STACK §3.2 Tool 7 已给 run_python_phase boundary tool 签名; §11.2 已给 allowlist location (round-table-schema.yaml). 本节给出 7 个 retained step 的显式声明 + enforcement mechanism + 旧 v7.0 mem0 agent_id=hermes memory 遗留策略 + 30-day sunset window."

  §5.1 run_python_phase boundary tool 引用 (~30 lines):
  - **Cite STACK §3.2 Tool 7 verbatim:** `async def run_python_phase(phase: str, ...)` — B3a Python runner 增量迁移的 boundary tool (Phase 44 决策 2 直接落实).
  - **Purpose:** Allow CC to invoke specific Python pipeline phases (ComfyUI generation / script audit / continuity audit / etc.) during round-table turns without leaving the MCP tool boundary.
  - **Boundary semantics:** `openWorldHint=True` per STACK §3.2 — ComfyUI call is open-world (may fail, may produce unexpected output).

  §5.2 retained-phases allowlist 7 个 step (~80 lines):
  - **Step 0 — project init / setup:** delegate-only (CC asks Hermes to init project structure). Why retained: pure setup, no creative decisions.
  - **Step 6.5 — storyboard assembly pre-generation:** delegate-only (CC asks Hermes to assemble storyboard from prior decisions). Why retained: mechanical assembly, not creative.
  - **Step 7 — ComfyUI/dreamina visual generation:** delegate-only (CC asks Hermes to run visual generation pipeline). Why retained: long-running exec task, CC cannot do directly.
  - **Step 10 — script audit gate:** delegate-only (CC asks Hermes to run script_auditor 5-dim check). Why retained: deterministic gate, not creative.
  - **Step 11 — continuity audit:** delegate-only (CC asks Hermes to run continuity_auditor 4-dim check). Why retained: deterministic gate.
  - **Step 12 — color grading:** delegate-only (CC asks Hermes to run colorist LUT application). Why retained: deterministic transform.
  - **Step 15 — final render/export:** delegate-only (CC asks Hermes to run final render pipeline). Why retained: long-running exec task.
  - **Why OTHER steps NOT in allowlist:** Step 1-5 (creative decisions — must be round-table consensus, not delegated); Step 8 (audio pipeline — Hermes internal sub_step orchestration, not single-phase delegate); Step 9 (editor cut — creative decision); Step 13 (compliance gate — operator policy, not CC-dispatchable); Step 14 (theory critic — creative advisory). Cite v86-pipeline-mapping.md step semantics.

  §5.3 allowlist location resolution (~40 lines):
  - **Cite STACK §11.2 line 1120 verbatim:** "run_python_phase 的 retained-phases allowlist 在哪定义? —— round-table-schema.yaml."
  - **Resolve 02-ROUND-TABLE-PROTOCOL.md §5.7 ambiguity:** Phase 46 §5.7 mentions `~/.hermes/agents/{slug}/retained_phases.yaml` — this doc clarifies the canonical location is the **schema-level field** `retained_python_phases` in `round-table-state-schema.yaml` (because it is a schema-level constraint valid across all projects, not per-project config).
  - **Per-project override (v12+ optional):** `~/.hermes/agents/.runtime/{slug}/retained_phases_overrides.yaml` may add project-specific steps — but base allowlist always comes from schema field.
  - **Schema field spec:** `retained_python_phases: array of enum` — enum values = `["step_0", "step_6_5", "step_7", "step_10", "step_11", "step_12", "step_15"]`. Required field in round-table-state-schema.yaml (v10.0 baseline; future bumps may add steps).

  §5.4 enforcement mechanism (~30 lines):
  - **Dispatcher-layer validation:** `run_python_phase` MCP tool handler validates `phase` parameter against `retained_python_phases` allowlist BEFORE invoking Hermes Python pipeline.
  - **Not-in-allowlist phase → reject:** return MCP error `"phase '{phase}' not in retained_python_phases allowlist; allowed: {allowlist}"`. CC cannot bypass (run_python_phase is Hermes-side MCP tool, CC only invokes via MCP call).
  - **No silent fallback:** rejected calls return explicit error — never auto-fall to "execute anyway" (would violate Phase 44 决策 7 Hermes-controls-structure invariant).

  §5.5 旧 v7.0 mem0 agent_id=hermes memory 遗留策略 (~40 lines):
  - **OQ-3 resolution verbatim:** v7.0 ship 默认 `agent_id=hermes` 的旧 memory 不迁移, 不删除. 新 agent invocation 从 `agent_id=screenplay` / `agent_id=cinematographer` 等 per-agent namespace 开始.
  - **Rationale:** v7.0 memory is cross-agent global (no per-agent isolation); v10.0+ requires per-agent isolation (决策 6). Migration would require partitioning global memory by skill_id — but v7.0 records lack skill_id field reliably. Cleaner to start fresh per-agent.
  - **Legacy read-only fallback:** curator can read `agent_id=hermes` records as fallback context (e.g. "what did the system learn pre-v10.0?") but never writes new records to this namespace.

  §5.6 legacy memory 30-day sunset window (~30 lines):
  - **Sunset timeline:** v11.0 PoC launch + 30 days = sunset date.
  - **During window:** curator + dispatcher read from BOTH `agent_id=hermes` (legacy) + `agent_id={per-agent}` (new) for queries that span the transition. Prefer new namespace; fall back to legacy if new is empty.
  - **After sunset:** read-path removes legacy fallback. `agent_id=hermes` records remain in mem0 (never deleted) but are no longer queried by default. Operator can extend sunset via `~/.hermes/config.yaml` `legacy_memory_sunset_days: 60` (default 30).

  **§6 — Phase 44 7 决策 Cross-Validation Audit + OQ/P14 Resolution**

  §6.0 (~20 lines): "本节 audit 本 doc 的 75-cell transform + skill_fallback + memory migration + allowlist 是否一致支持 Phase 44 锁定的 7 决策. 预期 = 7/7 一致 (本 doc 是迁移路径落实, 不是 re-derivation). 同时声明 OQ-3 + OQ-10 + P14 三条 open question 的 resolution 位置."

  §6.1 7-row 决策 audit table (~80 lines):
  | 决策 # | 决策 (一句话) | 本 doc 实现位置 | 一致? | Citation |
  |--------|---------------|------------------|-------|----------|
  | 1 | T6 协议 (Hermes MCP server + tmux dispatch + CC native MCP client) | §3 default_invocation 路由 (mcp_tool mode uses MCP tool); §5.4 run_python_phase enforcement (Hermes-side MCP tool) | ✅ | STACK §3.2 + Phase 44 §2.1 |
  | 2 | B3a Python runner 增量迁移 (delegate-only phase 迁 CC) | §5.1-§5.4 run_python_phase boundary tool + retained-phases allowlist (7 steps) — 直接落实 | ✅ | STACK §3.2 Tool 7 + Phase 44 §2.2 |
  | 3 | D2 storyboard-first-class | §5.2 Step 6.5 storyboard assembly in retained-phases allowlist (D2 storyboard is delegate-eligible) | ✅ | ARCHITECTURE §6 + Phase 44 §2.3 |
  | 4 | G2 通用编排框架 | §2 15-expert transform table 是 G2 runtime 的 agent registry 基础; §3 default_invocation 是 G2 dispatch path | ✅ | ARCHITECTURE §6 + Phase 44 §2.4 |
  | 5 | α agent form (YAML + persona + tools + refs + memory_scope + lineage) | §2 75-cell transform 是 α form 的 15-expert 落实; §3 default_invocation 是 α form 的 invocation 语义 | ✅ | Phase 45 agents-schema.yaml 18 fields + ARCHITECTURE §1 |
  | 6 | per-agent memory + curator-driven 自进化 | §4 memory schema 迁移 (v6.0 FeedbackStore → per-agent memory-record-schema) + §5.5 legacy mem0 policy | ✅ | Phase 45 memory-record-schema.yaml + ARCHITECTURE §3 + Phase 44 §2.6 |
  | 7 | 分层 CC 角色 (Hermes 控结构, CC 控内容) | §3.4 default_invocation transition (operator controls); §5.4 run_python_phase enforcement (Hermes validates allowlist, CC only invokes) | ✅ | STACK §3.2 + Phase 44 §2.7 |

  §6.2 偏差分析 (~25 lines):
  - **预期:** 7/7 一致 (本 doc 是迁移路径落实).
  - **实际:** 7/7 一致. 本 doc 未发现 Phase 44 决策需修正.
  - **特别声明:** 决策 2 (B3a Python runner) 是本 doc 的直接 root argument —— §5 retained-phases allowlist 是决策 2 的可执行落实. 若 v11.0 PoC 实施中发现 allowlist 需调整 (例如加 Step 8 audio), 应在 05-POC-PLAN.md 的 risk register 标注 + 在 Phase 51 audit 复审.

  §6.3 OQ-3 + OQ-10 + P14 resolution 声明 (~40 lines):
  - **OQ-3 (mem0 agent_id 冲突, v7.0 agent_id=hermes legacy): RESOLVED in §5.5-§5.6.** Legacy memory 不迁移 + 30-day sunset window + read-only fallback during transition.
  - **OQ-10 (run_python_phase retained-phases allowlist location): RESOLVED in §5.3.** Location = `round-table-state-schema.yaml` `retained_python_phases` field (per STACK §11.2 + SUMMARY OQ-10).
  - **P14 (Schema Migration Breaks Memory Store): RESOLVED in §4.** schema_version (§4.4) + dry-run mode (§4.5) + safe-default-on-unknown (§4.6) 三层 mitigation.
  - **Cite SUMMARY.md OQ table + PITFALLS §P14 as resolution audit source.**

  **§7 — Downstream Citation Guide + Summary + References**

  §7.0 downstream citation card table (~40 lines):
  | Downstream doc | Cite from this doc | Do NOT re-derive | Should derive |
  | 05-POC-PLAN.md | §2 75-cell transform table (PoC 实施 15 *.agent.yaml must follow this) + §3.4 transition path (PoC per-agent switch sequence) + §4.5 dry-run (PoC acceptance: dry-run must run clean) + §4.7 migration steps (PoC week-1 work) + §5.2 allowlist (PoC run_python_phase test scope = 7 steps) | 18-field schema, 5-field mapping framework, OQ-3/OQ-10/P14 resolutions | PoC fitness battery, vertical slice selection, acceptance timeline |
  | Phase 50 POC-PLAN | §5.2 retained-phases 7 steps (PoC run_python_phase test scope) + §4.9 P14 PoC acceptance (<1% shadow discrepancy) + §3.6 compliance_gate/theory_critic special handling (PoC switch order) | entire doc | PoC implementation sequence + budget |
  | 51 VALIDATE lint | §6.1 7 决策 audit table + §6.3 OQ-3/OQ-10/P14 resolution declarations + §2.18 FOUND-08 invariant + §5.3 retained_python_phases schema field | entire doc | cross-doc consistency lint script (term/schema/decision/citation checks) |

  §7.1 coherence 声明 (~30 lines): "15-expert 75-cell transform 规则表 (§2) + default_invocation skill_fallback → mcp_tool 切换机制 (§3) + memory schema 迁移计划 (§4) + retained-phases allowlist + legacy mem0 policy (§5) + Phase 44 7 决策 cross-validation audit (§6) + OQ-3/OQ-10/P14 resolution declarations = 完整 ROADMAP SC#1-5 coverage. v11.0 PoC implementer 引用本 doc 的 §2 75-cell + §3.4 transition path + §4.5 dry-run + §5.2 allowlist 即可 defending 'how do I transform this SKILL? when do I switch to mcp_tool? how do I migrate memory safely? which Python phases can run_python_phase execute?', 不需重新推导 Phase 44 决策或 re-litigate P14."

  §7.2 References (~30 lines): 00-FIRST-PRINCIPLES.md §2.1-§2.7 (esp §2.2 决策 2 B3a Python runner) + 01-AGENT-REGISTRY-SCHEMA.md (18-field schema + memory-record-schema.yaml) + 02-ROUND-TABLE-PROTOCOL.md §5.7 (run_python_phase consumer) + 06-CROSS-REPO-IMPACT.md §3 (lineage chain complement) + ARCHITECTURE.md §1.1 + §1.2 + §1.3 + §2 + §3.1 + §3.4 + §6.1 + §6.4 + FEATURES.md §11 (anti-features context) + STACK.md §3.2 Tool 7 + §11.2 + PITFALLS.md §P14 + SUMMARY.md (OQ-3 + OQ-10) + agent/feedback_store.py (v6.0 source schema ground truth) + MEMORY.md v6-self-evolution-milestone + kais-movie-agent-v5-hermes-native-migration + CLAUDE.md (HERMES_HOME + skill file conventions) + Phase 44/45/46/48 commit references.

  Length target: ~250-300 lines for Task 5.
  </action>

  <verify>
    <automated>grep -qE "^## §5|^## 5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -qE "^## §6|^## 6\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -qE "^## §7|^## 7\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md && grep -cE "^### §5\.|^### 5\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -cE "^### §6\.|^### 6\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 3 {exit 0} {exit 1}' && grep -cE "^### §7\.|^### 7\." /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 2 {exit 0} {exit 1}' && for term in "run_python_phase" "retained_python_phases\\|retained-phases\\|retained phases" "Step 0" "Step 6.5\\|Step 6\\.5" "Step 7" "Step 10" "Step 11" "Step 12" "Step 15" "allowlist" "round-table-state-schema\\|round-table-schema" "agent_id=hermes\\|agent_id = hermes" "sunset\\|退役" "legacy\\|遗留" "决策 1" "决策 2" "决策 3" "决策 4" "决策 5" "决策 6" "决策 7" "audit" "一致" "OQ-3" "OQ-10" "P14" "RESOLVED\\|resolved" "00-FIRST-PRINCIPLES" "01-AGENT-REGISTRY-SCHEMA" "02-ROUND-TABLE-PROTOCOL" "05-POC-PLAN" "51 VALIDATE\\|51-VALIDATE\\|VALIDATE" "STACK §3.2" "STACK §11.2" "v86-pipeline-mapping\\|v86"; do grep -q "$term" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md || { echo "MISSING: $term"; exit 1; }; done && awk '/^### §6\.1/,/^### §6\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | grep -cE "决策 [1-7]" | awk '$1 >= 7 {exit 0} {exit 1}' && awk '/^### §6\.1/,/^### §6\.2/' /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | grep -c "✅" | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "v6-self-evolution-milestone\\|kais-movie-agent-v5-hermes-native-migration\\|feedback_store" /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 1 {exit 0} {exit 1}' && wc -l /data/workspace/hermes-agent/.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md | awk '$1 >= 1300 {exit 0} {exit 1}'</automated>
  </verify>

  <done>
  - §5 exists with §5.0-§5.6 (retained-phases allowlist SC#5 deep-dive + legacy mem0 policy, 7 subsections)
  - §5.2 lists 7 retained steps (0/6.5/7/10/11/12/15) + rationale per step + why other steps excluded
  - §5.3 resolves allowlist location = round-table-state-schema.yaml retained_python_phases field (per STACK §11.2 + SUMMARY OQ-10)
  - §5.4 declares enforcement mechanism (dispatcher-layer validation, no silent fallback)
  - §5.5-§5.6 declare legacy v7.0 mem0 agent_id=hermes policy (遗留/不迁移 + 30-day sunset)
  - §6 exists with §6.0-§6.3 (7 决策 cross-validation audit + 偏差分析 + OQ-3/OQ-10/P14 resolution)
  - §6.1 has 7-row audit table with 决策 1-7 each marked ✅ + citation column
  - §6.2 declares 7/7 一致 (no Phase 44 revision needed)
  - §6.3 declares OQ-3 + OQ-10 + P14 all RESOLVED with section pointer
  - §7 exists with §7.0 downstream citation card + §7.1 coherence 声明 + §7.2 References
  - All 7 决策 (1-7) appear in §6.1 audit table
  - MEMORY.md references (v6-self-evolution-milestone + kais-movie-agent-v5-hermes-native-migration) + feedback_store appear
  - Total doc length ≥ 1300 lines (final)
  - 05-POC-PLAN + Phase 50 + 51 VALIDATE all appear in downstream citation card
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-doc → v11.0 PoC implementer | The deliverable is a design doc. Misinterpretation by v11.0 PoC implementers (e.g. "all 15 agents must switch to mcp_tool simultaneously" or "FeedbackStore is deleted after migration") is the primary risk. Mitigated by §6 cross-validation audit + §7.0 downstream citation card + §3.4 per-agent transition path + §4.7 6-step migration (backup FIRST, decommission LAST) + §4.8 rollback path. |
| 04-MIGRATION-PATH.md → ARCHITECTURE §1/§2/§6 + Phase 44/45/46 | This doc CONSUMES ARCHITECTURE §1.1 18-field schema + §1.2 SKILL→YAML disposition + §2 15-expert table + §6.1 transform procedure + Phase 44 决策 1-7 + Phase 45 agents-schema.yaml + memory-record-schema.yaml + Phase 46 round-table-state-schema.yaml. Re-derivation would cause cross-doc drift. Mitigated by explicit "CITE ONLY, do NOT redefine" preamble in §1.2 + per-section "Cites ARCHITECTURE §X / agents-schema.yaml field Y" annotations. |
| 75-cell transform table → v11.0 PoC manual transform | The 75-cell table (15 experts × 5 fields) is the direct input to v11.0 PoC manual transform work. If cells are ambiguous or defaults incorrect, transform produces wrong agent YAMLs. Mitigated by §2.1 5-field default + edge case pattern + §2.2-§2.16 per-expert concrete persona excerpts + §2.17 75-cell coverage audit. |
| Memory migration → v6.0 FeedbackStore records | §4 migration plan operates on existing v6.0 FeedbackStore data. Mis-handling (silent drop, unsafe defaults) corrupts memory store (P14). Mitigated by §4.4 schema_version forward-compat + §4.5 dry-run mode + §4.6 safe-default-on-unknown + §4.7 6-step backup-first + §4.8 rollback path + §4.9 P14 RESOLVED declaration. |
| Retained-phases allowlist → run_python_phase execution scope | §5 allowlist (Steps 0/6.5/7/10/11/12/15) defines what CC can invoke via run_python_phase. Too-permissive allowlist = CC runs creative steps it shouldn't; too-restrictive = CC can't do its job. Mitigated by §5.2 rationale per step (why retained / why excluded) + §5.3 schema-level lock + §5.4 dispatcher enforcement + §6.2 偏差分析 declares allowlist is starting point revisable in Phase 51 audit. |
| Legacy v7.0 mem0 agent_id=hermes → v11.0+ per-agent namespace | §5.5-§5.6 legacy policy determines whether old memory is reachable. Premature sunset loses historical context; indefinite retention pollutes per-agent namespace. Mitigated by §5.6 30-day sunset window (operator-extendable) + read-only fallback during transition + source never deleted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | §2 75-cell transform table — persona excerpts could be misinterpreted as final (vs. starting templates) leading to operator not hand-tuning | mitigate | §2.18 cites ARCHITECTURE §6.1 "operator owns persona, hand-tuned beyond initial transform" + §3.4 transition step (2) "disabled initially, verify persona, then mcp_tool" + §6.1 决策 5 audit reaffirms operator ownership |
| T-49-02 | Information Disclosure | §4 memory migration — FeedbackStore source may contain sensitive feedback text that gets duplicated to mem0 target without redaction | accept | FeedbackStore is operator-owned local file (~/.hermes/); no new disclosure surface; mem0 backend inherits same trust boundary; §4.6 default confidentiality="confidential" defends downstream |
| T-49-03 | Denial of Service | §5.2 retained-phases allowlist — too-restrictive list blocks legitimate CC work; too-permissive allows dangerous phase execution | mitigate | §5.2 each step cites v86-pipeline-mapping.md rationale (why retained / why excluded); §5.4 dispatcher rejects with explicit error listing allowed steps; §6.2 偏差分析 declares revisable in Phase 51 audit |
| T-49-04 | Elevation of Privilege | §3 default_invocation switching — operator could accidentally set compliance_gate to mcp_tool before policy review, enabling agent to block pipeline | mitigate | §3.6 compliance_gate special handling (recommend disabled initially, operator unlocks after policy review); §3.2 failure-mode matrix covers disabled state explicitly |
| T-49-05 | Repudiation | §4.7 migration steps — operator may run live migration without dry-run, then blame Hermes for data corruption | mitigate | §4.7 Step 2 (dry-run) + Step 3 (operator approval gate) are explicit human checkpoints; §4.8 rollback path always available (source never deleted); §4.9 P14 acceptance criterion requires dry-run clean record |
| T-49-06 | Spoofing | §5.5 legacy mem0 policy — new agent could masquerade with agent_id=hermes to read legacy cross-agent memory | mitigate | §5.5 explicit "new agent invocation starts from agent_id=screenplay etc."; §5.6 sunset window removes read-path after 30 days; dispatcher enforces per-agent namespace per ARCHITECTURE §3.2 Option B filter |

Note: v10.0 is a design-only milestone with zero code/infra changes — T-49-SC (supply chain from package installs) is not applicable.
</threat_model>

<verification>
**Phase 49 DESIGN-05 success criteria (from ROADMAP §Phase 49):**

1. SC#1 — File `04-MIGRATION-PATH.md` exists (verified by Task 1)
2. SC#2 — 15 expert × 5-field transform 规则表 complete (75 cells, each cell lists SKILL frontmatter → agent YAML field mapping + default + edge cases) (verified by Task 2 §2.0-§2.19 full deep-dive)
3. SC#3 — `default_invocation: skill_fallback → mcp_tool` 切换机制 documented (agent prefers MCP tool, on failure falls back to SKILL form) (verified by Task 3 §3.0-§3.7 full deep-dive)
4. SC#4 — Memory schema 迁移计划 complete (v6.0 FeedbackStore JSONL → new memory-record-schema, includes `schema_version` field + dry-run migration mode, prevents P14) (verified by Task 4 §4.0-§4.9 full deep-dive)
5. SC#5 — Retained-phases allowlist 显式声明 (`run_python_phase` only accepts Steps 0/6.5/7/10/11/12/15, allowlist location = `round-table-schema.yaml`, resolves OQ-10) + 旧 v7.0 mem0 `agent_id=hermes` memory 遗留策略 (resolves OQ-3) (verified by Task 5 §5.0-§5.6 full deep-dive)

**Final deliverable target:** ~1300+ lines for the single markdown deliverable (mirrors Phase 44's 1181 lines + Phase 48's ~1300 lines at comparable rigor; Phase 49 is single-deliverable so the entire budget concentrates in the narrative).

**Methodology audit (self-check before commit):**
- Every section cites ARCHITECTURE §X / PITFALLS §PX / SUMMARY OQ-X / Phase 44 决策号 / Phase 45 schema source / Phase 46 protocol / STACK §X (not invented)
- Phase 44 decisions 1-7 cited by 决策号, never re-derived (esp 决策 2 B3a Python runner — load-bearing for SC#5)
- Phase 45 schema fields (18 fields incl. default_invocation, lineage, memory_scope + 10 memory-record fields incl. schema_version, agent_id, scope) cited by name, never redefined
- Phase 46 protocol invariants (round-table-state-schema.yaml, run_python_phase consumer in §5.7) cited by section, never redefined
- ARCHITECTURE §1.1 + §1.2 + §1.3 + §2 + §3.1 + §3.4 + §6.1 + §6.4 each cited by section number
- PITFALLS §P14 each cited by section number (3 mitigations all reflected in §4.4/§4.5/§4.6)
- STACK §3.2 Tool 7 + §11.2 each cited
- SUMMARY OQ-3 + OQ-10 each cited
- Bilingual: EN structure (headers, schemas, citation tags) + 中文 prose (rationale, examples) per CLAUDE.md
- §6 audit confirms 7/7 决策 cross-validation consistent (no silent Phase 44 revision)
- §6.3 declares OQ-3 + OQ-10 + P14 RESOLVED with section pointer (not deferred)
- v6.0 FeedbackStore source schema read from agent/feedback_store.py ground truth (not invented)
- FOUND-08 preservation: all 15 expert_id values copy verbatim (§2.18 declaration)
</verification>

<success_criteria>
**Phase 49 PLAN is complete when:**

1. PLAN.md exists at `.planning/phases/49-migration-path/49-01-PLAN.md` with valid frontmatter (this file).
2. (Execution phase) The 1 deliverable file exists:
   - `.planning/research/v10-orchestrator-design/04-MIGRATION-PATH.md` (≥ 1300 lines, §0-§7, 15-expert 75-cell transform + skill_fallback switching + memory schema migration with P14 mitigation + retained-phases allowlist + legacy mem0 policy + 7 决策 audit + OQ-3/OQ-10/P14 resolution)
3. ROADMAP.md Phase 49 entry updated (Plans: 1 plan, plan list with checkmark).
4. PLAN committed to git via gsd-sdk.
</success_criteria>

<output>
Create `.planning/phases/49-migration-path/49-01-SUMMARY.md` when done. SUMMARY should record:
- Final line count of the deliverable
- 15-expert 75-cell transform rule lock (15 × 5 fields, all 15 expert_ids preserved FOUND-08)
- default_invocation skill_fallback → mcp_tool switching mechanism lock (3-state enum + 12-cell failure matrix + safe-default-on-unknown)
- Memory schema migration lock (v6.0 FeedbackStore → Phase 45 memory-record-schema, 17-row mapping table, schema_version + dry-run + safe-default mitigates P14)
- Retained-phases allowlist lock (Steps 0/6.5/7/10/11/12/15, location = round-table-state-schema.yaml retained_python_phases field)
- Legacy v7.0 mem0 agent_id=hermes policy lock (遗留/不迁移 + 30-day sunset window)
- Phase 44 决策 1-7 cross-validation lock (7/7 ✅ 一致, no revision needed)
- OQ-3 + OQ-10 + P14 RESOLVED declarations
- SC#1-5 coverage confirmed
- Cross-validation: every section cites ARCHITECTURE §X / PITFALLS §PX / SUMMARY OQ-X / Phase 44 决策号 / Phase 45/46 schema source / STACK §X
</output>

---
phase: 30-eval-gate-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/movie-experts/_eval/runner.py
  - skills/movie-experts/_eval/gate.py
  - skills/movie-experts/_eval/gate_config.yaml.example
  - skills/movie-experts/_eval/tests/test_runner.py
  - skills/movie-experts/_eval/tests/test_gate.py
  - skills/movie-experts/_eval/tests/fixtures
autonomous: true
requirements: [GATE-01, GATE-02, GATE-04]
user_setup: []

must_haves:
  truths:
    - "runner.parse_judge_scores() extracts the 4 per-dimension numeric scores (1.0-5.0) from a judge response text"
    - "runner.run_position_swap() returns scores_ab + scores_ba dicts alongside the existing ordering_ab/ordering_ba/final fields (backward-compatible extension)"
    - "gate.decide_verdict() returns pass / fail_mean / fail_regression / inconclusive deterministically for fixed numeric inputs"
    - "gate.apply_patch() validates via `git apply --check` then applies via `git apply`; gate.revert_patch() restores files via `git checkout --`"
    - "gate.revert_patch() runs even when evaluate raises (try/finally guarantees clean working tree)"
    - "Gate loads thresholds from gate_config.yaml with CLI flag overrides (--threshold-delta, --per-prompt-threshold, --min-prompts)"
    - "Pre-generated answers (JSON file input) drive the gate; live answer generation is out of scope (P31 bridges runtime)"
  artifacts:
    - path: "skills/movie-experts/_eval/runner.py"
      provides: "parse_judge_scores() + composite_score() + run_position_swap() extended return (numeric scores)"
      contains: "def parse_judge_scores"
    - path: "skills/movie-experts/_eval/gate.py"
      provides: "Gate orchestrator: run_gate() + main() CLI + decide_verdict + apply/revert + config load"
      exports: ["run_gate", "main", "decide_verdict", "apply_patch", "revert_patch", "extract_patched_files", "load_gate_config"]
    - path: "skills/movie-experts/_eval/gate_config.yaml.example"
      provides: "Committed default thresholds (delta_threshold=0.3, per_prompt_threshold=1.0, min_prompts=5, ab_positions=2, judge_model)"
      contains: "delta_threshold"
    - path: "skills/movie-experts/_eval/tests/test_gate.py"
      provides: "Unit tests for decide_verdict, config load, parse_judge_scores integration, exit code mapping"
      min_lines: 200
    - path: "skills/movie-experts/_eval/tests/test_runner.py"
      provides: "Extended with TestParseJudgeScores class (well-formed, missing dim, out-of-range, malformed)"
      contains: "class TestParseJudgeScores"
  key_links:
    - from: "skills/movie-experts/_eval/gate.py"
      to: "skills/movie-experts/_eval/runner.py"
      via: "sibling import (import runner) + call run_position_swap / make_judge_client / load_prompts / parse_judge_scores"
      pattern: "^import runner|^from runner"
    - from: "skills/movie-experts/_eval/gate.py::decide_verdict"
      to: "numeric thresholds (delta_threshold, per_prompt_threshold, min_prompts)"
      via: "pure function over baseline_scores + candidate_scores lists"
      pattern: "delta_threshold|per_prompt_threshold|min_prompts"
    - from: "skills/movie-experts/_eval/gate.py::apply_patch"
      to: "git subprocess"
      via: "subprocess.run(['git', 'apply', '--check', ...]) then subprocess.run(['git', 'apply', ...])"
      pattern: "git.*apply.*--check"
---

<objective>
Build the foundation of the Phase 30 eval gate: (1) extend `runner.py` with `parse_judge_scores()` to lift the 4 per-dimension numeric scores the judge already emits but the v1 harness discards (LOAD-BEARING per RESEARCH.md Pitfall 1), (2) build `gate.py` as a thin orchestrator over `runner.py` that applies a candidate patch, looks up the baseline, evaluates the patched skill with position-swapped numeric scoring, decides pass/fail via mean-δ (GATE-02) + per-prompt regression (GATE-04) thresholds, writes a report, reverts the patch, and exits with a verdict code (GATE-01).

Purpose: This plan unblocks the numeric gate. Without `parse_judge_scores()`, GATE-02 ("mean score drops more than δ=0.3") and GATE-04 ("single prompt drops more than 1.0") cannot be evaluated — they are numeric by definition but v1 `runner.py:53 _DECISION_RE` only parses the `<decision>A|B|tie</decision>` tag.

Output: 3 new files (`gate.py`, `gate_config.yaml.example`, `tests/test_gate.py` + `tests/fixtures/`) + 2 extended files (`runner.py`, `tests/test_runner.py`). All `_eval/` offline tooling — zero Hermes runtime touch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-eval-gate-reuse/30-CONTEXT.md
@.planning/phases/30-eval-gate-reuse/30-RESEARCH.md

# Existing code under extension (READ BEFORE EDITING):
@skills/movie-experts/_eval/runner.py
@skills/movie-experts/_eval/tests/test_runner.py
@skills/movie-experts/_eval/judge_prompt.md
@skills/movie-experts/_eval/snapshot.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From skills/movie-experts/_eval/runner.py (EXISTING — extend, do NOT fork):
```python
# Line 53: existing decision-only parser (preserved for backward compat)
_DECISION_RE = re.compile(r"<decision>\s*(A|B|tie)\s*</decision>", re.IGNORECASE)

# Line 85: existing public function
def parse_judge_decision(raw_text: str) -> str: ...  # returns "A" | "B" | "tie"

# Line 241: existing position-swap — EXTEND return dict (backward compat)
def run_position_swap(
    prompt: str,
    answer_a: str,
    answer_b: str,
    judge_client: JudgeClient,
    judge_model: str,
    *,
    prompt_id: str = "",
) -> dict[str, Any]:
    """
    Current return keys: prompt_id, ordering_ab, ordering_ba, final
    EXTENDED return keys (Plan 01 adds): raw_ab, raw_ba, scores_ab, scores_ba
    """

# Line 416: existing — REUSE verbatim
def make_judge_client(config: dict[str, Any]) -> Any: ...

# Line 398: existing — REUSE verbatim
def load_prompts(prompts_path: str | Path) -> list[dict[str, str]]: ...

# Line 185: existing — REUSE verbatim (called by run_position_swap)
def _call_judge(judge_client, messages, judge_model, *, swap: bool) -> str: ...
```

From skills/movie-experts/_eval/judge_prompt.md (judge instruction — defines dimension names):
```
industry_accuracy (行业准确度)       — score 1-5
professional_depth (专业深度)        — score 1-5
actionability (可执行性)             — score 1-5
language_quality (语言质量)          — score 1-5
```
The judge emits these as `industry_accuracy: 4 — justification` format. `parse_judge_scores()` MUST lift all 4 via regex.

From skills/movie-experts/_eval/tests/test_runner.py (existing sibling-import convention to match):
```python
_EVAL_DIR = Path(__file__).resolve().parent.parent
if str(_EVAL_DIR) not in sys.path:
    sys.path.insert(0, str(_EVAL_DIR))
import runner  # noqa: E402
```
test_gate.py MUST follow the same pattern (`import gate` + `import runner`).

From skills/movie-experts/_eval/snapshot.py:14 (load-bearing constraint):
```
"stdlib only (no third-party packages)"
```
NEW gate.py code MUST be stdlib-only (json, statistics, subprocess, argparse, hashlib, re, datetime, pathlib, logging, math). NO scipy, NO pydantic — manual validation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runner.py with parse_judge_scores() and numeric-scored run_position_swap</name>
  <files>skills/movie-experts/_eval/runner.py, skills/movie-experts/_eval/tests/test_runner.py</files>
  <read_first>
    - skills/movie-experts/_eval/runner.py (full file — focus lines 40-70 constants, 85-118 parse_judge_decision, 185-225 _call_judge, 226-240 _final_verdict, 241-279 run_position_swap)
    - skills/movie-experts/_eval/judge_prompt.md (full file — confirms exact dimension names + score format)
    - skills/movie-experts/_eval/tests/test_runner.py (full file — match MockJudgeClient pattern + sibling-import convention)
    - .planning/phases/30-eval-gate-reuse/30-RESEARCH.md (Code Examples section lines 397-435 — reference implementation of parse_judge_scores + composite_score)
  </read_first>
  <behavior>
    - TestParseJudgeScores::test_well_formated: judge text with all 4 dimensions ("industry_accuracy: 4 — good", etc.) → parse_judge_scores returns dict with all 4 keys, values 1.0-5.0 floats
    - TestParseJudgeScores::test_case_insensitive: "Industry_Accuracy: 3" → returns {"industry_accuracy": 3.0}
    - TestParseJudgeScores::test_missing_dimension: text with only 3 of 4 dimensions → returned dict has only those 3 keys (no KeyError, no default imputation)
    - TestParseJudgeScores::test_out_of_range_dropped: "industry_accuracy: 9" → key silently dropped (out of [1.0, 5.0] range)
    - TestParseJudgeScores::test_non_numeric_dropped: "industry_accuracy: good" → key silently dropped
    - TestParseJudgeScores::test_empty_when_no_dimensions: text with no recognizable dimension → returns empty dict {}
    - TestParseJudgeScores::test_decimal_score: "industry_accuracy: 4.5" → returns {"industry_accuracy": 4.5}
    - TestRunPositionSwapNumeric::test_returns_scores_ab_and_ba: run_position_swap with MockJudgeClient (canned responses containing numeric scores) → return dict has scores_ab AND scores_ba keys, each a dict[str, float]
    - TestRunPositionSwapNumeric::test_backward_compat_existing_keys: extended return dict STILL has prompt_id, ordering_ab, ordering_ba, final keys (no breaking change to existing tests)
    - TestRunPositionSwapNumeric::test_raw_ab_raw_ba_returned: return dict includes raw_ab and raw_ba strings (enables gate.py to re-parse if needed)
    - TestCompositeScore::test_mean_of_dimensions: composite_score({"a": 2.0, "b": 4.0}) → 3.0
    - TestCompositeScore::test_empty_returns_none: composite_score({}) → None
  </behavior>
  <action>
    Add to runner.py (preserve all existing code — this is additive extension per CONTEXT.md "extends, does NOT fork"):

    1. Module-level constant `_SCORE_DIMENSIONS = ("industry_accuracy", "professional_depth", "actionability", "language_quality")` — tuple of the 4 dimension names matching judge_prompt.md exactly. Place near _DECISION_RE (line 53 area).

    2. Module-level compiled regexes — one per dimension — built from `_SCORE_RE_TEMPLATE = r"{dim}:\s*([1-5](?:\.\d+)?)"`. Use `re.compile` once at module load (avoid re-compiling per call). Pattern matches the dimension name followed by colon, whitespace, then a number 1-5 with optional decimal. Case-insensitive.

    3. New public function `parse_judge_scores(raw_text: str) -> dict[str, float]`: iterates _SCORE_DIMENSIONS, searches raw_text with the corresponding compiled regex, validates parsed float is in [1.0, 5.0], silently drops out-of-range/non-numeric/missing dimensions. Returns dict with only successfully-parsed dimensions. Empty dict if none found. Pure function — no side effects, no logging of parsed values (T-00-09: never log judge raw text).

    4. New public function `composite_score(scores: dict[str, float]) -> float | None`: returns mean of dict values, or None if dict is empty. Pure function.

    5. EXTEND run_position_swap (line 241) — add `raw_ab`, `raw_ba`, `scores_ab`, `scores_ba` keys to its return dict. Concretely: after computing `raw_ab = _call_judge(...)` and `ordering_ab = parse_judge_decision(raw_ab)`, also compute `scores_ab = parse_judge_scores(raw_ab)`. Same for ba. Return dict gains these 4 keys alongside existing `prompt_id`, `ordering_ab`, `ordering_ba`, `final` keys. Existing return keys MUST remain unchanged (backward-compat: existing test_runner.py tests must still pass without modification).

    Add to tests/test_runner.py — new test class `TestParseJudgeScores` covering the behaviors above + new test class `TestRunPositionSwapNumeric` (reuse the existing MockJudgeClient pattern but make canned responses contain numeric dimension scores like `"industry_accuracy: 4 — good\nprofessional_depth: 3 — ok\n...\n<decision>A</decision>"`). Use the existing sibling-import convention (`sys.path.insert` already at top of file).

    CRITICAL: Do NOT modify parse_judge_decision, _DECISION_RE, build_judge_messages, _call_judge, _final_verdict, run_ablation, load_prompts, make_judge_client, or main() — those are unchanged. Only ADD parse_judge_scores, composite_score, _SCORE_DIMENSIONS, _SCORE_RE_TEMPLATE, and EXTEND run_position_swap's return dict.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/movie-experts/_eval/tests/test_runner.py -x -q && python -m pytest skills/movie-experts/_eval/tests/test_runner.py::TestParseJudgeScores skills/movie-experts/_eval/tests/test_runner.py::TestRunPositionSwapNumeric skills/movie-experts/_eval/tests/test_runner.py::TestCompositeScore -x -v</automated>
  </verify>
  <done>
    - parse_judge_scores() extracts all 4 dimensions from well-formed judge responses; silently drops malformed/out-of-range; returns {} when none found
    - run_position_swap() return dict includes scores_ab, scores_ba, raw_ab, raw_ba alongside existing keys (all pre-existing test_runner.py tests still pass — backward compat verified)
    - composite_score() returns mean of available dimensions or None
    - All new tests green; all pre-existing test_runner.py tests still green
    - Ruff PLW1514 clean on runner.py (`ruff check skills/movie-experts/_eval/runner.py`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build gate.py orchestrator with patch mechanics + decide_verdict + config load + CLI surface</name>
  <files>skills/movie-experts/_eval/gate.py, skills/movie-experts/_eval/gate_config.yaml.example, skills/movie-experts/_eval/tests/test_gate.py, skills/movie-experts/_eval/tests/fixtures</files>
  <read_first>
    - skills/movie-experts/_eval/runner.py (after Task 1 extension — for parse_judge_scores, composite_score, run_position_swap return shape, make_judge_client, load_prompts)
    - skills/movie-experts/_eval/snapshot.py (full file — for compute_provenance + EXPERT_DIRS pattern; gate reuses snapshot-style sha256)
    - skills/movie-experts/_eval/tests/test_runner.py (for sibling-import + MockJudgeClient pattern)
    - .planning/phases/30-eval-gate-reuse/30-RESEARCH.md (Code Examples lines 397-510 + Pattern 2 lines 223-265 + reject-log JSON shape lines 512-577)
    - .planning/phases/30-eval-gate-reuse/30-CONTEXT.md (Gate CLI Surface section — exit code contract, CLI flags, threshold defaults)
  </read_first>
  <behavior>
    - TestExtractPatchedFiles::test_single_file: patch with one `+++ b/skills/movie-experts/screenplay/SKILL.md` header → returns ["skills/movie-experts/screenplay/SKILL.md"]
    - TestExtractPatchedFiles::test_rejects_path_traversal: patch with `+++ b/../../../etc/passwd` → raises ValueError (security: V4 access control per RESEARCH.md threat register)
    - TestExtractPatchedFiles::test_rejects_outside_skills: patch touching `+++ b/agent/curator.py` → raises ValueError (gate only patches skills/movie-experts/)
    - TestDecideVerdict::test_pass: baseline=[3.5, 3.0, 4.0, 3.5, 3.2], candidate=[3.4, 3.1, 4.0, 3.5, 3.3], delta_threshold=0.3, per_prompt_threshold=1.0, min_prompts=5 → ("pass", evidence_dict)
    - TestDecideVerdict::test_fail_mean: baseline all 4.0, candidate all 3.5 (mean_delta=-0.5 < -0.3) → ("fail_mean", evidence with mean_delta field)
    - TestDecideVerdict::test_fail_regression: baseline=[4.0,4.0,4.0,4.0,4.0], candidate=[4.0,4.0,3.0,4.0,4.0] (one prompt drops -1.0, NOT < -1.0 so still pass); candidate=[4.0,4.0,2.5,4.0,4.0] (drops -1.5 < -1.0) → ("fail_regression", evidence with regressing_prompt_idx)
    - TestDecideVerdict::test_inconclusive: only 3 valid prompts, min_prompts=5 → ("inconclusive", evidence with n_valid=3)
    - TestDecideVerdict::test_deterministic: decide_verdict called twice with same inputs → identical verdict + evidence (pure function)
    - TestLoadGateConfig::test_defaults_when_no_file: load_gate_config(None) → dict with delta_threshold=0.3, per_prompt_threshold=1.0, min_prompts=5, ab_positions=2
    - TestLoadGateConfig::test_loads_from_yaml: temp gate_config.yaml with delta_threshold: 0.2 → loaded value 0.2
    - TestLoadGateConfig::test_cli_override: CLI flags --threshold-delta 0.15 override YAML value 0.3
    - TestLoadGateConfig::test_rejects_invalid_delta: delta_threshold=5.5 (out of [0, 5]) → raises ValueError
    - TestExitCode::test_pass_to_0: verdict "pass" → exit code 0
    - TestExitCode::test_fail_mean_to_1, test_fail_regression_to_2, test_inconclusive_to_3
    - TestPatchMechanics::test_apply_then_revert_restores_bytes: in temp git repo, write SKILL.md, create patch, apply_patch() then revert_patch() → file sha256 matches original (integration test with real git subprocess)
    - TestPatchMechanics::test_apply_check_failure_raises: patch that does not apply cleanly → apply_patch() raises subprocess.CalledProcessError BEFORE mutating working tree
    - TestRevertOnException:: test_revert_runs_in_finally: gate run_gate() with mocked evaluate that raises → revert_patch() still invoked (working tree clean after exception)
    - TestRunGateEndToEnd::test_with_mock_judge_and_pregenerated_answers: synthetic patch + pre-generated baseline/candidate answers JSON + MockJudgeClient (canned responses with numeric scores) → run_gate() returns GateResult with verdict matching decide_verdict expectation
    - TestRunGateEndToEnd::test_writes_report_json: run_gate() writes `_eval/reports/<patch_id>.json` with schema_version=1, verdict, thresholds, per_prompt array, judge_model, ts (ISO 8601)
    - TestRunGateEndToEnd::test_writes_reject_log_on_failure: on verdict != "pass", also writes `_eval/reports/<patch_id>.reject.json` with operator_hint field
    - TestRunGateEndToEnd::test_pre_generated_answers_input: gate accepts --baseline-answers and --candidate-answers JSON file paths (no live answer generation — P30 scope fence per RESEARCH Open Q2 resolution)
  </behavior>
  <action>
    Create gate.py as a sibling module to runner.py. Module docstring MUST state: "Phase 30 Eval Gate. Offline developer tooling — NOT imported by Hermes runtime. Extends runner.py (does NOT fork). Pre-generated answers input only; live answer generation is P31 scope." Top of file: `from __future__ import annotations` (CLAUDE.md mandate).

    Implement these public functions (all stdlib-only per snapshot.py:14):

    DIMENSIONS / CONSTANTS:
    - `GATE_DIMENSIONS = runner._SCORE_DIMENSIONS` (import from extended runner)
    - `VERDICT_TO_EXIT = {"pass": 0, "fail_mean": 1, "fail_regression": 2, "inconclusive": 3}` — per CONTEXT.md Gate CLI Surface

    PATCH MECHANICS (Pattern 2 from RESEARCH.md):
    - `extract_patched_files(patch_path: Path) -> list[str]`: parse `+++ b/<path>` lines from unified diff. SECURITY: reject (raise ValueError with clear message) any path that does not start with `skills/movie-experts/` OR contains `..` — mitigates path traversal (T-30-01).
    - `apply_patch(patch_path: Path, repo_root: Path) -> None`: run `git apply --check <patch>` first (validation, raises subprocess.CalledProcessError on failure BEFORE any working-tree mutation); on success run `git apply <patch>`. Use `subprocess.run([...], cwd=str(repo_root), check=True, capture_output=True, text=True, encoding="utf-8")`. No `shell=True` (T-30-02 subprocess injection).
    - `revert_patch(files: list[str], repo_root: Path) -> None`: run `git checkout -- <files...>`. For files that the patch added new (detect via `+++ b/<path>` paired with `--- /dev/null`), use `git clean -f <path>` instead — `git checkout` fails on files not in HEAD. Log revert outcome.

    CONFIG LOADER:
    - `load_gate_config(config_path: Path | None, cli_overrides: dict) -> dict`: if config_path and exists, load YAML (use `yaml.safe_load` with `encoding="utf-8"`). Apply CLI overrides last (highest precedence). Validate: `delta_threshold` in [0.0, 5.0], `per_prompt_threshold` in [0.0, 5.0], `min_prompts` >= 1, `ab_positions` in {1, 2}, `judge_model` is non-empty str. Raise ValueError on invalid (fail-fast per RESEARCH V5 control).

    DECISION LOGIC (GATE-02 + GATE-04 — Pattern 3 from RESEARCH.md):
    - `decide_verdict(baseline_scores: list[float], candidate_scores: list[float], *, delta_threshold: float, per_prompt_threshold: float, min_prompts: int) -> tuple[str, dict]`: pure function. n = min(len(baseline), len(candidate)). If n < min_prompts → ("inconclusive", {"n_valid": n, "min_prompts": min_prompts}). Compute deltas = [c - b for ...]. mean_delta = statistics.mean(deltas). If mean_delta < -delta_threshold → ("fail_mean", evidence). For each i, if deltas[i] < -per_prompt_threshold → ("fail_regression", evidence with regressing_prompt_idx + all_deltas). Else ("pass", evidence). Deterministic — no I/O, no logging.

    EVALUATION (calls runner.py — the reuse per GATE-01):
    - `evaluate_candidate(prompts: list[dict], baseline_answers: list[str], candidate_answers: list[str], judge_client, judge_model: str) -> list[dict]`: for each prompt i, call `runner.run_position_swap(prompt=p["text"], answer_a=baseline_answers[i], answer_b=candidate_answers[i], judge_client=judge_client, judge_model=judge_model, prompt_id=p["id"])`. From the extended return dict, extract `scores_ab` and `scores_ba`, compute per-dimension average: `dim_avg[dim] = (scores_ab.get(dim, 0) + scores_ba.get(dim, 0)) / 2` for dims present in EITHER (position-bias mitigation on numeric axis — Pitfall 5). composite = runner.composite_score(dim_avg). Return list of per-prompt records {prompt_id, baseline_composite, candidate_composite, delta, scores_ab, scores_ba, ordering_ab, ordering_ba, final}. NOTE: in this scope, baseline_answers and candidate_answers are PRE-GENERATED (loaded from JSON files passed via CLI); gate does NOT call the Hermes agent. (Scope fence per RESEARCH Open Q2 resolution.)

    PATCH ID + PROVENANCE:
    - `generate_patch_id(skill_id: str, patch_path: Path) -> str`: returns `f"{skill_id}_{ts_unix}_{sha256[:8]}"` where ts_unix = int(datetime.now(timezone.utc).timestamp()) and sha256 = first 8 hex of hashlib.sha256(patch_path.read_bytes()).hexdigest() — per CONTEXT.md Claude's-discretion recommendation.

    ORCHESTRATOR:
    - `run_gate(patch_path: Path, skill_id: str, baseline_answers_path: Path, candidate_answers_path: Path, config: dict, *, repo_root: Path, prompts_path: Path, baseline_scores_cache: Path | None = None) -> GateResult`: the main entrypoint. Steps in order: (1) extract_patched_files + security validation; (2) apply_patch (try/finally wrapper — see below); (3) load baseline answers JSON + candidate answers JSON; (4) load prompts via runner.load_prompts(prompts_path); (5) make judge client via runner.make_judge_client(config); (6) evaluate_candidate → per-prompt records; (7) baseline_composite_list = [r["baseline_composite"] for r in records] — but NOTE: baseline scores come from a `_eval/baseline/<skill>/scores.json` cache file (lazy-populated); if missing, treat as first-run and write the candidate's pre-evaluated baseline values to the cache for next time; (8) decide_verdict(baseline_composite_list, candidate_composite_list, delta_threshold=..., per_prompt_threshold=..., min_prompts=...); (9) write report JSON to `_eval/reports/<patch_id>.json` (always) + `_eval/reports/<patch_id>.reject.json` if verdict != "pass"; (10) FINALLY block: revert_patch(files, repo_root) — runs even on exception. Return GateResult dataclass with verdict, exit_code, report_path, per_prompt records. Use `logging.getLogger(__name__)` for diagnostics; NEVER log raw answer text or API key (T-30-03).

    CLI:
    - `main(argv: list[str] | None = None) -> int`: argparse with flags `--patch`, `--skill`, `--baseline-answers`, `--candidate-answers`, `--config` (optional gate_config.yaml path), `--threshold-delta`, `--per-prompt-threshold`, `--min-prompts`, `--ab-positions`, `--judge-model`, `--prompts-dir`, `--rebuild-baseline` (NO-OP stub for Plan 01 — full impl in Plan 02; print "not yet implemented, see Plan 02" + exit 0 if passed alone, ignore otherwise), `--dry-run` (use runner._StubJudgeClient). Calls load_gate_config + run_gate, maps verdict to exit code via VERDICT_TO_EXIT, calls sys.exit(code). Return the int code (matches runner.main pattern).

    Create gate_config.yaml.example with committed defaults per CONTEXT.md:
    delta_threshold: 0.3, per_prompt_threshold: 1.0, min_prompts: 5, ab_positions: 2, judge_model: "claude-sonnet-4-6". Add a header comment documenting each field + where operator-local overrides go (gate_config.yaml, gitignored if diverges).

    Create tests/test_gate.py with the test classes enumerated in <behavior> above. Follow the sibling-import convention: `_EVAL_DIR = Path(__file__).resolve().parent.parent; sys.path.insert(0, str(_EVAL_DIR)); import gate; import runner`. Use pytest tmp_path fixture for temp git repos (subprocess `git init` in tmp_path, write a fake skills/movie-experts/screenplay/SKILL.md, create patches via `git diff`). MockJudgeClient class returns canned judge responses containing numeric dimension scores.

    Create tests/fixtures/ directory with 3 synthetic patch files (text files, not real git patches): `pass.patch` (touches only skills/movie-experts/screenplay/SKILL.md, well-formed header), `multi_skill.patch` (touches 2 different skill dirs — for Plan 02 multi-skill detection), `path_traversal.patch` (contains `+++ b/../../../etc/passwd` — security test).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest skills/movie-experts/_eval/tests/test_gate.py -x -v && ruff check skills/movie-experts/_eval/gate.py skills/movie-experts/_eval/runner.py && python -c "import sys; sys.path.insert(0, 'skills/movie-experts/_eval'); import gate; print('imports OK:', sorted(x for x in dir(gate) if not x.startswith('_')))"</automated>
  </verify>
  <done>
    - gate.py exposes: extract_patched_files, apply_patch, revert_patch, load_gate_config, decide_verdict, evaluate_candidate, generate_patch_id, run_gate, main, VERDICT_TO_EXIT
    - decide_verdict is a pure function returning deterministic verdicts for pass / fail_mean / fail_regression / inconclusive cases (GATE-02 + GATE-04)
    - apply_patch validates via `git apply --check` before mutating; revert_patch always runs in finally block (clean working tree on any exception)
    - Path-traversal in patch headers raises ValueError (T-30-01 mitigation)
    - load_gate_config applies defaults → YAML → CLI overrides in precedence order; validates ranges
    - gate accepts pre-generated baseline_answers + candidate_answers JSON (no live answer-gen — P30 scope fence)
    - All TestGate* tests green; existing test_runner.py + test_snapshot.py still green (no regressions)
    - gate_config.yaml.example committed with documented defaults (delta_threshold=0.3, per_prompt_threshold=1.0, min_prompts=5, ab_positions=2)
    - Ruff PLW1514 clean on gate.py + runner.py
    - Gate runs offline (no live LLM calls in unit tests — MockJudgeClient only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch file → gate.py | Untrusted unified diff text crosses here (candidate patch from P31 EVOL or operator-supplied) |
| gate_config.yaml → gate.py | Operator-controlled config (semi-trusted but validated) |
| judge API response → runner.parse_judge_scores | External LLM output, may be malformed or adversarial |
| subprocess (git apply / checkout) | OS-level subprocess invocation with patch-derived paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering / Elevation | extract_patched_files / patch header parsing | mitigate | Reject any `+++ b/<path>` line whose path does not start with `skills/movie-experts/` or contains `..`. Raise ValueError with clear message. Prevents path traversal outside the skills tree. |
| T-30-02 | Tampering | apply_patch / revert_patch subprocess calls | mitigate | Pass patch path + file list as separate argv elements to `subprocess.run([...])`. Never `shell=True`. Never f-string-interpolate paths into a shell command. |
| T-30-03 | Information Disclosure | gate.py logging + report JSON | mitigate | Never log raw judge responses, raw answer text, or OPENROUTER_API_KEY. Reject-log per_prompt entries contain only composite scores + deltas (not full text per RESEARCH V8 control). |
| T-30-04 | Denial of Service | load_gate_config | mitigate | Validate delta_threshold in [0, 5], per_prompt_threshold in [0, 5], min_prompts >= 1, ab_positions in {1, 2} on load. Raise ValueError fail-fast (matches runner.py WR-04 pattern). Prevents malformed config from crashing mid-run. |
| T-30-05 | Tampering | parse_judge_scores / judge response | mitigate | Bounded regex per dimension (no ReDoS — fixed simple pattern). Validate parsed float in [1.0, 5.0]; silently drop out-of-range. Empty dict on no-match (fail-safe). |
| T-30-06 | Repudiation | gate.py report writes | accept | Reports written to `_eval/reports/<patch_id>.json` are append-only JSON with patch_sha256 + ts + judge_model provenance. Tamper-evidence is P32 CURATE-03 scope (audit log), not P30. |
| T-30-SC | Tampering | (N/A — no new packages installed) | accept | Phase 30 installs zero new packages (all deps already pinned in pyproject.toml). slopcheck N/A. |
</threat_model>

<verification>
Per-task automated checks above. Phase-level gate:

1. `python -m pytest skills/movie-experts/_eval/tests/ -v` — full _eval test suite green (test_runner + test_snapshot + test_gate)
2. `ruff check skills/movie-experts/_eval/gate.py skills/movie-experts/_eval/runner.py` — PLW1514 clean
3. `grep -rn "from _eval\|import _eval" agent/ hermes_cli/ tools/ gateway/ cli.py run_agent.py 2>/dev/null | wc -l` — returns 0 (Hermes runtime isolation preserved)
4. `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` — returns 0 (FOUND-08 byte-intact: no bundled SKILL.md or refs changed)
5. `python skills/movie-experts/_eval/gate.py --help` — exits 0, prints usage
6. `python skills/movie-experts/_eval/gate.py --dry-run --patch skills/movie-experts/_eval/tests/fixtures/pass.patch --skill screenplay --baseline-answers /tmp/empty.json --candidate-answers /tmp/empty.json` — dry-run completes without live API call (stub judge)
</verification>

<success_criteria>
- GATE-01 (partial): gate.run_gate() accepts a candidate patch + pre-generated answers, applies patch, evaluates against baseline, writes report, reverts patch, returns verdict. (Full GATE-01 including `--rebuild-baseline` lands in Plan 02.)
- GATE-02 (complete): decide_verdict() rejects patches with mean_delta < -delta_threshold (default 0.3); reject-log emits score delta breakdown.
- GATE-04 (complete): decide_verdict() rejects patches with ANY per-prompt delta < -per_prompt_threshold (default 1.0), even when mean is acceptable.
- parse_judge_scores() extension to runner.py SHIPPED (load-bearing per RESEARCH.md Pitfall 1) — backward-compatible (existing test_runner.py tests still pass).
- Patch apply + revert mechanics specified and verified via `git apply --check` + `git apply` + `git checkout --` (stdlib subprocess only).
- Hermes runtime isolation verified (grep returns 0).
- FOUND-08 byte-intact verified (no bundled SKILL.md or refs touched).
- Paired-t significance deferred to Plan 02 (GATE-03 scope).
- A/B position-swap mechanics REUSED from runner.run_position_swap (GATE-03 significance report lands in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/30-eval-gate-reuse/30-01-SUMMARY.md` when done.
</output>

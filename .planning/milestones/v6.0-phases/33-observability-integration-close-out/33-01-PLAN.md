---
phase: 33-observability-integration-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hermes_cli/curator.py
  - tests/hermes_cli/test_curator_stats.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
user_setup: []

must_haves:
  truths:
    - "Operator runs `hermes curator stats <skill_id>` and sees verdict-bucket counts + patch history + eval-score trend for that skill"
    - "Operator runs `hermes curator stats --all` and sees top-N negative-feedback skills + zero-feedback skill list across the bundled movie-experts"
    - "Operator runs `hermes curator stats --by-source` and sees CLI / kais_aigc / manual verdict distribution"
    - "Operator runs `hermes curator stats --json ...` and receives machine-readable counts (no correction text leakage)"
    - "Operator runs `hermes curator stats` on an empty FeedbackStore and receives exit 0 + friendly 'no feedback yet' message"
  artifacts:
    - path: "hermes_cli/curator.py"
      provides: "_cmd_stats handler + stats subparser wired into register_cli (mirrors P32 pattern)"
      contains: "_cmd_stats"
    - path: "tests/hermes_cli/test_curator_stats.py"
      provides: "CLI smoke + aggregation helper tests for OBS-01/02/03"
      contains: "class TestStatsPerSkill"
  key_links:
    - from: "hermes_cli/curator.py:_cmd_stats"
      to: "agent/feedback_store.py:FeedbackStore.summary"
      via: "lazy in-handler import + summary(skill_id=..., source=...) call"
      pattern: "from agent\\.feedback_store import FeedbackStore"
    - from: "hermes_cli/curator.py:_cmd_stats"
      to: "agent/curator_audit.py:read_audit"
      via: "lazy in-handler import + read_audit(action=\"apply\", skill=...) call"
      pattern: "from agent\\.curator_audit import read_audit"
    - from: "hermes_cli/curator.py:_cmd_stats"
      to: "agent/evolution/__init__.py:read_queue"
      via: "lazy in-handler import + read_queue(status=\"applied\") call"
      pattern: "from agent\\.evolution import read_queue"
---

<objective>
Implement `hermes curator stats` — a read-only observability surface (OBS-01/02/03) that aggregates over the three shipped data stores (P29 FeedbackStore + P31 evolution queue + P32 audit log). Pure presentation layer: no new persistence, no new schema, no new dependencies. `rich==14.3.3` already in stack.

Purpose: Operators can finally see feedback-driven learning health. Without this, v6.0 ships a learning loop with no way to inspect whether it is actually learning. Mirrors P32 `register_cli` extension pattern exactly (lazy in-handler imports — zero module-level `agent.evolution` imports).
Output: One modified file (`hermes_cli/curator.py` — additive `_cmd_stats` + subparser) + one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-observability-integration-close-out/33-CONTEXT.md
@.planning/phases/33-observability-integration-close-out/33-RESEARCH.md

<interfaces>
<!-- Shipped query APIs (read end-to-end — do NOT re-implement): -->

From agent/feedback_store.py:990 (summary):
```python
def summary(
    self,
    *,
    skill_id: str | None = None,
    source: str | None = None,
    verdict: str | None = None,
) -> dict[str, dict]:
    """Return {bucket_key: {count, weighted_count, first_ts, last_ts}}.
    bucket_key format: "<skill_id>:<source>:<verdict>".
    Supersession-aware (demoted records excluded from count)."""
```

From agent/curator_audit.py:345 (read_audit):
```python
def read_audit(
    *,
    action: str | None = None,
    skill: str | None = None,
    since: str | None = None,
) -> list[dict]:
    """Read ~/.hermes/skills/.audit/log.jsonl with optional filters.
    Each entry: {ts, action, patch_id, skill_id, operator,
                 feedback_ids, eval_score, commit_sha, prev_sha, entry_sha}.
    since is ISO date; naive -> promoted to aware UTC."""
```

From agent/evolution/__init__.py (read_queue — P31):
```python
def read_queue(
    *,
    evolution_dir: Path | None = None,
    status: str = "pending",
) -> list[PatchRecord]:
    """Read {pending,applied,rejected}.jsonl from evolution/ dir.
    status="applied" returns PatchRecord list with patch_id + skill_id + timestamps."""
```

From hermes_cli/curator.py:994-1005 (P32 register_cli insertion point):
```python
p_auto = subs.add_parser("auto-apply-eligible", ...)
p_auto.add_argument("--dry-run", ...)
p_auto.set_defaults(func=_cmd_auto_apply_eligible)
# <-- INSERT new p_stats subparser block AFTER p_auto
```

From hermes_cli/curator.py:632-668 (_cmd_audit_log — empty-store pattern to mirror):
```python
def _cmd_audit_log(args) -> int:
    ...
    if not entries:
        print("(no audit entries match)")
        return 0
    for e in entries:
        ...
    return 0
```
</interfaces>

# Locked decisions honored:
# - D-stats-format: rich.table.Table for human output; --json for machine (CONTEXT.md line 28)
# - D-runs-default: --runs default 10 (CONTEXT.md line 33)
# - D-json-counts-only: --json emits counts only — NO correction text (CONTEXT.md line 29; research OQ#4 RESOLVED)
# - D-empty-store: exit 0 + friendly message, mirrors P32 _cmd_audit_log (research OQ#2 RESOLVED)
# - D-sparkline: unicode block chars "▁▂▃▄▅▆▇█" for mean_delta trend column (CONTEXT.md line 35)
# - D-read-only: stats MUST NOT mutate any store (CONTEXT.md line 113)
# - D-lazy-imports: ALL agent.* imports INSIDE _cmd_stats body (P32 runtime-isolation invariant)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Create test_curator_stats.py with all OBS-01/02/03 test classes (RED state)</name>
  <files>tests/hermes_cli/test_curator_stats.py</files>
  <read_first>
    - tests/hermes_cli/test_curator_cli.py (P32 CLI test pattern — class-per-subcommand, register_cli subparser assertion)
    - agent/feedback_store.py:919-1050 (summary() + query() signatures — do NOT re-implement)
    - agent/curator_audit.py:345-430 (read_audit signature + filters)
    - tests/conftest.py:496-522 (_live_system_guard — must mock find_gateway_pids / os.kill if any subprocess)
  </read_first>
  <behavior>
    - TestStatsPerSkill: populates a tmp FeedbackStore with 3 verdicts (good/needs_work/bad) + verifies CLI output contains the 3 verdict labels + counts via capsys
    - TestStatsAll: populates multi-skill store + verifies --all emits top-N negative + zero-feedback sections
    - TestStatsBySource: populates 2 sources (cli + kais_aigc) + verifies --by-source emits per-source verdict rows
    - TestRunsFlag: seeds 15 audit-log apply entries with eval_score + verifies `--runs 5` limits trend to last 5
    - TestJsonOutput: runs `--json` + json.loads(stdout) + asserts each entry has "count" key + asserts "correction" key absent in any entry (counts-only per D-json-counts-only)
    - TestEmptyStore: empty FeedbackStore + asserts exit code 0 + asserts stdout contains "no feedback yet" + suggests /feedback or `hermes feedback import` (mirrors P32 _cmd_audit_log empty path at curator.py:659-661)
    - TestReadOnly: snapshots ~/.hermes/skills/.feedback/ file list before + after stats invocation + asserts zero file additions/removals/mutations (verifies D-read-only)
    - TestLazyImportIsolation: AST-walks hermes_cli/curator.py module body + asserts NO module-level `from agent.evolution` or `import agent.evolution` statements (mirror P31 TestNonBypassableHumanInLoop pattern, runtime-isolation invariant per P32 SC)
  </behavior>
  <action>
    Create the new test file with one test class per behavior bullet above. Use `tmp_path` + `monkeypatch.setenv("HERMES_HOME", str(tmp_path))` for FeedbackStore isolation. For TestStatsPerSkill/All/BySource construct a FeedbackStore directly via `agent.feedback_store.FeedbackStore(hermes_home=tmp_path)` and call record_feedback() to seed verdicts (do NOT go through CLI to populate). Invoke _cmd_stats by constructing an argparse.Namespace matching the subparser fields (skill_id, all_skills, by_source, top, runs, skill_filter, as_json) — this avoids argparse boilerplate. For TestLazyImportIsolation use `ast.parse(Path("hermes_cli/curator.py").read_text(encoding="utf-8"))` + walk only top-level body (not inside function defs) for `ast.Import`/`ast.ImportFrom` nodes whose module starts with "agent.evolution". Test file must have `from __future__ import annotations` + every `open()` must pass `encoding="utf-8"` (Ruff PLW1514). Tests MUST fail at end of this task (handler does not exist yet) — RED state is the success criterion.
  </action>
  <verify>
    <automated>python -m pytest tests/hermes_cli/test_curator_stats.py -x 2>&1 | tail -5</automated>
    Expected: collection succeeds; all tests fail with ImportError or AttributeError referencing _cmd_stats (RED).
  </verify>
  <done>
    Test file exists with all 8 test classes. pytest collection succeeds (no syntax errors). All tests fail because _cmd_stats / stats subparser does not exist yet. No module-level agent.* imports introduced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _cmd_stats + stats subparser in hermes_cli/curator.py (GREEN state)</name>
  <files>hermes_cli/curator.py</files>
  <read_first>
    - hermes_cli/curator.py:632-668 (_cmd_audit_log — empty-store exit-0 pattern to mirror)
    - hermes_cli/curator.py:671-730 (_cmd_auto_apply_eligible — lazy-import + delegation pattern)
    - hermes_cli/curator.py:994-1005 (P32 register_cli insertion point — p_auto block, insert p_stats AFTER)
    - hermes_cli/curator.py:1-30 (module header — verify no existing agent.evolution module-level imports)
    - hermes_cli/bundles.py:52-60 (existing rich.table.Table pattern)
    - agent/feedback_store.py:990-1022 (summary() return shape — bucket_key format)
  </read_first>
  <behavior>
    All 8 test classes from Task 1 transition RED → GREEN:
    - Per-skill dashboard renders 3 verdict rows with correct counts
    - Cross-skill --all view renders top-N negative + zero-feedback
    - --by-source renders per-source verdict distribution
    - --runs N limits trend depth to last N audit entries
    - --json emits counts only (no "correction" field anywhere)
    - Empty store → exit 0 + "no feedback yet" + suggests /feedback
    - Read-only: zero filesystem mutation in ~/.hermes/skills/.feedback/
    - AST-walk: zero module-level agent.evolution imports in hermes_cli/curator.py
  </behavior>
  <action>
    Add a new `_cmd_stats(args) -> int` handler BELOW the existing `_cmd_auto_apply_eligible` definition (around curator.py:730). The handler body MUST contain ALL three lazy imports INSIDE the function (never at module level): `from agent.feedback_store import FeedbackStore`, `from agent.curator_audit import read_audit`, `from agent.evolution import read_queue`. Dispatch on args: if `args.all_skills` → call a private `_render_cross_skill_view(store, top_n=args.top, as_json=args.as_json)`; elif `args.by_source` → `_render_source_breakdown(store, skill_filter=args.skill_filter, as_json=args.as_json)`; elif `args.skill_id` → `_render_per_skill_dashboard(store, read_audit, read_queue, skill_id=args.skill_id, runs=args.runs, as_json=args.as_json)`; else → print help-style usage hint and return 0 (no args = ambiguous). Build FeedbackStore via `FeedbackStore()` (default hermes_home) — do NOT pass root= (P32 Rule 1 already established that FeedbackStore signature uses hermes_home kwarg). For per-skill: call `store.summary(skill_id=skill_id)` and parse bucket keys via `key.split(":")` to collapse sources per verdict. Pull eval trend via `read_audit(action="apply", skill=skill_id)` then filter entries where `e.get("eval_score")` truthy, take last `runs` entries. Pull patch history via `read_queue(status="applied")` then filter `p.skill_id == skill_id`. Render human path with rich.table.Table — verdict color map good=green / needs_work=yellow / bad=red. Render sparkline via a private `_sparkline(values: list[float]) -> str` helper using `_SPARK = "▁▂▃▄▅▆▇█"` (8 buckets, lo/hi normalization, all-identical → index 4). JSON path: emit a dict with keys like {"skill_id": ..., "verdict_buckets": {"good": N, "needs_work": N, "bad": N}, "patch_count": N, "eval_trend_count": N} — COUNTS ONLY, NEVER include correction text, feedback_ids of operator-authored content, or output_snapshots. Cross-skill view: iterate `store.summary()` (all buckets) → parse "<skill>:<source>:<verdict>" → tally per_skill dict → compute neg_count = needs_work + bad → top-N sorted desc → zero-feedback = bundled experts not appearing in per_skill keys (read the bundled expert_id list from skills/movie-experts/ directory scan at hermes_home, OR fall back to "no bundled-skill manifest available" footer if scan fails). Source breakdown: for source in ("cli", "kais_aigc", "manual") call `store.summary(source=source, skill_id=skill_filter)` and tally verdicts across buckets. Empty-store branch: if `not summary` (empty dict) for the relevant query → print friendly message ("no feedback yet — run /feedback in a Hermes conversation or `hermes feedback import <jsonl>` to seed data") and return 0 (D-empty-store, mirrors curator.py:659-661). Then wire the subparser: in register_cli AFTER the p_auto block (curator.py:1005), add p_stats = subs.add_parser("stats", help="Per-skill / cross-skill feedback stats (read-only; OBS-01/02/03)"). Add args: positional skill_id (nargs="?", default=None), --all (dest="all_skills", action="store_true"), --by-source (dest="by_source", action="store_true"), --top (type=int, default=10), --runs (type=int, default=10), --skill (dest="skill_filter", default=None), --json (dest="as_json", action="store_true"). set_defaults(func=_cmd_stats). Do NOT add any module-level imports — verify via the TestLazyImportIsolation test.
  </action>
  <verify>
    <automated>python -m pytest tests/hermes_cli/test_curator_stats.py -x && ruff check hermes_cli/curator.py</automated>
  </verify>
  <done>
    All 8 test classes GREEN. Ruff PLW1514 passes (every open() has encoding="utf-8"). No module-level agent.evolution imports introduced (TestLazyImportIsolation passes). `hermes curator stats --help` lists all 6 flags. CLI exits 0 on empty store with friendly message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → CLI argparse | Untrusted args cross here (--runs N, skill_id positional) |
| CLI → ~/.hermes/skills/.feedback/ (P29 store) | Read-only query path (MUST NOT mutate) |
| CLI → ~/.hermes/skills/.audit/log.jsonl (P32 audit) | Read-only query path |
| CLI → ~/.hermes/skills/.feedback/evolution/*.jsonl (P31 queue) | Read-only query path |
| CLI stdout → operator / downstream pipeline | --json output crosses here (counts-only leakage boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | _cmd_stats --json output | mitigate | Per D-json-counts-only: emit ONLY counts (verdict_buckets, patch_count, eval_trend_count). NEVER include correction text, output_snapshot, or feedback_ids (could contain operator PII). TestJsonOutput asserts no "correction" key in any --json entry. |
| T-33-02 | Tampering | _cmd_stats → FeedbackStore / audit / queue | mitigate | Stats handler is READ-ONLY per CONTEXT.md line 113. Never calls record_feedback / append_audit / queue mutation. TestReadOnly snapshots filesystem state before/after to enforce. |
| T-33-03 | Tampering | _cmd_stats triggering FeedbackStore.__init__ migration side-effect | mitigate | Per RESEARCH Pitfall 4: FeedbackStore.__init__ runs _maybe_migrate_phase28_incoming only if incoming/ has files. Production safe after P29 has run. Tests use tmp_path with empty incoming/ to avoid the side-effect path. |
| T-33-04 | Denial of Service | Malformed bucket file crashing stats | accept | FeedbackStore._read_bucket_records already skips malformed lines with warning (verified feedback_store.py:664-678). Re-implementing fault tolerance is out of scope. |
| T-33-05 | Repudiation | Empty store exit code | mitigate | Empty store returns exit 0 with informative stdout (mirrors P32 _cmd_audit_log) — operator sees a clear "no feedback yet" signal, not a silent pass. |
| T-33-06 | Tampering | Module-level agent.evolution import breaking P31 runtime-isolation invariant | mitigate | TestLazyImportIsolation AST-walks module body asserting zero module-level agent.evolution imports. All imports inside _cmd_stats function body. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed (rich already pinned at 14.3.3). No supply-chain surface. |
</threat_model>

<verification>
- All TestStats* classes in test_curator_stats.py pass
- TestReadOnly passes (zero filesystem mutation)
- TestLazyImportIsolation passes (zero module-level agent.evolution imports)
- TestJsonOutput passes (no correction text leakage)
- TestEmptyStore passes (exit 0 + friendly message)
- `hermes curator stats --help` lists all 6 flags
- `ruff check hermes_cli/curator.py` clean
- `hermes curator stats` on real ~/.hermes (if populated) renders meaningfully (manual smoke — see VALIDATION.md Manual-Only)
- FOUND-08 preserved (this plan does NOT touch skills/movie-experts/)
- Runtime isolation: `grep -c "^from agent\.evolution\|^import agent\.evolution" hermes_cli/curator.py` returns 0
</verification>

<success_criteria>
- OBS-01 satisfied: `hermes curator stats <skill_id>` renders verdict buckets + patch history + eval-score trend (configurable --runs)
- OBS-02 satisfied: `hermes curator stats --all` renders top-N negative + zero-feedback list
- OBS-03 satisfied: `hermes curator stats --by-source` renders per-source verdict distribution
- --json emits counts only (no correction text)
- Empty store returns exit 0 with friendly message
- Read-only enforced (no state mutation)
- P31 runtime-isolation invariant preserved (zero module-level agent.evolution imports)
- All tests green; Ruff clean
</success_criteria>

<output>
Create `.planning/phases/33-observability-integration-close-out/33-01-SUMMARY.md` when done
</output>

---
phase: 33-observability-integration-close-out
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - skills/movie-experts/README.md
  - skills/movie-experts/_shared/glossary.md
  - tests/hermes_cli/test_curator_stats.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
user_setup: []

must_haves:
  truths:
    - "skills/movie-experts/README.md corpus tree (_shared/ block) lists v6-feedback-loop-architecture.md alongside existing v4/v5 refs"
    - "skills/movie-experts/_shared/glossary.md has a new v6.0 section with 4 H3 bilingual entries (Feedback Ingestion / Knowledge Evolution / Eval Gate / Curator Proposal)"
    - "Each glossary entry uses EN-first format per CONTEXT.md + cross-references the architecture doc"
    - "SC-7 milestone-wide FOUND-08 check passes: zero bundled SKILL.md / non-_eval non-_shared changes vs v5.0"
    - "SC-8 v5/v4 refs byte-intact check passes: snowflake / e-konte / scamper / dreamina-cli-baseline / v86-pipeline-mapping all unchanged vs v5.0"
    - "A footer note in glossary.md explains the EN-first format shift for v6 entries (existing CN-first entries remain byte-intact)"
  artifacts:
    - path: "skills/movie-experts/README.md"
      provides: "Corpus tree _shared/ block updated with v6 ref entry"
      contains: "v6-feedback-loop-architecture.md"
    - path: "skills/movie-experts/_shared/glossary.md"
      provides: "4 new H3 bilingual entries under a new v6.0 additions section"
      contains: "### Feedback Ingestion / 知识反馈采集"
    - path: "tests/hermes_cli/test_curator_stats.py"
      provides: "TestReadmeCorpusTree + TestGlossaryEntries verification tests"
      contains: "class TestReadmeCorpusTree"
  key_links:
    - from: "skills/movie-experts/_shared/glossary.md:v6 entries"
      to: "skills/movie-experts/_shared/v6-feedback-loop-architecture.md"
      via: "Each entry's Context: bullet cross-references the doc section"
      pattern: "v6-feedback-loop-architecture\\.md"
    - from: "skills/movie-experts/README.md:corpus tree"
      to: "skills/movie-experts/_shared/v6-feedback-loop-architecture.md"
      via: "_shared/ block now lists the new ref"
      pattern: "v6-feedback-loop-architecture\\.md"
---

<objective>
Complete the SC-6 + SC-7 + SC-8 deliverables: README corpus tree update + glossary 4 new bilingual entries + milestone-wide byte-intact checks. This is the final integration close-out mirroring v5.0 Phase 27.

Purpose: Operators navigating the corpus tree need to discover the new v6 architecture doc; new v6 terminology (Feedback Ingestion / Knowledge Evolution / Eval Gate / Curator Proposal) must be glossary-defined for onboarding; milestone-wide byte-intact checks prove the v6.0 work honored the scope-discipline invariant (no v5/v4 ref corruption, no FOUND-08 break).
Output: README + glossary modified (append-only); verification tests appended; SC-7 + SC-8 byte-intact check commands documented and executable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-observability-integration-close-out/33-CONTEXT.md
@.planning/phases/33-observability-integration-close-out/33-RESEARCH.md
@.planning/phases/33-observability-integration-close-out/33-01-SUMMARY.md
@.planning/phases/33-observability-integration-close-out/33-02-SUMMARY.md

<interfaces>
<!-- README.md corpus tree _shared/ block (lines 417-424 — current state): -->
```
└── _shared/
    ├── glossary.md                               (EN↔CN term dictionary — Phase 7 expanded)
    ├── known-external-models.yaml                (model name allowlist — Phase 7 expanded)
    ├── platform-comparison.md
    ├── RAG-INVOCATION-PATTERN.md
    ├── SKILL-LAYOUT.md                           (reference anatomy spec)
    ├── cognitive-resonance-metrics.md            (Phase 7C-1 NEW — 4-scale evaluation rubric)
    └── quality-rubric.md                         (Phase 7C-2 NEW — 6-dim publish-gate rubric)
```

<!-- Latent gap (research A4): block does NOT list dreamina-cli-baseline.md or v86-pipeline-mapping.md -->
<!-- D-no-backfill (research OQ#1 RESOLVED): do NOT backfill v5.0 entries — they are documented -->
<!-- in the v5.0 summary section. Only add the v6 entry. -->

<!-- Existing glossary H3 format (CN-first, established): -->
`### 运镜 / cinematography / camera movement`
**CN:** ... **EN:** ... **Context:** ...

<!-- v6 H3 format (EN-first per CONTEXT.md line 50-54): -->
`### Feedback Ingestion / 知识反馈采集`
**CN:** ... **EN:** ... **Context:** ... [cross-ref to v6-feedback-loop-architecture.md §X]

<!-- SC-7 byte-intact command (from CONTEXT.md specifics line 118): -->
git diff v5.0..HEAD -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l
# Expected: 0 (only _eval/ + _shared/ additions permitted)

<!-- SC-8 byte-intact command (from ROADMAP.md Phase 33 SC #8): -->
git diff v5.0..HEAD -- skills/movie-experts/_shared/{snowflake-method,e-konte-format,scamper-variations,dreamina-cli-baseline,v86-pipeline-mapping}.md | wc -l
# Expected: 0 (5 v4/v5 refs unchanged)
</interfaces>

# Locked decisions honored:
# - D-glossary-4-terms: Feedback Ingestion / Knowledge Evolution / Eval Gate / Curator Proposal (CONTEXT.md line 50-54)
# - D-glossary-en-first: EN-first format for v6 entries (CONTEXT.md examples) — footer note explains shift (research Pitfall 3)
# - D-glossary-cross-ref: each entry cross-references architecture doc (CONTEXT.md specifics line 116)
# - D-no-backfill: do NOT backfill v5.0 entries to corpus tree (research OQ#1 RESOLVED — v5.0 entries are documented in summary section)
# - D-corpus-tree-insertion: alphabetical by English term OR append — CONTEXT.md discretion; choose append-after-existing to preserve order
# - D-sc7-sc8-commands: commands run as part of phase verification (CONTEXT.md specifics line 118)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append TestReadmeCorpusTree + TestGlossaryEntries + TestByteIntactChecks to test_curator_stats.py (RED state)</name>
  <files>tests/hermes_cli/test_curator_stats.py</files>
  <read_first>
    - tests/hermes_cli/test_curator_stats.py (Plan 01 + Plan 02 file — append at end)
    - skills/movie-experts/README.md:415-425 (_shared/ corpus tree block)
    - skills/movie-experts/_shared/glossary.md:1-30 (How to use + Core terms structure)
    - skills/movie-experts/_shared/glossary.md:last 60 lines (Phase 27 INTEGRATION-05 verification footer pattern)
  </read_first>
  <behavior>
    - TestReadmeCorpusTree.test_v6_ref_listed: README contains a line listing "v6-feedback-loop-architecture.md" within the _shared/ corpus tree block (within 20 lines of "_shared/" header)
    - TestReadmeCorpusTree.test_v5_entries_not_backfilled_documented: README does NOT need to add dreamina-cli-baseline.md or v86-pipeline-mapping.md to the corpus tree (D-no-backfill); test only verifies v6 entry added
    - TestGlossaryEntries.test_v6_section_header_present: glossary contains a new H2 section header mentioning v6.0 / v6 additions (e.g., "## v6.0 additions (4 new feedback-loop terms)")
    - TestGlossaryEntries.test_4_h3_entries_present: glossary contains H3 headers for all 4 terms in EN-first format: "### Curator Proposal / 策展提案", "### Eval Gate / 评估闸门", "### Feedback Ingestion / 知识反馈采集", "### Knowledge Evolution / 知识进化"
    - TestGlossaryEntries.test_each_entry_has_cn_en_context_subsections: each of the 4 v6 H3 entries has **CN:** + **EN:** + **Context:** subsection markers (mirrors existing glossary convention)
    - TestGlossaryEntries.test_each_entry_cross_refs_architecture_doc: each v6 entry's Context subsection mentions "v6-feedback-loop-architecture.md" (D-glossary-cross-ref)
    - TestGlossaryEntries.test_existing_entries_byte_intact: spot-check 3 existing CN-first H3 entries (e.g., "### 运镜 / cinematography / camera movement") still present unchanged (do NOT flip to EN-first — research Pitfall 3 warning)
    - TestGlossaryEntries.test_format_shift_footer_note_present: glossary has a footer note explaining "v6.0 entries use EN-first format per Phase 33 CONTEXT.md; earlier entries retain CN-first for backward compatibility" (or equivalent wording)
    - TestByteIntactChecks.test_sc7_bundled_skill_unchanged: runs `git diff v5.0..HEAD -- skills/movie-experts/` via subprocess + filters through grep -v _eval + grep -v _shared + asserts output line count is 0 (subprocess.run with shell=False; split via shlex; pipeline via Python)
    - TestByteIntactChecks.test_sc8_v5_v4_refs_unchanged: runs `git diff v5.0..HEAD -- <5 explicit v4/v5 ref paths>` and asserts output length is 0
  </behavior>
  <action>
    Append three new test classes (TestReadmeCorpusTree, TestGlossaryEntries, TestByteIntactChecks) to test_curator_stats.py. For TestReadmeCorpusTree + TestGlossaryEntries: read the relevant file via Path.read_text(encoding="utf-8") and assert substring presence. For TestByteIntactChecks: use subprocess.run(["git", "diff", "--quiet", "v5.0..HEAD", "--", "<path>"], capture_output=True) — non-zero exit means diff exists (i.e., file changed). For SC-7 use `git diff --name-only v5.0..HEAD -- skills/movie-experts/` (capture_output=True, encoding="utf-8"), split stdout by lines, filter out lines containing "_eval/" or "_shared/", assert resulting list is empty. For SC-8 list the 5 v4/v5 ref paths explicitly and run a single git diff --name-only on them, assert empty. All opens pass encoding="utf-8". Tests fail RED because README/glossary not yet updated.
  </action>
  <verify>
    <automated>python -m pytest tests/hermes_cli/test_curator_stats.py::TestReadmeCorpusTree tests/hermes_cli/test_curator_stats.py::TestGlossaryEntries tests/hermes_cli/test_curator_stats.py::TestByteIntactChecks -x 2>&1 | tail -8</automated>
    Expected: TestReadmeCorpusTree + TestGlossaryEntries fail (RED); TestByteIntactChecks may pass already if prior phases honored scope (note this in test result — pre-existing green is fine).
  </verify>
  <done>
    Three new test classes appended. Collection succeeds. TestReadmeCorpusTree + TestGlossaryEntries fail (files not updated). TestByteIntactChecks should pass if prior phases honored scope discipline (informational).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update README corpus tree + append 4 glossary entries + run SC-7/SC-8 milestone checks (GREEN state)</name>
  <files>skills/movie-experts/README.md, skills/movie-experts/_shared/glossary.md</files>
  <read_first>
    - skills/movie-experts/README.md:415-425 (corpus tree _shared/ block — insertion point)
    - skills/movie-experts/_shared/glossary.md (FULL — find insertion point: AFTER last existing section + its footer verification line; the new v6 section is a new H2)
    - skills/movie-experts/_shared/v86-pipeline-mapping.md (cross-reference target — confirm section names for glossary Context: bullets)
    - .planning/REQUIREMENTS.md (INGEST/EVOL/GATE/CURATE definitions — source for CN+EN glossary prose)
  </read_first>
  <behavior>
    All TestReadmeCorpusTree + TestGlossaryEntries + TestByteIntactChecks tests transition to GREEN.
  </behavior>
  <action>
    (1) Modify skills/movie-experts/README.md: within the `_shared/` corpus tree block (around line 417-424), insert a new line listing v6-feedback-loop-architecture.md. Insertion point: after the existing `quality-rubric.md` line (last current entry in the block). Use the same tree-character format (├── or └── depending on insertion position; if inserting at end, change the previous `└──` to `├──` and add the new line with `└──`). Annotation format mirrors existing: "v6-feedback-loop-architecture.md  (Phase 33 v6.0 NEW — feedback-loop canonical architecture ref)". Do NOT backfill dreamina-cli-baseline.md or v86-pipeline-mapping.md per D-no-backfill. (2) Modify skills/movie-experts/_shared/glossary.md: append a new H2 section AFTER the last existing section's footer line. Header: `## v6.0 additions (4 new feedback-loop terms)`. Brief intro line: "Phase 33 (Observability + Integration Close-out) adds 4 terms documenting the v6.0 feedback-driven learning paradigm. v6.0 entries use EN-first header format per Phase 33 CONTEXT.md; earlier entries retain CN-first format for backward compatibility." Then 4 H3 entries in alphabetical order by English term (per CONTEXT.md discretion), each with **CN:** / **EN:** / **Context:** subsections. Term content (synthesized from REQUIREMENTS.md + ROADMAP.md): (a) `### Curator Proposal / 策展提案` — CN/EN describing agent/curator.py extension (P32) that auto-triggers EVOL pipeline when negative feedback crosses threshold; bundled NEVER auto-apply (CURATE-05); Context bullet cross-refs v6-feedback-loop-architecture.md §Human-in-Loop Boundaries + cites REQUIREMENTS CURATE-01/02. (b) `### Eval Gate / 评估闸门` — describes patch-vs-baseline MT-Bench position-swap harness (P30) reusing _eval/runner.py; thresholds δ=0.3 mean / 1.0 per-prompt; Context cross-refs §Eval-Gate Thresholds + cites GATE-01..04. (c) `### Feedback Ingestion / 知识反馈采集` — describes multi-source intake (CLI /feedback + kais-aigc watcher + manual import) normalizing to FeedbackRecord schema (P28); Context cross-refs §JSON Schema Reference + cites INGEST-01..05. (d) `### Knowledge Evolution / 知识进化` — describes LLM-aggregated candidate insights → unified-diff patches → review queue → human approve → apply+rollback pipeline (P31/P32); Context cross-refs §Data Flow + cites EVOL-01..05. Each Context bullet must mention "v6-feedback-loop-architecture.md" by filename. After the 4 entries add a footer line: "*Phase 33 INTEGRATION close-out verification: 4 / 4 new H3 entries added (Curator Proposal / Eval Gate / Feedback Ingestion / Knowledge Evolution), each with CN/EN/Context + cross-reference to v6-feedback-loop-architecture.md. v6.0 entries use EN-first format; pre-v6 entries retain CN-first.*". (3) After file edits, run the SC-7 and SC-8 verification commands manually (git diff ... | wc -l) to confirm both return 0 — this is a sanity check; the tests will also assert this. Do NOT modify any existing CN-first H3 entries (would break TestGlossaryEntries.test_existing_entries_byte_intact + SC-7 byte-intact).
  </action>
  <verify>
    <automated>python -m pytest tests/hermes_cli/test_curator_stats.py::TestReadmeCorpusTree tests/hermes_cli/test_curator_stats.py::TestGlossaryEntries tests/hermes_cli/test_curator_stats.py::TestByteIntactChecks -x && git diff --name-only v5.0..HEAD -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l | grep -q '^0$' && git diff --quiet v5.0..HEAD -- skills/movie-experts/_shared/snowflake-method.md skills/movie-experts/_shared/e-konte-format.md skills/movie-experts/_shared/scamper-variations.md skills/movie-experts/_shared/dreamina-cli-baseline.md skills/movie-experts/_shared/v86-pipeline-mapping.md</automated>
  </verify>
  <done>
    README corpus tree _shared/ block lists v6-feedback-loop-architecture.md (no v5.0 backfill). Glossary has new v6.0 section with 4 EN-first H3 entries, each with CN/EN/Context + cross-ref to architecture doc + footer note explaining format shift. Existing CN-first entries unchanged. SC-7 check returns 0 lines (only _eval/ + _shared/ additions). SC-8 check returns 0 lines (5 v4/v5 refs unchanged). All tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README corpus tree → operator discovery | Doc navigability; missing entries hide docs from new operators |
| Glossary → operator onboarding | Terminology reference; inconsistent format hinders lookup |
| Existing CN-first glossary entries → SC-7 byte-intact invariant | MUST NOT be edited (would break milestone-wide preservation check) |
| v4/v5 _shared refs → SC-8 byte-intact invariant | MUST remain unchanged vs v5.0 baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-11 | Tampering | Existing CN-first glossary entries flipped to EN-first | mitigate | TestGlossaryEntries.test_existing_entries_byte_intact spot-checks 3 CN-first entries unchanged. SC-7 byte-intact check also catches any non-_eval non-_shared changes. Do NOT edit existing entries. |
| T-33-12 | Tampering | v4/v5 _shared refs accidentally modified | mitigate | TestByteIntactChecks.test_sc8_v5_v4_refs_unchanged asserts git diff on 5 explicit paths returns empty. SC-8 in VALIDATION.md enforces. |
| T-33-13 | Tampering | SC-7 false-pass from over-broad grep filter | mitigate | TestByteIntactChecks.test_sc7_bundled_skill_unchanged uses git diff --name-only + Python-side filter (grep -v _eval/ + grep -v _shared/), asserts resulting list empty. Verifies the exact CONTEXT.md line 118 command. |
| T-33-14 | Repudiation | Glossary format shift undocumented | mitigate | TestGlossaryEntries.test_format_shift_footer_note_present enforces a footer note explaining EN-first shift. Operators reading older CN-first entries alongside v6 EN-first entries see the rationale. |
| T-33-15 | Information Disclosure | Glossary entries leak operator feedback text | accept | Entries describe terminology abstractly with REQUIREMENTS/ROADMAP citations, not live feedback content. No PII surface. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages — pure markdown edits. No supply-chain surface. |
</threat_model>

<verification>
Phase-wide final verification (run after this plan + Plan 01 + Plan 02 all complete):
- Full test suite: `python -m pytest tests/hermes_cli/test_curator_stats.py -x` all green
- Ruff: `ruff check hermes_cli/curator.py tests/hermes_cli/test_curator_stats.py` clean
- SC-7: `git diff v5.0..HEAD -- skills/movie-experts/ | grep -v _eval | grep -v _shared | wc -l` returns 0
- SC-8: `git diff v5.0..HEAD -- skills/movie-experts/_shared/{snowflake-method,e-konte-format,scamper-variations,dreamina-cli-baseline,v86-pipeline-mapping}.md | wc -l` returns 0
- FOUND-08: `git diff --name-only v5.0..HEAD -- skills/movie-experts/ | grep -v _eval | grep -v _shared | grep -v README.md | grep -v _shared/glossary.md | wc -l` returns 0 (only doc additions permitted)
- Runtime isolation (carry-forward from P31/P32): `grep -c "^from agent\.evolution\|^import agent\.evolution" hermes_cli/curator.py` returns 0
</verification>

<success_criteria>
- SC-6 satisfied: README corpus tree lists v6 ref; glossary has 4 new H3 bilingual EN-first entries with cross-refs
- SC-7 satisfied: FOUND-08 milestone-wide preservation check passes (0 bundled-SKILL.md / non-_shared changes vs v5.0)
- SC-8 satisfied: v5/v4 refs byte-intact across all of v6.0
- Existing CN-first glossary entries byte-intact
- v5/v4 _shared refs byte-intact
- All tests green; Ruff clean
</success_criteria>

<output>
Create `.planning/phases/33-observability-integration-close-out/33-03-SUMMARY.md` when done
</output>

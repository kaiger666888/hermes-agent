---
phase: 28-feedback-ingestion-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/feedback_schema.py
  - agent/feedback_snapshot.py
  - agent/feedback_ingest.py
  - tests/agent/test_feedback_schema.py
  - tests/agent/test_feedback_snapshot.py
  - tests/agent/test_feedback_ingest.py
  - tests/fixtures/feedback/valid_10.jsonl
  - tests/fixtures/feedback/invalid_verdict.jsonl
  - tests/fixtures/feedback/invalid_skill.jsonl
autonomous: true
requirements: [INGEST-03, INGEST-04]
user_setup: []

must_haves:
  truths:
    - "FeedbackRecord Pydantic model accepts valid records from any of the three sources (cli/kais_aigc/manual) and rejects malformed payloads with field-level errors"
    - "OutputSnapshot builder captures sha256 (deterministic), output_text, prompt, model, provider, api_mode, and JSON-serializable params from live agent + conversation state"
    - "All three sources (cli/kais_aigc/manual) produce identical JSON for the same input — source is the only varying field"
    - "write_feedback_record persists validated records atomically to ~/.hermes/skills/.feedback/incoming/ via utils.atomic_json_write"
    - "No bundled SKILL.md or references/*.md bytes are modified (FOUND-08)"
  artifacts:
    - path: "agent/feedback_schema.py"
      provides: "FeedbackRecord + OutputSnapshot Pydantic models with field validators + auto-discovered _KNOWN_EXPERT_IDS"
      contains: "class FeedbackRecord"
    - path: "agent/feedback_snapshot.py"
      provides: "build_output_snapshot(agent, conversation_history, assistant_idx) -> OutputSnapshot"
      contains: "def build_output_snapshot"
    - path: "agent/feedback_ingest.py"
      provides: "write_feedback_record(record) -> Path (atomic persistence)"
      contains: "def write_feedback_record"
    - path: "tests/agent/test_feedback_schema.py"
      provides: "Pydantic validator tests (accept/reject for verdict, skill_id, ts, sha256, source parity)"
    - path: "tests/agent/test_feedback_snapshot.py"
      provides: "snapshot builder tests (sha256 determinism, param extraction, content shapes)"
    - path: "tests/agent/test_feedback_ingest.py"
      provides: "write path tests (atomicity, filename format, directory creation)"
    - path: "tests/fixtures/feedback/valid_10.jsonl"
      provides: "10 valid JSONL records for batch import cold-start"
  key_links:
    - from: "agent/feedback_snapshot.py"
      to: "agent/feedback_schema.py"
      via: "imports OutputSnapshot"
      pattern: "from agent\\.feedback_schema import"
    - from: "agent/feedback_ingest.py"
      to: "agent/feedback_schema.py"
      via: "imports FeedbackRecord"
      pattern: "from agent\\.feedback_schema import"
    - from: "agent/feedback_ingest.py"
      to: "utils.py:atomic_json_write"
      via: "atomic persistence"
      pattern: "atomic_json_write"
    - from: "agent/feedback_ingest.py"
      to: "hermes_constants.get_hermes_home"
      via: "path resolution (NOT Path.home())"
      pattern: "get_hermes_home"
---

<objective>
Build the foundational schema + snapshot + atomic write layer for the v6.0 feedback ingestion pipeline.

Purpose: This plan ships the shared contract that ALL three feedback sources (CLI `/feedback`, kais-aigc file exchange, manual JSONL import) emit. Without this layer, Plan 02 has nothing to validate against or persist. Splits feedback ingestion work into: (Plan 01 = schema + capture + write primitives; Plan 02 = sources that produce records).

Output: Three new `agent/feedback_*.py` modules + their pytest suites + JSONL fixtures. Zero bundled-skill byte changes. Zero new dependencies (pydantic 2.13.4 already pinned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-feedback-ingestion-mvp/28-CONTEXT.md
@.planning/phases/28-feedback-ingestion-mvp/28-RESEARCH.md
@.planning/phases/28-feedback-ingestion-mvp/28-VALIDATION.md

<interfaces>
<!-- Key codebase contracts the executor needs. Extracted during planning.
     Executor should use these directly — no codebase exploration needed. -->

From `utils.py:111-153` (atomic write — reuse for write_feedback_record):
```python
def atomic_json_write(
    target: Path,
    data: Any,
    *,
    indent: int = 2,
    ensure_ascii: bool = False,
    sort_keys: bool = False,
) -> None:
    """Write JSON to *target* atomically (temp + fsync + os.replace)."""
```

From `hermes_constants.py:53` (path resolution — CLAUDE.md MANDATES this over Path.home()):
```python
def get_hermes_home() -> Path: ...
```

From `skills/movie-experts/_eval/snapshot.py:96-97` (sha256 + ISO 8601 provenance pattern):
```python
sha = hashlib.sha256(raw).hexdigest()         # raw = output_text.encode("utf-8")
ts = datetime.now(timezone.utc).isoformat()
```

From `agent/agent_init.py:486-489` (param attrs the snapshot builder reads via getattr):
```python
agent.max_tokens = ...
agent.reasoning_config = ...
agent.service_tier = ...
agent.request_overrides = dict(request_overrides or {})
```

Existing movie-experts directory list (auto-discovered from skills/movie-experts/*/SKILL.md):
animation_studio, animator, audio_pipeline, character_designer, cinematographer,
colorist, compliance_gate, compliance_marketing, composer, continuity,
continuity_auditor, creative_source, documentary_maker, drawer, editor, foley,
hook_retention, lip_sync, mixer, performer, production, prompt_injector,
scene_builder, screenplay, script_auditor, spatial_audio, storyboard_designer,
style_genome, theory_critic, visual_executor, voicer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeedbackRecord + OutputSnapshot Pydantic schema with validators + auto-discovered expert IDs</name>
  <files>agent/feedback_schema.py, tests/agent/test_feedback_schema.py</files>
  <read_first>
    - .planning/phases/28-feedback-ingestion-mvp/28-RESEARCH.md (Code Examples §"Pydantic v2 Model with Field Validators" — full draft to adapt; Common Pitfalls §3 sha256 determinism)
    - skills/movie-experts/_eval/snapshot.py:40-55,96-97 (EXPERT_DIRS pattern + sha256/ISO 8601 provenance — pattern source)
    - CLAUDE.md §"Code Style" (Ruff PLW1514 encoding rule; double-quote strings; from __future__ import annotations at top of new modules)
    - pyproject.toml:60 (confirm pydantic==2.13.4 pin)
  </read_first>
  <behavior>
    - FeedbackRecord accepts valid record: skill_id="screenplay", expert_id="screenplay", source="cli", verdict="good", correction="x", output_snapshot=<valid>, ts=datetime.now(timezone.utc) → no error
    - FeedbackRecord rejects verdict="excellent" with field-level error mentioning verdict
    - FeedbackRecord rejects skill_id="not_a_skill" with field-level error listing known skills
    - FeedbackRecord rejects ts=datetime(2026,1,1) (naive, no tzinfo) with error mentioning timezone
    - OutputSnapshot rejects sha256="abc" (not 64 hex chars) with error
    - OutputSnapshot accepts sha256 with uppercase hex and normalizes to lowercase
    - test_source_enum_same_schema: construct FeedbackRecord with source="cli", source="kais_aigc", source="manual" — only the source field differs; assert model_dump() is identical modulo source value
    - test_known_expert_ids_includes_v5_additions: _KNOWN_EXPERT_IDS contains "hook_retention", "compliance_gate", "compliance_marketing", "lip_sync", "cinematographer" (verifies auto-discovery caught v3/v4/v5 additions, not just v1 frozen list)
  </behavior>
  <action>
    Create `agent/feedback_schema.py` with `from __future__ import annotations` at the top. Define two Pydantic v2 BaseModel classes.

    Define a module-level `_KNOWN_EXPERT_IDS: frozenset[str]` populated by calling a private `_discover_expert_ids() -> frozenset[str]` function at import time. `_discover_expert_ids` walks `skills/movie-experts/*/SKILL.md`, parses YAML frontmatter (reuse `agent/skill_utils.parse_frontmatter` — read that module first to confirm signature), and collects the `name` field. Cache the result in a module-level `_EXPERT_DISCOVERY_CACHE` so repeated calls are cheap. On ANY exception (missing dir, parse error, running outside repo), fall back to a hardcoded list containing ALL directories currently in skills/movie-experts/ (the 31 names listed in this plan's interfaces block — copy them verbatim) and emit `logger.warning("expert ID auto-discovery failed: %s; using fallback list", exc)`. Per CONTEXT.md D-04 + planner resolution of research open question #4: auto-discovery is preferred over copying snapshot.py's STALE EXPERT_DIRS list.

    Define `OutputSnapshot(BaseModel)` with fields: `sha256: str`, `output_text: str`, `prompt: str`, `model: str`, `provider: str`, `api_mode: str = ""`, `params: dict`, `captured_at: datetime`. Add `@field_validator("sha256")` `@classmethod` `_sha256_is_64_hex(cls, v: str) -> str` that raises `ValueError("sha256 must be 64 lowercase hex characters")` if `len(v) != 64` OR any char is not in `"0123456789abcdef"` (case-insensitive input, return `v.lower()`).

    Define `FeedbackRecord(BaseModel)` with fields: `skill_id: str`, `expert_id: str`, `source: Literal["cli", "kais_aigc", "manual"]`, `verdict: Literal["good", "needs_work", "bad"]`, `correction: str = ""`, `revised_output: str | None = None`, `output_snapshot: OutputSnapshot`, `ts: datetime`. Add `@field_validator("skill_id", "expert_id")` `@classmethod` `_known_expert(cls, v: str) -> str` raising `ValueError(f"skill_id '{v}' is not a known movie-expert. Known: {sorted(_KNOWN_EXPERT_IDS)}")` when not in the set. Add `@field_validator("ts")` `@classmethod` `_ts_has_tz(cls, v: datetime) -> datetime` raising `ValueError("ts must be timezone-aware (ISO 8601 with offset)")` when `v.tzinfo is None`.

    Per D-04 (CONTEXT.md locked): this IS the INGEST-04 validation layer. Use double quotes for strings (CLAUDE.md). Module docstring at top explains: schema is the single contract all three feedback sources (cli/kais_aigc/manual) emit; downstream P29 STORE + P30 GATE + P31 EVOL all consume this shape. Do NOT inline a full implementation of `_discover_expert_ids` in the action — name the function, its inputs/outputs, the fallback policy, and the cache; the executor writes the body.

    Create `tests/agent/test_feedback_schema.py` with one `class TestFeedbackRecordSchema:` containing pytest test methods matching each <behavior> bullet above. Use `pytest.raises(pydantic.ValidationError)` and inspect `exc.errors()[0]["loc"]` and `exc.errors()[0]["msg"]` to assert field-level error messages (INGEST-04 clear-error requirement). Each test constructs the model with one field invalid at a time. For `test_source_enum_same_schema`, build the same record 3x with each source value and assert the three `model_dump(mode="json")` dicts differ ONLY in the `source` key. For `test_known_expert_ids_includes_v5_additions`, import `_KNOWN_EXPERT_IDS` and `assert` the 5 listed names are present (this guards against silently shipping with the stale 14-expert snapshot.py list).
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_schema.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `agent/feedback_schema.py` exists and starts with `from __future__ import annotations`
    - `grep -c "class FeedbackRecord" agent/feedback_schema.py` returns 1
    - `grep -c "class OutputSnapshot" agent/feedback_schema.py` returns 1
    - `grep -c "field_validator" agent/feedback_schema.py` returns at least 3 (sha256 + skill_id/expert_id + ts)
    - `grep -c "_discover_expert_ids" agent/feedback_schema.py` returns at least 2 (definition + call site)
    - `python -m pytest tests/agent/test_feedback_schema.py -x` exits 0 with all test methods passing
    - `python -c "from agent.feedback_schema import FeedbackRecord, OutputSnapshot, _KNOWN_EXPERT_IDS; assert 'hook_retention' in _KNOWN_EXPERT_IDS and 'screenplay' in _KNOWN_EXPERT_IDS; print(len(_KNOWN_EXPERT_IDS))"` prints a number ≥ 28 (current expert count)
    - `ruff check agent/feedback_schema.py` exits 0 (no PLW1514 violations — no open() in this file, but check anyway)
  </acceptance_criteria>
  <done>
    FeedbackRecord + OutputSnapshot Pydantic models exist with field-level validators that reject bad verdict, unknown skill_id, naive datetime, and non-64-hex sha256. _KNOWN_EXPERT_IDS auto-discovers from skills/movie-experts/*/SKILL.md with hardcoded fallback. All schema tests pass. Zero bundled-skill bytes touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_output_snapshot() capture function with sha256 determinism + content-shape defense</name>
  <files>agent/feedback_snapshot.py, tests/agent/test_feedback_snapshot.py</files>
  <read_first>
    - agent/feedback_schema.py (Task 1 output — import OutputSnapshot from here)
    - .planning/phases/28-feedback-ingestion-mvp/28-RESEARCH.md (Code Examples §"Output Snapshot Builder" — full draft to adapt; Pitfalls #3 sha256 determinism, #5 content shapes, #8 non-serializable request_overrides)
    - agent/agent_init.py:486-489 (the agent attributes the builder reads via getattr)
    - agent/message_sanitization.py (skim — Pitfall #3 recommends running sanitization BEFORE hashing for surrogate safety; confirm the public function name to call)
    - CLAUDE.md §"Error Handling" (specific exceptions, bind with `as exc`, lazy %-formatting in logs)
  </read_first>
  <behavior>
    - test_sha256_deterministic: same assistant_text + same agent state → identical sha256 across two calls
    - test_captures_agent_params: builder reads agent.max_tokens=8192, agent.provider="openai", agent.model="gpt-4", agent.api_mode="chat_completions" → resulting OutputSnapshot has model="gpt-4", provider="openai", api_mode="chat_completions", params contains max_tokens=8192
    - test_extract_text_all_shapes: _extract_text("hello") → "hello"; _extract_text([{"type":"text","text":"a"},{"type":"text","text":"b"}]) → "ab"; _extract_text(None) → ""; _extract_text([{"type":"tool_use"}]) → "" (no text parts)
    - test_finds_preceding_user_prompt: conversation_history = [{role:user, content:"hi"}, {role:assistant, content:"yo"}] with assistant_idx=1 → snapshot.prompt == "hi"
    - test_drops_non_serializable_request_overrides: agent.request_overrides = {"temperature": 0.7, "callback": <lambda>} → snapshot.params contains temperature but NOT callback (Pydantic's JSON mode or manual filtering drops it; no TypeError raised)
  </behavior>
  <action>
    Create `agent/feedback_snapshot.py` with `from __future__ import annotations`. Import `hashlib`, `datetime` (`datetime`, `timezone`), `typing.Any`, `logging`, and `from agent.feedback_schema import OutputSnapshot`.

    Define `build_output_snapshot(agent: Any, conversation_history: list[dict[str, Any]], assistant_idx: int) -> OutputSnapshot`. Read `assistant_msg = conversation_history[assistant_idx]` and extract text via `_extract_text(assistant_msg.get("content"))`. Scan backward from `assistant_idx - 1` for the first message with `role == "user"` and extract its text as `prompt_text`. Compute sha via `hashlib.sha256(output_text.encode("utf-8")).hexdigest()` — DOCUMENT in the OutputSnapshot class docstring (Task 1) and in a comment at the hash site that this exact encoding path is the canonical dedup contract for P29/P30.

    Before hashing, optionally apply `agent.message_sanitization.sanitize_text` (read that module first to confirm function name and signature; if it's named differently, use whatever the module exports as the public surrogate-scrubbing function). Per RESEARCH Pitfall #3 — this prevents surrogate-character non-determinism. If the sanitization module's public entrypoint is unclear after reading, hash `output_text.encode("utf-8", errors="surrogatepass")` directly and document that choice.

    Build `params: dict[str, Any]` by iterating over the tuple `("max_tokens", "reasoning_config", "service_tier", "request_overrides")` and calling `getattr(agent, attr, None)`. For `request_overrides` specifically, filter to JSON-serializable types (str/int/float/bool/None/list/dict) — use `pydantic.TypeAdapter(dict).dump_python(val, mode="json")` OR a manual recursive filter; on dropped keys, log `logger.debug("dropped non-serializable request_overrides key: %s", key)` (per RESEARCH Pitfall #8). Construct and return an `OutputSnapshot` instance.

    Define private `_extract_text(content: Any) -> str` handling three shapes per RESEARCH Pitfall #5: (a) `None` → `""`; (b) `str` → return as-is; (c) `list` → iterate dicts, concatenate `p.get("text", "")` for entries where `isinstance(p, dict) and p.get("type") == "text"`; (d) anything else → `str(content)` as last resort. This must NOT mutate the input list.

    Create `tests/agent/test_feedback_snapshot.py` with `class TestOutputSnapshotBuilder:` containing test methods matching each <behavior> bullet. Use a small `FakeAgent` test double (a simple class with the attrs set as instance attributes) — DO NOT import AIAgent. Build conversation_history as a list of plain dicts. For `test_drops_non_serializable_request_overrides`, include a lambda in request_overrides and assert the resulting `snapshot.params["request_overrides"]` either omits the callable key or that the call doesn't raise — assert no `TypeError` escapes the builder.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_snapshot.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `agent/feedback_snapshot.py` exists and starts with `from __future__ import annotations`
    - `grep -c "def build_output_snapshot" agent/feedback_snapshot.py` returns 1
    - `grep -c "def _extract_text" agent/feedback_snapshot.py` returns 1
    - `grep -c "hashlib.sha256" agent/feedback_snapshot.py` returns at least 1
    - `python -m pytest tests/agent/test_feedback_snapshot.py -x` exits 0 with all test methods passing
    - `python -c "from agent.feedback_snapshot import build_output_snapshot; print('ok')"` prints `ok`
    - `ruff check agent/feedback_snapshot.py` exits 0
  </acceptance_criteria>
  <done>
    build_output_snapshot() captures deterministic sha256 + agent params + handles all content shapes (str/list/None) defensively. Non-serializable request_overrides values are filtered without crashing. All snapshot tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: write_feedback_record() atomic persistence + JSONL fixtures</name>
  <files>agent/feedback_ingest.py, tests/agent/test_feedback_ingest.py, tests/fixtures/feedback/valid_10.jsonl, tests/fixtures/feedback/invalid_verdict.jsonl, tests/fixtures/feedback/invalid_skill.jsonl</files>
  <read_first>
    - agent/feedback_schema.py (Task 1 — import FeedbackRecord from here)
    - agent/feedback_snapshot.py (Task 2 — OutputSnapshot used in fixtures)
    - .planning/phases/28-feedback-ingestion-mvp/28-RESEARCH.md (Pattern 3 atomic write; Pattern 4 watcher skeleton — Task 3 implements ONLY write_feedback_record, the watcher + JSONL import land in Plan 02)
    - utils.py:111-153 (atomic_json_write signature — the EXACT function to call)
    - hermes_constants.py (get_hermes_home — the function to call, NOT Path.home())
    - tests/agent/test_curator_reports.py:16-33 (curator_env fixture pattern for HERMES_HOME isolation — the template for the test fixture)
    - tests/conftest.py:57-73,496-560 (_CREDENTIAL_SUFFIXES, _live_system_guard autouse fixture — test hermeticity invariants)
  </read_first>
  <behavior>
    - test_write_creates_file_under_hermes_home: call write_feedback_record(record) → file appears under `<tmp_hermes_home>/skills/.feedback/incoming/`
    - test_filename_format: filename matches `{skill_id}_{source}_{ts_compact}.json` where ts_compact is ISO-ish sortable (e.g. `screenplay_cli_20260624T120000000000Z.json`)
    - test_directory_created_lazily: starting from a clean HERMES_HOME with no `.feedback/` tree, write_feedback_record creates the directory chain (no FileNotFoundError)
    - test_atomic_write_no_partial_on_crash: this is hard to test directly — instead assert the implementation calls `atomic_json_write` (mock or inspect source). Alternative: write a record, verify the file content is valid JSON matching `record.model_dump(mode="json")`, and verify there are no `.tmp` files left in the directory
    - test_round_trip_jsonl_compat: write a record, read the file back with `json.loads`, reconstruct FeedbackRecord(**parsed) → equals original record
    - test_rejects_when_hermes_home_unwritable: skip on Windows; on POSIX, chmod the parent dir 0500 and assert a clear OSError is raised (not a silent failure)
    - test_import_jsonl_atomic_reject (fixture-driven): this test will live in Plan 02 — but the fixtures created here (invalid_verdict.jsonl, invalid_skill.jsonl) are referenced by it, so the fixtures must be valid JSONL where each line is JSON-parseable but fails Pydantic validation in the documented way
  </behavior>
  <action>
    Create `agent/feedback_ingest.py` with `from __future__ import annotations`. Import `json`, `logging`, `from pathlib import Path`, `from datetime import datetime`, `from agent.feedback_schema import FeedbackRecord`, `from utils import atomic_json_write`, `from hermes_constants import get_hermes_home`.

    Define `def write_feedback_record(record: FeedbackRecord) -> Path:`. Compute `target_dir = get_hermes_home() / "skills" / ".feedback" / "incoming"`. Call `target_dir.mkdir(parents=True, exist_ok=True)`. Compute `ts_compact = record.ts.strftime("%Y%m%dT%H%M%S%fZ")`. Compute `target = target_dir / f"{record.skill_id}_{record.source}_{ts_compact}.json"`. Call `atomic_json_write(target, record.model_dump(mode="json"), indent=2, ensure_ascii=False)`. Return `target`. This signature is the single persistence entrypoint — Plan 02's watcher and JSONL importer both call this; do not introduce alternative write paths.

    Per RESEARCH Pattern 3 (file:line cited): the filename pattern `{skill_id}_{source}_{ts_compact}.json` is sortable + collision-resistant (microsecond precision + source disambiguation). DO NOT use `Path.home() / ".hermes"` — RESEARCH Anti-Patterns explicitly forbids this.

    Create `tests/agent/test_feedback_ingest.py` with `class TestWriteFeedbackRecord:`. Add a `feedback_env` pytest fixture modeled on `tests/agent/test_curator_reports.py:16-33 curator_env` — monkeypatch `HERMES_HOME` to a `tmp_path`, reload `hermes_constants` and `agent.feedback_ingest` via `importlib.reload` so the hermes_home cache picks up the new path. Each test constructs a minimal valid `FeedbackRecord` (use a helper `_make_record()` that returns a record with `skill_id="screenplay"`, `source="cli"`, `verdict="good"`, `correction="x"`, a dummy `OutputSnapshot` with sha256 of 64 zeros, `ts=datetime.now(timezone.utc)`) and calls `write_feedback_record`. List the directory after the call and assert exactly one file matches the expected name pattern.

    Create `tests/fixtures/feedback/valid_10.jsonl` — 10 lines, each a JSON object representing a valid FeedbackRecord. Vary skill_id across a few experts (screenplay, editor, colorist, ...) and verdict across the 3 values. Each line must include a complete `output_snapshot` object with a valid 64-char hex sha256 (use 64 zeros or repeated hex patterns) and ISO 8601 timezone-aware `ts`. This file is the cold-start fixture referenced by Plan 02's `test_import_jsonl_cold_start_10` (INGEST-05).

    Create `tests/fixtures/feedback/invalid_verdict.jsonl` — 3 lines: line 1 valid, line 2 with `verdict: "excellent"` (invalid enum), line 3 valid. Tests that the importer (Plan 02) rejects the whole batch and reports line 2 as the error. Each line individually must be JSON-parseable (the JSON parser succeeds; only Pydantic validation fails).

    Create `tests/fixtures/feedback/invalid_skill.jsonl` — 3 lines: line 1 valid, line 2 with `skill_id: "not_a_real_skill"`, line 3 valid. Same atomic-reject pattern as above.
  </action>
  <verify>
    <automated>cd /data/workspace/hermes-agent && python -m pytest tests/agent/test_feedback_ingest.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `agent/feedback_ingest.py` exists and starts with `from __future__ import annotations`
    - `grep -c "def write_feedback_record" agent/feedback_ingest.py` returns 1
    - `grep -c "atomic_json_write" agent/feedback_ingest.py` returns at least 1
    - `grep -c "get_hermes_home" agent/feedback_ingest.py` returns at least 1
    - `grep -v '^ *#' agent/feedback_ingest.py | grep -c "Path.home"` returns 0 (anti-pattern forbidden)
    - `python -m pytest tests/agent/test_feedback_ingest.py -x` exits 0
    - `wc -l tests/fixtures/feedback/valid_10.jsonl` returns 10
    - `wc -l tests/fixtures/feedback/invalid_verdict.jsonl` returns 3
    - `wc -l tests/fixtures/feedback/invalid_skill.jsonl` returns 3
    - `python -c "import json; [json.loads(l) for l in open('tests/fixtures/feedback/valid_10.jsonl', encoding='utf-8') if l.strip()]"` exits 0 (all lines valid JSON)
    - `ruff check agent/feedback_ingest.py` exits 0
  </acceptance_criteria>
  <done>
    write_feedback_record() atomically persists validated FeedbackRecord instances under ~/.hermes/skills/.feedback/incoming/ with sortable collision-resistant filenames. Three JSONL fixtures exist for cold-start (10 valid) and atomic-reject tests (verdict + skill_id). All write-path tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem → Hermes process | Feedback files written under ~/.hermes/skills/.feedback/incoming/ are read back by future phases (P29 store, P30 gate). Trust assumption: operator-controlled HERMES_HOME is a trusted zone (per PROJECT.md v6 scope). |
| Auto-discovery walk of skills/movie-experts/*/SKILL.md | Reading SKILL.md frontmatter to build _KNOWN_EXPERT_IDS. These files are in-repo (trusted) but parsing happens at import time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | OutputSnapshot.sha256 field | mitigate | Pydantic field_validator enforces 64-char hex; rejects malformed values before persistence. Deterministic encoding path (output_text.encode utf-8) documented in docstring. |
| T-28-02 | Elevation of Privilege | _KNOWN_EXPERT_IDS auto-discovery | mitigate | Auto-discovery reads ONLY in-repo skills/movie-experts/*/SKILL.md at import time. Frontmatter parser is `agent/skill_utils.parse_frontmatter` (existing, hardened). No user-supplied paths are walked. Fallback list is hardcoded. |
| T-28-03 | Information Disclosure | Pydantic ValidationError messages | accept | Error messages may reveal schema field names — acceptable because schema is in the public repo. Helps operators fix their JSON (INGEST-04 clear-error requirement). |
| T-28-04 | Denial of Service | write_feedback_record disk fill | accept | Out of P28 scope (P29 STORE-03 handles time-decay). Operator can manually prune ~/.hermes/skills/.feedback/. Low risk — feedback volume is operator-driven, not externally triggered in P28. |
| T-28-05 | Tampering | _discover_expert_ids fallback list | mitigate | Fallback list is a hardcoded literal in the module — not loaded from any user-writable file. If discovery fails, the warning log surfaces the failure so operator notices. |
</threat_model>

<verification>
Plan 01 verification (run after all 3 tasks complete):

1. **Schema test suite green:** `python -m pytest tests/agent/test_feedback_schema.py -x` exits 0
2. **Snapshot test suite green:** `python -m pytest tests/agent/test_feedback_snapshot.py -x` exits 0
3. **Ingest (write-path) test suite green:** `python -m pytest tests/agent/test_feedback_ingest.py -x` exits 0
4. **Lint clean on all 3 new modules:** `ruff check agent/feedback_schema.py agent/feedback_snapshot.py agent/feedback_ingest.py` exits 0
5. **Auto-discovery sanity check:** `python -c "from agent.feedback_schema import _KNOWN_EXPERT_IDS; assert 'screenplay' in _KNOWN_EXPERT_IDS and 'hook_retention' in _KNOWN_EXPERT_IDS; print(f'{len(_KNOWN_EXPERT_IDS)} experts discovered')"` prints a number ≥ 28
6. **FOUND-08 byte-intact check:** `git diff --name-only v5.0 -- skills/movie-experts/ | grep -v _eval | grep -v _shared` returns empty (no bundled SKILL.md or refs touched)
7. **Round-trip persistence:** In a Python REPL, construct a FeedbackRecord, call write_feedback_record, read the resulting file with `json.loads`, reconstruct FeedbackRecord(**parsed) — assert equality (executor validates this via test_round_trip_jsonl_compat)
</verification>

<success_criteria>
- FeedbackRecord Pydantic model exists with field-level validators for verdict, skill_id, expert_id, ts, sha256 — rejects malformed payloads with clear errors (INGEST-04 partial — full cross-source parity in Plan 02)
- OutputSnapshot auto-captured from agent + conversation state with deterministic sha256 (INGEST-04)
- write_feedback_record atomically persists to ~/.hermes/skills/.feedback/incoming/ via utils.atomic_json_write (foundation for INGEST-01/02/03 in Plan 02)
- _KNOWN_EXPERT_IDS auto-discovers the current 28+ expert IDs from skills/movie-experts/*/SKILL.md with hardcoded fallback
- All 3 new test files pass; 3 JSONL fixtures created for Plan 02 consumption
- Zero bundled SKILL.md or references/*.md bytes changed (FOUND-08)
- Zero new dependencies introduced (pydantic 2.13.4 already pinned)
</success_criteria>

<output>
Create `.planning/phases/28-feedback-ingestion-mvp/28-01-SUMMARY.md` when done.
</output>

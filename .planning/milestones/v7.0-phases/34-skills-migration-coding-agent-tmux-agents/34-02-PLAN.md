---
phase: 34-skills-migration-coding-agent-tmux-agents
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/autonomous-ai-agents/tmux-agents/SKILL.md
autonomous: true
requirements: [SKILL-02, SKILL-03, SKILL-04]
user_setup: []

must_haves:
  truths:
    - "User invoking `/skill tmux-agents` (or natural-language trigger like 'spawn agent' / 'background task') from hermes-agent gets the tmux session lifecycle skill loaded into context"
    - "Skill documents 4 operations: spawn, list, attach, get-results (check) — adapted to hermes invocation patterns"
    - "Skill frontmatter is schema-indistinguishable from existing native hermes skills (claude-code, codex, opencode)"
    - "No openclaw-runtime-specific dependencies remain (no `metadata.clawdbot.requires.*`, no clawdhub install path, no `./skills/tmux-agents/scripts/` runtime invocation that assumes the scripts exist in the hermes tree)"
    - "Operator-runtime scripts (spawn.sh / check.sh / status.sh) are documented in body as available-upstream but NOT migrated as files into the hermes repo"
  artifacts:
    - path: "skills/autonomous-ai-agents/tmux-agents/SKILL.md"
      provides: "tmux session lifecycle management for background coding agents (spawn/list/attach/get-results)"
      contains: "name: tmux-agents"
  key_links:
    - from: "skills/autonomous-ai-agents/tmux-agents/SKILL.md metadata.hermes.related_skills"
      to: "existing 4 autonomous-ai-agents skills + coding-agent"
      via: "YAML list [claude-code, codex, opencode, hermes-agent, coding-agent]"
      pattern: "related_skills:\\s*\\[.*claude-code"
---

<objective>
Migrate the `tmux-agents` skill from `~/.openclaw/skills/openclaw-skills-tmux-agents/SKILL.md` to `skills/autonomous-ai-agents/tmux-agents/SKILL.md` in the hermes-agent repo, adapting frontmatter to native hermes schema, documenting (not migrating) the operator-runtime scripts, and adapting the agent inventory to hermes invocation patterns.

Purpose: Establishes tmux session lifecycle management (spawn / list / attach / get-results) as a discoverable hermes skill that supplements (does not replace) the 4 existing single-agent skills. Addresses SKILL-02 (migration + 4 ops documented), SKILL-03 (frontmatter schema compliance), and SKILL-04 (prerequisite format mapping).

Output: One new SKILL.md at `skills/autonomous-ai-agents/tmux-agents/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md
@/data/workspace/hermes-agent/CLAUDE.md

<interfaces>
<!-- Native hermes SKILL.md frontmatter schema (same as Plan 34-01). -->
<!-- Source tmux-agents uses openclaw's `metadata.clawdbot.requires.bins` (older schema name) — -->
<!-- NOT `metadata.openclaw.requires.anyBins`. Both map to the same hermes equivalent. -->

```yaml
---
name: <skill-name>
description: "<one-line EN description>"
version: <semver>
author: <author>
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [<Capitalized-Hyphenated-Tags>]
    related_skills: [<other-skill-name>, ...]
---
```

Source-specific tokens requiring rewrite:
- `metadata.clawdbot.{emoji,requires.bins,install}` YAML block → drop entirely; `requires.bins: ["tmux"]` → prose under `## Prerequisites` ("**tmux:** installed (apt/brew install tmux)")
- `triggers:` YAML list at top-level (openclaw pattern) → DROP. Hermes skills do not use a `triggers` field; discovery is via description text + tag matching.
- `homepage: https://clawdhub.com/skills/tmux-agents` → DROP (openclaw hub URL; not relevant to hermes). Author moves to "Hermes Agent + clawdhub upstream" credit.
- `./skills/tmux-agents/scripts/spawn.sh <name> <task> [agent]` runtime invocations → these scripts are NOT being migrated (CONTEXT.md deferred decision). Rewrite as either:
  (a) document the script upstream + provide equivalent inline tmux command sequence, OR
  (b) rewrite as hermes-terminal-tool invocation pattern: `terminal(command="tmux new-session -d -s <name> ...", pty=true)`
  Prefer (b) for the primary flow; mention (a) as upstream-option in a footnote.
- "Sessions persist even if Clawdbot restarts" → "Sessions persist even if hermes-agent restarts" (or genericize to "the agent runtime")
- `clawdhub install tmux-agents` install command in README → DROP (not migrating the README; SKILL.md body only)
- Ollama section (local agents via `ollama-claude` / `ollama-codex`): preserve content but cross-reference the `coding-agent` skill for actual delegation; the local-Ollama flow is one of the agent inventory options.

Body conventions: Same as Plan 34-01 (H1 title, `## Prerequisites` prose, `## When to Use` typical, terminal-tool invocation pattern, closing `## Rules` section).
</interfaces>

<decisions_honored>
- **D (Coexistence: SUPPLEMENT)** — tmux-agents supplements (does not replace) the 4 existing single-agent skills. It's the session lifecycle layer; orthogonal to single-agent deep-dives.
- **D (Frontmatter mapping)** — `metadata.clawdbot.requires.bins: ["tmux"]` → prose under `## Prerequisites`. `metadata.clawdbot.install` (brew formula spec) → DROP (openclaw-internal install automation).
- **D (Drop `triggers:`)** — openclaw pattern; hermes uses description text + tags for discovery.
- **D (Scripts NOT migrated)** — `scripts/spawn.sh`, `scripts/check.sh`, `scripts/status.sh` are operator-runtime tools. Per CONTEXT.md deferred decision: document them in SKILL.md body (mention they exist upstream) but DO NOT copy them into the hermes repo. Provide equivalent inline tmux commands using `terminal(command="...")` pattern.
- **D (Drop _meta.json)** — openclaw pattern; SKILL.md only.
- **D (Session naming)** — use `hermes-<task>-<rand4>` for consistency with coding-agent skill (source tmux-agents didn't enforce a naming convention, but for cross-skill consistency apply this in body examples).
</decisions_honored>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tmux-agents SKILL.md with adapted frontmatter and hermes-native invocation patterns</name>
  <files>skills/autonomous-ai-agents/tmux-agents/SKILL.md</files>
  <read_first>
    - /home/kai/.openclaw/skills/openclaw-skills-tmux-agents/SKILL.md (SOURCE — full read, 144 lines)
    - /home/kai/.openclaw/skills/openclaw-skills-tmux-agents/README.md (CONTEXT ONLY — install/hub context, do NOT migrate this file)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/hermes-agent/SKILL.md (REFERENCE — shows tmux spawning pattern at lines ~567-620; useful for invocation syntax)
    - /data/workspace/hermes-agent/skills/autonomous-ai-agents/claude-code/SKILL.md (REFERENCE — frontmatter schema + terminal-tool invocation pattern; first 80 lines sufficient)
    - /data/workspace/hermes-agent/.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-CONTEXT.md (decisions to honor)
    - /data/workspace/hermes-agent/CLAUDE.md (Skill File Conventions)
  </read_first>
  <behavior>
    - Frontmatter parses as valid YAML with name=tmux-agents, description present, version, author, license=MIT, platforms=[linux,macos,windows], metadata.hermes.tags non-empty, metadata.hermes.related_skills including [claude-code, codex, opencode, hermes-agent, coding-agent]
    - No top-level `triggers:` field; no `metadata.clawdbot.*`; no `metadata.openclaw.*`; no `homepage: clawdhub`
    - Body documents 4 operations: spawn / list / attach / get-results with hermes-native terminal-tool invocation
    - `scripts/spawn.sh`/`check.sh`/`status.sh` are MENTIONED in body (upstream artifacts) but the files themselves are NOT created in the hermes repo
    - No clawdhub install command; no "Clawdbot restarts" reference
  </behavior>
  <action>
    Create `skills/autonomous-ai-agents/tmux-agents/SKILL.md` by adapting the source.

    **Frontmatter (exact shape):**
    - `name: tmux-agents`
    - `description:` — keep close to source intent: "Manage background coding agents in tmux sessions. Spawn Claude Code, Codex, Gemini, or other agents; check progress; get results." (Source had this; minor cleanup.)
    - `version: 1.0.0` (first hermes release)
    - `author: "Hermes Agent + clawdhub upstream"` (credit both)
    - `license: MIT`
    - `platforms: [linux, macos, windows]`
    - `metadata.hermes.tags:` — `[Coding-Agent, tmux, Background-Worker, Session-Management, Delegation, Parallel, Ollama]`
    - `metadata.hermes.related_skills:` — `[claude-code, codex, opencode, hermes-agent, coding-agent]` (coding-agent is the sibling migrated in Plan 34-01)
    - DROP: top-level `triggers:` block (openclaw pattern), `homepage:` URL, `metadata.clawdbot.*` block entirely.

    **Body structure (preserve source sectioning, adapt content):**

    1. `# Tmux Agents` — preserve H1
    2. Opening paragraph: "Run coding agents in persistent tmux sessions. They work in the background while you do other things." (keep verbatim — no openclaw strings)

    3. `## Prerequisites` (NEW section at top of body — source had no explicit prereq section, only `metadata.clawdbot.requires.bins`). Convert YAML to prose:
       - `**tmux:** installed (apt install tmux / brew install tmux).`
       - `**At least one coding-agent CLI** for spawning: `claude`, `codex`, `gemini`, or an Ollama-backed variant. See the `claude-code`, `codex`, and `opencode` skills for install + auth details. For unified tmux-backed delegation across Codex/Claude Code/Pi/OpenCode, use the sibling `coding-agent` skill.`
       - `**For local (free) agents:** Ollama installed + a coding model pulled (e.g. \`ollama pull glm-4.7-flash\`).`
       - DROP `metadata.clawdbot.install` brew formula spec.

    4. `## Available Agents` — preserve the two tables (Cloud / Local) verbatim. Add a brief note above tables: "Inventory of agents typically spawned via this skill. For per-agent install + auth deep-dives, invoke the dedicated skill (`claude-code`, `codex`, `opencode`)."

    5. `## Quick Commands` → rename to `## Operations`. This is the core SKILL-02 surface (4 ops). Restructure into 4 subsections matching the requirement (spawn / list / attach / get-results):

       **`### Spawn a new agent session`**
       Source used `./skills/tmux-agents/scripts/spawn.sh <name> <task> [agent]`. Scripts are NOT migrated (per decisions). Provide the **hermes-native inline equivalent** instead:
       ```
       terminal(command="tmux new-session -d -s hermes-<name> -x 200 -y 50 -c /path/to/repo 'claude --dangerously-skip-permissions \"<task>\"'", pty=true)
       ```
       Provide 4 examples (one per Cloud agent type: claude / codex / gemini / ollama-claude) using `terminal(command="...")` wrapping. Use session name format `hermes-<task>-<rand4>` in examples (per session-naming decision). Footnote at end of subsection: "The upstream `scripts/spawn.sh` helper (from the clawdhub source) bundles this same tmux invocation; if you've cloned the upstream repo locally, `./skills/tmux-agents/scripts/spawn.sh <name> <task> [agent]` is equivalent."

       **`### List running sessions`**
       Source used `tmux list-sessions` or `./scripts/status.sh`. Provide:
       ```
       terminal(command="tmux list-sessions", pty=false)
       ```
       Footnote: "Upstream `scripts/status.sh` provides a formatted overview of all agent sessions; not migrated into hermes-agent."

       **`### Check on a session (get results)`**
       Source used `./scripts/check.sh session-name` and `tmux capture-pane`. Provide:
       ```
       terminal(command="tmux capture-pane -t <session-name> -p -S -50", pty=true)
       ```
       This covers the "get-results" operation — capture recent pane output. Footnote: "Upstream `scripts/check.sh <name>` wraps `tmux capture-pane` with formatting; not migrated into hermes-agent."

       **`### Attach to watch live`**
       Source: `tmux attach -t session-name` (detach with Ctrl+B, D). Keep verbatim — no openclaw strings. Add the inline-tmux-command form: `terminal(command="tmux attach -t <session-name>", pty=true)`.

       **`### Other operations`** (preserve source's send-instruction + kill-session blocks, wrapped in `terminal(command="...")`):
       - Send additional instructions: `tmux send-keys -t <session> -l -- "..." && tmux send-keys -t <session> Enter`
       - Kill: `tmux kill-session -t <session>`

    6. `## When to Use Local vs Cloud` — preserve the decision table verbatim (no openclaw strings).

    7. `## Parallel Agents` — preserve concept. Rewrite the parallel-spawn example to use `terminal(command="...")` wrapping with `hermes-backend` / `hermes-frontend` / `hermes-docs` / `hermes-tests` session names. Status check at end → `tmux list-sessions` (drop `./scripts/status.sh` reference).

    8. `## Ollama Setup` — preserve content verbatim (no openclaw strings).

    9. `## Tips` — preserve content. Rewrite one line: "Sessions persist even if Clawdbot restarts" → "Sessions persist even if hermes-agent restarts (tmux is the process owner, not the agent runtime)."

    10. **NEW closing section `## Coexistence with other autonomous-ai-agents skills`** (1 paragraph):
        "This skill manages the **session lifecycle** of background coding agents (spawn / list / attach / get-results). For the unified delegation layer that picks between Codex / Claude Code / Pi / OpenCode with a structured notification block, use the sibling `coding-agent` skill. For single-agent deep-dives (full CLI surface area for one tool), invoke the dedicated skill (`claude-code`, `codex`, `opencode`, or `hermes-agent`) directly."

    **DO NOT** create `scripts/spawn.sh`, `scripts/check.sh`, or `scripts/status.sh` files in the hermes repo. They are mentioned in body footnotes as upstream artifacts only.

    **DO NOT** modify the 4 existing skills in this plan — Plan 34-03 handles bidirectional `related_skills` updates.

    **File hygiene:** UTF-8 encoding; double-quoted YAML description; single trailing newline.
  </action>
  <verify>
    <automated>test -f /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md &amp;&amp; python3 -c "import yaml; d=yaml.safe_load(open('/data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md','r',encoding='utf-8').read().split('---',2)[1]); assert d['name']=='tmux-agents'; assert d['license']=='MIT'; assert 'coding-agent' in d['metadata']['hermes']['related_skills']; assert 'claude-code' in d['metadata']['hermes']['related_skills']; assert 'triggers' not in d; assert 'clawdbot' not in d.get('metadata',{}); print('FRONTMATTER_OK')"</automated>
    <automated>! grep -E "metadata\.clawdbot|metadata\.openclaw|clawdhub\.com|clawdhub install|Clawdbot restarts|^triggers:" /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md</automated>
    <automated>! test -f /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/scripts/spawn.sh</automated>
    <automated>grep -c "^### Spawn a new agent session\|^### List running sessions\|^### Attach to watch live\|^### Check on a session" /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md | grep -q "4"</automated>
    <automated>grep -c "terminal(command=" /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md | grep -q "[1-9]"</automated>
    <automated>grep -c "hermes-backend\|hermes-frontend\|hermes-docs\|hermes-tests\|hermes-<name>\|hermes-<task>" /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md | grep -q "[1-9]"</automated>
    <automated>grep -c "scripts/spawn.sh\|scripts/check.sh\|scripts/status.sh" /data/workspace/hermes-agent/skills/autonomous-ai-agents/tmux-agents/SKILL.md | grep -q "[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `skills/autonomous-ai-agents/tmux-agents/SKILL.md`
    - Frontmatter parses as valid YAML
    - Frontmatter contains: name=tmux-agents, description, version, author, license=MIT, platforms=[linux,macos,windows], metadata.hermes.tags (non-empty), metadata.hermes.related_skills (containing all 5: claude-code, codex, opencode, hermes-agent, coding-agent)
    - NO top-level `triggers:` field in frontmatter
    - NO `metadata.clawdbot.*` or `metadata.openclaw.*` block
    - NO `clawdhub.com` URL, `clawdhub install` command, or "Clawdbot restarts" reference
    - Body has 4 operation subsections (spawn / list / attach / check+get-results) — verified by H3 grep
    - At least one `terminal(command="...")` invocation pattern in body
    - `scripts/spawn.sh` / `check.sh` / `status.sh` are MENTIONED in body footnotes (upstream documentation) but DO NOT EXIST as files in the hermes repo (negative file existence check)
    - Session naming examples use `hermes-` prefix
    - File is valid UTF-8 with single trailing newline
  </acceptance_criteria>
  <done>
    `tmux-agents` skill is fully migrated with native frontmatter, 4 operations documented with hermes-native terminal-tool invocations, scripts documented-but-not-migrated per CONTEXT.md deferred decision. Ready for Plan 34-03 to wire bidirectional related_skills on the existing 4 skills.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill-content → agent-runtime | SKILL.md body injected into LLM context; body contains shell-command examples that the agent may mimic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Tampering | SKILL.md frontmatter YAML | mitigate | yaml.safe_load verify gate in `<verify>` |
| T-34-05 | Elevation of Privilege | tmux session spawn commands in body | accept | Analogous to existing claude-code/codex/opencode skill content; no new privilege surface beyond the category baseline |
| T-34-06 | Information Disclosure | Local Ollama setup section | accept | No secrets in body; only public install commands already present in upstream |

No package installs (markdown-only migration) — supply-chain threats N/A; no T-34-SC entry.
</threat_model>

<verification>
1. `tmux-agents/SKILL.md` exists and parses
2. Frontmatter matches native hermes schema (no `metadata.clawdbot.*`, no top-level `triggers`)
3. 4 operations (spawn/list/attach/get-results) documented
4. Prerequisites in prose form
5. Scripts documented upstream but not migrated as files
6. Session naming uses `hermes-` prefix
</verification>

<success_criteria>
- SKILL-02 (tmux-agents migration + 4 ops documented): file in correct directory; spawn/list/attach/get-results all present with hermes invocation patterns
- SKILL-03 (frontmatter schema compliance): tags[], related_skills[] present
- SKILL-04 (prerequisites mapping): `metadata.clawdbot.requires.bins` → prose `## Prerequisites`
</success_criteria>

<output>
Create `.planning/phases/34-skills-migration-coding-agent-tmux-agents/34-02-SUMMARY.md` when done.
</output>

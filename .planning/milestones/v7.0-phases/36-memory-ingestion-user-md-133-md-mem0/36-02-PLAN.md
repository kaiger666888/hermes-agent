---
phase: 36-memory-ingestion-user-md-133-md-mem0
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - ~/.hermes/memories/USER.md
  - .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md
autonomous: true
requirements: [MEM-01]
user_setup:
  - service: mem0
    why: "Plan 02 only does dry-run validation of batch_ingest.py (no API calls). To complete MEM-02/MEM-03/MEM-04 end-to-end the operator must configure MEM0_API_KEY and run the live commands documented in 36-01-INGESTION-NOTE.md."
    env_vars:
      - name: MEM0_API_KEY
        source: "https://app.mem0.ai → API Keys (paste into ~/.hermes/.env)"
    dashboard_config:
      - task: "Run batch_ingest live: python3 plugins/memory/mem0/scripts/batch_ingest.py"
        location: "Repo root after MEM0_API_KEY is set"
      - task: "Run spot-check live: python3 plugins/memory/mem0/scripts/spot_check.py"
        location: "Repo root after batch_ingest succeeds"

must_haves:
  truths:
    - "~/.hermes/memories/USER.md exists and carries openclaw-origin frontmatter"
    - "USER.md body is byte-for-byte identical to ~/.openclaw/workspace/USER.md body (only a frontmatter block is prepended)"
    - "36-01-INGESTION-NOTE.md documents the 124-file correction (vs ROADMAP's 133) and the 817KB total size"
    - "36-01-INGESTION-NOTE.md documents the MEM0_API_KEY operator-action-required step with exact commands"
    - "batch_ingest.py --dry-run (built in 36-01) successfully enumerates all 124 files when run from this plan's verification step"
    - "USER.md is NOT git-added (operator-state); only INGESTION-NOTE.md is committed"
  artifacts:
    - path: "~/.hermes/memories/USER.md"
      provides: "Kai's migrated personal identity profile for hermes runtime"
      contains: "openclaw-origin"
      operator_state: true
    - path: ".planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md"
      provides: "Audit trail for the migration: 124-file inventory, operator-action steps, expected outcomes"
      contains: "MEM0_API_KEY"
  key_links:
    - from: ".planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md"
      to: "plugins/memory/mem0/scripts/batch_ingest.py"
      via: "documented command the operator runs"
      pattern: "batch_ingest\\.py"
    - from: "~/.hermes/memories/USER.md"
      to: "~/.openclaw/workspace/USER.md"
      via: "byte-for-byte body preservation with prepended frontmatter"
      pattern: "source-path: ~/.openclaw/workspace/USER\\.md"
---

<objective>
Migrate Kai's USER.md from openclaw to hermes (`~/.hermes/memories/USER.md`) with provenance frontmatter, then validate that the batch_ingest.py tool from Plan 36-01 correctly enumerates all 124 source files via a dry-run (which needs no MEM0_API_KEY). Finally author the audit-trail note that Phase 37 verification and Kai will read to complete the live ingestion once MEM0_API_KEY is configured.

Purpose: This plan delivers everything achievable *without* the cloud API key (USER.md migration + dry-run validation of tooling + the operator-action-required doc). Plan 36-01 built the tools; this plan uses them safely and hands the operator a single document with the exact commands to run.

Output: `~/.hermes/memories/USER.md` (operator-state, NOT git-added) + `36-01-INGESTION-NOTE.md` (repo-commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-CONTEXT.md
@.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- USER.md migration contract -->

Source: ~/.openclaw/workspace/USER.md (~1986 bytes, no frontmatter, body starts with `# USER.md - About Your Human`)

Target: ~/.hermes/memories/USER.md

Frontmatter to prepend (exact keys per CONTEXT.md decision 1):

```yaml
---
openclaw-origin: true
migrated-at: 2026-06-25
source-path: ~/.openclaw/workspace/USER.md
---
```

Followed by a single blank line, then the source body verbatim.

<!-- INGESTION-NOTE.md content contract -->

Required sections in 36-01-INGESTION-NOTE.md:
1. **File count correction:** ROADMAP/STATE.md say "133 files / 1.3MB" — actual verified count is **124 files / 837,136 bytes (817KB)**. Per CONTEXT.md.
2. **Tooling built (Plan 36-01):** batch_ingest.py + spot_check.py under plugins/memory/mem0/scripts/.
3. **Operator-action-required (BLOCKER for live ingestion):**
   - `MEM0_API_KEY` is not set in `~/.hermes/.env`
   - `~/.hermes/mem0.json` does not exist
   - Operator steps: (a) `pip install mem0ai` if not installed, (b) obtain api key from https://app.mem0.ai, (c) add `MEM0_API_KEY=...` to `~/.hermes/.env` OR create `~/.hermes/mem0.json` with `{"api_key": "..."}`, (d) run the commands below.
4. **Exact commands:**
   - Live ingest: `python3 plugins/memory/mem0/scripts/batch_ingest.py`
   - Live spot-check: `python3 plugins/memory/mem0/scripts/spot_check.py`
   - Idempotency check (re-run): `python3 plugins/memory/mem0/scripts/batch_ingest.py` — expect `ingested=0 skipped=124 failed=0`
5. **Expected outcomes for SC verification (Phase 37):**
   - SC#1: USER.md frontmatter — `head -6 ~/.hermes/memories/USER.md` shows `openclaw-origin: true`
   - SC#2: 124 entries — `python3 -c "from mem0 import MemoryClient; import json; cfg=json.load(open('$HOME/.hermes/mem0.json')); c=MemoryClient(api_key=cfg['api_key']); print(len(c.get_all(filters={'user_id':'hermes-user'})))"` returns ≥ 124
   - SC#3: spot-check returns ≥1 result for each of the 5 topics (non-empty)
   - SC#4: re-run batch_ingest.py → `ingested=0 skipped=124`
6. **Phase 36 status under current operator config:** Plan 36-01 (tooling) + Plan 36-02 (USER.md + dry-run validation + this note) are COMPLETE. Live ingestion (MEM-02/03/04 final verification) is BLOCKED on MEM0_API_KEY — operator runs the commands above, then Phase 37 verification confirms SC#2-4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ~/.hermes/memories/ dir and migrate USER.md with openclaw-origin frontmatter</name>
  <files>~/.hermes/memories/USER.md</files>
  <read_first>
    - /home/kai/.openclaw/workspace/USER.md (entire file — source body to preserve)
    - .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-CONTEXT.md (decision 1 — exact frontmatter keys)
    - CLAUDE.md "Configuration" section (~/.hermes/ is canonical state root; not git-tracked)
  </read_first>
  <behavior>
    - After this task, `~/.hermes/memories/` directory exists with mode 0755 (default)
    - `~/.hermes/memories/USER.md` exists
    - First 6 lines of `~/.hermes/memories/USER.md` are exactly:
      ```
      ---
      openclaw-origin: true
      migrated-at: 2026-06-25
      source-path: ~/.openclaw/workspace/USER.md
      ---
      ```
      followed by a blank line, then the source body starting with `# USER.md - About Your Human`
    - Body byte-for-byte identical to source: `tail -n +7 ~/.hermes/memories/USER.md | diff - <(tail -n +1 ~/.openclaw/workspace/USER.md)` produces no output
    - File is NOT `git add`-ed (it lives under ~/.hermes/ which is outside the repo)
  </behavior>
  <action>
    1. Create directory: `mkdir -p ~/.hermes/memories` (idempotent).
    2. Read source `~/.openclaw/workspace/USER.md` with `encoding="utf-8"`.
    3. Construct the target content by prepending the exact frontmatter block from the interfaces section, followed by one blank line, followed by the source body verbatim. Do NOT modify any byte of the source body (no trimming, no re-indentation, no newline normalization).
    4. Write the target file with the Write tool (operator-state, not git-tracked) or via Python with `encoding="utf-8"`. Use mode 0644 (default).
    5. Verify with the diff command in <verify>.

    Use Python or the Write tool — not heredoc. The frontmatter uses lowercase kebab-case keys exactly as specified in CONTEXT.md decision 1.
  </action>
  <verify>
    <automated>
      [ -d ~/.hermes/memories ] &&
      [ -f ~/.hermes/memories/USER.md ] &&
      head -5 ~/.hermes/memories/USER.md | grep -q 'openclaw-origin: true' &&
      head -5 ~/.hermes/memories/USER.md | grep -q 'migrated-at: 2026-06-25' &&
      head -5 ~/.hermes/memories/USER.md | grep -q 'source-path: ~/.openclaw/workspace/USER.md' &&
      diff <(tail -n +7 ~/.hermes/memories/USER.md) ~/.openclaw/workspace/USER.md &&
      ! git -C /data/workspace/hermes-agent status --porcelain | grep -q 'hermes/memories/USER.md'
    </automated>
  </verify>
  <done>
    USER.md exists at the hermes target path, frontmatter is present with all 3 required keys, body is byte-identical to openclaw source, file is NOT in git working tree (operator-state).
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate batch_ingest.py via dry-run (no API) and capture the 124-file inventory</name>
  <files>.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md</files>
  <read_first>
    - plugins/memory/mem0/scripts/batch_ingest.py (Plan 36-01 output — to understand the --dry-run summary format)
    - .planning/ROADMAP.md Phase 36 entry (success criteria source of truth — note "133 files" must be corrected to 124 in the note)
    - /home/kai/.openclaw/workspace/memory (run `find ... | wc -l` and `find ... -exec wc -c {} + | tail -1` to confirm inventory numbers)
  </read_first>
  <behavior>
    - `python3 plugins/memory/mem0/scripts/batch_ingest.py --dry-run` runs successfully without MEM0_API_KEY
    - Its stdout includes the summary line "would ingest 124 files"
    - The 124-file inventory (paths + hashes) is captured into the INGESTION-NOTE.md audit trail
    - INGESTION-NOTE.md exists at the path `.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md`
    - The note is git-added and committed
  </behavior>
  <action>
    1. Run the dry-run and capture output:
       ```
       python3 plugins/memory/mem0/scripts/batch_ingest.py --dry-run > /tmp/p36-dryrun.log 2>&1
       ```
       Verify exit 0. The log contains 124 `{path}\t{hash}` lines plus a summary line.

    2. Confirm the inventory numbers independently:
       - `find ~/.openclaw/workspace/memory -name '*.md' | wc -l` → 124
       - `find ~/.openclaw/workspace/memory -name '*.md' -exec wc -c {} + | tail -1` → 837136 total
       If these numbers differ from 124 / 837136, use whatever the commands report (do not hardcode).

    3. Write `.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md` with these sections (use the exact content contract from <interfaces>):

       - `# Phase 36 — Memory Ingestion Operator-Action Note`
       - `## Status` — 3-line summary: tooling built (36-01), USER.md migrated + dry-run validated (36-02), live ingestion BLOCKED on MEM0_API_KEY.
       - `## File Count Correction` — explicit paragraph: ROADMAP.md + STATE.md + REQUIREMENTS.md MEM-02 say "133 files / 1.3MB". Verified actual count via `find ~/.openclaw/workspace/memory -name '*.md' | wc -l` = **124 files**. Verified total content size = **837,136 bytes (817KB)**, not 1.3MB. Per CONTEXT.md gathered 2026-06-25. Phase 37 verification + REQUIREMENTS.md MEM-02 should treat "124 files" as the source of truth; flag for a future doc-consistency patch (NOT in Phase 36 scope).
       - `## Inventory (dry-run output)` — embed the 124 `{path}\t{hash}` lines from `/tmp/p36-dryrun.log` inside a fenced code block. Include the summary line.
       - `## Tooling Built (Plan 36-01)` — brief bullet list of batch_ingest.py + spot_check.py with their --help one-liners.
       - `## Operator Action Required — Live Ingestion` — explicit BLOCKER callout:
         - Prerequisite 1: `pip install mem0ai` (if `python3 -c "import mem0"` fails)
         - Prerequisite 2: Set `MEM0_API_KEY` — either append `MEM0_API_KEY=<key>` to `~/.hermes/.env` OR create `~/.hermes/mem0.json` with content `{"api_key": "<key>", "user_id": "hermes-user", "agent_id": "hermes"}`. Obtain key from https://app.mem0.ai → API Keys.
         - Command 1 (ingest): `python3 plugins/memory/mem0/scripts/batch_ingest.py`
         - Command 2 (spot-check): `python3 plugins/memory/mem0/scripts/spot_check.py`
         - Command 3 (idempotency re-test): re-run Command 1 — expect `ingested=0 skipped=124 failed=0`.
       - `## Expected Outcomes for Phase 37 Verification` — list SC#1-4 with the exact assertion commands (use the commands from <interfaces>).
       - `## Partial-Ingest Decision` — note that no partial-ingest decision has been made; if the operator chooses to ingest only a subset (e.g. most recent N files), document the rationale here before running and update SC#2 expectation accordingly. Default = all 124.
       - `## What Phase 36 Ships Without MEM0_API_KEY` — bullet list: tooling (36-01) + USER.md migration (36-02) + dry-run-validated inventory (36-02). Live MEM-02/03/04 verification deferred to operator.

    4. Stage and commit INGESTION-NOTE.md. Do NOT stage `~/.hermes/memories/USER.md` (it is outside the repo working tree anyway — `git status` from /data/workspace/hermes-agent will not see it).

    Use the Write tool for the note. Use `encoding="utf-8"` if writing via Python.
  </action>
  <verify>
    <automated>
      cd /data/workspace/hermes-agent &&
      python3 plugins/memory/mem0/scripts/batch_ingest.py --dry-run --quiet 2>&1 | grep -q 'would ingest 124' &&
      [ -f .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md ] &&
      grep -q '124 files' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md &&
      grep -q 'MEM0_API_KEY' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md &&
      grep -q 'batch_ingest.py' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md &&
      grep -q 'spot_check.py' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md &&
      grep -qi 'operator action' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md &&
      grep -qi 'partial-ingest' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md
    </automated>
  </verify>
  <done>
    Dry-run successfully enumerates 124 files. INGESTION-NOTE.md exists, commits, contains the file-count correction (124 not 133), the MEM0_API_KEY operator-action section with exact commands, the spot_check.py reference, and the partial-ingest escape hatch. Phase 37 has everything it needs to verify once the operator runs the live commands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openclaw workspace → ~/.hermes/memories | cross-app data migration into hermes state root |
| note author → repo | INGESTION-NOTE.md becomes part of the audit trail read by Phase 37 + future maintainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | USER.md source modified mid-migration | accept | Single-shot read+write within Task 1; no concurrent modification expected. Byte-identity verified by diff in <verify>. |
| T-36-07 | Information disclosure | INGESTION-NOTE.md could leak sensitive memory content | mitigate | Note embeds file PATHS + hashes, not file CONTENTS. Operator running dry-run captures only hashes (which are non-reversible). Do NOT paste sample memory bodies into the note. |
| T-36-08 | Repudiation | "Did ingestion actually happen?" ambiguity | mitigate | INGESTION-NOTE.md status section explicitly states ingestion is BLOCKED on MEM0_API_KEY until operator runs the commands. Phase 37 verification checks `get_all` count directly — no claim-based verification. |
| T-36-09 | Elevation | Writing under ~/.hermes/ could collide with runtime | accept | `~/.hermes/memories/` is a new subdir; nothing in existing hermes runtime reads it (mem0 provider uses cloud backend, not this dir). USER.md is documentation-grade, not executed. |
</threat_model>

<verification>
After both tasks:

```bash
# Task 1 — USER.md migration
[ -f ~/.hermes/memories/USER.md ]
head -5 ~/.hermes/memories/USER.md | grep -q 'openclaw-origin: true'
diff <(tail -n +7 ~/.hermes/memories/USER.md) ~/.openclaw/workspace/USER.md

# Task 2 — dry-run validation + INGESTION-NOTE
cd /data/workspace/hermes-agent
python3 plugins/memory/mem0/scripts/batch_ingest.py --dry-run --quiet 2>&1 | grep -q 'would ingest 124'
[ -f .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md ]
grep -q '124 files' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md
grep -q 'MEM0_API_KEY' .planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-01-INGESTION-NOTE.md

# Git state — only the note is staged, USER.md is not in repo
git status --porcelain | grep '36-01-INGESTION-NOTE.md'
! git status --porcelain | grep -q 'memories/USER.md'
```
</verification>

<success_criteria>
- `~/.hermes/memories/USER.md` exists with the 3 required frontmatter keys and a byte-identical body to the openclaw source
- `~/.hermes/memories/USER.md` is NOT in git working tree (operator-state)
- batch_ingest.py dry-run from Plan 36-01 successfully enumerates exactly 124 files
- INGESTION-NOTE.md is committed and covers: 124-correction, MEM0_API_KEY operator-action, exact live commands, SC#1-4 verification commands, partial-ingest escape hatch
- Operator (Kai) has a single document they can act on to complete MEM-02/03/04 end-to-end
</success_criteria>

<output>
Create `.planning/phases/36-memory-ingestion-user-md-133-md-mem0/36-02-SUMMARY.md` when done. Commit message: `docs(memory): migrate USER.md + ingestion operator-action note (Phase 36-02)`. Commit ONLY `36-01-INGESTION-NOTE.md` — do NOT attempt to git-add `~/.hermes/memories/USER.md`.
</output>

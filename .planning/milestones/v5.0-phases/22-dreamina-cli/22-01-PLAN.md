---
phase: 22-dreamina-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/movie-experts/_shared/dreamina-cli-baseline.md
  - skills/movie-experts/_shared/LICENSE.md
autonomous: true
requirements: [DREAMINA-01, DREAMINA-02, DREAMINA-03, DREAMINA-04, DREAMINA-05]

must_haves:
  truths:
    - "A new ref `_shared/dreamina-cli-baseline.md` exists and documents dreamina CLI as the canonical image/video generation tool per kais-movie-agent V8.5"
    - "All 6 dreamina CLI sub-commands (text2image / image2image / multimodal2video / multiframe2video / frames2video / image_upscale) appear with full command signatures"
    - "The L1/L2/L3/L4 character asset library strategy is documented with the CN canonical terms (L1 身份锚点 / L2 造型卡片 / L3 姿势包 / L4 表情标定)"
    - "The async poll pattern (--poll 0 + dreamina query_result --submit_id + aria2c URL) is documented with the 3-step flow"
    - "The gold-team fallback is documented: image generation does NOT route through gold-team; gold-team is video/TTS/3D only; gold-team image_draw is a DEGRADE path only"
    - "The jimeng-client.js deprecation notice is explicit: marked 废弃 in V8.5, retained in lib/ for compat reference only"
    - "_shared/LICENSE.md has a per-ref attribution row for dreamina-cli-baseline.md with license_status declared"
  artifacts:
    - path: "skills/movie-experts/_shared/dreamina-cli-baseline.md"
      provides: "Cross-expert shared reference for dreamina CLI baseline (commands, L1-L4 strategy, async poll, fallback, deprecation)"
      contains: "verified_date: 2026-06-19"
    - path: "skills/movie-experts/_shared/LICENSE.md"
      provides: "Attribution table for _shared/ refs (including dreamina-cli-baseline.md)"
      contains: "dreamina-cli-baseline.md"
  key_links:
    - from: "_shared/dreamina-cli-baseline.md §6 Commands"
      to: "kais-movie-agent V8.5 SKILL.md §工具映射"
      via: "literal transcription of 6 command signatures"
      pattern: "dreamina (text2image|image2image|multimodal2video|multiframe2video|frames2video|image_upscale)"
    - from: "_shared/dreamina-cli-baseline.md §L1-L4"
      to: "kais-movie-agent V8.5 SKILL.md §L1/L2 双参考角色一致性系统"
      via: "enumeration of 4 layers with API entry mapping"
      pattern: "L1 身份锚点"
    - from: "_shared/dreamina-cli-baseline.md §Async Poll"
      to: "kais-movie-agent V8.5 SKILL.md §dreamina CLI 用法"
      via: "3-step submit→poll→download pattern"
      pattern: "query_result --submit_id"
---

<objective>
Create `skills/movie-experts/_shared/dreamina-cli-baseline.md` — the cross-expert shared reference documenting dreamina CLI as the canonical image/video generation tool per kais-movie-agent V8.5.

Purpose: This ref unblocks downstream V8.6 sync phases — Phase 23 VISUAL-02 (visual_executor dreamina CLI integration) and Phase 25 AUDIO-02 (multimodal2video audio binding) both reference this baseline. Phase 27 INTEGRATION-01 also cites it in the canonical tool registry. Without this baseline, downstream experts would emit deprecated tool recommendations (jimeng-client.js / gold-team image_draw) that break the consumer pipeline.

Output: A single new `_shared/` ref (~300-500 lines, matching v4.0 ref sizes) + a new `_shared/LICENSE.md` governing all `_shared/` refs with the dreamina-cli-baseline.md attribution row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dreamina-cli/22-CONTEXT.md

# Source-of-truth (read_first)
@/data/workspace/kais-movie-agent/SKILL.md

# Existing _shared convention
@skills/movie-experts/_shared/glossary.md
@skills/movie-experts/_shared/SKILL-LAYOUT.md

# v4.0 ref exemplars (structural template)
@skills/movie-experts/style_genome/references/scamper-variations.md

# Project skill conventions
@CLAUDE.md

<interfaces>
<!-- Skill file conventions (from CLAUDE.md §"Skill File Conventions"): -->
<!-- - Bilingual: EN YAML structure + metadata (frontmatter) + CN prose/examples in body -->
<!-- - Each ref MUST carry verified_date + LICENSE attribution -->
<!-- - Refs use code blocks for command signatures, tables for parameter mappings -->

<!-- _shared/ ref conventions (from SKILL-LAYOUT.md §"Reference file anatomy"): -->
<!-- Header block format: -->
<!--   # <Topic Title> -->
<!--   **Source:** <book / paper / URL / interview> -->
<!--   **Copyright:** <© Year Holder | Public Domain | Fair Use | Licensed> -->
<!--   **Last-verified:** YYYY-MM-DD -->
<!-- (Plus verified_date: YYYY-MM per v4.0 convention) -->

<!-- LICENSE.md pattern (from style_genome/references/LICENSE.md): -->
<!--   - Top: # LICENSE — <scope> -->
<!--   - Scope: list of governed ref files -->
<!--   - Per-Ref Attribution: numbered H3 sections per ref with Source / Copyright status / Specific notes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write dreamina-cli-baseline.md header + 6 CLI command signatures (DREAMINA-01)</name>
  <files>skills/movie-experts/_shared/dreamina-cli-baseline.md</files>

  <read_first>
    <file>/data/workspace/kais-movie-agent/SKILL.md</file>
    <file>skills/movie-experts/_shared/SKILL-LAYOUT.md</file>
    <file>skills/movie-experts/style_genome/references/scamper-variations.md</file>
    <file>CLAUDE.md</file>
  </read_first>

  <behavior>
    - The new file exists at the exact path skills/movie-experts/_shared/dreamina-cli-baseline.md
    - Header block contains Source / Copyright / Last-verified: 2026-06-19 + verified_date: 2026-06
    - A "## Summary" section explains dreamina CLI as the canonical image/video generation tool per kais-movie-agent V8.5
    - A "## The 6 dreamina CLI Sub-Commands" section documents all 6 commands with full signatures
    - Each command signature is transcribed LITERALLY from kais-movie-agent V8.5 SKILL.md §工具映射 (no paraphrasing of the CLI flags)
    - text2image, image2image, multimodal2video, multiframe2video, frames2video, image_upscale all present
    - Each command has a brief CN explanation of when each is used (which Step, which scenario)
    - A bilingual note maps each command to its kais-movie-agent V8.6 Step (e.g. image2image → Step 4 L1/L2 character assets; multimodal2video → Step 10 视频模式B; frames2video → Step 10 视频模式C)
  </behavior>

  <action>
    Create the file at `skills/movie-experts/_shared/dreamina-cli-baseline.md`.

    **Header block (mandatory):**
    - H1: `# dreamina CLI Baseline — kais-movie-agent V8.5+ Canonical Image/Video Tool`
    - `**Source:**` kais-movie-agent V8.5 SKILL.md (commit `c22867d`, 2026-06-18) + V8.6 SKILL.md (commit `e41fa68`, 2026-06-18) — both at `/data/workspace/kais-movie-agent/SKILL.md`
    - `**Copyright:**` Fair Use — paraphrased CLI command signatures (command-name + flags are factual API surface, not copyrightable); integration context + L1-L4 strategy are original Hermes Agent analytical encoding layer
    - `**Last-verified:**` 2026-06-19
    - `verified_date: 2026-06`

    **Body section order:**
    1. `## Summary` (2-3 sentences explaining the role of this ref: dreamina CLI is the SOLE image/video generation tool per V8.5; jimeng-client.js is deprecated; gold-team image_draw is degrade-only)
    2. `## V-Version Provenance` (brief: V8.5 commit c22867d introduced dreamina CLI; V8.6 commit e41fa68 kept it as canonical tool)
    3. `## The 6 dreamina CLI Sub-Commands` (the main DREAMINA-01 content)

    **For each of the 6 sub-commands, include:**
    - H3 `### dreamina <name>` (e.g. `### dreamina text2image`)
    - The literal command signature in a bash code block (transcribe EXACTLY from kais-movie-agent V8.5 SKILL.md §工具映射)
    - A bullet list with: 用途 (purpose, CN), 何时使用 (when to use — which V8.6 Step), 关键参数 (key parameters with brief CN explanation)

    **The 6 signatures to transcribe (per kais-movie-agent V8.5 SKILL.md §工具映射):**
    - `dreamina text2image --prompt "..." --model_version 5.0 --ratio 16:9 --resolution_type 2k --poll 0` → Step 4 L1 身份锚点生成 + Step 5 场景俯视图 + Step 7 视觉种子
    - `dreamina image2image --images L1_face.png,L2_costume.png --prompt "..." --model_version 5.0 --ratio 3:4 --resolution_type 2k --poll 0` → Step 4 L2 造型卡片 + L3 姿势包 + L4 表情标定 + Step 5 场景侧面变体
    - `dreamina multimodal2video --image L1_01.png --image L1_02.png --image scene.png --prompt "@Image1 provides identity..." --model_version seedance2.0fast --duration 5 --ratio 16:9 --poll 0` → Step 10 视频模式B (全能参考,最强一致性)
    - `dreamina multiframe2video --images frame1.png,frame2.png,frame3.png --transition-prompt "A to B" --transition-prompt "B to C" --poll 0` → Step 10 视频模式A (多帧故事视频)
    - `dreamina frames2video --first ./start.png --last ./end.png --prompt "..." --model_version seedance2.0fast --duration 5 --poll 0` → Step 10 视频模式C (首尾帧)
    - `dreamina image_upscale --image ./photo.png --resolution_type 4k --poll 0` → 任意 Step 需要超分时

    **Bilingual format:** YAML-style metadata + EN keywords in command names, CN prose in 用途 / 何时使用 / 关键参数 explanations (per CLAUDE.md "Skill File Conventions").

    **Anti-pattern:** Do NOT invent new CLI flags or parameter values. Transcribe the 6 signatures verbatim from the source-of-truth, then add CN explanation prose around them.
  </action>

  <verify>
    <automated>test -f skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -c "dreamina text2image\|dreamina image2image\|dreamina multimodal2video\|dreamina multiframe2video\|dreamina frames2video\|dreamina image_upscale" skills/movie-experts/_shared/dreamina-cli-baseline.md | grep -q "^[6-9]" &amp;&amp; grep -q "verified_date: 2026-06" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "Last-verified:\*\* 2026-06-19\|Last-verified: 2026-06-19" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File exists at skills/movie-experts/_shared/dreamina-cli-baseline.md
    - Header has verified_date: 2026-06 and Last-verified: 2026-06-19
    - All 6 CLI command signatures appear in the file (one each of text2image / image2image / multimodal2video / multiframe2video / frames2video / image_upscale)
    - Each command has a bash code block with the full signature (including --poll 0 flag)
    - Source citation includes kais-movie-agent V8.5 commit c22867d
    - No CLI flags invented that are not in the source-of-truth
    - DREAMINA-01 fully addressed
  </acceptance_criteria>

  <done>
    dreamina-cli-baseline.md header + Summary + 6 CLI command sections all written; DREAMINA-01 acceptance criteria met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append L1-L4 asset strategy + async poll pattern + fallback + deprecation sections (DREAMINA-02..05)</name>
  <files>skills/movie-experts/_shared/dreamina-cli-baseline.md</files>

  <read_first>
    <file>skills/movie-experts/_shared/dreamina-cli-baseline.md</file>
    <file>/data/workspace/kais-movie-agent/SKILL.md</file>
    <file>skills/movie-experts/_shared/glossary.md</file>
  </read_first>

  <behavior>
    - The file contains a "## L1/L2/L3/L4 Character Asset Library Strategy" section documenting all 4 layers
    - The 4 layers use the exact CN canonical terms: L1 身份锚点 / L2 造型卡片 / L3 姿势包 / L4 表情标定
    - A "核心原则" subsection states: 角色参考只传脸，智能参考传衣服/姿势. 不要混放!
    - L1 maps to 角色参考 (Character Ref) API entry; L2/L3/L4 map to 智能参考 (Smart Ref) API entry
    - The file contains an "## Async Poll Pattern" section with the 3-step submit→poll→download flow
    - The 3-step flow includes: (1) `--poll 0` submit returns submit_id, (2) `dreamina query_result --submit_id ID` poll, (3) `aria2c URL` download
    - The file contains a "## Gold-Team Fallback Path" section explicitly stating image generation does NOT route through gold-team
    - Gold-team is documented as video/TTS/3D only; gold-team image_draw is a DEGRADE path (即梦限流/超时降级)
    - The file contains a "## jimeng-client.js Deprecation Notice" section marking it 废弃 per V8.5
    - The deprecation notice explains: retained in lib/ for compat reference only; do NOT use for new code
    - All 5 DREAMINA requirements traceable in the file body
  </behavior>

  <action>
    Append (or insert after the §"6 CLI Sub-Commands" section, before any closing section) these 4 sections to the existing `skills/movie-experts/_shared/dreamina-cli-baseline.md`:

    **Section 1: `## L1/L2/L3/L4 Character Asset Library Strategy`** (DREAMINA-02)
    - Intro paragraph citing V8.5 §"L1/L2 双参考角色一致性系统" + V8.5 §"Step 7 角色资产库完整化"
    - A 4-row table with columns: 层级 (Level) / 名称 (Name) / 内容 (Content) / API入口 (API Entry) / 用途 (Use)
      - Row L1: `L1 / 身份锚点 / 1-3张面部/半身特写 / 角色参考 (Character Ref) / 锁定五官/骨相/发型/肤色,**永不更换**`
      - Row L2: `L2 / 造型卡片 / 每套服装全身正面+侧面 / 智能参考 (Smart Ref) / 锁定服装/道具/造型`
      - Row L3: `L3 / 姿势包 / 坐/站/走/跑等姿态 / 智能参考 (Smart Ref) / 动作参考`
      - Row L4: `L4 / 表情标定 / 微笑/怒/惊/泪 / 智能参考 (Smart Ref) / 表情戏时使用`
    - `### 核心原则 (Core Principles)` subsection with bullets:
      - 角色参考只传脸，智能参考传衣服/姿势. 不要混放!
      - prompt 零面部描述 — 面部特征通过参考图传递，prompt 只写动作/场景/镜头
      - 一造型一卡片，不混放多套服装
    - `### 参考图黄金标准 (Golden Standard for Reference Images)` subsection: 光线柔和均匀 / 正面微侧<30° / 中性表情 / 浅灰背景(#D3D3D3) / 高清无滤镜 / 无遮挡
    - `### V8.6 Step Mapping` subsection: L1 → Step 4A (text2image ×6 选1, 黄金标准检测, 不合格重生≤3轮) → Step 4B 审核通过注册 CharacterAssetManager; L2 → Step 4C (image2image with L1 reference, 正面+侧面各3变体=36张); L3/L4 → Step 4E/4F (按需)

    **Section 2: `## Async Poll Pattern`** (DREAMINA-03)
    - Intro paragraph citing V8.5 SKILL.md §"图片生成默认引擎" §"dreamina CLI 用法"
    - `### 3-Step Flow` subsection with a numbered list:
      1. **提交 (Submit):** `dreamina <command> ... --poll 0` — the `--poll 0` flag means "do NOT block wait"; returns a `submit_id` immediately
      2. **轮询 (Poll):** `dreamina query_result --submit_id <ID>` — call repeatedly until status returns the final asset URL
      3. **下载 (Download):** `aria2c <URL>` — multi-threaded download of the final image/video
    - A bash code block showing the full 3-step pattern (use image2image as example, with placeholder variables for clarity):
      ```bash
      # Step 1: Submit (does not block)
      RESULT=$(dreamina image2image --images L1.png,L2.png --prompt "..." --model_version 5.0 --ratio 3:4 --resolution_type 2k --poll 0)
      SUBMIT_ID=$(echo "$RESULT" | parse_submit_id)
      # Step 2: Poll until ready
      IMAGE_URL=$(dreamina query_result --submit_id "$SUBMIT_ID" | jq -r '.image_url')
      # Step 3: Download
      aria2c "$IMAGE_URL" -o output.png
      ```
    - `### 积分查询 (Credit Query)` note: `dreamina user_credit` queries current credit balance (use for budget awareness, but kais-movie-agent V8 principle #4 says "积分不设限 — 质量优先")

    **Section 3: `## Gold-Team Fallback Path`** (DREAMINA-04)
    - Intro paragraph citing V8.5 SKILL.md §"图片生成默认引擎" + §工具映射 §"GPU 渲染"
    - A two-column table clarifying tool responsibility:
      | 任务类型 | 工具 | 何时 |
      |---------|------|------|
      | 文生图 / 图生图 / 超分 (image generation) | dreamina CLI | DEFAULT — image gen does NOT route through gold-team |
      | 视频生成 (video) | dreamina CLI multimodal2video/multiframe2video/frames2video | DEFAULT |
      | TTS 语音合成 | gold-team `:8002/api/v1/tasks` (type: tts) | DEFAULT |
      | 3D (image_to_3d) | gold-team `:8002/api/v1/tasks` (type: image_to_3d) | DEFAULT |
      | 即梦限流/超时降级 (image degrade) | gold-team `:8002/api/v1/tasks` (type: image_draw, engine: comfyui-local/cloud-jimeng) | DEGRADE ONLY |
    - Explicit load-bearing statement in CN: "图片生成默认走 dreamina CLI,不走 gold-team。gold-team image_draw 是即梦限流/超时时的降级路径,不是主路径。"
    - A `### Hermes Expert Guidance` subsection: When a movie-expert (visual_executor drawer sub-step / cinematographer / character_designer) emits prompt for image generation, the prompt MUST route through dreamina CLI (not gold-team image_draw). The expert should NOT recommend gold-team image_draw as a primary path.

    **Section 4: `## jimeng-client.js Deprecation Notice`** (DREAMINA-05)
    - Intro paragraph citing V8.5 SKILL.md §"V8.5 更新" §5 + §"关键文件" (jimeng-client.js line)
    - A `> ⚠️ DEPRECATED` blockquote at the top stating: "jimeng-client.js 在 kais-movie-agent V8.5 标记为 **@deprecated / 废弃**,已被 dreamina CLI 完全取代。lib/jimeng-client.js 文件保留仅做兼容参考,**不可用于新代码**。"
    - `### 迁移映射 (Migration Mapping)` subsection — a table showing old jimeng-client.js method → new dreamina CLI command:
      | jimeng-client.js (废弃) | dreamina CLI (canonical) |
      |------------------------|---------------------------|
      | jimengClient.text2image(...) | `dreamina text2image --prompt ...` |
      | jimengClient.compositions(refs[], prompt) | `dreamina image2image --images ... --prompt ...` |
      | jimengClient.imageUpscale(...) | `dreamina image_upscale --image ... --resolution_type 4k` |
    - `### Hermes Expert Guidance` subsection: When a movie-expert (visual_executor / prompt_injector / character_designer) emits guidance that references the consumer pipeline, the guidance MUST reference dreamina CLI, NOT jimeng-client.js. Any existing SKILL.md body text mentioning `jimengClient.*` or `jimeng-client.js` is a pre-V8.5 artifact and SHOULD be flagged for V8.6 sync (Phase 23-26).

    Close the file with a `## Refresh Cadence` subsection (quarterly re-verification per _shared convention; drift triggers: new dreamina CLI sub-command added, kais-movie-agent V-number bump, gold-team task-type catalog changes) + a `## See Also` cross-link line to `_shared/glossary.md` (for L1-L4 canonical terms — to be added in Phase 27 INTEGRATION-05) + a `## Source Citation` line reiterating kais-movie-agent V8.5 commit c22867d + V8.6 commit e41fa68.

    **Anti-pattern:** Do NOT mark the L1-L4 / poll / fallback / deprecation sections as optional or "future enhancement". These are required V8.5 baseline knowledge per DREAMINA-02..05.
  </action>

  <verify>
    <automated>grep -q "L1 身份锚点" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "L2 造型卡片" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "L3 姿势包" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "L4 表情标定" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "query_result --submit_id" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "aria2c" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -q "jimeng-client.js" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -qi "废弃" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -qi "gold-team" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; grep -qi "DEGRADE\|降级" skills/movie-experts/_shared/dreamina-cli-baseline.md &amp;&amp; echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File contains all 4 CN canonical L1-L4 terms (身份锚点 / 造型卡片 / 姿势包 / 表情标定)
    - File contains the 核心原则 quote: 角色参考只传脸...不要混放
    - File contains `query_result --submit_id` and `aria2c` in the async poll section
    - File explicitly states image generation does NOT route through gold-team (or equivalent)
    - File marks gold-team image_draw as DEGRADE / 降级 path only
    - File contains "jimeng-client.js" with the term 废弃 / DEPRECATED nearby
    - All 4 DREAMINA-02..05 requirements traceable in body text
    - File ends with Refresh Cadence + See Also + Source Citation per _shared convention
  </acceptance_criteria>

  <done>
    All 4 sections (L1-L4 strategy / async poll / gold-team fallback / jimeng-client deprecation) appended with verbatim source citations; DREAMINA-02..05 acceptance criteria met.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create _shared/LICENSE.md with dreamina-cli-baseline.md attribution row (project-wide copyright gate)</name>
  <files>skills/movie-experts/_shared/LICENSE.md</files>

  <read_first>
    <file>skills/movie-experts/style_genome/references/LICENSE.md</file>
    <file>skills/movie-experts/_shared/dreamina-cli-baseline.md</file>
    <file>skills/movie-experts/_shared/SKILL-LAYOUT.md</file>
    <file>CLAUDE.md</file>
  </read_first>

  <behavior>
    - A new file `_shared/LICENSE.md` exists
    - The LICENSE scope declares it governs all `_shared/` refs (including dreamina-cli-baseline.md)
    - The file lists dreamina-cli-baseline.md as a governed ref with its Source / Copyright status / Specific notes
    - dreamina-cli-baseline.md's license_status is declared (Fair Use — CLI signatures are factual API surface; L1-L4 strategy + integration context are original Hermes analytical encoding)
    - Source citation in the LICENSE row traces to kais-movie-agent V8.5 commit c22867d + V8.6 commit e41fa68
    - verified_date: 2026-06 stamp appears in the LICENSE header
  </behavior>

  <action>
    Create `skills/movie-experts/_shared/LICENSE.md` (this is a NEW file — `_shared/` previously had no LICENSE.md, so we establish the convention here, mirroring the per-expert `references/LICENSE.md` pattern from `style_genome/references/LICENSE.md`).

    **File structure (mirror style_genome/references/LICENSE.md format):**

    - H1: `# LICENSE — _shared/ cross-expert references`
    - `**Scope:**` paragraph listing governed refs:
      - `dreamina-cli-baseline.md` (Phase 22 / v5.0)
      - (Forward-looking note: `_shared/` will accumulate more refs in Phase 27 — v86-pipeline-mapping.md — and that ref will append its own row here in Phase 27.)
      - Pre-existing _shared refs (glossary.md / known-external-models.yaml / RAG-INVOCATION-PATTERN.md / SKILL-LAYOUT.md / quality-rubric.md / platform-comparison.md / cognitive-resonance-metrics.md) are governed implicitly by virtue of being under _shared/ but do not require individual rows in this LICENSE (they predate this convention; their copyright status is established in their own file headers).
    - `**Last-verified:**` 2026-06-19
    - `verified_date: 2026-06`

    - `## Copyright Status Summary` section: All _shared/ refs are authored under Fair Use or are original Hermes Agent analytical work.

    - `## Per-Ref Attribution` section with H3:

    **`### dreamina-cli-baseline.md` (Phase 22 / v5.0)**
    - **Source:** kais-movie-agent V8.5 SKILL.md (commit `c22867d`, 2026-06-18) + V8.6 SKILL.md (commit `e41fa68`, 2026-06-18), both at `/data/workspace/kais-movie-agent/SKILL.md`. Primary source sections: §"V8.5 更新" + §工具映射 + §"L1/L2 双参考角色一致性系统" + §"图片生成默认引擎".
    - **Copyright status:** Fair Use — (1) dreamina CLI command signatures (command name + CLI flags + parameter values) are factual API surface of an external tool, not copyrightable expression; (2) the L1/L2/L3/L4 asset library strategy is an original Hermes Agent analytical encoding layer (4-tier classification, layer → API entry mapping, golden-standard heuristics) — not reproduced from any single kais-movie-agent copyrighted document; (3) the async poll pattern + gold-team fallback path are factual integration descriptions of open-source kais-movie-agent pipeline behavior; (4) the jimeng-client.js deprecation notice references the upstream project's own @deprecated annotation. No verbatim reproduction of kais-movie-agent prose beyond brief factual API signatures and the explicitly-attributed V8.5 update notes (≤ 50 words total, fair-use citation context).
    - **Specific notes:** "dreamina" CLI name and the 6 sub-command names (text2image / image2image / multimodal2video / multiframe2video / frames2video / image_upscale) are factual API surface of the consumer-side tool. "jimeng-client.js" deprecation status is the upstream project's own annotation. L1 身份锚点 / L2 造型卡片 / L3 姿势包 / L4 表情标定 CN terms are factual classification labels of the upstream asset-library convention.
    - **license_status: fair_use_paraphrase** (mirrors v4.0 INTEGRATION-04 pattern; to be re-declared in Phase 27 skills-mapping.yaml v5_ref_signoffs section).

    - `## Author Warrant` section: The L1-L4 4-tier classification + golden-standard heuristics + Hermes expert guidance subsections are original analytical work by Hermes Agent. The dreamina CLI command signatures + flag values are factual API surface transcribed from the upstream open-source kais-movie-agent project (commit c22867d / e41fa68, MIT-compatible open-source license per the project's public declaration).

    - `## Refresh Cadence` section: Quarterly re-verification. Drift triggers for dreamina-cli-baseline.md: (1) new dreamina CLI sub-command added, (2) kais-movie-agent V-number bump, (3) gold-team task-type catalog changes, (4) jimeng-client.js fully removed from kais-movie-agent lib/.

    - `## Out of Scope` section: This LICENSE governs ONLY the per-ref attribution + copyright status for _shared/ refs. It does NOT govern: (a) pre-existing _shared refs (glossary.md etc.) which carry their own header copyright; (b) per-expert `references/LICENSE.md` files (those are governed per-expert); (c) the LICENSE of the upstream kais-movie-agent project itself (which is its own copyright concern).
  </action>

  <verify>
    <automated>test -f skills/movie-experts/_shared/LICENSE.md &amp;&amp; grep -q "dreamina-cli-baseline.md" skills/movie-experts/_shared/LICENSE.md &amp;&amp; grep -qi "fair_use_paraphrase\|Fair Use" skills/movie-experts/_shared/LICENSE.md &amp;&amp; grep -q "c22867d" skills/movie-experts/_shared/LICENSE.md &amp;&amp; grep -q "verified_date: 2026-06" skills/movie-experts/_shared/LICENSE.md &amp;&amp; echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File exists at skills/movie-experts/_shared/LICENSE.md
    - Header scope explicitly governs _shared/ refs including dreamina-cli-baseline.md
    - Has a numbered H3 attribution row for dreamina-cli-baseline.md
    - License_status is declared (fair_use_paraphrase or Fair Use)
    - Source citation includes kais-movie-agent V8.5 commit c22867d
    - verified_date: 2026-06 stamp present
    - Refresh Cadence + Out of Scope sections present
  </acceptance_criteria>

  <done>
    _shared/LICENSE.md created with dreamina-cli-baseline.md attribution row; Phase 22 SC #6 (LICENSE.md attribution) satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream kais-movie-agent → hermes-agent _shared/ ref | Source-of-truth SKILL.md crosses into hermes-agent knowledge layer; transcription accuracy is the boundary concern |
| _shared/dreamina-cli-baseline.md → downstream Phase 23/25/27 experts | Ref content is consumed by 16 active movie-experts via RAG; factual accuracy of CLI signatures propagates to expert prompt outputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | CLI command signatures in dreamina-cli-baseline.md | mitigate | Transcribe signatures LITERALLY from kais-movie-agent V8.5 SKILL.md §工具映射; no flag invention; acceptance criteria grep-checks for all 6 command names |
| T-22-02 | Information Disclosure | LICENSE attribution + source citation | mitigate | _shared/LICENSE.md explicitly cites kais-movie-agent V8.5 commit c22867d + V8.6 commit e41fa68 + declares license_status: fair_use_paraphrase (mirrors v4.0 INTEGRATION-04 pattern) |
| T-22-03 | Repudiation | verified_date stamp | mitigate | Both files carry verified_date: 2026-06 + Last-verified: 2026-06-19 for audit traceability |
| T-22-04 | Elevation of Privilege | FOUND-08 frozen rule (no new expert_id, no DAG change) | accept | Phase 22 is pure _shared/ ref creation — by design creates no expert_id directory, touches no DAG node; no rename involved. Low risk; verify via final grep that no SKILL.md under skills/movie-experts/ was modified. |
| T-22-05 | Spoofing | gold-team / jimeng-client factual accuracy | mitigate | Gold-team role (video/TTS/3D only) + jimeng-client.js 废弃 status transcribed directly from V8.5 SKILL.md §"图片生成默认引擎" + §"关键文件"; no interpretation |
| T-22-SC | Tampering | (no package installs in this phase — pure markdown authoring) | accept | N/A — Phase 22 creates no package_manager installs; slopcheck gate not applicable |
</threat_model>

<verification>
**Phase 22 success criteria (from ROADMAP §"Phase 22 Success Criteria"):**
1. ✅ `_shared/dreamina-cli-baseline.md` exists with `verified_date: 2026-06-19` documenting all 6 dreamina CLI sub-commands per V8.5 — verified by Task 1 <verify> grep
2. ✅ L1/L2/L3/L4 character asset library strategy documented per V8.5 — verified by Task 2 <verify> grep for 身份锚点 / 造型卡片 / 姿势包 / 表情标定
3. ✅ Async poll pattern (--poll 0 + query_result --submit_id + aria2c) documented — verified by Task 2 <verify> grep
4. ✅ Gold-team fallback path documented (image gen does NOT route through gold-team) — verified by Task 2 <verify> grep for DEGRADE / 降级 + gold-team
5. ✅ jimeng-client.js deprecation notice (废弃, retained in lib/ for compat reference) — verified by Task 2 <verify> grep for 废弃 + jimeng-client.js
6. ✅ LICENSE.md attribution row with license_status declared — verified by Task 3 <verify> grep
7. ✅ FOUND-08 preservation: no new expert_id directory created, no DAG node change — verified by files_modified list (only _shared/ files touched, no expert dirs)

**Final post-task grep gate (executor runs after Task 3):**
```bash
test -f skills/movie-experts/_shared/dreamina-cli-baseline.md \
  && test -f skills/movie-experts/_shared/LICENSE.md \
  && grep -c "dreamina text2image\|dreamina image2image\|dreamina multimodal2video\|dreamina multiframe2video\|dreamina frames2video\|dreamina image_upscale" skills/movie-experts/_shared/dreamina-cli-baseline.md | grep -q "^[6-9]" \
  && grep -q "L1 身份锚点\|L2 造型卡片\|L3 姿势包\|L4 表情标定" skills/movie-experts/_shared/dreamina-cli-baseline.md \
  && grep -q "query_result --submit_id" skills/movie-experts/_shared/dreamina-cli-baseline.md \
  && grep -q "废弃\|DEPRECATED" skills/movie-experts/_shared/dreamina-cli-baseline.md \
  && grep -q "fair_use_paraphrase\|Fair Use" skills/movie-experts/_shared/LICENSE.md \
  && echo "PHASE 22 ALL CHECKS PASS"
```

**Anti-regression gate (verify FOUND-08 preservation):**
```bash
# Confirm no SKILL.md under an expert directory was modified (only _shared/ files should be touched)
git diff --name-only HEAD -- 'skills/movie-experts/*/SKILL.md' | wc -l | grep -q "^0$" && echo "FOUND-08 PRESERVED"
```
</verification>

<success_criteria>
**Phase 22 is complete when ALL of the following are TRUE:**

1. `skills/movie-experts/_shared/dreamina-cli-baseline.md` exists as a new file (300-500 lines, matching v4.0 ref size)
2. `skills/movie-experts/_shared/LICENSE.md` exists as a new file with dreamina-cli-baseline.md attribution row
3. All 5 DREAMINA requirements addressed in body text:
   - DREAMINA-01: 6 CLI command signatures transcribed verbatim (Task 1)
   - DREAMINA-02: L1/L2/L3/L4 strategy with CN canonical terms (Task 2)
   - DREAMINA-03: Async poll pattern with 3-step flow (Task 2)
   - DREAMINA-04: Gold-team fallback path with explicit "image gen does NOT route through gold-team" (Task 2)
   - DREAMINA-05: jimeng-client.js 废弃 notice + migration mapping (Task 2)
4. Source citations present (kais-movie-agent V8.5 commit c22867d + V8.6 commit e41fa68)
5. verified_date: 2026-06 stamped in both files
6. LICENSE.md license_status declared as fair_use_paraphrase (mirrors v4.0 pattern)
7. FOUND-08 preserved: zero new expert_id directories, zero SKILL.md body edits, zero DAG changes
8. Final post-task grep gate prints "PHASE 22 ALL CHECKS PASS"
9. Anti-regression grep prints "FOUND-08 PRESERVED"

**Output:** Create `.planning/phases/22-dreamina-cli/22-01-SUMMARY.md` when done.
</success_criteria>

<output>
Create `.planning/phases/22-dreamina-cli/22-01-SUMMARY.md` when done
</output>

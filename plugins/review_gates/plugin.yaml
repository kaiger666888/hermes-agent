name: review_gates
version: "0.2.0"
description: "review gate framework plugin — HIL gate lifecycle (submit/wait/resolve) + 11 gates (8 V8.6 + 3 Phase 40 redline)"
author: kais-movie-agent
kind: standalone
provides_tools:
  - gate_submit
  - gate_wait
  - gate_resolve
  - gates_list

"""pipeline package — kais-movie-pipeline orchestration (Phase 35 skeleton).

Modules:
    runner   — 13-phase sequential loop + checkpoint resume (Phase 35-02).
    phases   — PHASE_REGISTRY (populated by 35-03 p01-p03; extended by Phase 36).

This ``__init__.py`` is intentionally minimal. The skill manifest lives in
``SKILL.md`` (Phase 35-01) at the parent directory.
"""
